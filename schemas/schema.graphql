schema {
	query: Query
}

input AccentsConstraint {
	"""
	A name match must have ALL of these accents.
	No filtering is applied if omitted.
	"""
	allAccents: [AccentType]
	"""
	A name match must have AT LEAST ONE of these accents.
	No filtering is applied if omitted.
	"""
	anyAccents: [AccentType]
	"""
	A name match must not have any of these accents.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeAccents: [AccentType]
}

"""
Accent types for talent
"""
enum AccentType {
	AFRICAN
	ARMENIAN
	ASIAN
	AUSTRALIAN
	BRITISH
	CAJUN
	CANADIAN
	CHINESE
	COCKNEY
	FRENCH
	GERMAN
	IRISH
	ITALIAN
	JAMAICAN
	JAPANESE
	MEXICAN
	MIDDLE_EASTERN
	MIDWEST
	NEW_ENGLAND
	NEW_YORK_BRONX
	NEW_YORK_BROOKLYN
	PUERTO_RICAN
	RUSSIAN
	SCANDINAVIAN
	SCOTTISH
	SOUTHERN
	SOUTH_AFRICAN
	SPANISH
	TEXAN
	WEST_INDIAN
}

enum AccountDataDialogInput {
	"""
	Retrieve the dialog for confirming account data access request
	"""
	CONFIRM
	"""
	Retrieve the dialog for requesting account data access
	"""
	REQUEST
}

type AccountDataDialogOutput {
	"""
	Provides prompt text for either a data access REQUEST or CONFIRM
	"""
	prompt: LocalizedMarkdown!
	"""
	A prompt to instruct Amazon Account Pool customers to initiate the data request process on Amazon page.
	Null if not Amazon Account Pool customers.
	"""
	redirectionPrompt: AccountDataDialogRedirectionPrompt
	"""
	Provides supporting body text for either a data access REQUEST or CONFIRM
	"""
	supportMessage: LocalizedMarkdown!
}

type AccountDataDialogRedirectionPrompt {
	"""
	Link to the Amazon page.
	"""
	action: RedirectLink!
	"""
	Text to instruct customers to initiate the data request process on Amazon page.
	"""
	message: LocalizedMarkdown!
}

"""
Generic type for an action link which has a url and optionally associated label (localized text)
"""
type ActionLink {
	"""
	Optional label (localized text) to associate with URL.
	The label could be used with a button, text link, or other UI component.
	"""
	label: CallToActionText
	"""
	The URL for the action link
	"""
	url: String!
}

"""
A name identifier for a given action link in a CTA. This value is only unique within a list of action links.
It is not globally unique.
"""
scalar ActionLinkName

type AdCreativeInfo {
	"""
	Flag used to communicate if the ad creative contains a video to be autoplayed by the client.
	"""
	hasAutoplay: Boolean!
	"""
	Flag that determines if the app slot is eligible for third party ad content.
	"""
	isEligibleFor3pAd: Boolean!
	"""
	Flag used to communicate if the ad returned is premium.
	"""
	isPremium: Boolean!
	"""
	Flag used to indicate if the client should fit the ad creative to the width of the screen.
	"""
	shouldFitToWidth: Boolean!
	"""
	Metadata including height and width of the returned creative
	"""
	size: CreativeSize!
	"""
	HTML markup for the slot containing the ad creative
	"""
	slotMarkup: String!
}

input AdditionalAuthContext {
	"""
	Additional auth context used for a web sign in request
	"""
	webContext: AdditionalWebAuthContext
}

input AdditionalCreditCategoriesInput {
	"""
	The categories to be excluded from the request.

	If no value is provided, no categories are excluded.
	"""
	excludeCategories: [ID]
	"""
	The categories to be include in the request.

	If no value is provided, all categories are returned.
	"""
	includeCategories: [ID]
}

"""
This is an unverified Resume credit, which Pro customers can create for display on
  their pro name page. These credits are not vetted and are accepted as is.
"""
type AdditionalCreditItem {
	"""
	The name of the category of the credit.
	"""
	category: LocalizedString!
	"""
	The name of the company or director that made the project.
	"""
	details: LocalizedString!
	"""
	Unique identifier for the additional credit object.
	"""
	id: ID!
	"""
	The role or position the individual had on the project.
	"""
	job: LocalizedString!
	"""
	The name or title of the project.
	"""
	title: LocalizedString!
}

"""
A paginated connection for additional credit items, following GraphQL connection specification.
Contains the list of self-verified additional credits that a Pro customer has added to their resume.
"""
type AdditionalCreditItemConnection {
	"""
	A list of edges containing additional credit items and pagination cursors.
	"""
	edges: [AdditionalCreditItemEdge]!
	"""
	Standard GraphQL pagination information including hasNextPage and hasPreviousPage.
	"""
	pageInfo: PageInfo!
	"""
	Total number of credits across all categories.
	"""
	total: Int!
}

type AdditionalCreditItemEdge {
	cursor: ID!
	node: AdditionalCreditItem!
	position: Int!
}

input AdditionalCreditsInput {
	"""
	The categories to be include in the request.

	If no value is provided, all categories are returned.
	"""
	includeCategories: [ID]
}

type AdditionalResumeInfo {
	"""
	The details for an additional resume info section.
	"""
	details: LocalizedString
	"""
	Unique identifier for the additional resume info object.
	"""
	id: ID!
	"""
	The title for the additional resume info section.
	"""
	title: LocalizedString
}

type AdditionalResumeInfoConnection {
	edges: [AdditionalResumeInfoEdge]!
	pageInfo: PageInfo!
	total: Int!
}

type AdditionalResumeInfoEdge {
	cursor: ID!
	node: AdditionalResumeInfo!
	position: Int!
}

input AdditionalWebAuthContext {
	"""
	'Unique browser ID' i.e ubid context identifier, which in practice means an identifier for the domain+browser combination.
	"""
	clientContext: String!
}

"""
A single extended identifier from a CxM identity vendor, following the OpenRTB 2.6 eids spec.
See: https://www.iab.com/wp-content/uploads/2016/03/OpenRTB-API-Specification-Version-2-6.pdf

Supported vendors:
- SharedID / PubCommon: source="pubcid.org", agentType=1
- ID5:                  source="id5-sync.com", agentType=1
- Lotame Panorama ID:   source="crwdcntrl.net", agentType=1
- LiveRamp ATS:         source="liveramp.com", agentType=3
"""
input AdExtendedId {
	"""
	The vendor domain that is the source of the identifier
	(e.g. 'id5-sync.com', 'pubcid.org', 'crwdcntrl.net', 'liveramp.com').
	"""
	source: String!
	"""
	The user IDs from this source. These are different than the standard urXXXX within IMDb, see WebAdExtendedIdUserId.
	There will be one userId per AdExtendedId, but we keep the array data structure to match what's expected in the
	OpenRTB spec: https://www.iab.com/wp-content/uploads/2016/03/OpenRTB-API-Specification-Version-2-6.pdf

	No maximum array size, we will always use the first value in the array downstream

	Example: userIds: [{ id: "AsvkAsvQTfYZNRVUbs0LJ1C5Tti...", agentType: 3 }]
	"""
	userIds: [AdExtendedIdUserId]!
}

"""
A single user ID within an extended identifier.

Example: { id: "e52a-4033-b54c-f67d", agentType: 1 }
"""
input AdExtendedIdUserId {
	"""
	The agent type of the ID per OpenRTB 2.6 atype enum.
	Valid values: 1 (device/cookie-based), 2 (in-app impression-based), 3 (person-based).
	Unrecognized values will be forwarded as-is to downstream buyers.
	We map this to `atype` downstream in IMDbAdsGraphlet -> PAS -> AAX

	OpenRTB/AdCOM standard defines the values for agentType here:
	https://github.com/InteractiveAdvertisingBureau/AdCOM/blob/main/AdCOM%20v1.0%20FINAL.md#list_agenttypes

	Examples: 1 for SharedID/ID5/Lotame, 3 for LiveRamp. Possible values include 1, 2, or 3
	"""
	agentType: Int!
	"""
	The user ID value
	"""
	id: String!
}

"""
An entry to the ad layout slot configuration map containing an ad layout type and a list of supported slots
and their configurations.
"""
type AdLayoutConfiguration {
	"""
	The ad layout type represented as a string.
	"""
	adLayoutType: String!
	"""
	A list of AdSlotConfigurations.
	"""
	adSlotConfigs: [AdSlotConfiguration]!
}

"""
Optional common parameters used to fetch ads.
All params default to null when not present.
"""
input AdParameters {
	"""
	Flag used to communicate whether the ad is for an autostarted video. Impacts slot name and placement type. If omitted, defaults to null and is treated as false.
	"""
	autoStartVideo: Boolean
	"""
	Optional context of the page used for special section targeting - i.e. oscars.bestOf. If omitted, no special section targeting is applied.
	"""
	context: String
	"""
	The page type the query was made from. If omitted, no page type targeting is applied.
	"""
	pageType: String
	"""
	The subpage type the query was made from. If omitted, no subpage type targeting is applied.
	"""
	subPageType: String
	"""
	The user agent of the client. This field is passed to Amazon ad systems via the backend ad requests. If omitted, user agent will not be included in the ad request and video ad URL requests will be blocked.
	"""
	userAgent: String
	"""
	The viewport size of the client's device. If omitted, screen dimensions will not be set on the device info sent to ad systems.
	"""
	viewportSize: ViewPortSize
}

"""
Required and optional parameters used to form the VAES request URL that come from the context of the app client.
Optional params default to null when not present.
"""
input AdParametersApp {
	"""
	Flag used to communicate whether the ad is for an autostarted video. Impacts slot name and placement type. If omitted, defaults to null and is treated as false.
	"""
	autoStartVideo: Boolean
	"""
	Optional context of the page used for special section targeting - i.e. oscars.bestOf. If omitted, no special section targeting is applied.
	"""
	context: String
	"""
	The device id of the client. If omitted, the value will not be included in the ads request.
	"""
	deviceId: String
	"""
	The device info of the client. Used in the backend IMDb advertising systems to determine ad placement related info.
	"""
	deviceInfo: DeviceInfo!
	"""
	The Identifier For Advertising (IDFA) of the user. IDFA is a well-established Apple/industry-standard abbreviation. If omitted, the value will not be included in the ads request.
	"""
	idfa: String
	"""
	The page type the query was made from. If omitted, no page type targeting is applied.
	"""
	pageType: String
	"""
	The subpage type the query was made from. If omitted, no subpage type targeting is applied.
	"""
	subPageType: String
	"""
	The user agent of the client. This field is passed to Amazon ad systems via the backend ad requests. If omitted, user agent will not be included in the ad request and video ad URL requests will be blocked.
	"""
	userAgent: String
	"""
	The viewport size of the client's device. If omitted, screen dimensions will not be set on the device info sent to ad systems.
	"""
	viewportSize: ViewPortSize
}

"""
Optional parameters used to form the VAES request URL that come from the context of the web client.
All params default to null when not present.
"""
input AdParametersWeb {
	"""
	Flag used to communicate whether the ad is for an autostarted video. Impacts slot name and placement type. If omitted, defaults to null and is treated as false.
	"""
	autoStartVideo: Boolean
	"""
	Optional context of the page used for special section targeting - i.e. oscars.bestOf. If omitted, no special section targeting is applied.
	"""
	context: String
	"""
	The page type the query was made from. If omitted, no page type targeting is applied.
	"""
	pageType: String
	"""
	The subpage type the query was made from. If omitted, no subpage type targeting is applied.
	"""
	subPageType: String
	"""
	The user agent of the client. If omitted, user agent will not be included in the ad request and video ad URL requests will be blocked.
	"""
	userAgent: String
	"""
	The viewport size of the client's device. If omitted, screen dimensions will not be set on the device info sent to ad systems.
	"""
	viewportSize: ViewPortSize
}

"""
Attributes of an ad product
"""
type AdProductType {
	"""
	Enumeration of the relative specialness of this AdProductType.
	Values include "standard" and "premium".
	"""
	grade: String!
	"""
	Name of the ad product. Human readable, but not intended for display.
	Values include "premium-page".
	"""
	name: String!
	"""
	A 3-character symbol for this ad product.
	Values include "prm".
	"""
	symbol: String!
}

"""
The IMDb site variant on which the ads are intended to be shown
"""
enum AdSite {
	BOX_OFFICE_MOJO
	CONSUMER
	MOBILE
}

type AdSlot {
	"""
	Ad feedback URL to be linked in the sponsored label of the ad
	"""
	adFeedbackUrl: URL
	"""
	The creative metadata and slotsMarkup returned for the given ad slot
	"""
	creativeInfo: AdCreativeInfo!
	"""
	The name of the ad slot
	"""
	name: String!
}

"""
The configuration for an ad slot.
"""
type AdSlotConfiguration {
	"""
	The APS configuration for the ad slot in this AdSlotConfiguration. The config contain information for each supported APS placement for the slot.
	When this field is missing, the client will not make any 3p ad requests for this slot.
	"""
	apsConfig: ApsConfiguration
	"""
	The IMDb slot name (e.g. inline20).
	"""
	name: String!
	"""
	The width and height of the slot.
	"""
	size: CreativeSize!
}

type AdvancedNameSearchConnection {
	edges: [AdvancedNameSearchEdge]!
	facet(
		facetField: NameFacetField!
		"""
		When true, returns facet counts ignoring constraints for the specified facetField (drill-down behavior).
		When false (default), returns facet counts with all constraints applied (drill-sideways behavior).
		"""
		isDrillDown: Boolean = false
		limit: Int
	): [SearchFacet]
	pageInfo: PageInfo!
	total: Int!
}

input AdvancedNameSearchConstraints {
	"""
	Match names based on accents.
	"""
	accentsConstraint: AccentsConstraint
	"""
	Match names based on age playing range.
	"""
	agePlayingRangeConstraint: AgePlayingRangeConstraint
	"""
	Match names based on athletic skills.
	"""
	athleticSkillsConstraint: AthleticSkillsConstraint
	"""
	Match names based on their awards (Oscar-winner, Oscar-nominated, etc.)
	"""
	awardConstraint: AwardSearchConstraint
	"""
	Match names based on words in their biography, or the biography's author (currently only searches in miniBiography)
	"""
	biographyConstraint: BiographySearchConstraint
	"""
	Match names based on when they were born
	"""
	birthDateConstraint: BirthDateSearchConstraint
	"""
	Match names based on their birth place
	"""
	birthPlaceConstraint: BirthPlaceSearchConstraint
	"""
	Match names based on representation companies.
	"""
	companyRepresentativesConstraint: CompanyRepresentativesConstraint
	"""
	Match names based on dance styles.
	"""
	danceStylesConstraint: DanceStylesConstraint
	"""
	Match names based on when they died
	"""
	deathDateConstraint: DeathDateSearchConstraint
	"""
	Match names based on their death place
	"""
	deathPlaceConstraint: DeathPlaceSearchConstraint
	"""
	Match names based on demographic IDs.
	"""
	demographicIdsConstraint: DemographicIdsConstraint
	"""
	Match names based on employment companies.
	"""
	employmentCompaniesConstraint: EmploymentCompaniesConstraint
	"""
	Match names based on employment job titles.
	"""
	employmentJobTitlesConstraint: EmploymentJobTitlesConstraint
	"""
	Match names based on employment status.
	"""
	employmentStatusConstraint: EmploymentStatusConstraint
	"""
	Match names based on employment work history.
	"""
	employmentWorkHistoryConstraint: EmploymentWorkHistoryConstraint
	"""
	Match names based on the adult flag (explicit), with the default being excluding names flagged as adult
	"""
	explicitContentConstraint: ExplicitContentSearchConstraint
	"""
	Match names based on eye colors.
	"""
	eyeColorsConstraint: EyeColorsConstraint
	"""
	Match names based on filmography box office earnings.
	"""
	filmoBoxOfficeEarningsConstraint: FilmographyBoxOfficeEarningsConstraint
	"""
	Match names based on filmography budget ranges.
	"""
	filmoBudgetConstraint: FilmographyBudgetConstraint
	"""
	Match names based on filmography company credits.
	"""
	filmoCompanyCreditsConstraint: FilmographyCompanyCreditsConstraint
	"""
	Match names based on filmography credited year ranges.
	"""
	filmoCreditYearRangeConstraint: FilmographyCreditYearRangeConstraint
	"""
	Match names based on filmography genres.
	"""
	filmoGenresConstraint: FilmographyGenresConstraint
	"""
	Match names based on filmography keywords.
	"""
	filmoKeywordsConstraint: FilmographyKeywordsConstraint
	"""
	Match names based on filmography name credit category id.
	"""
	filmoNameCreditCategoryConstraint: FilmographyNameCreditCategoryConstraint
	"""
	Match names based on filmography credit occupations.
	Uses fuzzy matching to handle typos and variations.
	"""
	filmoOccupationsConstraint: FilmoOccupationsConstraint
	"""
	Match names based on filmography production status types.
	"""
	filmoProductionStatusConstraint: FilmographyProductionStatusConstraint
	"""
	Match names based on filmography title types.
	"""
	filmoTitleTypeConstraint: FilmographyTitleTypeConstraint
	"""
	Match names based on titles they've been credited in
	"""
	filmographyConstraint: FilmographySearchConstraint
	"""
	Match names based on their gender identity
	"""
	genderIdentityConstraint: GenderIdentitySearchConstraint
	"""
	Match names based on self-verified and verified guild affiliations.
	"""
	guildAffiliationsConstraint: GuildAffiliationsConstraint
	"""
	Match names based on guild status.
	"""
	guildStatusConstraint: GuildStatusConstraint
	"""
	Match names based on hair colors.
	"""
	hairColorsConstraint: HairColorsConstraint
	"""
	Match names based on haircut lengths.
	"""
	hairLengthsConstraint: HairLengthsConstraint
	"""
	Match names based on height.
	"""
	heightConstraint: HeightConstraint
	"""
	Match names based on whether to include deceased individuals.
	When this constraint is provided, deceased names are excluded by default.
	Omitting this constraint entirely has no effect on deceased filtering.

	Auto-enable behavior: When deathDateConstraint, deathPlaceConstraint, or
	withNameDataConstraint (with DEATH_DATE/DEATH_PLACE) are present,
	deceased names are automatically included (unless includeDeceased is explicitly set to false).
	"""
	includeDeceasedConstraint: IncludeDeceasedConstraint
	"""
	Match names based on their presence in lists.

	If any list specified in this constraint is not a list of Names then
	`GRAPHQL_VALIDATION_FAILED` will be thrown.

	Specifying lists that contain in aggregate a large number of Names may
	trigger an error due to query term limits imposed by the implementation. Note
	that this term count limit is for the overall query, not just this constraint.
	"""
	listConstraint: ListSearchConstraint
	"""
	Match names based on locations.
	"""
	locationsConstraint: LocationsConstraint
	"""
	Match names based on media availability.
	"""
	mediaAvailabilityConstraint: MediaAvailabilityConstraint
	"""
	Match names based on musical instruments.
	"""
	musicalInstrumentsConstraint: MusicalInstrumentsConstraint
	"""
	Match names based on their name (name text)
	"""
	nameTextConstraint: NameTextSearchConstraint
	"""
	Match names based on number of clients.
	"""
	numClientsConstraint: NumClientsConstraint
	"""
	Match names based on number of credits.
	"""
	numCreditsConstraint: NumCreditsConstraint
	"""
	Match names based on performance skills.
	"""
	performanceSkillsConstraint: PerformanceSkillsConstraint
	"""
	Match names based on physiques.
	"""
	physiquesConstraint: PhysiquesConstraint
	"""
	Match names based on their profession categories
	"""
	professionCategoryConstraint: NameProfessionCategorySearchConstraint
	"""
	Match names based on their professions
	"""
	professionConstraint: NameProfessionSearchConstraint
	"""
	Match names based on the words in quotes
	"""
	quoteMatchingConstraint: NameQuoteMatchingSearchConstraint
	"""
	Match names based on their representation relationships with agents, managers, and other industry professionals.
	"""
	representationTypesConstraint: RepresentationTypesConstraint
	"""
	Match names based on specific representative individuals (agents, managers, etc.).
	"""
	representativesIdsConstraint: RepresentativesIdsConstraint
	"""
	Match names based on spoken languages.
	"""
	spokenLanguagesConstraint: SpokenLanguagesConstraint
	"""
	Match names based on STARmeter ranking.
	"""
	starMeterConstraint: StarMeterConstraint
	"""
	Match names based on the words in trivia
	"""
	triviaMatchingConstraint: NameTriviaMatchingSearchConstraint
	"""
	Match names based on union affiliations.
	"""
	unionAffiliationsConstraint: UnionAffiliationsConstraint
	"""
	Match names based on unique traits.
	"""
	uniqueTraitsConstraint: UniqueTraitsConstraint
	"""
	Match names based on voice types.
	"""
	voiceTypesConstraint: VoiceTypesConstraint
	"""
	Match names based on their available data (has trivia data, has a birth place, etc.)
	"""
	withNameDataConstraint: WithNameDataSearchConstraint
}

type AdvancedNameSearchEdge {
	cursor: ID!
	node: AdvancedNameSearchResult!
}

type AdvancedNameSearchResult {
	name: Name!
}

input AdvancedNameSearchSort {
	sortBy: AdvancedNameSearchSortBy!
	sortOrder: SortOrder!
}

enum AdvancedNameSearchSortBy {
	"""
	Sort names based on their birth date
	ASC: Earliest dates to Highest dates (Oldest - Youngest)
	"""
	BIRTH_DATE
	"""
	Sort names based on their death date
	ASC: Earliest dates to last dates (Died First - Died Last)
	"""
	DEATH_DATE
	"""
	Sort names alphabetically by last name
	ASC: A-Z
	"""
	LAST_NAME
	"""
	Sort names alphabetically
	ASC: A-Z
	"""
	NAME
	"""
	Sort names based on their starMeterCurrentWeekRank
	ASC: Lower popularity score means that the name is more popular, so the most popular names will be first
	"""
	POPULARITY
	"""
	Sort names by search relevance score
	ASC: Least relevant to most relevant
	DESC: Most relevant to least relevant
	Uses ES _score with starMeter and nameId as tiebreakers
	"""
	RELEVANCE
}

type AdvancedTitleSearchConnection {
	edges: [AdvancedTitleSearchEdge]!
	facet(
		facetField: TitleFacetField!
		"""
		When true, returns facet counts ignoring constraints for the specified facetField (drill-down behavior).
		When false (default), returns facet counts with all constraints applied (drill-sideways behavior).
		"""
		isDrillDown: Boolean = false
		limit: Int
	): [SearchFacet]
	pageInfo: PageInfo!
	total: Int!
}

input AdvancedTitleSearchConstraints {
	"""
	Match titles based on the words in alternate versions
	"""
	alternateVersionMatchingConstraint: AlternateVersionMatchingSearchConstraint
	"""
	Match titles based on awards they have been nominated for and/or won.
	Use query `searchMetadata.advancedSearchAwardOptions` to fetch a curated list of options to use for this constraint.
	"""
	awardConstraint: AwardSearchConstraint
	"""
	Match titles based on their box office earnings and production budget.

	Throws UNAUTHENTICATED if not logged in
	Throws FORBIDDEN if customer w/o any IMDbPro subscription
	"""
	boxOfficeEarningsConstraint: BoxOfficeEarningsSearchConstraint
	"""
	Match titles based on the certificate ratings they are assigned
	"""
	certificateConstraint: CertificateSearchConstraint
	"""
	This constraint is deprecated in favor of the titleCreditsConstraint.

	Match titles based on the names of their characters.
	"""
	characterConstraint: CharacterSearchConstraint
	"""
	Match titles based on the color info
	"""
	colorationConstraint: ColorationSearchConstraint
	"""
	Match titles based on the words in crazy credits
	"""
	crazyCreditMatchingConstraint: CrazyCreditMatchingSearchConstraint
	"""
	Match titles based on the company
	"""
	creditedCompanyConstraint: CreditedCompanySearchConstraint
	"""
	This constraint is deprecated in favor of the titleCreditsConstraint.

	Match titles based on cast/crew names
	"""
	creditedNameConstraint: CreditedNameConstraint
	"""
	Match titles based on their current production status stage.

	Throws UNAUTHENTICATED if not logged in
	Throws FORBIDDEN if customer w/o any IMDbPro subscription
	"""
	currentProductionStatusStageConstraint: CurrentProductionStatusStageConstraint
	"""
	Match titles based on episode data
	"""
	episodicConstraint: EpisodicSearchConstraint
	"""
	Match titles based on their explicit content classification
	"""
	explicitContentConstraint: ExplicitContentSearchConstraint
	"""
	Match titles based on filming location
	"""
	filmingLocationConstraint: FilmingLocationSearchConstraint
	"""
	Match titles based on their genre
	"""
	genreConstraint: GenreSearchConstraint
	"""
	Match titles based on the words in goofs
	"""
	goofMatchingConstraint: GoofMatchingSearchConstraint
	"""
	Match titles based on their availability in theaters
	"""
	inTheatersConstraint: InTheatersSearchConstraint
	"""
	Match titles based on their interests
	"""
	interestConstraint: InterestSearchConstraint
	"""
	Match titles based on keywords
	"""
	keywordConstraint: KeywordSearchConstraint
	"""
	Match titles based on their language
	"""
	languageConstraint: LanguageSearchConstraint
	"""
	Match titles based on their presence in lists.

	If any list specified in this constraint is not a list of Titles then
	`GRAPHQL_VALIDATION_FAILED` will be thrown.

	Specifying lists that contain in aggregate a large number of Titles may
	trigger an error due to query term limits imposed by the implementation. Note
	that this term count limit is for the overall query, not just this constraint.
	"""
	listConstraint: ListSearchConstraint
	"""
	Match titles based on rating given by the requesting user
	"""
	myRatingConstraint: MyRatingSearchConstraint
	"""
	Match titles based on the requesting customer's watched history
	"""
	myWatchedConstraint: MyWatchedSearchConstraint
	"""
	Match titles based on occupation credits attached to their credits.

	Throws UNAUTHENTICATED if not logged in
	Throws FORBIDDEN if customer w/o any IMDbPro subscription
	"""
	occupationCreditConstraint: OccupationCreditSearchConstraint
	"""
	Match titles based on their country of origin
	"""
	originCountryConstraint: OriginCountrySearchConstraint
	"""
	Match titles based on the words in plot summary and outline, or plot author
	"""
	plotMatchingConstraint: PlotMatchingSearchConstraint
	"""
	Match titles based on when their production status was last updated.
	Example: statusUpdateDays: 30 matches titles with production status updated in the last 30 days

	Throws UNAUTHENTICATED if not logged in
	Throws FORBIDDEN if customer w/o any IMDbPro subscription
	"""
	productionStatusUpdateDaysConstraint: ProductionStatusUpdateDaysConstraint
	"""
	Match titles based on profession categories attached to their credits.

	Throws UNAUTHENTICATED if not logged in
	Throws FORBIDDEN if customer w/o any IMDbPro subscription
	"""
	professionCategoryConstraint: ProfessionCategorySearchConstraint
	"""
	Match titles based on the words in quotes
	"""
	quoteMatchingConstraint: TitleQuoteMatchingSearchConstraint
	"""
	Match titles based on ranked list type
	"""
	rankedTitleListConstraint: RankedTitleListSearchConstraint
	"""
	Match titles based on their release country.
	"""
	releaseCountrySearchConstraint: ReleaseCountrySearchConstraint
	"""
	Match titles based on when they were released
	"""
	releaseDateConstraint: ReleaseDateSearchConstraint
	"""
	Match titles based on their runtime
	"""
	runtimeConstraint: RuntimeSearchConstraint
	"""
	Match titles based on ratings given by a single user
	"""
	singleUserRatingConstraint: SingleUserRatingSearchConstraint
	"""
	Match titles based on a specific user's public watched history
	"""
	singleUserWatchedConstraint: SingleUserWatchedSearchConstraint
	"""
	Match titles based on the sound mix
	"""
	soundMixConstraint: SoundMixSearchConstraint
	"""
	Match titles based on the words in soundtracks
	"""
	soundtrackMatchingConstraint: SoundtrackMatchingSearchConstraint
	"""
	Match titles based on their credits.
	"""
	titleCreditsConstraint: TitleCreditsConstraint
	"""
	This constraint is deprecated in favor of the rankedTitleListConstraint.

	Match titles based on meter ranking
	"""
	titleMeterConstraint: TitleMeterSearchConstraint
	"""
	Match titles based on their name (title text)
	"""
	titleTextConstraint: TitleTextSearchConstraint
	"""
	Match titles based on their type (tv, movie, etc.)
	"""
	titleTypeConstraint: TitleTypeSearchConstraint
	"""
	Match titles based on the words in trivia
	"""
	triviaMatchingConstraint: TitleTriviaMatchingSearchConstraint
	"""
	Match titles based on their user ratings
	"""
	userRatingsConstraint: UserRatingsSearchConstraint
	"""
	Match titles based on their available watch options

	Throws FORBIDDEN if queried by non-1P clients.
	"""
	watchOptionsConstraint: WatchOptionsSearchConstraint
	"""
	Match titles based on the data for the title (locations, trivia, crazyCredits, etc. )
	"""
	withTitleDataConstraint: WithTitleDataSearchConstraint
}

type AdvancedTitleSearchEdge {
	cursor: ID!
	node: AdvancedTitleSearchResult!
}

type AdvancedTitleSearchResult {
	title: Title!
}

input AdvancedTitleSearchSort {
	sortBy: AdvancedTitleSearchSortBy!
	sortOrder: SortOrder!
}

enum AdvancedTitleSearchSortBy {
	"""
	Gross revenue pulled in via box-office in Domestic market for entire lifetime of title.
	Domestic refers to North America (U.S., Canada, and Puerto Rico)
	ASC: Lower numbers means the title has pulled in less box-office revenue, so poorer performing titles will be first.
	"""
	BOX_OFFICE_GROSS_DOMESTIC
	"""
	Overall Metascore based on critic reviews. Titles without a metascore are
	placed at the end when using ASC sort order.
	ASC: Lower Metacritic score means the title is rated more poorly, so titles with worse scores will be first.
	"""
	METACRITIC_SCORE
	"""
	Star Rating given by the requesting user.
	ASC: Lower star rating means the title the user rated the title more poorly, so most disliked titles will be first.
	"""
	MY_RATING
	"""
	Date when customer rated a title.
	ASC: Earlier (older) ratings will be first.
	"""
	MY_RATING_DATE
	"""
	Date when the requesting customer watched a title.
	Throws UNAUTHENTICATED if the requesting customer is not authenticated.
	ASC: Earlier (older) watched dates will be first.
	"""
	MY_WATCHED_DATE
	"""
	TitleMeterType.TITLE_METER (aka Pro MOVIEMeter). Score given to non-episodic title types.
	ASC: Lower popularity score means that the title is more popular, so the most popular titles will be first.
	"""
	POPULARITY
	"""
	Sort based on production status update time.
	ASC: Titles with older status updates will be placed first (least recently updated first)
	DESC: Titles with newer status updates will be placed first (most recently updated first)
	"""
	PRODUCTION_STATUS_UPDATE_DAYS
	"""
	Sort results based on specified ranking algorithm.  For the advancedTitleSearch query, exactly one ranked title list
	constraint must be specified for using this sort option.
	ASC: Higher ranks will be first.
	"""
	RANKING
	"""
	Earliest wide release date of a title. Titles without a release date are
	placed at the end when using ASC sort order.
	ASC: Earlier (older) released title will be first.
	"""
	RELEASE_DATE
	"""
	The length of the title in terms of runtime.
	ASC: Lower runtime means the title is shorter, so shortest titles will be first.
	"""
	RUNTIME
	"""
	Star Rating given by the specified user in the SpecifiedUserRatingSearchConstraint.
	Throws BAD_USER_INPUT if the SpecifiedUserRatingSearchConstraint is not specified.
	ASC: Lower star rating means the title the user rated the title more poorly, so most disliked titles will be first.
	"""
	SINGLE_USER_RATING
	"""
	Date when the specified user rated a title.
	Throws BAD_USER_INPUT if the SpecifiedUserRatingSearchConstraint is not specified.
	ASC: Earlier (older) ratings will be first.
	"""
	SINGLE_USER_RATING_DATE
	"""
	Date when the specified user watched a title.
	Throws BAD_USER_INPUT if the SingleUserWatchedSearchConstraint is not specified.
	ASC: Earlier (older) watched dates will be first.
	"""
	SINGLE_USER_WATCHED_DATE
	"""
	Alphabetical sorting based on regional title text as determined by user language preferences.
	Language preference is determined by x-imdb-user-country and x-imdb-user-language headers.
	Only supports the languages/regions we support for localized search. Defaults to original title otherwise.
	ASC: Lower numbers and letters near the top of the alphabet will be returned first.
	"""
	TITLE_REGIONAL
	"""
	Weighted IMDb Star Rating as determined by users
	Note: IMDb maintains a threshold to a minimum number of ratings before it is considered.
	ASC: Lower star rating means the title is rated more poorly, so titles with worse ratings will be first.
	"""
	USER_RATING
	"""
	Count of ratings given by users
	Note: IMDb maintains a threshold to a minimum number of ratings before it is considered.
	ASC: Lower count of ratings means the title has been rated a fewer number of
	times, so titles with least ratings will be first.
	"""
	USER_RATING_COUNT
	"""
	The recognized year of the title. Typically, the release year, but guidelines are here:
	https://help.imdb.com/article/contribution/titles/title-formatting/G56U5ERK7YY47CQB
	ASC: Earlier (older) titles will be first.
	"""
	YEAR
}

enum Age {
	AGE_18_29
	AGE_30_44
	AGE_45_PLUS
	AGE_UNDER_18
}

type AgeDetails {
	"""
	Age displayable property
	"""
	displayableProperty: DisplayableNameAgeDetailsProperty!
	"""
	An opaque ID, suitable for caching the concept
	"""
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	"""
	Age display text
	"""
	text: String!
	"""
	Number which represents person's age
	"""
	value: Int!
}

"""
An agency that represents a name
"""
type Agency {
	"""
	The employees at this company who specifically represent a person.

	A name can be represented by 0, 1, or many people at a specific company.
	"""
	agents: [Agent]
	"""
	The company representing a name
	"""
	company: Company!
}

"""
An employee of a company who represents a name in some capacity
"""
type Agent {
	branch: CompanyBranch
	company: Company!
	employeeContact: CompanyContactDetails
	id: ID!
	"""
	Whether or not this person is the primary representative.

	Each representation type and company combination can have one point of contact
	but is not guaranteed to have one, even if there is only one associated representative.

	For example, if a person has multiple Talent Agents at CAA, this being
	true would indicate the associated name is the person's main Talent Agent
	at CAA
	"""
	isPrimaryAgent: Boolean!
	jobTitle: LocalizedString
	name: Name!
	occupation: OccupationValue
	"""
	The type of relationship this representation has with their client
	"""
	relationshipType: RepresentationRelationshipType!
}

type AgePlayingRange {
	from: Int!
	to: Int!
}

input AgePlayingRangeConstraint {
	"""
	Match names based on their age playing range in years.
	Infers min 0 and unbounded max if part of the range is not provided.
	"""
	agePlayingRange: IntRangeInput!
}

type AggregateRatingsBreakdown {
	"""
	Default value for demographicFilter
	"""
	histogram(
		demographicFilter: DemographicFilter = { userCategory: IMDB_USERS }
	): Histogram
	"""
	Whether the aggregate rating breakdown info display should be hidden by default
	"""
	isCollapsed: Boolean!
	"""
	Country is set by detection during registration or the user, the latter taking precedence.
	RatingsSummaryByCountry will vend only the top N country summary ordered specifically by number of ratings.
	We will only return countries with a voting count and rating data, countries with no votes will be filtered
	out. If there is no limit we will return all country rating data available for a title.
	"""
	ratingsSummaryByCountry(limit: Int): [RatingsSummaryByCountry]
	ratingsSummaryByDemographics: [DemographicRatings]
}

"""
Aka details
"""
type Aka {
	"""
	Other attributes about this aka
	"""
	attributes: [DisplayableAttribute]!
	"""
	If applicable, the country where this was the title text
	"""
	country: DisplayableCountry
	"""
	Aka as displayable property
	"""
	displayableProperty: DisplayableTitleAkaProperty!
	"""
	The language of this title text
	"""
	language: DisplayableLanguage
	"""
	The alternative title text
	"""
	text: String!
}

"""
Pagination for AKA types which always has a known total number of edges.
"""
type AkaConnection {
	"""
	Pages of AKAs
	"""
	edges: [AkaEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of Akas
"""
type AkaEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A AKA node
	"""
	node: Aka!
	"""
	Position of the node in this context, eg: AKA 4 out of 31
	"""
	position: Int!
}

enum AkaFilter {
	"""
	Filter the AKAs to a list of 1 AKA per region and language
	"""
	DISPLAY_TITLES_ONLY
	"""
	Exclude the aka if it's the same as the primary title text
	"""
	EXCLUDE_IF_SAME_AS_PRIMARY
}

input AkaSort {
	by: AkaSortBy!
	order: SortOrder!
}

enum AkaSortBy {
	COUNTRY
	RELEVANCE
}

"""
Alternate version details
"""
type AlternateVersion {
	"""
	Alternate version as displayable article
	"""
	displayableArticle: DisplayableArticle!
	"""
	Machine translated displayable article. Returns the displayable article translated based on the language header.
	"""
	machineTranslatedDisplayableArticle: MachineTranslatedDisplayableArticle
	"""
	The alternate version text
	"""
	text: Markdown!
}

"""
Pagination for alternate version types which always has a known total number of edges.
"""
type AlternateVersionConnection {
	"""
	Pages of alternate versions
	"""
	edges: [AlternateVersionEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of alternate versions
"""
type AlternateVersionEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	An alternate version node
	"""
	node: AlternateVersion!
	"""
	Position of the node in this context, eg: AlternateVersion 4 out of 46
	"""
	position: Int!
}

"""
Example: if you want to match titles of alternate version 'car chase' OR 'excessive', PLUS
the title must contain 'omits nudity', specify anyAlternateTextTerms: ['car chase', 'excessive'] plus
allAlternateTextTerms: ['omits nudity']. Note that this is term match rather than
phrase match. 'omits nudity' would match titles that contain 'omits the nudity'; 'chase car' would
match titles that contain 'car chase'.
"""
input AlternateVersionMatchingSearchConstraint {
	"""
	A title match must contain ALL of these search terms in at least one of its alternate versions.
	"""
	allAlternateVersionTextTerms: [String]
	"""
	A title match must contain AT LEAST ONE of these search terms in at least one of its alternate versions.
	"""
	anyAlternateVersionTextTerms: [String]
}

"""
Describes whether to show the AAP conversion prompt and provides banner content when eligible.
"""
type AmazonAccountPoolConversionPromptStatus {
	"""
	Non-null when shouldShow=true. Contains home page banner content with INITIATE_CONVERSION button.
	Null when suppressed or already AAP. Use as sole banner render signal.
	"""
	interstitial: Interstitial
	"""
	True when the user is eligible and outside the 60-day TTL window:
	  - Account is PAP (not yet converted to AAP)
	  - No active decline record exists (TTL not active)
	  - No active IMDbPro subscription on the PAP account

	Always false — with no suppressedUntil set — for:
	  - AAP users (already converted)
	  - Users with an active IMDbPro subscription (permanently ineligible;
	    Pro account conversion is a separate effort out of scope for this feature)

	False with suppressedUntil set for users in the 60-day cooling period after
	clicking "Not Now" (temporary suppression — becomes true again after TTL expiry).
	"""
	shouldShow: Boolean!
	"""
	Present when shouldShow=false due to active decline TTL.
	Clients should cache this value locally to skip server round-trips.
	Null when shouldShow=false due to permanent ineligibility (Pro subscription, already AAP).
	"""
	suppressedUntil: DateTime
}

type AmazonMusicProduct {
	"""
	The Amazon Standard Identification Number for the product
	"""
	amazonId: AmazonStandardId!
	"""
	Artist data for the product
	"""
	artists: [AmazonMusicProductArtist]
	"""
	The format of the product being offered, e.g. CD
	"""
	format: AmazonMusicProductFormat!
	"""
	Image for the product
	"""
	image: Image
	"""
	The product title
	"""
	productTitle: AmazonMusicProductTitle!
}

type AmazonMusicProductArtist {
	"""
	The name of the artist
	"""
	artistName: AmazonMusicProductArtistName!
}

type AmazonMusicProductArtistName {
	"""
	An opaque ID, suitable for caching the concept
	"""
	id: ID!
	"""
	The language the text is in
	"""
	language: DisplayableLanguage!
	"""
	The text for format
	"""
	text: String!
}

type AmazonMusicProductFormat {
	"""
	An opaque ID, suitable for caching the concept
	"""
	id: ID!
	"""
	The language the text is in
	"""
	language: DisplayableLanguage!
	"""
	The text for format
	"""
	text: String!
}

type AmazonMusicProductTitle {
	"""
	An opaque ID, suitable for caching the concept
	"""
	id: ID!
	"""
	The language the text is in
	"""
	language: DisplayableLanguage!
	"""
	The product title
	"""
	text: String!
}

input AmazonSsoEligibilityInput {
	"""
	Additional auth context including the client context (i.e. ubid cookie) for web token validation.
	Required if customerIdentifier is a web auth token (Atza|...), otherwise throws GRAPHQL_VALIDATION_FAILED error.
	Not used when anything else is passed in.
	"""
	additionalContext: AdditionalAuthContext
	"""
	Optional association handle for SSO eligibility verification.
	Must be a valid association handle identifier (e.g., "imdb_us", "imdb_pro_amazon").
	Throws GRAPHQL_VALIDATION_FAILED if an invalid handle is provided.
	When omitted, SSO eligibility is checked without association context validation.
	"""
	associationHandle: String
	"""
	Optional. When true, the response includes whether the account is eligible for
	conversion. Only evaluated when the account is not SSO eligible.
	Defaults to false when omitted, meaning isEligibleForConversion will not be
	evaluated and will return null in the output.
	"""
	checkConversionEligibility: Boolean
	"""
	Customer identifier used for eligibility verification.

	Format:
	- For apps: Directed ID or app access token
	- For web: Cross-domain SSO auth token or web auth token

	This field is optional because future versions may introduce alternative customer
	identification fields. However, at least one form of customer identification
	must be provided in the input object.

	If no customer identification is provided, the query will throw
	GRAPHQL_VALIDATION_FAILED.
	"""
	customerIdentifier: String
}

type AmazonSsoEligibilityOutput {
	"""
	Indicates whether the customer is eligible for Amazon SSO integration.
	Returns true if eligible, false otherwise.
	"""
	isEligible: Boolean!
	"""
	Whether the account is eligible for conversion.
	Null when checkConversionEligibility is not set or is false in the input.
	When checkConversionEligibility is true: returns true if the account meets
	conversion criteria, false otherwise (including when the account is SSO eligible).
	"""
	isEligibleForConversion: Boolean
}

type AmazonStandardId {
	"""
	Amazon Standard Identification Number
	"""
	asin: ID!
	"""
	Amazon marketplace identifier
	"""
	obfuscatedMarketplaceId: ID!
	"""
	Region of the external identifier e.g. US
	"""
	region: String!
}

union AnswerItem = Image | Name | Title

"""
Contains the eligibility status for apps to serve 3p ads and configuration map for slots on each mobile app page.
"""
type AppAdsInfoOutput {
	"""
	A map using ad layout types as keys containing a list of slots and their information as values.
	"""
	adLayoutSlotConfig: [AdLayoutConfiguration]!
	"""
	Represents whether the app is currently eligible to request and serve 3p ads based on user's geo and the enablement status of the feature.
	A value of true indicates the apps can request 3p ads and a value of false indicates they should not.
	"""
	thirdPartyAdsEligibility: Boolean!
}

"""
The configuration of APS placements for an individual IMDb ad slot.
"""
type ApsConfiguration {
	"""
	The APS slot object for the given IMDb ad slot.
	"""
	apsSlot: ApsSlot!
	"""
	The APS slot object for the given IMDb ad slot on ad refresh requests.
	A missing value indicates the client cannot make a 3p ad request for this slot on ad refresh.
	"""
	apsSlotAdRefresh: ApsSlot
}

"""
The APS slot level information needed for 3p ad requests.
"""
type ApsSlot {
	"""
	The slot id generated by APS for the given ad slot.
	"""
	apsSlotId: ID!
	"""
	The slot name associated with the slot id in APS for the given ad slot (e.g. app-title-inline20).
	"""
	apsSlotName: String!
}

"""
Key-value pair for APS slot parameters.
(e.g. { key: "ggProduct", value: "inScreen" } used for APS adhesion ads)
"""
type ApsSlotParam {
	"""
	The key of the parameter
	"""
	key: String!
	"""
	The value of the parameter
	"""
	value: String!
}

"""
Filter of Archived  or Unarchived credits
"""
enum ArchivedOrUnarchivedFilter {
	ALL_CREDITS
	ARCHIVED_ONLY
	UNARCHIVED_ONLY
}

"""
Product data for a single Amazon ASIN.
Transformed from the Amazon API's ProductV2 response.
"""
type AsinData {
	"""
	The Amazon Standard Identification Number.
	"""
	asin: ID!
	"""
	Product availability status.
	"""
	availability: AvailabilityType!
	"""
	Amazon product detail page URL ('View on Amazon' link).
	Returns null if no product link is available.
	"""
	clickThroughUrl: URL
	"""
	Pricing information for the product, derived from ProductV2 buying options.
	Returns null if no buying options with pricing are available for this product.
	"""
	priceDetail: PriceDetail
	"""
	URL of the product's primary image (low-resolution variant).
	Returns null if the product has no images.
	"""
	primaryImageUrl: URL
	"""
	Aggregate customer rating on a scale of 0.0 to 5.0.
	Returns null if the product has no customer ratings.
	"""
	rating: Float
	"""
	The product title display string.
	Returns null if the product has no title data.
	"""
	title: LocalizedString
	"""
	Total number of customer ratings.
	"""
	totalRatings: Int!
}

"""
Input for querying ASIN product data.
"""
input AsinDataInput {
	"""
	List of Amazon Standard Identification Numbers to retrieve product data for.
	"""
	asins: [ID]!
}

"""
Output containing product data for the requested ASINs.
"""
type AsinDataOutput {
	"""
	Successfully retrieved product data entries. Empty list if no products could be retrieved.
	"""
	asinData: [AsinData]!
	"""
	ASINs that failed to resolve, with error details. Empty list if all ASINs were retrieved successfully.
	"""
	asinErrors: [AsinError]!
}

"""
An ASIN that failed to resolve during AmazonAPI ProductV2 request, with the reason for the failure.
"""
type AsinError {
	"""
	The Amazon Standard Identification Number that failed to resolve.
	"""
	asin: ID!
	"""
	Human-readable description of why the ASIN could not be retrieved.
	"""
	errorMessage: LocalizedString!
}

"""
An aspect ratio, along with any attributes. For example, the aspect ratio for the IMAX and IMAX3D versions might be
2.35:1.
"""
type AspectRatio {
	"""
	Aspect ratio
	"""
	aspectRatio: String!
	attributes: [DisplayableAttribute]!
	"""
	Aspect ratio as displayable property
	"""
	displayableProperty: DisplayableTechnicalSpecificationProperty!
}

"""
Aspect ratios for this title.
"""
type AspectRatios {
	"""
	The list of aspect ratio items
	"""
	items: [AspectRatio]!
	restriction: TechnicalSpecificationsRestriction
	"""
	Total number of aspect ratio items
	"""
	total: Int!
}

input AthleticSkillsConstraint {
	"""
	A name match must have ALL of these athletic skills.
	No filtering is applied if omitted.
	"""
	allAthleticSkills: [AthleticSkillType]
	"""
	A name match must have AT LEAST ONE of these athletic skills.
	No filtering is applied if omitted.
	"""
	anyAthleticSkills: [AthleticSkillType]
	"""
	A name match must not have any of these athletic skills.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeAthleticSkills: [AthleticSkillType]
}

"""
Athletic skills and sports for talent
"""
enum AthleticSkillType {
	AEROBICS
	BASEBALL
	BASKETBALL
	BOWLING
	BOXING
	CYCLING
	EQUESTRIAN
	FENCING
	FIGURE_SKATING
	FOOTBALL
	GOLF
	GYMNASTICS
	ICE_HOCKEY
	ICE_SKATING
	MARTIAL_ARTS
	ROLLERBLADING
	SKATEBOARDING
	SNOWBOARDING
	SNOW_SKIING
	SOCCER
	SOFTBALL
	SURFING
	SWIMMING
	TENNIS
	TRACK_AND_FIELD
	VOLLEYBALL
	WATER_SKIING
	WEIGHT_LIFTING
	WRESTLING
	YOGA
}

"""
The broad groups of professions that attachments can be filtered on
"""
enum AttachmentGroup {
	CAST
	CASTING_DIRECTORS
	FILMMAKERS
	WRITERS
}

"""
Filter search by broad groups of professions

Example:
AttachmentSearchConstraint: {
    attachmentGroup: 'CASTING_DIRECTORS'
}
"""
input AttachmentSearchConstraint {
	attachmentGroup: AttachmentGroup!
}

"""
Boolean attribute value type.
Represents an attribute with a true/false value.
"""
type AttributeBooleanValue {
	"""
	Whether a value has been set for this attribute.
	When false, the value field should be ignored.
	"""
	isSet: Boolean!
	"""
	The name/key of the attribute
	"""
	name: String!
	"""
	The data type of the attribute value (always BOOLEAN for this type)
	"""
	type: AttributeType!
	"""
	The boolean value of the attribute.
	Only meaningful when isSet is true. Null when isSet is false.
	"""
	value: Boolean
}

"""
Error information for a single attribute that failed to retrieve.
"""
type AttributeError {
	"""
	Error code indicating what went wrong.
	"""
	errorCode: AttributeErrorCode!
	"""
	Human-readable error message.
	"""
	message: String!
	"""
	The attribute name that failed.
	"""
	name: String!
}

"""
Error codes for attribute operations.
"""
enum AttributeErrorCode {
	"""
	Attribute is invalid - either unknown or not accessible to the requester.
	These cases are intentionally indistinguishable to prevent leaking attribute names.
	Returned when:
	- Attribute name does not exist in AttributesConfig
	- Attribute exists but requester lacks permission (graphReadable/graphWritable not set or insufficient)

	Note: System/downstream errors throw SERVICE_UNAVAILABLE_ERROR instead of using this error code,
	allowing clients to distinguish retriable errors from invalid attribute requests.
	"""
	INVALID_ATTRIBUTE
	"""
	The provided value failed validation against the attribute's validation rules.
	Returned when the attribute name is valid and accessible, but the value doesn't pass
	the validation rules configured for that attribute in AttributesConfig.

	Examples of validation failures:
	- Text value not in allowed values list
	- Number value outside allowed range
	- List item value doesn't match required pattern

	Note: This is different from INVALID_ATTRIBUTE - the attribute itself is valid,
	only the value is rejected.
	"""
	INVALID_ATTRIBUTE_VALUE
	"""
	Operation would cause the list to exceed the maximum allowed items.
	Returned when adding items to a list attribute would exceed the limit.
	The operation fails entirely and the list remains unchanged.
	Note: The limit can potentially be relaxed to meet specific requirements.
	"""
	LIST_LIMIT_EXCEEDED
}

"""
List attribute value type.
Represents an attribute with an array of string values.
"""
type AttributeListValue {
	"""
	Whether a value has been set for this attribute.
	When false, the values field will be null.
	When true, the values field contains the list (may be empty if explicitly set to empty list).
	"""
	isSet: Boolean!
	"""
	The name/key of the attribute
	"""
	name: String!
	"""
	The data type of the attribute value (always LIST for this type)
	"""
	type: AttributeType!
	"""
	The list of string values for this attribute.
	- When isSet=false: null (attribute not set)
	- When isSet=true: the list values (may be empty [] if explicitly set to empty list)
	"""
	values: [String]
}

"""
Numeric attribute value type.
Represents an attribute with a floating point number value.
"""
type AttributeNumberValue {
	"""
	Whether a value has been set for this attribute.
	When false, the value field should be ignored.
	"""
	isSet: Boolean!
	"""
	The name/key of the attribute
	"""
	name: String!
	"""
	The data type of the attribute value (always NUMBER for this type)
	"""
	type: AttributeType!
	"""
	The numeric value of the attribute.
	Only meaningful when isSet is true. Null when isSet is false.
	"""
	value: Float
}

"""
Output wrapper for UserProfile.attributes query.
Contains successfully retrieved attributes and any errors encountered.
"""
type AttributesOutput {
	"""
	List of successfully retrieved attribute values.
	"""
	attributes: [AttributeValue]!
	"""
	List of attributes that failed to retrieve with error details.
	Empty if all attributes were successfully retrieved.
	"""
	errors: [AttributeError]!
}

"""
Text attribute value type.
Represents an attribute with a string value.
"""
type AttributeTextValue {
	"""
	Whether a value has been set for this attribute.
	When false, the value field should be ignored.
	"""
	isSet: Boolean!
	"""
	The name/key of the attribute
	"""
	name: String!
	"""
	The data type of the attribute value (always TEXT for this type)
	"""
	type: AttributeType!
	"""
	The text value of the attribute.
	Only meaningful when isSet is true. Null when isSet is false.
	"""
	value: String
}

"""
Enum representing the data type of an attribute value.
Used to distinguish between different attribute value types in the generic attributes API.
"""
enum AttributeType {
	"""
	Boolean attribute type (true/false)
	"""
	BOOLEAN
	"""
	List attribute type (array of strings)
	"""
	LIST
	"""
	Numeric attribute type (floating point number)
	"""
	NUMBER
	"""
	Text/string attribute type
	"""
	TEXT
}

"""
Union type representing any attribute value.
Clients should use inline fragments to access type-specific fields.
"""
union AttributeValue =
	| AttributeBooleanValue
	| AttributeListValue
	| AttributeNumberValue
	| AttributeTextValue

type AuthProviderDeprecationMessage {
	"""
	Deprecation reason message with a user's obfuscated email address.
	"""
	message: LocalizedMarkdown!
	"""
	URLs for actions to take based on this deprecated provider (i.e. resetting password, learn more, etc.)
	"""
	urls: [AuthProviderDeprecationUrl]!
}

type AuthProviderDeprecationUrl {
	"""
	The url label to create a hyperlink.
	"""
	label: AuthProviderDeprecationUrlLabel!
	"""
	The url value.
	"""
	value: URL!
}

type AuthProviderDeprecationUrlLabel {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

type AuthProviderStatus {
	"""
	The auth provider type
	"""
	provider: AuthProviderType!
	"""
	Link if the provider is not linked.
	Not provided if provider is already linked.
	"""
	providerLinkingURL: URL
}

enum AuthProviderType {
	"""
	For LWA login
	"""
	AMAZON
	"""
	For AAP Login
	"""
	AMAZON_AAP
	"""
	For Amazon login with email gating association handle
	"""
	AMAZON_EMAIL_GATING
	APPLE
	GOOGLE
	IMDB
}

"""
Product availability status, derived from ProductV2's AvailabilityV2.AvailabilityType.
"""
enum AvailabilityType {
	AVAILABLE_DATE
	IN_STOCK
	IN_STOCK_SCARCE
	LEADTIME
	OUT_OF_STOCK
	PREORDER
	UNAVAILABLE
	UNKNOWN
	UNSUPPORTED_COR
}

type AwardCategory {
	text: String
}

type AwardCompany {
	"""
	The associated company
	"""
	company: Company!
	"""
	The award company displayable property, e.g. '[link=co0274041] (production company/broadcaster)'
	"""
	displayableProperty: DisplayableAwardedEntity!
	"""
	Note associated with the company
	"""
	note: Markdown
}

type AwardDetails {
	"""
	Nominations for an award grouped by its category
	"""
	awardNominationCategories(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): AwardNominationsWithCategoryConnection
	"""
	Edition of this event (e.g. The Golden Globes 2013) for which this award nomination occurred.
	"""
	eventEdition: EventEdition!
	"""
	Award Id
	"""
	id: ID!
	"""
	Award name
	"""
	text: String!
}

"""
Pagination for AwardDetails types which always has a known total number of edges.
"""
type AwardDetailsConnection {
	"""
	Pages of award details
	"""
	edges: [AwardDetailsEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of award details
"""
type AwardDetailsEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	An AwardDetails node
	"""
	node: AwardDetails!
	"""
	Position of the node in this context, eg: AwardDetails 4 out of 312
	"""
	position: Int!
}

type AwardedCompanies {
	"""
	The company/companies that this nomination is primarily form, multiple companies may share
	an award. Ordered by display preference
	"""
	awardCompanies: [AwardCompany]
}

union AwardedEntities = AwardedNames | AwardedTitles

type AwardedNames {
	"""
	The name(s) that this nomination is primarily for. There is typically one name, but multiple names may share
	an award. Ordered by display preference.
	"""
	awardNames: [AwardName]!
	"""
	Optional companies associated with the primary name(s) for this award nomination
	"""
	secondaryAwardCompanies: [AwardCompany]
	"""
	Optional titles associated with the primary name(s) for this award nomination
	"""
	secondaryAwardTitles: [AwardTitle]
}

type AwardedTitles {
	"""
	The title(s) that this nomination is primarily for. There is typically one title, but multiple titles may share
	an award. Ordered by display preference.
	"""
	awardTitles: [AwardTitle]!
	"""
	Optional companies associated with the primary name(s) for this award nomination
	"""
	secondaryAwardCompanies: [AwardCompany]
	"""
	Optional names associated with the primary title(s) for this award nomination
	"""
	secondaryAwardNames: [AwardName]
}

input AwardEventNominationSearchInput {
	"""
	Event Id associated with an award.
	For example, specify `ev0000003` to match titles that have been nominated for an Oscar
	"""
	eventId: ID!
	"""
	Search-specific identifier for an award category associated with the event, limited options available.
	If not specified, titles that have been nominated for any award from the specified event will be matched.
	Fetch a curated list of award events with category ids via query `searchMetadata.advancedSearchAwardOptions`
	An invalid award category id will cause an error. For more information about what options are available,
	see https://w.amazon.com/bin/view/IMDb/GraphQL/Search/ClientUsage/#HAwardConstraintCategories
	"""
	searchAwardCategoryId: ID
	"""
	Optionally specify whether a title can only be matched if it was, or was not, selected as a winner of an award.
	If omitted, a title will be matched if it was nominated for the award, regardless of whether it won or not.
	"""
	winnerFilter: AwardWinnerSearchFilter
}

type AwardName {
	"""
	The award name displayable property, e.g. '[link=nm0745074] (unit production manager) (plaque)'
	"""
	displayableProperty: DisplayableAwardedEntity!
	"""
	The associated name
	"""
	name: Name!
	"""
	Note associated with the name
	"""
	note: Markdown
}

type AwardNomination {
	"""
	Details about the award, such as the name and year
	"""
	award: AwardDetails!
	"""
	Title(s) and/or name(s) the award is for
	"""
	awardedEntities: AwardedEntities!
	"""
	The category in which the award was given. Example: Best Motion Picture of the Year.
	"""
	category: AwardCategory
	"""
	Optional specific episode(s) for which the nomination is associated with
	"""
	forEpisodes: [Title]
	"""
	Optional song title(s) for which the nomination is associated with
	"""
	forSongTitles: [String]
	"""
	Award Nomination ID
	"""
	id: ID!
	"""
	Did the title/name win this award
	"""
	isWinner: Boolean!
	"""
	Additional notes associated with the award.
	"""
	notes: Markdown
	"""
	Optional date and time when the award win was officially announced.
	"""
	winAnnouncementDate: DisplayableDate
	"""
	The ranking of this nomination, when applicable.
	"""
	winningRank: Int
}

input AwardNominationCategoriesFilter {
	"""
	Filter for/out given nomination award categories
	"""
	categories: NominationCategoriesFilter
}

"""
Pagination for award types which always has a known total number of edges.
"""
type AwardNominationConnection {
	"""
	Pages of award nominations
	"""
	edges: [AwardNominationEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of awards
"""
type AwardNominationEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	An award nomination node
	"""
	node: AwardNomination!
	"""
	Position of the node in this context, eg: Award 4 out of 31
	"""
	position: Int!
}

input AwardNominationsFilter {
	"""
	Filter awards by award id
	"""
	awards: [ID]
	"""
	Filter awards by event id
	"""
	events: [ID]
	"""
	Filter nominations on win status
	"""
	wins: WinsFilter
}

input AwardNominationsSort {
	by: AwardNominationsSortBy!
	order: SortOrder!
}

"""
Default sort is done by sorting nomination id
"""
enum AwardNominationsSortBy {
	"""
	Sorts by prestigious awards first, then reverse chronological by year
	"""
	PRESTIGIOUS
}

type AwardNominationsWithCategory {
	"""
	Nominations belong to the category
	"""
	awardNominations(
		after: ID
		before: ID
		filter: AwardNominationsFilter
		first: Int
		jumpTo: ID
		last: Int
		sort: AwardNominationsSort
	): AwardNominationConnection
	"""
	The category of the nomination e.g. 'Best Picture'
	Category can be null because we have nominations without category, those
	nominations will be listed under the category 'null'
	"""
	category: AwardCategory
}

"""
Pagination for AwardNominationsWithCategory types which always has a known total number of edges.
"""
type AwardNominationsWithCategoryConnection {
	"""
	Pages of awardNominations with category
	"""
	edges: [AwardNominationsWithCategoryEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of awardNominations with category
"""
type AwardNominationsWithCategoryEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	An AwardNominationsWithCategory node
	"""
	node: AwardNominationsWithCategory!
	"""
	Position of the node in this context, eg: AwardNominationsWithCategory 4 out of 312
	"""
	position: Int!
}

"""
Constrain title search results based on the awards they have been nominated for and/or won.
Any combination of `any`, `all`, and `exclude` inputs can be specified.
"""
input AwardSearchConstraint {
	"""
	A title must match ALL of the specified award event nominations.
	"""
	allEventNominations: [AwardEventNominationSearchInput]
	"""
	A title must match AT LEAST ONE of the specified award event nominations.
	"""
	anyEventNominations: [AwardEventNominationSearchInput]
	"""
	Constrain to awards in the specified date range.
	"""
	awardDateRange: DateRange
	"""
	A title must match NONE of the specified award event nominations.

	If the same input is specified in the any/all and exclude constraints,
	then the results should be empty.
	"""
	excludeEventNominations: [AwardEventNominationSearchInput]
	"""
	Constrain to titles that have a number of award wins in the specified range.

	Note: only supports min award constraints.
	"""
	numAwardsRange: IntRangeInput
	"""
	Constrain to titles that have a number of award nominations in the specified range.

	Note: only supports min nomination constraints.
	"""
	numNominationsRange: IntRangeInput
}

type AwardsEvent {
	"""
	A list of all the event's editions sorted by year, followed by
	instanceWithinYear, with the most recent first (e.g. 2023, 2022 edition 2,
	2022 edition 1, ...)
	"""
	editions(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): EventEditionConnection
	"""
	The type of the event, e.g. industry_event / festival / media_event /
	critics_event. Returns null when the event has not been assigned a type.
	"""
	eventType: String
	"""
	Event ID
	"""
	id: ID!
	"""
	The location of the event
	"""
	location: DisplayableLocation
	"""
	Event name
	"""
	text: String!
	"""
	List of trivia items related to the event with potential markdown links for movies/people
	"""
	trivia: [Markdown]
	"""
	The urls for the event
	"""
	urls: [EventUrl]
}

input AwardsFilter {
	"""
	Filter out given Awards IDs
	"""
	excludeAwards: [ID]
	"""
	Filter for given Awards IDs
	"""
	includeAwards: [ID]
}

type AwardTitle {
	"""
	The award title displayable property, e.g. '[link=tt3513498] (as Queen Watevra Wa'Nabi)'
	"""
	displayableProperty: DisplayableAwardedEntity!
	"""
	Note associated with the title
	"""
	note: Markdown
	"""
	The associated title
	"""
	title: Title!
}

enum AwardWinnerSearchFilter {
	"""
	Match names that were nominated but did not win
	"""
	NON_WINNER_ONLY
	"""
	Match names that won the award
	"""
	WINNER_ONLY
}

"""
Badge represents an achievement that can be earned by customers
"""
type Badge {
	"""
	Localized description of the badge and awarding criteria
	"""
	description: LocalizedMarkdown!
	"""
	Unique identifier for the badge
	"""
	id: ID!
	"""
	Image data associated with the badge.
	Includes `url` field for fetching the image asset from media storage.
	Includes `fileType` field indicating the delivered image type.
	Includes `accessibilityText` field which is intended to be used for screen reader accessibility.
	Includes `height` and `width` fields for determining image size ratio.
	Throws UNSUPPORTED_IMAGE_TYPE if the service cannot provide the requested image type. Supports SVG initially.
	"""
	image(fileType: ImageFileType!): MediaServiceImage!
	"""
	Additional title information e.g. 2025 for an Oscars titled badge
	"""
	subtitle: CommonLocalizedDisplayableConcept
	"""
	The localized display title of the badge
	"""
	title: CommonLocalizedDisplayableConcept!
}

"""
Connection type for paginated badge results
"""
type BadgeConnection {
	"""
	A list of edges containing badge nodes
	"""
	edges: [BadgeEdge]!
	"""
	Information about pagination in a connection
	"""
	pageInfo: PageInfo!
	"""
	Total number of badges for the user
	"""
	total: Int!
}

"""
An edge in a connection
"""
type BadgeEdge {
	"""
	A cursor for use in pagination
	"""
	cursor: ID!
	"""
	The earned badge node at the end of the edge
	"""
	node: Badge!
}

"""
BadgeGuideEntry is a help page description for directions on how to achieve one or many badges.
e.g. All Oscars badge awarding criteria is represented as the Oscars BadgeGuideEntry
"""
type BadgeGuideEntry {
	"""
	Localized description of the entry and awarding criteria
	"""
	description: LocalizedMarkdown!
	"""
	Image data associated with the entry.
	Includes `url` field for fetching the image asset from media storage.
	Includes `fileType` field indicating the delivered image type.
	Includes `accessibilityText` field which is intended to be used for screen reader accessibility.
	Includes `height` and `width` fields for determining image size ratio.
	Throws UNSUPPORTED_IMAGE_TYPE if the service cannot provide the requested image type. Supports SVG initially.
	"""
	image(fileType: ImageFileType!): MediaServiceImage!
	"""
	Additional title information e.g. 20XX for the Oscars entry
	"""
	subtitle: CommonLocalizedDisplayableConcept
	"""
	Localized display title of the entry. e.g. Oscars
	"""
	title: CommonLocalizedDisplayableConcept!
}

"""
Connection type for paginated badge guide entry results
"""
type BadgeGuideEntryConnection {
	"""
	A list of edges containing badge guide entry nodes
	"""
	edges: [BadgeGuideEntryEdge]!
	"""
	Information about pagination in a connection
	"""
	pageInfo: PageInfo!
	"""
	Total number of badges for the user
	"""
	total: Int!
}

"""
An edge in a connection
"""
type BadgeGuideEntryEdge {
	"""
	A cursor for use in pagination
	"""
	cursor: ID!
	"""
	The badge guide entry node at the end of the edge
	"""
	node: BadgeGuideEntry!
}

type Banner {
	"""
	Link provided by the image source, if required by the license
	"""
	attributionUrl: String!
	"""
	Original height of the banner image resource, in pixels
	"""
	height: Int!
	"""
	DEPRECATED: recommend using `url` instead - URL for the image resource
	"""
	imageUrl: String!
	"""
	URL for the image resource
	"""
	url: String!
	"""
	Original width of the banner image resource, in pixels
	"""
	width: Int!
}

input BiographySearchConstraint {
	"""
	Match names whose biography was written by AT LEAST ONE of these authors
	"""
	anyBiographyAuthors: [String]
	"""
	Match names containing searchText in their biography
	"""
	searchText: String
}

"""
The constraints here are combined (AND), using both allows matching names born on a certain date in a certain range.
example:
birthDateConstraint: {
    birthday: "--09-02",
    birthDateRange: {
        start:"1996-01-01",
        end: "2022-12-31"
    }
}
the above constraint will match names born on September 2nd, between 1996 and 2022.
"""
input BirthDateSearchConstraint {
	"""
	Match names born within this date range.
	No date range filtering is applied if omitted.
	"""
	birthDateRange: DateRange
	"""
	Match names born on this specific month and day, regardless of year.
	Format: "--MM-DD" (e.g., "--09-02" for September 2nd).
	No birthday filtering is applied if omitted.
	"""
	birthday: MonthDay
}

type BirthName {
	"""
	The birthName as a displayable property
	"""
	displayableProperty: DisplayableBirthNameProperty!
	"""
	The birthName as raw data
	"""
	text: String!
}

"""
Matches names based on where they were born.
There is no fuzzy matching.
For example: 'Venice Italy' will be matched by 'Venice' and 'Italy' but not 'Venice Ita'
"""
input BirthPlaceSearchConstraint {
	birthPlace: String!
}

type BlogLink {
	label: String
	url: String!
}

"""
Represents a boolean validation constraint with associated error feedback that can be evaluated client-side.
Used to enforce true/false conditions in form elements such as whether input is required or optional.
"""
type BooleanValidationConstraint {
	"""
	The feedback to display to the customer related to the constraint.
	"""
	feedback: ValidationFeedbackContent!
	"""
	The required boolean value for validation
	"""
	value: Boolean!
}

enum BoxOfficeArea {
	"""
	Domestic refers to gross box-office revenue from North America (U.S., Canada, and Puerto Rico)
	"""
	DOMESTIC
	"""
	International covers the entire world excluding Domestic
	"""
	INTERNATIONAL
	"""
	Worldwide covers the entire world
	"""
	WORLDWIDE
}

"""
Displayable BoxOfficeArea type.
See sections 'Area Rollups and Special Areas' and 'Individual Areas' at
https://developer.imdb.com/documentation/bulk-data-documentation/data-dictionary/box-office.
"""
type BoxOfficeAreaType {
	"""
	A non-localized field representing the BoxOfficeAreaType code, e.g. 'XDOM'.
	"""
	code: String!
	"""
	An Opaque unique Identifier for the BoxOfficeAreaType.
	"""
	id: ID!
	"""
	The language the text is written in.
	"""
	language: DisplayableLanguage!
	"""
	The displayable BoxOfficeAreaType, e.g. 'Domestic'.
	"""
	text: String!
}

"""
Current period performance data. Appears on every chart row.
"""
type BoxOfficeCurrentPeriod {
	"""
	Percent change from previous day. Null on non-daily charts.
	"""
	dailyDelta: Float
	"""
	Number of days in release.
	"""
	daysInRelease: Int
	"""
	Cumulative gross to date.
	"""
	grossToDate: BoxOfficeGross
	"""
	Gross for this interval (e.g. this weekend's gross).
	"""
	intervalGross: BoxOfficeGross
	"""
	Per-theater average gross for this interval.
	"""
	perTheaterAverage: BoxOfficeGross
	"""
	Number of theaters showing the release.
	"""
	theaterCount: Int
	"""
	Percent change from previous week.
	"""
	weeklyDelta: Float
	"""
	Number of weeks in release.
	"""
	weeksInRelease: Int
}

"""
A date interval with localized display label and navigation reference.
"""
type BoxOfficeDateInterval {
	"""
	Identifier for navigating to the detail page for this interval. Null if not navigable.
	"""
	id: ID
	"""
	Localized display label (e.g. 'Jan 12-14, 2024').
	"""
	label: LocalizedString!
	"""
	Non-standard occasion names for this interval (e.g. 'MLK Day Weekend'). Empty for standard intervals.
	"""
	occasionNames: [LocalizedString]!
	"""
	The interval type.
	"""
	type: BoxOfficeIntervalType!
}

"""
Match titles based on their box office earnings and production budget.
"""
input BoxOfficeEarningsSearchConstraint {
	"""
	Match titles that have a gross box office revenue in the specified range (USD).
	"""
	boxOfficePerformanceRange: FloatRangeInput
	"""
	Match titles that have a production budget in the specified range (USD).
	"""
	productionBudgetRange: FloatRangeInput
}

type BoxOfficeGross {
	total: Money!
}

"""
Grosses calculation method (e.g. Total Grosses, Calendar Grosses).
"""
type BoxOfficeGrossesOption {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

"""
Box office chart interval type (e.g. Daily, Weekend, Weekly, Monthly, Yearly).
"""
type BoxOfficeIntervalType {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

"""
Time occasion for aggregation (e.g. Year, Weekend, Weekly, Daily, Monthly).
"""
type BoxOfficeOccasion {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

"""
Opening performance — one-time facts about how the release opened.
"""
type BoxOfficeOpeningPerformance {
	"""
	Peak number of theaters during the run.
	"""
	maxTheaters: Int
	"""
	Number of theaters on opening weekend.
	"""
	openingTheaters: Int
	"""
	Opening weekend gross.
	"""
	openingWeekendGross: BoxOfficeGross
}

"""
A potentially partial date, used for box office editorial dates which may be
incomplete (e.g. only a year, or a year and month). A `BoxOfficePartialDate` is
only present when at least one component is known — the parent `date` field is
null when no date information is available (we do not return an all-null object).
"""
type BoxOfficePartialDate {
	"""
	The day of month (1-31) as per the Gregorian calendar. Null if unknown.
	"""
	day: Int
	"""
	The month (1-12) as per the Gregorian calendar. Null if unknown.
	"""
	month: Int
	"""
	Year expressed with a non-numerical wildcard to signify a decade or century
	(e.g. '197?'). Null when a precise year is available or the year is entirely unknown.
	"""
	partialYear: String
	"""
	The year as per the Gregorian calendar. Null if unknown.
	"""
	year: Int
}

"""
BoxOfficeRelease: a program of content designed to be consumed in a single
session that is exhibited in a specific Box Office Area in a continuous period of time.
Most releases consist of a single film shown during a limited run in a cinema, but other cases refer to more than one title.
For example a double-bill (two full-length movies shown back-to-back with a single ticket)
or the "Oscar Shorts" program (theatrically exhibited collection of multiple films back to back with a single ticket).
"""
type BoxOfficeRelease {
	"""
	Studios/distributors for this release.
	"""
	studios: [BoxOfficeStudio]!
	"""
	Titles in the release
	"""
	titles: [Title]!
	"""
	Number of weeks this release has been running
	"""
	weeksRunning: Int
}

"""
A single release's box office performance — the universal table row type.
All fields in this type are nullable because different views populate different subsets.
"""
type BoxOfficeReleasePerformance {
	"""
	Current period performance — the main data on chart rows.
	"""
	currentPeriod: BoxOfficeCurrentPeriod
	"""
	Date interval with display label and navigation reference.
	"""
	dateInterval: BoxOfficeDateInterval
	"""
	Whether this row's data is estimated rather than confirmed actuals.
	"""
	isEstimated: Boolean
	"""
	Opening performance — one-time facts about how the release opened.
	"""
	opening: BoxOfficeOpeningPerformance
	"""
	Position in chart. Null if not ranked.
	"""
	rank: Int
	"""
	The release (title + release metadata). Null for aggregate rows.
	"""
	release: BoxOfficeRelease
	"""
	Release date.
	"""
	releaseDate: Date
}

type BoxOfficeReleasePerformanceConnection {
	edges: [BoxOfficeReleasePerformanceEdge]!
	pageInfo: PageInfo!
	total: Int!
}

type BoxOfficeReleasePerformanceEdge {
	cursor: ID!
	node: BoxOfficeReleasePerformance!
	position: Int!
}

"""
Box Office Area Filter for box office results. Please provide either the Box Office Area or the Country, not both.
Throws GRAPHQL_VALIDATION_FAILED if both boxOfficeArea and country code fields are not empty.
"""
input BoxOfficeReleasesAreaFilter {
	"""
	Box Office Area code: Box Office area codes for which the grosses are calculated.
	See sections 'Area Rollups and Special Areas' and 'Individual Areas' at
	https://developer.imdb.com/documentation/bulk-data-documentation/data-dictionary/box-office.
	Throws GRAPHQL_VALIDATION_FAILED if an invalid box office area code is provided.
	"""
	boxOfficeArea: String
	"""
	Country code in ISO 3166 (IMDbLocaleConfig:region_codes.json)
	This is a convenience option: will be mapped internally to the Box Office Area code of interest to users of this country.
	Throws GRAPHQL_VALIDATION_FAILED if an invalid country code is provided.
	"""
	country: String
}

"""
A studio/distributor in box office context.
"""
type BoxOfficeStudio {
	"""
	The associated IMDb Company entity. Null if no mapping exists.
	"""
	company: Company
	"""
	Mojo studio identifier.
	"""
	id: ID!
	language: DisplayableLanguage!
	"""
	Localized studio name.
	"""
	text: String!
}

type BoxOfficeWeekendChart {
	entries: [ChartEntry]!
	weekendEndDate: Date!
	weekendStartDate: Date!
}

"""
Domain type based on business
"""
enum BusinessDomain {
	CONSUMER
	PRO
}

type CallToAction {
	landingPagePro(resultOverride: ResultID): LinkCallToAction
	nameBanner(resultOverride: ResultID): MarkdownSlotCallToAction
	nameClaimPageForFree(resultOverride: ResultID): SectionCallToAction
	nameDiscoverMoreInsights(resultOverride: ResultID): LinkCallToAction
	nameImagesReels(resultOverride: ResultID): LinkCallToAction
	nameProUpsell(resultOverride: ResultID): MultiLinkCallToAction
	nameViewStarMeter(resultOverride: ResultID): LinkCallToAction
	navbarProFlyout(resultOverride: ResultID): ImageAndLinkCallToAction
	titleProUpsell(resultOverride: ResultID): LinkCallToAction
}

input CallToActionContextInput {
	"""
	Optional parameter specifying the const ID for the context.
	For example, CTAs on name pages would provide the nconst, while CTAs on title pages would provide
	the tconst. If all requested CTAs do not need a constId, then it could be omitted.
	"""
	constId: ID
	"""
	Optional parameter to specify if the user is logged in.
	This parameter is provided for browser cache to allow query to differ for logged in/logged out user.
	Responses for logged out user can be cached, while responses for logged in user can not.
	The back-end graphlet ignores this input argument.
	"""
	isLoggedIn: Boolean
}

"""
Generic type for an image resource which has height, url, width, and optional caption for accessibility
"""
type CallToActionImage {
	"""
	Image caption text
	"""
	caption: LocalizedMarkdown
	"""
	Original height of the image resource, in pixels
	"""
	height: Int!
	"""
	URL for the image resource
	"""
	url: String!
	"""
	Original width of the image resource, in pixels
	"""
	width: Int!
}

"""
Generic type for a localized text which has an ID or token, associated display text, and the language of the display text
"""
type CallToActionText {
	"""
	The text ID or token
	"""
	id: ID!
	"""
	The language for the display text
	"""
	language: DisplayableLanguage!
	"""
	The localized display text
	"""
	text: String!
}

"""
A camera (or lens), along with any attributes. For example, a wide angle lens used for outdoor shots.
"""
type Camera {
	attributes: [DisplayableAttribute]!
	"""
	Camera
	"""
	camera: String!
	"""
	Camera as displayable property
	"""
	displayableProperty: DisplayableTechnicalSpecificationProperty!
}

"""
Cameras for this title.
"""
type Cameras {
	"""
	The list of camera items
	"""
	items: [Camera]!
	restriction: TechnicalSpecificationsRestriction
	"""
	Total number of camera items
	"""
	total: Int!
}

type CanRate {
	isRatable: Boolean!
}

"""
Cast details
"""
type Cast implements Credit {
	attributes(limit: Int): [CreditAttribute]
	category: CreditCategory!
	"""
	Characters (e.g. 'Neo')
	"""
	characters(limit: Int): [Character]
	episodeCredits(
		after: ID
		before: ID
		filter: EpisodeCreditsFilter
		first: Int
		last: Int
		sort: EpisodeCreditsSort
	): EpisodeCastConnection
	name: Name!
	"""
	A cast credit may have an optional position attribute indicating the order of
	appearance within the title’s ending credits. This position can be null for
	some or all of the title’s credits. By default (see CreditConnection.orderBy),
	the cast credit that has a value in their position field will be ordered first
	(by that number), then all other cast credits that have a null in their
	position field will be ordered alphabetically.
	"""
	position: Int
	title: Title!
}

type CategorizedWatchOptions {
	"""
	Localized categoryName of the watch option category.
	"""
	categoryName: LocalizedString!
	"""
	Returns a list with the maximum number of watch options in this category.
	NO FILTERS, NO PAGINATION
	"""
	watchOptions(limit: Int): [WatchOption]!
}

type CategorizedWatchOptionsList {
	categorizedWatchOptionsList: [CategorizedWatchOptions]!
}

type Certificate {
	"""
	Additional descriptive attributes about the rating
	"""
	attributes(limit: Int): [DisplayableAttribute]!
	country: DisplayableCountry!
	id: String!
	rating: String!
	ratingReason: String
	ratingsBody: RatingsBody
	"""
	The certificate id assigned by the ratings body
	"""
	ratingsBodyCertificateId: String
}

type CertificatesConnection {
	"""
	Pages of certificates
	"""
	edges: [CertificatesEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Match titles that are assigned the specified certificates
"""
input CertificateSearchConstraint {
	"""
	Specify certificates by their region and rating name.
	For example: region 'US' and rating name 'R'.
	"""
	anyRegionCertificateRatings: [RegionCertificateRatingInput]
	"""
	Titles with any of the specified certificates will not be matched.

	If the same input is specified in the any and exclude constraints,
	then the results should be empty.
	"""
	excludeRegionCertificateRatings: [RegionCertificateRatingInput]
}

type CertificatesEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A Certificate node
	"""
	node: Certificate!
	"""
	Position of the node in this context
	"""
	position: Int!
}

input CertificatesFilter {
	"""
	Filter out for specific country code e.g FR, CA
	"""
	excludeCountries: [ID]
	"""
	Filter for specific country code e.g FR, CA
	"""
	includeCountries: [ID]
	ratingsBody: ID
}

input ChangeLoginSecurityRedirectURLInput {
	"""
	Association handle of the login context i.e. imdb_us
	"""
	associationHandle: String!
	"""
	Unique identifier for client request.
	For web, 'Unique browser ID' i.e ubid context identifier, which in practice means an identifier for the domain+browser combination.
	"""
	clientContext: String!
	"""
	Optional URL to return to post login & security change, otherwise defaults to main IMDb account settings page (/registration/accountsettings/)
	"""
	returnURL: URL
	"""
	'Login & Security' sub page to redirect to, if omitted redirects to the root 'Login & Security' page
	"""
	subPageType: ChangeLoginSecuritySubPageType
}

type ChangeLoginSecurityRedirectURLOutput {
	"""
	Redirect URL for user login & security page or user instructions to access their login & security settings
	"""
	redirectURL: URL!
}

enum ChangeLoginSecuritySubPageType {
	"""
	Change Email page
	"""
	CHANGE_EMAIL
	"""
	Change Password page
	"""
	CHANGE_PASSWORD
}

"""
Character details.
"""
type Character {
	"""
	Opaque ID, suitable for caching this concept.
	"""
	id: ID
	language: DisplayableLanguage
	name: String!
}

type CharacterConnection {
	"""
	Pages of Character
	"""
	edges: [CharacterEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of Character
"""
type CharacterEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	Details of the Character
	"""
	node: Character!
}

"""
Example:
1. if you want to match titles with character name ('Micky Mouse' OR 'Pluto')
specify anyCharacterName: ['Micky Mouse', 'Pluto'].

2. if you want to match titles where name const ('nm0000138') played a character called ('Jordan')
specify characterSearchConstraint: { anyCharacterName: ['Jordan'], shouldLimitToCreditedNameIds: true } and
creditedNameConstraint: { anyNameIds: ['nm0000138'] }
"""
input CharacterSearchConstraint {
	"""
	A title match must contain AT LEAST ONE of these character names.
	"""
	anyCharacterNames: [String]
	"""
	If this is set to true when a CreditedNameConstraint anyNameIds, or allNameIds is specified, then a title match
	must contain AT LEAST ONE of these character names, where AT LEAST ONE of the name ids
	is playing them.

	the constraint will behave the same whether anyNameIds or allNameIds were used, meaning
	characterSearchConstraint: { anyCharacterName: ['Jordan'], shouldLimitToCreditedNameIds: true } and
	creditedNameConstraint: { anyNameIds: ['nm1706767', 'nm0000138', 'nm0000190'] } will return the same results
	if anyNameIds was replaced with allNameIds, and specifying both allNameIds and anyNameIds will just combine the
	name ids that are matched against the specified characters.
	"""
	shouldLimitToCreditedNameIds: Boolean
}

type ChartEntry {
	title: Title!
	weekendGross: BoxOfficeGross!
}

type ChartNameEdge {
	"""
	Current rank of the chart item
	"""
	currentRank: Int!
	cursor: ID!
	node: Name!
	"""
	If a previous rank exists, this is a set of fields comparing the current rank
	to the previous rank
	"""
	rankChange: RankChange
}

input ChartNameOptions {
	chartType: ChartNameType!
}

type ChartNameSearchConnection {
	edges: [ChartNameEdge]!
	facet(facetField: NameFacetField!, limit: Int): [SearchFacet]
	pageInfo: PageInfo!
	total: Int!
}

"""
Enum exposing all the available name chart types
"""
enum ChartNameType {
	MOST_POPULAR_NAMES
}

type ChartTitleEdge {
	"""
	Current rank of the chart item
	"""
	currentRank: Int!
	cursor: ID!
	node: Title!
	"""
	If a previous rank exists, this is a set of fields comparing the current rank
	to the previous rank
	"""
	rankChange: RankChange
}

input ChartTitleOptions {
	chartType: ChartTitleType!
}

type ChartTitleSearchConnection {
	edges: [ChartTitleEdge]!
	facet(facetField: TitleFacetField!, limit: Int): [SearchFacet]
	"""
	The number of titles in the filtered list that a customer has rated.
	Customer Id is required for this field or an authentication error will be thrown.
	"""
	myRatedCount: Int
	"""
	The percentage of titles in the filtered list that a customer has rated.  We round the percentage to the nearest
	integer but we may change the precision depending on Product's requirement update.
	Customer Id is required for this field or an authentication error will be thrown.
	"""
	myRatedPercentage: LocalizedString
	pageInfo: PageInfo!
	total: Int!
}

"""
Enum exposing all the available title chart types
"""
enum ChartTitleType {
	LOWEST_RATED_MOVIES
	MOST_POPULAR_MOVIES
	MOST_POPULAR_TV_SHOWS
	TOP_RATED_ENGLISH_MOVIES
	TOP_RATED_INDIAN_MOVIES
	TOP_RATED_MALAYALAM_MOVIES
	TOP_RATED_MOVIES
	TOP_RATED_TAMIL_MOVIES
	TOP_RATED_TELUGU_MOVIES
	TOP_RATED_TV_SHOWS
}

"""
Defines standard form constraints for a checkbox type of input.
"""
type CheckboxFieldConstraints {
	"""
	Specifies whether selecting the checkbox is required, i.e.:
	- If isRequired is true, the checkbox must be checked by the customer (e.g. a
	confirmation dialog or "I agree to the terms and conditions").
	- If isRequired is false, the checkbox can be either checked or unchecked
	(e.g. a checkbox to confirm if a credit is uncredited/not listed in the end credits)
	"""
	isRequired: BooleanValidationConstraint
}

"""
Cinema type
Extends external type.
"""
type Cinema {
	"""
	Accessibility information for the cinema.

	---------------------
	Graphlet Details:
	- Name: titleAppointments
	- CTI: IMDb/GraphQL/Title Appointments
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	accessibility: CinemaAccessibility
	"""
	Contact details for the cinema.

	---------------------
	Graphlet Details:
	- Name: titleAppointments
	- CTI: IMDb/GraphQL/Title Appointments
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	contactDetails: CinemaContactDetails
	"""
	Distance from the requested location to the cinema.
	'location.radiusInMeters' has no effect on the result of this query

	Errors:
	- Throws `BAD_USER_INPUT` with symbol `location.postalCode` if the location is unknown.

	---------------------
	Graphlet Details:
	- Name: titleAppointments
	- CTI: IMDb/GraphQL/Title Appointments
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	distanceToCinema(location: ShowtimesLocation!): DistanceToCinema
	"""
	Unique identifier for this cinema.
	"""
	id: ID!
	"""
	Location information for the cinema.

	---------------------
	Graphlet Details:
	- Name: titleAppointments
	- CTI: IMDb/GraphQL/Title Appointments
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	location: CinemaLocation
	"""
	Name of the cinema.

	---------------------
	Graphlet Details:
	- Name: titleAppointments
	- CTI: IMDb/GraphQL/Title Appointments
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	name: CinemaName
}

"""
Accessibility information for a cinema.
"""
type CinemaAccessibility {
	"""
	Indicates if the cinema provides hearing devices.
	"""
	audioAccessibility: CinemaAudioAccessibility
	"""
	Indicates if the cinema has wheelchair access.
	"""
	wheelchairAccessibility: CinemaWheelchairAccessibility
}

"""
Audio accessibility information for a cinema.
"""
type CinemaAudioAccessibility {
	"""
	Indicates if the cinema provides hearing devices.
	"""
	hasHearingDevices: Boolean
}

"""
Connection type for a paginated cinema result.
"""
type CinemaConnection {
	"""
	Edges of CinemaConnection.
	"""
	edges: [CinemaEdge]!
	"""
	Pagination information.
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set.
	"""
	total: Int!
}

"""
Contact details for a cinema.
"""
type CinemaContactDetails {
	"""
	Phone number for the cinema.
	A null `phoneNumber` value indicates that there is no phoneNumber available.
	"""
	phoneNumber: String
}

"""
Edge type for a Cinema in a paginated result.
"""
type CinemaEdge {
	"""
	Cursor for pagination.
	"""
	cursor: ID!
	"""
	Cinema information.
	"""
	node: Cinema!
	"""
	Position of this edge in the result set.
	"""
	position: Int!
}

"""
Location data of a cinema.
"""
type CinemaLocation {
	"""
	Unique identifier for this cinema location.
	"""
	id: ID!
	"""
	Language of the cinema location display text.
	"""
	language: DisplayableLanguage!
	"""
	Displayable text for this cinema location.
	"""
	text: String!
}

"""
The name of a cinema.
"""
type CinemaName {
	"""
	Unique identifier for this cinema name.
	"""
	id: ID!
	"""
	Language of the cinema name display text.
	"""
	language: DisplayableLanguage!
	"""
	Text indicating the name of this cinema.
	"""
	text: String!
}

"""
Wheelchair accessibility information for a cinema.
"""
type CinemaWheelchairAccessibility {
	"""
	Indicates if the cinema has wheelchair access.
	"""
	hasWheelchairAccess: Boolean
}

type ClaimedName {
	"""
	The requesting customer's claimed name
	"""
	name: Name
	"""
	The status of name claiming
	"""
	status: ClaimedNameStatus!
}

enum ClaimedNameStatus {
	"""
	Customer's name claiming is blocked (usually by customer service admins)
	"""
	BLOCKED
	"""
	Customer has claimed the name
	"""
	CLAIMED
	"""
	Customer has not requested to claim a name
	"""
	NOT_REQUESTED
	"""
	Customer's name claiming is pending manual approval
	"""
	PENDING_APPROVAL
	"""
	The new name page requested by customer is being created
	"""
	PENDING_CREATION
	"""
	Customer had claimed the name page before but not anymore
	"""
	PREVIOUS_CLAIMED
	"""
	Customer's name claiming is in an unknown status
	"""
	UNKNOWN
}

input ClientSort {
	by: ClientSortBy
	order: SortOrder!
}

enum ClientSortBy {
	"""
	Sort names alphabetically ASC: A-Z
	"""
	NAME
	"""
	Sort names based on their starMeterCurrentWeekRank

	ASC: Lower popularity score means that the name is more popular,
	so the most popular names will be first
	"""
	POPULARITY
}

"""
A coloration, along with any attributes. For example, we could have a color picture that used technicolor, with black
and white flashback scenes.
"""
type Coloration {
	attributes: [DisplayableAttribute]!
	"""
	An ID for this coloration, suitable for use in URLs. For example, `black_and_white` or `color`.
	"""
	conceptId: String!
	"""
	Coloration as displayable property
	"""
	displayableProperty: DisplayableTechnicalSpecificationProperty!
	"""
	An opaque ID, suitable for caching the concept.
	"""
	id: ID!
	"""
	The display text
	"""
	text: String!
}

"""
Colorations for this title.
"""
type Colorations {
	"""
	The list of coloration items
	"""
	items: [Coloration]!
	restriction: TechnicalSpecificationsRestriction
	"""
	Total number of coloration items
	"""
	total: Int!
}

"""
Example: if you want to match titles of colorations COLOR OR ACES AND NOT BLACK_AND_WHITE,
specify anyColorationTypes: [COLOR,ACES] plus excludeColorationTypes: [BLACK_AND_WHITE]
"""
input ColorationSearchConstraint {
	"""
	A title match must contain AT LEAST ONE of these colorations
	"""
	anyColorationTypes: [ColorationType]
	"""
	A title match must contain NONE of these colorations

	If the same input is specified in the any and exclude constraints,
	then the results should be empty.
	"""
	excludeColorationTypes: [ColorationType]
}

enum ColorationType {
	ACES
	BLACK_AND_WHITE
	COLOR
	COLORIZED
}

input ComingSoonSort {
	"""
	Field to sort by
	"""
	sortBy: ComingSoonSortBy!
	"""
	ASC or DESC sorting order
	"""
	sortOrder: SortOrder!
}

enum ComingSoonSortBy {
	"""
	Movie meter value
	"""
	POPULARITY
	"""
	Release Date for sorting purposes
	"""
	RELEASE_DATE
}

enum ComingSoonType {
	"""
	Feature films
	"""
	MOVIE
	"""
	Series/miniseries with upcoming episodes and made-for-TV movies
	"""
	TV
	"""
	TV Episodes
	"""
	TV_EPISODE
}

"""
Implementation of the LocalizedDisplayableConcept interface. See LocalizedDisplayableConcept docs
"""
type CommonLocalizedDisplayableConcept {
	"""
	Type token
	"""
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	"""
	The display text
	"""
	text: String!
}

type Company {
	"""
	The acronyms of a given company

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	acronyms(
		after: ID
		before: ID
		first: Int
		last: Int
	): CompanyAcronymConnection
	"""
	The affiliations of a given company

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	affiliations(
		after: ID
		before: ID
		first: Int
		last: Int
		"""
		Sort options. Defaults to COMPANY_ID ASC if omitted
		"""
		sort: CompanyAffiliationSort
	): CompanyAffiliationConnection
	"""
	Details of the company bio. Returns null if no bio available.

	---------------------
	Graphlet Details:
	- Name: managedCompany
	- CTI: IMDb/GraphQL/Managed Company
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	bio: CompanyBio
	"""
	All historical box office releases for this company's studio.
	Returns null if no studio mapping exists for this company.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: mojo
	- CTI: IMDb/GraphQL/Mojo
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	boxOfficeAllReleases(
		filter: CompanyBoxOfficeAllReleasesFilter
	): CompanyBoxOfficeAllReleasesPerformance
	"""
	Box office releases for this company's studio in a specific year.
	Returns null if no studio mapping exists for this company.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: mojo
	- CTI: IMDb/GraphQL/Mojo
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	boxOfficeByYear(
		filter: CompanyBoxOfficeByYearFilter
	): CompanyBoxOfficeByYearPerformance
	"""
	Upcoming releases for this company's studio.
	Returns null if no studio mapping exists for this company.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: mojo
	- CTI: IMDb/GraphQL/Mojo
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	boxOfficeUpcoming(
		filter: CompanyBoxOfficeUpcomingFilter
	): CompanyBoxOfficeUpcomingPerformance
	"""
	Details about the branches (generally the different offices) of this company.
	Larger companies like Disney have offices in multiple locations, each one
	with its own staff, contact details, etc.

	Not all companies have branch information, and some companies only have one.

	Will return an empty set of edges if we have no branch information for the
	company.

	@entitlement(PRO_PREMIUM, NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: contactsGQL
	- CTI: IMDb/GraphQL/ContactsGQL
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-name-redesign-2024
	"""
	branches(after: ID, first: Int!): CompanyBranchConnection
	"""
	The names represented by this company in some capacity, like
	the names this company provides PR or Legal services for.

	Generally when a company represents a name there are employees
	at the company specifically associated with that representation
	but not all companies have associated representatives.

	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: contactsGQL
	- CTI: IMDb/GraphQL/ContactsGQL
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-name-redesign-2024
	"""
	clients(after: ID, first: Int!, sort: ClientSort): CompanyClientConnection
	"""
	The localized text for the company.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	companyText: CompanyText
	"""
	The types of a given company

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	companyTypes(limit: Int): [CompanyType]
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	country: LocalizedDisplayableCountry
	"""
	The employees of this company — people who work at the company in
	some capacity (e.g. agents, legal representatives, managers).

	Not all companies have employee information.

	Will return an empty set of edges if we have no employee information for
	the company.

	Employees are returned in the order they appear in IMDb's underlying
	company employees record (in practice, ascending by name ID). No other
	ordering is applied or guaranteed.

	Up to 250 employees can be requested at a time. If `first` is greater
	than 250, only 250 employees will be returned.

	If `after` is omitted, results start from the beginning of the list.

	Returns a connection with empty `edges` and a `total` of 0 when the
	customer is not entitled, including when the customer is not logged in.

	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: contactsGQL
	- CTI: IMDb/GraphQL/ContactsGQL
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-name-redesign-2024
	"""
	employees(after: ID, first: Int!): CompanyEmployeeConnection
	id: ID!
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	images(
		after: ID
		before: ID
		"""
		Parameter to go around cache, expected to use cursor as a value
		"""
		bust: String
		filter: CompanyImagesFilter
		first: Int
		jumpTo: ID
		last: Int
	): ImageConnection
	"""
	Key staff for the company, ordered by StarMeter ranking (ASC) by default.
	Defaults to 10 results if first is omitted.
	Returns null if the user is not entitled.

	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: managedCompany
	- CTI: IMDb/GraphQL/Managed Company
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	keyStaff(
		after: ID
		filter: KeyStaffFilter
		first: Int
		sort: KeyStaffSort
	): CompanyKeyStaffConnection
	"""
	Known for clients for this company, ordered by relevance.
	Defaults to 10 results if first is omitted.
	Returns null if the user is not entitled.

	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: managedCompany
	- CTI: IMDb/GraphQL/Managed Company
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	knownForClients(after: ID, first: Int): CompanyKnownForClientConnection
	"""
	Known for titles for this company, ordered by relevance.
	Defaults to 10 results if first is omitted.

	---------------------
	Graphlet Details:
	- Name: managedCompany
	- CTI: IMDb/GraphQL/Managed Company
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	knownForTitles(after: ID, first: Int): CompanyKnownForTitleConnection
	"""
	Data that company managers control for consumption by company manager and IMDb admins.

	---------------------
	Graphlet Details:
	- Name: managedCompany
	- CTI: IMDb/GraphQL/Managed Company
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	managedData: ManagedCompanyData
	"""
	CompanyMeter rank information
	Ranking information beyond 5000 requires entitlement proving Pro subscription.
	Use useEntitlement = false to access non entitled version.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: ranking
	- CTI: IMDb/GraphQL/ProRanking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	meterRank(useEntitlement: Boolean = true): CompanyMeterRanking
	"""
	History of IMDbPro COMPANYmeter rankings of a company.
	Requires entitlement proving Pro subscription.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: ranking
	- CTI: IMDb/GraphQL/ProRanking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	meterRankingHistory(
		input: CompanyMeterRankingHistoryInput
	): CompanyMeterRankingHistory
	"""
	The primary image for the company.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	primaryImage: Image
	"""
	All occupations used by staff at this company. Returns null if the company has no staff.
	Occupations is a limited set of 10 possibilities (as of 2025) so there is no need for pagination.

	---------------------
	Graphlet Details:
	- Name: managedCompany
	- CTI: IMDb/GraphQL/Managed Company
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	staffOccupations: [CompanyEmployeeOccupation]
}

"""
An acronym of a company
"""
type CompanyAcronym {
	"""
	Company acronym ID
	"""
	id: ID!
	"""
	The language the text is written in
	"""
	language: DisplayableLanguage!
	"""
	The displayable acronym , e.g. 'CAA' for Creative Artists Agency
	"""
	text: String!
}

"""
Pagination for CompanyAcronym which always has a known total number of edges
"""
type CompanyAcronymConnection {
	"""
	Pages of CompanyAcronym
	"""
	edges: [CompanyAcronymEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of CompanyAcronym
"""
type CompanyAcronymEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	Details of the page
	"""
	node: CompanyAcronym!
	"""
	Position of the node in this context, eg: CompanyAcronym 4 out of 31
	"""
	position: Int!
}

"""
The affiliated company
"""
type CompanyAffiliation {
	"""
	The company that is affiliated
	"""
	company: Company!
	"""
	Company affiliation ID
	"""
	id: ID!
	"""
	The language the text is written in
	"""
	language: DisplayableLanguage!
	"""
	The displayable affiliation, e.g. 'Subsidiary of'
	"""
	text: String!
}

"""
Pagination for CompanyAffiliation which always has a known total number of edges
"""
type CompanyAffiliationConnection {
	"""
	Pages of CompanyAffiliation
	"""
	edges: [CompanyAffiliationEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of CompanyAffiliation
"""
type CompanyAffiliationEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	Details of the page
	"""
	node: CompanyAffiliation!
	"""
	Position of the node in this context, eg: CompanyAffiliation 4 out of 31
	"""
	position: Int!
}

"""
Sort options for company affiliations
"""
input CompanyAffiliationSort {
	"""
	Field to sort by
	"""
	by: CompanyAffiliationSortBy!
	"""
	Sort order
	"""
	order: SortOrder!
}

"""
Fields available for sorting company affiliations
"""
enum CompanyAffiliationSortBy {
	"""
	Sort by affiliation type alphabetically using CompanyAffiliation.text
	"""
	AFFILIATION_TYPE
	"""
	Sort by affiliated company ID (default behavior)
	"""
	COMPANY_ID
	"""
	Sort by affiliated company name alphabetically using Company.companyText
	"""
	COMPANY_NAME
}

type CompanyBio {
	"""
	The bio as a displayable article
	"""
	displayableArticle: DisplayableArticle!
	"""
	Unique ID for this bio item
	"""
	id: ID!
	"""
	The language of the bio text
	"""
	language: DisplayableLanguage!
	"""
	The bio text
	"""
	text: Markdown!
}

input CompanyBoxOfficeAllReleasesFilter {
	"""
	Area filter. If omitted, defaults to the first available area.
	"""
	area: BoxOfficeReleasesAreaFilter
}

type CompanyBoxOfficeAllReleasesFilterOptions {
	"""
	Available areas with data.
	"""
	availableAreas: [BoxOfficeAreaType]!
	"""
	Currently selected geographic area.
	"""
	selectedArea: BoxOfficeAreaType!
}

type CompanyBoxOfficeAllReleasesPerformance {
	"""
	Filter options contextual to the current selection.
	"""
	filterOptions: CompanyBoxOfficeAllReleasesFilterOptions!
	"""
	Releases matching the filter. Up to 100 releases per page.
	If `first` exceeds 100, only 100 will be returned.
	Default ordering: gross to date descending.
	"""
	releases(
		"""
		Cursor for pagination. If omitted, returns results from the beginning.
		"""
		after: ID
		first: Int!
		sort: CompanyBoxOfficeAllReleasesSortInput
	): BoxOfficeReleasePerformanceConnection!
}

enum CompanyBoxOfficeAllReleasesSortBy {
	"""
	ASC: fewest days first. DESC: most days first.
	"""
	DAYS_IN_RELEASE
	"""
	ASC: lowest cumulative gross first. DESC: highest cumulative gross first.
	"""
	GROSS_TO_DATE
	"""
	ASC: fewest theaters first. DESC: most theaters first.
	"""
	MAX_THEATERS
	"""
	ASC: fewest opening theaters first. DESC: most opening theaters first.
	"""
	OPENING_THEATERS
	"""
	ASC: lowest opening gross first. DESC: highest opening gross first.
	"""
	OPENING_WEEKEND_GROSS
	"""
	ASC: lowest rank number first (best ranked). DESC: highest rank number first.
	"""
	RANK
	"""
	ASC: earliest release date first. DESC: most recent release date first.
	"""
	RELEASE_DATE
}

input CompanyBoxOfficeAllReleasesSortInput {
	by: CompanyBoxOfficeAllReleasesSortBy!
	"""
	If omitted, defaults to DESC.
	"""
	order: SortOrder
}

input CompanyBoxOfficeByYearFilter {
	"""
	Area filter. If omitted, defaults to the first available area.
	"""
	area: BoxOfficeReleasesAreaFilter
	"""
	Grosses option ID. If omitted, defaults to Calendar Grosses. Throws BAD_USER_INPUT if the ID is invalid.
	"""
	grossesOption: ID
	"""
	Occasion ID. If omitted, defaults to Year. Throws BAD_USER_INPUT if the ID is invalid.
	"""
	occasion: ID
	"""
	Year filter (e.g. 2024). Valid range is determined by available data. If
	omitted, defaults to the current year. Throws BAD_USER_INPUT if the year is
	not a valid filter input.
	"""
	year: Int
}

type CompanyBoxOfficeByYearFilterOptions {
	"""
	Available areas with data (contextual to current year/grosses selection).
	"""
	availableAreas: [BoxOfficeAreaType]!
	"""
	Available grosses calculation methods.
	"""
	availableGrossesOptions: [BoxOfficeGrossesOption]!
	"""
	Available time occasions.
	"""
	availableOccasions: [BoxOfficeOccasion]!
	"""
	Available years with data, sorted descending.
	"""
	availableYears: [Int]!
	"""
	Currently selected geographic area.
	"""
	selectedArea: BoxOfficeAreaType!
	"""
	Currently selected grosses calculation method.
	"""
	selectedGrossesOption: BoxOfficeGrossesOption!
	"""
	Currently selected time occasion.
	"""
	selectedOccasion: BoxOfficeOccasion!
	"""
	Currently selected year.
	"""
	selectedYear: Int!
}

type CompanyBoxOfficeByYearPerformance {
	"""
	Filter options contextual to the current selection.
	"""
	filterOptions: CompanyBoxOfficeByYearFilterOptions!
	"""
	Releases matching the filter. Up to 100 releases per page.
	If `first` exceeds 100, only 100 will be returned.
	Default ordering: rank ascending.
	"""
	releases(
		"""
		Cursor for pagination. If omitted, returns results from the beginning.
		"""
		after: ID
		first: Int!
		sort: CompanyBoxOfficeByYearSortInput
	): BoxOfficeReleasePerformanceConnection!
}

enum CompanyBoxOfficeByYearSortBy {
	"""
	ASC: lowest gross first. DESC: highest gross first.
	"""
	GROSS
	"""
	ASC: lowest cumulative gross first. DESC: highest cumulative gross first.
	"""
	GROSS_TO_DATE
	"""
	ASC: lowest rank number first (best ranked). DESC: highest rank number first.
	"""
	RANK
	"""
	ASC: earliest release date first. DESC: most recent release date first.
	"""
	RELEASE_DATE
}

input CompanyBoxOfficeByYearSortInput {
	by: CompanyBoxOfficeByYearSortBy!
	"""
	If omitted, defaults to ASC for RANK, DESC for others.
	"""
	order: SortOrder
}

input CompanyBoxOfficeUpcomingFilter {
	"""
	Area filter. If omitted, defaults to the first available area.
	"""
	area: BoxOfficeReleasesAreaFilter
}

type CompanyBoxOfficeUpcomingFilterOptions {
	"""
	Available areas with data.
	"""
	availableAreas: [BoxOfficeAreaType]!
	"""
	Currently selected geographic area.
	"""
	selectedArea: BoxOfficeAreaType!
}

type CompanyBoxOfficeUpcomingPerformance {
	"""
	Filter options contextual to the current selection.
	"""
	filterOptions: CompanyBoxOfficeUpcomingFilterOptions!
	"""
	Releases matching the filter. Up to 100 releases per page.
	If `first` exceeds 100, only 100 will be returned.
	Default ordering: release date ascending.
	"""
	releases(
		"""
		Cursor for pagination. If omitted, returns results from the beginning.
		"""
		after: ID
		first: Int!
		sort: CompanyBoxOfficeUpcomingSortInput
	): BoxOfficeReleasePerformanceConnection!
}

enum CompanyBoxOfficeUpcomingSortBy {
	"""
	ASC: earliest release date first. DESC: most recent release date first.
	"""
	RELEASE_DATE
}

input CompanyBoxOfficeUpcomingSortInput {
	by: CompanyBoxOfficeUpcomingSortBy!
	"""
	If omitted, defaults to ASC.
	"""
	order: SortOrder
}

type CompanyBranch {
	"""
	Ways to contact this part of the company directly
	"""
	directContact: CompanyContactDetails
	"""
	An obfuscated persistent id to uniquely identify this data
	"""
	id: ID!
	"""
	Branch name
	"""
	name: LocalizedString
}

type CompanyBranchConnection {
	edges: [CompanyBranchEdge]!
	pageInfo: PageInfo!
	total: Int!
}

type CompanyBranchEdge {
	cursor: ID!
	node: CompanyBranch!
	position: Int!
}

type CompanyClient {
	"""
	The employees working at this company who work with the client
	in this type of relationship

	Returns nothing if we have no information about the individual
	employees at this company who work specifically with this client.
	"""
	agents: [Agent]
	"""
	The name being represented
	"""
	client: Name!
	"""
	An obfuscated persistent id to uniquely identify this data
	"""
	id: ID!
}

type CompanyClientConnection {
	edges: [CompanyClientEdge]!
	pageInfo: PageInfo!
	total: Int!
}

type CompanyClientEdge {
	cursor: ID!
	node: CompanyClient!
	position: Int!
}

type CompanyContactDetails {
	emailAddress: String
	faxNumber: LocalizedString
	phoneNumbers: [LocalizedString]
	"""
	A physical mailing address for this company or this company's specific office
	"""
	physicalAddress: Location
	website: WebsiteLink
}

type CompanyCredit {
	"""
	This field may contain distribution format, opening credit and other attributes related to credit of a company
	"""
	attributes(limit: Int): [DisplayableAttribute]!
	"""
	Category (e.g. 'Production').
	"""
	category: CompanyCreditCategory!
	"""
	The company credited
	"""
	company: Company!
	"""
	The countries in which this company was involved with this title
	"""
	countries(limit: Int): [DisplayableCountry]
	"""
	Company credit as displayable property
	"""
	displayableProperty: DisplayableTitleCompanyCreditProperty!
	"""
	For distribution companies it lists the different formats the company is
	distributing the title in. For other company credits it always returns null.
	"""
	distributionFormats: [DistributionFormat]
	"""
	The title on which this credit appears
	"""
	title: Title!
	"""
	Start and optionally end year(s) that this company was involved with this title
	"""
	yearsInvolved: YearRange
}

"""
The category for the company credit, e.g. sales, distribution etc.
"""
type CompanyCreditCategory {
	"""
	The unique identifier for the credit category
	"""
	id: ID!
	"""
	The displayable text for the credit category
	"""
	text: String!
}

type CompanyCreditCategoryWithCompanyCredits {
	category: CompanyCreditCategory
	companyCredits(first: Int): CompanyCreditConnection
	restriction: CompanyCreditRestriction
}

"""
Pagination for CompanyCredit type.
"""
type CompanyCreditConnection {
	"""
	Pages of credits
	"""
	edges: [CompanyCreditEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Restriction related information if exists for the connection
	"""
	restriction: CompanyCreditRestriction
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of credits
"""
type CompanyCreditEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A CompanyCredit node
	"""
	node: CompanyCredit!
	"""
	Position of the node in this context, eg: credit 4 out of 312
	"""
	position: Int!
}

"""
Information about restrictions applied to Company Credits
"""
type CompanyCreditRestriction {
	"""
	Explanation(s) for the restriction
	"""
	explanations: [RestrictionExplanation]!
	reasons: [ContentRestrictionReason]!
	"""
	DEPRECATED: Renamed, use `reasons` instead.
	"""
	restrictionReason: [ContentRestrictionReason]!
	"""
	The total number of items if there was no restriction
	"""
	unrestrictedTotal: Int
}

input CompanyCreditsFilter {
	categories: [ID]
}

type CompanyEmployee {
	"""
	An obfuscated persistent id to uniquely identify this employment record.
	Stable across edits to the record.
	"""
	id: ID!
	"""
	The specific job title this person has at this company. Null if job title information is not available.
	"""
	jobTitle: LocalizedString
	"""
	The person employed by this company
	"""
	name: Name!
	"""
	The general occupation of this person. Null if occupation information is not available.
	"""
	occupation: OccupationValue
}

type CompanyEmployeeConnection {
	edges: [CompanyEmployeeEdge]!
	pageInfo: PageInfo!
	total: Int!
}

type CompanyEmployeeEdge {
	cursor: ID!
	node: CompanyEmployee!
	"""
	The 1-based position of this employee within the overall result set, stable across pages.
	"""
	position: Int!
}

"""
The occupation the client held while employed by the company
"""
type CompanyEmployeeOccupation {
	"""
	The unique identifier for a particular occupation
	"""
	id: ID!
	"""
	The language that the text is in
	"""
	language: DisplayableLanguage!
	"""
	The localized text describing the job, e.g. talent agent, executive
	"""
	text: String!
}

"""
The title a company employee held while working for a company
"""
type CompanyEmployeeTitle {
	"""
	The unique identifier for a particular title
	"""
	id: ID!
	"""
	The language that the text is in
	"""
	language: DisplayableLanguage!
	"""
	The localized text describing the title, e.g. managing partner
	"""
	text: String!
}

"""
The employment relationship a staff member has with a company
"""
type CompanyEmployment {
	"""
	The name of the branch that the employee works
	"""
	branch: EmployeeBranchName
	"""
	The occupation the staff memeber performed for this company, e.g. talent agent, executive
	"""
	occupation: CompanyEmployeeOccupation!
	"""
	The title the staff member had at this company, e.g. managing partner
	"""
	title: CompanyEmployeeTitle!
}

input CompanyImagesFilter {
	"""
	Return only images which are one of these types
	"""
	types: [ID]
}

"""
The job for a company credit that has detail beyond the job category, free form text
"""
type CompanyJob {
	"""
	Unique identifier that matches the text string itself since jobs are free form text
	"""
	id: ID!
	"""
	The language that the text is in
	"""
	language: DisplayableLanguage!
	"""
	The text describing the job
	"""
	text: String!
}

"""
A key staff memeber of a company
"""
type CompanyKeyStaff {
	"""
	A key staff memeber for this company
	"""
	name: Name!
	"""
	A summary of the relationship the staff member has with this company
	"""
	summary: CompanyKeyStaffSummary!
}

"""
Pagination for company key staff which always has a known total number of edges
"""
type CompanyKeyStaffConnection {
	"""
	Pages of company key staff for this company
	"""
	edges: [CompanyKeyStaffEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of staff members available in the pool for this company
	"""
	recordPoolSize: Int!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of company key staff
"""
type CompanyKeyStaffEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A company key staff node
	"""
	node: CompanyKeyStaff!
	"""
	Position of the node in this context, eg: CompanyKeyStaffEdge 4 out of 100
	"""
	position: Int!
}

"""
A summary of the relationship a company has a the key staff member
"""
type CompanyKeyStaffSummary {
	"""
	How this company has employed this staff member
	"""
	employment(limit: Int!): [CompanyEmployment]
}

"""
Page of company key staff version
"""
type CompanyKeyStaffVersionEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A key saff version node
	"""
	node: ManagedCompanyKnownForKeyStaffVersion!
	"""
	Position of the node in this context, eg: ManagedCompanyKnownForKeyStaffVersion 4 out of 100
	"""
	position: Int!
}

"""
A known for client for a company
"""
type CompanyKnownForClient {
	"""
	A client this company is known for
	"""
	name: Name!
	"""
	A summary of the relationship the known for client has with this company
	"""
	summary: CompanyKnownForClientSummary!
}

"""
Pagination for company known for client which always has a known total number of edges
"""
type CompanyKnownForClientConnection {
	"""
	Pages of company known for client for this company
	"""
	edges: [CompanyKnownForClientEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of clients available in the pool for this company
	"""
	recordPoolSize: Int!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of company known for client
"""
type CompanyKnownForClientEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A company known for client node
	"""
	node: CompanyKnownForClient!
	"""
	Position of the node in this context, eg: CompanyKnownForClient 4 out of 100
	"""
	position: Int!
}

"""
A summary of the relationship the company has with the client it is known for
"""
type CompanyKnownForClientSummary {
	"""
	The ways in which this company represented this client
	"""
	representation(limit: Int): [CompanyRepresentationCategory]
	"""
	The ways in which this company represents this client aggregated by representation type
	"""
	representationCategories: [CompanyRepresentationCategories]
}

"""
Page of company known for client version
"""
type CompanyKnownForClientVersionEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A known for client version node
	"""
	node: ManagedCompanyKnownForClientVersion!
	"""
	Position of the node in this context, eg: ManagedCompanyKnownForClientVersion 4 out of 100
	"""
	position: Int!
}

"""
The job category for a company credit
"""
type CompanyKnownForCreditCategory {
	"""
	Unique identifier for the credit category
	"""
	id: ID!
	"""
	The language the text is in
	"""
	language: DisplayableLanguage!
	"""
	The text describing the credit category
	"""
	text: String!
}

"""
The job a company did on a particular known for
"""
type CompanyKnownForJob {
	"""
	The job category, e.g. distribution, production etc.
	"""
	category: CompanyKnownForCreditCategory!
	"""
	The job that has detail beyond the job category, free form text
	"""
	job: CompanyJob
}

"""
The status of a known for category for consumption by IMDb admins
"""
enum CompanyKnownForStatus {
	"""
	Indicates that the known for is active and can be modified
	"""
	ACTIVE
	"""
	Indicates that the known for is blocked from modification
	"""
	BLOCKED
}

"""
A Known for title for a company
"""
type CompanyKnownForTitle {
	"""
	A summary of the relationship the known for title has with this company
	"""
	summary: CompanyKnownForTitleSummary!
	"""
	A title this company is known for
	"""
	title: Title!
}

"""
Pagination for company known for title which always has a known total number of edges
"""
type CompanyKnownForTitleConnection {
	"""
	Pages of company known for title for this company
	"""
	edges: [CompanyKnownForTitleEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of titles available in the pool for this company
	"""
	recordPoolSize: Int!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of company known for title
"""
type CompanyKnownForTitleEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A company known for title node
	"""
	node: CompanyKnownForTitle!
	"""
	Position of the node in this context, eg: CompanyKnownForTitle 4 out of 100
	"""
	position: Int!
}

"""
A summary of the relationship a company has with a title it is known for
"""
type CompanyKnownForTitleSummary {
	"""
	The countries the company did work for the known for title in
	"""
	countries: [DisplayableCountry]
	"""
	The jobs this company performed on the known for title
	"""
	jobs: [CompanyKnownForJob]!
	"""
	The year range the company was associated with the known for title. A single displayed if the start matches the end.
	"""
	yearRange: YearRange
}

"""
Page of company known for title version
"""
type CompanyKnownForTitleVersionEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A known for title version node
	"""
	node: ManagedCompanyKnownForTitleVersion!
	"""
	Position of the node in this context, eg: ManagedCompanyKnownForTitleVersion 4 out of 100
	"""
	position: Int!
}

type CompanyMetadata {
	"""
	All the company credit categories
	"""
	companyCreditCategories: [CompanyCreditCategory]!
}

type CompanyMeterRanking {
	"""
	The current IMDbPro CompanyMeter ranking of the company. A lower value is better.
	Ranking information beyond 5000 requires entitlement proving Pro subscription.
	"""
	currentRank: Int!
	"""
	If a previous rank exists, this is a set of fields comparing the current rank
	to the previous rank
	"""
	rankChange: MeterRankChange
}

type CompanyMeterRankingHistory {
	"""
	The best historical IMDbPro COMPANYmeter ranking of a company.
	A lower value is better. By default, returns the values over all time.
	Requires entitlement proving Pro subscription.
	"""
	bestRank: MeterRankingHistoryEntry
	"""
	History of IMDbPro COMPANYmeter rankings of a company.
	Requires entitlement proving Pro subscription.
	"""
	ranks: [MeterRankingHistoryEntry]
	restriction: MeterRestriction
}

input CompanyMeterRankingHistoryInput {
	"""
	Last date to obtain COMPANYmeter history for. If not provided, default is most recent ranking date.
	If there is no ranking on end date, the date is the most recent date before this one.
	"""
	endDate: Date
	"""
	First date to obtain COMPANYmeter history for. If not provided, default is first ranking date.
	If there is no ranking on start date, the date is the earliest date after this one.
	"""
	startDate: Date
}

"""
A breakdown of a type of representative that represents this client
"""
type CompanyRepresentationCategories {
	"""
	The unique id for the representation type
	"""
	id: ID!
	"""
	The language that the text is in
	"""
	language: DisplayableLanguage!
	"""
	The text for the representation type, will be plural for total > 1. Agent types rolled up to 'agent'/'agents'
	"""
	text: String!
	"""
	The total number of this type of representative that represent the client
	"""
	total: Int!
}

"""
The type of client representation a company performed for a client
"""
type CompanyRepresentationCategory {
	"""
	The unique identifier for a particular category
	"""
	id: ID!
	"""
	The language that the text is in
	"""
	language: DisplayableLanguage!
	"""
	The localized text describing the type of representation, e.g. talent agent
	"""
	text: String!
}

input CompanyRepresentativesConstraint {
	"""
	A name match must be represented by ALL of these companies.
	Company IDs should be provided in IMDb company format.
	Example: "co0015930" for SAG-AFTRA, "co0081427" for DGA
	No filtering is applied if omitted.
	"""
	allRepresentationCompanyIds: [ID]
	"""
	A name match must be represented by AT LEAST ONE of these companies.
	Company IDs should be provided in IMDb company format.
	Example: "co0015930" for SAG-AFTRA, "co0081427" for DGA
	No filtering is applied if omitted.
	"""
	anyRepresentationCompanyIds: [ID]
	"""
	A name match must not be represented by any of these companies.
	Company IDs should be provided in IMDb company format.
	Example: "co0015930" for SAG-AFTRA, "co0081427" for DGA
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeRepresentationCompanyIds: [ID]
}

input CompanySearchOptions {
	"""
	The types of companies to include in results.
	By default, all company types are included in the search.
	"""
	type: [MainSearchCompanyType]
}

type CompanyText {
	text: String!
}

"""
The type of a company
"""
type CompanyType {
	"""
	Company type ID
	"""
	id: ID!
	"""
	The language the text is written in
	"""
	language: DisplayableLanguage!
	"""
	The displayable company type, e.g. 'Talent Agency'
	"""
	text: String!
}

type ConnectionCategoryWithConnections {
	category: TitleConnectionCategory!
	connections(first: Int): TitleConnectionConnection
}

input ConnectionsFilter {
	categories: [ID]
}

"""
Common interface for all consent types
"""
interface Consent {
	consentOperation: ConsentOperation!
	consentType: ConsentType!
	"""
	This is the time at which this consent
	is no longer valid for this user.
	"""
	expirationDate: DateTime!
}

enum ConsentOperation {
	"""
	Customized consent, can be returned by Queries but not used as Mutation input.
	"""
	CUSTOM
	"""
	Opt in to the consent. i.e. Yes or 'accept all'
	Note: Default consent operation for a given type
	is provided in the consentRequirement
	"""
	IN
	"""
	Opt out of the consent. i.e. No or 'reject all'
	Note: Default consent operation for a given type
	is provided in the consentRequirement
	"""
	OUT
}

enum ConsentType {
	"""
	Consent for not sharing customer information with third parties.
	For IMDb, this disables cross contextual behavioral advertising.
	"""
	THIRD_PARTY_DATA_SHARING
	"""
	Used for Cookie/Tracker consent. Corresponds to "CookieConsent"
	model in TollhouseService. https://tiny.amazon.com/1hii9awk1/codeamazpackPRIVblobb59emode
	"""
	TRACKING_COOKIE
}

enum ContentRestrictionReason {
	"""
	The title is announced
	"""
	PRO_ANNOUNCED_TITLE
	"""
	The title is in development
	"""
	PRO_IN_DEV_TITLE
}

type ContentWarnings {
	"""
	A boolean flag that indicates whether IMDb considers this person's work to be primarily pornographic or adult genre.
	"""
	isPrimarilyAdultActor: Boolean!
}

"""
All of the context required to render a contribution link.
"""
input ContributionContext {
	"""
	Determines which site we are rendering from. Valid: 'consumer', 'pro'.
	Defaults to 'consumer'.
	See: https://tiny.amazon.com/t8frko7h
	"""
	business: String = "consumer"
	"""
	Optional flag to determine if this is in an iframe or behind a redirect
	"""
	isInIframe: Boolean
	"""
	Where to redirect to once contribution is done. Accepts variable substitutions.
	{id} will be substituted with the item's ID you are trying to contribute to (eg: Name, Title, Image)
	{relatedId} if provided will be substituted with the ID of the context this
	item is in, if applicable (eg: Name that Image is in the gallery of)
	"""
	returnUrl: URL!
}

"""
Contains the URL for contributing to this data type. Is a type for extendability.
"""
type ContributionLink {
	url: URL!
}

type Contributor {
	"""
	The user ID
	"""
	id: ID!
	"""
	The user profile for this contributor
	"""
	user: UserProfile
}

type ContributorLeaderboard {
	"""
	Description of the leaderboard, to appear under title
	"""
	description: LocalizedString
	id: ID!
	"""
	Have rankings been finalized or can they still change
	"""
	isFinalized: Boolean!
	"""
	Date last updated
	"""
	lastUpdated: DateTime!
	"""
	Public url of the leaderboard
	"""
	leaderboardUrl: URL!
	"""
	Month of leaderboard if appropriate (null for ALL_TIME and YEAR periods)
	1=Jan, 2=Feb, 3=Mar etc
	"""
	month: Int
	"""
	Type of period that the leaderboard covers
	"""
	period: ContributorLeaderboardPeriodType!
	"""
	Paginated rankings in ascending order, jumpTo option for jumping to a specific contributorId or rank in the leaderboard.
	Please do not use cursors (after, before, jumpTo) in rankings queries when
	nested within the results of a paginated leaderboard query
	to avoid unintended side effects.
	"""
	rankings(
		after: ID
		before: ID
		filter: ContributorLeaderboardRankingsFilter
		first: Int
		jumpTo: ID
		last: Int
		sort: ContributorRankSort
	): ContributorRankingsConnection!
	"""
	Title of the leaderboard
	"""
	title: LocalizedString!
	"""
	Total approved items for this period and type
	"""
	totalApprovedItems: Int!
	"""
	Total contributors for this period and type
	"""
	totalContributors: Int!
	"""
	Year of leaderboard if appropriate (null for ALL_TIME periods)
	"""
	year: Int
}

type ContributorLeaderboardConnection {
	edges: [ContributorLeaderboardEdge]!
	pageInfo: PageInfo!
}

type ContributorLeaderboardEdge {
	cursor: ID!
	node: ContributorLeaderboard!
}

type ContributorLeaderboardPeriodType {
	id: ContributorLeaderboardPeriodTypeId!
}

enum ContributorLeaderboardPeriodTypeId {
	ALL_TIME
	MONTH
	YEAR
}

type ContributorLeaderboardRank {
	"""
	Leaderboard this ranking is sourced from
	"""
	leaderboard: ContributorLeaderboard!
	"""
	Rank position and counts
	"""
	ranking: ContributorRank!
}

type ContributorLeaderboardRankConnection {
	edges: [ContributorLeaderboardRankEdge]!
	pageInfo: PageInfo!
}

type ContributorLeaderboardRankEdge {
	cursor: ID!
	node: ContributorLeaderboardRank!
}

input ContributorLeaderboardRankingsFilter {
	"""
	Limit the returned and sorted results to a maximum rank to support top ranking queries
	"""
	maximumRank: Int
}

type ContributorLeaderboards {
	"""
	Returns page of all leaderboards in reverse chronological order
	"""
	all(after: ID, first: Int): ContributorLeaderboardConnection!
	"""
	Returns the all-time leaderboard
	"""
	allTime: ContributorLeaderboard
	"""
	Returns the leaderboard for a specific month
	"""
	month(month: Int!, year: Int!): ContributorLeaderboard
	"""
	Returns page of monthly leaderboards in reverse chronological order
	"""
	months(
		after: ID
		filter: ContributorLeaderboardsByMonthFilter
		first: Int
	): ContributorLeaderboardConnection!
	"""
	Returns the leaderboard for a specific year
	"""
	year(year: Int!): ContributorLeaderboard
	"""
	Returns page of yearly leaderboards in reverse chronological order
	"""
	years(
		after: ID
		filter: ContributorLeaderboardsByYearFilter
		first: Int
	): ContributorLeaderboardConnection!
}

input ContributorLeaderboardsByMonthFilter {
	"""
	Optionally filter by leaderboards for specific months
	"""
	months: [Int]
	"""
	Optionally filter by leaderboards for specific years
	"""
	years: [Int]
}

input ContributorLeaderboardsByYearFilter {
	"""
	Optionally filter by leaderboards for specific years
	"""
	years: [Int]
}

type ContributorRank {
	"""
	Number of contribution items submitted and subsequently approved
	"""
	approvedItems: Int!
	"""
	Difference in items contributed from last period
	"""
	approvedItemsDelta: Int
	"""
	Contributor ranked at this position, null means they are anonymous
	"""
	contributor: Contributor
	id: ID!
	"""
	Numerical rank position, where 1 is the contributor with the highest approved contribution items.
	"""
	rank: Int!
	"""
	Difference in rank from last period
	"""
	rankDelta: Int
}

type ContributorRankEdge {
	cursor: ID!
	node: ContributorRank!
}

type ContributorRankingsConnection {
	edges: [ContributorRankEdge]!
	pageInfo: PageInfo!
}

input ContributorRankingsFilter {
	"""
	User id of contributor to filter on
	"""
	contributorId: ID!
	"""
	Optionally filter by rankings for leaderboards for specific months
	"""
	months: [Int]
	"""
	Returns ranking for leaderboards of all period types if omitted
	"""
	period: [ContributorLeaderboardPeriodTypeId]
	"""
	Optionally filter by rankings for leaderboards for specific years
	"""
	years: [Int]
}

input ContributorRankSort {
	by: ContributorRankSortBy
	order: SortOrder
}

enum ContributorRankSortBy {
	"""
	Sort contributors by delta in their number of approved items from the previous period
	"""
	APPROVED_ITEMS_DELTA
	"""
	Sort contributors by approved contribution item count (i.e number of items accepted into the IMDb catalog)
	"""
	RANK
	"""
	Sort contributors by delta in their rank from the previous period
	"""
	RANK_DELTA
}

"""
Closed interval of a countable value
"""
input CountInterval {
	max: Int
	min: Int
}

type CountriesOfOrigin {
	countries(limit: Int): [CountryOfOrigin]!
	"""
	DEPRECATED - DO NOT USE
	"""
	language: DisplayableLanguage!
}

enum Country {
	AD
	AE
	AF
	AG
	AI
	AL
	AM
	AN
	AO
	AQ
	AR
	AS
	AT
	AU
	AW
	AX
	AZ
	BA
	BB
	BD
	BE
	BF
	BG
	BH
	BI
	BJ
	BL
	BM
	BN
	BO
	BQ
	BR
	BS
	BT
	BV
	BW
	BY
	BZ
	CA
	CC
	CD
	CF
	CG
	CH
	CI
	CK
	CL
	CM
	CN
	CO
	CR
	CS
	CU
	CV
	CW
	CX
	CY
	CZ
	DE
	DJ
	DK
	DM
	DO
	DZ
	EC
	EE
	EG
	EH
	ER
	ES
	ET
	FI
	FJ
	FK
	FM
	FO
	FR
	GA
	GB
	GD
	GE
	GF
	GG
	GH
	GI
	GL
	GM
	GN
	GP
	GQ
	GR
	GS
	GT
	GU
	GW
	GY
	HK
	HM
	HN
	HR
	HT
	HU
	ID
	IE
	IL
	IM
	IN
	IO
	IQ
	IR
	IS
	IT
	JE
	JM
	JO
	JP
	KE
	KG
	KH
	KI
	KM
	KN
	KP
	KR
	KW
	KY
	KZ
	LA
	LB
	LC
	LI
	LK
	LR
	LS
	LT
	LU
	LV
	LY
	MA
	MC
	MD
	ME
	MF
	MG
	MH
	MK
	ML
	MM
	MN
	MO
	MP
	MQ
	MR
	MS
	MT
	MU
	MV
	MW
	MX
	MY
	MZ
	NA
	NC
	NE
	NF
	NG
	NI
	NL
	NO
	NON_US
	NP
	NR
	NU
	NZ
	OM
	PA
	PE
	PF
	PG
	PH
	PK
	PL
	PM
	PN
	PR
	PS
	PT
	PW
	PY
	QA
	RE
	RO
	RS
	RU
	RW
	SA
	SB
	SC
	SD
	SE
	SG
	SH
	SI
	SJ
	SK
	SL
	SM
	SN
	SO
	SR
	SS
	ST
	SV
	SX
	SY
	SZ
	TC
	TD
	TF
	TG
	TH
	TJ
	TK
	TL
	TM
	TN
	TO
	TR
	TT
	TV
	TW
	TZ
	UA
	UG
	UM
	US
	UY
	UZ
	VA
	VC
	VE
	VG
	VI
	VN
	VU
	WF
	WS
	YE
	YT
	ZA
	ZM
	ZW
}

type CountryAttributeMetadata {
	"""
	The maximum number of values a customer can have for a country-related attribute. A null value means no limit
	"""
	limit: Int
	"""
	All valid countries
	"""
	validValues: [LocalizedDisplayableCountry]!
}

type CountryOfOrigin {
	"""
	Country of origin as displayable property
	"""
	displayableProperty: DisplayableTitleCountryOfOriginProperty!
	"""
	The country code - either an ISO 3166 code or an internally defined code if no ISO code exists for that country.
	"""
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	"""
	Display text for the country (e.g. 'United States').
	"""
	text: String!
}

type CountryWatchOptions {
	"""
	Categorized watch options available in this country
	"""
	categorizedWatchOptions: CategorizedWatchOptionsList!
	country: LocalizedDisplayableCountry!
}

"""
Crazy credit details
"""
type CrazyCredit {
	"""
	Crazy credit as a displayable article
	"""
	displayableArticle: DisplayableArticle!
	"""
	Crazy credit ID
	"""
	id: ID!
	"""
	Votes from users about whether this crazy credit item is interesting.
	"""
	interestScore: InterestScore!
	"""
	Machine translated displayable article. Returns the displayable article translated based on the language header.
	"""
	machineTranslatedDisplayableArticle: MachineTranslatedDisplayableArticle
	"""
	Machine translated crazy credit text. Returns the text field translated based on the language header.
	"""
	machineTranslatedText: MachineTranslatedMarkdown!
	"""
	The crazy credit text
	"""
	text: Markdown!
}

"""
Pagination for crazy credit types which always has a known total number of edges.
"""
type CrazyCreditConnection {
	"""
	Pages of crazy credits
	"""
	edges: [CrazyCreditEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of crazy credits
"""
type CrazyCreditEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A crazy credit node
	"""
	node: CrazyCredit!
	"""
	Position of the node in this context, eg: CrazyCredit 4 out of 46
	"""
	position: Int!
}

"""
Example: if you want to match titles of crazy credit 'Ian Underwood' OR 'Ian Underworld', PLUS
the title must contain 'incorrectly', specify anyCrazyCreditTextTerms: ['Ian Underwood', 'Ian Underworld'] plus
allCrazyCreditTextTerms: ['incorrectly']. Note that this is term match rather than
phrase match, e.g., 'Ian Underwood' would match titles that contain 'Underwood, Ian'
"""
input CrazyCreditMatchingSearchConstraint {
	"""
	A title match must contain ALL of these search terms in at least one of its crazy credits.
	"""
	allCrazyCreditTextTerms: [String]
	"""
	A title match must contain AT LEAST ONE of these search terms in at least one of its crazy credits.
	"""
	anyCrazyCreditTextTerms: [String]
}

input CreateAccountRedirectURLInput {
	"""
	Optional additional web context for URL building including the client context (i.e. ubid token)
	"""
	additionalWebAuthContext: AdditionalWebAuthContext
	"""
	Association handle of the login context for IMDb account creation i.e. imdb_us
	"""
	associationHandle: String!
	"""
	Optional URL to return to post account creation, defaults to home page if omitted
	"""
	returnURL: URL
	"""
	Optional prefilled email for use on Pro when signing in with an Amazon account email that matches an IMDb account email
	"""
	signInPrefilledEmail: String
}

type CreateAccountRedirectURLOutput {
	"""
	Redirect URL for user account creation
	"""
	redirectURL: URL!
}

type CreativeSize {
	"""
	The height of the creative in pixels
	"""
	height: Int!
	"""
	The width of the creative in pixels
	"""
	width: Int!
}

"""
Credit details. Open question: do we want to add a persistent ID for credits?
Without this we require a nameID, titleID and Category to uniquely identify it.
"""
interface Credit {
	"""
	Category (e.g. 'Producer').
	Open question: the name of this field comes from TitleCreditsBaseV5. Should we change it?
	"""
	category: CreditCategory!
	"""
	The person credited
	"""
	name: Name!
	"""
	The title on which this credit appears
	"""
	title: Title!
}

input CreditAggregation {
	"""
	Defines how credits are aggregated together into a single CreditV2 result.

	Aggregation allows clients to choose the level of detail to show for credits, avoiding repetition of the same
	named individual across many roles or appearances.

	Parameter is modelled as a string to allow for altering the list of allowed values over time. Supported values are:

	NAME: All credits for a single name on a single title are aggregated together.
	This supports experiences like Filmmakers tab on pro.imdb.com, where all work by a name within a category is
	shown together.
	For example, see Hugh Wilson in https://pro.imdb.com/title/tt0116130/filmmakers.

	NAME_WITHOUT_ORDERABLE_GROUPS: All credits for a single name on a single title are aggregated together,
	except for groupings that support hierarchies (for example the Writer grouping). For these, aggregation is disabled.
	This allows for the hierarchy of writer credits to be vended, capturing a single writer's individual contributions
	to a title in the correct order.
	For example, see Hugh Wilson in https://www.imdb.com/title/tt0116130/fullcredits/#writer.

	NAME_AND_GROUPING: All credits of the same grouping and the same single name on a single title are aggregated together.
	"""
	aggregationType: String
}

interface CreditAttribute {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

type CreditCategory {
	"""
	Unique ID for this category within IMDb's catalog systems.
	Allows lookup of category metadata for later display, for example for localising credits fetched previously
	and stored offline.
	This ID is stable across languages, unlike the `id` field.
	"""
	categoryId: String
	"""
	A list of the most relevant credited roles for this name credit category e.g 'lighting rigger'
	"""
	featuredCreditedRoles(limit: Int!): [DisplayableFeaturedCreditedRole]
	"""
	Opaque ID, suitable for caching this concept.
	As an ID of a LocalizedDisplayableConcept, the ID must change based on the language of the returned content.
	This is to ensure that caches can distinguish between different languages of the same content.
	"""
	id: ID!
	language: DisplayableLanguage!
	"""
	Displayable text for this category.
	"""
	text: String!
	"""
	The set of traits associated with this name credit category.
	Supported values are: 'CAST_TRAIT', 'CREW_TRAIT', 'ADDITIONAL_APPEARANCES_TRAIT'.
	For details see CreditCategoryTrait type.
	"""
	traits: [String]
}

"""
Arg for the variant of the credit category display label
"""
enum CreditCategoryDisplayVariant {
	PLURAL
	SINGLE
	TITLE_HEADING
}

type CreditCategorySummary {
	"""
	The credit category
	"""
	category: CreditCategory!
	"""
	The total number of credits for the category
	"""
	total: Int!
}

"""
Enumeration of credit category traits that are allow-listed for use in credit field filters.

Note that response types will vend traits as strings, not CreditCategoryTrait values. This is to avoid risk of
breaking clients when new values are added to the enum.
"""
enum CreditCategoryTrait {
	"""
	Identifies categories where the person appears as themselves in a on-screen
	capacity. Specifically, their Self role is not captured in a more specific
	category like Host or Judge.
	"""
	ADDITIONAL_APPEARANCES_TRAIT
	"""
	Identifies categories that make up the 'Cast' of a title
	"""
	CAST_TRAIT
	"""
	Identifies categories that make up the 'Crew' of a title
	"""
	CREW_TRAIT
	"""
	Identifies categories for crew typically responsible for the creative shape of
	a feature movie. Not exhaustive, intended as a summary of credits only.
	"""
	MAJOR_CREATIVE_INPUT_TRAIT
	"""
	Identifies categories that have been newly added to the catalog
	"""
	RECENTLY_ADDED_TRAIT
	"""
	Identifies categories where the name is in a 'Self' role on the title
	"""
	SELF_TRAIT
	"""
	Identifies categories where the name is in a 'Thanks' role on the title
	"""
	THANKS_TRAIT
	"""
	Identifies categories that are a catch-all of credits that do not belong in
	the taxonomy of other categories. Allows clients to put less emphasis on the
	category as a signifier of the work done by a name on a title.
	"""
	UNCATEGORIZED_TRAIT
	"""
	Identifies categories that are still in testing phase and should be hidden
	from public-facing interfaces such as search filters, dropdowns, and selection
	controls. The presence of this trait indicates the category exists in the
	system but is not yet ready for general availability in user experiences.
	"""
	UNDERGOING_TESTING_TRAIT
}

"""
Pagination for Credit type on a Title.
"""
type CreditConnection {
	"""
	Indicates whether the requested filmography data accurately reflects the onscreen cast or crew credits
	"""
	completenessStatus(
		filter: CreditsCompletenessStatusFilter
	): CreditsCompletenessStatus
	"""
	Pages of credits
	"""
	edges: [CreditEdge]!
	"""
	How the credits are ordered
	"""
	orderBy: CreditsOrderedBy
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Restriction related information if exists for the connection
	"""
	restriction: CreditRestriction
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

type CreditedAsCreditAttribute implements CreditAttribute {
	creditedAs: String!
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

"""
Example: if you want to match titles with companyIds ('co0764707' OR 'co0854530') PLUS 'co0005073' PLUS NOT 'co0921192',
specify anyCompanyIds: ['co0764707', 'co0854530'] plus allCompanyIds: ['co0005073'] plus excludeCompanyIds: ['co0921192']
"""
input CreditedCompanySearchConstraint {
	"""
	A title match must contain ALL of these companies
	"""
	allCompanyIds: [ID]
	"""
	Used alongside the `anyCompanyIds` and/or `allCompanyIds` fields to match titles that have company credits for
	the companyId(s) AND those credits are for ONE OF the categories specified.

	This has no effect on `excludeCompanyIds`. Throws `GRAPHQL_VALIDATION_FAILED` if the field is used without
	passing in any companyIds (`anyCompanyIds` and/or `allCompanyIds`).

	Example: To match titles where co0005073 is the production company, you would specify allCompanyIds: ['co0005073']
	plus anyCompanyCategories: ['production']
	"""
	anyCompanyCategories: [ID]
	"""
	A title match must contain AT LEAST ONE of these companies
	"""
	anyCompanyIds: [ID]
	"""
	A title match must contain NONE of these companies

	If the same input is specified in the any/all and exclude constraints,
	then the results should be empty.
	"""
	excludeCompanyIds: [ID]
}

"""
Page of credits
"""
type CreditEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A Credit node
	"""
	node: Credit!
	"""
	Position of the node in this context, eg: Credit 4 out of 31
	"""
	position: Int!
}

"""
Example: if you want to match titles with nameIds ('nm0634240' OR 'nm5397459') AND 'nm1139258' AND NOT 'nm0331516',
specify anyNameIds: ['nm0634240', 'nm5397459'], allNameIds: ['nm1139258'], and excludeNameIds: [nm0331516']
"""
input CreditedNameConstraint {
	"""
	A title match must contain ALL of these names
	"""
	allNameIds: [ID]
	"""
	A title match must contain AT LEAST ONE of these names
	"""
	anyNameIds: [ID]
	"""
	A title match must contain NONE of these names

	If the same input is specified in the any/all and exclude constraints,
	then the results should be empty.
	"""
	excludeNameIds: [ID]
}

"""
Filter of Credited or Uncredited credits
"""
enum CreditedOrUncreditedFilter {
	ALL_CREDITS
	CREDITED_ONLY
	UNCREDITED_ONLY
}

type CreditedRole {
	"""
	List of attributes related to this credited role.
	Attributes are ordered based on a fixed ordering of attribute type. We prioritise contextual information
	(like what segments of a title the role relates to and whether the person only appears in archive footage) over
	credit accuracy markers (like whether the person is uncredited, rumored, or does not appear onscreen while still
	receiving a credit).
	"""
	attributes(
		"""
		Number of items to return. Default is unbounded, returning all attributes for this credit.
		"""
		limit: Int
	): [CreditAttribute]
	"""
	The category this role belongs to.
	When retrieving credits in the context of a name (such as name.knownForV2 field), the text's natural gender
	is chosen to match the gender of the person, if known.
	category.categoryId is always populated when accessed within this context.
	"""
	category: CreditCategory!
	"""
	For appearance credits, a breakdown of multiple characters into individual strings. This field will be populated
	with at least one value for all appearance credits, even where the credited role for that credit is blank (in
	which case we vend the category text as the role).
	For crew credits, this field will always be null.
	Allows clients to display a subset of characters.
	"""
	characters(
		"""
		Cursor from which to begin providing characters from.
		"""
		after: ID
		"""
		Whether to disambiguate Self-category credits where it could be unclear if this is a self-role or not.
		For example, on a title with a mix of acting and self appearances,
		such as Saturday Night Live (1975-) (https://www.imdb.com/title/tt0072562/).

		Example: for a character name of "Host" in the Self category
		- if disambiguateSelf = true, the name is returned as "Self - Host"
		- otherwise, the name is returned as "Host"
		Any character name including "Himself/Herself/Themselves" will not have a prefix added.
		Where there are no character names, we will return a single name of "Self".
		"""
		disambiguateSelf: Boolean = true
		"""
		Number of items to return. Default is 10. Max is 2500.
		"""
		first: Int
	): CharacterConnection
	"""
	Details of per-episode credits for when the parent credit is a series.
	Only supports fetching an initial page of credits. To continue pagination, use name.episodeCredits (yet to be built).
	"""
	episodeCredits(
		"""
		Allows for filtering in or out of credits based on several criteria.
		See EpisodeCreditsFilter for details.
		If not specified, no episode-level filtering is performed.

		Filters specified at a parent field level always take effect.
		For example if calling title.creditsV2 with filter:{creditLevelFilter: { includeTraits:["CAST_TRAIT"] }},
		then only credits on episodes that feature this trait will be returned.
		"""
		filter: EpisodeCreditsFilter
		"""
		Number of items to return. Max is 100.
		"""
		first: Int
		"""
		Default is ascending by RELEASE_DATE sortBy. See EpisodeCreditsSortBy for details.
		"""
		sort: EpisodeCreditsSort
	): EpisodeCreditConnection
	"""
	Opaque ID, suitable for caching this concept.
	"""
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage
	"""
	Role this person performed on this title. Optional, as some roles are implicit or duplicate the category of the credit.
	See CreditV2 for details of how aggregation of data applies to this field.
	"""
	text(
		"""
		Whether to disambiguate self credits where it could be unclear if this is a self-role or not.
		For example, on a title with a mix of acting and self appearances,
		such as Saturday Night Live (1975-) (https://www.imdb.com/title/tt0072562/).

		Example: for a character name of "Host" in the Self category
		- if disambiguateSelf = true, the name is returned as "Self - Host"
		- otherwise, the name is returned as "Host"
		Any credited role including "Himself/Herself/Themselves" will not have a prefix added.
		Where there is no credited role text, we will return "Self".
		"""
		disambiguateSelf: Boolean = true
	): String
}

type CreditedRoleConnection {
	"""
	Pages of CreditedRole
	"""
	edges: [CreditedRoleEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of CreditedRole
"""
type CreditedRoleEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	Details of the CreditedRole
	"""
	node: CreditedRole!
}

input CreditedRoleSort {
	"""
	Captures the ordering of credited roles within a CreditV2. This ensures consistent ordering of roles
	when a name has performed many different roles on a title. This is often seen on long-running series.

	This is modelled as a string to allow for altering the list of allowed values over time. Supported values are:

	CREDITED_ROLE_IMPORTANCE: Credit ordering follows this priority sequence:
	1. Profession - The credit categories associated with the name's professions, in profession order
	2. Latest release date - For series, most recent episode release date that this credit applies to
	3. Episode count - For a series, higher episode counts appear first
	4. Credited status - Credited roles take precedence over uncredited
	5. Role - Alphabetical comparison of the credited role text
	6. Title ID - A final tie-break by episode title ID to ensure consistent ordering (largest first)

	CREDITED_ROLE_IMPORTANCE is the default.

	The sort order can be controlled using the order parameter.
	"""
	by: String
	order: SortOrder
}

type CreditedWithNameEdge {
	"""
	Cursor ID of the credited with name
	"""
	cursor: ID!
	"""
	A credited with name item
	"""
	node: CreditedWithNameItem!
	"""
	Position of the node in this context, eg: CreditedWithNameEdge 2 out of 10
	"""
	position: Int!
}

type CreditedWithNameItem {
	"""
	The credited with name
	"""
	name: Name!
}

type CreditedWithNamesConnection {
	"""
	A page of credited with names
	"""
	edges: [CreditedWithNameEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of credited with names
	"""
	total: Int!
}

"""
Saved credit filter preferences for Pro Name credits page.
"""
type CreditFilterPreferences {
	"""
	Genre IDs excluded from credits. Human-readable genre slugs (e.g. 'Short', 'Documentary'). Empty if none excluded.
	"""
	excludedGenres: [ID]!
	"""
	Credit grouping URNs excluded from credits (e.g.
	'amzn1.imdb.concept.name_credit_category.d6017bdb-...'). Empty if none excluded.
	"""
	excludedRoles: [ID]!
	"""
	The grouping mode for credits. Valid values: "status", "type", "category".
	Null if no preference has been saved, or if the user is not authenticated.
	"""
	groupBy: ID
}

"""
Credits are assigned to exactly one category, but may be displayed in different
groupings to better represent talent contributions. These groupings provide
flexible presentation of credits that may differ from their strict categorical
classification.
"""
type CreditGrouping {
	"""
	ID for this credit grouping.
	Stable identifier that remains constant across localizations, used for filtering and pagination.
	"""
	groupingId: String!
	"""
	Opaque ID, suitable for caching this concept.
	"""
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

"""
Pagination for NameCreditGrouping type
"""
type CreditGroupingConnection {
	"""
	Pages of NameCreditGroupings
	"""
	edges: [CreditGroupingEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of groupings in the entire set
	"""
	total: Int!
}

"""
Single entry of NameCreditGroupings pagination
"""
type CreditGroupingEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	node: CreditGroupingNode!
	"""
	Position of the node in this context, eg: CreditGrouping 4 out of 31
	"""
	position: Int!
}

type CreditGroupingNode {
	"""
	Credits within this grouping, aggregated by title.
	Multiple roles on the same title are combined into a single CreditV2 with multiple CreditedRole entries.
	"""
	credits(
		"""
		Number of CreditV2 values to fetch.
		Maximum per request is 250.
		"""
		first: Int!
	): CreditV2Connection
	"""
	The credit grouping information with optional text formatting.
	Supports localization and different text formats (DEFAULT, SHORT).
	"""
	grouping(filter: GroupingTextInput): CreditGrouping!
	"""
	Captures details of the hierarchy of credits, for credit groups that support a hierarchy.
	Only populated in title-sourced responses such as title.creditsV2.
	"""
	hierarchy: CreditHierarchy
}

input CreditGroupingSort {
	"""
	Capture the order of CreditGrouping items in a listing.

	This is modelled as a string to allow for altering the list of allowed values over time. Supported values are:

	CREDIT_GROUPING_IMPORTANCE: Credit grouping ordering follows this priority sequence:
	1. Profession - The credit categories associated with the name's professions, in profession order
	2. Fixed grouping order for additional and passive appearances - Credit
	groupings that capture when a name is appearing as Self,
	or where they were not actively involved in the title (such as Thanks or
	Archive credits) are moved to the end of the ordering
	3. Credit count - The number of credits a name has within the grouping, largest first
	4. Fixed grouping order - A fixed order, presenting groupings in an order similar to that seen on movie or TV
	end credits.

	CREDIT_GROUPING_IMPORTANCE is the default.

	The sort order can be controlled using the order parameter (ASC/DESC). When DESC (default), primary professions
	and high-credit-count groupings appear first. When ASC, passive appearances and low-credit-count groupings appear first.
	"""
	by: String
	order: SortOrder
}

"""
Captures details of the hierarchy of credits, for credit groups that support a hierarchy.
Two examples where this exists today:
* Writer (https://help.imdb.com/article/contribution/filmography-credits/writers/GPLAT3NTCGA67A6R#)
  * This category supports a three-level hierarchy. The levels are named
"WriterFamily", "WriterTeam", and "WriterTeamMember".
  * Documentation below shows examples of this hierarchy
* Cast (https://help.imdb.com/article/contribution/filmography-credits/how-are-cast-credits-ordered-why-don-t-the-main-stars-appear-at-the-top-of-the-cast/G39K5N4YYV2QJ4GR)
  * These credits are vended as a collection of ordered credits, followed by a collection of unordered credits.
    Our hierarchy here is therefore one level deep, with up to two expected levelKeys at that level.
  * Concretely we would see a hierarchy of the form:

    [Root]
      /\
     /  \
    A    B        # CastGrouping
   /|    /|\
  / |   / | \
 C  D  E  F  G    # CastMember

 * In this example, C and D would be cast members with an order number associated with them. E, F, G would have no
   associated order, and so are in the second CastGrouping.
"""
type CreditHierarchy {
	"""
	Informs clients when to use the filterKeys in hierarchy levelDetails for
	the given filter. hierarchyFilter is an enum for available filters that a hierarchy can use.
	So for locale it would be `hierarchyFilter: "LOCALE"`. When the hierarchyFilter field is null
	then the hierarchy is not filterable.
	"""
	hierarchyFilter: HierarchyFilter
	"""
	For each non-leaf node in our hierarchy, provide a count for the number of child nodes.
	Allows a client to look at an individual credit record and understand its relative position in the hierarchy,
	without knowledge of the hierarchy itself.
	For example, given the following hierarchy:

	[Root]
	|
	A
	| \
	B  C
	|  | \
	D  E  F

	If a client is rendering the credit F, they can determine that F is the last credit within the C subtree,
	because:
	* This group of credits is vended with `hierarchy` containing:
	  `{ "levels": ["WriterFamily", "WriterTeam", "WriterTeamMember"],
	     "levelDetails": [
	       { "levelsKey": "WriterFamily[0].WriterTeam[0]", childCount: 1 },
	       { "levelsKey": "WriterFamily[0].WriterTeam[1]", childCount: 2 },
	     ]
	   }`
	* F is vended with `hierarchyDetail` of
	  `[ { "level": "WriterFamily", "position": 0 }, { "level": "WriterTeam",
	"position": 1 }, { "level": "WriterTeamMember", "position": 1 } ]`
	* A client can see that within "WriterFamily[0].WriterTeam[1]", F's WriterTeamMember-level position (2) matches the
	  total childCount for that node in the `hierarchy` data.
	"""
	levelDetails: [CreditHierarchyLevelDetail]
	"""
	Ordered list of levels. Allows a client to understand the content of `levelDetails`,
	and the `hierarchyDetails` against a grouped credit edge (see CreditGroupingEdge).
	For the Writer group this would return `["WriterFamily", "WriterTeam", "WriterTeamMember"]`.
	"""
	levels: [String]!
}

"""
Captures an individual CreditV2's position within a credit group's hierarchy.
This type captures the position within a single level of the hierarchy.
An array of these can identify a credit's unique position within a multi-level hierarchy.
Values are zero-indexed.
For example, for the Writer credit group (with levels "WriterFamily", "WriterTeam", "WriterTeamMember"),
and hierarchy of shape:

      [Root]
      |
      A          # WriterFamily
     / \
    /   \
   B     C       # WriterTeam
  /|    /|\
 / |   / | \
D  E  F  G  H    # WriterTeamMember

Node "F" would have values:
`[
   { "level": "WriterFamily", "position": 0 },
   { "level": "WriterTeam", "position": 1 },
   { "level": "WriterTeamMember", "position": 0 }
 ]`
"""
type CreditHierarchyDetail {
	"""
	Name of the level of hierarchy we are giving a position for.
	"""
	level: String!
	"""
	This credit's relative position within this level of the hierarchy.
	Values are zero-indexed.
	Considers sibling nodes under the same parent, and not other nodes from the same level but
	with different parents.
	"""
	position: Int!
}

type CreditHierarchyLevelDetail {
	"""
	Count of the number of children beneath this node in the hierarchy.
	Does not count children of children.
	In the example above, the childCount for node "C" is 2 (counting nodes "E" and "F").
	"""
	childCount: Int!
	"""
	A key that should be used as a filter value when `hierarchyFilter` is not null.
	The filterKey will be unique across the levelDetails. This field may be empty for hierarchies that
	are not used as filters like the cast and writer hierarchies.
	"""
	filterKey: String
	"""
	Unique ID for this displayable text.
	ID will encode the titleId for this title as well as `levelsKey`, as each
	title can have different headers for their hierarchies.
	Required as part of LocalizedDisplayableConcept interface to support caching.
	"""
	id: ID!
	language: DisplayableLanguage!
	"""
	Identifies a single node in a hierarchy, in xpath-like syntax
	For the three-level Writer hierarchy, a levelsKey value would be "WriterFamily[0].WriterTeam[1]".
	This corresponds to node "C" in the following hierarchy:

	[Root]
	|
	A
	| \
	B  C
	|  | \
	D  E  F

	We only represent nodes with children with a CreditHierarchyLevelDetail. Leaf nodes would not be
	expected to have a value, since their childCount is always zero.
	Null values are possible, for credit groups that have no inherent hierarchy but do have text to associate with
	the credits. For example in Writers grouping, a series of hierarchical credits can be followed by unordered credits.
	These are vended with no levelsKey but with other fields like text populated.
	"""
	levelsKey: String
	"""
	Descriptive text for this level of the hierarchy.
	For Cast, we could expect en-US text of the form "(in credits order) verified as complete" and
	"Rest of cast listed alphabetically". Some levels will have no text to show, in which case we vend an
	empty text, not null.
	"""
	text: Markdown!
}

"""
Filters that apply to credits at the contributed credit level.
Each specified filter is AND-ed together.
For filters that accept a list of values, the filter is an OR across that list, so any
credit matching one value meets the filter's criteria.
"""
input CreditLevelCreditsFilterSet {
	"""
	Filter for credits with attributes of "(archive footage)" or "(archive sound)".
	By default, all credits are included, including those marked as "(archive footage)" or "(archive sound)".
	"""
	archived: ArchivedOrUnarchivedFilter
	"""
	Filter for credited or uncredited credits only.
	By default, all credits are included, including those marked as "(uncredited)".
	"""
	credited: CreditedOrUncreditedFilter
	"""
	Filter for credits with specified role only.
	Clients would typically fetch the credited role first from name.creditsV2.creditedRolesConnection then use the role text
	to filter credits in subsequent calls.
	By default, all credits are included.
	"""
	creditedRole: String
	"""
	Filter out specific credit groupings. Individual grouping IDs are found in name.creditGroupings responses.
	Clients would often make a call to name.creditGroupings, retrieve an initial page of credits per grouping, then
	fetch additional pages using this API.
	Cannot be used in conjunction with "groupings": a contributed credit can only have one of these values;
	if you specify values to include and exclude, the behaviour for unspecified values is ambiguous.
	By default, credits for all groupings are included.

	Note https://quip-amazon.com/li5NAI0grla9/Guidance-on-credit-category-traits - we encourage using excludeTraits
	instead
	"""
	excludeGroupings: [ID]
	"""
	Drop credits that have at least one of the given traits on their credit category.
	By default, credits are not filtered by trait.
	"""
	excludeTraits: [CreditCategoryTrait]
	"""
	Filter for specific credit groupings. Individual grouping IDs are found in name.creditGroupings responses.
	Clients would often make a call to name.creditGroupings, retrieve an initial page of credits per grouping, then
	fetch additional pages using this API.
	Cannot be used in conjunction with "excludeGroupings": a contributed credit can only have one of these values;
	if you specify values to include and exclude, the behaviour for unspecified values is ambiguous.
	By default, credits for all groupings are included.

	Note https://quip-amazon.com/li5NAI0grla9/Guidance-on-credit-category-traits - we encourage using includeTraits
	instead
	"""
	groupings: [ID]
	"""
	Keep credits that have at least one of the given traits on their credit category.
	By default, credits are not filtered by trait.
	"""
	includeTraits: [CreditCategoryTrait]
	"""
	Filter for credits with given locale.
	Credits will be filtered by a given locale or fallback to the preferred language and if no
	preferred language or preferred language credits then "en-US". This filter when given will
	trigger a hierarchy to be generated for supported credit groupings that can be filtered by locales.

	If filter value is "DEFAULT" then credits will be filtered by preferred language given in the headers.
	The "DEFAULT" filter is to be used on page load or the initial query for the credits.

	By default, credits are not filtered by locale.

	When filter value is "DEFAULT"
	Fallback Pattern: preferred language -> "en-US" -> empty credit response.

	When filter value is populated with a locale
	Fallback Pattern: filter.creditLevelFilters.locale -> empty credit response
	"""
	locale: ID
	"""
	Filter the returned groupings based on their relationship to the name's listed professions.
	By default, credits are not filtered by professions.
	"""
	professions: ProfessionsFilter
}

"""
Information about restrictions applied to Credits
"""
type CreditRestriction {
	"""
	Explanation(s) for the restriction
	"""
	explanations: [RestrictionExplanation]!
	reasons: [ContentRestrictionReason]!
	"""
	DEPRECATED: Renamed, use `reasons` instead.
	"""
	restrictionReason: [ContentRestrictionReason]!
	"""
	The total number of items if there was no restriction
	"""
	unrestrictedTotal: Int
}

type CreditsCompletenessStatus {
	id: ID!
	text: String!
}

input CreditsCompletenessStatusFilter {
	"""
	Excludes by returning null. Uses the following values: 'unknown', 'complete', 'complete_and_verified'
	"""
	excludeValues: [String]
}

"""
Captures several functional parameter choices in a single value, enabling consistent results across many callsites.

A CreditsModePreset can provide settings for filter, sort, and other functional inputs.
Clients can still provide additional filters, but the preset may override or conflict with them.
"""
enum CreditsModePreset {
	"""
	Matches the behaviour of NONE
	"""
	KNOWN_FOR
	"""
	Contributed credits are aggregated to the category level.
	If a name has performed several different Producer roles across episodes in a TV series, these would be presented
	as a single CreditedRole within the CreditV2 response value for that title.
	If a name has performed several categories of work on a title (for example producer and actor), only the first
	of these is presented. This allows the CreditV2 shape to represent all the work done within a single category
	for a single title.
	"""
	KNOWN_FOR_WITH_CATEGORY_AGGREGATION
	"""
	No preset is used
	"""
	NONE
}

type CreditsOrderedBy {
	id: ID!
	text: String!
}

"""
Details of a credit for a name on a title. Distinct from Company Credits or Crazy Credits.
Successor to Credit type.
Denotes the role(s) a person had on a title.
Fields within this record may be aggregations or summaries of work done, depending on how the data was requested.

When requesting credits for a series, roles performed on the episodes of that series are rolled up to the series level.
Each rolled-up role comes with its own listing of the episodes that the role was performed on.
If the attributes of a role are not shared across all episodes, we drop that detail.
* Example: Kaley Cuoco (nm0192505) was credited as Kaley Cuoco-Sweeting for some but not all episodes of
The Big Bang Theory (tt0898266). When viewing credits at the series level, this attribute is dropped. The attribute
is still present at an episode level.

Characters and CreditedRoles for a series are ordered by most recent value first. This ensures a person's most recent
contribution to a title (and so the work they are most likely to demonstrate their skillset) is shown first.
This also helps to highlight the roles that users are likely to be exploring, particularly for long-running shows
where acting roles change over time (for example, Saturday Night Live).
Note that the ordering of these fields differs when using name.knownForV2 (see its documentation for details).

Appearance credits (such as acting and self-appearances) are contributed to IMDb's catalog as a single credit per title.
* Example: Ryan Reynolds (nm0005351) plays Deadpool and Nicepool in Deadpool & Wolverine (tt6263850).
This is different to crew credits, where a single person can have many roles on a title, even within a single
credit category.
* Example: Hugh Wilson (nm0933505) is both screenplay and story writer for Down Periscope (tt0116130), and these are
captured in the catalog as separate credits.

The CreditV2 shape maintains this distinction. For appearance credits, the individual roles played can be accessed
individually from the `CreditV2.creditedRoles.characters` field.

When a person performs different appearance roles across episodes in a TV series, we maintain this cast-specific
difference in data shape. All appearance work is aggregated into a single CreditV2 entry, with a single
`creditedRoles.text` value that includes up to 100 roles, and all roles available in the `characters` field.
* Example: Kaley Cuoco (nm0192505) plays two different roles across episodes in The Big Bang Theory (tt0898266).
This is vended at the series level as a single credit with one `creditedRoles.text` ("Penny/Penny Hofstatder") and
two character values (["Penny", "Penny Hofstatder"]).
Character names are ordered by their frequency across episodes, then by which occurs first in release order.
"""
type CreditV2 {
	"""
	Persistent catalog identifiers linking this credit to its source entries in the catalog, if any.
	Returns null if no catalog entries are associated with this credit.
	- Single ID: a 1:1 mapping between this credit and a single catalog entry.
	- Multiple IDs: an aggregated credit combining multiple catalog entries
	  (e.g., episode credits rolled up to the series level, where each constituent episode credit
	  has its own catalog entry).
	"""
	catalogIds: [ID]
	"""
	List of credited roles and related details.
	"""
	creditedRoles(
		"""
		Cursor from which to begin providing credited roles from.
		"""
		after: ID
		"""
		Number of items to return. Default is 10. Max is 2500.
		"""
		first: Int
	): CreditedRoleConnection
	"""
	Details of per-episode credits for when the parent credit is a series.
	Only supports fetching an initial page of credits. To continue pagination, use name.episodeCredits (yet to be built).
	"""
	episodeCredits(
		"""
		Default is unfiltered.
		"""
		filter: EpisodeCreditsFilter
		"""
		Number of items to return. Default is 10. Max is 100.
		"""
		first: Int
		"""
		Default is ascending by release date, series number, then episode number.
		"""
		sort: EpisodeCreditsSort
	): EpisodeCreditConnection
	"""
	Details of this credit position in its group's hierarchy, if any.
	Is only present when fetching credit data from a title source.
	See CreditHierarchyDetail for details.
	"""
	hierarchyDetails: [CreditHierarchyDetail]
	"""
	Opaque ID, suitable for caching this concept.
	"""
	id: ID!
	"""
	The person credited
	"""
	name: Name!
	"""
	The title on which this credit appears
	"""
	title: Title!
}

"""
Pagination for CreditV2 type
"""
type CreditV2Connection {
	"""
	Pages of credits
	"""
	edges: [CreditV2Edge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of credits
"""
type CreditV2Edge {
	cursor: ID!
	"""
	A Credit node
	"""
	node: CreditV2!
	"""
	Position of the node in this context, eg: CreditV2 4 out of 31. 1-indexed values
	"""
	position: Int!
}

"""
Crew details
"""
type Crew implements Credit {
	attributes(limit: Int): [CreditAttribute]
	category: CreditCategory!
	episodeCredits(
		after: ID
		before: ID
		filter: EpisodeCreditsFilter
		first: Int
		last: Int
		sort: EpisodeCreditsSort
	): EpisodeCrewConnection
	"""
	List of job and related Attributes
	"""
	jobDetails(limit: Int): [CrewJobDetails]
	"""
	List of jobs (e.g. 'Executive Producer')
	Open questions: the name of this field comes from TitleCreditsBaseV5. Should we change it?
	"""
	jobs(limit: Int): [CrewJob]
	name: Name!
	title: Title!
}

type CrewJob {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

type CrewJobDetails {
	"""
	List of attributes related to the Job
	"""
	attributes(limit: Int): [CreditAttribute]
	"""
	Crew Job as a standardized displayableProperty
	"""
	displayableProperty: CrewJobDisplayableProperty
	"""
	Job information
	"""
	job: CrewJob!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage
}

type CrewJobDisplayableProperty {
	"""
	The crew job text qualifiers in Markdown such as uncredited, voice etc.
	"""
	qualifiersInMarkdownList: [Markdown]
	"""
	Displayable text for Job and attribute
	"""
	value: Markdown!
}

"""
Parameters used to define how an image should be cropped
"""
type CroppingParameters {
	"""
	The height of the cropped image
	"""
	height: Int!
	"""
	The width of the cropped image
	"""
	width: Int!
	"""
	The x-coordinate by which to offset the crop box
	"""
	xOffset: Int!
	"""
	The y-coordinate by which to offset the crop box
	"""
	yOffset: Int!
}

"""
Example: if you want to match titles with production status stages
('in_development' OR 'pre_production') AND NOT 'abandoned',
specify anyProductionStageIds: ['in_development', 'pre_production'], and excludeProductionStageIds: ['abandoned']

If both anyProductionStageIds and anyGranularProductionStageIds are specified, titles must match
AT LEAST ONE from each list (AND logic between the two fields).
"""
input CurrentProductionStatusStageConstraint {
	"""
	Match titles with more granular production stage IDs.
	Production stages are more fine-grained than the standard production stage IDs.

	Example: Standard stage 'in_development' may have granular stages like
	'script_development', 'financing', 'casting'.

	Use the PRODUCTION_STATUS facet to discover available granular stage IDs.
	"""
	anyGranularProductionStageIds: [ID]
	"""
	A title match must contain AT LEAST ONE of these production status stages
	"""
	anyProductionStageIds: [ID]
	"""
	Exclude titles with more granular production stage IDs.
	Production stages are more fine-grained than the standard production stage IDs.

	Example: Standard stage 'in_development' may have granular stages like
	'script_development', 'financing', 'casting'.

	Use the PRODUCTION_STATUS facet to discover available granular stage IDs.
	"""
	excludeGranularProductionStageIds: [ID]
	"""
	A title match must contain NONE of these production status stages

	If the same input is specified in the any/all and exclude constraints,
	then the results should be empty.
	"""
	excludeProductionStageIds: [ID]
}

"""
Featured images that customer selected manually
"""
type CustomFeaturedImages {
	images: [Image]
	"""
	True if data status is EDITED
	"""
	isAdminEdited: Boolean!
	isAdminNotificationSeen: Boolean!
	"""
	True if data status is BLOCKED
	"""
	isBlocked: Boolean!
	"""
	True if custom selections within this type should override automatic ones
	"""
	isPublished: Boolean!
	"""
	True if data status is RESET
	"""
	isReset: Boolean!
	lastEdited: DateTime
	lastEditedByAdmin: DateTime
}

"""
Known for titles that customer selected manually
"""
type CustomKnownFor {
	"""
	True if data status is EDITED
	"""
	isAdminEdited: Boolean!
	isAdminNotificationSeen: Boolean!
	"""
	True if data status is BLOCKED
	"""
	isBlocked: Boolean!
	"""
	True if custom selections within this type should override automatic ones
	"""
	isPublished: Boolean!
	"""
	True if data status is RESET
	"""
	isReset: Boolean!
	lastEdited: DateTime
	lastEditedByAdmin: DateTime
	"""
	Known for titles that customer selected manually last time. Max number of titles is 4
	"""
	titles: [Title]
}

"""
Primary image that the name owner had edited
"""
type CustomPrimaryImage {
	"""
	Parameters used to define the cropping on the image
	"""
	imageEditParameters: ImageEditParameters
	"""
	The original image, without any cropping or rotation customizations applied
	"""
	originalImage: Image!
}

input DanceStylesConstraint {
	"""
	A name match must have ALL of these dance styles.
	No filtering is applied if omitted.
	"""
	allDanceStyles: [DanceStyleType]
	"""
	A name match must have AT LEAST ONE of these dance styles.
	No filtering is applied if omitted.
	"""
	anyDanceStyles: [DanceStyleType]
	"""
	A name match must not have any of these dance styles.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeDanceStyles: [DanceStyleType]
}

"""
Dance styles for talent
"""
enum DanceStyleType {
	BALLET
	BALLROOM
	BELLY
	BREAK
	CAPOEIRA
	CLOG
	CLUB_FREESTYLE
	DISCO
	FLAMENCO
	HIP_HOP
	HULA
	IRISH_DANCE
	JAZZ
	LINE
	MODERN
	POLKA
	POP_LOCKING
	ROBOT
	SALSA
	SQUARE
	SWING
	TANGO
	TAP
	WALTZ
}

"""
A date string in the ISO-8601 format: https://www.iso.org/iso-8601-date-and-time-format.html
"""
scalar Date

"""
A type to represent dates that cannot be represented in ISO 8601 format, e.g. missing year in 23 March.
"""
type DateComponents {
	"""
	The day as per Gregorian calendar.
	"""
	day: Int
	"""
	If the date is approximate, e.g. circa 370 BC.
	"""
	isApproximate: Boolean!
	"""
	If the date is before Common Era as per Gregorian calendar.
	"""
	isBCE: Boolean!
	"""
	The month of the date as per Gregorian calendar.
	"""
	month: Int
	"""
	The year of the date as per Gregorian calendar, can contain non-numerical
	wildcard to signify decade or century, e.g. 1 May 197?.
	"""
	partialYear: String
	"""
	The year of the date as per Gregorian calendar.
	"""
	year: Int
}

input DateRange {
	end: Date
	start: Date
}

"""
A datetime string in the ISO-8601 format: https://www.iso.org/iso-8601-date-and-time-format.html
(e.g. 2020-05-11T18:15:55Z)
"""
scalar DateTime

input DeathDateSearchConstraint {
	deathDateRange: DateRange!
}

"""
Matches names based on where they died.
There is no fuzzy matching.
For example: 'Venice Italy' will be matched by 'Venice' and 'Italy' but not 'Venice Ita'
"""
input DeathPlaceSearchConstraint {
	deathPlace: String!
}

enum DeletionDialogInput {
	"""
	Retrieve the dialog for confirming deletion
	"""
	CONFIRM
	"""
	Retrieve the dialog for requesting deletion
	"""
	REQUEST
}

type DeletionDialogOutput {
	deletionPrompt: LocalizedMarkdown!
	"""
	Supplementary prompt rendered in place of the deletion action.
	Only populated when the customer already has a deletion request in flight, in which case it
	carries the "deletion in progress" message with showActionButton false.
	Null otherwise, including for Amazon Account Pool customers.
	"""
	redirectionPrompt: DeletionDialogRedirectionPrompt
	"""
	For REQUEST dialogs, this will be the requestId to pass in to requestAccountDeletion
	mutation. This is not needed for other types of dialogs.
	"""
	requestId: String
}

type DeletionDialogRedirectionPrompt {
	"""
	Link to the Amazon page.
	"""
	action: RedirectLink!
	"""
	Text to instruct customers to initiate the deletion process on Amazon page.
	"""
	message: LocalizedMarkdown!
	"""
	Whether clients should show the button to redirect to the Amazon page
	"""
	showActionButton: Boolean!
}

type Demographic {
	age: Age
	country: Country
	displayText: LocalizedString!
	gender: Gender
	userCategory: UserCategory
}

"""
A single component of a demographic data value
"""
type DemographicDataComponent {
	"""
	The id of the demographic data component e.g. asian
	"""
	id: ID!
	"""
	The language of the displayable text
	"""
	language: DisplayableLanguage!
	"""
	The localized displayable text of the demographic data component e.g. Asian
	"""
	text: String!
}

"""
Common input type for filtering demographic data items
"""
input DemographicDataFilter {
	"""
	Types to exclude in the response items (mutually exclusive to 'includeTypes')
	"""
	excludeTypes: [DemographicDataTypeValue]
	"""
	Types to include in the response items (mutually exclusive to 'excludeTypes')
	"""
	includeTypes: [DemographicDataTypeValue]
	"""
	If set to true returns selfVerified items only, if set to false returns non-selfVerified items only
	"""
	selfVerified: Boolean
	"""
	Visibility levels to include in the response items
	"""
	visibility: VisibilityLevel
}

"""
A single demographic data item
"""
type DemographicDataItem {
	"""
	The selfVerified status of the demographic data item
	"""
	selfVerified: SelfVerified!
	"""
	The type of the demographic data item
	"""
	type: DemographicDataType!
	"""
	The value of the demographic data item
	"""
	value: DemographicDataValue!
}

"""
A single demographic data type
"""
type DemographicDataType {
	"""
	The id of the demographic data type e.g. gender_identity
	"""
	id: ID!
	"""
	The language of the displayable text
	"""
	language: DisplayableLanguage!
	"""
	The localized displayable text of the demographic data type e.g. Gender Identity
	"""
	text: String!
	"""
	The enum value for the demographic data type
	"""
	value: DemographicDataTypeValue!
}

"""
Enum values for the valid demographic data types
"""
enum DemographicDataTypeValue {
	BIRTH_DATE
	BIRTH_YEAR
	CITIZENSHIP
	DISABILITY
	ETHNICITY
	GENDER_IDENTITY
	IDENTIFIES_AS_DISABLED
	IDENTIFIES_AS_TRANSGENDER
	NATIONALITY
	PRONOUN
	SEXUAL_ORIENTATION
}

"""
A single demographic data value
"""
type DemographicDataValue {
	"""
	The individual components that make up the value
	"""
	components: [DemographicDataComponent]!
	"""
	The id of the demographic data value e.g. male
	"""
	id: ID!
	"""
	The language of the displayable text
	"""
	language: DisplayableLanguage!
	"""
	The localized displayable text of the demographic data value e.g. Male
	"""
	text: String!
}

input DemographicFilter {
	age: Age
	country: Country
	gender: Gender
	userCategory: UserCategory
}

input DemographicIdsConstraint {
	"""
	A name match must have ALL of these demographic IDs.
	No filtering is applied if omitted.
	Example: "amzn1.imdb.concept.gender-identity.00000000-0000-0000-0000-000000000000"
	Example: "amzn1.imdb.concept.ethnicity.00000000-0000-0000-0000-000000000000"
	"""
	allDemographicIds: [ID]
	"""
	A name match must have AT LEAST ONE of these demographic IDs.
	No filtering is applied if omitted.
	Example: "amzn1.imdb.concept.gender-identity.00000000-0000-0000-0000-000000000000"
	Example: "amzn1.imdb.concept.ethnicity.00000000-0000-0000-0000-000000000000"
	"""
	anyDemographicIds: [ID]
	"""
	A name match must not have any of these demographic IDs.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.

	Example: "amzn1.imdb.concept.gender-identity.00000000-0000-0000-0000-000000000000"
	Example: "amzn1.imdb.concept.ethnicity.00000000-0000-0000-0000-000000000000"
	"""
	excludeDemographicIds: [ID]
}

type DemographicRatings {
	aggregate: Float
	demographic: Demographic!
	voteCount: Int!
}

"""
Device info used in the display and video ads flows for apps.
This information is used upstream in Amazon Advertising systems for targeting and metrics.
"""
input DeviceInfo {
	"""
	Carrier of the device (eg. AT&T). If omitted, carrier will not be included in device info sent to ad systems.
	"""
	carrier: String
	"""
	Whether the user has limited ad tracking on their device (iOS ATT opt-out). If omitted, defaults to false (unrestricted).
	"""
	limitAdTracking: Boolean
	"""
	The make of the device the request originates from (eg. Samsung)
	"""
	make: String!
	"""
	The model of the device the request originates from (eg. Galaxy S4)
	"""
	model: String!
	"""
	The OS type of the device the request originates from (eg. iOS)
	"""
	os: String!
	"""
	The OS version of the device the request originates from (eg. 4.4.2). If omitted, OS version will not be included in device info sent to ad systems.
	"""
	osVersion: String
	"""
	Scaling Factor of the phone screen (eg. 3.0). If omitted, scaling factor will not be included in device info sent to ad systems.
	"""
	scalingFactor: String
}

"""
Required and optional parameters that come from the context of the app client.
Optional params default to null when not present.
"""
input DeviceParameters {
	"""
	The device id of the client. If omitted, the value will not be included in the ads request.
	"""
	deviceId: String
	"""
	The device info of the client. Used in the backend IMDb advertising systems to determine ad placement related info.
	"""
	deviceInfo: DeviceInfo!
	"""
	The Identifier For Advertising (IDFA) of the user. IDFA is a well-established Apple/industry-standard abbreviation. If omitted, the value will not be included in the ads request.
	"""
	idfa: String
}

type DirectContactDetails {
	emailAddress: String
	faxNumber: LocalizedString
	phoneNumbers: [LocalizedString]
	website: WebsiteLink
}

enum DirectedIdContext {
	"""
	Context for integration with the Braze marketing platform
	"""
	BRAZE
	"""
	Context for customer's public-facing user profile
	"""
	USER_PROFILE
}

input DirectedIdInput {
	"""
	The context for which the directed ID will be used. Different contexts return different
	directed IDs for the same customer to maintain separation between use cases.

	Use USER_PROFILE when displaying the customer's public profile.
	Use BRAZE when integrating with the Braze marketing platform.
	"""
	context: DirectedIdContext!
}

type DirectedIdOutput {
	"""
	The context-specific directed ID for the customer. This ID is stable and will be the same
	for subsequent requests with the same customer and context combination.
	"""
	id: ID!
}

type Disambiguation {
	number: Int!
	text: String!
}

"""
Represents the author of a discussion post or reply.

Handles the case where the original author's account has been deleted:
- `profile` is null when the user account has been deleted.
- `deletedText` is non-null only when author's account has been deleted, and
  provides a localized string for the client to display in place of the author
  name (e.g. "[deleted user]").
"""
type DiscussionAuthor {
	"""
	Localized placeholder text to display when the author is deleted.
	Non-null only if the author's account has been deleted. Null otherwise.
	"""
	deletedText: LocalizedString
	"""
	The author's user profile.
	Null if the user's account has been deleted.
	"""
	profile: UserProfile
}

"""
An interest community where discussions take place.

Communities are tied to IMDb Interest entities (titles, names, etc.) and are
created/managed by IMDb staff. Only IMDb staff can create, edit, or disable
a community.
"""
type DiscussionCommunity {
	"""
	Operational status of this community.
	Determines visibility and whether new content can be created.
	"""
	communityStatusInfo: DiscussionCommunityStatusInfo!
	"""
	Paginated list of discussions in this community.

	Default ordering:
	1. First 10 by popularity (upvotes minus downvotes, highest first)
	2. Remaining by creation date (newest first)

	Priority override:
	1. Sponsored content first
	2. Staff-featured discussions
	3. Organic discussions

	Sorting and filtering inputs are not currently supported but will be added in future.

	Note: Maximum 100 discussions can be requested at a time.
	@throws GRAPHQL_VALIDATION_FAILED if first greater than 100.
	"""
	discussionThreads(
		"""
		Cursor for pagination - ID of the last item from previous page.
		"""
		after: ID
		"""
		Number of discussions to return (max 100).
		"""
		first: Int!
	): DiscussionThreadConnection!
	"""
	The IMDb entity this community is associated with.
	Can be an Interest or Title entity.
	"""
	entity: DiscussionCommunityEntity!
	"""
	Unique identifier for the community.
	"""
	id: ID!
	"""
	Moderation permissions for the current viewer in this community.
	Null if the viewer does not have moderator privileges.

	@entitlement(IMDB_STAFF)
	"""
	viewerModerationPermissions: [DiscussionModerationPermission]
}

"""
Union of entity types that can be associated with a discussion community.
Currently only Interest. Will be expanded in future, for example, Title
"""
union DiscussionCommunityEntity = Interest

"""
Operational status of a discussion community.
Determines discoverability, readability, and whether new content can be created.
"""
enum DiscussionCommunityStatus {
	"""
	Community is hidden from customers but still visible to staff.
	"""
	ADMIN_ONLY
	"""
	Community is hidden from non-staff users entirely.
	Superseded by ADMIN_ONLY; will be removed now that the new statuses exist.
	"""
	CLOSED
	"""
	Community has never been launched for customers.
	"""
	NOT_RELEASED
	"""
	Community is active — customers can view and contribute.
	"""
	OPEN
	"""
	Community is visible but posting is disabled.
	"""
	READ_ONLY
}

"""
Operational status information for a discussion community.
"""
type DiscussionCommunityStatusInfo {
	"""
	The operational status of the community.
	"""
	status: DiscussionCommunityStatus!
	"""
	Localized message associated with the current status.
	Null when status is OPEN.
	"""
	statusMessage: LocalizedString
}

"""
A single reason a moderator may attach to a community status change.
"""
type DiscussionCommunityStatusReason {
	"""
	Unique identifier for the status change reason.
	"""
	id: ID!
	"""
	Localized display text for the status change reason.
	"""
	text: LocalizedString!
}

"""
A reason for deleting a discussion or reply.
Used to populate the deletion reason dropdown.
"""
type DiscussionDeletionReason {
	"""
	Unique identifier for the deletion reason.
	"""
	id: ID!
	"""
	Localized display text for the deletion reason.
	"""
	text: LocalizedString!
}

"""
Union of entity types that can be mentioned in a discussion post.
Displayed as hyperlinks in the post body and as thumbnails in the
mentions carousel beneath the post.
"""
union DiscussionMentionableEntity = Name | Title

"""
Moderation actions the current viewer is permitted to perform.
Each value represents a specific capability that may be granted
independently in permission tiers.
"""
enum DiscussionModerationPermission {
	"""
	Viewer can delete any post in this community via moderation mutations.
	"""
	DELETE_POSTS
	"""
	Viewer can change the status of this community.
	"""
	MANAGE_COMMUNITY_STATUS
	"""
	Viewer can manage the status of a thread.
	"""
	MANAGE_THREAD_STATUS
	"""
	Viewer can override the spoiler flag on any post.
	"""
	MARK_SPOILER
}

"""
The publication status of a discussion post or reply.

- PUBLISHED: The post is live and visible to all users.
- PENDING: The post is awaiting moderation review before becoming visible.
- DELETED: The post has been deleted. Body content is withheld; child replies remain visible.
- REJECTED: The post was rejected by moderation. Body content is withheld; child replies remain visible.
- REMOVED_BY_ADMIN: The post was removed by a staff moderator. Body content is withheld; child replies remain visible.
"""
enum DiscussionPostStatus {
	DELETED
	PENDING
	PUBLISHED
	REJECTED
	REMOVED_BY_ADMIN
}

"""
A reply within a discussion.

Replies can be nested one level deep:
- A reply to the original post can have child replies
- Child replies cannot themselves have children

When a reply is deleted, rejected, or removed by admin, the body is replaced
with placeholder text appropriate to the viewer. All child replies remain visible.

Implements DiscussionCommon interface for common discussion fields.
"""
type DiscussionReply {
	"""
	The author of this reply.
	Returns a placeholder "[deleted user]" representation if the author's
	account has been deleted.
	"""
	author: DiscussionAuthor!
	"""
	Body content of the reply with rich text formatting (bold, italics, indent).
	When status is DELETED, REJECTED, or REMOVED_BY_ADMIN, returns localized placeholder text
	appropriate to the viewer (e.g. author sees detailed reason, public sees generic removal text).
	"""
	body: Markdown!
	"""
	Whether the current viewer can delete this post as a customer action.

	Returns true if the viewer is:
	- The reply author (deleting their own reply)

	Returns false if:
	- The viewer is not authenticated
	- The viewer is not the author
	- The post has already been deleted

	Note: This field does NOT reflect moderator capability. Moderators delete any
	reply via moderateDeleteDiscussionReply, gated by
	DiscussionCommunity.viewerModerationPermissions.

	Used by the client to conditionally render the customer "Delete" action.
	"""
	canDelete: Boolean!
	"""
	Whether the reply body contains spoilers.
	When true, the client should obfuscate the body content and show a
	"spoiler" label with a reveal CTA.
	"""
	containsSpoilers: Boolean!
	"""
	Timestamp of when this reply was created, in ISO-8601 format.
	"""
	createdTime: DateTime!
	"""
	The discussion this reply belongs to.
	"""
	discussionThread: DiscussionThread!
	"""
	IMDb entity mentions (titles and names) referenced in this reply.
	Bounded list; maximum 100 mentions per post.
	The 'after' parameter is not available because pagination is not supported.
	Returns a Connection type to wrap the total count and provide future flexibility.
	@throws GRAPHQL_VALIDATION_FAILED if first greater than 100.
	"""
	entityMentions(
		"""
		Maximum number of mentions to return (max 100).
		"""
		first: Int!
	): EntityMentionConnection!
	"""
	Unique identifier for the reply.
	"""
	id: ID!
	"""
	Whether this reply has been edited since creation.
	"""
	isEdited: Boolean!
	"""
	Timestamp of the most recent edit in ISO-8601 format.
	Null if the reply has never been edited.
	"""
	lastUpdated: DateTime
	"""
	The direct parent reply of this reply, or null when this is a top-level reply
	(i.e. its parent is the DiscussionThread itself, not another reply).

	Clients should avoid recursive selection patterns such as
	`parentReply { replies { parentReply } }` and instead fetch ancestors or
	siblings in follow-up queries.
	"""
	parentReply: DiscussionReply
	"""
	Paginated child replies to this reply. Up-to one nesting level.
	Will return an empty connection for child replies (i.e., replies that are themselves nested under a top-level reply).
	Default ordering: chronological, oldest first.
	On the discussion page, the client shows the first 2 by default with
	an "x more replies" CTA to lazy-load the next 20.
	Maximum 100 child replies can be requested at a time.
	Sorting inputs are not currently supported but will be added in future.
	@throws GRAPHQL_VALIDATION_FAILED if first greater than 100.
	"""
	replies(
		"""
		Cursor for pagination - ID of the last item from previous page.
		"""
		after: ID
		"""
		Number of child replies to return (max 100).
		"""
		first: Int!
	): DiscussionReplyConnection!
	"""
	Total number of direct child replies to this reply.
	"""
	replyCount: Int!
	"""
	Publication status of this reply.
	All child replies remain visible regardless of status.
	"""
	status: DiscussionPostStatus!
}

"""
Connection type for paginated discussion replies.
"""
type DiscussionReplyConnection {
	"""
	List of reply edges.
	"""
	edges: [DiscussionReplyEdge]!
	"""
	Pagination information.
	"""
	pageInfo: PageInfo!
	"""
	Total number of replies matching the query.
	"""
	total: Int!
}

"""
Edge type for discussion reply pagination.
"""
type DiscussionReplyEdge {
	"""
	Cursor for this edge, used for pagination.
	"""
	cursor: ID!
	"""
	The reply at this edge.
	"""
	node: DiscussionReply!
}

"""
A discussion thread within a community.
A discussion is created by a user and consists of an original post
(with a mandatory title) and zero or more replies.

Implements DiscussionCommon interface for common post fields.
"""
type DiscussionThread {
	"""
	The author of this discussion.
	Returns a placeholder "[deleted user]" representation if the author's
	account has been deleted.
	"""
	author: DiscussionAuthor!
	"""
	Availability status for this discussion, with the community's status taken into account.
	Determines visibility and whether its content can be created or changed.
	"""
	availabilityStatusInfo: DiscussionThreadAvailabilityStatusInfo!
	"""
	Body content of the discussion with rich text formatting (bold, italics, indent).
	Null if the discussion was created without body content and the post is not in a removed state.
	When status is DELETED, REJECTED, or REMOVED_BY_ADMIN, returns localized placeholder text
	appropriate to the viewer (e.g. author sees detailed reason, public sees null or generic text).
	"""
	body: Markdown
	"""
	Whether the current viewer can delete this post as a customer action.

	Returns true if the viewer is the post author (deleting their own discussion).

	Returns false if:
	- The viewer is not authenticated
	- The viewer is not the author
	- The post has already been deleted

	Note: This field does NOT reflect moderator capability. Moderators delete any
	discussion via moderateDeleteDiscussion, gated by
	DiscussionCommunity.viewerModerationPermissions. A moderator viewing another
	user's post sees canDelete = false; a moderator viewing their own post sees
	canDelete = true (their author self-delete).

	Used by the client to conditionally render the customer "Delete" action.
	"""
	canDelete: Boolean!
	"""
	Whether the post body contains spoilers.
	When true, the client should obfuscate the body content and show a
	"spoiler" label with a reveal CTA.
	"""
	containsSpoilers: Boolean!
	"""
	Timestamp of when this discussion was created, in ISO-8601 format.
	"""
	createdTime: DateTime!
	"""
	IMDb entity mentions (titles and names) referenced in this discussion.
	Bounded list; maximum 100 mentions per post.
	The 'after' parameter is not available because pagination is not supported.
	Returns a Connection type to wrap the total count and provide future flexibility.
	@throws GRAPHQL_VALIDATION_FAILED if first greater than 100.
	"""
	entityMentions(
		"""
		Maximum number of mentions to return (max 100).
		"""
		first: Int!
	): EntityMentionConnection!
	"""
	Unique identifier for the discussion.
	"""
	id: ID!
	"""
	Interest tagged for this discussion thread.
	Non-null for all interest-tagged discussion threads.
	Null when no interest is associated (e.g., future discussion types such as Video
	that may not have an interest tagged).
	"""
	interest: Interest
	"""
	Whether this discussion has been edited since creation.
	"""
	isEdited: Boolean!
	"""
	Whether this discussion is pinned as staff-featured.
	Featured discussions appear near the top of the feed (after sponsored content).
	"""
	isFeatured: Boolean!
	"""
	Whether this discussion contains sponsored content.
	Sponsored discussions always appear first in the feed.
	"""
	isSponsored: Boolean!
	"""
	Timestamp of the most recent edit in ISO-8601 format.
	Null if the discussion has never been edited.
	"""
	lastUpdated: DateTime
	"""
	Timestamp of the most recent reply across all levels in this discussion.
	Null if no replies exist.
	"""
	latestReplyTime: DateTime
	"""
	Paginated list of top-level replies to the original post.
	Default ordering: chronological, oldest first.
	Maximum 100 replies can be requested at a time.
	Sorting inputs are not currently supported but will be added in future.
	@throws GRAPHQL_VALIDATION_FAILED if first greater than 100.
	"""
	replies(
		"""
		Cursor for pagination - ID of the last item from previous page.
		"""
		after: ID
		"""
		Number of replies to return (max 100).
		"""
		first: Int!
	): DiscussionReplyConnection!
	"""
	Total number of direct replies to this discussion.
	"""
	replyCount: Int!
	"""
	Publication status of this discussion post.
	All child replies remain visible regardless of status.
	"""
	status: DiscussionPostStatus!
	"""
	The title of this discussion.
	When status is DELETED, REJECTED, or REMOVED_BY_ADMIN, returns localized placeholder text
	appropriate to the viewer (e.g. author sees removal reason, public sees generic removal text).
	"""
	title: Markdown!
}

"""
Availability status of a discussion thread.
Determines visibility, and whether its content can be created or changed.
"""
enum DiscussionThreadAvailabilityStatus {
	"""
	Thread is hidden from customers but still visible to staff.
	"""
	ADMIN_ONLY
	"""
	Thread is active — customers can view and reply.
	"""
	OPEN
	"""
	Thread is visible but replying and editing are disabled.
	"""
	READ_ONLY
}

"""
Availability status information for a discussion thread.
"""
type DiscussionThreadAvailabilityStatusInfo {
	"""
	The availability status of the thread, with the community's status taken into account.
	A thread that is OPEN in a READ_ONLY community is READ_ONLY here.
	"""
	status: DiscussionThreadAvailabilityStatus!
	"""
	Localized message associated with the current status.
	Null when status is OPEN.
	"""
	statusMessage: LocalizedString
}

"""
A single reason a moderator may attach to a thread availability change.
"""
type DiscussionThreadAvailabilityStatusReason {
	"""
	Unique identifier for the availability change reason.
	"""
	id: ID!
	"""
	Localized display text for the availability change reason.
	"""
	text: LocalizedString!
}

"""
Connection type for paginated discussions.
"""
type DiscussionThreadConnection {
	"""
	List of discussion edges.
	"""
	edges: [DiscussionThreadEdge]!
	"""
	Pagination information.
	"""
	pageInfo: PageInfo!
	"""
	Total number of discussions matching the filter criteria.
	"""
	total: Int!
}

"""
Edge type for discussion pagination.
"""
type DiscussionThreadEdge {
	"""
	Cursor for this edge, used for pagination.
	"""
	cursor: ID!
	"""
	The discussion at this edge.
	"""
	node: DiscussionThread!
}

"""
Generic type for a display-ready article representation of the parent type
"""
type DisplayableArticle {
	"""
	An optional body for the article
	"""
	body: Markdown
	"""
	An optional footer for the article
	"""
	footer: Markdown
	"""
	An optional header for the article
	"""
	header: Markdown
}

"""
Generic type for a concept which has display text and an optional ID
"""
type DisplayableAttribute {
	"""
	Type token
	"""
	id: ID
	"""
	The display text
	"""
	text: String!
}

type DisplayableAwardedEntity {
	value: Markdown!
}

type DisplayableBirthNameProperty {
	value: Markdown!
}

"""
Type for the display of a country
"""
type DisplayableCountry {
	"""
	The country code - either an ISO 3166 code or an internally defined code if no ISO code exists for that country.
	"""
	id: ID!
	"""
	Display text for the country (e.g. 'United States').
	"""
	text: String!
}

type DisplayableDate {
	"""
	A date in ISO 8601.
	"""
	date: Date
	"""
	Details of the individual date components, usually when ISO date is not available.
	"""
	dateComponents: DateComponents
	"""
	The date as a displayable property, e.g. '23 June 1970'
	"""
	displayableProperty: DisplayableDateProperty!
}

type DisplayableDateProperty {
	"""
	Language of the displayed value.
	"""
	language: DisplayableLanguage!
	"""
	A complete, formatted date that can be displayed.
	"""
	value: Markdown!
}

type DisplayableDateRange {
	"""
	Filming end date, use displayableProperty for localized value
	"""
	endDate: DisplayableDate
	"""
	Filming start date, use displayableProperty for localized value
	"""
	startDate: DisplayableDate
}

type DisplayableEpisodeNumber {
	"""
	Displayable season number/text as a standardized localizedDisplayableConcept
	"""
	displayableSeason: LocalizedDisplayableSeason!
	"""
	Displayable episode number as a standardized localizedDisplayableConcept
	"""
	episodeNumber: LocalizedDisplayableEpisodeNumber!
}

type DisplayableExternalLinkProperty {
	"""
	The external link as a markdown object
	"""
	value: Markdown!
}

"""
Featured roles represents common roles associated with credit category.
Can be used in search scenario to suggest role values for selected credit category.
"""
type DisplayableFeaturedCreditedRole {
	"""
	An opaque ID, suitable for caching the concept
	"""
	id: ID!
	"""
	The language of the localized text
	"""
	language: DisplayableLanguage!
	"""
	The localized text
	"""
	text: String!
}

type DisplayableLanguage {
	"""
	The language code - either an ISO 639 code or an internally defined code if no ISO code exists for the language.
	"""
	id: ID!
	"""
	Display text for the language (e.g. 'American English').
	"""
	text: String!
}

type DisplayableLocation {
	"""
	The location as a displayable property, e.g. 'Shawnee, Oklahoma, USA'
	"""
	displayableProperty: DisplayableLocationProperty!
	"""
	Location text.
	"""
	text: String
}

type DisplayableLocationProperty {
	"""
	Language of the displayed value.
	"""
	language: DisplayableLanguage!
	"""
	A complete, formatted location that can be displayed.
	"""
	value: Markdown!
}

type DisplayableNameAgeDetailsProperty {
	"""
	Name's age value that can be displayed
	"""
	value: Markdown!
}

"""
Name AKA as a Displayable Property
"""
type DisplayableNameAkaProperty {
	"""
	The displayable name aka
	"""
	value: Markdown!
}

type DisplayableNameDeathCause {
	"""
	The cause of death as a displayable property, e.g. 'accidental drowning'
	"""
	displayableProperty: DisplayableNameDeathCauseProperty!
	"""
	The cause of death of a person.
	"""
	text: String
}

type DisplayableNameDeathCauseProperty {
	"""
	Language of the displayed value.
	"""
	language: DisplayableLanguage!
	"""
	The cause of death property that can be displayed.
	"""
	value: Markdown!
}

type DisplayableNameFilmBiographyProperty {
	"""
	The biographical title about the person.
	"""
	value: Markdown!
}

type DisplayableNameHeightProperty {
	language: DisplayableLanguage!
	value: Markdown!
}

type DisplayableNameOtherWorkProperty {
	"""
	Machine translated other work displayable property. Returns translated field translated based on the language header.
	"""
	machineTranslatedValue: MachineTranslatedMarkdown!
	"""
	The work qualifiers, e.g. ['2009', 'Stage']
	"""
	qualifiersInMarkdownList: [Markdown]
	"""
	The details of the work this person was involved in
	"""
	value: Markdown!
}

type DisplayableNamePortrayalProperty {
	"""
	The title in which the person was portrayed by an actor.
	"""
	value: Markdown!
}

type DisplayableNamePrintBiographyProperty {
	"""
	The biographical book formatted as <Author>. <Title>. <Publisher>. ISBN: <ISBN>
	"""
	value: Markdown!
}

type DisplayableNameSpouseProperty {
	"""
	The language of the localized text
	"""
	language: DisplayableLanguage!
	"""
	A list of qualifiers including the time range, number of children, e.g. (9 July 2009 - 16 October 2018) (1 child)
	"""
	qualifiersInMarkdownList: [Markdown]
	"""
	The spouse name as plain text, or as nmconst link if person is an IMDb name
	"""
	value: Markdown!
}

type DisplayableNickNameProperty {
	value: Markdown!
}

type DisplayableProfession {
	"""
	An opaque ID, suitable for caching the concept
	"""
	id: ID!
	"""
	The language of the localized text
	"""
	language: DisplayableLanguage!
	"""
	The localized text
	"""
	text: String!
}

type DisplayableProfessionCategory {
	"""
	An opaque ID, suitable for caching the concept
	"""
	id: ID!
	"""
	The language of the localized text
	"""
	language: DisplayableLanguage!
	"""
	The localized text
	"""
	text: String!
}

type DisplayableProfessionDescription {
	"""
	An opaque ID, suitable for caching the concept
	"""
	id: ID!
	"""
	The language of the localized text
	"""
	language: DisplayableLanguage!
	"""
	The localized text
	"""
	text: String!
}

type DisplayablePrompt {
	constId: ID!
	display: Boolean!
	promptType: PromptType!
}

type DisplayablePublicityListingProperty {
	"""
	The name and region of the publication. e.g The New York Post (US)
	"""
	key: Markdown!
	"""
	The title of the publication, and optional date and authorship, in a display ready format
	"""
	value: Markdown!
}

type DisplayableRelationNameProperty {
	"""
	The displayable relative name
	"""
	value: Markdown!
}

type DisplayableSalaryProperty {
	"""
	The title for which this salary applies.
	"""
	key: Markdown!
	"""
	The salary as a diplayable properity.
	"""
	value: Markdown!
}

type DisplayableSeasonConnection {
	"""
	Pages of DisplayableSeason
	"""
	edges: [DisplayableSeasonEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of DisplayableSeason
"""
type DisplayableSeasonEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	Details of the DisplayableSeason
	"""
	node: LocalizedDisplayableSeason!
	"""
	Position of the node in this context, eg: DisplayableSeason 4 out of 8
	"""
	position: Int!
}

input DisplayableSeasonsSort {
	"""
	Ascending sorted in increasing order by numerical seasons
	followed by non-numerical season in alphabetical order e.g. [1, 2, 3, C, D]

	Descending sorting in decreasing order by numerical seasons
	followed by non-numerical season in reverse alphabetical order e.g. [3, 2, 1, D, C]
	"""
	order: SortOrder!
}

type DisplayableSongTitle {
	"""
	Type token
	"""
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	"""
	The display text
	"""
	text: String!
}

type DisplayableSpouseTimeRange {
	"""
	The time range as a localised, displayable property, e.g. '12 janvier 2020 - présent'
	"""
	displayableProperty: DisplayableSpouseTimeRangeProperty!
	"""
	The start date of this time range
	"""
	fromDate: DisplayableDate
	"""
	The end date of this time range
	"""
	toDate: DisplayableDate
}

type DisplayableSpouseTimeRangeProperty {
	"""
	Language of the displayed value.
	"""
	language: DisplayableLanguage!
	"""
	A complete, formatted time range that can be displayed.
	"""
	value: Markdown!
}

type DisplayableTechnicalSpecificationLocalizedProperty {
	language: DisplayableLanguage!
	qualifiersInMarkdownList: [Markdown]
	value: Markdown!
}

type DisplayableTechnicalSpecificationProperty {
	qualifiersInMarkdownList: [Markdown]
	value: Markdown!
}

type DisplayableTitleAkaProperty {
	"""
	The country of the alternative title text
	"""
	key: Markdown!
	"""
	The language of this title text
	"""
	language: DisplayableLanguage
	"""
	The alternative title text qualifiers such as 'Alternative Title' and 'Working Title'
	"""
	qualifiersInMarkdownList: [Markdown]
	"""
	The alternative title text
	"""
	value: Markdown!
}

"""
Company credit displayable property
"""
type DisplayableTitleCompanyCreditProperty {
	"""
	The company credit qualifiers, e.g. ['2009', 'Netherlands', 'Blu-ray']
	"""
	qualifiersInMarkdownList: [Markdown]
	"""
	The company credit value as link in Markdown
	"""
	value: Markdown!
}

type DisplayableTitleCountryOfOriginProperty {
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	"""
	The country of origin (e.g. 'United States')
	"""
	value: Markdown!
}

"""
Filming location displayable property
"""
type DisplayableTitleFilmingLocationProperty {
	"""
	The language of the localized text
	"""
	language: DisplayableLanguage
	"""
	The filming location qualifiers such as 'Butch in his car encounters Marsellus Wallace' and 'Exterior'
	"""
	qualifiersInMarkdownList: [Markdown]
	"""
	The filming location localized text
	"""
	value: Markdown!
}

"""
Genre displayable property
"""
type DisplayableTitleGenreProperty {
	"""
	The language the text is written in
	"""
	language: DisplayableLanguage!
	"""
	The genre Markdown
	"""
	value: Markdown!
}

type DisplayableTitleReleaseDateProperty {
	"""
	The country in Markdown
	"""
	key: Markdown!
	"""
	The release date qualifiers in Markdown such as internet, re-release, and Digital Film Festival
	"""
	qualifiersInMarkdownList: [Markdown]
	"""
	The formatted release date in Markdown
	"""
	value: Markdown!
}

type DisplayableTitleRuntimeProperty {
	language: DisplayableLanguage!
	qualifiersInMarkdownList: [Markdown]
	value: Markdown!
}

type DisplayableTitleSpokenLanguageProperty {
	"""
	The language of the display text.
	"""
	language: DisplayableLanguage!
	"""
	Display text for the language (e.g. 'English').
	"""
	value: Markdown!
}

type DisplayableTitleSubtitleLanguageProperty {
	"""
	The language of the display text.
	"""
	language: DisplayableLanguage!
	"""
	Display text for the language (e.g. 'English').
	"""
	value: Markdown!
}

"""
Tagline displayable property
"""
type DisplayableTitleTaglineProperty {
	"""
	Machine translated tagline displayable property. Returns translated field based on the language header.
	"""
	machineTranslatedValue: MachineTranslatedMarkdown!
	"""
	The tagline Markdown
	"""
	value: Markdown!
}

type DisplayableTitleTypeProperty {
	"""
	The title type Markdown
	"""
	value: Markdown!
}

type DisplayableYearConnection {
	"""
	Pages of DisplayableYear
	"""
	edges: [DisplayableYearEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of DisplayableYear
"""
type DisplayableYearEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	Details of the DisplayableYear
	"""
	node: LocalizedDisplayableEpisodeYear!
	"""
	Position of the node in this context, eg: DisplayableYear 4 out of 8
	"""
	position: Int!
}

input DisplayableYearsSort {
	"""
	Ascending sorting in increasing order by numerical years
	followed by non-numerical in alphabetical order e.g. [2015, 2018, 2020, UNKNOWN]

	Descending sorting in decreasing order by numerical years
	followed by non-numerical in reverse alphabetical order e.g. [2020, 2018, 2015, 2018, UNKNOWN]
	"""
	order: SortOrder!
}

"""
Required parameters used to make an AAX request that come from the context of the app client.
Includes app-specific parameters and general targeting parameters.
"""
input DisplayAdsForAppInput {
	"""
	A set of parameters needed from app clients to target ads.
	These parameters are used in both display and video ad flows.
	"""
	adParametersApp: AdParametersApp!
	"""
	A set of parameters used to target display ads across all site variants.
	"""
	displayAdTargetingParameters: DisplayAdTargetingParameters!
}

"""
Required and optional parameters used to make an AAX request that come from the context of the client.
"""
input DisplayAdTargetingParameters {
	"""
	The layout of the page being targeted, indicating the slots it supports.
	"""
	adLayout: String!
	"""
	The primary const of the page element being targeted for ads.
	"""
	pageConst: ID
	"""
	A list of ad slots that are eligible to be refreshed (i.e. render new ads on a set interval).
	"""
	refreshableSlots: [String]
	"""
	The subSectionType of a special section - i.e. 'winners' is a subSectionType under 'oscars' special section.
	"""
	subSectionType: String
}

type DisplayLabels {
	primaryLabel: String!
	quaternaryLabel: String
	secondaryLabel: String
	tertiaryLabel: String
}

"""
The distance from a requested location to a cinema.
"""
type DistanceToCinema {
	"""
	Integer representing the distance from the requested location to the cinema in meters.
	"""
	distanceInMeters: Int!
	"""
	Unique identifier for this distance.
	"""
	id: ID!
	"""
	Language of the distance display text.
	"""
	language: DisplayableLanguage!
	"""
	Localized text indicating the distance from the requested location to the cinema.
	e.g. `3 miles` or `4.8 km`.
	"""
	text: String!
}

"""
The distribution format of a title
"""
type DistributionFormat {
	"""
	The id of the distribution format.
	"""
	id: ID!
	"""
	The language of the distribution format
	"""
	language: DisplayableLanguage!
	"""
	Display text for the distribution format (e.g. 'Blu-ray').
	"""
	text: String!
}

"""
Represents a user's preference for a specific type of email notification.
Contains both the notification details (title, description) and the user's subscription status.
Each preference represents a specific email topic that a user can subscribe to or opt out from.
"""
type EmailPreference {
	"""
	Detailed explanation of what this email notification contains.
	"""
	description: LocalizedString!
	"""
	Unique identifier for this email preference.
	"""
	id: ID!
	"""
	Indicates whether the user is currently subscribed to this email notification.
	true means the user will receive emails, false means the user has opted out.
	"""
	subscribed: Boolean!
	"""
	Display name of the email notification.
	"""
	title: LocalizedString!
}

"""
Represents all email notification preferences for a user.
Contains a master marketing preference toggle and categorized lists of specific email preferences.
"""
type EmailPreferences {
	"""
	User's overall marketing email preference.
	When false, the user is opted out of ALL marketing emails regardless of individual preferences.
	When true, individual marketing preferences determine which specific marketing emails the user receives.
	"""
	allMarketingPreference: Boolean!
	"""
	List of marketing email preferences available to the user.
	These are promotional emails that are subject to the allMarketingPreference.
	Results are ordered alphabetically by email preference id.
	"""
	marketingPreferences: [EmailPreference]!
	"""
	List of subscription email preferences available to the user.
	These are subscription emails that are not affected by the allMarketingPreference toggle.
	Results are ordered alphabetically by email preference id.
	"""
	subscriptionPreferences: [EmailPreference]!
}

type EmployeeBranchName {
	"""
	The unique identifier for the branch name
	"""
	id: ID!
	"""
	The language that the text is in
	"""
	language: DisplayableLanguage!
	"""
	The name of the branch
	"""
	text: String!
}

input EmploymentCompaniesConstraint {
	"""
	A name match must be employed by ALL of these companies.
	Company IDs should be provided in IMDb company format.
	Example: "co0319272" for Amazon Studios, "co0476953" for Amazon Prime Video
	No filtering is applied if omitted.
	"""
	allEmploymentCompanyIds: [ID]
	"""
	A name match must be employed by AT LEAST ONE of these companies.
	Company IDs should be provided in IMDb company format.
	Example: "co0319272" for Amazon Studios, "co0476953" for Amazon Prime Video
	No filtering is applied if omitted.
	"""
	anyEmploymentCompanyIds: [ID]
	"""
	A name match must not be employed by any of these companies.
	Company IDs should be provided in IMDb company format.
	Example: "co0319272" for Amazon Studios, "co0476953" for Amazon Prime Video
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeEmploymentCompanyIds: [ID]
}

type EmploymentConnection {
	edges: [EmploymentEdge]!
	pageInfo: PageInfo!
	total: Int!
}

type EmploymentEdge {
	cursor: ID!
	node: PersonalEmployment!
	position: Int!
}

input EmploymentJobTitlesConstraint {
	"""
	A name match must have ALL of these employment job titles.
	Free text string search for professional roles (e.g., "Producer", "Director", "Executive Assistant").
	No filtering is applied if omitted.
	"""
	allEmploymentJobTitles: [String]
	"""
	A name match must have AT LEAST ONE of these employment job titles.
	Free text string search for professional roles (e.g., "Producer", "Director", "Executive Assistant").
	No filtering is applied if omitted.
	"""
	anyEmploymentJobTitles: [String]
	"""
	A name match must not have any of these employment job titles.
	Free text string search for professional roles (e.g., "Producer", "Director", "Executive Assistant").
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeEmploymentJobTitles: [String]
}

"""
Employment eligibility and work authorization status for talent
"""
enum EmploymentStatus {
	"""
	Valid US Passport
	"""
	PASSPORT_VALID
	"""
	Authorized to work in US
	"""
	USA_WORK_AUTHORIZED
	"""
	Willing to work unpaid
	"""
	WORKS_UNPAID
}

input EmploymentStatusConstraint {
	"""
	A name match must have ALL of these employment statuses.
	No filtering is applied if omitted.
	"""
	allEmploymentStatuses: [EmploymentStatus]
	"""
	A name match must have AT LEAST ONE of these employment statuses.
	No filtering is applied if omitted.
	"""
	anyEmploymentStatuses: [EmploymentStatus]
	"""
	A name match must not have any of these employment statuses.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeEmploymentStatuses: [EmploymentStatus]
}

"""
Work history types for employment
"""
enum EmploymentWorkHistory {
	COMMERCIAL
	FILM
	INDUSTRIAL
	INTERNET
	MUSIC_VIDEO
	RADIO
	TELEVISION
	THEATER
	VOICE_OVER
}

input EmploymentWorkHistoryConstraint {
	"""
	A name match must have ALL of these types in their work history.
	No filtering is applied if omitted.
	"""
	allWorkHistory: [EmploymentWorkHistory]
	"""
	A name match must have AT LEAST ONE of these types in their work history.
	No filtering is applied if omitted.
	"""
	anyWorkHistory: [EmploymentWorkHistory]
	"""
	A name match must not have any of these types in their work history.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeWorkHistory: [EmploymentWorkHistory]
}

"""
A credit processing job associated with a title.
"""
type EndCreditIngestionProcessingJob {
	"""
	User ID assigned to review this job. Null if unassigned.
	"""
	assignedTo: ID
	"""
	The entity identifier (e.g. tconst) this job is for.
	"""
	entityId: ID!
	"""
	Pre-signed URL to download extracted credits as Excel. Only present when
	status is READY or PUBLISHED. Expires after 1 hour.
	"""
	extractedCreditsUrl: URL
	"""
	Original uploaded filename.
	"""
	fileName: String
	"""
	Opaque unique identifier for the job.
	"""
	id: ID!
	"""
	Free-text notes about the job.
	"""
	notes: String
	"""
	URL to open the Hidden Figures review UI for editing extracted credits. Points
	to the internal IMDb Hidden Figures tool and requires authentication.
	"""
	reviewUrl: URL
	"""
	Pre-signed URL to download the original uploaded file. Expires after 1 hour.
	"""
	sourceFileUrl: URL
	"""
	Current processing status of the job.
	"""
	status: EndCreditIngestionProcessingJobStatus!
	"""
	Timestamp when the job was submitted for publishing. Null if not yet submitted.
	"""
	submittedAt: DateTime
	"""
	Timestamp when the file was uploaded.
	"""
	uploadedAt: DateTime!
	"""
	User ID of the person who uploaded the file.
	"""
	uploadedBy: ID!
}

"""
Paginated list of processing jobs.
"""
type EndCreditIngestionProcessingJobConnection {
	"""
	The list of job edges in this page.
	"""
	edges: [EndCreditIngestionProcessingJobEdge]!
	"""
	Pagination metadata.
	"""
	pageInfo: PageInfo!
	"""
	Total number of jobs matching the filter criteria.
	"""
	total: Int!
}

"""
Edge type for paginated processing jobs.
"""
type EndCreditIngestionProcessingJobEdge {
	"""
	Opaque cursor for this edge.
	"""
	cursor: ID!
	"""
	The processing job at this edge.
	"""
	node: EndCreditIngestionProcessingJob!
}

"""
Filter criteria for querying processing jobs.
"""
input EndCreditIngestionProcessingJobsInput {
	"""
	Filter by job statuses (OR logic within the list).
	If omitted, returns all non-PUBLISHED jobs (EXTRACTING, READY, FAILED).
	"""
	statuses: [EndCreditIngestionProcessingJobStatus]
}

"""
Possible statuses for a credit processing job.
"""
enum EndCreditIngestionProcessingJobStatus {
	"""
	Job is currently being processed.
	"""
	EXTRACTING
	"""
	Processing failed. Check job notes for details.
	"""
	FAILED
	"""
	Credits have been published to IMDb.
	"""
	PUBLISHED
	"""
	Processing complete, ready for human review.
	"""
	READY
}

type EngagementStatistics {
	"""
	The follower statistics for a followable entity. Follower stats will not be vended if the count is below 1000.
	"""
	followerStatistics: FollowerStatistics
	"""
	The watchlist statistics for a title. Watchlist stats will not be vended if the count is below 1000.
	"""
	watchlistStatistics: WatchlistStatistics
}

"""
Connection type for paginated entity mentions.
"""
type EntityMentionConnection {
	"""
	List of entity mention edges.
	"""
	edges: [EntityMentionEdge]!
	"""
	Pagination information.
	"""
	pageInfo: PageInfo!
	"""
	Total number of entity mentions in this post.
	"""
	total: Int!
}

"""
Edge type for entity mention pagination.
"""
type EntityMentionEdge {
	"""
	Placeholder string, as pagination is not supported for EntityMentionConnection.
	"""
	cursor: ID!
	"""
	The mentionable entity.
	"""
	node: DiscussionMentionableEntity!
}

"""
Reactions data for a specific entity
Supports any IMDb entity type: videos (vi*), lists (ls*), etc.
"""
type EntityReactions {
	"""
	The entity that is being reacted upon, e.g. vi1234567, ls7654321
	"""
	entityId: ID!
	"""
	Cacheable
	Aggregate reaction counts for this entity
	Returns null if entity has no reactions yet
	"""
	reactionsSummary: ReactionsSummary
	"""
	Not cacheable
	User's reactions on this entity
	Requires x-imdb-customer-id header from logged-in user
	Returns null if user is not logged in
	"""
	userReactions: [UserReaction]
}

"""
Pagination for Cast credit type on a Cast credit (for episode credits of a series title).
"""
type EpisodeCastConnection {
	"""
	All of the characters in this list of Cast credits
	"""
	characters(limit: Int): [Character]
	"""
	The seasons of episodes in the connection

	Sorted in increasing order by numerical seasons
	followed by non-numerical season in alphabetical order e.g. [1, 2, 3, C, D]
	"""
	displayableSeasons(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): DisplayableSeasonConnection
	"""
	The years of episodes in the connection

	Sorted in increasing order by numerical years
	followed by non-numerical in alphabetical order e.g. [2015, 2018, 2020, UNKNOWN]
	"""
	displayableYears(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): DisplayableYearConnection
	edges: [CreditEdge]!
	pageInfo: PageInfo!
	total: Int!
	"""
	The earliest and latest release years for the Title(s) in these credits. If these would be the same,
	only a single year is populated.
	"""
	yearRange: YearRange
}

"""
Pagination for Episode types which always has a known total number of edges.
"""
type EpisodeConnection {
	"""
	Pages of episodes
	"""
	edges: [EpisodeEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

type EpisodeCreditConnection {
	"""
	The seasons of episodes in the connection

	Sorted in increasing order by numerical seasons
	followed by non-numerical season in alphabetical order e.g. [1, 2, 3, C, D]
	Field is null when no season information is present on the associated episodes.
	"""
	displayableSeasons(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): DisplayableSeasonConnection
	"""
	The years of episodes in the connection

	Sorted in increasing order by numerical years
	followed by non-numerical in alphabetical order e.g. [2015, 2018, 2020, UNKNOWN].
	Field is null when no year information is present on the associated episodes.
	"""
	displayableYears(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): DisplayableYearConnection
	"""
	Results are sorted by release date ascending.
	"""
	edges: [CreditV2Edge]!
	pageInfo: PageInfo!
	total: Int!
	"""
	The earliest and latest release years for the Title(s) in these credits. If these would be the same,
	only a single year is populated.
	Field is null when no year information is present on the associated episodes.
	"""
	yearRange: YearRange
}

input EpisodeCreditsFilter {
	episodes: EpisodesFilter
}

input EpisodeCreditsSort {
	by: EpisodeCreditsSortBy!
	order: SortOrder!
}

enum EpisodeCreditsSortBy {
	"""
	Episode ordering follows this priority sequence:
	1. Release date
	2. Series number
	3. Episode number.
	"""
	RELEASE_DATE
}

"""
Pagination for Crew credit type on a Crew credit (for episode credits of a series title).
"""
type EpisodeCrewConnection {
	"""
	The seasons of episodes in the connection

	Sorted in increasing order by numerical seasons
	followed by non-numerical season in alphabetical order e.g. [1, 2, 3, C, D]
	"""
	displayableSeasons(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): DisplayableSeasonConnection
	"""
	The years of episodes in the connection

	Sorted in increasing order by numerical years
	followed by non-numerical in alphabetical order e.g. [2015, 2018, 2020, UNKNOWN]
	"""
	displayableYears(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): DisplayableYearConnection
	edges: [CreditEdge]!
	"""
	All of the jobs in this list of Crew credits
	"""
	jobs(limit: Int): [CrewJob]
	pageInfo: PageInfo!
	total: Int!
	"""
	The earliest and latest release years for the Title(s) in these credits. If these would be the same,
	only a single year is populated.
	"""
	yearRange: YearRange
}

"""
Page of episodes
"""
type EpisodeEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	An episode node
	"""
	node: Title!
	"""
	Position of the node in this context
	"""
	position: Int!
}

type EpisodeNumberDisplayableProperty {
	value: Markdown!
}

input EpisodeReleaseDate {
	day: Int
	month: Int
	year: Int!
}

type Episodes {
	"""
	  Displayable season as a standardized localizedDisplayableConcept
	​
	  Sorting is ascending by default.
	"""
	displayableSeasons(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
		sort: DisplayableSeasonsSort
	): DisplayableSeasonConnection
	"""
	Displayable year as a standardized localizedDisplayableConcept

	Sorting in ascending by default.
	"""
	displayableYears(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
		sort: DisplayableYearsSort
	): DisplayableYearConnection
	episodes(
		after: ID
		before: ID
		filter: EpisodesFilter
		first: Int
		jumpTo: ID
		last: Int
		sort: EpisodesSort
	): EpisodeConnection
	"""
	A boolean flag that indicates if the series is ongoing (i.e. it has an unknown number of future episodes).
	"""
	isOngoing: Boolean
}

input EpisodesFilter {
	"""
	Filter out season values e.g. ['1', '2', 'unknown']
	"""
	excludeSeasons: [String]
	"""
	Filter for season values e.g. ['1', '2', 'unknown']
	"""
	includeSeasons: [String]
	"""
	Filter for episodes with release date newer or equal the given date excluding episodes with unknown release date
	"""
	releasedOnOrAfter: EpisodeReleaseDate
	"""
	Filter for episodes with release date older or equal the given date excluding episodes with unknown release date
	"""
	releasedOnOrBefore: EpisodeReleaseDate
	"""
	Filter for episodes with unknown release date
	"""
	unknownReleaseDate: UnknownReleaseDateFilter
}

input EpisodesSort {
	by: EpisodesSortBy!
	order: SortOrder!
}

enum EpisodesSortBy {
	EPISODE_THEN_RELEASE
	RATING
	RELEASE_DATE
}

"""
Inputs here are AND'd together

If the same input is specified in the any and exclude constraints,
then the results should be empty.

Example: To match the Season 8, Episode 6 of tt0944947 you can search
anySeriesIds: ["tt0944947"], anySeasons: ["8"], anyEpisodeNumbers: ["6"]

Example: To exclude the Season 8, Episode 6 of tt0944947 you can search
excludeSeriesIds: ["tt0944947"], excludeSeasons: ["8"], excludeEpisodeNumbers: ["6"]
"""
input EpisodicSearchConstraint {
	"""
	Match episodes that have AT LEAST ONE of these episode numbers
	ES: For first three episodes ['1', '2', '3']
	ES: For episodes without episode number ['Unknown']
	"""
	anyEpisodeNumbers: [String]
	"""
	Match episodes are a part of AT LEAST ONE of these seasons
	Example: To restrict to first three seasons: ['1', '2', '3']
	Example: To restrict to Unknown season, or Holiday Special Season: ['Unknown', 'Holiday Special']
	"""
	anySeasons: [String]
	"""
	Match episodes are a part of AT LEAST ONE of these series Title Ids
	"""
	anySeriesIds: [ID]
	"""
	Exclude episodes that have AT LEAST ONE of these episode numbers
	ES: For all episodes except the first three ['1', '2', '3']
	ES: For all episodes with an episode number ['Unknown']
	"""
	excludeEpisodeNumbers: [String]
	"""
	Exclude episodes that are a part of AT LEAST ONE of these seasons
	Example: To restrict to none of the first three seasons: ['1', '2', '3']
	Example: To restrict to neither Unknown season, or Holiday Special Season: ['Unknown', 'Holiday Special']
	"""
	excludeSeasons: [String]
	"""
	Exclude episodes that are a part of AT LEAST ONE of these series Title Ids
	"""
	excludeSeriesIds: [ID]
}

"""
Pagination for Events which always has a known total number of edges.
"""
type EventConnection {
	"""
	Pages of Events
	"""
	edges: [EventEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of Events
"""
type EventEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	An AwardsEvent node
	"""
	node: AwardsEvent!
	"""
	Position of the node in this context, eg: Event 4 out of 31
	"""
	position: Int!
}

"""
The instance of an event that took place in a specific year
"""
type EventEdition {
	"""
	A list of awards announced in an event edition e.g. 'Oscar'
	"""
	awards(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): AwardDetailsConnection
	"""
	The event where the event edition is happening e.g Academy Awards, USA
	"""
	event: AwardsEvent!
	"""
	A unique id prefixed with 'ee'
	"""
	id: ID!
	"""
	A number that defines the sequence of editions within a year, starting with 1
	"""
	instanceWithinYear: Int!
	"""
	List of trivia items related to the event edition with potential markdown links for movies/people
	"""
	trivia: [Markdown]
	"""
	The year this edition of the event took place
	"""
	year: Int!
}

"""
Data for a combination of Event Edition and Award.
"""
type EventEditionAward {
	"""
	Award Name. eg: 'Oscar'

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	awardName: String!
	"""
	The compound ID key for an event edition and award combination
	"""
	id: ID!
	"""
	Award Nominations List. Sorted by win time order, most recent first.
	An empty array indicates no winners have been announced (yet).

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	winners(limit: Int): [AwardNomination]!
}

"""
Pagination for EventEdition types which always has a known total number of edges.
"""
type EventEditionConnection {
	"""
	Pages of event editions
	"""
	edges: [EventEditionEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of event editions
"""
type EventEditionEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	An EventEdition node
	"""
	node: EventEdition!
	"""
	Position of the node in this context, eg: EventEdition 4 out of 312
	"""
	position: Int!
}

type EventLiveResults {
	"""
	The optional localized string to display as subtext for the widget.
	"""
	displayDescription: LocalizedString
	"""
	The localized string to display as the title of the widget.
	"""
	displayTitle: LocalizedString!
	"""
	The event edition and award currently live on the site.
	"""
	eventEditionAward: EventEditionAward!
	"""
	The event id, e.g, ev0000003 for Oscars
	"""
	eventId: String!
	"""
	URL for the event page on the consumer website, if there is one.
	"""
	eventPageUrl: URL
	"""
	The localized blurb to act as a placeholder if there are no winners yet.
	"""
	noWinnersBlurb: LocalizedString!
}

input EventLiveResultsOverrideInput {
	"""
	Event override will not be applied unless this is 'true'
	"""
	enableOverride: Boolean!
	"""
	Optionally specify a specific event edition and award to return data for. Otherwise return
	data for a pre-configured override event which is subject to change.
	"""
	overrideEvent: OverrideLiveEventInput
}

type EventMetadata {
	"""
	Single event by evconst
	"""
	event(id: ID!): AwardsEvent
	"""
	All the event Ids
	"""
	events(after: ID, before: ID, first: Int, last: Int): EventConnection
}

input EventsFilter {
	"""
	Filter out given Events IDs
	"""
	excludeEvents: [ID]
	"""
	Filter for given Events IDs
	"""
	includeEvents: [ID]
}

type EventUrl {
	category: EventUrlCategory!
	url: String!
}

type EventUrlCategory {
	"""
	The Url category Id, e.g. official
	"""
	categoryId: String!
	"""
	Opaque URL category ID
	"""
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	"""
	The display text for Url category, e.g. Official
	"""
	text: String!
}

"""
The viewer's active (in-progress) contribution state on a title. A wrapper around the
viewer's single open basket today.
"""
type Experimental_ActiveContributionSubmissions {
	"""
	The viewer's open (SAVED) basket on this title, or null if they have none.
	Non-null means "Resume"; null means "Start".
	"""
	previousSubmission: Experimental_ContributionSubmission
}

"""
A single field changed by a staged item, with its previous and new value
(e.g. Billing Order 2 → 1).
"""
type Experimental_ContributionFieldChange {
	"""
	Display label of the field that changed, e.g. 'Billing Order'.
	"""
	label: LocalizedString!
	"""
	The new value, rendered for display. Null when the field is being removed (a delete).
	"""
	newValue: LocalizedMarkdown
	"""
	The previous value, rendered for display. Null when there was no prior value (an add).
	"""
	oldValue: LocalizedMarkdown
}

"""
A single staged change within a contribution basket.
"""
type Experimental_ContributionItem {
	"""
	Supplementary display lines explaining the item beyond its field changes, e.g. the
	reason a match was chosen ("Matched nm0382676 because it is already on the title").
	Null when the item carries no such metadata.
	"""
	additionalMetadata: [LocalizedMarkdown]
	"""
	The id of the catalog item this change targets.
	"""
	catalogId: ID!
	"""
	The per-field changes this item makes, each with its previous and new value
	(e.g. Billing Order 2 → 1)
	For an add, every field is new (no previous value); for a removal the list is empty.
	"""
	changes: [Experimental_ContributionFieldChange]!
	"""
	When this item was added to the basket.
	"""
	createdAt: DateTime!
	"""
	The kind of data being changed, e.g. "name_credit". A data-schema id used by the client
	to bucket and route items, not rendered directly.
	"""
	itemType: ID!
	"""
	Whether this item adds, modifies, or removes data.
	"""
	operation: Experimental_ContributionOperation!
	"""
	Post-submit moderation status (PENDING, APPROVED, or DECLINED), relevant once the basket
	is submitted. Null while the basket is still being edited.
	"""
	postSubmitStatus: Experimental_PostSubmitStatus
	"""
	Pre-submit validation status (READY or NEEDS_ATTENTION), relevant while the basket is
	being edited. Null once the basket is submitted.
	"""
	preSubmitStatus: Experimental_PreSubmitStatus
	"""
	Opaque per-type section key the client uses to bucket items into UI sections and to
	deep-link/scroll to the relevant section. For credits this is the credit-category id
	(e.g. Cast, Director); for other data types it is the type's own section key (the
	section then coincides with `itemType`). Raw key only -- the human-readable label is
	rendered inside `changes` (e.g. a "Category" field), so this is not a duplicate of that.
	"""
	sectionId: ID!
}

"""
Lifecycle state of a contribution basket.
"""
enum Experimental_ContributionLifecycle {
	"""
	Open and editable; not yet submitted.
	"""
	SAVED
	"""
	Submitted for moderation; read-only.
	"""
	SUBMITTED
}

"""
Whether a staged item adds new data, modifies existing data, or removes data.
"""
enum Experimental_ContributionOperation {
	"""
	Adds new data to the subject.
	"""
	ADD
	"""
	Removes existing data from the subject.
	"""
	DELETE
	"""
	Modifies existing data on the subject.
	"""
	MODIFY
}

"""
Item counts broken down by operation. Each count is zero when no items match.
"""
type Experimental_ContributionOperationCounts {
	"""
	Items being added.
	"""
	added: Int!
	"""
	Items being modified.
	"""
	modified: Int!
	"""
	Items being removed.
	"""
	removed: Int!
}

"""
The subject of a contribution when it is not (yet) a resolvable entity -- e.g. a brand-new
title that has no id yet. Carries only display text.
"""
type Experimental_ContributionOtherSubject {
	"""
	Rendered display of the subject, e.g. the entered name for a not-yet-created entity.
	"""
	display: LocalizedMarkdown!
}

"""
The entity a contribution is about. When that entity already exists it is returned as the
real federated entity (Title, Name, or Company) so the client can fetch whatever it needs
about it directly. A brand-new entity that has no id yet is returned as
Experimental_ContributionOtherSubject, which carries only its display text.
"""
union Experimental_ContributionSubject =
	| Company
	| Experimental_ContributionOtherSubject
	| Name
	| Title

"""
A contribution "basket": a set of staged changes about a single subject, submitted or
discarded together.
"""
type Experimental_ContributionSubmission {
	"""
	When the basket was first created.
	"""
	createdAt: DateTime!
	"""
	The staged items in the basket. A bounded working set, not a separately paginated
	collection, so the full set is returned inline (C6).
	"""
	items: [Experimental_ContributionItem]!
	"""
	SAVED while still being edited; SUBMITTED once submitted.
	"""
	lifecycle: Experimental_ContributionLifecycle!
	"""
	The entity this contribution is about -- a Title, Name, or Company when it already
	exists, or Experimental_ContributionOtherSubject (display text only) for a brand-new
	entity that has no id yet or for a deleted entity.
	"""
	subject: Experimental_ContributionSubject!
	"""
	Stable identifier for the basket.
	"""
	submissionReference: ID!
	"""
	When the basket was submitted; null while it is still being edited.
	"""
	submittedAt: DateTime
	"""
	Aggregate counts over the items in this basket.
	"""
	summary: Experimental_ContributionSubmissionSummary!
	"""
	When the basket was last modified.
	"""
	updatedAt: DateTime!
	"""
	Monotonic per-basket version (1-based), bumped on every write to the basket.
	The submit token: re-read it on every refresh and pass the latest value back as
	`expectedVersion` on experimental_submitContributionSubmission. A moved version
	means the basket changed since it was last read (e.g. a save from another tab).
	"""
	version: Int!
}

"""
Aggregate counts over the items in a basket -- a roll-up of each item's operation and
status
"""
type Experimental_ContributionSubmissionSummary {
	"""
	Counts by operation (added / modified / removed).
	"""
	byOperation: Experimental_ContributionOperationCounts!
	"""
	Post-submit moderation counts (relevant once the basket is submitted).
	"""
	postSubmitStatus: Experimental_PostSubmitStatusCounts!
	"""
	Pre-submit validation counts (relevant while the basket is being edited).
	"""
	preSubmitStatus: Experimental_PreSubmitStatusCounts!
	"""
	Total number of staged items in the basket.
	"""
	totalItems: Int!
}

"""
A leaderboard scoped to a game type, entity, and time period.
"""
type Experimental_GameLeaderboard {
	"""
	The requesting customer's own standing in this leaderboard.

	Throws UNAUTHENTICATED if not logged in.
	Returns null if the customer has earned no points in this scope.
	"""
	myStanding: Experimental_GameLeaderboardRanking
	"""
	Ranked entries, ordered by points descending. Tied users share the same rank (competition ranking).
	Supports two pagination modes (mutually exclusive):
	- `after`: Forward pagination from the top (standard Relay cursor).
	- `aroundProfileId`: Centers the page on the given user's position — returns
	  floor(first/2) entries above, the user, and the remaining below.

	When both `after` and `aroundProfileId` are omitted, pagination starts from rank 1.

	At most 100 entries per page; first is capped at 100 (smaller values return that many).
	Pass first: 0 to fetch only the connection `total` (page metadata) with no ranking edges.

	Users who have not earned points in this scope do not appear in the rankings; passing
	their profileId to `aroundProfileId` throws RESOURCE_NOT_FOUND (no position to center on).

	Throws GRAPHQL_VALIDATION_FAILED if first is less than 0.
	Throws GRAPHQL_VALIDATION_FAILED if both `after` and `aroundProfileId` are provided.
	Throws RESOURCE_NOT_FOUND if `aroundProfileId` references a user with no ranking in this leaderboard.
	"""
	rankings(
		after: ID
		aroundProfileId: ID
		first: Int!
	): Experimental_GameLeaderboardRankingConnection!
	"""
	The const ID this leaderboard is scoped to. Null for the global leaderboard.
	"""
	scopeId: ID
	"""
	Past seasons (excludes the current one), most recent first. Each season is one
	calendar month (UTC). Not paginated.
	"""
	seasons: [Experimental_GameLeaderboardSeason]!
	"""
	The resolved time period of this leaderboard.
	"""
	timePeriod: Experimental_GameLeaderboardTimePeriod!
}

"""
A game that has a leaderboard.
"""
type Experimental_GameLeaderboardGame {
	"""
	Localized display name for the game.
	"""
	displayName: LocalizedString!
	"""
	The game type id clients pass as gameType.
	"""
	id: ID!
}

input Experimental_GameLeaderboardInput {
	"""
	The game type for this leaderboard. One of the ids returned by experimental_gameLeaderboardGames.
	"""
	gameType: ID!
	"""
	The entity this leaderboard is scoped to — an IMDb const with one of the standard
	prefixes (in/tt/nm/co/ni/ev/ch/rm/vi), e.g. 'in0000076'. Omit for the global
	leaderboard spanning all scopes; a game is not required to be tied to any const.

	A well-formed scopeId that simply has no scores yet returns an empty ranking set,
	not an error. Throws GRAPHQL_VALIDATION_FAILED only if scopeId is malformed (does
	not match the const format above).
	"""
	scopeId: ID
	"""
	Time period to rank by. Defaults to ALL_TIME if omitted.
	"""
	timePeriod: Experimental_GameLeaderboardTimePeriodInput
}

"""
Stats for a single time period (all-time or current season).
"""
type Experimental_GameLeaderboardPeriodStats {
	"""
	The scope in which the user earned the most points. Null if no points earned.
	"""
	bestScope: Experimental_UserScopeStanding
	"""
	The user's standing in a specific scope (e.g. one interest). Null if the user has no points in that scope.
	"""
	scopeStanding(scopeId: ID!): Experimental_UserScopeStanding
	"""
	Total points across all scopes in this period.
	"""
	totalPoints: Float!
}

"""
A single user's ranking entry.
"""
type Experimental_GameLeaderboardRanking {
	"""
	This user's current streak in this leaderboard's scope. 0 if no active streak.
	"""
	currentStreak: Int!
	"""
	Total points earned by this user within the leaderboard's scope.

	Float (IEEE 754 double) is used deliberately: it holds exact integers up to 2^53
	(~9 quadrillion), far above any realistic total, and maps 1:1 to Redis ZSET scores.
	No Long/Int64 scalar exists in IMDbCommonGraphletSchema, so a custom scalar (an F1
	contribution) would add cost with no precision benefit.
	"""
	points: Float!
	"""
	1-based rank. Tied users share the same rank (competition ranking).
	"""
	rank: Int!
	"""
	The ranked user.
	"""
	user: UserProfile!
}

"""
Relay connection of leaderboard rankings. Cursors are opaque and must not be parsed
by clients. `total` is the full number of ranked users across all pages, not just this page.
"""
type Experimental_GameLeaderboardRankingConnection {
	"""
	A page of rankings.
	"""
	edges: [Experimental_GameLeaderboardRankingEdge]!
	"""
	Details of the page.
	"""
	pageInfo: PageInfo!
	"""
	Total number of ranked users in this leaderboard across all pages.
	"""
	total: Int!
}

"""
Item in a page of leaderboard rankings.
"""
type Experimental_GameLeaderboardRankingEdge {
	"""
	Opaque cursor for this entry; pass to `after` to page forward. Do not parse.
	"""
	cursor: ID!
	"""
	A ranking node.
	"""
	node: Experimental_GameLeaderboardRanking!
}

"""
Input for entity-scoped leaderboard queries (Interest, Title, Name). The scope is implicit from the parent entity.
"""
input Experimental_GameLeaderboardScopeInput {
	"""
	The game type for this leaderboard. See Experimental_GameLeaderboardInput.gameType.
	"""
	gameType: ID!
	"""
	Time period to rank by. Defaults to ALL_TIME if omitted.
	"""
	timePeriod: Experimental_GameLeaderboardTimePeriodInput
}

"""
A competition season.
"""
type Experimental_GameLeaderboardSeason {
	"""
	Localized display name (e.g. 'June 2026').
	"""
	displayName: LocalizedString!
	"""
	Last day of the season, inclusive (UTC).
	"""
	endDate: Date!
	"""
	Season identifier in 'YYYY-MM' format.
	"""
	id: ID!
	"""
	True if this is the currently active season.
	"""
	isCurrent: Boolean!
	"""
	First day of the season, inclusive (UTC).
	"""
	startDate: Date!
}

"""
Input for a user's leaderboard stats. Wrapped in an input object (rather than a bare
gameType argument) so future filters — e.g. a time period — can be added without a
breaking change.
"""
input Experimental_GameLeaderboardStatsInput {
	"""
	The game type to fetch stats for. One of the ids returned by experimental_gameLeaderboardGames.
	"""
	gameType: ID!
}

"""
Resolved time period returned on an Experimental_GameLeaderboard.
"""
type Experimental_GameLeaderboardTimePeriod {
	"""
	The season covered. Null when type is ALL_TIME.
	"""
	season: Experimental_GameLeaderboardSeason
	"""
	Whether this leaderboard covers all time or a single season.
	Reflects input defaulting: omitted input → ALL_TIME; SEASON without seasonId → current season.
	"""
	type: Experimental_GameLeaderboardTimePeriodType!
}

input Experimental_GameLeaderboardTimePeriodInput {
	"""
	Season identifier in 'YYYY-MM' format. Optional when type is SEASON; defaults to the
	current season. Ignored when type is ALL_TIME. Throws GRAPHQL_VALIDATION_FAILED if not 'YYYY-MM'.
	"""
	seasonId: ID
	"""
	The time period type.
	"""
	type: Experimental_GameLeaderboardTimePeriodType!
}

"""
Time period for leaderboard rankings.
"""
enum Experimental_GameLeaderboardTimePeriodType {
	"""
	Rankings accumulated across all seasons; never reset.
	"""
	ALL_TIME
	"""
	Rankings for a single competition season (default cadence: one calendar month).
	"""
	SEASON
}

"""
Aggregated leaderboard statistics for a user.
"""
type Experimental_GameLeaderboardUserStats {
	"""
	All-time performance, accumulated across every season.
	"""
	allTime: Experimental_GameLeaderboardPeriodStats!
	"""
	Current global daily streak (consecutive correct days). 0 if no active streak.
	"""
	currentGlobalDailyStreak: Int!
	"""
	Performance in the current season only.
	"""
	currentSeason: Experimental_GameLeaderboardPeriodStats!
	"""
	Longest global daily streak ever achieved by this user.
	"""
	longestGlobalDailyStreak: Int!
}

input Experimental_GetLatestUIChatInput {
	"""
	The unique identifier for a chat session
	"""
	sessionId: ID!
}

"""
Defines the output response type for the experimental_getLatestUIChat query.
"""
type Experimental_GetLatestUIChatOutput {
	"""
	The complete chat session including session information and chat items.
	"""
	chat: Experimental_UIChat!
}

"""
Represents a connection to a list of name filter search results.
"""
type Experimental_NameFilterSearchConnection {
	edges: [Experimental_NameFilterSearchEdge]!
	total: Int!
}

"""
Represents an edge in the name filter search results connection.
"""
type Experimental_NameFilterSearchEdge {
	node: Experimental_NameFilterSearchResult!
}

"""
Represents a single name filter that matched the search query, including metadata about the filter
and the specific values that matched.
"""
type Experimental_NameFilterSearchResult {
	constraintName: String!
	displayName: String!
	filterId: ID!
	matchedTerms: [Experimental_NameFilterSearchTerm]!
	requiresEntitlement: [String]!
}

"""
Represents a specific filter value that matched the search query. Contains both the display-friendly
value for UI presentation and the constraint value for use in ANS queries. The terms array contains
all searchable variations of this value (e.g., ["Cinematographer", "DP"]).
"""
type Experimental_NameFilterSearchTerm {
	constraintValue: String!
	displayValue: String!
	terms: [String]!
}

"""
Post-submit moderation status of a staged item, relevant once the basket is submitted.
"""
enum Experimental_PostSubmitStatus {
	"""
	Approved by moderation.
	"""
	APPROVED
	"""
	Declined by moderation.
	"""
	DECLINED
	"""
	Submitted and awaiting moderation.
	"""
	PENDING
}

"""
Item counts by post-submit moderation status, relevant once the basket is submitted.
Each count is zero when no items match.
"""
type Experimental_PostSubmitStatusCounts {
	"""
	Items approved by moderation.
	"""
	approved: Int!
	"""
	Items declined by moderation.
	"""
	declined: Int!
	"""
	Items awaiting moderation.
	"""
	pending: Int!
}

"""
Pre-submit validation status of a staged item, relevant while the basket is being edited.
"""
enum Experimental_PreSubmitStatus {
	"""
	Failed pre-submit validation; needs fixing before the basket can be submitted.
	"""
	NEEDS_ATTENTION
	"""
	Passed pre-submit validation; ready to submit.
	"""
	READY
}

"""
Item counts by pre-submit validation status, relevant while the basket is being edited.
Each count is zero when no items match.
"""
type Experimental_PreSubmitStatusCounts {
	"""
	Items that still need attention before the basket can be submitted.
	"""
	needsAttention: Int!
}

"""
Represents a connection to a list of title filter search results.
"""
type Experimental_TitleFilterSearchConnection {
	edges: [Experimental_TitleFilterSearchEdge]!
	total: Int!
}

"""
Represents an edge in the title filter search results connection.
"""
type Experimental_TitleFilterSearchEdge {
	node: Experimental_TitleFilterSearchResult!
}

"""
Represents a single title filter that matched the search query, including metadata about the filter
and the specific values that matched.
"""
type Experimental_TitleFilterSearchResult {
	constraintName: String!
	displayName: String!
	filterId: ID!
	matchedTerms: [Experimental_TitleFilterSearchTerm]!
	requiresEntitlement: [String]!
}

"""
Represents a specific filter value that matched the search query. Contains both the display-friendly
value for UI presentation and the constraint value for use in ATS queries. The terms array contains
all searchable variations of this value (e.g., ["Action", "action movies"]).
"""
type Experimental_TitleFilterSearchTerm {
	constraintValue: String!
	displayValue: String!
	terms: [String]!
}

"""
Represents a complete UI chat session including session information and chat items.
"""
type Experimental_UIChat {
	"""
	The chat items in this session, ordered chronologically by timestamp (oldest first)
	"""
	chatItems: [Experimental_UIChatItemUnion]!
	"""
	Contains information about the subject of the chat, the context within which
	the user is taking some action, e.g. updating my profile
	"""
	contextHeader: UIWorkflowContextHeader!
	"""
	When the session was created
	"""
	createdAt: DateTime!
	"""
	Unique identifier for the chat session
	"""
	sessionId: ID!
	"""
	Current status of the chat session
	"""
	status: Experimental_UIChatSessionStatus!
	"""
	When the session was last updated
	"""
	updatedAt: DateTime!
}

"""
Represents an action that can be taken in a chat item.
"""
type Experimental_UIChatAction {
	"""
	Encoded action key for processing the action
	"""
	actionId: ID!
	"""
	Optional icon to display for action
	"""
	icon: String
	"""
	Optional mode for the action
	"""
	mode: Experimental_UIChatActionMode
	"""
	Display text for the action
	"""
	text: LocalizedString!
}

"""
Mode values for chat actions.
PRIMARY - primary CTA such as Submit
SECONDARY - secondary CTA such as Cancel
DESTRUCTIVE - destructive / dangerous CTA such as Delete
"""
enum Experimental_UIChatActionMode {
	DESTRUCTIVE
	PRIMARY
	SECONDARY
}

"""
Represents an action prompt in the UI chat system.
This is for decision/choice prompts where the user must select from available actions.
"""
type Experimental_UIChatActionPrompt {
	"""
	Available actions the user can take
	"""
	actions: [Experimental_UIChatAction]!
	"""
	Error message to display when status is INVALID, null otherwise
	"""
	errorMessage: LocalizedString
	"""
	Unique identifier for the chat item
	"""
	id: ID!
	"""
	The prompt content to display to the user
	"""
	prompt: LocalizedMarkdown!
	"""
	The user's response, if they have responded
	This is null when status is PENDING_RESPONSE and populated when status is RESPONDED or INVALID.
	"""
	response: LocalizedString
	"""
	Current status of this chat item
	"""
	status: Experimental_UIChatItemStatus!
	"""
	Timestamp when the chat item was created
	"""
	timestamp: DateTime!
}

"""
Represents an article display in the UI chat system.
This is for rendering external article content within the chat flow with no user interaction.
"""
type Experimental_UIChatArticle {
	"""
	The article content to display as markdown
	"""
	articleContent: LocalizedMarkdown!
	"""
	Optional link to the original source article. Returns null if no external source link is available.
	"""
	articleLink: LocalizedLink
	"""
	Unique identifier for the chat item
	"""
	id: ID!
	"""
	Timestamp when the chat item was created
	"""
	timestamp: DateTime!
}

type Experimental_UIChatFormElementResponse {
	"""
	The form element ID the associated data values relate to
	"""
	id: ID!
	"""
	The 0 or more values associated with the form element.
	While most form elements have a single value, some such as multi-select or attribute-pickers will support
	multiple options. For extensibility we are making this an array type.
	"""
	values: [String]!
}

"""
Represents a form prompt in the UI chat system.
Contains form elements that match the existing UIWorkflowElementOrGroup structure.
"""
type Experimental_UIChatFormPrompt {
	"""
	Available actions the user can take
	"""
	actions: [Experimental_UIChatAction]!
	"""
	Form elements that reuse the existing workflow element structure
	"""
	elements: [UIWorkflowElementOrGroup]!
	"""
	Error message to display when status is INVALID, null otherwise
	"""
	errorMessage: LocalizedString
	"""
	Unique identifier for the chat item
	"""
	id: ID!
	"""
	The prompt/instructions to display to the user
	"""
	prompt: LocalizedMarkdown!
	"""
	The user's response, if they have responded, matching the input structure.
	This is null when status is PENDING_RESPONSE and populated when status is RESPONDED or INVALID.
	"""
	response: [Experimental_UIChatFormElementResponse]
	"""
	Current status of this chat item
	"""
	status: Experimental_UIChatItemStatus!
	"""
	Optional summary information for completed forms.
	Returns null when status is PENDING_RESPONSE or INVALID.
	Populated when status is RESPONDED to provide a summary view of submitted values.
	"""
	summary: Experimental_UIChatSummary
	"""
	Timestamp when the chat item was created
	"""
	timestamp: DateTime!
}

"""
Represents an input prompt in the UI chat system.
This is for prompts where the user needs to provide text input.
"""
type Experimental_UIChatInputPrompt {
	"""
	Available actions the user can take
	"""
	actions: [Experimental_UIChatAction]!
	"""
	Error message to display when status is INVALID, null otherwise
	"""
	errorMessage: LocalizedString
	"""
	Unique identifier for the chat item
	"""
	id: ID!
	"""
	Placeholder text to show in the input field
	"""
	placeholder: LocalizedString
	"""
	The prompt content to display to the user
	"""
	prompt: LocalizedMarkdown!
	"""
	The user's response, if they have responded
	This is null when status is PENDING_RESPONSE and populated when status is RESPONDED or INVALID.
	"""
	response: LocalizedString
	"""
	Current status of this chat item
	"""
	status: Experimental_UIChatItemStatus!
	"""
	Timestamp when the chat item was created
	"""
	timestamp: DateTime!
}

"""
Status values for chat items that require user interaction.
"""
enum Experimental_UIChatItemStatus {
	INVALID
	PENDING_RESPONSE
	RESPONDED
}

"""
Union type for all possible UI chat items that can be returned from a chat session.
"""
union Experimental_UIChatItemUnion =
	| Experimental_UIChatActionPrompt
	| Experimental_UIChatArticle
	| Experimental_UIChatFormPrompt
	| Experimental_UIChatInputPrompt
	| Experimental_UIChatListPrompt
	| Experimental_UIChatSystemPrompt
	| Experimental_UIChatWorkflowExecutionItem

"""
Represents a data item within a list chat item.
"""
type Experimental_UIChatListDataItem {
	"""
	Actions that apply specifically to this individual list item (e.g., "Select", "Edit", "Delete").
	These are typically displayed as buttons or menu options for each item.
	"""
	actions: [Experimental_UIChatAction]!
	"""
	Unique identifier for the list item
	"""
	id: ID!
	"""
	Optional image URL
	"""
	imageUrl: URL
	"""
	Primary text to display
	"""
	primaryText: LocalizedString!
	"""
	Optional secondary text to display
	"""
	secondaryText: LocalizedString
}

"""
Represents a list prompt in the UI chat system.
"""
type Experimental_UIChatListPrompt {
	"""
	Actions that apply to the entire list (e.g., "Cancel", "Skip", "Go Back").
	These are typically displayed at the bottom of the list or as general list controls.
	"""
	actions: [Experimental_UIChatAction]!
	"""
	Error message to display when status is INVALID, null otherwise
	"""
	errorMessage: LocalizedString
	"""
	Unique identifier for the chat item
	"""
	id: ID!
	"""
	List of items to display, ordered by display order
	"""
	items: [Experimental_UIChatListDataItem]!
	"""
	The prompt/instructions to display to the user
	"""
	prompt: LocalizedMarkdown!
	"""
	The user's response, if they have responded
	This is null when status is PENDING_RESPONSE and populated when status is RESPONDED or INVALID.
	"""
	response: LocalizedString
	"""
	Current status of this chat item
	"""
	status: Experimental_UIChatItemStatus!
	"""
	Timestamp when the chat item was created
	"""
	timestamp: DateTime!
}

"""
Status values for chat sessions.
"""
enum Experimental_UIChatSessionStatus {
	ABANDONED
	ACTIVE
	COMPLETED
	ERROR
}

"""
Summary information for completed forms.
Contains processed display items with labels and values for showing a summary to the user.
"""
type Experimental_UIChatSummary {
	"""
	Processed summary items for display, each with a label and value.
	Returns empty array if summary exists but has no displayable items.
	"""
	items: [Experimental_UIChatSummaryItem]!
	"""
	Summary title
	"""
	title: LocalizedString!
}

"""
Represents a single item in a chat form summary.
Contains a label-value pair for display purposes.
"""
type Experimental_UIChatSummaryItem {
	"""
	Display label for this summary item
	"""
	label: LocalizedString!
	"""
	Display value for this summary item
	"""
	value: LocalizedString!
}

"""
Represents a simple system message in the UI chat system.
This is for informational messages from the system/bot with no user interaction required.
"""
type Experimental_UIChatSystemPrompt {
	"""
	Unique identifier for the chat item
	"""
	id: ID!
	"""
	The message content to display to the user
	"""
	prompt: LocalizedMarkdown!
	"""
	Timestamp when the chat item was created
	"""
	timestamp: DateTime!
}

"""
Represents a workflow execution chat item in the UI chat system.
"""
type Experimental_UIChatWorkflowExecutionItem {
	"""
	Action to close/dismiss the workflow
	"""
	closeAction: Experimental_UIChatAction!
	"""
	Execution identifier
	"""
	executionId: ID!
	"""
	Unique identifier for the chat item
	"""
	id: ID!
	"""
	Message about the workflow execution
	"""
	message: LocalizedMarkdown!
	"""
	Current status of the workflow execution
	"""
	status: Experimental_UIChatWorkflowStatus!
	"""
	Timestamp when the chat item was created
	"""
	timestamp: DateTime!
	"""
	Workflow identifier
	"""
	workflowId: ID!
}

"""
Status values for workflow execution chat items.
"""
enum Experimental_UIChatWorkflowStatus {
	COMPLETED
	FAILED
	PENDING
	STARTED
}

"""
The behaviour carried by a clickable body element (for example a Happy or Not
face): what the engine does when that element is clicked. Applies only to
interactive action elements, not to display or input elements.
"""
type Experimental_UIWorkflowElementAction {
	"""
	An engine-generated token representing the operation to perform. Its contents are internal to the engine.
	"""
	actionKey: String!
	"""
	The kind of execution transition this action triggers.
	"""
	operation: Experimental_UIWorkflowElementActionOperation!
}

"""
The kind of workflow execution transition a body-element action triggers.
"""
enum Experimental_UIWorkflowElementActionOperation {
	"""
	Launch a fresh, independent execution.
	"""
	START
	"""
	Advance the current execution.
	"""
	UPDATE
}

"""
A body element presenting an optional prompt and three happiness faces
(happy / neutral / unhappy). Each face carries its own action describing what
happens when it is clicked; the faces are intrinsic to the element.

Weblab gating (Standard H1): access is enforced at authoring time in the
workflow engine — a contribution thank-you step only emits this element when
the customer resolves to the gating weblab (`IMDB_SDUI_EMBEDDED_HON_1429116`).
Clients outside the experiment never receive it. An `@experimental(weblab:)`
directive cannot be applied here because it targets FIELD_DEFINITION, not union
members.
"""
type Experimental_UIWorkflowHappyOrNotElement {
	"""
	Action dispatched when the happy face is clicked.
	"""
	happy: Experimental_UIWorkflowElementAction!
	"""
	Unique identifier for the element.
	"""
	id: ID!
	"""
	Action dispatched when the neutral face is clicked.
	"""
	neutral: Experimental_UIWorkflowElementAction!
	"""
	Optional plain-text prompt shown above the faces.
	"""
	prompt: String
	"""
	Action dispatched when the unhappy face is clicked.
	"""
	unhappy: Experimental_UIWorkflowElementAction!
}

"""
Edge type for search options containing a search option node and pagination metadata.
"""
type Experimental_UiWorkflowOptionsSearchEdge {
	"""
	Cursor for this edge
	"""
	cursor: ID!
	"""
	The search option at this edge
	"""
	node: UIWorkflowSearchOption!
	"""
	Position of this edge in the connection
	"""
	position: Int!
}

"""
Input for the experimental_uiWorkflowOptionsSearch query to fetch searchable options for form elements.
"""
input Experimental_UiWorkflowOptionsSearchInput {
	"""
	Pagination cursor for retrieving additional results after a specific item.
	Pass the cursor value from the previous response's pageInfo to fetch the next page.
	If omitted, fetches the first page.
	"""
	after: ID
	"""
	Constraints to filter the data source query results. If omitted, no constraints are applied
	"""
	constraints: Experimental_UiWorkflowSearchConstraintsInput
	"""
	Data source identifier specifying which custom data source to query.
	Clients should use the data source id returned in the workflow from SDUI.
	"""
	dataSourceId: ID!
	"""
	Maximum number of results to return per page. Must be between 1 and 250.
	"""
	first: Int!
	"""
	All current form values from the workflow form. The server extracts relevant values based on the data source type.
	If omitted, no form values are used for data source filtering
	"""
	formValues: [UIWorkflowFormElementValues]
	"""
	Sort configuration. If omitted, results are returned in the order they are stored in the data source
	"""
	sort: Experimental_UiWorkflowSearchSortInput
}

"""
Output from the experimental_uiWorkflowOptionsSearch query containing searchable options in standardized format.
Implements Connection interface for consistent pagination patterns.
"""
type Experimental_UiWorkflowOptionsSearchOutput {
	"""
	List of edges containing search options and cursors
	"""
	edges: [Experimental_UiWorkflowOptionsSearchEdge]!
	"""
	Pagination information for loading additional results
	"""
	pageInfo: PageInfo!
}

"""
Custom data source search constraints input.
Supports both SDUI-managed fixed option lists and custom backend services.
"""
input Experimental_UiWorkflowSearchConstraintsInput {
	"""
	The search query text to filter results. If omitted, returns all available options
	"""
	searchTerm: String
}

"""
Sort configuration input.
"""
input Experimental_UiWorkflowSearchSortInput {
	"""
	Field to sort by
	"""
	sortBy: Experimental_UiWorkflowSearchSortOption!
	"""
	Sort direction
	"""
	sortOrder: SortOrder!
}

"""
Sort by options
"""
enum Experimental_UiWorkflowSearchSortOption {
	NAME
}

"""
A user's standing in a specific scope.
"""
type Experimental_UserScopeStanding {
	"""
	The user's current streak in this scope. 0 if no active streak.
	"""
	currentStreak: Int!
	"""
	The user's longest-ever streak in this scope.
	"""
	longestStreak: Int!
	"""
	Points the user earned in this scope.
	"""
	points: Float!
	"""
	The user's rank in this scope (competition ranking).
	"""
	rank: Int!
	"""
	The const ID of the scope (e.g. 'in0000076').
	"""
	scopeId: ID!
}

"""
Experimental Field: DO NOT USE. Cast details
"""
type ExperimentalCast implements ExperimentalCredit {
	attributes(limit: Int): [CreditAttribute]
	category: CreditCategory!
	"""
	Characters (e.g. 'Neo')
	"""
	characters(limit: Int): [Character]
	"""
	Experimental Field: DO NOT USE
	"""
	experimental_episodeCredits(
		after: ID
		before: ID
		filter: EpisodeCreditsFilter
		first: Int
		last: Int
		sort: EpisodeCreditsSort
	): ExperimentalEpisodeCastConnection
	name: Name!
	"""
	A cast credit may have an optional position attribute indicating the order of
	appearance within the title’s ending credits. This position can be null for
	some or all of the title’s credits. By default (see CreditConnection.orderBy),
	the cast credit that has a value in their position field will be ordered first
	(by that number), then all other cast credits that have a null in their
	position field will be ordered alphabetically.
	"""
	position: Int
	title: Title!
}

"""
Experimental Field: DO NOT USE.
Credit details. Open question: do we want to add a persistent ID for credits?
Without this we require a nameID, titleID and Category to uniquely identify it.
"""
interface ExperimentalCredit {
	"""
	Category (e.g. 'Producer').
	Open question: the name of this field comes from TitleCreditsBaseV5. Should we change it?
	"""
	category: CreditCategory!
	"""
	The person credited
	"""
	name: Name!
	"""
	The title on which this credit appears
	"""
	title: Title!
}

"""
Experimental Field: DO NOT USE. Pagination for Credit type on a Title.
"""
type ExperimentalCreditConnection {
	"""
	Indicates whether the requested filmography data accurately reflects the onscreen cast or crew credits
	"""
	completenessStatus: CreditsCompletenessStatus
	"""
	Pages of credits
	"""
	edges: [ExperimentalCreditEdge]!
	"""
	How the credits are ordered
	"""
	orderBy: CreditsOrderedBy
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Restriction related information if exists for the connection
	"""
	restriction: CreditRestriction
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Experimental Field: DO NOT USE. Page of credits
"""
type ExperimentalCreditEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A Credit node
	"""
	node: ExperimentalCredit!
	"""
	Position of the node in this context, eg: Credit 4 out of 31
	"""
	position: Int!
}

"""
Experimental Field: DO NOT USE. Crew details
"""
type ExperimentalCrew implements ExperimentalCredit {
	attributes(limit: Int): [CreditAttribute]
	category: CreditCategory!
	"""
	Experimental Field: DO NOT USE
	"""
	experimental_episodeCredits(
		after: ID
		before: ID
		filter: EpisodeCreditsFilter
		first: Int
		last: Int
		sort: EpisodeCreditsSort
	): ExperimentalEpisodeCrewConnection
	"""
	List of job and related Attributes
	"""
	jobDetails(limit: Int): [CrewJobDetails]
	"""
	List of jobs (e.g. 'Executive Producer')
	Open questions: the name of this field comes from TitleCreditsBaseV5. Should we change it?
	"""
	jobs(limit: Int): [CrewJob]
	name: Name!
	title: Title!
}

"""
Experimental Field: DO NOT USE. Pagination for Cast credit type on a Cast credit (for episode credits of a series title).
"""
type ExperimentalEpisodeCastConnection {
	"""
	All of the characters in this list of Cast credits
	"""
	characters(limit: Int): [Character]
	"""
	The seasons of episodes in the connection

	Sorted in increasing order by numerical seasons
	followed by non-numerical season in alphabetical order e.g. [1, 2, 3, C, D]
	"""
	displayableSeasons(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): DisplayableSeasonConnection
	"""
	The years of episodes in the connection

	Sorted in increasing order by numerical years
	followed by non-numerical in alphabetical order e.g. [2015, 2018, 2020, UNKNOWN]
	"""
	displayableYears(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): DisplayableYearConnection
	edges: [ExperimentalCreditEdge]!
	pageInfo: PageInfo!
	total: Int!
	"""
	The earliest and latest release years for the Title(s) in these credits. If these would be the same,
	only a single year is populated.
	"""
	yearRange: YearRange
}

"""
Experimental Field: DO NOT USE. Pagination for Crew credit type on a Crew credit (for episode credits of a series title).
"""
type ExperimentalEpisodeCrewConnection {
	"""
	The seasons of episodes in the connection

	Sorted in increasing order by numerical seasons
	followed by non-numerical season in alphabetical order e.g. [1, 2, 3, C, D]
	"""
	displayableSeasons(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): DisplayableSeasonConnection
	"""
	The years of episodes in the connection

	Sorted in increasing order by numerical years
	followed by non-numerical in alphabetical order e.g. [2015, 2018, 2020, UNKNOWN]
	"""
	displayableYears(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): DisplayableYearConnection
	edges: [ExperimentalCreditEdge]!
	"""
	All of the jobs in this list of Crew credits
	"""
	jobs(limit: Int): [CrewJob]
	pageInfo: PageInfo!
	total: Int!
	"""
	The earliest and latest release years for the Title(s) in these credits. If these would be the same,
	only a single year is populated.
	"""
	yearRange: YearRange
}

type ExperimentalNameCreditCategoryWithCredits {
	category: CreditCategory!
	credits(first: Int): ExperimentalNameCreditConnection
}

"""
Experimental Field: DO NOT USE. Pagination for Credit type on a Name.
"""
type ExperimentalNameCreditConnection {
	"""
	Pages of credits
	"""
	edges: [ExperimentalCreditEdge]!
	"""
	How the credits are ordered
	"""
	orderBy: CreditsOrderedBy
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Restriction related information if exists for the connection
	"""
	restriction: CreditRestriction
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Experimental Field: DO NOT USE.
"""
input ExperimentalTitleCreditsFilter {
	"""
	Filter for specific credit categories
	"""
	categories: [ID]
	"""
	Filter to maintain current implementation of copying jobs to attributes
	"""
	copy_jobs: Boolean
	"""
	Filter for credited or uncredited credits only
	"""
	credited: CreditedOrUncreditedFilter
	"""
	Filter out specific credit categories
	"""
	excludeCategories: [ID]
	excludePrincipal: Boolean
	"""
	Filter credits on these specific names
	"""
	names: [ID]
}

enum ExplicitContentFilter {
	"""
	Remove names that are flagged as adult from the results, this is the default
	"""
	EXCLUDE_ADULT
	"""
	Allow names that are flagged as adult to be included in results
	"""
	INCLUDE_ADULT
	"""
	Only allow names that are flagged as adult
	"""
	ONLY_ADULT
}

input ExplicitContentSearchConstraint {
	"""
	Allow titles that are flagged as adult to be included in results
	By default, no titles flagged as adult will be matched
	"""
	explicitContentFilter: ExplicitContentFilter
}

interface ExportDetail {
	"""
	The date when the export becomes unavailable.
	After this date, the export will no longer be returned by the backend.
	"""
	expiresOn: DateTime
	exportType: ExportType!
	resultUrl: URL
	startedOn: DateTime!
	status: ExportStatus!
	"""
	number of objects in the export
	"""
	totalExportedObjects: Int
}

type ExportDetailConnection {
	edges: [ExportDetailEdge]!
	pageInfo: PageInfo!
	total: Int!
}

type ExportDetailEdge {
	cursor: ID!
	node: ExportDetail!
}

input ExportFilterByInput {
	status: [ExportStatusId]
}

enum ExportSortByField {
	STARTED_ON
}

input ExportSortByInput {
	by: ExportSortByField!
	order: SortOrder!
}

type ExportStatus {
	id: ExportStatusId
	name: ExportStatusName
}

enum ExportStatusId {
	"""
	Indicates there's an existing export for the request and it has failed.
	"""
	FAILED
	"""
	Indicates there's an existing export for the request and it's in progress.
	"""
	PROCESSING
	"""
	Indicates there's an existing export for the request and it has finished successfully.
	"""
	READY
	"""
	Indicates there's an existing export for the request and it has failed due to
	the requester not having access to the requested resource.

	This state is typically unexpected since users are generally expected to have
	the necessary access to the list/ratings before requesting an export. However,
	it can occur in unusual cases, such as when there is a change in list
	permissions immediately before the export starts or if a user issues GraphQL
	queries directly without proper access to the resource.
	"""
	UNAUTHORIZED
}

type ExportStatusName {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

enum ExportType {
	LIST
	RATINGS
}

"""
External link details
"""
type ExternalLink {
	"""
	The external link as a displayable property
	"""
	displayableProperty: DisplayableExternalLinkProperty!
	"""
	The type of link
	"""
	externalLinkCategory: ExternalLinkCategory!
	"""
	The languages supported by the site
	"""
	externalLinkLanguages: [DisplayableLanguage]
	"""
	The user-supplied region of the site.
	"""
	externalLinkRegion: DisplayableCountry
	id: ID!
	"""
	The link's label
	"""
	label: String
	"""
	The language of the link's label
	"""
	labelLanguage: DisplayableLanguage
	"""
	The URL of the site
	"""
	url: String!
}

"""
External link type
"""
type ExternalLinkCategory {
	"""
	A token representing the link type.
	"""
	id: ID!
	"""
	Display text for the link type (e.g. 'Official Sites').
	"""
	text: String!
}

type ExternalLinkCategoryWithExternalLinks {
	category: ExternalLinkCategory
	externalLinks(first: Int): ExternalLinkConnection
	restriction: ExternalLinkRestriction
}

"""
Pagination for external link types which always has a known total number of edges.
"""
type ExternalLinkConnection {
	"""
	Pages of external links
	"""
	edges: [ExternalLinkEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	restriction: ExternalLinkRestriction
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of external links
"""
type ExternalLinkEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	An External link node
	"""
	node: ExternalLink!
	"""
	Position of the node in this context, eg: external link 4 out of 31
	"""
	position: Int!
}

"""
Information about restrictions applied to external links
"""
type ExternalLinkRestriction {
	"""
	Explanation(s) for the restriction
	"""
	explanations: [RestrictionExplanation]!
	reasons: [ContentRestrictionReason]!
	"""
	DEPRECATED: Renamed, use `reasons` instead.
	"""
	restrictionReason: [ContentRestrictionReason]!
	"""
	The total number of items if there was no restriction
	"""
	unrestrictedTotal: Int
}

input ExternalLinksFilter {
	categories: [ID]
	excludeCategories: [ID]
}

input EyeColorsConstraint {
	"""
	A name match must have AT LEAST ONE of these eye colors.
	Heterochromia cases can be represented by the MIXED eye color enum.
	No filtering is applied if omitted.
	"""
	anyEyeColors: [EyeColorType]
}

"""
Eye color types for talent
"""
enum EyeColorType {
	BLUE
	BROWN
	GRAY
	GREEN
	HAZEL
	MIXED
}

"""
Represents a person's family name (surname).
"""
type FamilyName {
	"""
	The full text representation of the person's family name.
	"""
	text: String!
}

type FanPicksConnection {
	edges: [FanPicksEdge]!
	pageInfo: PageInfo!
	refTag: RefTag
}

type FanPicksEdge {
	cursor: ID!
	node: Title!
	position: Int!
}

input FanPicksFilter {
	"""
	Includes fan picks titles for below title type category values. At time of writing, fanPicksTitles() only
	supports movie and tv title type categories, and can only support filtering for a single title type.
	"""
	includeTitleTypes: [TitleTypeCategoryValue]
}

"""
Faq details
"""
type Faq {
	"""
	The answer text. Note that [not all questions have answers](https://www.imdb.com/title/tt10547784/quotes/qt4903101).
	"""
	answer: Markdown
	"""
	FAQ ID
	"""
	id: ID!
	"""
	Is this faq item a spoiler
	"""
	isSpoiler: Boolean!
	"""
	The language in which the FAQ was written. Note that each question-answer pair has the same language.
	"""
	language: DisplayableLanguage!
	"""
	Machine translated answer text. Returns the answer field translated based on the language header.
	"""
	machineTranslatedAnswer: MachineTranslatedMarkdown
	"""
	Machine translated question text. Returns the question field translated based on the language header.
	"""
	machineTranslatedQuestion: MachineTranslatedMarkdown!
	"""
	The question text
	"""
	question: Markdown!
}

"""
Pagination for FAQ types which always has a known total number of edges.
"""
type FaqConnection {
	"""
	Pages of FAQs
	"""
	edges: [FaqEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of Faqs
"""
type FaqEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A FAQ node
	"""
	node: Faq!
	"""
	Position of the node in this context, eg: FAQ 4 out of 31
	"""
	position: Int!
}

input FaqsFilter {
	hasAnswer: Boolean
	spoilers: FilterSpoilers
}

"""
Enum that includes values for filtering cinemas based on whether a user has marked a cinemas as a favorite.
Specifying this filter option will make the query personalized, so it will result in the query not being cached.
"""
enum FavoriteFilterOption {
	"""
	Don't include cinemas that a user has marked as a favorite.
	"""
	EXCLUDE_FAVORITES
	"""
	Only include cinemas that a user has marked as a favorite.
	"""
	ONLY_FAVORITES
}

type FeatureAccess {
	"""
	Signal that a customer can access the end credit upload functionality.

	Returns true if the customer has the required entitlement, false if not.
	Returns false if customer is logged out.

	@entitlement(IMDB_STAFF, SCORECARD)
	"""
	endCreditsFileUploadEnabled: Boolean!
	"""
	Signal that a customer can access staff actions for end credits (e.g. assign, view uploaded by).

	Returns true if the customer has the required entitlement, false if not.
	Returns false if customer is logged out.

	@entitlement(IMDB_STAFF)
	"""
	endCreditsUploadStaffActionsEnabled: Boolean!
	"""
	Whether the customer's detected region is eligible for Alexa+ features.
	Uses IP-based detection via the x-imdb-detected-country Zuko header.
	Returns false if the detected region is not in the eligible set for ALEXA_PLUS, or if the header is absent.
	"""
	isRegionEligibleForAlexaPlus: Boolean!
	"""
	Signal that a customer can use the IMDb Pro App.
	Pro app access does not correspond 1-1 to any specific entitled API, which is why this field is needed.

	Returns true if the customer can access the app, false if not.
	Returns false if customer is logged out.

	Zuko does not support cache TTLs on customer-specific requests.
	When true, it is acceptable to cache this field during a user's session.
	When false, it is not acceptable to cache this field, so we do not withhold app access immediately after signup.

	@entitlement(PRO_PREMIUM)
	"""
	proAppFunctionality: Boolean!
	"""
	Signal that a customer can use the IMDb Pro Basic functionality.
	Pro basic functionality does not correspond 1-1 to any specific entitled API, which is why this field is needed.

	Returns true if the customer can access basic Pro features, false if not.
	Returns false if customer is logged out.

	Zuko does not support cache TTLs on customer-specific requests.
	When true, it is acceptable to cache this field during a user's session.
	When false, it is not acceptable to cache this field, so we do not withhold access immediately after signup.

	@entitlement(PRO_BASIC)
	"""
	proBasicFunctionality: Boolean!
	"""
	Signal that a customer can use the Connections feature.
	Returns true if the customer is entitled to Connections access, false if not.
	Returns false if customer is logged out.

	@entitlement(PRO_CONNECTIONS_ACCESS)
	"""
	proConnectionsEnabled: Boolean!
	"""
	Signal that a customer can use functionality for the IMDbPro Name Owner.
	Pro name owner functionality does not correspond 1-1 to any specific entitled API, which is why this field is needed.

	Returns true if the customer can access name owner Pro features for the specified name (nconst), false if not.
	Returns false if customer is logged out.

	Zuko does not support cache TTLs on customer-specific requests.
	When true, it is acceptable to cache this field during a user's session.
	When false, it is not acceptable to cache this field, so we do not withhold access immediately after signup.

	@entitlement(NAME_OWNER)
	"""
	proNameOwnerFunctionality(nameId: ID!): Boolean!
	"""
	Signal that a customer can use the IMDb Pro Premium functionality.
	Pro premium functionality does not correspond 1-1 to any specific entitled API, which is why this field is needed.

	Returns true if the customer can access premium Pro features, false if not.
	Returns false if customer is logged out.

	Zuko does not support cache TTLs on customer-specific requests.
	When true, it is acceptable to cache this field during a user's session.
	When false, it is not acceptable to cache this field, so we do not withhold access immediately after signup.

	@entitlement(PRO_PREMIUM)
	"""
	proPremiumFunctionality: Boolean!
	"""
	Signal that a customer should see Pro Premium upsells.
	Returns true for users who should see premium upsells (those without PRO_PREMIUM entitlement).
	Returns false for PRO_PREMIUM users who should not see upsells.
	Returns true if customer is logged out.

	Zuko does not support cache TTLs on customer-specific requests.
	When true, it is not acceptable to cache this field, so we do want to keep showing upsells immediately after signup.
	When false, it is acceptable to cache this field during a user's session.

	@entitlement(PRO_PREMIUM)
	"""
	showProPremiumUpsells: Boolean!
}

enum FeaturedImagesOption {
	"""
	Return everything except featured images for this name.
	"""
	EXCLUDE_FEATURED
	"""
	Only return featured images for this name. There can be a maximum of 6.
	"""
	FEATURED_ONLY
}

type FeedbackGiven {
	"""
	Whether or not the user has given feedback for that feature
	"""
	hasGivenFeedback: Boolean!
}

enum FeedbackGivenFeatureType {
	"""
	Consumer Advanced Search Results Page
	"""
	CONSUMER_ADVANCED_SEARCH_RESULTS
	"""
	Consumer Main Search Results Page
	"""
	CONSUMER_MAIN_SEARCH_RESULTS
}

input FeedbackGivenInput {
	"""
	Feature type for whether or not feedback has been given for
	"""
	featureType: FeedbackGivenFeatureType!
}

type FileMetadata {
	"""
	Presigned URL containing file for account data
	"""
	accountDataURL: URL!
	"""
	Date and time file was created
	"""
	creationDate: DateTime!
	"""
	Date and time file will expire
	"""
	expirationDate: DateTime!
	"""
	Name of file returned from data access process
	"""
	name: String!
	"""
	Formatted size of file
	"""
	sizeLabel: LocalizedString!
}

type FilmingDates {
	"""
	Displayable filming dates range from start to end date
	"""
	dateRange: DisplayableDateRange
}

"""
Pagination for FilmingDates types which always has a known total number of edges.
"""
type FilmingDatesConnection {
	"""
	Pages of FilmingDates
	"""
	edges: [FilmingDatesEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of FilmingDates
"""
type FilmingDatesEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A FilmingDates node
	"""
	node: FilmingDates!
	"""
	Position of the node in this context, eg: FilmingDates 4 out of 31
	"""
	position: Int!
}

"""
Filming location details
"""
type FilmingLocation {
	"""
	Optional. A list of filming location attributes, can contain:
	- Free text attributes e.g. { text: 'Butch in his car encounters Marsellus Wallace' }
	- Location properties e.g. { id: 'exterior', text: 'Exterior' }
	"""
	attributes(limit: Int): [DisplayableAttribute]
	"""
	Filming location as displayable property
	"""
	displayableProperty: DisplayableTitleFilmingLocationProperty!
	"""
	Filming location ID
	"""
	id: ID!
	"""
	Votes from users about whether this filming location is interesting.
	"""
	interestScore: InterestScore!
	"""
	The language of the localized text
	"""
	language: DisplayableLanguage!
	"""
	The non-localized location, e.g. 'Los Alamos, New Mexico, USA'
	"""
	location: String!
	"""
	The filming location localized text
	"""
	text: String!
}

"""
Pagination for filming locations which always has a known total number of edges
"""
type FilmingLocationConnection {
	"""
	Pages of locations
	"""
	edges: [FilmingLocationEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Restriction related information if exists for the connection
	"""
	restriction: FilmingLocationRestriction
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of filming locations
"""
type FilmingLocationEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A filming location node
	"""
	node: FilmingLocation!
	"""
	Position of the node in this context, eg: Location 3 out of 28
	"""
	position: Int!
}

"""
Information about restrictions applied to filming locations
"""
type FilmingLocationRestriction {
	"""
	Explanation(s) for the restriction
	"""
	explanations: [RestrictionExplanation]!
	reasons: [ContentRestrictionReason]!
	"""
	DEPRECATED: Renamed, use `reasons` instead.
	"""
	restrictionReason: [ContentRestrictionReason]!
	"""
	The total number of items if there was no restriction
	"""
	unrestrictedTotal: Int
}

"""
Example: if you want to match titles of locations 'Los Angeles' OR 'Paris', PLUS
the title must contain 'Austin', specify anyKeywords: ['Los Angeles', 'Paris'] plus
allKeywords: ['Austin']
"""
input FilmingLocationSearchConstraint {
	"""
	A title match must contain ALL of these locations
	"""
	allLocations: [String]
	"""
	A title match must contain AT LEAST ONE of these locations
	"""
	anyLocations: [String]
}

"""
A film length or number of reels, or both, along with some flags and any attributes. For example, we could have a film
that used 3447 meters of film using 8.5 reels in the US and Canada.
"""
type FilmLength {
	attributes: [DisplayableAttribute]!
	"""
	The countries where this film length applies
	"""
	countries: [DisplayableCountry]
	"""
	Film length as displayable property
	"""
	displayableProperty: DisplayableTechnicalSpecificationLocalizedProperty!
	"""
	The film length in meters
	"""
	filmLength: Float
	"""
	Indicates if a split reel is used
	"""
	isSplitReel: Boolean!
	"""
	The number of reels
	"""
	numReels: Float
}

"""
Film lengths for this title.
"""
type FilmLengths {
	"""
	The list of film length items
	"""
	items: [FilmLength]!
	restriction: TechnicalSpecificationsRestriction
	"""
	Total number of film length items
	"""
	total: Int!
}

input FilmographyBoxOfficeEarningsConstraint {
	"""
	A name match must have at least one credit with box office earnings within this range.
	Infers min 0 and unbounded max if part of the range is not provided.
	"""
	boxOfficeRange: IntRangeInput!
}

input FilmographyBudgetConstraint {
	"""
	A name match must have at least one credit with a budget within this range.
	Infers min 0 and unbounded max if part of the range is not provided.
	"""
	budgetRange: IntRangeInput!
}

input FilmographyCompanyCreditsConstraint {
	"""
	A name match must have credits for ALL of these companies.
	Company IDs should be provided in IMDb company format.
	Example: "co0319272" for Amazon Studios, "co0476953" for Amazon Prime Video
	No filtering is applied if omitted.
	"""
	allCompanyIds: [ID]
	"""
	A name match must have credits for AT LEAST ONE of these companies.
	Company IDs should be provided in IMDb company format.
	Example: "co0319272" for Amazon Studios, "co0476953" for Amazon Prime Video
	No filtering is applied if omitted.
	"""
	anyCompanyIds: [ID]
	"""
	A name match must not have credits for any of these companies.
	Company IDs should be provided in IMDb company format.
	Example: "co0319272" for Amazon Studios, "co0476953" for Amazon Prime Video
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeCompanyIds: [ID]
}

input FilmographyCreditYearRangeConstraint {
	"""
	A name match must have a credit within their filmography released this year range.
	A filmography can have multiple release years, the match should choose the earliest one where they are credited.
	No filtering is applied if omitted.
	"""
	creditedYearRange: YearRangeInput
}

input FilmographyGenresConstraint {
	"""
	A name match must have credits for ALL of these genres.
	Free text string search for genres, i.e. Action, Adventure, etc.
	No filtering is applied if omitted.
	"""
	allGenres: [String]
	"""
	A name match must have credits for AT LEAST ONE of these genres.
	Free text string search for genres, i.e. Action, Adventure, etc.
	No filtering is applied if omitted.
	"""
	anyGenres: [String]
	"""
	A name match must not have credits for any of these genres.
	Free text string search for genres, i.e. Action, Adventure, etc.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeGenres: [String]
}

input FilmographyKeywordsConstraint {
	"""
	A name match must have credits that match ALL of these keyword terms.
	Free text string search for keyword terms, i.e. cooking, italian-football
	No filtering is applied if omitted.
	"""
	allKeywordTerms: [String]
	"""
	A name match must have credits that match AT LEAST ONE of these keyword terms.
	Free text string search for keyword terms, i.e. cooking, italian-football
	No filtering is applied if omitted.
	"""
	anyKeywordTerms: [String]
	"""
	A name match must not have credits that match any of these keyword terms.
	Free text string search for keyword terms, i.e. cooking, italian-football
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeKeywordTerms: [String]
}

input FilmographyNameCreditCategoryConstraint {
	"""
	A name match must have a credit category for ALL of these ids.
	No filtering is applied if omitted.
	Example: "amzn1.imdb.concept.name_credit_category.00000000-0000-0000-0000-000000000000"
	"""
	allCreditCategoryIds: [ID]
	"""
	A name match must have a credit category for AT LEAST ONE of these ids.
	No filtering is applied if omitted.
	Example: "amzn1.imdb.concept.name_credit_category.00000000-0000-0000-0000-000000000000"
	"""
	anyCreditCategoryIds: [ID]
	"""
	A name match must not have a credit category for any of these ids.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.

	Example: "amzn1.imdb.concept.name_credit_category.00000000-0000-0000-0000-000000000000"
	"""
	excludeCreditCategoryIds: [ID]
}

input FilmographyProductionStatusConstraint {
	"""
	A name match must have credits for ALL of these production stage types.
	No filtering is applied if omitted.
	"""
	allProductionStageIds: [ProductionStageType]
	"""
	A name match must have credits for AT LEAST ONE of these production stage types.
	No filtering is applied if omitted.
	"""
	anyProductionStageIds: [ProductionStageType]
	"""
	A name match must have credits for NONE of these production stage types.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.

	No filtering is applied if omitted.
	"""
	excludeProductionStageIds: [ProductionStageType]
}

"""
The constraints here are combined (AND).

example:

filmographyConstraint: {
    allTitleIds: ["tt0133093", "tt0209144"],
    anyTitleIds: ["tt0457572", "tt2357547"]
    excludeTitleIds: ["tt0468569"]
}
will match names that are credited in ("tt0133093" AND "tt0209144") AND ("tt0457572" OR "tt2357547") AND NOT "tt0468569"
"""
input FilmographySearchConstraint {
	"""
	A name match must be credited in ALL of these titles.
	"""
	allTitleIds: [ID]
	"""
	A name match must be credited in AT LEAST ONE of these titles.
	"""
	anyTitleIds: [ID]
	"""
	A name match must be credited in NONE of these titles.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeTitleIds: [ID]
}

input FilmographyTitleTypeConstraint {
	"""
	A name match must have credits for ALL of these title types.
	For example: movie, tvSeries, videoGame, etc.
	No filtering is applied if omitted.
	"""
	allTitleTypes: [String]
	"""
	A name match must have credits for AT LEAST ONE of these title types.
	For example: movie, tvSeries, videoGame, etc.
	No filtering is applied if omitted.
	"""
	anyTitleTypes: [String]
	"""
	A name match must not have credits for any of these title types.
	For example: movie, tvSeries, videoGame, etc.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeTitleTypes: [String]
}

"""
Match names based on filmography credit occupations.
Uses fuzzy matching to handle typos and variations.
"""
input FilmoOccupationsConstraint {
	"""
	Match names with any of these credit occupations.
	Searches both jobs and occupations fields in filmography using fuzzy matching to handle typos.

	No filtering is applied if omitted or empty. Duplicate values are ignored.
	"""
	anyOccupations: [String]
}

"""
Common enum for filtering options include/exclude/no filter(omitted)
"""
enum FilterInclusion {
	EXCLUDE
	INCLUDE
}

"""
Represents a single filter option with a display value for UI presentation
and a constraint value for use in search filter queries.
"""
type FilterOption {
	"""
	Constraint value for use in URL parameters and search queries (always English).
	"""
	constraintValue: String!
	"""
	Localized display value for UI presentation (translated via Panther pipeline).
	"""
	displayValue: LocalizedString!
}

"""
Represents a group of filter options for a specific filter ID.
Contains the filter's display name and all available options.
"""
type FilterOptionsGroup {
	"""
	Localized display name of the filter group (translated via Panther pipeline).
	"""
	displayName: LocalizedString!
	filterId: ID!
	options: [FilterOption]!
}

"""
Input for filterOptions query.
"""
input FilterOptionsInput {
	"""
	Array of filter IDs to retrieve options for. Returns empty results if empty.
	Duplicate IDs are deduplicated; only the first occurrence is kept.
	"""
	filterIds: [String]!
	"""
	Maximum number of filter groups to return. Must be a positive integer. Maximum
	value is 100. If first exceeds 100, only 100 results will be returned.
	"""
	first: Int!
	"""
	Selects which search domain's filter index to query. TITLE queries title-filters; NAME queries name-filters.
	"""
	searchType: SearchFilterType!
}

input FilterPlots {
	spoilers: FilterSpoilers
	type: [PlotType]
}

enum FilterSpoilers {
	EXCLUDE_SPOILERS
	SPOILERS_ONLY
}

enum FilterVersions {
	ALL_VERSIONS
	ORIGINAL_ONLY
}

input FloatRangeInput {
	max: Float
	min: Float
}

"""
Represents a floating-point threshold constraint with an associated error feedback that can be evaluated client-side.
Clients should always treat the threshold as inclusive (ie minimum 5.5 would mean 5.5 is valid).
Used to express numeric limits for number inputs that support decimal values.
TODO: Move into common schema validation types after review and update standards.
"""
type FloatThresholdValidationConstraint {
	"""
	The feedback to display to the customer related to the constraint.
	"""
	feedback: ValidationFeedbackContent!
	"""
	The threshold value for validation (inclusive)
	"""
	value: Float!
}

"""
Pagination for followed entities, which always has a known total number of edges.
"""
type FollowedEntitiesConnection {
	"""
	A page of followed entities.
	"""
	edges: [FollowedEntitiesEdge]!
	"""
	Details of the page.
	"""
	pageInfo: PageInfo!
	"""
	Total number of followed entities in the entire set.
	"""
	total: Int!
}

"""
Item in a page of followed entities.
"""
type FollowedEntitiesEdge {
	"""
	Cursor ID.
	"""
	cursor: ID!
	"""
	A followed entity node.
	"""
	node: FollowedEntity!
	"""
	Position of this item in this context.
	"""
	position: Int!
}

"""
Follow entity type union.
"""
union FollowedEntity = Interest

enum FollowedEntityType {
	INTEREST
}

type FollowerStatistics {
	"""
	Displayable Count of total number of users who have followed a followable entity
	"""
	displayableCount: LocalizedDisplayableCount!
	"""
	The total number of users who have followed a followable entity
	"""
	totalCount: Int!
}

"""
Follow filter options.
"""
input FollowFilter {
	"""
	Return followed entities meeting the specified type constraints.  If no
	followTypeConstraints are specified, all FollowEntityTypes are returned.
	"""
	followTypeConstraints: FollowTypeFilterConstraints
	"""
	Return followed entities meeting the specified interest constraints. If no
	interestConstraints are specified, all followed Interests are returned.
	"""
	interestConstraints: FollowInterestFilterConstraints
}

input FollowInterestFilterConstraints {
	"""
	Match followed Interests matching any of the requested Interest IDs
	"""
	anyInterestIds: [ID]!
}

input FollowSort {
	by: FollowSortBy!
	order: SortOrder!
}

enum FollowSortBy {
	"""
	Sort entities based on their last updated time
	DESC: Latest dates to earliest dates
	"""
	LAST_UPDATED
}

input FollowTypeFilterConstraints {
	"""
	Match followed entities matching any of the requested types
	"""
	anyFollowTypes: [FollowedEntityType]!
}

input ForgotPasswordRedirectURLInput {
	"""
	Association handle of the login context i.e. imdb_us
	"""
	associationHandle: String!
}

type ForgotPasswordRedirectURLOutput {
	"""
	Redirect URL for user forgot password flow
	"""
	redirectURL: URL!
}

enum Gender {
	FEMALE
	MALE
}

type GenderIdentity {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

input GenderIdentitySearchConstraint {
	"""
	returns names that identify as ANY of the gender identities in the list.
	"""
	anyGender: [NameGenderIdentity]
	"""
	returns names that identify as NONE of the gender identities in the list.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeGender: [NameGenderIdentity]
}

type GenreItem {
	"""
	Genre as displayable property
	"""
	displayableProperty: DisplayableTitleGenreProperty!
	"""
	A non-localized token representing the genre (e.g. 'Action').
	"""
	genreId: String!
	"""
	An opaque unique identifier for the genre.
	"""
	id: ID!
	"""
	The language the text is written in
	"""
	language: DisplayableLanguage!
	"""
	Display text for the genre (e.g. 'Action').
	"""
	text: String!
}

"""
Available genres https://tiny.amazon.com/1ch8tksjf/codeamazpackIMDbblobmainsrc
"""
input GenreSearchConstraint {
	"""
	A title match must contain ALL of the specified genres.

	Number of genres specified should not exceed the optional `maxRelevantGenres` parameter.
	"""
	allGenreIds: [String]
	"""
	A title match must contain at least one of the specified genres
	"""
	anyGenreIds: [String]
	"""
	Titles that include the specified genres will not be matched.

	Not affected by `maxRelevantGenres`.

	If the same input is specified in the any/all and exclude constraints,
	then the results should be empty.
	"""
	excludeGenreIds: [String]
	"""
	Specify a number as the n most relevant genres associated with a title to match against when also specifying
	`allGenreIds` and/or `anyGenreIds`. For example, if you provide a maxRelevantGenres of 2, only the two most relevant
	genres associated with a title will be considered for your search, and all others are ignored.

	Must be a postive non-zero number. Has no effect on `excludeGenreIds`. Has no effect if no genres are specified.
	Throws GRAPHQL_VALIDATION_FAILED if `maxRelevantGenres` is lower than the number of genres specified under
	`allGenreIds`
	"""
	maxRelevantGenres: Int
}

input GenreSort {
	by: GenreSortBy
	order: SortOrder
}

"""
Default Sorting is currently alphabetical by English value
"""
enum GenreSortBy {
	DEFAULT
	RELEVANCE
}

type GenreSummary {
	"""
	The genre
	"""
	genre: GenreItem!
	"""
	The total number of credits for the genre
	"""
	total: Int!
}

input GetExportsInput {
	exportTypes: [ExportType]!
}

input GetLatestUIWorkflowInput {
	"""
	The unique identifier for a UIWorkflow execution
	"""
	executionId: ID!
}

"""
Defines the output response type for the getLatestUIWorkflow query.
"""
type GetLatestUIWorkflowOutput {
	"""
	The latest server-driven UI workflow model stored by the service for the requested input.
	In the future there may be other types of workflow such as a full-page workflow so for extensibility it is
	optional.
	"""
	workflow: UIWorkflow
}

input GetNameWeightInput {
	unit: WeightUnit = POUND
}

input GetPlacementsInput {
	"""
	An optional set of page targeting criteria

	Symphony allows you to specify page targeting criteria in addition to container type, ID, and slot
	Documentation: https://w.amazon.com/bin/view/ContentSymphony/UserGuide#HSchedulingplacements
	"""
	additionalPageTargetingCriteria: [SymphonyMultiValueMapEntry]
	"""
	The id of the Symphony container.
	eg. tconst, nconst, home, india
	"""
	containerId: String!
	"""
	The type of symphony container.
	eg. imdb-app-title
	"""
	containerType: String!
	"""
	An optional set of query params

	Symphony allows you to override content in the form of query params. This is useful for content creators
	to preview new content, content in different languages, etc.
	Documentation: https://w.amazon.com/bin/view/ContentSymphony/UserGuide#HPreviewingyourcontent
	"""
	queryParams: [SymphonyMultiValueMapEntry]
	"""
	An array of the slots to retrieve placements for.
	eg. ["center-1", "center-2"]
	"""
	slotNames: [String]!
}

"""
Details of a single goof
"""
type Goof {
	category: GoofCategory!
	"""
	Goof as a displayable article
	"""
	displayableArticle: DisplayableArticle!
	"""
	Goof ID
	"""
	id: ID!
	"""
	Votes from users about whether this goof item is interesting.
	"""
	interestScore: InterestScore!
	"""
	Is this goof a spoiler
	"""
	isSpoiler: Boolean!
	"""
	Machine translated displayable article. Returns the displayable article translated based on the language header.
	"""
	machineTranslatedDisplayableArticle: MachineTranslatedDisplayableArticle
	"""
	Machine translated goof text. Returns the text field translated based on the language header.
	"""
	machineTranslatedText: MachineTranslatedMarkdown!
	"""
	The goof text
	"""
	text: Markdown!
}

"""
A category of goofs describing the nature of a subset of goofs, e.g. 'Continuity'
"""
type GoofCategory {
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	text: String!
}

type GoofCategoryWithGoofs {
	category: GoofCategory!
	goofs(filter: GoofCategoryWithGoofsFilter, first: Int): GoofConnection
}

input GoofCategoryWithGoofsFilter {
	spoilers: FilterSpoilers
}

"""
Pagination for Goof types which always has a known total number of edges.
"""
type GoofConnection {
	"""
	Pages of goofs
	"""
	edges: [GoofEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of goofs
"""
type GoofEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A Goof node
	"""
	node: Goof!
	"""
	Position of the node in this context, eg: Goof 4 out of 312
	"""
	position: Int!
}

"""
Example: if you want to match titles of goof 'no handcuffs visible' OR 're-appear', PLUS
the title must contain 'military building', specify anyGoofTextTerms: ['no handuffs visible',
 're-appear'] plus allGoofsTextTerms: ['military building']. Note that this is term match rather than
phrase match, e.g., 'no handcuffs visible' would match titles that contain 'no handcuffs are visible'
"""
input GoofMatchingSearchConstraint {
	"""
	A title match must contain ALL of these search terms in at least one of its goofs.
	"""
	allGoofTextTerms: [String]
	"""
	A title match must contain AT LEAST ONE of these search terms in at least one of its goofs
	"""
	anyGoofTextTerms: [String]
}

input GoofsFilter {
	categories: [ID]
	excludeCategories: [ID]
	spoilers: FilterSpoilers
}

type GranularDirective {
	"""
	Whether or not to allow a given action for this entity
	"""
	allow: Boolean!
	"""
	Identifier for the entity (currently purpose or vendor)
	"""
	id: ID!
}

"""
Input object for specifying how the grouping name should be displayed.
"""
input GroupingTextInput {
	"""
	Specifies the text format to return.
	Supported values:
	- 'DEFAULT' (example: "Director")
	- 'SHORT' (example: "Dir.")

	If omitted, defaults to 'DEFAULT'.
	"""
	format: String!
}

"""
Page of guild affilation
"""
type GuildAffiliationEdge {
	cursor: ID!
	node: GuildAffiliationVerificationStatus!
	"""
	Position of this item in this context.
	"""
	position: Int!
}

"""
Pagination for the list of affiliated guilds
"""
type GuildAffiliationsConnection {
	edges: [GuildAffiliationEdge]!
	pageInfo: PageInfo!
	"""
	Total number of affiliated guilds
	"""
	total: Int!
}

input GuildAffiliationsConstraint {
	"""
	A name match must have ALL of these self-verified guild affiliations.
	Self-verified affiliations are guild memberships that a name has claimed but have not been verified by IMDb.
	Guild affiliations should be provided as company IDs.
	Example: "co0015930" for SAG-AFTRA, "co0000406" for IATSE

	No filtering is applied if omitted or empty. Duplicate IDs are ignored.
	"""
	allSelfVerifiedGuildAffiliations: [ID]
	"""
	A name match must have ALL of these verified guild affiliations.
	Verified affiliations are guild memberships that have been verified by IMDb.
	Guild affiliations should be provided as company IDs.
	Example: "co0081427" for DGA, "co0024632" for WGA

	No filtering is applied if omitted or empty. Duplicate IDs are ignored.
	"""
	allVerifiedGuildAffiliations: [ID]
	"""
	A name match must have AT LEAST ONE of these self-verified guild affiliations.
	Self-verified affiliations are guild memberships that a name has claimed but have not been verified by IMDb.
	Guild affiliations should be provided as company IDs.
	Example: "co0015930" for SAG-AFTRA, "co0000406" for IATSE

	No filtering is applied if omitted or empty. Duplicate IDs are ignored.
	"""
	anySelfVerifiedGuildAffiliations: [ID]
	"""
	A name match must have AT LEAST ONE of these verified guild affiliations.
	Verified affiliations are guild memberships that have been verified by IMDb.
	Guild affiliations should be provided as company IDs.
	Example: "co0081427" for DGA, "co0024632" for WGA

	No filtering is applied if omitted or empty. Duplicate IDs are ignored.
	"""
	anyVerifiedGuildAffiliations: [ID]
	"""
	A name match must not have any of these self-verified guild affiliations.
	Self-verified affiliations are guild memberships that a name has claimed but have not been verified by IMDb.
	Guild affiliations should be provided as company IDs.
	Example: "co0015930" for SAG-AFTRA, "co0000406" for IATSE

	No filtering is applied if omitted or empty. Duplicate IDs are ignored.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeSelfVerifiedGuildAffiliations: [ID]
	"""
	A name match must not have any of these verified guild affiliations.
	Verified affiliations are guild memberships that have been verified by IMDb.
	Guild affiliations should be provided as company IDs.
	Example: "co0081427" for DGA, "co0024632" for WGA

	No filtering is applied if omitted or empty. Duplicate IDs are ignored.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeVerifiedGuildAffiliations: [ID]
}

"""
Filter to specify the data verification type to include
"""
input GuildAffiliationsFilter {
	includeDataVerificationTypes: [GuildDataVerificationType]
}

"""
The guild affiliation that includes the Company and verification status
"""
type GuildAffiliationVerificationStatus {
	"""
	The company that represents the guild
	"""
	company: Company!
	"""
	Whether this affiliation has been verified by the guild
	"""
	isVerifiedByGuild: Boolean!
}

"""
Pagination for the list of visibility settings for guilds a name is affiliated with
"""
type GuildAffiliationVisibilitiesConnection {
	edges: [GuildAffiliationVisibilitiesEdge]!
	pageInfo: PageInfo!
	"""
	Total number of guilds affiliated with this name
	"""
	total: Int!
}

"""
Page of guild affiliation visibilities
"""
type GuildAffiliationVisibilitiesEdge {
	cursor: ID!
	node: GuildAffiliationVisibilityStatus!
	"""
	Position of this item in this context
	"""
	position: Int!
}

"""
The visibility setting for guild affiliations
"""
enum GuildAffiliationVisibilityLevel {
	"""
	Visible on IMDbPro only
	"""
	IMDB_PRO_ONLY
	"""
	Publicly visible
	"""
	PUBLIC
}

"""
The visibility configured for a specific guild affiliation company for a name
"""
type GuildAffiliationVisibilityStatus {
	"""
	The company that represents the guild
	"""
	company: Company!
	"""
	The visibility level configured for the guild
	"""
	visibility: GuildAffiliationVisibilityLevel!
}

"""
The different verification types for guild data
"""
enum GuildDataVerificationType {
	"""
	The data has been verified by the customer
	"""
	SELF_VERIFIED
	"""
	The data has been verified by a third party
	"""
	THIRD_PARTY_VERIFIED
}

type GuildMembershipDetail {
	"""
	The company that represents the guild
	"""
	company: Company!
	"""
	The membershipId of the customer associated with the guild
	"""
	membershipId: String!
}

"""
Additional metadata describing guild status
"""
type GuildStatus {
	"""
	Whether or not the name is Actra Apprentice
	"""
	isActraApprentice: Boolean
	"""
	Whether or not the name is non-union
	"""
	isNonUnion: Boolean
	"""
	Whether or not the name is SAG eligible
	"""
	isSagEligible: Boolean
}

input GuildStatusConstraint {
	"""
	Filter names based on ACTRA apprentice membership.
	- true: Match names that are ACTRA apprentice members
	- false: Match names that are not ACTRA apprentice members
	- omitted (null): No filtering applied based on ACTRA apprentice status.
	"""
	isActraApprentice: Boolean
	"""
	Filter names based on non-union status.
	- true: Match names that are non-union members
	- false: Match names that are union members
	- omitted (null): No filtering applied based on non-union status.
	"""
	isNonUnion: Boolean
	"""
	Filter names based on SAG-AFTRA eligibility.
	- true: Match names that are SAG-eligible
	- false: Match names that are not SAG-eligible
	- omitted (null): No filtering applied based on SAG eligibility.
	"""
	isSagEligible: Boolean
}

input HairColorsConstraint {
	"""
	A name match must have ALL of these hair colors.
	No filtering is applied if omitted.
	"""
	allHairColors: [HairColorType]
	"""
	A name match must have AT LEAST ONE of these hair colors.
	No filtering is applied if omitted.
	"""
	anyHairColors: [HairColorType]
	"""
	A name match must not have any of these hair colors.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeHairColors: [HairColorType]
}

"""
Hair color types for talent
"""
enum HairColorType {
	BALD
	BLACK
	BLONDE
	BROWN
	GREY
	OTHER
	RED
	SALT_AND_PEPPER
	SILVER
	WHITE
}

"""
Haircut lengths and style types for talent
"""
enum HairLength {
	BALD
	BUZZ_CUT
	CHIN_LENGTH
	LONG
	RECEDING
	SHORT
	SHOULDER_LENGTH
}

input HairLengthsConstraint {
	"""
	A name match must have AT LEAST ONE of these hair lengths.
	No filtering is applied if omitted.
	"""
	anyHairLengths: [HairLength]
	"""
	A name match must not have any of these hair lengths.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeHairLengths: [HairLength]
}

input HeightConstraint {
	"""
	List of height ranges to search across different units.
	Each entry allows specifying a different unit of measurement (inches or centimeters).
	Results will match names that fall within ANY of the specified ranges (OR logic).
	"""
	heightRanges: [HeightRangeInput]!
}

input HeightRangeInput {
	"""
	Height range values.
	Infers min 0 and unbounded max if part of the range is not provided.
	"""
	heightRange: IntRangeInput!
	"""
	Unit of measurement for height values.
	"""
	unit: LinearUnit!
}

"""
Defines a link to a help article.
Clients can show a relevant help icon such as a question mark, based on its type.
"""
type HelpLink implements LocalizedLink {
	"""
	The localized label for the link, for example 'See help guide'
	"""
	label: LocalizedString!
	"""
	The URL target for the link.
	E.g. https://help.imdb.com/article/contribution/contribution-information/user-review-guidelines/GABTWSNLDNFLPRRH
	"""
	url: URL!
}

"""
Enumeration of available filters that hierarchies support.

Note that response types will vend hierarchyFilter as strings, not HierarchyFilter values. This is to avoid risk of
breaking clients when new values are added to the enum.
"""
enum HierarchyFilter {
	"""
	Identifies the given hierarchy as a locale filterable hierarchy.
	"""
	LOCALE
}

type Histogram {
	demographic: Demographic!
	histogramValues: [HistogramValues]!
}

type HistogramValues {
	rating: Int!
	voteCount: Int!
}

"""
Image type
Extends external type.
"""
type Image {
	"""
	Image caption text

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	caption: Markdown
	"""
	Image copyright text

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	copyright: String
	"""
	Returns the 'correct this image' Link.
	Will be null if there is no correction Link.

	---------------------
	Graphlet Details:
	- Name: kahlo
	- CTI: IMDb/GraphQL/Kahlo
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-contributors-tech
	"""
	correctionLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
		"""
		The const the image is being rendered from (name, list, etc).
		"""
		relatedId: ID
	): ContributionLink
	"""
	Countries associated with the image

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	countries(limit: Int): [DisplayableCountry]
	"""
	The creator of the image

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	createdBy: String
	"""
	Date the photo was created

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	createdOn: DisplayableDate
	"""
	Original height of the image resource, in pixels

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	height: Int
	id: ID!
	"""
	Languages of text in the image

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	languages(limit: Int): [DisplayableLanguage]
	"""
	Name IDs for people in the image

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	names(limit: Int): [Name]
	"""
	Returns the 'report this image' Link.
	Will be null if there is no report Link.

	---------------------
	Graphlet Details:
	- Name: kahlo
	- CTI: IMDb/GraphQL/Kahlo
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-contributors-tech
	"""
	reportingLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
		"""
		The const the image is being rendered from (name, list, etc).
		"""
		relatedId: ID
	): ContributionLink
	"""
	Image source and attribution data

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	source: Source
	"""
	Title IDs for titles related to the image

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	titles(limit: Int): [Title]
	"""
	Image type token

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	type: String
	"""
	URL for the image resource

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	url: String
	"""
	Original width of the image resource, in pixels

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	width: Int
}

type ImageAndLinkCallToAction {
	"""
	Contains the action link data
	"""
	action: ActionLink!
	"""
	Provides data for background image resource.
	"""
	backgroundImage: CallToActionImage
	"""
	An identifier for the returned result of a CTA.
	A CTA will have multiple potential results, with each result having its own identifier.
	This value is only unique within the CTA. It is not globally unique.
	The resultId can be used as resultOverride input argument to specify a specific result
	should be returned for the requested CTA.
	"""
	resultId: ResultID!
}

"""
Pagination for Image types which always has a known total number of edges.
"""
type ImageConnection {
	edges: [ImageEdge]!
	"""
	Indicates the possible image facets available for filtering on
	"""
	facets: ImageFacets
	"""
	Indicates that image collection was changed, pagination result might be outdated if true
	"""
	isPaginationDirty: Boolean
	pageInfo: PageInfo!
	restriction: ImageRestriction
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

type ImageEdge {
	cursor: ID!
	node: Image!
	"""
	Position of the node in this context. eg: Image 4 out of 312
	"""
	position: Int!
}

type ImageEditParameters {
	"""
	The cropping parameters to be applied to this name page's primary image
	"""
	cropBox: CroppingParameters
	"""
	The degree (0-360) by which the image should be rotated for display
	"""
	rotation: Float
}

type ImageFacets {
	"""
	Paginatable list of galleries related to this const's images, with associated count of how many images match
	"""
	galleries(
		after: ID
		before: ID
		first: Int
		last: Int
	): ImageGalleryFacetConnection
	"""
	Paginatable list of names related to this const's images, with associated count of how many images match
	"""
	names(after: ID, before: ID, first: Int, last: Int): NameFacetConnection
	"""
	Paginatable list of titles related to this const's images, with associated count of how many images match
	"""
	titles(after: ID, before: ID, first: Int, last: Int): TitleFacetConnection
	"""
	Image types with counts of how many images are of that type, sorted by their counts in descending order
	"""
	types(
		"""
		Optionally limit the number of image type filter options returned
		"""
		limit: Int
	): [ImageTypeFacet]
}

"""
File type of the image asset
"""
enum ImageFileType {
	JPG
	PNG
	SVG
}

type ImageGallery {
	galleryText: String
	id: ID!
	images(
		after: ID
		before: ID
		"""
		Parameter to go around cache, expected to use cursor as a value
		"""
		bust: String
		filter: ImagesFilter
		first: Int
		jumpTo: ID
		last: Int
	): ImageConnection
}

"""
Pagination for Image gallery facets
"""
type ImageGalleryFacetConnection {
	edges: [ImageGalleryFacetEdge]!
	pageInfo: PageInfo!
	"""
	Provides information on the total number of galleries you could filter on
	"""
	total: Int!
}

type ImageGalleryFacetEdge {
	cursor: ID!
	node: ImageGallery!
	position: Int!
	"""
	How many items are associated with this image gallery node
	"""
	total: Int!
}

input ImageGalleryFilterConstraints {
	"""
	Match images appearing in all of the requested galleries
	"""
	allGalleryIds: [ID]
	"""
	Match images appearing in any of the requested galleries
	"""
	anyGalleryIds: [ID]
}

input ImageNameFilterConstraints {
	"""
	Match images featuring all of the requested names
	"""
	allNameIds: [ID]
	"""
	Match images featuring any of the requested names
	"""
	anyNameIds: [ID]
}

"""
Information about restrictions applied to images
"""
type ImageRestriction {
	"""
	Explanation(s) for the restriction
	"""
	explanations: [RestrictionExplanation]!
	reasons: [ContentRestrictionReason]!
	"""
	DEPRECATED: Renamed, use `reasons` instead.
	"""
	restrictionReason: [ContentRestrictionReason]!
	"""
	The total number of items if there was no restriction
	"""
	unrestrictedTotal: Int
}

input ImagesFilter {
	"""
	Return images meeting the specified gallery constraints
	"""
	galleryConstraints: ImageGalleryFilterConstraints
	"""
	Return images meeting the specified name constraints
	"""
	nameConstraints: ImageNameFilterConstraints
	"""
	Return only images tagged with at least a minimum and/or at most a maximum number of names
	"""
	namesCount: CountInterval
	"""
	Return images meeting the specified title constraints
	"""
	titleConstraints: ImageTitleFilterConstraints
	"""
	Return only images which are one of these types
	"""
	types: [ID]
}

input ImageTitleFilterConstraints {
	"""
	Match images related to all of the requested titles
	"""
	allTitleIds: [ID]
	"""
	Match images related to any of the requested titles
	"""
	anyTitleIds: [ID]
}

"""
An ImageType provides meta information describing the type of a given image,
such as whether it is a Poster or a photo from an Event.
It includes a localized display label as well as a canonical non-localized imageTypeID,
that can be used as input to image filter APIs.
"""
type ImageType {
	"""
	An opaque ID for this localized concept
	"""
	id: ID!
	"""
	The canonical ID for this image type, for use as inputs to other fields, e.g. still_frame
	"""
	imageTypeId: String!
	"""
	The language of the display label returned in the text field
	"""
	language: DisplayableLanguage!
	"""
	The display label for the image type, such as Still Frame
	"""
	text: String!
}

"""
Provides information on how many images in a gallery match the specified image type
"""
type ImageTypeFacet {
	"""
	Number of images of this type
	"""
	total: Int!
	"""
	Image Type information including display label
	"""
	type: ImageType!
}

"""
Pagination for images by type, page info is intended for use with the related images field.
"""
type ImageTypesConnection {
	edges: [ImageEdge]!
	pageInfo: PageInfo!
	restriction: ImageRestriction
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

type ImageTypeWithImages {
	"""
	The image type common for all of the associated images
	"""
	imageType: ImageType!
	"""
	The first page of images in each category
	"""
	images(first: Int): ImageTypesConnection
}

"""
Match names based on whether to include deceased individuals.

Default behavior: Deceased names are excluded from search results.

Auto-enable: When deathDateConstraint, deathPlaceConstraint, or
withNameDataConstraint (with DEATH_DATE/DEATH_PLACE) are present,
deceased names are automatically included (unless includeDeceased is explicitly set to false).

Explicit control: Set includeDeceased to true to always include deceased names,
or false to always exclude them (even when death constraints are present).
"""
input IncludeDeceasedConstraint {
	"""
	Controls whether deceased names appear in search results.

	- true: Always include deceased names
	- false: Always exclude deceased names (even with death constraints)
	- null/undefined: Exclude deceased by default, but auto-include when death constraints are present
	"""
	includeDeceased: Boolean
}

"""
Placeholder for Interest type (IMDb interest entity).
Should be federated from the interest service.
"""
type Interest {
	"""
	The Category for this specific interest.

	Note: querying the nested `interests` connection from this path will throw an
	INVALID_PARAMETER error. Use the top-level `interestCategories` query instead.

	---------------------
	Graphlet Details:
	- Name: interests
	- CTI: IMDb/GraphQL/Interests
	- Affected by headers: x-imdb-client-name, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 600 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	category: InterestCategory
	"""
	The displayable description of this interest, e.g. 'Psycological horror is a subgenre of horror and psychological
	fiction...'.

	---------------------
	Graphlet Details:
	- Name: interests
	- CTI: IMDb/GraphQL/Interests
	- Affected by headers: x-imdb-client-name, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 600 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	description: LocalizedMarkdown
	"""
	Return Engagement Statistics for an interest

	---------------------
	Graphlet Details:
	- Name: engagementData
	- CTI: IMDb/GraphQL/Engagement Data
	- Affected by headers: x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-engagement-growth-public
	"""
	engagementStatistics: EngagementStatistics
	"""
	@experimental The leaderboard scoped to this interest. Convenience shortcut equivalent to
	experimental_gameLeaderboard(input: { scopeId: <this id>, gameType, timePeriod }).
	Null if leaderboard data is temporarily unavailable.

	---------------------
	Graphlet Details:
	- Name: leaderboard
	- CTI: IMDb/GraphQL/Leaderboard
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-engagement-growth-public
	"""
	experimental_gameLeaderboard(
		input: Experimental_GameLeaderboardScopeInput!
	): Experimental_GameLeaderboard
	"""
	A unique ID for this Interest.
	"""
	id: ID!
	"""
	The primary image for this interest.

	---------------------
	Graphlet Details:
	- Name: interests
	- CTI: IMDb/GraphQL/Interests
	- Affected by headers: x-imdb-client-name, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 600 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	primaryImage: Image
	"""
	The displayable primary descriptor of this interest, e.g. 'Psychological Horror'.

	---------------------
	Graphlet Details:
	- Name: interests
	- CTI: IMDb/GraphQL/Interests
	- Affected by headers: x-imdb-client-name, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 600 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	primaryText: InterestText
	"""
	The current score for this interest. A higher value is better.

	---------------------
	Graphlet Details:
	- Name: interests
	- CTI: IMDb/GraphQL/Interests
	- Affected by headers: x-imdb-client-name, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 600 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	score: InterestImportanceScore
	"""
	The displayable secondary descriptor of this interest, e.g. 'subgenre'.

	---------------------
	Graphlet Details:
	- Name: interests
	- CTI: IMDb/GraphQL/Interests
	- Affected by headers: x-imdb-client-name, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 600 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	secondaryText: InterestText
	"""
	Paginated list of similar interests. Sorted by the number of users following
	the interest (most to least). Supports page size of at most 250.
	Requesting a page size larger than the maximum supported will result in the maximum size being returned.

	---------------------
	Graphlet Details:
	- Name: interests
	- CTI: IMDb/GraphQL/Interests
	- Affected by headers: x-imdb-client-name, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 600 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	similarInterests(after: ID, first: Int!): InterestConnection
	"""
	The type classification of this interest.

	---------------------
	Graphlet Details:
	- Name: interests
	- CTI: IMDb/GraphQL/Interests
	- Affected by headers: x-imdb-client-name, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 600 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	type: InterestType
	"""
	Visibility status of the interest

	Only when the below header values are satisfied
	  x-imdb-internal-client == true and
	  x-imdb-non-1p-client == false and
	  x-imdb-client-name == 'imdb-search-indexer'
	will we return both PUBLIC and HIDDEN interests
	Otherwise, we will only return PUBLIC interests

	---------------------
	Graphlet Details:
	- Name: interests
	- CTI: IMDb/GraphQL/Interests
	- Affected by headers: x-imdb-client-name, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 600 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	visibilityLevel: InterestVisibilityLevel
}

"""
Filter options for interest categories.
"""
input InterestCategoriesFilter {
	"""
	Filter for specific interest categories. Category IDs are ephemeral and invalid IDs will be ignored.
	"""
	categories: [ID]
}

type InterestCategory {
	"""
	An opaque unique identifier for this InterestCategory.
	This can be used in InterestCategoriesFilter.categories to refine an interestCategories query, but should be assumed
	to be ephemeral like a pagination cursor.
	"""
	id: ID!
	"""
	A paginated list of all interests in this category, sorted alphabetically. Supports page size of at most 250.
	Requesting a page size larger than the maximum supported will result in the maximum size being returned.
	"""
	interests(after: ID, first: Int!): InterestConnection
	"""
	The language of the display text.
	"""
	language: DisplayableLanguage!
	"""
	The displayable description of this category, e.g. 'Horror'.
	"""
	text: String!
}

"""
Pagination for InterestCategory types which always has a known total number of edges.
"""
type InterestCategoryConnection {
	"""
	A page of InterestCategories.
	"""
	edges: [InterestCategoryEdge]!
	"""
	Details of the page.
	"""
	pageInfo: PageInfo!
	"""
	Total number of categories in the entire set.
	"""
	total: Int!
}

"""
Item in a page of InterestCategories.
"""
type InterestCategoryEdge {
	"""
	Cursor ID.
	"""
	cursor: ID!
	"""
	An InterestCategory node.
	"""
	node: InterestCategory!
}

"""
Pagination for Interest types which always has a known total number of edges.
"""
type InterestConnection {
	"""
	A page of Interests.
	"""
	edges: [InterestEdge]!
	"""
	Details of the page.
	"""
	pageInfo: PageInfo!
	"""
	Total number of interests in the entire set.
	"""
	total: Int!
}

"""
Item in a page of Interests.
"""
type InterestEdge {
	"""
	Cursor for this interest
	"""
	cursor: ID!
	"""
	The node for this interest.
	"""
	node: Interest!
}

type InterestImportanceScore {
	currentScore: Int!
}

"""
Votes from users about whether an item is interesting.
"""
type InterestScore {
	"""
	The number of users who found this interesting.
	"""
	usersInterested: Int!
	"""
	The total number of users who have voted on this item being interesting or not.
	"""
	usersVoted: Int!
}

"""
Pagination for interest search results.
"""
type InterestSearchConnection {
	edges: [InterestSearchEdge]!
	pageInfo: PageInfo!
	total: Int!
}

"""
Match titles based on their tagged interests.
Any combination of `all`, `any`, and `exclude` inputs can be specified.
Note that a title tagged with both an included and excluded interest would
still be excluded.
"""
input InterestSearchConstraint {
	"""
	A title match must contain ALL of the specified interests
	"""
	allInterestIds: [ID]
	"""
	A title match must contain at least one of the specified interests
	"""
	anyInterestIds: [ID]
	"""
	Titles that include any of the specified interests will never be matched
	"""
	excludeInterestIds: [ID]
}

"""
Item in a page of interest search results.
"""
type InterestSearchEdge {
	cursor: ID!
	node: Interest!
}

type InterestText {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

"""
Classification type for interests
"""
enum InterestType {
	FORM
	FRANCHISE
	GENRE
	LANGUAGE
	STYLE
	SUBGENRE
	TECHNIQUE
}

"""
Visibility status of an interest

PUBLIC means callers may expose the interest to the end user
HIDDEN means callers may get the data but may not expose the interest to the end user
"""
enum InterestVisibilityLevel {
	HIDDEN
	PUBLIC
}

type Interstitial {
	"""
	Buttons to be rendered in the list order
	Client should always render them at the bottom of the page/view
	"""
	buttons: [InterstitialButtonComponent]!
	"""
	UI components to be rendered in the list order
	Currently, InterstitialButtonComponent are only returned in buttons field
	"""
	components: [InterstitialComponent]!
}

enum InterstitialButtonAction {
	"""
	Dismiss the interstitial and continue browsing.
	Client should close the interstitial without any authentication changes.
	"""
	CONTINUE_BROWSING
	"""
	Pro signup button on the second Pro PAP → AAP conversion interstitial
	(SHOW_PRO_PAP_TO_AAP_CONVERSION second variant). Client performs the cookie swap and
	navigates to /signup/pro/start?pap_cnvrt=<value> to begin the Pro signup flow.
	pap_cnvrt is a base64-encoded string in the same format as the siteState value on an
	AuthPortal sign-in URL.
	"""
	CONTINUE_TO_PRO_SIGNUP
	"""
	"Continue with IMDb" button on the SHOW_AMAZON_ACCOUNT_IN_USE_INTERSTITIAL error interstitial.
	Client calls recordAmazonAccountPoolConversionDecline(reason: AMAZON_ACCOUNT_IN_USE) →
	120-day TTL before PAP sign-in completes. The Amazon account entered during conversion is
	already linked to a different IMDb account.
	"""
	CONTINUE_WITH_IMDB
	"""
	Convert an IMDb account that has existing LWA linkage to the authenticated Amazon account.
	Client should call conversion mutation with just the Amazon token.
	"""
	CONVERT_LWA_TO_AAP
	"""
	Convert the authenticated IMDb account to the authenticated Amazon account.
	Client should call conversion mutation with both tokens.
	"""
	CONVERT_TO_AAP
	"""
	Create new IMDb account using authenticated Amazon account.
	Client should proceed with new account creation.
	"""
	CREATE_NEW_ACCOUNT
	"""
	Decline the PAP-to-AAP conversion offer.
	Client should record the decline (60-day TTL) and complete sign-in normally.
	"""
	DECLINE_CONVERSION
	"""
	Home page banner "Get started" button.
	Navigates to /registration/conversion/?conversionMode=INITIATE, bypassing the TTL check.
	"""
	INITIATE_CONVERSION
	"""
	Start IMDb account linking process.
	Client should initiate the IMDb authentication flow for linking.
	"""
	LINK_IMDB_ACCOUNT
	"""
	Start IMDb account linking process.
	Client should initiate the IMDb authentication flow for linking with a prefilled email.
	"""
	LINK_IMDB_ACCOUNT_EMAIL_PREFILLED
	"""
	Return user to sign-in options.
	Client should navigate to the main sign-in options page/screen.
	"""
	SHOW_SIGN_IN_OPTIONS
	"""
	Redirect user to Amazon (AAP) sign-in flow.
	Client should navigate to the Sign in with Amazon page/screen.
	Used for standard Amazon account authentication.
	"""
	SIGN_IN_WITH_AMAZON
	"""
	Redirect user to Amazon linking sign-in flow.
	Client should navigate to the platform-appropriate imdb_amazon_linking association handle.
	"""
	SIGN_IN_WITH_AMAZON_LINKING
	"""
	3P deprecation interstitial primary button. Web and iOS only.
	Apple sign-in is not supported on Android — this value is never returned for Android handles.
	Returned on SHOW_3P_DEPRECATION_INTERSTITIAL for imdb_apple (web) and imdb_mobile_ios_apple (iOS).
	Client routes to AuthPortal / MAP SDK with the platform-appropriate Apple handle.
	"""
	SIGN_IN_WITH_APPLE
	"""
	"Sign in with Apple to verify" button on the conversionMode: INITIATE interstitial variant.
	Shown when the PAP user has Apple auth linked — web and iOS only (Apple unavailable on Android).
	Determined via userLinkedAuthProvidersDataLoader at render time.
	Client routes to: Web: imdb_apple_linking · iOS: imdb_mobile_ios_apple_linking.
	RAR returns immediateAction: SIGN_IN_WITH_AMAZON_LINKING → zero-UI redirect.
	"""
	SIGN_IN_WITH_APPLE_LINKING
	"""
	3P deprecation interstitial primary button. Web, iOS, and Android.
	Returned on SHOW_3P_DEPRECATION_INTERSTITIAL for imdb_google (web),
	imdb_mobile_ios_google (iOS), and imdb_mobile_android_google (Android).
	Client routes to AuthPortal / MAP SDK with the platform-appropriate Google handle.
	"""
	SIGN_IN_WITH_GOOGLE
	"""
	"Sign in with Google to verify" button on the conversionMode: INITIATE interstitial variant.
	Shown when the PAP user has Google auth linked — all platforms.
	Determined via userLinkedAuthProvidersDataLoader at render time.
	Client routes to: Web: imdb_google_linking · iOS: imdb_mobile_ios_google_linking · Android: imdb_mobile_android_google_linking.
	RAR returns immediateAction: SIGN_IN_WITH_AMAZON_LINKING → zero-UI redirect.
	"""
	SIGN_IN_WITH_GOOGLE_LINKING
	"""
	Redirect user to IMDb sign-in flow.
	Client should navigate to the Sign in with IMDb page/screen.
	"""
	SIGN_IN_WITH_IMDB
	"""
	Button on the email match conversion interstitial.
	Redirects to the platform-appropriate email match linking association handle
	(imdb_email_match_linking, imdb_mobile_ios_email_match_linking, or
	imdb_mobile_android_email_match_linking). The client navigates to AuthPortal with
	the Amazon token in the primary cookie — the server resolves the PAP account from
	the linking cookie and initiates the email match AAP conversion flow.
	"""
	SIGN_IN_WITH_IMDB_EMAIL_MATCH_LINKING
	"""
	Redirect user to IMDb sign-in flow with a prefilled email.
	Client should navigate to the Sign in with IMDb page/screen.
	"""
	SIGN_IN_WITH_IMDB_EMAIL_PREFILLED
	"""
	"Sign in to verify" button on the conversionMode: INITIATE interstitial variant.
	Redirects to the imdb_us_linking association handle. RAR returns SIGN_IN_WITH_AMAZON_LINKING
	(zero-UI). Always shown as the default verify option for all users — no credential check required.
	Shown alongside any 3P verify buttons determined by userLinkedAuthProvidersDataLoader.
	"""
	SIGN_IN_WITH_IMDB_LINKING
	"""
	Redirect user to LWA sign-in flow.
	Client should navigate to the Sign in with Amazon page/screen.
	Used for third-party applications (e.g., IMDb) requiring Amazon authentication.
	"""
	SIGN_IN_WITH_LWA
	"""
	"Sign in with Amazon" button on the first Pro PAP → AAP conversion interstitial
	(SHOW_PRO_PAP_TO_AAP_CONVERSION). Returned when the user last signed in via an LWA handle.
	Client routes to the imdb_pro_amazon_convert_linking association handle — the Amazon sign-in
	step where both the "Sign in with IMDb" and "Sign in with Amazon" Pro conversion paths converge.
	The "Sign in with IMDb" button on the same interstitial reuses the existing
	SIGN_IN_WITH_IMDB_LINKING action.
	"""
	SIGN_IN_WITH_PRO_AMAZON_CONVERT_LINKING
	"""
	Sign out the current user.
	Client should clear authentication state and return to unauthenticated state.
	"""
	SIGN_OUT
}

"""
Component representing an interactive button
"""
type InterstitialButtonComponent {
	"""
	Action identifier for client handling when URL is not provided
	"""
	action: InterstitialButtonAction!
	"""
	Button text
	"""
	label: LocalizedString!
	"""
	Visual style of the button
	"""
	style: InterstitialButtonStyle!
}

enum InterstitialButtonStyle {
	"""
	Primary button - filled, emphasized style for main actions
	"""
	PRIMARY
	"""
	Secondary button - outlined style for alternative actions
	"""
	SECONDARY
	"""
	Text button - transparent style for alternative actions
	"""
	TEXT
}

"""
Union of possible interstitial components
"""
union InterstitialComponent =
	| InterstitialButtonComponent
	| InterstitialConsumerAccountInfoComponent
	| InterstitialDialogComponent
	| InterstitialProAccountInfoComponent

"""
Component displaying account information being converted in Consumer flow
"""
type InterstitialConsumerAccountInfoComponent {
	"""
	User's first name, or nickname if no first name was provided
	"""
	displayName: String!
	"""
	Email address associated with the IMDb account
	Null if account conversion has partial failure (email was removed but conversion is not finished)
	Client should render view without email when null
	"""
	email: String
	"""
	Public profile information
	Client should render user's profile image if available
	"""
	profile: UserProfile!
}

"""
Component displaying dialog content
"""
type InterstitialDialogComponent {
	"""
	Visual style of the dialog
	"""
	style: InterstitialDialogStyle
	"""
	Text content
	"""
	text: LocalizedMarkdown!
}

enum InterstitialDialogStyle {
	"""
	Contained text - display text within a box
	Used for highlighting important messages (e.g., highlighting account conversion text)
	"""
	BOX
	"""
	Primary text - used for key information
	"""
	PRIMARY
	"""
	Secondary text - used for additional information (e.g., company trivia)
	"""
	SECONDARY
	"""
	Title text - used for dialog titles
	"""
	TITLE
}

"""
Component displaying account information being converted in Pro flow
"""
type InterstitialProAccountInfoComponent {
	"""
	User's full name, or nickname if no full name was provided
	"""
	displayName: String!
	"""
	IMDbPro membership tier
	Null if user does not have active IMDbPro subscription
	Client should render view without membership tier when null
	"""
	imdbProMembershipTier: LocalizedString
}

input InTheatersSearchConstraint {
	"""
	A title must have a showtime in a theater that has all the specified attributes.
	When using this field, it is REQUIRED to use the Location field, or GRAPHQL_VALIDATION_FAILED will be thrown.
	"""
	allTheaterAttributes: [SearchTheaterAttribute]
	"""
	A title match must be showing in at least one of the specified theaters (e.g., `ci1234567`).
	Throws GRAPHQL_VALIDATION_FAILED if more than 400 Cinema IDs are provided.
	Throws GRAPHQL_VALIDATION_FAILED if both anyCinemaIds and location are provided.
	"""
	anyCinemaIds: [ID]
	"""
	A title must have a showtime in a theater within the specified date/time range.
	`start` and `end` must be provided in ISO-8601 format (e.g. `2025-06-19T17:35:25Z`).
	If `dateTimeRange.end` is not provided, the `end` DateTime will default to `3000-01-01T00:00:00Z`.
	When using this field, it is REQUIRED to use either the location field or the
	anyCinemaIds field, otherwise GRAPHQL_VALIDATION_FAILED will be thrown.
	"""
	dateTimeRange: ShowtimesTitlesDateTimeRangeFilter
	"""
	A title match must have a showtime in a theater within the specified ShowtimesLocation.
	Throws GRAPHQL_VALIDATION_FAILED if both anyCinemaIds and location are provided.
	"""
	location: ShowtimesLocation
	"""
	A title match must have a showtime in one of the theaters in the requesting user's favorite theaters list.
	CustomerId is required and is determined by x-imdb-customer-id header (gateway determines from request).
	Throws UNAUTHENTICATED if no CustomerId is provided.
	"""
	myFavoriteTheaters: MyFavoriteTheaterSearchFilter
}

input IntRangeInput {
	max: Int
	min: Int
}

"""
Represents an integer threshold constraint with an associated error feedback that can be evaluated client-side.
Clients should always treat the threshold as inclusive (ie minimum 5 would mean 5 is valid).
Used to express a numeric limit such as maximum words or minimum characters etc.
"""
type IntThresholdValidationConstraint {
	"""
	The feedback to display to the customer related to the constraint.
	"""
	feedback: ValidationFeedbackContent!
	"""
	The threshold value for validation (inclusive)
	"""
	value: Int!
}

input InvalidAuthProviderInterstitialInput {
	"""
	Allows input of Google or Apple to activate the missing account page
	"""
	type: AuthProviderType!
}

type InvalidAuthProviderInterstitialOutput {
	interstitial: Interstitial!
}

type IsElementInList {
	"""
	Returns true or false with respect to the presence of const in the list
	"""
	isElementInList: Boolean!
	"""
	Returns const id
	"""
	itemElementId: ID!
	"""
	The list items that contain the const id
	"""
	itemIds: [ID]!
}

type JobCreditAttribute implements CreditAttribute {
	id: ID!
	job: String!
	language: DisplayableLanguage!
	text: String!
}

"""
Filter options for company key staff
"""
input KeyStaffFilter {
	"""
	Filter by occupation IDs. Returns staff who have ANY of the specified occupations (OR logic)
	"""
	occupations: [ID]
}

"""
Input for sorting key staff
"""
input KeyStaffSort {
	"""
	Field to sort by
	"""
	by: KeyStaffSortBy!
	"""
	Sort order
	"""
	order: SortOrder!
}

"""
Available fields for sorting key staff
"""
enum KeyStaffSortBy {
	"""
	Sort by branch name alphabetically
	"""
	BRANCH
	"""
	Sort by job title/occupation alphabetically
	"""
	JOB_TITLE
	"""
	Sort by person's name alphabetically
	"""
	NAME
	"""
	Sort by StarMeter ranking (lower rank number = better rank)
	"""
	STARMETER
}

type Keyword {
	"""
	A category describing the nature of the keyword, e.g. 'plot related' or 'potentially offensive'

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	category: KeywordCategory
	id: ID!
	"""
	The display data for a keyword

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	text: KeywordText
	"""
	The associated titles for a keyword

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	titles(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): KeywordTitleConnection
}

"""
Keyword Category details
"""
type KeywordCategory {
	"""
	Category ID
	"""
	id: ID!
}

input KeywordItemCategorySort {
	"""
	Field to sort by.
	"""
	by: KeywordItemCategorySortBy!
	"""
	Sort direction. When omitted, defaults to the natural order for the chosen
	sort (DESC for RELEVANCE, ASC for ALPHABETICAL).
	"""
	order: SortOrder
}

enum KeywordItemCategorySortBy {
	"""
	Sort alphabetically — locale-aware.
	ASC: A-Z
	DESC: Z-A
	"""
	ALPHABETICAL
	"""
	Sort by relevance.
	ASC: Least relevant first
	DESC: Most relevant first
	"""
	RELEVANCE
}

type KeywordMetadata {
	"""
	All the valid keyword categories
	"""
	keywordCategories(limit: Int): [KeywordCategory]!
}

"""
Example: if you want to match titles of sub-genre 'martial-arts' OR 'cyberpunk', AND
the title must contain 'cat' AND the title must NOT contain 'dog', specify anyKeywords: ['martial-arts', 'cyberpunk'],
allKeywords: ['cat'], and excludeKeywords: ['dog']
"""
input KeywordSearchConstraint {
	"""
	A title match must contain ALL of these keywords
	"""
	allKeywords: [String]
	"""
	A title match must contain AT LEAST ONE of these keywords
	"""
	anyKeywords: [String]
	"""
	A title match must contain NONE of these keywords

	If the same input is specified in the any/all and exclude constraints,
	then the results should be empty.
	"""
	excludeKeywords: [String]
}

"""
The keyword itself. Can contain lower case characters and punctuation, but no uppercase characters or spaces.
"""
type KeywordText {
	"""
	Keyword ID
	"""
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	"""
	Displayed keyword text
	"""
	text: String!
}

"""
Pagination for Title types which always has a known total number of edges.
"""
type KeywordTitleConnection {
	"""
	Pages of titles
	"""
	edges: [KeywordTitleEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of titles
"""
type KeywordTitleEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A Title node
	"""
	node: Title!
	"""
	Position of the node in this context, eg: Title 4 out of 312
	"""
	position: Int!
}

"""
The known for source preference for a particular known for category for a company
"""
enum KnownForPreference {
	"""
	Known for calculated using an algorithm
	"""
	AUTOMATIC
	"""
	Custom overridden known for
	"""
	CUSTOM
}

type KnownForV2 {
	credits: [CreditV2]!
	"""
	Restriction related information if exists for the connection
	"""
	restriction: NameKnownForRestriction
}

"""
Laboratories for this title.
"""
type Laboratories {
	"""
	The list of laboratory items
	"""
	items: [Laboratory]!
	restriction: TechnicalSpecificationsRestriction
	"""
	Total number of laboratory items
	"""
	total: Int!
}

"""
A lab used for a film, along with some flags and any attributes. For example, we could have a lab named ILM, which was
credited as Industrial Light & Magic, and used for developing specific sequences.
"""
type Laboratory {
	attributes: [DisplayableAttribute]!
	"""
	The name of the laboratory as credited
	"""
	creditedAs: String
	"""
	Laboratory as displayable property
	"""
	displayableProperty: DisplayableTechnicalSpecificationProperty!
	"""
	Indicates if the laboratory is uncredited
	"""
	isUncredited: Boolean!
	"""
	The laboratory
	"""
	laboratory: String!
}

"""
Input for filtering the prototype list.
"""
input LabsAllPrototypesInput {
	"""
	When true, returns a list suitable for caching.
	"""
	cached: Boolean
	"""
	Maximum number of prototypes to return. Returns all if omitted.
	"""
	limit: Int
	"""
	Surface to list prototypes for (e.g. www, pro). Lists all if omitted.
	"""
	surface: String
}

"""
Aggregate feedback statistics across all customers.
"""
type LabsAllUsersFeedback {
	"""
	Number of thumbs-down ratings
	"""
	thumbsDownCount: Int!
	"""
	Number of thumbs-up ratings
	"""
	thumbsUpCount: Int!
	"""
	Total number of try events across all customers
	"""
	totalTries: Int!
	"""
	Number of distinct customers who have tried the prototype
	"""
	uniqueCustomers: Int!
}

"""
An answer value returned to the user.
"""
type LabsAnswerValue {
	"""
	A securely-stored free-text comment
	"""
	secureComment: String
}

"""
A category for filtering prototypes in the gallery.
"""
type LabsCategory {
	"""
	Accent color identifier for the UI
	"""
	accentId: ID!
	"""
	Number of featured prototypes to surface in this category
	"""
	featuredCount: Int!
	"""
	Icon identifier for the UI
	"""
	iconId: ID
	"""
	Unique identifier (e.g. 'games-trivia')
	"""
	id: ID!
	"""
	Human-readable display label
	"""
	label: LocalizedString!
	"""
	Short reference marker for this category
	"""
	refMarker: String!
	"""
	Tags that match prototypes to this category
	"""
	tags: [String]!
}

"""
A resolved custom property with optional error.
"""
type LabsCustomPropResult {
	error: String
	key: String!
	value: String
}

"""
A feedback question configured for a prototype.
"""
type LabsFeedbackQuestion {
	"""
	Optional longer description or context
	"""
	detail: LocalizedString
	"""
	Unique question identifier
	"""
	id: ID!
	"""
	Display order (lower numbers first)
	"""
	order: Int!
	"""
	Placeholder text for the input field
	"""
	placeholder: LocalizedString
	"""
	Short question title
	"""
	title: LocalizedString!
	"""
	The kind of answer expected
	"""
	type: LabsQuestionType!
}

"""
Represents a Labs prototype entity.
"""
type LabsPrototype {
	"""
	Whether the prototype can be tried (has a published bundle)

	---------------------
	Graphlet Details:
	- Name: labs
	- CTI: IMDb/GraphQL/Labs
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-labs
	"""
	canTryIt: Boolean!
	"""
	Resolve custom properties. Omit keys to resolve all.

	---------------------
	Graphlet Details:
	- Name: labs
	- CTI: IMDb/GraphQL/Labs
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-labs
	"""
	customProps(keys: [String]): [LabsCustomPropResult]!
	"""
	Description of the prototype

	---------------------
	Graphlet Details:
	- Name: labs
	- CTI: IMDb/GraphQL/Labs
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-labs
	"""
	description: LocalizedMarkdown!
	"""
	Feedback data for this prototype

	---------------------
	Graphlet Details:
	- Name: labs
	- CTI: IMDb/GraphQL/Labs
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-labs
	"""
	feedback: LabsPrototypeFeedback!
	"""
	Fetch data files by name. Returns matching files with CloudFront URLs.

	---------------------
	Graphlet Details:
	- Name: labs
	- CTI: IMDb/GraphQL/Labs
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-labs
	"""
	files(names: [String]!): [LabsPrototypeFile]!
	"""
	Behavioral flags (e.g. DESKTOP_ONLY, FULL_SCREEN)

	---------------------
	Graphlet Details:
	- Name: labs
	- CTI: IMDb/GraphQL/Labs
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-labs
	"""
	flags: [String]!
	"""
	Unique identifier (directory name)
	"""
	id: ID!
	"""
	Usage instructions or guidance

	---------------------
	Graphlet Details:
	- Name: labs
	- CTI: IMDb/GraphQL/Labs
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-labs
	"""
	instructions: LocalizedMarkdown
	"""
	Human-readable name

	---------------------
	Graphlet Details:
	- Name: labs
	- CTI: IMDb/GraphQL/Labs
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-labs
	"""
	label: LocalizedString!
	"""
	URL path or route

	---------------------
	Graphlet Details:
	- Name: labs
	- CTI: IMDb/GraphQL/Labs
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-labs
	"""
	path: String!
	"""
	Short description for cards and previews (auto-generated from first sentence)

	---------------------
	Graphlet Details:
	- Name: labs
	- CTI: IMDb/GraphQL/Labs
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-labs
	"""
	shortDescription: LocalizedString!
	"""
	Categorization tags for display and filtering

	---------------------
	Graphlet Details:
	- Name: labs
	- CTI: IMDb/GraphQL/Labs
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-labs
	"""
	tags: [LocalizedString]!
}

"""
Feedback data for a prototype.
"""
type LabsPrototypeFeedback {
	"""
	Aggregate feedback across all customers
	"""
	allUsers: LabsAllUsersFeedback!
	"""
	Active feedback questions, sorted by order
	"""
	questions: [LabsFeedbackQuestion]!
	"""
	Current customer's feedback (null if no customerId in context)
	"""
	user: LabsUserFeedback
}

"""
A data file attached to a prototype.
"""
type LabsPrototypeFile {
	"""
	MIME content type
	"""
	contentType: String!
	"""
	User-supplied file name
	"""
	name: String!
	"""
	File size in bytes
	"""
	size: Int!
	"""
	Public URL to the file
	"""
	url: String!
}

"""
Query namespace for Labs prototypes.
"""
type LabsQuery {
	"""
	All prototypes
	"""
	allPrototypes(input: LabsAllPrototypesInput): [LabsPrototype]!
	"""
	Category definitions for gallery filtering
	"""
	categories: [LabsCategory]!
	"""
	Whether Labs is currently offline
	"""
	isOffline: Boolean!
	"""
	Fetch a single prototype by ID
	"""
	prototype(id: ID!): LabsPrototype
	"""
	Fetch a single prototype by its URL path
	"""
	prototypeByPath(path: String!, surface: String): LabsPrototype
	"""
	Fetch prototypes by IDs
	"""
	prototypes(ids: [ID]!): [LabsPrototype]!
}

"""
Type of feedback question.
"""
enum LabsQuestionType {
	"""
	A free-text comment stored securely
	"""
	SECURE_COMMENT
}

"""
Thumbs rating direction.
"""
enum LabsThumbsRating {
	"""
	Negative rating
	"""
	DOWN
	"""
	Positive rating
	"""
	UP
}

"""
A user's answer to a feedback question.
"""
type LabsUserAnswer {
	"""
	The answer value
	"""
	answer: LabsAnswerValue!
	"""
	When the answer was recorded
	"""
	answeredAt: String!
	"""
	The question ID this answers
	"""
	questionId: ID!
	"""
	The try session ID associated with this answer, if any
	"""
	tryId: ID
}

"""
The current customer's feedback state for a prototype.
"""
type LabsUserFeedback {
	"""
	User's answers to currently active questions only
	"""
	answers: [LabsUserAnswer]!
	"""
	ISO timestamp of the customer's first try
	"""
	firstTriedAt: String
	"""
	ISO timestamp of the customer's most recent try
	"""
	lastTriedAt: String
	"""
	The customer's current thumbs rating, if any
	"""
	thumbs: LabsThumbsRating
	"""
	Whether the customer has tried the prototype at least once
	"""
	triedIt: Boolean!
	"""
	Total number of times the customer has tried the prototype
	"""
	triedItCount: Int!
}

"""
A BCP-47 String with the primary language in ISO 639-1 followed by
a dash and ISO 3166-1 or UN M.49 code (e.g en-US es-419 fr-FR)

https://en.wikipedia.org/wiki/ISO_639-1
https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2
https://en.wikipedia.org/wiki/UN_M.49
"""
scalar Language

"""
Language contribution guidelines:
https://help.imdb.com/article/contribution/titles/languages/GSCRBZYEH5NX5SXL
"""
input LanguageSearchConstraint {
	"""
	Match titles that are spoken in ALL of these languages.
	Language in ISO 639 codes (IMDbLocaleConfig:language_codes.json)
	Example: To match titles that are spoken in Serbo-Croation AND Kriolu: ['qbo', 'qaq']
	"""
	allLanguages: [String]
	"""
	Match titles that are spoken in AT LEAST ONE of these languages at all.
	Language in ISO 639 codes (IMDbLocaleConfig:language_codes.json)
	Example: To match titles that are spoken in Serbo-Croation or Kriolu: ['qbo', 'qaq']
	"""
	anyLanguages: [String]
	"""
	Match titles that are primarily spoken in ONE of these languages.
	Language in ISO 639 codes (IMDbLocaleConfig:language_codes.json)
	Example: To match titles that are primarily spoken in Australian Sign Language or Brazilian Sign Language: ['asf', 'bfi']
	"""
	anyPrimaryLanguages: [String]
	"""
	Exclude titles that are spoken in AT LEAST ONE of these languages.
	Language in ISO 639 codes (IMDbLocaleConfig:language_codes.json)
	Example: To match titles that are not spoken in Serbo-Croation or Kriolu: ['qbo', 'qaq']

	If the same input is specified in the any/all and exclude constraints,
	then the results should be empty.
	"""
	excludeLanguages: [String]
	"""
	Exclude titles that are primarily spoken in ONE of these languages.
	Language in ISO 639 codes (IMDbLocaleConfig:language_codes.json)
	Example: To match titles that are not primarily spoken in Australian Sign
	Language or Brazilian Sign Language: ['asf', 'bfi']

	If the same input is specified in the any/all and exclude constraints,
	then the results should be empty.
	"""
	excludePrimaryLanguages: [String]
	"""
	Match titles that are silent, or not silent.
	"""
	isSilent: Boolean
}

input LatestTrailerFilter {
	"""
	The country field is an ISO 3166-1 alpha-2 country code (ex. US or GB).
	Determine video eligibility based on the country.
	If it is not set, it will default to US.
	"""
	country: String
	"""
	Match trailers based on their maturity classification.
	If it is not set, it will default to exclude all the mature videos.
	"""
	maturityLevel: MaturityLevel
	"""
	Prefer trailers whose audioLanguages include the preferredAudioLanguage.
	If it is not set, it will default to en-US.
	"""
	preferredAudioLanguage: Language
}

"""
Represents a location in the latitude/longitude format.
Values should not be more precise than one hundredth of a degree. More precise inputs will be
treated as invalid inputs.
"""
input LatLong {
	lat: LocationDecimal!
	long: LocationDecimal!
}

type LengthMeasurement {
	"""
	Unit this measurement value is recorded in
	"""
	unit: LengthUnit
	"""
	Numeric value for this measurement
	"""
	value: Float
}

enum LengthUnit {
	CENTIMETER
	METER
}

"""
Unit of measurement for linear dimensions (length, height, and other dimensional values)
"""
enum LinearUnit {
	"""
	Measurement in centimeters.
	"""
	CENTIMETERS
	"""
	Measurement in inches.
	"""
	INCHES
}

type LinkCallToAction {
	"""
	Contains the action link data
	"""
	action: ActionLink!
	"""
	An identifier for the returned result of a CTA.
	A CTA will have multiple potential results, with each result having its own identifier.
	This value is only unique within the CTA. It is not globally unique.
	The resultId can be used as resultOverride input argument to specify a specific result
	should be returned for the requested CTA.
	"""
	resultId: ResultID!
}

type LinkedAuthProvider {
	"""
	Returns a help message with an obfuscated email in regards to resetting a user's password for a certain deprecated linked auth provider.
	A message is only returned if the auth provider link exists and the user has last logged in with that auth provider.
	An empty response means no message needs to be shown.
	"""
	deprecationMessage: AuthProviderDeprecationMessage
	"""
	Returns the type of 3rd party linked auth provider.
	"""
	type: AuthProviderType!
}

"""
List Type
Entity for federation. Other implementing services can extend using field 'id'.
Non key fields are all marked as optional since this is a federation entity.
"""
type List {
	"""
	Are elements with id (e.g. tt1000, tt2000,  etc.,) present in this list
	Multiple ids can be passed at a time. There's a limit of 10000 ids
	If one of the id can’t be checked then it throws an exception

	Results are eventually consistent: after a recent add or remove on the list, entries in
	the response may briefly reflect the pre-write value until the change propagates
	(typically within a fraction of a second under normal conditions).

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	areElementsInList(itemElementIds: [ID]!): [IsElementInList]
	"""
	Author of list.

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	author: UserProfile
	"""
	Created time

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	createdDate: DateTime
	"""
	List description

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	description: ListDescription
	"""
	Unique id of list.
	"""
	id: ID!
	"""
	Is element with id (e.g. tt00000001, nm00000002) part of the list
	Only supported for authenticated users at this time.

	Results are eventually consistent: after a recent add or remove on the list, this field
	may briefly return the pre-write value until the change propagates (typically within a
	fraction of a second under normal conditions).

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	isElementInList(itemElementId: ID!): Boolean
	"""
	Gets whether the list is a predefined list.
	Predefined User Lists have a ListClass of CHECK_INS, FAVORITE_ACTORS,
	FAVORITE_THEATRES, NOT_INTERESTED, RESEARCH_NOTES, SEEN, or WATCH_LIST.
	They are automatically created for a user, and cannot be deleted or renamed.

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	isPredefined: Boolean
	"""
	Flag indicating whether this list should be indexed by search engines.
	Returns true if the list is trusted and can be indexed, false otherwise.

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	isTrusted: Boolean!
	"""
	Returns list items by increasing position order.

	jumpTo: option for token-based pagination following IMDbCommonGraphletSchema standards. Jumps to a specific list item
	ID and will return the "first" items starting with the provided item ID. If the provided item ID is not found, this
	option will be ignored and will have the returned ListConnection's jumpToFound be false.
	Will throw a BAD_USER_INPUT if used with after, last, before, or jumpToPosition.

	jumpToPosition: option for index-based pagination. Jumps to a particular numerical, positive contextual position in
	the list (the position after taking ListItemFilter and ListItemSort into account), and will return the "first" items
	starting at the provided position value. Item positions are one-based.
	If the provided jumpToPosition is 1 and the list is empty, this option will be ignored and will have the returned
	ListConnection's jumpToFound be false while returning the empty list of items.
	Will throw a BAD_USER_INPUT if used with after, last, before, or jumpTo, or if there are no items at the provided
	position.

	jumpToPosition should only be used when displaying only a single page at a time, and the entire list is not being
	loaded into memory. We recommend clients to use the standard token-based pagination mechanism whenever possible.
	This option is available to enable the following list features -
	1. The ability to have bookmarks / links to specific points in a list for editing
	2. The ability to grab list chunks in parallel

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	items(
		after: ID
		before: ID
		filter: ListItemFilter
		first: Int
		jumpTo: ID
		jumpToPosition: Int
		last: Int
		sort: ListItemSort = { by: LIST_ORDER, order: ASC }
	): ListConnection
	"""
	Last modified time

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	lastModifiedDate: DateTime
	"""
	List Class

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	listClass: ListClass
	"""
	Get list interaction counts (views, shares) for a list, for each time range (ex: one week ago, all time).

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	listInteractionCounts(
		timeRanges: [ListInteractionCountTimeRange]!
	): [ListInteractionCounts]
	"""
	List Type

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	listType: ListType
	"""
	List Name

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	name: ListName
	"""
	InvalidParameterErrors would be thrown for non-name lists.

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	nameListItemSearch(
		after: String
		filter: AdvancedNameSearchConstraints
		first: Int!
		"""
		Option for index-based pagination. Jumps to a particular numerical, positive contextual position in
		the list (the position after taking filter and sort into account), and will return the "first" items
		starting at the provided position value.

		Will throw a BAD_USER_INPUT if used with "after", if less than or equal to zero, or greater than or equal to 10,000.

		If the specified position is larger than the result set, will return no results (not null).
		"""
		jumpToPosition: Int
		sort: NameListSearchSort
	): NameListItemSearchConnection
	"""
	Primary Image image for List

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	primaryImage: ListPrimaryImage
	"""
	InvalidParameterErrors would be thrown for non-title lists.

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	titleListItemSearch(
		after: String
		filter: AdvancedTitleSearchConstraints
		first: Int!
		"""
		Option for index-based pagination. Jumps to a particular numerical, positive contextual position in
		the list (the position after taking filter and sort into account), and will return the "first" items
		starting at the provided position value.

		Will throw a BAD_USER_INPUT if used with "after", if less than or equal to zero, or greater than or equal to 10,000.

		If the specified position is larger than the result set, will return no results (not null).
		"""
		jumpToPosition: Int
		sort: TitleListSearchSort
	): TitleListItemSearchConnection
	"""
	List Visibility

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	visibility: ListVisibility
}

type ListClass {
	id: ListClassId!
	name: ListClassName
}

"""
List classes.
Predefined Lists have a ListClassId of CHECK_INS, FAVORITE_ACTORS,
FAVORITE_THEATRES, FOLLOW_PEOPLE, FOLLOW_TITLES, NOT_INTERESTED, RESEARCH_NOTES,
SEEN, or WATCH_LIST.
"""
enum ListClassId {
	CHECK_INS
	FAVORITE_ACTORS
	FAVORITE_THEATRES
	FOLLOW_PEOPLE
	FOLLOW_TITLES
	INTERNAL
	LIST
	PRO_LIST
	RESEARCH_NOTES
	WATCH_LIST
}

"""
The type of list
Ex: Watchlist, check ins list
"""
type ListClassName {
	id: ID!
	"""
	The language of the predefined list class name
	"""
	language: DisplayableLanguage!
	"""
	The displayed predefined list class name
	"""
	text: String!
}

type ListCollectionConnection {
	edges: [ListCollectionEdge]!
	pageInfo: PageInfo!
	total: Int!
}

type ListCollectionEdge {
	cursor: ID!
	node: List!
	position: Int!
}

"""
A Connection which always has a known total number of edges.
"""
type ListConnection {
	edges: [ListItemEdge]!
	"""
	Denotes whether the data in connection has been updated after the passed pagination token
	"""
	isPaginationDirty: Boolean
	"""
	Optional field returned if jumpTo param passed
	"""
	jumpToFound: Boolean
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

type ListDescription {
	"""
	Markdown text for list item description.
	This field is editable by the list owner and is displayed under the item's image for each item on the list page.
	"""
	originalText: Markdown!
}

type ListExportDetail implements ExportDetail {
	"""
	The date when the export becomes unavailable.
	After this date, the export will no longer be returned by the backend.
	"""
	expiresOn: DateTime
	exportType: ExportType!
	"""
	The metadata of the list being exported at the time of export request
	"""
	listExportMetadata: ListExportMetadata
	resultUrl: URL
	startedOn: DateTime!
	status: ExportStatus!
	"""
	number of objects in the export
	"""
	totalExportedObjects: Int
}

type ListExportMetadata {
	"""
	Nickname of the user who created the list being exported
	"""
	authorNickname: String!
	"""
	The list ID
	"""
	id: ID!
	"""
	Class of the list
	"""
	listClassId: ListClassId!
	"""
	Type of the list
	"""
	listType: ListTypeId!
	"""
	Name of the list at the time of export request
	"""
	name: String!
	"""
	Primary image of the list at the time of export request
	"""
	primaryImage: Image
}

"""
Attribute Metadata for List Fields
"""
type ListFieldAttributeMetadata {
	listDescription: ListTextFieldMetadata!
	listItemDescription: ListTextFieldMetadata!
	listName: ListTextFieldMetadata!
}

input ListFilter {
	"""
	Collection of listClass filters, e.g. WATCH_LIST
	"""
	classTypes: [ListClassId]
	"""
	ListType filter, e.g. TITLES
	"""
	listElementType: ListTypeId
}

type ListInteractionCounts {
	"""
	Number of clicks this list has, or 0 if none.
	"""
	clicks: Int!
	listId: ID!
	"""
	List interactions for the specified time range.
	"""
	timeRange: ListInteractionCountTimeRange!
	"""
	Number of views this list has, or 0 if none.
	"""
	views: Int!
}

"""
Enum used for list interaction count ranges
"""
enum ListInteractionCountTimeRange {
	ALL_TIME
	ONE_WEEK
}

"""
List Item Union type
"""
union ListItem = Cinema | Image | Name | Title | Video

"""
List Item Edge type
"""
type ListItemEdge {
	"""
	Cursor for this item.
	"""
	cursor: ID!
	"""
	Item node.
	"""
	node: ListItemNode!
	"""
	Item's contextual position in the list (i.e. after applying filtering and/or sorting).
	"""
	position: Int!
}

"""
Filter options (deprecated - use Advanced Title Search instead)
"""
input ListItemFilter {
	"""
	Filter Title items. Requires authentication.
	INCLUDE restricts only to titles rated by authenticated user.
	EXCLUDE filters out titles rated by authenticated user.
	(deprecated - use Advanced Title Search instead)
	"""
	rated: FilterInclusion
	"""
	Filter Title items.
	INCLUDE restricts only to titles that have been released by current date.
	EXCLUDE filters out titles released.
	(deprecated - use Advanced Title Search instead)
	"""
	released: FilterInclusion
}

"""
Strongly typed list item metadata
Handles metadata specific to individual list items
"""
type ListItemMetadata {
	"""
	Freeform text tags associated with this list item
	User-defined strings for categorization and filtering (e.g., "action", "favorite", "must-watch")
	Returns empty array if no tags exist.
	"""
	tags: [String]!
}

type ListItemNode {
	"""
	Item's one-based position in the entire list before applying any sorting or filtering, i.e. its position in ascending LIST_ORDER ordering.
	Will be present except for the ModifiedListOutput of removeElementFromList, removeElementFromPredefinedList, addItemToList, and addItemToPredefinedList.
	"""
	absolutePosition: Int
	"""
	Created time
	Will be present except for the ModifiedListOutput of removeElementFromList, removeElementFromPredefinedList, addItemToList, and addItemToPredefinedList
	"""
	createdDate: DateTime
	"""
	Description of the list
	"""
	description: ListDescription
	"""
	Will be present except for the ModifiedListOutput of removeElementFromList and removeElementFromPredefinedList
	"""
	itemId: ID
	"""
	Any metadata for this list item
	Includes free form tags currently.
	Returns null if no metadata exists for this item.
	"""
	itemMetadata: ListItemMetadata
	"""
	Last modified time
	Will be present except for the ModifiedListOutput of removeElementFromList, removeElementFromPredefinedList, addItemToList, and addItemToPredefinedList
	"""
	lastModifiedDate: DateTime
	"""
	The list item (title, name, video, image, cinema).
	Videos can be nullable when returning geo-blocked videos (visible to users in one location but not another) or transient videos associated with marketing campaigns.
	Cinemas can be nullable when the cinema has become inaccessible.
	"""
	listItem: ListItem
}

type ListItemSearchNode {
	"""
	Item's one-based position in the entire list before applying any sorting or
	filtering, i.e. its position in ascending LIST_ORDER ordering.
	If this result set contains items from multiple lists, this will be each
	item's position within its own list, so there could
	be duplicate values in this field.
	"""
	absolutePosition: Int!
	createdDate: DateTime
	description: ListDescription
	itemId: ID!
	lastModifiedDate: DateTime
}

"""
Sort input for list items
"""
input ListItemSort {
	by: ListItemSortBy = LIST_ORDER
	order: SortOrder
}

"""
Enum for list item sort options
"""
enum ListItemSortBy {
	CREATED_DATE
	LIST_ORDER
	MODIFIED_DATE
}

"""
String Text
"""
type ListName {
	"""
	Original text
	"""
	originalText: String!
}

type ListPrimaryImage {
	image: Image!
}

"""
List filtering restriction. Matched entities must be in at least one of the
`inAnyList` lists and not in any of the `notInAnyList` lists.
Matched entities must also be in all of `inAllLists` lists. If multiple filters are provided entities returned
must satisfy all of the filters.

Note the total count of the lsconsts, included in `inAnyList`, `notInAnyList`,
and `inAllLists`, must not exceed 10,000 (limit of OpenSearch).
If the total count exceeds 10,000 then `GRAPHQL_VALIDATION_FAILED` will be thrown.

Note that IMDb supports lists of multiple list types: Titles, Names, etc -- if
a list specified here is of a type incompatible with the expected context
then `GRAPHQL_VALIDATION_FAILED` will be thrown.

If the requesting user does not have permission to access any
list identified here then `FORBIDDEN` will be thrown.
"""
input ListSearchConstraint {
	"""
	Match entities that are in all of these lists, identified by ListIds.
	"""
	inAllLists: [ID]
	"""
	Match entities that are in all of these predefined lists.
	Currently only allows for filtering by the predefined lists of the requesting user.
	"""
	inAllPredefinedLists: [PredefinedListInput]
	"""
	Match entities that are in any of these lists, identified by ListIds.
	"""
	inAnyList: [ID]
	"""
	Match entities that are in any of these predefined lists.
	Currently only allows for filtering by the predefined lists of the requesting user.
	"""
	inAnyPredefinedList: [PredefinedListInput]
	"""
	Exclude entities that are in any of these lists, identified by ListIds.
	"""
	notInAnyList: [ID]
	"""
	Exclude entities that are in any of these predefined lists.
	Currently only allows for filtering by the predefined lists of the requesting user.
	"""
	notInAnyPredefinedList: [PredefinedListInput]
}

input ListSearchFilter {
	"""
	Collection of listClass filters, e.g. WATCH_LIST
	"""
	anyClassTypes: [ListClassId]
	"""
	Collection of ListType filter, e.g. TITLES.
	"""
	anyListTypes: [ListTypeId]
	"""
	Privacy status of List.
	"""
	anyVisibilities: [ListVisibilityId]
}

input ListSearchSort {
	by: ListSearchSortBy!
	order: SortSearchOrder!
}

enum ListSearchSortBy {
	"""
	Date when the list was created
	ASC: Earlier date will be first.
	"""
	DATE_CREATED
	"""
	Date when the list was last modified
	ASC: Earlier date will be first.
	"""
	DATE_MODIFIED
	"""
	List name, sort alphabetically
	ASC: Aa-Zz
	"""
	NAME
}

"""
Sort input for lists
"""
input ListSort {
	by: ListSortBy = MODIFIED_DATE
	order: SortOrder
}

"""
Enum for list sort options
"""
enum ListSortBy {
	MODIFIED_DATE
	NAME
}

"""
Attribute Metadata for a List Text Field
"""
type ListTextFieldMetadata {
	maxCharacters: Int!
}

input ListTextSearchConstraint {
	"""
	Optional date range filter for when lists were last modified
	"""
	modifiedDateRange: DateRange
	"""
	Matches list based on their name, description or the description of any contained list item.
	"""
	searchTerm: String
}

type ListType {
	id: ListTypeId!
}

enum ListTypeId {
	GALLERIES
	IMAGES
	LISTS
	PEOPLE
	THEATRES
	TITLES
	VIDEOS
}

type ListVisibility {
	id: ListVisibilityId
	name: ListVisibilityName
}

enum ListVisibilityId {
	PRIVATE
	PUBLIC
}

type ListVisibilityName {
	id: ID!
	"""
	The language of the list visibility name
	"""
	language: DisplayableLanguage!
	"""
	The displayed list visibility name
	"""
	text: String!
}

type LocalizedDisplayableCount {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

"""
Generic type for a localized country, which has an ID or token, associated
display text, and the language of the display text
"""
type LocalizedDisplayableCountry {
	"""
	The country code - either an ISO 3166 code or an internally defined code if no ISO code exists for that country.
	"""
	id: ID!
	"""
	The language of the display country
	"""
	language: DisplayableLanguage!
	"""
	Display text for the country (e.g. 'United States').
	"""
	text: String!
}

type LocalizedDisplayableEpisodeNumber {
	"""
	Episode number as a standardized displayableProperty
	"""
	displayableProperty: EpisodeNumberDisplayableProperty!
	"""
	Non localized episode number, can be used for sorting or filtering
	Returns 'unknown' if episode number is unknown
	"""
	episodeNumber: String!
	"""
	An opaque unique identifier for the displayable episode.
	"""
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	"""
	The display text
	"""
	text: String!
}

type LocalizedDisplayableEpisodeYear {
	"""
	Year as a standardized displayableProperty
	"""
	displayableProperty: YearDisplayableProperty!
	"""
	An opaque unique identifier for the displayable year.
	"""
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	"""
	The display text
	"""
	text: String!
	"""
	Non localized year, can be used for sorting or filtering
	Returns 'unknown' if release year is unknown
	"""
	year: String!
}

type LocalizedDisplayableSeason {
	"""
	Season number/text as a standardized displayableProperty
	"""
	displayableProperty: SeasonValueDisplayableProperty!
	"""
	An opaque unique identifier for the displayable season.
	"""
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	"""
	Non localized Season, can be used for sorting or filtering
	Returns 'unknown' if season is unknown
	"""
	season: String!
	"""
	The display text
	"""
	text: String!
}

"""
A web link with a localized label.
"""
interface LocalizedLink {
	"""
	The localized label for the link
	"""
	label: LocalizedString!
	"""
	The URL target for the link
	"""
	url: URL!
}

"""
An object composed of a displayable localized markdown along with the language it is in.

The language is determined by localization headers sent by the client, if the chosen language is available.
Otherwise an `en-US` string will likely be returned by default.

More information on the x-imdb-user-country and x-imdb-user-language headers at
https://w.amazon.com/bin/view/IMDb/Zuko/Headers/
"""
type LocalizedMarkdown {
	"""
	The language of the returned markdown
	"""
	language: Language!
	"""
	The displayable localized markdown
	"""
	value: Markdown!
}

"""
An object composed of a displayable localized string along with the language it is in.

The language is determined by localization headers sent by the client, if the chosen language is available.
Otherwise an `en-US` string will likely be returned by default.

More information on the x-imdb-user-country and x-imdb-user-language headers at
https://w.amazon.com/bin/view/IMDb/Zuko/Headers/
"""
type LocalizedString {
	"""
	The language of the returned string
	"""
	language: Language!
	"""
	The displayable localized string
	"""
	value: String!
}

type Location {
	"""
	The location/address string
	"""
	id: ID!
	"""
	The language of the localized text
	"""
	language: DisplayableLanguage!
	"""
	The localized text
	"""
	text: String!
}

"""
A decimal used for a latitude or longitude degree value. If more granular location data is given than one hundredth of a degree, the request will be rejected.
"""
scalar LocationDecimal

input LocationsConstraint {
	"""
	A name match must be associated with ALL of these locations.
	Locations should be provided as city/region names (e.g., "Los Angeles", "New York", "London").
	No filtering is applied if omitted.
	"""
	allLocations: [String]
	"""
	A name match must be associated with AT LEAST ONE of these locations.
	Locations should be provided as city/region names (e.g., "Los Angeles", "New York", "London").
	No filtering is applied if omitted.
	"""
	anyLocations: [String]
	"""
	A name match must not be associated with any of these locations.
	Locations should be provided as city/region names (e.g., "Los Angeles", "New York", "London").
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeLocations: [String]
}

input LogoutRedirectURLInput {
	"""
	Association handle of the login context i.e. imdb_us
	"""
	associationHandle: String!
	"""
	Optional URL to return to post logout, defaults to home page
	"""
	returnURL: URL
}

type LogoutRedirectURLOutput {
	"""
	Redirect URL for user logout flow
	"""
	redirectURL: URL!
}

"""
Type for a display-ready machine translated article representation of the parent type
"""
type MachineTranslatedDisplayableArticle {
	"""
	An optional machine translated body for the article
	"""
	body: MachineTranslatedMarkdown
	"""
	An optional machine translated footer for the article
	"""
	footer: MachineTranslatedMarkdown
	"""
	An optional machine translated header for the article
	"""
	header: MachineTranslatedMarkdown
}

"""
An object that holds details about Markdown translation.

This type supports data in graphs that can be machine translated.
"""
type MachineTranslatedMarkdown {
	"""
	Indicates if translation is automatic
	"""
	isMachineTranslation: Boolean!
	"""
	The language of the returned markdown
	"""
	language: Language!
	"""
	The displayable localized markdown
	"""
	value: Markdown!
}

"""
An object that holds details about String translation.

This type supports data in graphs that can be machine translated.
"""
type MachineTranslatedString {
	"""
	Indicates if translation is automatic
	"""
	isMachineTranslation: Boolean!
	"""
	The language of the returned string
	"""
	language: Language!
	"""
	The displayable localized string
	"""
	value: String!
}

enum MainSearchCompanyType {
	ART_DEPARTMENT
	CAMERA_EQUIPMENT
	CASTING
	CASTING_FACILITIES
	COSTUME_WARDROBE
	DISTRIBUTOR
	FILM_COMMISSION
	FILM_FESTIVAL
	FILM_VIDEO_AUDIO_STOCK
	FINANCING
	GRIP_EQUIPMENT
	GUILD_UNION_ASSOCIATION
	INTERNATIONAL_SALES_AGENT
	LAW_FIRM
	LIGHTING_EQUIPMENT
	LOCATION_FACILITIES
	MANAGEMENT
	MARKETING_PROMOTIONS
	OFFICE_SERVICES
	POST_PRODUCTION_FACILITIES
	PRODUCTION
	PRODUCTION_EQUIPMENT
	PUBLICIST
	SECURITY_SERVICES
	SOUND_EQUIPMENT
	SOUND_STAGES
	SPECIAL_EFFECTS
	TALENT_AGENCY
	TRANSPORTATION_SERVICES
	VISUAL_EFFECTS
}

type MainSearchConnection {
	edges: [MainSearchEdge]!
	pageInfo: PageInfo!
}

type MainSearchEdge {
	cursor: ID!
	node: MainSearchNode!
	position: Int!
}

union MainSearchEntity =
	| Company
	| Interest
	| Keyword
	| Name
	| NameProfession
	| ProfessionCategory
	| Title

type MainSearchNode {
	entity: MainSearchEntity!
	id: ID!
}

input MainSearchOptions {
	"""
	Additional filters to apply to searches including companies.
	"""
	companySearchOptions: CompanySearchOptions
	"""
	Include titles and names in results that have been flagged as explicit.
	By default explicit titles and names are omitted from results.
	"""
	includeAdult: Boolean
	"""
	Only return results that exactly match the search term.
	By default an exact match is not required.
	"""
	isExactMatch: Boolean
	"""
	Additional filters to apply to searches including professions.
	"""
	professionSearchOptions: ProfessionSearchOptions
	"""
	Term used to match against the names and akas of titles, names, keywords, and companies.
	"""
	searchTerm: String!
	"""
	Additional filters to apply to searches including titles.
	"""
	titleSearchOptions: TitleSearchOptions
	"""
	The types of IMDb entities to include in the search.
	By default search results will only contain titles and names.
	"""
	type: [MainSearchType]
}

enum MainSearchTitleType {
	MOVIE
	MUSIC_VIDEO
	PODCAST_EPISODE
	PODCAST_SERIES
	TV
	TV_EPISODE
	VIDEO_GAME
}

enum MainSearchType {
	COMPANY
	INTEREST
	KEYWORD
	NAME
	PROFESSION
	PROFESSION_CATEGORY
	TITLE
}

"""
Data that company managers control, defined here since it is shared with multple files
"""
type ManagedCompanyData {
	"""
	Company known for conrolled by managers of this company
	"""
	knownFor: ManagedCompanyKnownForGroup
}

"""
Company key staff that includes meta data needed to support managers. Intended for consumption by managers.
"""
type ManagedCompanyKeyStaffCategory {
	"""
	Key staff of this company
	"""
	staff(after: ID, first: Int): CompanyKeyStaffConnection!
	"""
	The status of the key staff for this company, e.g. active vs blocked. Defaults to active.
	"""
	status: CompanyKnownForStatus!
}

"""
Company key staff that company managers control
"""
type ManagedCompanyKeyStaffGroup {
	"""
	Algorithmically calculated key staff for this company for consumption by company managers
	"""
	automatic: ManagedCompanyKeyStaffCategory!
	"""
	History of changes to algorithmically calculated key staff for this company for consumption by IMDb admins
	"""
	automaticHistory: ManagedCompanyKeyStaffHistory!
	"""
	Custom key staff controlled by managers of this company for consumption by those managers
	"""
	custom: ManagedCompanyKeyStaffCategory!
	"""
	History of changes to custom key staff for this company for consumption by IMDb admins
	"""
	customHistory: ManagedCompanyKeyStaffHistory!
	"""
	Source preference for key staf for this company, e.g. custom vs automatic. Defaults to automatic.
	"""
	sourcePreference: KnownForPreference!
}

"""
Company key staff history
"""
type ManagedCompanyKeyStaffHistory {
	"""
	History of changes to key staff for this company
	"""
	keyStaffHistory(
		after: ID
		first: Int
	): ManagedCompanyKeyStaffHistoryConnection!
}

"""
Pagination for managed company key staff version which always has a known total number of edges
"""
type ManagedCompanyKeyStaffHistoryConnection {
	"""
	Pages of company key staff versions
	"""
	edges: [CompanyKeyStaffVersionEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Company known for client that includes meta data needed to support managers. Intended for consumption by managers.
"""
type ManagedCompanyKnownForClientCategory {
	"""
	Clients this company is known for
	"""
	clients(after: ID, first: Int): CompanyKnownForClientConnection!
	"""
	The status of the known for clients for this company, e.g. active vs blocked. Defaults to active.
	"""
	status: CompanyKnownForStatus!
}

"""
Company known for client that company managers control
"""
type ManagedCompanyKnownForClientGroup {
	"""
	Algorithmically calculated known for client for this company for consumption by company managers
	"""
	automatic: ManagedCompanyKnownForClientCategory!
	"""
	History of changes to algorithmically calculated known for client for this company for consumption by IMDb admins
	"""
	automaticHistory: ManagedCompanyKnownForClientHistory!
	"""
	Custom known for client controlled by managers of this company for consumption by those managers
	"""
	custom: ManagedCompanyKnownForClientCategory!
	"""
	History of changes to custom known for client for this company for consumption by IMDb admins
	"""
	customHistory: ManagedCompanyKnownForClientHistory!
	"""
	Source preference for known for client for this company, e.g. custom vs automatic. Defaults to automatic.
	"""
	sourcePreference: KnownForPreference!
}

"""
Company known for client history
"""
type ManagedCompanyKnownForClientHistory {
	"""
	History of changes to known for client for this company
	"""
	clientHistory(
		after: ID
		first: Int
	): ManagedCompanyKnownForClientHistoryConnection!
}

"""
Pagination for managed company known for client version which always has a known total number of edges
"""
type ManagedCompanyKnownForClientHistoryConnection {
	"""
	Pages of company known for client versions
	"""
	edges: [CompanyKnownForClientVersionEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
A version snapshot of company known for client for consumption by IMDb admins
"""
type ManagedCompanyKnownForClientVersion {
	"""
	The list of known for clients for this version snapshot
	"""
	clients(after: ID, first: Int): CompanyKnownForClientConnection!
	"""
	The time that the known for client version for this company was created
	"""
	createdDate: Date!
	"""
	The user and role that modified the known for client version for this company if available
	"""
	modifiedBy: ModifiedBy
	"""
	The status of known for client version for this company, e.g. active vs blocked. Defaults to active.
	"""
	status: CompanyKnownForStatus!
	"""
	The version number for the known for client version for this company
	"""
	versionNumber: Int!
}

"""
Company known for that company managers control
"""
type ManagedCompanyKnownForGroup {
	"""
	Group of key staff for this company
	"""
	keyStaff: ManagedCompanyKeyStaffGroup!
	"""
	Group of known for client for this company
	"""
	knownForClient: ManagedCompanyKnownForClientGroup!
	"""
	Group of known for title for this company
	"""
	knownForTitle: ManagedCompanyKnownForTitleGroup!
}

"""
A version snapshot of company key staff for consumption by IMDb admins
"""
type ManagedCompanyKnownForKeyStaffVersion {
	"""
	The time that the key staff version for this company was created
	"""
	createdDate: Date!
	"""
	The user and role that modified the key staff version for this company if available
	"""
	modifiedBy: ModifiedBy
	"""
	The list of key staff for this version snapshot
	"""
	staff(after: ID, first: Int): CompanyKeyStaffConnection!
	"""
	The status of key staff version for this company, e.g. active vs blocked. Defaults to active.
	"""
	status: CompanyKnownForStatus!
	"""
	The version number for the key staff version entry for this company
	"""
	versionNumber: Int!
}

"""
Company known for title that includes meta data needed to support managers. Intended for consumption by managers.
"""
type ManagedCompanyKnownForTitleCategory {
	"""
	The status of the known for titles for this company, e.g. active vs blocked. Defaults to active.
	"""
	status: CompanyKnownForStatus!
	"""
	Titles this company is known for
	"""
	titles(after: ID, first: Int): CompanyKnownForTitleConnection!
}

"""
Company known for title that company managers control
"""
type ManagedCompanyKnownForTitleGroup {
	"""
	Algorithmically calculated known for title for this company for consumption by company managers
	"""
	automatic: ManagedCompanyKnownForTitleCategory!
	"""
	History of changes to algorithmically calculated known for title for this company for consumption by IMDb admins
	"""
	automaticHistory: ManagedCompanyKnownForTitleHistory!
	"""
	Custom known for title controlled by managers for this company for consumption by those managers
	"""
	custom: ManagedCompanyKnownForTitleCategory!
	"""
	History of changes to custom known for title for this company for consumption by IMDb admins
	"""
	customHistory: ManagedCompanyKnownForTitleHistory!
	"""
	Source preference for known for title for this company, e.g. custom vs automatic. Defaults to automatic.
	"""
	sourcePreference: KnownForPreference!
}

"""
Company known for title history
"""
type ManagedCompanyKnownForTitleHistory {
	"""
	History of changes to known for title for this company
	"""
	titleHistory(
		after: ID
		first: Int
	): ManagedCompanyKnownForTitleHistoryConnection!
}

"""
Pagination for managed company known for title version which always has a known total number of edges
"""
type ManagedCompanyKnownForTitleHistoryConnection {
	"""
	Pages of company known for title versions
	"""
	edges: [CompanyKnownForTitleVersionEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
A version snapshot of company known for title for consumption by IMDb admins
"""
type ManagedCompanyKnownForTitleVersion {
	"""
	The time that the known for title version for this company was created
	"""
	createdDate: Date!
	"""
	The user and role that modified the known title for version for this company if available
	"""
	modifiedBy: ModifiedBy
	"""
	The status of known for title version for this company, e.g. active vs blocked. Defaults to active.
	"""
	status: CompanyKnownForStatus!
	"""
	The list of known for titles for this version snapshot
	"""
	titles(after: ID, first: Int): CompanyKnownForTitleConnection!
	"""
	The version number for the known for title version for this company
	"""
	versionNumber: Int!
}

input MarkAllAsWatchedInput {
	"""
	The type of list to mark as watched. WATCHLIST uses the customer's watchlist. LIST requires a listId.
	"""
	listClass: WatchedListClass!
	"""
	List ID (e.g. ls1234567890). Required when listClass is LIST — throws
	VALIDATION_ERROR if omitted. Must not be provided when listClass is WATCHLIST
	— throws VALIDATION_ERROR if provided.
	"""
	listId: ID
}

enum MarkAllAsWatchedJobStatus {
	"""
	The job failed after automatic retries were exhausted. The client may safely
	resubmit via submitMarkAllAsWatched — duplicate requests are deduplicated.
	"""
	FAILED
	"""
	The job has been accepted and is either queued or actively processing titles.
	"""
	IN_PROGRESS
	"""
	The job completed successfully and all titles have been marked as watched.
	"""
	SUCCESS
}

type MarkAllAsWatchedJobStatusResponse {
	"""
	Current status of the job.
	"""
	status: MarkAllAsWatchedJobStatus!
}

"""
Markdown type
Extends external type.
"""
type Markdown {
	"""
	Returns strings with markdown with [link=titleNumber]...[/link] embedded with rendered markdown so that links could be
	be displayed on mobile clients.

	e.g. '[link=tt0112022]' returns '[link=tt0112022]JAG (1995)[/link]'

	---------------------
	Graphlet Details:
	- Name: markdown
	- CTI: IMDb/GraphQL/Markdown
	- Affected by headers: x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	expandedMarkdown(
		"""
		Render titles within markdown using their original title text rather than localized based on localization headers
		"""
		showOriginalTitleText: Boolean
	): String!
	"""
	IMDb markdown format
	"""
	markdown: String!
	"""
	HTML optimized for PLAID. Renders consts as links with the const display text. Titles are localized according to localization headers.

	---------------------
	Graphlet Details:
	- Name: markdown
	- CTI: IMDb/GraphQL/Markdown
	- Affected by headers: x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	plaidHtml(
		"""
		Include optional pre-rendered query params without '?', e.g. 'ref=tt_ov_dr', to be appended to all links
		"""
		queryParams: String
		"""
		Render a single line break with a <br> tag rather than a whitespace.  Note that double line breaks \n\n are always
		rendered with <br/><br/> regardless of this option.  Default to false.

		For example, let the input text be "This is\nan example.\n\nStart a new paragraph."
		If showLineBreak is true, the returned text is "This is<br/>an example.<br/><br/>Start a new paragraph."
		If showLineBreak is false, the returned text is "This is an example.<br/><br/>Start a new paragraph."
		"""
		showLineBreak: Boolean
		"""
		Render titles within markdown using their original title text rather than localized based on localization headers
		"""
		showOriginalTitleText: Boolean
	): String
	"""
	The text with most formatting removed and const links replaced with const text. Titles are localized according to localization headers.

	---------------------
	Graphlet Details:
	- Name: markdown
	- CTI: IMDb/GraphQL/Markdown
	- Affected by headers: x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	plainText(
		"""
		Render titles within markdown using their original title text rather than localized based on localization headers
		"""
		showOriginalTitleText: Boolean
	): String
	"""
	List related entities in the markdown string.

	Note: this is limited to the first 100 entities in the markdown string.

	Ex:
	renderedMarkdown(markdownString: "Remember [link=nm0001094] in [link=tt0062512]?") {
	    relatedEntities {
	        ... on Name { nameText { text } }
	        ... on Title { titleText { text } }
	    }
	}

	---------------------
	Graphlet Details:
	- Name: markdown
	- CTI: IMDb/GraphQL/Markdown
	- Affected by headers: x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	relatedEntities(limit: Int!): [MarkdownEntity]
}

union MarkdownEntity = Company | Name | Title

type MarkdownSlotCallToAction {
	"""
	Specifies the localized markdown content for this CTA
	"""
	markdown: LocalizedMarkdown
	"""
	An identifier for the returned result of a CTA.
	A CTA will have multiple potential results, with each result having its own identifier.
	This value is only unique within the CTA. It is not globally unique.
	The resultId can be used as resultOverride input argument to specify a specific result
	should be returned for the requested CTA.
	"""
	resultId: ResultID!
}

"""
Defines a standard shape for vending form constraints for a markdown-enabled text area input.
Currently the same as text area constraints but in future may include markdown-specific constraints such as
allowed/forbidden markdown tags.
"""
type MarkdownTextAreaFieldConstraints {
	"""
	Specifies if the customer must provide a non-empty (ie not null or empty string) value
	"""
	isRequired: BooleanValidationConstraint
	"""
	Specifies the maximum allowed number of characters for this markdown text area input
	"""
	maximumCharacterLength: IntThresholdValidationConstraint
	"""
	Specifies the minimum allowed number of characters for this markdown text area input
	"""
	minimumCharacterLength: IntThresholdValidationConstraint
}

enum MaturityLevel {
	"""
	Include content flagged as mature. Such content is excluded by default.
	"""
	INCLUDE_MATURE
}

input MediaAvailabilityConstraint {
	"""
	A name match must have ALL of these media types.
	No filtering is applied if omitted.
	"""
	allMediaTypes: [MediaType]
	"""
	A name match must have AT LEAST ONE of these media types.
	No filtering is applied if omitted.
	"""
	anyMediaTypes: [MediaType]
	"""
	A name match must not have any of these media types.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeMediaTypes: [MediaType]
}

"""
A generic implementation of the ImageObject
"""
type MediaServiceImage {
	"""
	Text alternative description of the image for screen reader accessibility.
	"""
	accessibilityText: CommonLocalizedDisplayableConcept
	"""
	Type delineation so that clients are able to determine how to handle the delivered asset.
	"""
	fileType: ImageFileType
	"""
	Original height of the image resource, in pixels.
	"""
	height: Int
	"""
	URL for the image resource.
	"""
	url: String
	"""
	Original height of the image resource, in pixels.
	"""
	width: Int
}

"""
Media content for talent profiles
"""
enum MediaType {
	"""
	Video compilation demonstrating talent's work
	"""
	DEMO_REEL
	"""
	Portfolio images showcasing recent projects
	"""
	FEATURED_IMAGE
	"""
	Main headshot or profile photo
	"""
	PRIMARY_IMAGE
}

type Metacritic {
	metascore: Metascore!
	reviews(first: Int): MetacriticReviewConnection
	url: URL
}

type MetacriticReview {
	quote: LocalizedString
	reviewer: String
	score: Int!
	site: String
	url: URL
}

type MetacriticReviewConnection {
	edges: [MetacriticReviewEdge]!
	pageInfo: PageInfo!
}

type MetacriticReviewEdge {
	cursor: ID!
	node: MetacriticReview!
	position: Int!
}

type Metascore {
	reviewCount: Int!
	score: Int!
}

"""
Event that should be noted with meter
"""
type MeterEvent {
	"""
	Title of an event that should be noted with meter
	"""
	title: Title
	"""
	Type of event that occurred on a certain date and should be noted with meter
	"""
	type: LocalizedString
}

"""
Meter Insight Dimension union type
"""
union MeterInsightDimension = Interest | NameProfession

"""
Text to display for an insight
"""
type MeterInsightText {
	id: ID!
	"""
	The language of the displayable insight text
	"""
	language: DisplayableLanguage!
	"""
	The displayable insight text
	"""
	text: String!
}

enum MeterInsightType {
	"""
	Ranking of a Title within an Interest
	"""
	INTEREST
	"""
	Ranking of a Name within a NameProfession
	"""
	PROFESSION
}

type MeterRankChange {
	"""
	Indicates whether the current rank is better (UP), worse (DOWN),
	or unchanged (FLAT) from the previous rank
	"""
	changeDirection: MeterRankChangeDirection!
	"""
	An integer indicating the difference between the current
	and previous rank
	"""
	difference: Int!
}

enum MeterRankChangeDirection {
	DOWN
	FLAT
	UP
}

type MeterRankingHistoryEntry {
	"""
	Timestamp of a meter ranking in the form of YYYY-MM-DD
	"""
	date: Date
	"""
	Events on a certain date that should be noted with meter
	"""
	events: [MeterEvent]
	"""
	IMDbPro meter ranking of item on a certain date
	"""
	rank: Int!
}

"""
Restriction information applied to Meter.
By default, restriction will be based on IMDbPro entitlements.
"""
type MeterRestriction {
	"""
	Explanation(s) for the restriction
	"""
	explanations: [RestrictionExplanation]!
	reasons: [ContentRestrictionReason]!
	"""
	DEPRECATED: Renamed, use `reasons` instead.
	"""
	restrictionReason: [ContentRestrictionReason]!
}

"""
A container for metric dimension data
"""
type MetricDimension {
	name: String!
	value: String!
}

"""
Defines a MIME type validation constraint with an associated feedback message.
"""
type MimeTypeValidationConstraint {
	"""
	The feedback to display to the customer related to the constraint.
	"""
	feedback: ValidationFeedbackContent!
	"""
	The list of accepted MIME types (e.g., ['image/jpeg', 'image/png', 'application/pdf'])
	"""
	values: [String]!
}

type MiscellaneousCreditAttribute implements CreditAttribute {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

"""
The customer that modified the known for category and their role for consumption by IMDb admins
"""
type ModifiedBy {
	"""
	The role of the user modifying the known for of this company
	"""
	role: UserRole!
}

type Money {
	"""
	Amount of money
	"""
	amount: Float!
	"""
	ISO-4217 alphabetic currency codes.  See: http://www.currency-iso.org/
	"""
	currency: String!
}

"""
A month and day string in the ISO-8601 format, for example '--06-19'
"""
scalar MonthDay

type MoreLikeThisConnection {
	edges: [MoreLikeThisEdge]!
	pageInfo: PageInfo!
}

type MoreLikeThisEdge {
	"""
	This cursor doesn't work for pagination right now. Please use PageInfo.endCursor instead.
	"""
	cursor: ID!
	node: Title!
	position: Int!
}

type MoreLikeThisNameConnection {
	edges: [MoreLikeThisNameEdge]!
	pageInfo: PageInfo!
}

type MoreLikeThisNameEdge {
	cursor: ID!
	node: Name!
	position: Int!
}

"""
This is a multi-link CallToAction that may contain two action lists: abbreviatedActions and standardActions.
The client will decide which action list to use when displaying the actions. For example, the client may choose
to display the abbreviatedActions on devices with small screens, while using standardActions on devices with
larger screens. A responsive solution may utilize both action lists.
"""
type MultiLinkCallToAction {
	"""
	Provides an abbreviated list of action links for this CallToAction.
	This is not necessarily a subset of "standardActions", but typically will be.
	The action links in this list may be less in number and/or contain shorter label text.
	Each action link contains a name, URL, and an optional label (localized text) associated with the URL.
	The name will uniquely identify each action link.
	"""
	abbreviatedActions: [NamedActionLink]
	"""
	An identifier for the returned result of a CTA.
	A CTA will have multiple potential results, with each result having its own identifier.
	This value is only unique within the CTA. It is not globally unique.
	The resultId can be used as resultOverride input argument to specify a specific result
	should be returned for the requested CTA.
	"""
	resultId: ResultID!
	"""
	Provides the standard list of action links for this CallToAction.
	Each action link contains a name, URL, and an optional label (localized text) associated with the URL.
	The name will uniquely identify each action link.
	"""
	standardActions: [NamedActionLink]
}

"""
Defines standard form constraints for a multi-select / dropdown type of input.
"""
type MultiSelectFieldConstraints {
	"""
	Specifies if the customer must select at least one option in the multi-select dropdown
	"""
	isRequired: BooleanValidationConstraint
	"""
	Specifies the maximum number of selections allowed
	"""
	maximumSelections: IntThresholdValidationConstraint
	"""
	Specifies the minimum number of selections required
	"""
	minimumSelections: IntThresholdValidationConstraint
}

input MusicalInstrumentsConstraint {
	"""
	A name match must play ALL of these musical instruments.
	No filtering is applied if omitted.
	"""
	allMusicalInstruments: [MusicalInstrumentType]
	"""
	A name match must play AT LEAST ONE of these musical instruments.
	No filtering is applied if omitted.
	"""
	anyMusicalInstruments: [MusicalInstrumentType]
	"""
	A name match must not play any of these musical instruments.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeMusicalInstruments: [MusicalInstrumentType]
}

"""
Musical instruments for talent
"""
enum MusicalInstrumentType {
	ACCORDION
	AUTOHARP
	BANJO
	BASSOON
	BASS_GUITAR
	CELLO
	CLARINET
	DRUMS
	DULCIMER
	FIDDLE
	FLUTE
	FRENCH_HORN
	GUITAR
	HARMONICA
	HARP
	OBOE
	ORGAN
	PERCUSSION
	PIANO
	RECORDER
	SAX
	TROMBONE
	TRUMPET
	TUBA
	UKULELE
	VIOLA
	VIOLIN
}

enum MyFavoriteTheaterSearchFilter {
	ONLY_MY_FAVORITE
}

"""
Rating that the requesting user has given to the title.
Customer is determined by x-imdb-customer-id header  (gateway determines from request)
"""
input MyRatingSearchConstraint {
	"""
	Select whether constraint should restrict-to or exclude titles that the customer has rated.
	"""
	filterType: MyRatingSearchFilterType!
	"""
	Match titles based on rating given by the requesting user.
	Infers "min: 1", or "max: 10" if one of those is not provided.
	Example: To restrict to only titles the customer rated between 5-10, use {min: 5}
	Example: To restrict to only titles the customer has rated use {min: 1, max: 10}
	"""
	ratingRange: IntRangeInput
}

enum MyRatingSearchFilterType {
	EXCLUDE
	INCLUDE
}

"""
Filter titles based on the requesting customer's watched history.
Customer is determined by x-imdb-customer-id header (gateway determines from request).
"""
input MyWatchedSearchConstraint {
	"""
	Select whether constraint should restrict-to or exclude titles that the customer has watched.
	"""
	filterType: MyWatchedSearchFilterType!
}

enum MyWatchedSearchFilterType {
	EXCLUDE
	INCLUDE
}

"""
Placeholder for Name type (IMDb name/person entity).
Should be federated from the name service.
"""
type Name {
	"""
	Person's age

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	age(
		"""
		Optional parameter which represents today's date for client in format yyyy-mm-dd.
		It is used for age calculation based on client current date.
		If not specified, and the person is living, the server date will be used instead.
		If the person is dead, the death date will be used.
		"""
		currentDate: Date
	): AgeDetails
	"""
	A list of alternative names by which the person is also known

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	akas(after: ID, before: ID, first: Int, last: Int): NameAkaConnection
	"""
	This is Pro exclusive feature

	Returns the latest uncached auto selected user professions(default limit is 3).
	Returns null if no automatic profession details are available for this name.
	Throws UNAUTHENTICATED if not logged in.
	Returns null if user is not entitled.

	@entitlement(NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	autoSelectedProfessions(
		"""
		Maximum number of professions to return. Defaults to 3 if omitted.
		"""
		limit: Int
	): [NameProfession]
	"""
	The awards that the person has won or been nominated for

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	awardNominations(
		after: ID
		before: ID
		filter: AwardNominationsFilter
		first: Int
		jumpTo: ID
		last: Int
		sort: AwardNominationsSort
	): AwardNominationConnection
	"""
	Details of the person's bio

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	bio: NameBio
	"""
	NameBio items for a name

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	bios(
		after: ID
		before: ID
		filter: NameBiosFilter
		first: Int
		jumpTo: ID
		last: Int
	): NameBiosConnection
	"""
	Details of the person's date of birth.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	birthDate: DisplayableDate
	"""
	Details of the person's location of birth.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	birthLocation: DisplayableLocation
	"""
	The person's birth name as it appears on IMDb.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	birthName: BirthName
	"""
	Box office performance data for this name.
	Returns null if no box office data exists for this name.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: mojo
	- CTI: IMDb/GraphQL/Mojo
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	boxOfficePerformance(filter: NameBoxOfficeFilter): NameBoxOfficePerformance
	"""
	A full URL to see the name on www.imdb.com

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	canonicalUrl: String
	"""
	Individual representation records where this name is the representative,
	one edge per stored representation relationship.

	This is the representative-side counterpart of `representationRecords`:
	the same underlying records viewed from the agent's perspective. On each
	record, `client` is the represented name and `representative` is this
	name (or null for records where a company represents the client without
	a specific individual assigned).

	Unlike `clients`, which returns at most one entry per represented name,
	this field returns every record. A representative who is both the
	Manager and the Publicist of the same client gets two edges, one per
	relationship. Use this field when each record needs to be individually
	addressable, such as contribution experiences that load and edit a
	single record at a time. `total` and pagination therefore count records,
	not represented names.

	When `sort` is omitted, records are returned in alphabetical order of
	the client's display name, equivalent to sorting by NAME ascending.
	Records that share a client are returned consecutively, ordered
	alphabetically by their relationship type's display text.

	Up to 250 records can be requested at a time. If `first` is greater
	than 250, only 250 records are returned.

	Returns a connection with empty `edges` and a `total` of 0 when the
	customer is not entitled, including when the customer is not logged in.

	@entitlement(PRO_PREMIUM, NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: contactsGQL
	- CTI: IMDb/GraphQL/ContactsGQL
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-name-redesign-2024
	"""
	clientRecords(
		"""
		Cursor for forward pagination, as returned in `pageInfo.endCursor` or
		on an edge. Results start after the record at this cursor. When
		omitted, results start from the beginning of the list.
		"""
		after: ID
		"""
		The maximum number of records to return (capped at 250)
		"""
		first: Int!
		"""
		How to sort the records, by the client's display name or popularity.
		Records that share a client are always returned consecutively,
		ordered alphabetically by their relationship type's display text.
		When omitted, records are sorted by NAME ascending.
		"""
		sort: ClientSort
	): NameRepresentationRecordConnection
	"""
	The names represented by this person in some capacity, like the names this
	person is the Talent Agent for or the Publicist for.

	@entitlement(PRO_PREMIUM, NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: contactsGQL
	- CTI: IMDb/GraphQL/ContactsGQL
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-name-redesign-2024
	"""
	clients(
		after: ID
		first: Int!
		sort: ClientSort
	): NameRepresentationConnection
	"""
	Connection status between the authenticated viewer and this name.
	Returns null if the viewer is not authenticated or connections data is unavailable.
	Premium: canSendRequest is true for any eligible target.
	Basic: canSendRequest is true only for targets with shared titles.
	@entitlement(PRO_ANY_TIER, PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	connectionStatus: ProNetworkingConnectionStatus
	"""
	Connections tab data when viewing another person's name page.
	Returns null if connections data is unavailable.
	@entitlement(PRO_ANY_TIER, PRO_PREMIUM)
	Premium users see full mutual connections details. Basic tier users see count only with upsell.

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	connectionsTab: ProNetworkingNameConnectionsTab
	"""
	This answers if the (name) is primarly considered as an adult actor by IMDb

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	contentWarnings: ContentWarnings
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	creditCategories(
		filter: NameCreditCategoryFilter
		sort: NameCreditSort
	): [NameCreditCategoryWithCredits]
	"""
	The endorsements of this name that tag a specific title credit, including their notes.
	Sorted by `sort` (defaults to RELEVANCE, descending). Pagination with first defaulting to 5 and capped at 50.
	Returns null if unavailable. Throws UNAUTHENTICATED if not logged in.
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	creditEndorsements(
		after: ID
		first: Int
		input: ProNetworkingCreditEndorsementsInput!
		sort: ProNetworkingEndorsementSort
	): ProNetworkingEndorsementsConnection
	"""
	Vends credits for this name on titles, grouped into groupings defined by input parameter.

	We group credits into "groupings" in a way that best highlights a person's work.
	Appearances in a title where they were not directly involved, such as Archive Footage,
	are removed from their appearance credits and moved into separate groupings.
	Soundtrack credits are included within a name's credits as a separate category. This is different to a title-focused
	view, where Soundtrack credits are modelled as a separate data type.
	All other credit categories are presented 1-1 as groupings.

	When a name has credits on individual episodes in a series, these are rolled up and presented at the series level
	as a single CreditV2 value, giving a summary of their contribution. Details of individual episodes are available
	from "episodeCredits" on a CreditedRole within a CreditV2 value.

	When a credit has performed many roles on a title within a grouping, these are aggregated into a single CreditV2
	value, with each individual role represented as a separate CreditedRole within the CreditV2.

	Credits on in-development titles are only available to clients providing a Pro tier entitlement.

	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	creditGroupings(
		"""
		Cursor from which to begin providing credits from.
		Use cursor values vended on CreditGroupingEdge values to continue pagination in subsequent calls.

		Note that if you include filtering parameters in an initial call, you must pass the same parameters along with
		your cursor, to ensure the full listing of credits is consistent.
		"""
		after: String
		"""
		By default, credits are sorted by release date (see NameCreditV2Sort for definition)
		"""
		creditSort: NameCreditV2Sort
		"""
		By default, credited roles are sorted by credited role importance (see CreditedRoleSort for definition)
		"""
		creditedRoleSort: CreditedRoleSort
		"""
		Allows for filtering in or out of credits based on several criteria.
		See NameCreditsV2Filter for details.
		"""
		filter: NameCreditsV2Filter
		"""
		Number of CreditGrouping values to fetch.
		Maximum per request is 250.
		"""
		first: Int!
		"""
		By default, groupings are sorted by grouping importance (see CreditGroupingSort for definition)
		"""
		groupingSort: CreditGroupingSort
		"""
		In general: whether to utilise any entitlements passed to alter the behaviour of this API.
		Specifically: For industry professionals (IMDbPro members) IMDb discloses attachment credits; which are essentially
		rumored credits to in-development title that have not been publicly announced.

		When false; attachments are not provided, as seen on the IMDb website.
		When true; attachments are provided if the requester is entitled to receive the credits.
		"""
		useEntitlement: Boolean = true
	): CreditGroupingConnection
	"""
	Summary for the name credits

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	creditSummary: NameCreditSummary
	"""
	CreditSummaryV2 is a set of fields that summarises a names v2 credits.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	creditSummaryV2(
		"""
		useEntitlement will default to true meaning that
		titles that are restricted will be omited unless the client
		has the PRO_ANY_TIER entitlement
		"""
		useEntitlement: Boolean = true
	): NameCreditSummaryV2
	"""
	The names that are credited on titles with the parent name derived from their filmography.
	The results are in descending order by a relevance score calculated server side.

	Returns NULL if the requesting customer is not logged in.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	creditedWithNames(
		"""
		Pagination cursor
		"""
		after: ID
		"""
		Number of required credited with names. Default value is 10 if not passed. Max value is 10
		"""
		first: Int
	): CreditedWithNamesConnection
	"""
	The entire list of credits for the name

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	credits(
		after: ID
		before: ID
		filter: NameCreditsFilter
		first: Int
		last: Int
		sort: NameCreditSort
	): NameCreditConnection
	"""
	The entire list of credits for a name.

	When a name has credits on individual episodes in a series, these are rolled up and presented at the series level
	as a single CreditV2 value, giving a summary of their contribution. Details of individual episodes are available
	from "episodeCredits" on a CreditedRole within a CreditV2 value.

	When a credit has performed many roles on a title, these are aggregated into a single CreditV2 value, with each
	individual role represented as a separate CreditedRole within the CreditV2.

	Credits on in-development titles are only available to clients providing a Pro tier entitlement.


	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	creditsV2(
		"""
		Cursor from which to begin providing credits from.
		Clients wishing to retrieve credits within a grouping can fetch an initial list of credits for this name-title pair
		via the name.creditGroupings field. This call can provide a cursor to use to fetch further pages of credits.

		Note that if you include filtering parameters in an initial call, you must pass the same parameters along with
		your cursor, to ensure the full listing of credits is consistent.
		For example, if you first call name.creditGroupings with a filter of `grouping: "grouping-id"`,
		you must include this filter in your call to name.creditsV2 for the API to know to continue applying this filter.
		"""
		after: ID
		"""
		Defines how credits are aggregated together into a single CreditV2 result.

		By default, credits are aggregated by TITLE.
		"""
		aggregateBy: NameCreditAggregation
		"""
		By default, credits are sorted by release date (see NameCreditV2Sort for definition)
		"""
		creditSort: NameCreditV2Sort
		"""
		By default, credited roles are sorted by credited role importance (see CreditedRoleSort for definition)
		"""
		creditedRoleSort: CreditedRoleSort
		filter: NameCreditsV2Filter
		"""
		Number of CreditV2 values to fetch.
		Maximum per request is 1000.
		"""
		first: Int!
		"""
		In general: whether to utilise any entitlements passed to alter the behaviour of this API.
		Specifically: For industry professionals (IMDbPro members) IMDb discloses attachment credits; which are essentially
		rumored credits to in-development title that have not been publicly announced.

		When false; attachments are not provided, as seen on the IMDb website.
		When true; attachments are provided if the requester is entitled to receive the credits.
		"""
		useEntitlement: Boolean = true
	): CreditV2Connection
	"""
	This is Pro exclusive feature

	Returns a list of uncached professions for the name owner.
	This list will return userSelectedProfessions when available or fallback on autoSelectedProfessions otherwise.
	Returns null if no userSelectedProfessions/autoSelectedProfessions details are available for this name.
	Throws UNAUTHENTICATED if not logged in.
	Returns null if user is not entitled.

	@entitlement(NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	currentProfessions(
		"""
		Maximum number of professions to return. Must be a positive integer.
		"""
		limit: Int!
	): [NameProfession]
	"""
	Details of the person's cause of death.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	deathCause: DisplayableNameDeathCause
	"""
	Details of the person's date of death.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	deathDate: DisplayableDate
	"""
	Details of the person's location of death.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	deathLocation: DisplayableLocation
	"""
	Details of the person's death

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	deathStatus: NameDeathStatus
	"""
	The demographic data items for the given name

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	demographicData(
		"""
		Filters to apply to the demographic data items
		"""
		filter: DemographicDataFilter
		"""
		Limit the number of demographic data items to include in the response
		"""
		limit: Int
	): [DemographicDataItem]
	"""
	Ways to contact this name directly

	Return null if no contact details are available for this name or
	if the entitlement criteria is not met.

	@entitlement(PRO_PREMIUM, NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: contactsGQL
	- CTI: IMDb/GraphQL/ContactsGQL
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-name-redesign-2024
	"""
	directContact: DirectContactDetails
	"""
	Roman numeral used to differentiate between 2 people of the same name

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	disambiguator: Disambiguation
	"""
	Employment information for this name.

	Return null if no employment information is available for this name or
	if the entitlement criteria is not met.

	@entitlement(PRO_PREMIUM, NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: contactsGQL
	- CTI: IMDb/GraphQL/ContactsGQL
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-name-redesign-2024
	"""
	employment(after: ID, first: Int!): EmploymentConnection
	"""
	A single published endorsement this name received, addressed by its opaque
	`id` (the `id` field of ProNetworkingEndorsement). Backs an addressable,
	deep-link render of one endorsement without paging the full list. Only an
	APPROVED endorsement ever resolves here, so a resolved endorsement's
	`approvalStatus` is always APPROVED even though that enum also models PENDING.

	The `id` addresses an (author → subject) endorsement slot rather than one
	endorsement, so a persisted link can later resolve to a different endorsement
	than the one it was created for. See ProNetworkingEndorsement.id for the reuse
	semantics and for how to notice that the endorsement behind a persisted id has changed.

	Resolves to null, rather than to an error, in each of the cases below, and
	those cases are deliberately indistinguishable from one another:
	- no endorsement has that id
	- an endorsement has that id but is still awaiting the page owner's approval,
	so it is visible only to its author and its subject
	- the endorsement was removed or suppressed
	- the endorsement belongs to a subject other than the name being queried
	- the viewer is not entitled to PRO_ANY_TIER

	Returning blank data instead of an error for an endorsement the viewer may not
	see is deliberate: collapsing those cases into one null is what stops a caller
	from confirming that endorsements it is not allowed to read exist. No business
	meaning is encoded in the null: clients must infer nothing from it beyond
	"nothing to render here".

	Throws UNAUTHENTICATED if not logged in. A store fault raises an error instead
	of silently resolving to an empty result; that error is independent of the
	requested id and carries no information about whether the endorsement exists.
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	endorsement(id: ID!): ProNetworkingEndorsement
	"""
	Aggregate summary of the endorsements this name has received.
	Returns null if unavailable. Throws UNAUTHENTICATED if not logged in.
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	endorsementSummary: ProNetworkingEndorsementSummary
	"""
	This name's published endorsements.
	The viewer's own endorsement of this name (if they have authored one) is returned first; the
	remaining endorsements are sorted by `sort` (defaults to RELEVANCE, descending). Pagination with
	first defaulting to 5 and capped at 50. Returns null if unavailable. Throws UNAUTHENTICATED if not
	logged in.
	Optionally scoped via `filter` (see ProNetworkingEndorsementsFilter); when omitted, all published
	endorsements are returned.
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	endorsements(
		after: ID
		filter: ProNetworkingEndorsementsFilter
		first: Int
		sort: ProNetworkingEndorsementSort
	): ProNetworkingEndorsementsConnection
	"""
	Return Engagement Statistics for a name

	---------------------
	Graphlet Details:
	- Name: engagementData
	- CTI: IMDb/GraphQL/Engagement Data
	- Affected by headers: x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-engagement-growth-public
	"""
	engagementStatistics: EngagementStatistics
	"""
	Credits for this name on episodes within a particular series. For access to more broad credit data, see
	name.creditsV2 and name.creditGroupings fields.

	Each episode title is represented by a single CreditV2 value. When a name has performed multiple roles on the title,
	these are represented as separate CreditedRoles within one CreditV2 value.

	Credits on in-development titles are only available to clients providing a Pro tier entitlement.

	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	episodeCredits(
		"""
		Cursor from which to begin providing credits from.
		Clients wishing to retrieve episode credits can fetch an initial list of episodes for this name-title pair,
		via the name.creditsV2 or name.creditGroupings field. These calls can provide a cursor to use to fetch episodes
		specific to a CreditV2 or CreditedRole value.

		Note that if you include filtering parameters in an initial call, you must pass the same parameters along with
		your cursor, to ensure the full listing of credits is consistent.
		For example, if you first call name.creditsV2 with a filter of `titleType: PODCAST`, you must include this filter
		in your call to name.episodeCredits for the API to know to continue applying this filter.
		"""
		after: ID
		"""
		Multi-level filtering system supporting episode, credit, and title-level filters.
		Allows precise control over which episode credits are returned.
		"""
		filter: NameEpisodeCreditsFilter
		"""
		Number of episode CreditV2 values to fetch.
		Maximum per request is 250.
		"""
		first: Int!
		"""
		Captures several functional parameter choices in a single value, enabling consistent results across many callsites.
		Default is to have no mode preset.
		"""
		modePreset: CreditsModePreset
		"""
		Series title ID to display episode credits for.
		This parameter is required and scopes the results to episodes within the specified series.
		Use name.creditsV2 or name.creditGroupings to find series IDs where this name has credits.
		"""
		seriesId: ID!
		"""
		Sorting configuration for episode credits.
		Default is ascending by release date, then season number, then episode number.
		This provides chronological ordering of episodes within the series.
		"""
		sort: EpisodeCreditsSort
		"""
		In general: whether to utilise any entitlements passed to alter the behaviour of this API.
		Specifically: For industry professionals (IMDbPro members) IMDb discloses attachment credits; which are essentially
		rumored credits to in-development title that have not been publicly announced.

		When false; attachments are not provided, as seen on the IMDb website.
		When true; attachments are provided if the requester is entitled to receive the credits.
		"""
		useEntitlement: Boolean = true
	): EpisodeCreditConnection
	"""
	Experimental Field: DO NOT USE. The list of credits by category for the name

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	experimental_creditCategories(
		filter: NameCreditCategoryFilter
		sort: NameCreditSort
	): [ExperimentalNameCreditCategoryWithCredits]
	"""
	Experimental Field: DO NOT USE. The entire list of credits for the name

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	experimental_credits(
		after: ID
		before: ID
		filter: NameCreditsFilter
		first: Int
		last: Int
		sort: NameCreditSort
	): ExperimentalNameCreditConnection
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	externalLinkCategories(
		filter: ExternalLinksFilter
	): [ExternalLinkCategoryWithExternalLinks]
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	externalLinks(
		after: ID
		before: ID
		filter: ExternalLinksFilter
		first: Int
		last: Int
	): ExternalLinkConnection
	"""
	The person's family name (surname).

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	familyName: FamilyName
	"""
	Featured polls for the name.
	Capped at <LIMIT> polls. No pagination

	---------------------
	Graphlet Details:
	- Name: polls
	- CTI: IMDb/GraphQL/Polls
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	featuredPolls(first: Int!): PollsConnection
	"""
	Details of the person's height, default unit is centimeters

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	height(unit: LengthUnit = CENTIMETER): NameHeight
	id: ID!
	"""
	Images related to the name, grouped by image types

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	imageTypes(filter: NameImagesFilter): [ImageTypeWithImages]
	"""
	Returns the 'upload url' Link.

	---------------------
	Graphlet Details:
	- Name: kahlo
	- CTI: IMDb/GraphQL/Kahlo
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-contributors-tech
	"""
	imageUploadLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
	): ContributionLink
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	images(
		after: ID
		before: ID
		"""
		Parameter to go around cache, expected to use cursor as a value
		"""
		bust: String
		filter: NameImagesFilter
		first: Int
		jumpTo: ID
		last: Int
	): ImageConnection
	"""
	True if this name page is considered claimed.

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	isClaimed: Boolean
	"""
	Whether the caller has permission to edit managed data on this name page,
	e.g. custom known for, custom featured images, and custom primary image.
	Returns true if the caller is the premium name owner or has shared editing access as a representative.
	Returns false if the caller does not have permission or is unauthenticated.

	This field is for the V2 Shared Editing permission work. V1 will be deprecated.
	@entitlement(PREMIUM_NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	isPremiumOwnerOrRepresentative: Boolean
	"""
	Titles this name is known for.
	This API is being superseded by Name.knownForV2 in 2025. Contact imdb-relevance-tech@ before using this API.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	knownFor(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): NameKnownForConnection
	"""
	Titles this name is known for.
	Returns an empty list if there are no known for titles.
	Titles are returned in order from most known for to least.

	Each credit captures all of the roles performed by the name on a given title.
	Quentin Tarantino (nm0000233) was actor, writer and director for Reservoir Dogs (tt0105236).
	The Known For entry captures each of these as a separate creditedRole within the CreditV2 field.

	Within a credit, creditedRoles are ordered by the category of the role. We prioritise categories linked
	to the person's professions, then follow a fixed ordering of categories as seen on Title Full Credits pages
	on imdb.com.

	Within a category, creditedRoles are ordered by frequency of role (across episodes in a series), with alphabetical
	used as a tie-breaker. The same is true for characters on appearance credits.
	This behaviour differs from the ordering of creditedRoles and characters for other credits APIs (see CreditV2
	documentation). The idea is to highlight the work most frequently performed by a person, therefore capturing the
	work they are likely to be most recognised for on the title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	knownForV2(
		"""
		Number of known for titles to return. Default limit is 4, matching the maximum number of Known For titles
		we generate per name. Default limit exists to avoid breaking clients if we begin generating larger volumes
		of titles.
		Maximum is 10.
		"""
		limit: Int
		"""
		Captures several functional parameter choices in a single value, enabling consistent results across many callsites.
		Default is KNOWN_FOR. See CreditsModePreset for details.
		"""
		modePreset: CreditsModePreset
	): KnownForV2
	"""
	Name data customer can customize or receive automatically generated data for.

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	managedData: NameManagedData
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	meta: NameMeta
	"""
	Returns up to 5 category-specific rankings for a name.
	Requires entitlement proving Pro Premium subscription.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: ranking
	- CTI: IMDb/GraphQL/ProRanking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	meterInsights(input: NameMeterInsightsInput): [NameMeterInsight]
	"""
	StarMeter rank information
	Ranking information beyond 5000 requires entitlement proving Pro subscription or Name ownership.
	Use useEntitlement = false to access non entitled version.
	@entitlement(PREMIUM_OR_NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: ranking
	- CTI: IMDb/GraphQL/ProRanking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	meterRank(useEntitlement: Boolean = true): NameMeterRanking
	"""
	History of IMDbPro STARmeter rankings of a name.
	Requires entitlement proving Pro subscription.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: ranking
	- CTI: IMDb/GraphQL/ProRanking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	meterRankingHistory(
		input: NameMeterRankingHistoryInput
	): NameMeterRankingHistory
	"""
	Similar names to this name. The max similar names providable by the backend is 50.

	Pagination not supported:
	Pagination may produce duplicate results as the ML model is not deterministic and ranking is affected
	by customer behavioral data. Clients are advised to retrieve names using a single request for now.

	---------------------
	Graphlet Details:
	- Name: personalization
	- CTI: IMDb/GraphQL/Personalized Picks
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-consent-info, x-imdb-detected-country, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	moreLikeThisNames(after: ID, first: Int!): MoreLikeThisNameConnection
	"""
	Number of mutual connections between the viewer and this name.
	Returns null if the viewer is not authenticated or connections data is unavailable.
	Returns 0 if the viewer has no mutual connections with this name.
	Same data for all PRO tiers (no premium-specific behavior).
	@entitlement(PRO_ANY_TIER, PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	mutualConnectionCount: Int
	"""
	The viewer's own endorsement of this name, if any. Returns null if the viewer has not endorsed this name.
	Throws UNAUTHENTICATED if not logged in.
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	myEndorsement: ProNetworkingEndorsement
	"""
	The endorsements this name has authored (published and pending).
	When an authored endorsement has a pending edit, BOTH versions are returned as separate nodes with
	the same id: the published "Current" node (APPROVED) and the "Pending edit" node (PENDING).
	Owner-only — returns null for anyone who is not the name owner.
	Optionally scoped via `filter`; when omitted, all authored endorsements are returned.
	Sorted by `sort` (defaults to DATE, descending). Pagination with first defaulting to 20 and capped at 100.
	Throws UNAUTHENTICATED if not logged in.
	@entitlement(NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	myWrittenEndorsements(
		after: ID
		filter: ProNetworkingWrittenEndorsementsFilter
		first: Int
		sort: ProNetworkingEndorsementSort
	): ProNetworkingEndorsementsConnection
	"""
	The person's name as it appears on IMDb.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	nameText: NameText
	"""
	News articles about a name

	---------------------
	Graphlet Details:
	- Name: bugle
	- CTI: IMDb/GraphQL/Bugle
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 900 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	news(after: String, first: Int!): NewsConnection
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	nickNames(limit: Int): [NickName]
	"""
	The nominations that the person has won or been nominated for
	Default sort is done by sorting nomination id in ascending order

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	nominations(
		after: ID
		before: ID
		filter: NominationsFilter
		first: Int
		jumpTo: ID
		last: Int
		sort: NominationsSort
	): NominationConnection
	"""
	Other works for a name.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	otherWorks(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): NameOtherWorkConnection
	"""
	Number of pending incoming connection requests.
	Returns null when viewing another person's page or if the viewer is not authenticated (own page only).
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	pendingConnectionRequestCount: Int
	"""
	Endorsements of this name that are awaiting the name owner's approval.
	Owner-only — returns null for anyone who is not the name owner.
	Sorted by `sort` (defaults to DATE, descending). Pagination with first defaulting to 20 and capped at 100.
	Throws UNAUTHENTICATED if not logged in.
	@entitlement(NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	pendingEndorsements(
		after: ID
		first: Int
		sort: ProNetworkingEndorsementSort
	): ProNetworkingEndorsementsConnection
	"""
	Restrictions on this person's customer-suppressed personal details, for authorized clients.
	It describes which personal attributes the person has restricted from public display so a
	client can avoid surfacing them — for example, so IMDb Pro suppression preferences are honored
	even when a client sources data from third parties.

	This field never returns a suppressed value: suppressed demographics are reported as category
	names only, and suppressed names are excluded from the returned allowlist.

	Throws FORBIDDEN if the caller is not entitled. Returns null only when the underlying
	suppression data cannot be resolved — so a null response never signals an authorization issue.

	@entitlement(AI_AGENT_PERSONAL_DATA_ACCESS)

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	personalDetailsRestrictions: NamePersonalDetailsRestrictions
	"""
	The most prestigious awards that the person has won or been nominated for

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	prestigiousAwardSummary: PrestigiousAwardSummary
	"""
	The primary image for the name.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	primaryImage: Image
	"""
	An ordered list of the primary videos related to a name.
	If the first argument exceeds 100, the results will be capped to 100.

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	primaryVideos(first: Int!): VideoConnection
	"""
	Returns a list of profession for a name (default limit is 3).
	This list will return customerProfessions when available otherwise fallback to automaticProfessions.
	Returns null if no customerProfessions/automaticProfessions details are available for this name

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	professions(limit: Int): [NameProfession]
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	publicityCategories: [PublicityCategoryWithListings]
	"""
	Publicity Listings for a name.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	publicityListings(
		after: ID
		before: ID
		filter: PublicityListingsFilter
		first: Int
		last: Int
	): PublicityListingConnection
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	quotes(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): NameQuoteConnection
	"""
	Returns page view statistics of this claimed Pro name page. The count excludes the page owner's own views.
	Returns null if fewer than 3 people, excluding the page owner, have viewed the page.

	Throws UNAUTHENTICATED if not logged in.
	@entitlement(PREMIUM_NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: personalization
	- CTI: IMDb/GraphQL/Personalized Picks
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-consent-info, x-imdb-detected-country, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	recentlyViewedStatistics: RecentlyViewedStatistics
	"""
	Get lists related to a name

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	relatedLists(after: ID, first: Int!): ListCollectionConnection
	"""
	Details of the person's relations

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	relations(
		after: ID
		before: ID
		filter: NameRelationsFilter
		first: Int
		jumpTo: ID
		last: Int
	): NameRelationsConnection
	"""
	Information about what companies and people represent this name, operating
	in capacities including legal, talent, management, and publicity

	There will be one representation instance per combination
	of relationship type and agency/independent rep. For example,
	if someone is an actor's manager and talent agent, there will
	be one representation instance for each of those relationships.

	@entitlement(PRO_PREMIUM, NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: contactsGQL
	- CTI: IMDb/GraphQL/ContactsGQL
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-name-redesign-2024
	"""
	representation(after: ID, first: Int!): NameRepresentationConnection
	"""
	Individual representation records for this name, one edge per stored
	representation relationship.

	Unlike `representation`, which groups records by relationship type and
	agency for display purposes, this field returns exactly one edge per
	underlying record. Use this field when each record needs to be
	individually addressable, such as contribution experiences that load and
	edit a single record at a time. `total` and pagination therefore count
	records, not groups.

	Records that share a relationship type are returned consecutively. Within
	a relationship type, records whose representative is the point of contact
	are returned first, followed by the remaining records in alphabetical
	order of their representative's display name (or their company's display
	name, for records without an individual representative).

	Up to 250 records can be requested at a time. If `first` is greater
	than 250, only 250 records are returned.

	Returns a connection with empty `edges` and a `total` of 0 when the
	customer is not entitled, including when the customer is not logged in.

	@entitlement(PRO_PREMIUM, NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: contactsGQL
	- CTI: IMDb/GraphQL/ContactsGQL
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-name-redesign-2024
	"""
	representationRecords(
		"""
		Cursor for forward pagination, as returned in `pageInfo.endCursor` or
		on an edge. Results start after the record at this cursor. When
		omitted, results start from the beginning of the list.
		"""
		after: ID
		"""
		The maximum number of records to return (capped at 250)
		"""
		first: Int!
	): NameRepresentationRecordConnection
	"""
	Resume data for a name page.

	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	resume: Resume
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	searchIndexing: NameSearchIndexing
	"""
	Self-verified data for this person.

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	selfVerifiedData: SelfVerifiedNameData
	"""
	The shared names that the input and the parent names have worked with derived from their filmography credits.

	Returns NULL if the requesting customer is not entitlted.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	sharedNames(
		"""
		Pagination cursor
		"""
		after: ID
		filter: SharedNamesFilter
		"""
		Number of required shared names. Default value is 20 if not passed. Max value is 20
		"""
		first: Int
		input: SharedNamesInput
	): SharedNamesResult
	"""
	Returns a summary object for the shared name connections between the currently logged in user and the name being queried.

	Also returns a formatted string for displaying the name connection summary to customers.

	Returns NULL if the requesting customer is not logged in.

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	sharedNamesSummary: SharedNamesSummary
	"""
	The shared titles between the input and the parent name derived from their filmography credits.

	Returns NULL if the requesting customer is not entitlted.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	sharedTitles(
		"""
		Pagination cursor
		"""
		after: ID
		"""
		Number of required shared titles. Default value is 10 if not passed. Max value is 10
		"""
		first: Int
		input: SharedTitlesInput
	): SharedTitlesConnection
	"""
	Details of the person's spouses

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	spouses: [NameSpouse]
	"""
	Salary earned from a title

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	titleSalaries(after: ID, before: ID, first: Int, last: Int): SalaryConnection
	"""
	The requesting user's track state with notification preferences for this const.
	Throws FORBIDDEN if customer does not hve a pro subscription
	Throws UNAUTHENTICATED if logged out
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: track
	- CTI: IMDb/GraphQL/Track
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	trackNotificationPreferences: TrackNotificationPreferences
	"""
	Details of the person's trademarks

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	trademarks(after: ID, before: ID, first: Int, last: Int): TrademarkConnection
	"""
	Trivia for a name.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	trivia(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): NameTriviaConnection
	"""
	This is Pro exclusive feature

	Returns the latest uncached user selected profession overides only
	Returns null if no user selected profession details are available for this name.
	Throws UNAUTHENTICATED if not logged in.
	Returns null if user is not entitled.

	@entitlement(PREMIUM_NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	userSelectedProfessions: [NameProfession]
	"""
	The person's vanity URL

	Returns null if user is not entitled or not authenticated.
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	vanityUrl: VanityUrl
	"""
	Videos related to the name, grouped by video type

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	videoTypes(filter: VideosQueryFilter, sort: VideoSort): [VideoTypeWithVideos]
	"""
	An ordered list of videos related to a name.
	If the first argument exceeds 100, the results will be capped to 100.
	When there is no sort passed in it, default to the current videos sort logic and behavior.

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	videos(
		after: ID
		filter: VideosQueryFilter
		first: Int!
		sort: VideoSort
	): VideoConnection
}

"""
Aka details
"""
type NameAka {
	"""
	The alternative name as a displayable property
	"""
	displayableProperty: DisplayableNameAkaProperty!
	"""
	The alternative name
	"""
	text: String!
}

"""
Pagination for NameAkas types which always has a known total number of edges
"""
type NameAkaConnection {
	"""
	Pages of NameAkas
	"""
	edges: [NameAkaEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of NameAkas
"""
type NameAkaEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	Details of the person's alternative names
	"""
	node: NameAka!
	"""
	Position of the node in this context, eg: NameAka 4 out of 31
	"""
	position: Int!
}

type NameAreaPerformance {
	"""
	The geographic area for this performance row.
	"""
	area: BoxOfficeAreaType!
	"""
	Average gross per title in this area. Null if no titles with gross data.
	"""
	averageGrossToDate: BoxOfficeGross
	"""
	Total cumulative domestic/international gross across all titles for this name in this area. Null if no data.
	"""
	grossToDate: BoxOfficeGross
	"""
	Number of titles with reported gross in this area. Null if no data.
	"""
	numTitlesWithGross: Int
	"""
	Number of titles with reported opening weekend data in this area. Null if no data.
	"""
	numTitlesWithOpeningWeekend: Int
	"""
	Average opening weekend gross per title in this area. Null if no titles with opening weekend data.
	"""
	openingWeekendAverageGross: BoxOfficeGross
}

type NameBio {
	"""
	The bio author. Optional as it will be null for auto generated bios
	"""
	author: Markdown
	"""
	The category of bio
	"""
	category: NameBioCategory
	"""
	The bio as displayable article
	"""
	displayableArticle: DisplayableArticle
	"""
	Unique ID for this bio item
	"""
	id: ID!
	"""
	The language of the bio text
	"""
	language: DisplayableLanguage!
	"""
	Machine translated displayable article. Returns the displayable article translated based on the language header.
	"""
	machineTranslatedDisplayableArticle: MachineTranslatedDisplayableArticle
	"""
	Machine translated bio text. Returns the text field translated based on the
	language header. If the text is already localized in the requested language,
	returns it as-is with isMachineTranslation set to false.
	"""
	machineTranslatedText: MachineTranslatedMarkdown!
	"""
	The bio text
	"""
	text: Markdown!
}

"""
A category of person's bio describing the type of bio, e.g. 'auto-generated', 'mini-bio'
"""
type NameBioCategory {
	id: ID!
	text: String!
}

"""
Page of name bios
"""
type NameBioEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A NameBio node
	"""
	node: NameBio!
	"""
	Position of the node in this context, eg: NameBio 4 out of 312
	"""
	position: Int!
}

"""
Pagination for NameBio types which always has a known total number of edges.
"""
type NameBiosConnection {
	"""
	Pages of name bios
	"""
	edges: [NameBioEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

input NameBiosFilter {
	"""
	Return person's bios of specified categories
	"""
	categories: [ID]
	"""
	Return person's bios in all available locales when true, otherwise returns all available bios in the requested language
	"""
	includeAllLocales: Boolean
}

input NameBoxOfficeFilter {
	"""
	Filter by profession category. Accepts a `ProfessionCategory.id`
	(e.g. `amzn1.imdb.professionCategory.1.2.3`). If omitted, returns data for
	the default profession category (the highest-grossing one).
	"""
	professionCategoryId: ID
}

type NameBoxOfficeFilterOptions {
	"""
	Profession categories available for filtering.
	"""
	availableProfessionCategories: [ProfessionCategory]!
	"""
	Currently selected profession category.
	"""
	selectedProfessionCategory: ProfessionCategory!
}

type NameBoxOfficePerformance {
	"""
	Filter options contextual to the current selection.
	"""
	filterOptions: NameBoxOfficeFilterOptions!
	"""
	Box office performance by geographic area. Returns all areas with data (up to
	~40 entries). Ordered by gross to date descending.
	"""
	performances(
		"""
		Maximum number of entries to return. Callers typically want all values; if omitted, returns all areas (up to ~40).
		"""
		limit: Int
	): [NameAreaPerformance]!
}

type NameChartRankingsConnection {
	edges: [NameChartRankingsEdge]!
}

type NameChartRankingsEdge {
	node: NameChartRankingsNode!
	position: Int!
}

input NameChartRankingsInput {
	"""
	It is made optional for adapting future use cases.
	Not passing a valid value will result in client error.
	"""
	rankingsChartType: NameChartRankingsType
}

type NameChartRankingsNode {
	item: Name!
	rank: Int!
}

enum NameChartRankingsType {
	"""
	STARmeter chart geared to India
	"""
	INDIA_STAR_METER
}

input NameCreditAggregation {
	"""
	Defines how credits are aggregated together into a single CreditV2 result.

	Aggregation allows clients to choose the level of detail to show for credits, avoiding repetition of the same
	title across many roles.

	Parameter is modelled as a string to allow for altering the list of allowed values over time. Supported values are:

	TITLE: All credits for a single name on a single title are aggregated together.
	This supports experiences like Name Credits tab on pro.imdb.com, where all work by a name for a title is
	shown together. Multiple roles on the same title are combined into a single CreditV2 with multiple CreditedRole entries.
	For example, see Pulp Fiction (1994) in https://pro.imdb.com/name/nm0000233/credits.

	TITLE_AND_GROUPING: Credits are broken down by title and grouping.
	This reflects the same breakdown of credits seen on experiences like Name Main on imdb.com.
	Each role grouping on a title gets its own CreditV2 entry, even if the person had multiple roles.
	For example, for Quentin Tarantino (https://www.imdb.com/name/nm0000233/), we would return three separate credits for
	Pulp Fiction (1994): Director, Writer (covering two different roles) and Actor.
	"""
	aggregationType: String
}

type NameCreditCategory {
	id: ID!
	text: String!
}

input NameCreditCategoryFilter {
	"""
	Filter for archived / unarchived credits only
	"""
	archived: ArchivedOrUnarchivedFilter
	"""
	Filter for specific credit categories.
	The list of category IDs are available to query from nameMetadata.nameCreditCategories.
	Cannot be used in conjunction with "excludeCategories", as the behaviour for unspecified values is ambiguous.
	"""
	categories: [ID]
	"""
	Filter for credited or uncredited credits only
	"""
	credited: CreditedOrUncreditedFilter
	"""
	Filter out specific credit categories.
	The list of category IDs are available to query from nameMetadata.nameCreditCategories.
	Cannot be used in conjunction with "categories", as the behaviour for unspecified values is ambiguous.
	"""
	excludeCategories: [ID]
	"""
	Filter out credits on titles with specific genres.
	The list of genres can be accessed from titleMetadata.titleGenres.
	Cannot be used in conjunction with "genres", as the behaviour for unspecified values is ambiguous.
	"""
	excludeGenres: [ID]
	"""
	Filter out specific production stages.
	Cannot be used in conjunction with "productionStage", as the behaviour for unspecified values is ambiguous.
	"""
	excludeProductionStage: [ProductionStageFilter]
	"""
	Filter out credits for titles with specific titleTypes.
	The list of titleTypes can be accessed from titleMetadata.titleTypes.
	Cannot be used in conjunction with "titleType", as the behaviour for unspecified values is ambiguous.
	"""
	excludeTitleType: [ID]
	"""
	Filter for credits on titles with specific genres.
	The list of genres can be accessed from titleMetadata.titleGenres.
	Cannot be used in conjunction with "excludeGenres", as the behaviour for unspecified values is ambiguous.
	"""
	genres: [ID]
	"""
	Filter for specific production stages.
	Cannot be used in conjunction with "excludeProductionStage", as the behaviour for unspecified values is ambiguous.
	"""
	productionStage: [ProductionStageFilter]
	"""
	Filter for whether the credits belong to the name's primary professions or not
	"""
	professions: ProfessionsFilter
	"""
	Filter for a project status of UPCOMING, PREVIOUS or PROJECT_IN_DEVELOPMENT.
	On name.creditCategories, this field supports only one value, where more than
	one value is supplied only first value is used.
	On name.creditGroupings, multiple values are supported.
	"""
	projectStatus: [ID]
	"""
	Filter credits for titles with specific titleTypes.
	The list of titleTypes can be accessed from titleMetadata.titleTypes.
	Cannot be used in conjunction with "excludeTitleType", as the behaviour for unspecified values is ambiguous.
	"""
	titleType: [ID]
	"""
	Filter credits for titles with specific titleTypeCategory.
	"""
	titleTypeCategory: [TitleTypeCategoryValue]
	"""
	Filter credits to only those for these specific titles
	"""
	titles: [ID]
}

type NameCreditCategoryWithCredits {
	category: CreditCategory!
	credits(first: Int): NameCreditConnection
}

"""
Pagination for Credit type on a Name.
"""
type NameCreditConnection {
	"""
	Pages of credits
	"""
	edges: [CreditEdge]!
	"""
	How the credits are ordered
	"""
	orderBy: CreditsOrderedBy
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Restriction related information if exists for the connection
	"""
	restriction: CreditRestriction
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
In name.credits API, filters only applied to top level credits. Episodes under series have their own filters
"""
input NameCreditsFilter {
	"""
	Filter for archived / unarchived credits only.
	By default, all credits are included, including those marked as "(archive footage)" or "(archive sound)".
	"""
	archived: ArchivedOrUnarchivedFilter
	"""
	Filter for specific credit categories.
	The list of category IDs are available to query from nameMetadata.nameCreditCategories.
	Cannot be used in conjunction with "excludeCategories", as the behaviour for unspecified values is ambiguous.
	By default, all categories are included.
	"""
	categories: [ID]
	"""
	Filter for credited or uncredited credits only.
	By default, all credits are included, including those marked as "(uncredited)".
	"""
	credited: CreditedOrUncreditedFilter
	"""
	Filter out specific credit categories.
	The list of category IDs are available to query from nameMetadata.nameCreditCategories.
	Cannot be used in conjunction with "categories", as the behaviour for unspecified values is ambiguous.
	By default, all categories are included.
	"""
	excludeCategories: [ID]
	"""
	Filter out credits on titles with specific genres.
	The list of genres can be accessed from titleMetadata.titleGenres.
	Cannot be used in conjunction with "genres", as the behaviour for unspecified values is ambiguous.
	By default, credits for all genres are included.
	"""
	excludeGenres: [ID]
	"""
	Filter out specific credit groupings. Individual grouping IDs are found in name.creditGroupings responses.
	Clients would often make a call to name.creditGroupings, retrieve an initial page of credits per grouping, then
	fetch additional pages using this API.
	Cannot be used in conjunction with "groupings", as the behaviour for unspecified values is ambiguous.
	By default, credits for all groupings are included.
	"""
	excludeGroupings: [ID]
	"""
	Filter out specific production stages.
	Cannot be used in conjunction with "productionStage", as the behaviour for unspecified values is ambiguous.
	By default, credits for all production stages are included.
	"""
	excludeProductionStage: [ProductionStageFilter]
	"""
	Filter out credits for titles with specific titleTypes.
	The list of titleTypes can be accessed from titleMetadata.titleTypes.
	Cannot be used in conjunction with "titleType", as the behaviour for unspecified values is ambiguous.
	By default, credits for all title types are included.
	"""
	excludeTitleType: [ID]
	"""
	Drop credits that have at least one of the given traits on their credit category.
	Supported values are: 'CAST_TRAIT', 'CREW_TRAIT', 'SELF_TRAIT',
	'THANKS_TRAIT', 'ADDITIONAL_APPEARANCES_TRAIT', 'MAJOR_CREATIVE_INPUT_TRAIT',
	'UNCATEGORIZED_TRAIT', 'UNDERGOING_TESTING_TRAIT'.
	Cannot be used in conjunction with "includeTraits", as the behaviour for unspecified values is ambiguous.
	By default, credits are not filtered by trait.
	See CreditCategoryTrait for trait definitions.
	"""
	excludeTraits: [String]
	"""
	Filter for credits on titles with specific genres.
	The list of genres can be accessed from titleMetadata.titleGenres.
	Cannot be used in conjunction with "excludeGenres", as the behaviour for unspecified values is ambiguous.
	By default, credits for all genres are included.
	"""
	genres: [ID]
	"""
	Filter for specific credit groupings. Individual grouping IDs are found in name.creditGroupings responses.
	Clients would often make a call to name.creditGroupings, retrieve an initial page of credits per grouping, then
	fetch additional pages using this API.
	Cannot be used in conjunction with "excludeGroupings", as the behaviour for unspecified values is ambiguous.
	By default, credits for all groupings are included.
	"""
	groupings: [ID]
	"""
	Keep credits that have at least one of the given traits on their credit category.
	Supported values are: 'CAST_TRAIT', 'CREW_TRAIT', 'SELF_TRAIT',
	'THANKS_TRAIT', 'ADDITIONAL_APPEARANCES_TRAIT', 'MAJOR_CREATIVE_INPUT_TRAIT',
	'UNCATEGORIZED_TRAIT', 'UNDERGOING_TESTING_TRAIT'.
	Cannot be used in conjunction with "excludeTraits", as the behaviour for unspecified values is ambiguous.
	By default, credits are not filtered by trait.
	See CreditCategoryTrait for trait definitions.
	"""
	includeTraits: [String]
	"""
	Filter for specific production stages.
	Cannot be used in conjunction with "excludeProductionStage", as the behaviour for unspecified values is ambiguous.
	By default, credits for all production stages are included.
	"""
	productionStage: [ProductionStageFilter]
	"""
	Filter for a project status of UPCOMING, PREVIOUS or PROJECT_IN_DEVELOPMENT.
	On name.creditCategories, this field supports only one value, where more than
	one value is supplied only first value is used.
	On name.creditGroupings, multiple values are supported.
	By default, credits for all project statuses are included. Note that PROJECT_IN_DEVELOPMENT titles are not included
	when the request has no Pro tier entitlement.
	"""
	projectStatus: [ID]
	"""
	Filter credits for titles with specific titleTypes.
	The list of titleTypes can be accessed from titleMetadata.titleTypes.
	Cannot be used in conjunction with "excludeTitleType", as the behaviour for unspecified values is ambiguous.
	By default, credits for all title types are included.
	"""
	titleType: [ID]
	"""
	Filter credits for titles with specific titleTypeCategory.
	By default, credits for all title type categories are included.
	"""
	titleTypeCategory: [TitleTypeCategoryValue]
	"""
	Filter credits to only those for these specific titles.
	By default, credits for all titles are included.
	"""
	titles: [ID]
}

input NameCreditSort {
	by: NameCreditSortBy!
	order: SortOrder!
}

enum NameCreditSortBy {
	RELEASE_DATE
}

"""
Summary of the name credits.
"""
type NameCreditSummary {
	"""
	List of categories the name has credits on
	"""
	categories: [CreditCategorySummary]
	"""
	List of genres the name has credits on
	"""
	genres: [GenreSummary]
	"""
	List of title type categories the name has credits on
	"""
	titleTypeCategories: [TitleTypeCategorySummary]
	"""
	List of title types the name has credits on
	"""
	titleTypes: [TitleTypeSummary]
	"""
	Total number of credits after restriction is applied and restriction info
	"""
	totalCredits: TotalCredits
}

"""
CreditSummaryV2 is a set of fields that summarises a names v2 credits.
"""
type NameCreditSummaryV2 {
	"""
	Title Genres for a given name collected from the names v2 credits.
	The genres field contains the aggregation of all title genres for each v2 credit a given name has.
	They are sorted by occurance then alphabetically.
	No filters are applied to the credits before fetching the title genres.
	"""
	genres: [GenreSummary]!
}

input NameCreditsV2Filter {
	"""
	Filters that apply to credits at the contributed credit level.
	"""
	creditLevelFilter: CreditLevelCreditsFilterSet
	"""
	Filters that apply to credits at the title level. In this context, they apply to the parent series title.
	"""
	titleLevelFilter: TitleLevelCreditsFilterSet
}

input NameCreditV2Sort {
	"""
	Capture the order of CreditV2 items in a listing.

	This is modelled as a string to allow for altering the list of allowed values over time. Supported values are:

	RELEASE_DATE: Credit ordering follows this priority sequence:
	1. Release date - For singular titles, the primary release date (most recent first by default)
	2. Latest release date - For series, most recent episode release date that this credit applies to
	3. Title text - Alphabetical comparison of the title text
	4. Title ID - A final tie-break by title ID to ensure consistent ordering (largest first).

	RELEASE_DATE is the default.

	The sort order can be controlled using the order parameter (ASC for oldest first, DESC for newest first).
	"""
	by: String
	order: SortOrder
}

"""
Generic type for an action link which has a unique name, url, and optionally associated label (localized text)
"""
type NamedActionLink {
	"""
	The name for this action link. Each action link in a list will have a unique name assigned.
	This name will persist, allowing client to identify the action link by its name.
	"""
	actionName: ActionLinkName!
	"""
	Optional label (localized text) to associate with URL.
	The label could be used with a button, text link, or other UI component.
	"""
	label: CallToActionText
	"""
	The URL for the action link
	"""
	url: String!
}

enum NameDataType {
	"""
	Matches names that have awards data
	"""
	AWARD_NOMINATIONS
	"""
	Matches names that have biography data
	"""
	BIOGRAPHY
	"""
	Matches names that have a birth date
	"""
	BIRTH_DATE
	"""
	Matches names that have a birth place
	"""
	BIRTH_PLACE
	"""
	Matches names that have a death date
	"""
	DEATH_DATE
	"""
	Matches names that have a death place
	"""
	DEATH_PLACE
	"""
	Matches names that have height data
	"""
	HEIGHT_INFO
	"""
	Matches names that have quotes data
	"""
	QUOTES
	"""
	Matches names that have trivia data
	"""
	TRIVIA
}

"""
Name death status enum
"""
enum NameDeathStatus {
	ALIVE
	DEAD
	PRESUMED_DEAD
}

"""
Page display preferences (e.g. whether to hide awards, biography, etc.)
"""
type NameDisplayPreferences {
	"""
	Visibility setting for 'also known as'
	"""
	akas: NameDisplayVisibilityLevel!
	"""
	Visibility setting for self-verified awards
	"""
	awards: NameDisplayVisibilityLevel!
	"""
	Visibility setting for biography
	"""
	biography: NameDisplayVisibilityLevel!
	"""
	Visibility setting for height
	"""
	height: NameDisplayVisibilityLevel!
}

"""
Page display visibility preference
"""
enum NameDisplayVisibilityLevel {
	HIDDEN
	PUBLIC
}

input NameEpisodeCreditsFilter {
	"""
	Filters that apply to credits at the contributed credit level.
	"""
	creditLevelFilter: CreditLevelCreditsFilterSet
	"""
	Filters that apply to credits at the episode level.
	"""
	episodeLevelFilter: EpisodesFilter
	"""
	Filters that apply to credits at the title level. In this context, they apply to the parent series title.
	"""
	titleLevelFilter: TitleLevelCreditsFilterSet
}

"""
Pagination for name facets
"""
type NameFacetConnection {
	edges: [NameFacetEdge]!
	pageInfo: PageInfo!
	"""
	Provides information on the total number of names you could filter on
	"""
	total: Int!
}

type NameFacetEdge {
	cursor: ID!
	node: Name!
	position: Int!
	"""
	How many items are associated with this name node
	"""
	total: Int!
}

enum NameFacetField {
	"""
	Facet by accent (Pro entitlement required)
	"""
	ACCENT
	"""
	Facet by athletic skill (Pro entitlement required)
	"""
	ATHLETIC_SKILL
	"""
	Facet by award event ID
	"""
	AWARD_EVENT_ID
	"""
	Facet by credit categories
	"""
	CREDIT_CATEGORIES
	"""
	Facet by credit genres (Pro entitlement required)
	"""
	CREDIT_GENRES
	"""
	Facet by credit production status (Pro entitlement required)
	"""
	CREDIT_PRODUCTION_STATUS
	"""
	Facet by credit type (Pro entitlement required)
	"""
	CREDIT_TYPE
	"""
	Facet by dance style (Pro entitlement required)
	"""
	DANCE_STYLE
	"""
	Facet by demographic ID (Pro entitlement required)
	"""
	DEMOGRAPHIC_ID
	"""
	Facet by ethnicity (Pro entitlement required)
	"""
	ETHNICITY
	"""
	Facet by eye color (Pro entitlement required)
	"""
	EYE_COLOR
	"""
	Facet by gender identity
	"""
	GENDER_IDENTITY
	"""
	Facet by guild affiliation (Pro entitlement required)
	"""
	GUILD_AFFILIATION
	"""
	Facet by hair color (Pro entitlement required)
	"""
	HAIR_COLOR
	"""
	Facet by hair length (Pro entitlement required)
	"""
	HAIR_LENGTH
	"""
	Facet by whether the name has award nominations
	"""
	HAS_AWARD_NOMINATION
	"""
	Facet by whether the name has award wins
	"""
	HAS_AWARD_WIN
	"""
	Facet by whether the name has clients (Pro entitlement required)
	"""
	HAS_CLIENTS
	"""
	Facet by whether to include deceased names (Pro entitlement required)
	"""
	INCLUDE_DECEASED
	"""
	Facet by job categories
	"""
	JOB_CATEGORIES
	"""
	Facet by media availability (Pro entitlement required)
	"""
	MEDIA
	"""
	Facet by musical instrument (Pro entitlement required)
	"""
	MUSICAL_INSTRUMENT
	"""
	Facet by occupation (Pro entitlement required)
	"""
	OCCUPATION
	"""
	Facet by valid passport status (Pro entitlement required)
	"""
	PASSPORT_VALID
	"""
	Facet by performance skill (Pro entitlement required)
	"""
	PERFORMANCE_SKILL
	"""
	Facet by physique (Pro entitlement required)
	"""
	PHYSIQUE
	"""
	Facet by professions
	"""
	PROFESSIONS
	"""
	Facet by profession categories
	"""
	PROFESSION_CATEGORIES
	"""
	Facet by representation type for names without a representative (Pro entitlement required)
	"""
	REPRESENTATION_TYPE_WITHOUT_REP
	"""
	Facet by representation type for names with clients (Pro entitlement required)
	"""
	REPRESENTATION_TYPE_WITH_CLIENT
	"""
	Facet by representation type for names with a representative (Pro entitlement required)
	"""
	REPRESENTATION_TYPE_WITH_REP
	"""
	Facet by spoken language (Pro entitlement required)
	"""
	SPOKEN_LANGUAGE
	"""
	Facet by unique trait (Pro entitlement required)
	"""
	UNIQUE_TRAIT
	"""
	Facet by USA work authorization status (Pro entitlement required)
	"""
	USA_WORK_AUTHORIZED
	"""
	Facet by voice type (Pro entitlement required)
	"""
	VOICE_TYPE
	"""
	Facet by whether the name works unpaid (Pro entitlement required)
	"""
	WORKS_UNPAID
	"""
	Facet by work history (Pro entitlement required)
	"""
	WORK_HISTORY
}

type NameFilmBiography implements PublicityListingType {
	category: PublicityListingCategory!
	"""
	The publicity listing as a displayable property
	"""
	displayableProperty: DisplayableNameFilmBiographyProperty!
	"""
	A list of biographical titles about this person.
	"""
	title: Title!
}

enum NameGenderIdentity {
	"""
	Matches names whose gender is female
	"""
	FEMALE
	"""
	Matches names whose gender is male
	"""
	MALE
	"""
	Matches names whose gender is non_binary
	"""
	NON_BINARY
	"""
	Matches names whose gender is other
	"""
	OTHER
}

"""
Guild company affiliations for a single name, with canonical (post-const-merge)
company references for unsuppressed guild affiliations only.
"""
type NameGuildCompanies {
	"""
	Canonical guild companies for this name.
	Returns post-const-merge Company entity references for unsuppressed guild affiliations.
	Empty array if the name has no guild affiliations.
	"""
	guildCompanies: [Company]!
	"""
	The name entity that was looked up.
	"""
	name: Name!
}

type NameHeight {
	"""
	The name's height as a displayable property, e.g. '6′ 4″ (1.93 m)', with imperial provided for customers in the
	following countries: "US", "CA", "GB", "IN", "AU", "NZ", "IE", "HK", "MY"
	"""
	displayableProperty: DisplayableNameHeightProperty!
	"""
	The name's height measurement
	"""
	measurement: LengthMeasurement!
}

input NameImagesFilter {
	"""
	Return only a name's featured images, no featured images, or if not specified, all images
	"""
	featured: FeaturedImagesOption
	"""
	Return images meeting the specified gallery constraints
	"""
	galleryConstraints: ImageGalleryFilterConstraints
	"""
	Return images meeting the specified name constraints
	"""
	nameConstraints: ImageNameFilterConstraints
	"""
	Return only images tagged with at least a minimum and/or at most a maximum number of names
	"""
	namesCount: CountInterval
	"""
	Return images meeting the specified title constraints
	"""
	titleConstraints: ImageTitleFilterConstraints
	"""
	Return only images which are one of these types
	"""
	types: [ID]
}

type NameKnownFor {
	"""
	The credit this name is known for
	The known for credit is selected based on the principal category
	"""
	credit: Credit!
	"""
	The title this name is known for
	"""
	title: Title!
}

"""
Pagination for Known For titles which always has a known total number of edges.
"""
type NameKnownForConnection {
	"""
	Pages of Known For Titles
	"""
	edges: [NameKnownForEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Restriction related information if exists for the connection
	"""
	restriction: NameKnownForRestriction
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of Known For Titles
"""
type NameKnownForEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A Known For node
	"""
	node: NameKnownFor!
	"""
	Position of the node in this context, eg: KnownFor 4 out of 100
	"""
	position: Int!
}

"""
Restriction information applied to KnownFor
"""
type NameKnownForRestriction {
	"""
	Explanation(s) for the restriction
	"""
	explanations: [RestrictionExplanation]!
	reasons: [ContentRestrictionReason]!
	"""
	DEPRECATED: Renamed, use `reasons` instead.
	"""
	restrictionReason: [ContentRestrictionReason]!
	"""
	The total number of items if there was no restriction
	"""
	unrestrictedTotal: Int
}

"""
Filters that apply to credits at the name level.
Each specified filter is AND-ed together.
For filters that accept a list of values, the filter is an OR across that list, so any
credit matching one value meets the filter's criteria.
"""
input NameLevelCreditsFilterSet {
	"""
	Filter credits to only those for these specific names.
	By default, credits for all names are included.
	"""
	nameId: [ID]
}

type NameListItemSearchConnection {
	edges: [NameListItemSearchEdge]!
	facet(facetField: NameFacetField!, limit: Int): [SearchFacet]
	pageInfo: PageInfo!
	total: Int!
}

type NameListItemSearchEdge {
	cursor: ID!
	name: Name!
	node: ListItemSearchNode!
}

input NameListSearchSort {
	by: NameListSearchSortBy!
	order: SortOrder!
}

enum NameListSearchSortBy {
	"""
	Sort names based on their birth date
	ASC: Earliest dates to Highest dates (Oldest - Youngest)
	"""
	BIRTH_DATE
	"""
	Date when the list item was added to the list
	ASC: Earlier date will be first.
	"""
	DATE_ADDED
	"""
	Sort names based on their death date
	ASC: Earliest dates to last dates (Died First - Died Last)
	"""
	DEATH_DATE
	"""
	List order determined by the customer
	"""
	LIST_ORDER
	"""
	Sort names alphabetically
	ASC: A-Z
	"""
	NAME
	"""
	Sort names based on their starMeterCurrentWeekRank
	ASC: Lower popularity score means that the name is more popular, so the most popular names will be first
	"""
	POPULARITY
}

"""
Name data that provides additional context exclusively for name owner or authorized managers
"""
type NameManagedData {
	"""
	Given a list of image IDs, returns only those that are still ACTIVE in Paparazzi.
	Used to filter out recently-deleted images that may still appear in cached gallery results.
	Maximum of 200 image IDs allowed per request. Throws UserInputError if exceeded.

	Throws UNAUTHENTICATED if not logged in
	Throws FORBIDDEN if not entitled

	@entitlement(PRO_ANY_TIER, NAME_OWNER)
	"""
	activeResumeImageIds(imageIds: [ID]!): [ID]
	"""
	Automatic featured images to use if customer does not manually select with max limit 6

	Throws UNAUTHENTICATED if not logged in
	Throws FORBIDDEN if not entitled

	@entitlement(PRO_ANY_TIER, NAME_OWNER)
	"""
	automaticFeaturedImages(limit: Int!): [Image]
	"""
	Automatic known for titles to use if customer does not manually select with max limit 4

	Throws UNAUTHENTICATED if not logged in
	Throws FORBIDDEN if not entitled

	@entitlement(PRO_ANY_TIER, NAME_OWNER)
	"""
	automaticKnownFor(limit: Int!): [Title]
	"""
	Featured images that customer selected manually last time

	Returns null if customerId is not present or user is not entitled.

	@entitlement(PREMIUM_NAME_OWNER)
	"""
	customFeaturedImages: CustomFeaturedImages
	"""
	Known for titles that customer selected manually last time

	Returns null if user is not entitled or not authenticated

	@entitlement(PREMIUM_NAME_OWNER)
	"""
	customKnownFor: CustomKnownFor
	"""
	Primary image settings configured by the name owner

	Throws UNAUTHENTICATED if not logged in
	Returns null if not entitled

	@entitlement(PREMIUM_NAME_OWNER)
	"""
	customPrimaryImage: CustomPrimaryImage
	"""
	Page display preferences (e.g. whether to hide biography, akas, etc.)

	Throws UNAUTHENTICATED if not logged in
	Throws FORBIDDEN if not entitled
	"""
	displayPreferences: NameDisplayPreferences
	"""
	Up-to-date primary image for customer

	Throws UNAUTHENTICATED if not logged in
	Throws FORBIDDEN if not entitled

	@entitlement(PRO_ANY_TIER, NAME_OWNER)
	"""
	latestPrimaryImage: Image
}

type NameMeta {
	canonicalId: ID!
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	publicationStatus: PublicationStatus!
}

type NameMetadata {
	"""
	All the name credit categories
	"""
	nameCreditCategories: [NameCreditCategory]!
}

"""
Ranking of a Name within a set of dimensions.
Requires entitlement proving Pro subscription.
"""
type NameMeterInsight {
	"""
	The insight chart identifier
	"""
	chartId: ID
	"""
	The insight dimensions for which the rank was computed
	"""
	dimensions: [MeterInsightDimension]!
	"""
	The displayable text for the insight
	"""
	insightText: MeterInsightText
	"""
	The insight type
	"""
	insightType: MeterInsightType!
	"""
	The current rank within the category defined by the insight dimensions
	"""
	rank: Int!
}

input NameMeterInsightsInput {
	"""
	Maximum number of insights to return
	"""
	limit: Int
}

type NameMeterRanking {
	"""
	The current IMDbPro StarMeter ranking of the name. A lower value is better.
	Ranking information beyond 5000 requires entitlement proving Pro subscription.
	"""
	currentRank: Int!
	"""
	If a previous rank exists, this is a set of fields comparing the current rank
	to the previous rank
	"""
	rankChange: MeterRankChange
}

type NameMeterRankingHistory {
	"""
	The best historical IMDbPro StarMeter ranking of a name.
	A lower value is better. By default, returns the values over all time.
	Requires entitlement proving Pro subscription.
	"""
	bestRank: MeterRankingHistoryEntry
	"""
	History of IMDbPro STARmeter rankings of a name.
	Requires entitlement proving Pro subscription.
	"""
	ranks: [MeterRankingHistoryEntry]
	restriction: MeterRestriction
}

input NameMeterRankingHistoryInput {
	"""
	Last date to obtain STARmeter history for. If not provided, default is most recent ranking date.
	If there is no ranking on end date, the date is the most recent date before this one.
	"""
	endDate: Date
	"""
	First date to obtain STARmeter history for. If not provided, default is first ranking date.
	If there is no ranking on start date, the date is the earliest date after this one.
	"""
	startDate: Date
}

type NameOtherWork {
	"""
	Type of the Other Work item
	"""
	category: NameOtherWorkCategory
	"""
	Other work as displayable property
	"""
	displayableProperty: DisplayableNameOtherWorkProperty!
	"""
	Start date of the OtherWork item as structured date components (year, month, day, isBCE, isApproximate)
	"""
	fromDateComponents: DateComponents
	id: ID!
	"""
	Machine translated other work text. Returns the text field translated based on the language header.
	"""
	machineTranslatedText: MachineTranslatedMarkdown!
	"""
	The details of the work this person was involved in
	"""
	text: Markdown!
	"""
	End date of the OtherWork item as structured date components (year, month, day, isBCE, isApproximate)
	"""
	toDateComponents: DateComponents
}

type NameOtherWorkCategory {
	"""
	id will be the same as text
	"""
	id: ID!
	text: String!
}

"""
Pagination for Other Works types which always has a known total number of edges.
"""
type NameOtherWorkConnection {
	"""
	Pages of OtherWork
	"""
	edges: [NameOtherWorkEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of OtherWork
"""
type NameOtherWorkEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A OtherWork node
	"""
	node: NameOtherWork!
	"""
	Position of the node in this context, eg: OtherWork 4 out of 31
	"""
	position: Int!
}

"""
Restrictions on a person's customer-suppressed personal details. Never contains a suppressed
value: demographics are reported as category names, and names are reported as an allowlist of the
names that remain permitted.
"""
type NamePersonalDetailsRestrictions {
	"""
	The exhaustive set of names a client is permitted to use for this person: the canonical
	display name plus all non-suppressed alternate names (which already include credited-as
	spellings), nicknames, and the birth/real name when present. Suppressed names are never
	present in this list. Any name not in this list — including one sourced from a third party
	— must be treated as disallowed.

	This list is intentionally not paginated: it is returned in full so the permitted set is
	unambiguous. In practice it is expected to contain well under ~50 names for any given
	person (typically only a handful), so returning it in full is not a payload concern.
	"""
	allowedNames: [String]!
	"""
	Demographic categories this person has not made public (visibility is not PUBLIC — i.e.
	IMDb-Pro-only or hidden). Category names only; the suppressed values themselves are never
	included. Clients must not reveal these attributes. The list is empty when no demographic
	categories are suppressed. Age is not a distinct category: it is covered by BIRTH_DATE and
	BIRTH_YEAR.
	"""
	suppressedDemographics: [DemographicDataTypeValue]!
}

type NamePersonalLocation {
	"""
	ID of the location
	"""
	id: ID!
	language: DisplayableLanguage!
	latitude: String!
	longitude: String!
	"""
	Display text of the location, e.g. 'Sydney, Australia'
	"""
	text: String!
}

type NamePersonalLocationMetadata {
	"""
	The maximum number of values a customer can have for personal locations
	"""
	limit: Int!
	"""
	All valid values for personal locations
	"""
	validValues: [NamePersonalLocation]!
}

type NamePersonalLocations {
	locations: [NamePersonalLocation]!
	"""
	Total number of personal locations
	"""
	total: Int!
}

type NamePortrayal implements PublicityListingType {
	category: PublicityListingCategory!
	"""
	The publicity listing as a displayable property
	"""
	displayableProperty: DisplayableNamePortrayalProperty!
	"""
	A list of titles in which this person was portrayed by an actor, not including biographical titles about this person.
	"""
	title: Title!
}

type NamePrintBiography implements PublicityListingType {
	"""
	Text describing the authors for this book
	"""
	authors: [Markdown]
	category: PublicityListingCategory!
	"""
	The biographical book as a displayable property
	"""
	displayableProperty: DisplayableNamePrintBiographyProperty!
	"""
	The ISBN number, without hyphens, corresponding to the biography book. Can be ISBN-10 or ISBN-13.
	"""
	isbn: String
	"""
	Information about the publisher.
	"""
	publisher: String
	"""
	The title of the biographical book.
	"""
	title: PublicityListingTitleText
}

"""
A NameProfession is a type of work that someone wants to be known for doing on an ongoing basis.
For example a “Makeup Artist”.

All fields are marked as nullable in case of invalid graphlet cross-link - in practice these should never be null.
"""
type NameProfession {
	"""
	The profession ID e.g. amzn1.imdb.profession.1.2.3
	"""
	id: ID!
	"""
	Flags whether this profession can be selected by customers in menus during contribution or search.
	Staff-only professions like 'Monarch of the United Kingdom' are false.
	Undefined values will be treated as false

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	isCustomerSelectable: Boolean
	"""
	Details of the Profession

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	profession: DisplayableProfession
	"""
	Details of the Profession Category

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	professionCategory: ProfessionCategory
	"""
	short description of the Profession

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	shortDescription: DisplayableProfessionDescription
}

"""
The constraints here are combined (AND).

example:

professionCategoryConstraint: {
    allProfessionCategoryIds: ["amzn1.imdb.concept.profession_category.0001...",
"amzn1.imdb.concept.profession_category.0002..."],
    anyProfessionCategoryIds: ["amzn1.imdb.concept.profession_category.0003...",
"amzn1.imdb.concept.profession_category.0004..."]
    excludeProfessionCategoryIds: ["amzn1.imdb.concept.profession_category.0005..."]
}
will match names that have the profession categories ("...0001..." AND
"...0002...") AND ("...0003..." OR "...0004...") AND NOT "...0005..."
"""
input NameProfessionCategorySearchConstraint {
	"""
	A name match must have ALL of these profession categories.
	"""
	allProfessionCategoryIds: [ID]
	"""
	A name match must have AT LEAST ONE of these profession categories.
	"""
	anyProfessionCategoryIds: [ID]
	"""
	A name match must not have any of these profession categories.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeProfessionCategoryIds: [ID]
}

"""
The constraints here are combined (AND).

example:

professionConstraint: {
    allProfessionIds: ["amzn1.imdb.concept.profession.0001...", "amzn1.imdb.concept.profession.0002..."],
    anyProfessionIds: ["amzn1.imdb.concept.profession.0003...", "amzn1.imdb.concept.profession.0004..."]
    excludeProfessionIds: ["amzn1.imdb.concept.profession.0005..."]
}
will match names that have the professions ("...0001..." AND "...0002...") AND
("...0003..." OR "...0004...") AND NOT "...0005..."
"""
input NameProfessionSearchConstraint {
	"""
	A name match must have ALL of these professions.
	"""
	allProfessionIds: [ID]
	"""
	A name match must have AT LEAST ONE of these professions.
	"""
	anyProfessionIds: [ID]
	"""
	A name match must not have any of these professions.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeProfessionIds: [ID]
}

type NameQuote {
	"""
	The quote as a displayable article
	"""
	displayableArticle: DisplayableArticle
	id: ID!
	language: DisplayableLanguage
	"""
	Machine translated displayable article. Returns the displayable article translated based on the language header.
	"""
	machineTranslatedDisplayableArticle: MachineTranslatedDisplayableArticle
	"""
	Machine translated quote text. Returns the text field translated based on the language header.
	"""
	machineTranslatedText: MachineTranslatedMarkdown!
	text: Markdown!
}

type NameQuoteConnection {
	edges: [NameQuoteEdge]!
	pageInfo: PageInfo!
	total: Int!
}

type NameQuoteEdge {
	cursor: ID!
	node: NameQuote!
	position: Int!
}

input NameQuoteMatchingSearchConstraint {
	"""
	A name match must contain ALL of these search terms in at least one of its quotes.
	"""
	allQuoteTextTerms: [String]
	"""
	A name match must contain AT LEAST ONE of these search terms in at least one of its quotes.
	"""
	anyQuoteTextTerms: [String]
}

type NameRecommendation {
	"""
	Explanation for why we recommend this name, to comply with privacy laws
	"""
	explanation: LocalizedMarkdown!
	name: Name!
}

type NameRecommendationConnection {
	edges: [NameRecommendationEdge]!
	pageInfo: PageInfo!
}

type NameRecommendationEdge {
	cursor: ID!
	node: NameRecommendation!
}

type NameRecommendations {
	names(after: ID, first: Int!): NameRecommendationConnection
	refTag: String!
}

type NameRelation {
	"""
	Date of Birth for the related person
	"""
	birthDate: DisplayableDate
	"""
	Gender of the related person
	"""
	genderIdentity: GenderIdentity
	"""
	Relation ID, e.g. rs0023922
	"""
	id: ID!
	"""
	Details of the relative of this name.
	"""
	relationName: RelationName
	"""
	Relationship type, e.g. parent
	"""
	relationshipType: NameRelationType!
}

type NameRelationsConnection {
	"""
	Pages of NameRelation
	"""
	edges: [NameRelationsEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of NameRelation
"""
type NameRelationsEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	Details of the relationship
	"""
	node: NameRelation!
	"""
	Position of the node in this context, eg: NameRelation 4 out of 31
	"""
	position: Int!
}

input NameRelationsFilter {
	"""
	Filter out specific relation categories
	"""
	excludeRelationshipTypes: [RelationshipTypeFilter]
	"""
	Filter for specific relation categories
	"""
	relationshipTypes: [RelationshipTypeFilter]
}

type NameRelationType {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

type NameRepresentation {
	"""
	The company representing this name, if there is one

	Either agency or independentRepresentative will have data but not both
	"""
	agency: Agency
	"""
	The name being represented
	"""
	client: Name!
	"""
	An obfuscated persistent id to uniquely identify this data
	"""
	id: ID!
	"""
	Someone representing this name but not associated with a company.

	Either agency or independentRepresentative will have data but not both
	"""
	independentRepresentative: Name
	"""
	The type of relationship this representation has with the name
	"""
	relationshipType: RepresentationRelationshipType!
}

type NameRepresentationConnection {
	edges: [NameRepresentationEdge]!
	pageInfo: PageInfo!
	total: Int!
}

type NameRepresentationEdge {
	cursor: ID!
	node: NameRepresentation!
	position: Int!
}

"""
A single stored representation relationship between a name (the client) and
whoever represents them: a company, an individual, or an individual at a
company.

There is one record per stored relationship. For example, a client with two
talent agents at the same agency has two records (one per agent), and a
client whose manager is also their publicist has one record per
relationship type.
"""
type NameRepresentationRecord {
	"""
	The globally unique identifier (GUID) of the catalog item that stores
	this record in Charger, IMDb's contribution data store.

	Contribution workflows use this identifier to load and modify this exact
	record: pass it as the `catalogId` when starting an edit workflow for
	this record.

	Null while this graphlet's Charger read path is disabled (during the
	migration off Tango). When null, this record cannot be targeted by an
	edit workflow, and clients should not render an edit action for it.
	"""
	catalogId: ID
	"""
	The name being represented
	"""
	client: Name!
	"""
	The company representing this name on this record, if there is one.

	Null when the client is represented by an individual who is not
	associated with a company (see `representative`). At least one of
	`company` or `representative` is always present on a record.
	"""
	company: Company
	"""
	An obfuscated persistent id to uniquely identify this representation
	record. Stable across edits to the record.
	"""
	id: ID!
	"""
	Whether the representative on this record is the client's main point of
	contact among the representatives sharing this record's relationship
	type and company.

	Each relationship type and company combination can have at most one
	point of contact, but is not guaranteed to have one. Always false for
	records without an individual representative.
	"""
	isPointOfContact: Boolean!
	"""
	The type of relationship this representation has with the name
	"""
	relationshipType: RepresentationRelationshipType!
	"""
	The individual representing this name on this record, if there is one.

	Present both for representatives working at the record's company
	(`company` is also present) and for independent representatives
	(`company` is null). Null when a company represents the name without a
	specific individual assigned. At least one of `company` or
	`representative` is always present on a record.
	"""
	representative: Name
}

type NameRepresentationRecordConnection {
	edges: [NameRepresentationRecordEdge]!
	pageInfo: PageInfo!
	"""
	The total number of representation records for this name
	"""
	total: Int!
}

type NameRepresentationRecordEdge {
	cursor: ID!
	node: NameRepresentationRecord!
	position: Int!
}

type NameSearchConnection {
	edges: [NameSearchEdge]!
	pageInfo: PageInfo!
}

type NameSearchEdge {
	cursor: ID!
	node: Name!
}

type NameSearchIndexing {
	"""
	A boolean flag that indicates whether IMDb should set the `noIndex` tag on pages for this name.
	"""
	disableIndexing: Boolean!
}

type NameSpouse {
	"""
	Attributes that describe the spouse relationship
	"""
	attributes: [SpouseAttributes]
	"""
	Indicates if this is the current spouse
	"""
	current: Boolean!
	"""
	Spouse displayable property
	"""
	displayableProperty: DisplayableNameSpouseProperty!
	"""
	Details of the spouse of this name.
	"""
	spouse: SpouseName
	"""
	Spouse time range as a formatted string eg '12 January 2020 - present'
	"""
	timeRange: DisplayableSpouseTimeRange
}

type NameText {
	text: String!
}

input NameTextSearchConstraint {
	"""
	Matches names based on their name text.
	When useFuzzySearch is false or omitted, only exact token matches are returned.
	For example: 'Audrey Hepburn' will be matched by 'Audrey' and 'Hepburn' but not 'Audrey Hep'.
	When useFuzzySearch is true, approximate and prefix matches are also included.
	"""
	searchTerm: String!
	"""
	Enable fuzzy search results to include approximate matches.
	Defaults to false when omitted - only exact matches are returned.
	"""
	useFuzzySearch: Boolean
}

"""
Details about an attachment event, i.e. when a name is attached to a title
"""
type NameToTitleAttachment {
	"""
	The time (in ISO 8601 format) that the name-to-title attachment was published

	e.g. '2020-06-23T15:41:18.243Z'
	"""
	attachmentTime: DateTime!
	"""
	If the attached name is in the cast of the title, this list contains the
	names of the characters they are credited as
	"""
	characterList: [String]!
	"""
	The attached name's credited professions in the title
	"""
	creditCategories: [CreditCategory]!
	"""
	Unique Id associated with this attachment event
	"""
	id: ID!
	"""
	The name in this name to title attachment
	"""
	name: Name!
	"""
	The title in this name to title attachment
	"""
	title: Title!
}

type NameToTitleAttachmentConnection {
	edges: [NameToTitleAttachmentEdge]!
	pageInfo: PageInfo!
	"""
	Total number of attachments that match the input query
	"""
	total: Int!
}

"""
cursor matches the Id of the last attachment event returned
"""
type NameToTitleAttachmentEdge {
	cursor: ID!
	node: NameToTitleAttachment!
}

type NameTrackRecommendationEdge {
	"""
	Cursor ID of the name track recommendation
	"""
	cursor: ID!
	"""
	A name track recommendation node
	"""
	node: Name!
	"""
	Position of the node in this context, e.g. NameTrackRecommendationEdge 2 out of 10
	"""
	position: Int!
}

type NameTrackRecommendationsConnection {
	"""
	Page of name track recommendations
	"""
	edges: [NameTrackRecommendationEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of name track recommendations
	"""
	total: Int!
}

type NameTrivia {
	"""
	Date of the Trivia item
	"""
	date: Date
	"""
	The trivia as a displayable article
	"""
	displayableArticle: DisplayableArticle
	id: ID!
	"""
	trivia Voting summary
	"""
	interestScore: InterestScore!
	"""
	Machine translated displayable article. Returns the displayable article translated based on the language header.
	"""
	machineTranslatedDisplayableArticle: MachineTranslatedDisplayableArticle
	"""
	Machine translated trivia text. Returns the text field translated based on the language header.
	"""
	machineTranslatedText: MachineTranslatedMarkdown!
	"""
	The trivia text
	"""
	text: Markdown!
}

"""
Pagination for Trivia types which always has a known total number of edges.
"""
type NameTriviaConnection {
	"""
	Pages of Trivia
	"""
	edges: [NameTriviaEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of Trivia
"""
type NameTriviaEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A Trivia node
	"""
	node: NameTrivia!
	"""
	Position of the node in this context, eg: Trivia 4 out of 31
	"""
	position: Int!
}

input NameTriviaMatchingSearchConstraint {
	"""
	A name match must contain ALL of these search terms in at least one of its trivia.
	"""
	allTriviaTextTerms: [String]
	"""
	A name match must contain AT LEAST ONE of these search terms in at least one of its trivia.
	"""
	anyTriviaTextTerms: [String]
}

type NameWeight {
	unit: WeightUnit!
	value: Float!
}

input NearbyCinemasFilter {
	"""
	Filter cinemas based on favorite status.
	If not provided, defaults to return all cinemas.
	Specifying `favorites` will make the query personalized, so it will result in the query not being cached.
	"""
	favorites: FavoriteFilterOption
	"""
	Filter cinemas based on location.
	"""
	location: ShowtimesLocation!
}

"""
A negative format, along with any attributes. For example, we could have 70MM film that was used for the IMAX and IMAX3D
versions.
"""
type NegativeFormat {
	attributes: [DisplayableAttribute]!
	"""
	Negative format as displayable property
	"""
	displayableProperty: DisplayableTechnicalSpecificationProperty!
	"""
	The negative format
	"""
	negativeFormat: String!
}

"""
Negative formats for this title.
"""
type NegativeFormats {
	"""
	The list of negative format items
	"""
	items: [NegativeFormat]!
	restriction: TechnicalSpecificationsRestriction
	"""
	Total number of negative format items
	"""
	total: Int!
}

"""
News details
"""
type News {
	"""
	The title text of this news article
	"""
	articleTitle: Markdown!
	"""
	The byline of this news article
	"""
	byline: String
	"""
	The date of this news article
	"""
	date: Date!
	"""
	A direct link to the article on an external news site (if available)
	"""
	externalUrl: String
	"""
	Unique persistent ID for this news article
	"""
	id: ID!
	"""
	An image associated with this news article
	"""
	image: Image
	"""
	The language of this news article
	"""
	language: DisplayableLanguage
	"""
	Duplicate or very similar news articles are aggregated here
	"""
	similarNewsItems: [News]
	"""
	Name and link to homepage for the source website of this news article
	"""
	source: NewsSource!
	"""
	The lead paragraph of this news article
	"""
	text: Markdown!
}

"""
Categories of news articles.

This contains both Consumer and Pro categories.

The newsCategories() query can be used to fetch additional metadata
for each enum value and to fetch specifically Consumer or Pro sets of categories.
"""
enum NewsCategory {
	ALL_INDUSTRY
	AWARDS_AND_EVENTS
	CELEBRITY
	DEVELOPMENT_AND_PRODUCTION
	INDIE
	INTERVIEWS_PROFILES_AND_THINK_PIECES
	MOVIE
	RELEASES_AND_PREMIERES
	RESULTS
	REVIEWS_AND_RECAPS
	THE_BUSINESS
	TOP
	TOP_INDUSTRY
	TV
}

"""
Specifies which types of news categories we want to return.

domain can be used to return only Consumer or only Pro categories.
"""
input NewsCategoryConstraints {
	"""
	Specifies which domain to fetch news categories for
	"""
	domain: BusinessDomain
}

"""
Additional metadata about NewsCategory enum values
"""
type NewsCategoryMetadata {
	"""
	The NewsCategory enum value that corresponds to this metadata
	"""
	category: NewsCategory!
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

"""
Pagination for news articles which always has a known total number of edges.
"""
type NewsConnection {
	"""
	Pages of news
	"""
	edges: [NewsEdge]!
	"""
	Details of the page
	"""
	pageInfo: NewsPageInfo!
	restriction: NewsRestriction
	"""
	Total number of objects in the entire set.
	Live counts are supported for name and title news queries. For category news
	queries, this field returns a static value and clients should use
	`pageInfo.hasNextPage` for pagination instead.
	"""
	total: Int!
}

"""
Page of news articles.
"""
type NewsEdge {
	"""
	A news node
	"""
	node: News!
}

union NewsEntity = Company | Name | Title

type NewsLink {
	label: String
	url: String!
}

"""
Page info of news articles.
"""
type NewsPageInfo {
	"""
	When paginating forwards, the cursor to continue
	"""
	endCursor: ID
	hasNextPage: Boolean!
}

type NewsRestriction {
	"""
	Explanation(s) for the restriction
	"""
	explanations: [RestrictionExplanation]!
	reasons: [ContentRestrictionReason]!
	"""
	DEPRECATED: Renamed, use `reasons` instead.
	"""
	restrictionReason: [ContentRestrictionReason]!
	"""
	The total number of items if there was no restriction
	"""
	unrestrictedTotal: Int
}

"""
A news source
"""
type NewsSource {
	"""
	Text description of the source, which is only populated if homepage is undefined
	"""
	description: String
	"""
	Link to the homepage of the site (if available)
	"""
	homepage: NewsLink
	"""
	The icon associated with this news source
	"""
	icon: NewsSourceIconImage
	"""
	Unique persistent ID for this news source
	"""
	id: ID!
	"""
	Field to indicate that this news source has been verified to be a trusted news source by IMDb
	"""
	trustedSource: Boolean!
}

type NewsSourceIconImage {
	"""
	Height of the icon image, in pixels
	"""
	height: Int
	"""
	URL of the NewsSource icon image
	"""
	url: String
	"""
	Width of the icon image, in pixels
	"""
	width: Int
}

type NickName {
	"""
	The nickname as a displayable property
	"""
	displayableProperty: DisplayableNickNameProperty!
	text: String!
}

type Nomination {
	"""
	The award this nomination is associated with
	"""
	award: NominationAward
	"""
	Company/Companies, Title(s) and/or name(s) the award is for
	"""
	awardedEntities: NominationAwardedEntities
	"""
	The category in which the award was given. Example: Best Motion Picture of the Year.
	"""
	category: AwardCategory
	"""
	The event this nomination is associated with
	"""
	event: NominationEvent
	"""
	The event edition this nomination associated with
	"""
	eventEdition: NominationEventEdition
	"""
	Optional specific episode(s) for which the nomination is associated with
	"""
	forEpisodes: [Title]
	"""
	Optional song title(s) for which the nomination is associated with
	"""
	forSongTitles: [DisplayableSongTitle]
	id: ID!
	"""
	Did the awarded entities win this award
	"""
	isWinner: Boolean
	"""
	Additional notes associated with the award.
	"""
	notes: Markdown
	"""
	Optional date and time when the award win was officially announced.
	"""
	winAnnouncementDate: DisplayableDate
	"""
	The ranking of this nomination, when applicable.
	"""
	winningRank: Int
}

type NominationAward {
	"""
	Award Id
	"""
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	"""
	Nominations for an award grouped by its category
	"""
	nominationCategories(
		after: ID
		before: ID
		filter: AwardNominationCategoriesFilter
		first: Int
		jumpTo: ID
		last: Int
	): NominationsWithCategoryConnection
	"""
	Award name
	"""
	text: String!
}

union NominationAwardedEntities =
	| AwardedCompanies
	| AwardedNames
	| AwardedTitles

input NominationCategoriesFilter {
	"""
	Filter out given nomination award categories
	"""
	excludeCategories: [String]
	"""
	Filter for given nomination award categories
	"""
	includeCategories: [String]
}

type NominationConnection {
	"""
	Pages of award nominations
	"""
	edges: [NominationEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

type NominationEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	An award nomination node
	"""
	node: Nomination!
	"""
	Position of the node in this context, eg: Nomination 4 out of 31
	"""
	position: Int!
}

type NominationEvent {
	"""
	Other Event names by which this Event is known, default sort order is alphabetically
	"""
	akas(limit: Int): [NominationEventAka]!
	"""
	A list of all distinct awards across event's editions sorted alphabetically
	"""
	awards(limit: Int): [NominationAward]!
	"""
	A list of all the event's editions sorted by year, followed by
	instanceWithinYear, with the most recent first (e.g. 2023, 2022 edition 2,
	2022 edition 1, ...)
	"""
	editions(
		filter: NominationEventEditionFilter
		limit: Int
	): [NominationEventEdition]!
	"""
	The type of the event, e.g. industry_event / festival / media_event /
	critics_event. Returns null when the event has not been assigned a type.
	"""
	eventType: String
	id: ID!
	"""
	The location of the event
	"""
	location: DisplayableLocation
	"""
	Event name
	"""
	name: NominationEventName!
	"""
	List of trivia items related to the event with potential markdown links for movies/people
	"""
	trivia(limit: Int): [Markdown]!
	"""
	The urls for the event
	"""
	urls(limit: Int): [EventUrl]!
}

type NominationEventAka {
	"""
	End year associated with the event name
	"""
	endYear: Int
	"""
	Name of the event
	"""
	name: NominationEventName!
	"""
	Start year associated with the event name
	"""
	startYear: Int
}

type NominationEventEdition {
	"""
	A list of awards announced in an event edition e.g. 'Oscar'
	"""
	awards(
		filter: NominationEventEditionAwardsFilter
		limit: Int
	): [NominationAward]!
	"""
	Dates over which this event edition occurred or will occur. Field is null means the date(s) of the event are not known.
	If event was a 1 day event endDate will be null.
	"""
	dateRange: DisplayableDateRange
	"""
	The event to which this edition is a part of, e.g. Academy Awards
	"""
	event: NominationEvent!
	id: ID!
	"""
	A number that defines the sequence of editions within a year, starting with 1
	"""
	instanceWithinYear: Int!
	"""
	List of trivia items related to the event edition with potential markdown links for movies/people
	"""
	trivia(limit: Int): [Markdown]!
	"""
	The year this edition of the event took place
	"""
	year: Int!
}

input NominationEventEditionAwardsFilter {
	"""
	Filter awards by award id
	"""
	awards: AwardsFilter
}

input NominationEventEditionFilter {
	"""
	Filter out given instances order within the year. e.g [1, 2]
	"""
	excludeInstancesWithinYear: [Int]
	"""
	Filter out given EventEdition years
	"""
	excludeYears: [Int]
	"""
	Filter for given instances order within the year. e.g [1, 2]
	"""
	includeInstancesWithinYear: [Int]
	"""
	Filter for given EventEdition years
	"""
	includeYears: [Int]
}

type NominationEventName {
	"""
	Type token
	"""
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	"""
	The display text
	"""
	text: String!
}

input NominationsFilter {
	"""
	Filter awards by award id
	"""
	awards: AwardsFilter
	"""
	Filter awards by event id
	"""
	events: EventsFilter
	"""
	Filter nominations on win status
	"""
	wins: WinsFilter
}

input NominationsSort {
	by: NominationsSortBy!
	order: SortOrder!
}

"""
Default sort is done by sorting nomination id
"""
enum NominationsSortBy {
	"""
	Sorts by prestigious awards first, then reverse chronological by year
	"""
	PRESTIGIOUS
	"""
	Sorts by wins first, then preserve default nomination order
	"""
	WINS
}

type NominationsWithCategory {
	"""
	The category of the nomination e.g. 'Best Picture'. If no category exist for a nomination, category is returns null.
	"""
	category: AwardCategory
	"""
	Nominations belong to the category
	"""
	nominations(
		after: ID
		before: ID
		filter: NominationsFilter
		first: Int
		jumpTo: ID
		last: Int
		sort: NominationsSort
	): NominationConnection
}

"""
Pagination for NominationsWithCategory types which always has a known total number of edges.
"""
type NominationsWithCategoryConnection {
	"""
	Pages of nominations with category
	"""
	edges: [NominationsWithCategoryEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of nominations with category
"""
type NominationsWithCategoryEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	An NominationsWithCategory node
	"""
	node: NominationsWithCategory!
	"""
	Position of the node in this context, eg: NominationsWithCategory 4 out of 312
	"""
	position: Int!
}

"""
The type of notification channel, that can contain future metadata
"""
type NotificationChannel {
	type: NotificationChannelType!
}

"""
The preference state of notification channel
"""
type NotificationChannelState {
	"""
	The notification channel
	"""
	channel: NotificationChannel!
	"""
	Whether this channel is enabled or disabled
	"""
	enabled: Boolean!
	"""
	When this setting was last updated.
	Null if the user has never updated it.
	"""
	lastUpdated: DateTime
}

"""
The types of channels through which notifications can be delivered
"""
enum NotificationChannelType {
	IN_APP
	PUSH
}

"""
A type of notification topic and its details
"""
type NotificationTopic {
	"""
	Braze custom attribute string
	"""
	brazeCustomAttributeKey: String!
	"""
	ID of the notification topic
	"""
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	"""
	Display text for the notification topic
	"""
	text: String!
}

"""
Defines standard form constraints for a number input type of field.
TODO: Move into common schema validation types after review and update standards.
"""
type NumberFieldConstraints {
	"""
	Specifies whether only integer values are allowed. When true,
	decimal/floating-point values are invalid. Defaults to false.
	"""
	integerOnly: BooleanValidationConstraint
	"""
	Specifies if the customer must provide a non-null numeric value
	"""
	isRequired: BooleanValidationConstraint
	"""
	Specifies the maximum allowed numeric value for this number input (inclusive)
	"""
	maximumValue: FloatThresholdValidationConstraint
	"""
	Specifies the minimum allowed numeric value for this number input (inclusive)
	"""
	minimumValue: FloatThresholdValidationConstraint
}

input NumClientsConstraint {
	"""
	Match names based on the number of clients they represent or manage.
	Infers min 0 and unbounded max if part of the range is not provided.
	"""
	numClientsRange: IntRangeInput!
}

input NumCreditsConstraint {
	"""
	Match names based on their number of credits.
	Infers min 0 and unbounded max if part of the range is not provided.
	"""
	numCreditsRange: IntRangeInput!
}

"""
Match titles based on occupation credits attached to their credits.
"""
input OccupationCreditSearchConstraint {
	"""
	Match titles that have any of the specified occupations attached.
	This is a string, but may change in the future as PACE evolves.
	"""
	anyOccupationCreditKeywords: [String]
	"""
	Exclude titles that have any of the specified occupations attached.
	This is a string, but may change in the future as PACE evolves.
	"""
	excludeOccupationCreditKeywords: [String]
}

type OccupationValue {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

"""
The offer type of a watch option, indicating the commercial model.
"""
enum OfferType {
	FREE
	PURCHASE
	RENTAL
	SUBSCRIPTION
}

type OpeningWeekendBoxOfficeGrossConnection {
	"""
	Pages of opening weekend box office grosses
	"""
	edges: [OpeningWeekendBoxOfficeGrossEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

type OpeningWeekendBoxOfficeGrossEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	An opening weekend box office gross node
	"""
	node: OpeningWeekendGross!
	"""
	Position of the node in this context, eg: opening weekend box office gross 4 out of 31
	"""
	position: Int!
}

"""
Only opening weekend grosses for Box Office areas specified in the filter will be returned.
If the filter isn't provided, opening weekend grosses for all available Box Office areas will be returned.
If a specified Box Office area doesn't exist or no opening weekend grosses are available for it, nothing will be returned.
"""
input OpeningWeekendBoxOfficeGrossFilter {
	boxOfficeAreaCodes: [String]!
}

type OpeningWeekendGross {
	"""
	The BoxOfficeAreaType the gross is related to.
	"""
	boxOfficeAreaType: BoxOfficeAreaType!
	"""
	The opening weekend gross for the title within the area, between the start and end dates.
	"""
	gross: BoxOfficeGross!
	"""
	The theater count for the title within the area, between the start and end dates.
	"""
	theaterCount: Int
	"""
	The last day of the opening weekend for the title within the area.
	"""
	weekendEndDate: Date!
	"""
	The first day of the opening weekend for the title within the area.
	"""
	weekendStartDate: Date
}

"""
Country contribution guidelines:
https://help.imdb.com/article/contribution/titles/countries/GTSW4DN8H8LKCXER
"""
input OriginCountrySearchConstraint {
	"""
	Match titles that are at least partially from ALL of these countries
	Country code in ISO 3166 (IMDbLocaleConfig:region_codes.json)
	Example: To match titles that are at least partially from US and GB: ['US', 'GB']
	"""
	allCountries: [String]
	"""
	Match titles that are at least partially from AT LEAST ONE of these countries
	Country code in ISO 3166 (IMDbLocaleConfig:region_codes.json)
	Example: To match titles that are at least partially from US or GB: ['US', 'GB']
	"""
	anyCountries: [String]
	"""
	Match titles that are primarily from AT LEAST ONE of these countries
	Country code in ISO 3166 (IMDbLocaleConfig:region_codes.json)
	Example: To match titles that are from US or GB: ['US', 'GB']
	"""
	anyPrimaryCountries: [String]
	"""
	Exclude titles that are at least partially from AT LEAST ONE of these countries
	Country code in ISO 3166 (IMDbLocaleConfig:region_codes.json)
	Example: To match titles that are at least partially not from US or GB: ['US', 'GB']

	If the same input is specified in the any/all and exclude constraints,
	then the results should be empty.
	"""
	excludeCountries: [String]
	"""
	Exclude titles that are primarily from ONE of these countries
	Country code in ISO 3166 (IMDbLocaleConfig:region_codes.json)
	Example: To match titles that are not from US or GB: ['US', 'GB']

	If the same input is specified in the any/all and exclude constraints,
	then the results should be empty.
	"""
	excludePrimaryCountries: [String]
}

"""
The authenticated user's over-18 age attestation status.

Exposes whether the owner has self-declared being over 18.
"""
type Over18Status {
	"""
	True only when the owner has self-declared being over 18.
	False means the owner has not self-declared yet — it is NOT a determination of being under 18.
	"""
	isSelfDeclared: Boolean!
}

input OverrideLiveEventInput {
	"""
	Id for award associated with the specified event. Ex: aw0000016 for 'Oscar'
	"""
	awardId: ID!
	"""
	Event edition id. Ex: ee0131659 for Oscars 2022
	"""
	eventEditionId: ID!
}

type PageInfo {
	"""
	When paginating forwards, the cursor to continue
	"""
	endCursor: ID
	hasNextPage: Boolean!
	hasPreviousPage: Boolean!
	"""
	When paginating backwards, the cursor to continue
	"""
	startCursor: ID
}

type PaginatedTitles {
	paginationToken: String
	titles: [Title]!
}

type PaginatedVideos {
	"""
	The pagination token. Use this value as the token parameter on the
	next call to get the next page of results
	"""
	paginationToken: String
	"""
	List of Video Entities returned by a query
	"""
	videos: [Video]!
}

"""
Parents guides for a given title
"""
type ParentsGuide {
	categories(filter: ParentsGuideCategoryFilter): [ParentsGuideCategorySummary]
	guideItems(
		after: ID
		before: ID
		filter: ParentsGuideFilter
		first: Int
		jumpTo: ID
		last: Int
	): ParentsGuideConnection
}

"""
Parents guide category details
"""
type ParentsGuideCategory {
	"""
	Parents guide category ID
	"""
	id: ID!
	"""
	The language the text is written in
	"""
	language: DisplayableLanguage!
	"""
	The displayable text type of the parents guide category, e.g. 'Violence & Gore'
	"""
	text: String!
}

input ParentsGuideCategoryFilter {
	spoilers: FilterSpoilers
}

"""
Parents guide severity summary
"""
type ParentsGuideCategorySummary {
	"""
	Category for the guide, e.g. 'Violence & Gore'
	"""
	category: ParentsGuideCategory!
	"""
	Parents guides for a given category
	"""
	guideItems(first: Int): ParentsGuideConnection
	"""
	Displayable severity of this category
	"""
	severity: SeverityLevel
	"""
	A breakdown of the severity levels for this category
	"""
	severityBreakdown: [SeverityLevel]
	"""
	Total number of users who voted on the severity levels in this category
	"""
	totalSeverityVotes: Int!
}

"""
Pagination for parents guide items
"""
type ParentsGuideConnection {
	"""
	Pages of parents guides
	"""
	edges: [ParentsGuideEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of parents guide items
"""
type ParentsGuideEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A parents guide item
	"""
	node: ParentsGuideItem!
	"""
	Position of the node in this context, eg: Parents Guide 4 out of 312
	"""
	position: Int!
}

input ParentsGuideFilter {
	categories: [ID]
	spoilers: FilterSpoilers
}

type ParentsGuideItem {
	"""
	The category of parents guide, e.g. 'violenceGuide'

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	category: ParentsGuideCategory!
	"""
	Returns the parental guide correction link.

	---------------------
	Graphlet Details:
	- Name: kahlo
	- CTI: IMDb/GraphQL/Kahlo
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-contributors-tech
	"""
	correctionLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
	): ContributionLink
	id: ID!
	"""
	Is this guide a spoiler

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	isSpoiler: Boolean!
	"""
	Machine translated parents guide text. Returns the text field translated based on the language header.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	machineTranslatedText: MachineTranslatedMarkdown!
	"""
	Returns the parental guide reporting link.

	---------------------
	Graphlet Details:
	- Name: kahlo
	- CTI: IMDb/GraphQL/Kahlo
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-contributors-tech
	"""
	reportingLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
	): ContributionLink
	"""
	The parents guide text, including markdown if present

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	text: Markdown!
	"""
	The title related to the parental guide

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	title: Title
}

input PerformanceSkillsConstraint {
	"""
	A name match must have ALL of these performance skills.
	No filtering is applied if omitted.
	"""
	allPerformanceSkills: [PerformanceSkillType]
	"""
	A name match must have AT LEAST ONE of these performance skills.
	No filtering is applied if omitted.
	"""
	anyPerformanceSkills: [PerformanceSkillType]
	"""
	A name match must not have any of these performance skills.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludePerformanceSkills: [PerformanceSkillType]
}

"""
Performance skills for talent
"""
enum PerformanceSkillType {
	COMEDIAN
	CONTORTIONIST
	DANCING
	DISC_JOCKEY
	DIVING
	EAR_PROMPTER
	FIREARMS
	HOST
	IMPRESSIONIST
	IMPROVISATION
	JUGGLING
	MAGIC
	MARTIAL_ARTS
	MEDICAL_EQUIPMENT
	MIME
	MOTORCYCLIST
	PILOT
	PRECISION_DRIVER
	SINGING
	STAGE_COMBAT
	STUNTS
	TELEPROMPTER
	VENTRILOQUIST
	VOICEOVER
	WHISTLING
}

type PersonalDetailsOutput {
	"""
	Current country or region of user's residence. ID in ISO 3166 format.
	"""
	countryOfResidence: ID
	"""
	Currently set gender of user.
	"""
	gender: String
}

type PersonalEmployment {
	branch: CompanyBranch
	"""
	Persistent catalog identifier linking this employment to its source entry
	in the catalog, if any.

	Each PersonalEmployment corresponds to one catalog entry; contribution
	workflows can use this identifier to load and modify that exact entry.

	Returns null when no catalog identifier is available for this employment.
	"""
	catalogId: ID
	company: Company!
	employeeContact: CompanyContactDetails
	id: ID!
	jobTitle: LocalizedString
	name: Name!
	occupation: OccupationValue
}

type PersonalizedSuggestedVideosConnection {
	edges: [PersonalizedSuggestedVideosEdge]!
	pageInfo: PageInfo!
}

type PersonalizedSuggestedVideosEdge {
	cursor: ID!
	node: Video!
	position: Int!
}

input PhysiquesConstraint {
	"""
	A name match must have ALL of these physiques.
	No filtering is applied if omitted.
	"""
	allPhysiques: [PhysiqueType]
	"""
	A name match must have AT LEAST ONE of these physiques.
	No filtering is applied if omitted.
	"""
	anyPhysiques: [PhysiqueType]
	"""
	A name match must not have any of these physiques.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludePhysiques: [PhysiqueType]
}

"""
Physical build types for talent
"""
enum PhysiqueType {
	"""
	Athletic or muscular body build
	"""
	ATHLETIC
	"""
	Average or medium body build
	"""
	AVERAGE
	"""
	Heavyset or large body build
	"""
	HEAVYSET
	"""
	Slim or lean body build
	"""
	SLIM
}

"""
Provides context for the location of the list widget on client
"""
input PlacementContext {
	"""
	The page type the query was made from e.g. home, title
	"""
	pageType: String
}

"""
Key-value pair for PLAID overrides, formatted as a PLAID CSS property and its value
(e.g. { key: "baseAltShade1", value: "#0f0f0f" })
"""
type PlaidOverride {
	"""
	The key for the PLAID override
	"""
	key: String!
	"""
	The value for the PLAID override
	"""
	value: String!
}

"""
The client platforms that watch options are available on
"""
enum PlatformId {
	ANDROID
	ANDROID_FIRE
	FIRE_TV_DETAIL
	IOS
	MOBILE_WEB
	WEB
}

"""
Link format available for watch options
"""
enum PlatformLinkFormatId {
	ANDROID
	ANDROID_FIRE
	FIRE_TV_DETAIL
	IOS
	MDOT
	WEB
}

"""
Represents a single URL that can be used for video playback. Contains the mime type and
definition of content behind the URL.
"""
type PlaybackURL {
	"""
	A description of playback experience suitable for displaying to the customer to
	set expectations or be leveraged in via in player URL selection.
	Examples include short qualitative descriptions like 780p, SD, or Ultra HD
	"""
	displayName: LocalizedString!
	"""
	Metric dimensions about the playback URL. Maximum of 5 metric dimensions.
	"""
	metricDimensions: [MetricDimension]
	"""
	The URL that can be given to the player to view this videos content. It may or may
	not be valid for a limited time and thus is intended for immediate use by the caller.
	"""
	url: URL!
	"""
	Describes the resolution of the content behind the URL.
	"""
	videoDefinition: VideoDefinition!
	"""
	Mime type in the enum format that describes the content behind the URL. Suitable for
	supplying to the browser or video player.
	"""
	videoMimeType: VideoMimeType!
}

type Plot {
	"""
	The name of the plot's author. This field is not required as it can be null for an anonymous author.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	author: String
	"""
	Returns the plot correction link or null if a non-english plot or a plot synopsis.

	---------------------
	Graphlet Details:
	- Name: kahlo
	- CTI: IMDb/GraphQL/Kahlo
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-contributors-tech
	"""
	correctionLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
	): ContributionLink
	id: ID!
	"""
	Whether this plot contains spoilers

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	isSpoiler: Boolean
	"""
	The language of the plot text

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	language: DisplayableLanguage
	"""
	Machine translated plot text. Returns the plotText field translated based on
	the language header. If the text is already localized in the requested
	language, returns it as-is with isMachineTranslation set to false.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	machineTranslatedText: MachineTranslatedMarkdown
	"""
	The plot text

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	plotText: Markdown
	"""
	The type of the plot

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	plotType: PlotType
	"""
	Returns the plot reporting link or null if a non-english plot or a plot synopsis.

	---------------------
	Graphlet Details:
	- Name: kahlo
	- CTI: IMDb/GraphQL/Kahlo
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-contributors-tech
	"""
	reportingLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
	): ContributionLink
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	restriction: PlotRestriction
	"""
	Title related to the plot

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	title: Title
}

"""
Pagination for plot types which always has a known total number of edges.
"""
type PlotConnection {
	"""
	Pages of plots
	"""
	edges: [PlotEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	restriction: PlotRestriction
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of plots
"""
type PlotEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A plot node
	"""
	node: Plot!
	"""
	Position of the node in this context, eg: Plot 4 out of 31
	"""
	position: Int!
}

"""
Example: if you want to match titles of plot summary/outline 'Mushroom-Kingdom' OR 'Mysterious', PLUS
the title must contain 'bloodthirsty', PLUS the title had the plot written by 'huggo' OR 'guy',
specify anyPlotTextTerms: ['mushroom-kingdom', 'mysterious'] plus
allPlotTextTerms: ['bloodthirsty'] plus plotAuthor: ['huggo', 'guy']
"""
input PlotMatchingSearchConstraint {
	"""
	A title match must contain ALL of these search terms
	"""
	allPlotTextTerms: [String]
	"""
	A title match must contain a plot written by AT LEAST ONE of these authors
	"""
	anyPlotAuthors: [String]
	"""
	A title match must contain AT LEAST ONE of these search terms
	"""
	anyPlotTextTerms: [String]
}

"""
Information about restrictions applied to plot
"""
type PlotRestriction {
	"""
	Explanation(s) for the restriction
	"""
	explanations: [RestrictionExplanation]!
	reasons: [ContentRestrictionReason]!
	"""
	DEPRECATED: Renamed, use `reasons` instead.
	"""
	restrictionReason: [ContentRestrictionReason]!
}

"""
Our legacy plot types.
Here to support existing features that requires knowing this to power business logic.
"""
enum PlotType {
	OUTLINE
	SUMMARY
	SYNOPSIS
}

type Poll {
	"""
	Answers for the poll, by default it returns the list in a consistent order defined by the author.
	"""
	answers(
		first: Int
		sort: PollAnswerSort = { by: ANSWER_INDEX, order: ASC }
	): PollAnswersConnection!
	"""
	Author of this poll.
	"""
	author: UserProfile!
	"""
	The date when the poll was created.
	"""
	createDate: DateTime
	"""
	The id of the list that the poll is created from.
	Required for legacy pollId to listId redirection.
	"""
	createdFromListId: ID!
	"""
	Vote from the current logged in customer (if x-imdb-customer-id header is present).
	"""
	currentCustomerVote: PollVote
	"""
	Description of the poll.
	"""
	description: Markdown
	id: ID!
	"""
	Whether the poll is active, set by poll administrators.
	Inactive polls are only included in single 'poll' query, not in multiple-poll queries such as 'polls',
	'userVotedPolls', or related polls. Returns null if isActive status is not available.
	"""
	isActive: Boolean
	"""
	Whether the poll is closed for voting.
	Closed polls and its results can be viewed but not voted on.
	"""
	isClosed: Boolean!
	"""
	Primary image for poll, nullable response
	"""
	primaryImage: PollPrimaryImage
	"""
	Poll question, E.g., Favorite 'TITLE' Character.
	"""
	question: PollQuestion!
	"""
	Returns most recent votes on this poll, from latest to earliest.
	"""
	recentVotes(after: ID, first: Int!): PollVoteConnection
	"""
	Returns the list of related polls to this poll in random order.
	"""
	relatedPolls(after: ID, first: Int!): PollsConnection
	"""
	Total vote count on the poll across all answers.
	"""
	totalVotes: Int!
	"""
	Entity Type of the poll answer item (Title/Name/Image).
	"""
	type: PollAnswerItemType!
}

enum PollAdminAction {
	"""
	Admin created a new poll from a list.
	"""
	CREATE
	"""
	Admin synced an existing poll with the list.
	"""
	SYNC
}

"""
Entity type to represent a poll admin activity on the poll.
"""
type PollAdminActivity {
	"""
	The actual action made on the poll (create/sync).
	"""
	action: PollAdminAction!
	"""
	Time when the action was made on the poll.
	"""
	actionTime: DateTime!
	"""
	Admin User who made the action on the poll.
	"""
	admin: UserProfile!
	"""
	The poll on which the action was made.
	"""
	poll: Poll!
}

type PollAdminActivityConnection {
	edges: [PollAdminActivityEdge]!
	pageInfo: PageInfo!
	total: Int
}

type PollAdminActivityEdge {
	cursor: ID!
	node: PollAdminActivity!
}

type PollAnswer {
	"""
	Integer index of this answer in the poll. Defined by the poll author.
	Answer indices are not subject to ever change.
	"""
	answerIndex: Int!
	"""
	Optional description of the answer.
	"""
	description: Markdown
	item: AnswerItem!
	"""
	Percentage of total votes on this answer.
	"""
	votePercentage: Float!
	"""
	Total number of votes for this answer.
	"""
	votes: Int!
}

type PollAnswerEdge {
	cursor: ID!
	node: PollAnswer!
}

enum PollAnswerItemType {
	IMAGE
	NAME
	TITLE
}

type PollAnswersConnection {
	edges: [PollAnswerEdge]!
	pageInfo: PageInfo!
	total: Int!
}

"""
Sort input for poll answers
"""
input PollAnswerSort {
	by: PollAnswerSortBy = ANSWER_INDEX
	order: SortOrder
}

"""
Enum for poll answer sort options
"""
enum PollAnswerSortBy {
	"""
	Sort answers by author defined index value.
	ASC: From the least (1) to the greatest number.
	"""
	ANSWER_INDEX
	"""
	Sort answers based on their vote count.
	DESC: From greatest number to the least number. For same vote count, least ANSWER_INDEX first.
	"""
	VOTE_COUNT
}

type PollEdge {
	cursor: ID!
	node: Poll!
}

"""
Poll filter options.
"""
input PollFilter {
	"""
	False by default, if true, closed polls will not be returned.
	"""
	excludeClosed: Boolean
	"""
	False by default, if true, polls voted by the logged in customer will not be returned. (Ignored if x-imdb-customer-id header is not present.)
	"""
	excludeVoted: Boolean
}

type PollPrimaryImage {
	image: Image!
}

type PollQuestion {
	originalText: Markdown!
}

"""
Total will vend total polls returned instead of the absolute total number of polls.
PageInfo fields are accurate and will provide information on subsequent pagination requests.
"""
type PollsConnection {
	edges: [PollEdge]!
	pageInfo: PageInfo!
	total: Int!
}

"""
Sort input for polls
"""
input PollSort {
	by: PollSortBy = CREATE_TIME
	order: SortOrder
}

"""
Enum for poll sort options
"""
enum PollSortBy {
	"""
	Sort polls based on their create time.
	DESC: From latest to earliest.
	"""
	CREATE_TIME
}

"""
Entity type to represent a vote on the poll.
"""
type PollVote {
	"""
	The answer item of vote.
	"""
	answer: AnswerItem!
	"""
	Integer index of this answer in the poll.
	"""
	answerIndex: Int!
	"""
	The poll on which the vote was made.
	"""
	poll: Poll!
	"""
	User who voted on the poll.
	"""
	user: UserProfile!
	"""
	Time when the vote was made on the poll.
	"""
	voteTime: DateTime!
}

type PollVoteConnection {
	edges: [PollVoteEdge]!
	pageInfo: PageInfo!
	total: Int
}

type PollVoteEdge {
	cursor: ID!
	node: PollVote!
}

input PopularTitlesQueryFilter {
	"""
	Filter out current user's ratings
	"""
	filterOutUserRatings: Boolean
	"""
	Inclusive date range to filter on
	"""
	releaseDateRange: DateRange
}

"""
The location of the client by postal code. The country field is an ISO 3166-1 alpha-2 country code (ex. US or GB).
The postalCode field is alpha numeric (ex. 98109 or D-11179).
"""
input PostalCodeLocation {
	country: String!
	postalCode: String!
}

input PredefinedListInput {
	"""
	The Class Id associated with the predefined list.
	For example: classId 'CHECK_INS', or classId 'WATCH_LIST'
	"""
	classId: ListClassId!
}

type PrestigiousAwardSummary {
	awardNomination: AwardNomination!
	nominations: Int!
	wins: Int!
}

"""
Pricing details for a product, derived from ProductV2 buying options.
"""
type PriceDetail {
	"""
	ISO 4217 currency code (e.g., 'USD', 'GBP'). Returns null if unavailable.
	"""
	currencyCode: String
	"""
	ISO 8601 date/time string indicating when the deal expires. Returns null if no deal is active.
	"""
	dealEndDate: DateTime
	"""
	The current deal or sale price. Returns null if no deal is active.
	"""
	dealPrice: Float
	"""
	Whether the price is suppressed due to a Minimum Advertised Price (MAP) exception.
	When true, the actual price will be absent from the PriceDetail field.
	"""
	hasMapException: Boolean!
	"""
	Percentage discount off the standard price. Returns null if no savings information is available.
	"""
	percentageOff: Int
	"""
	Display string for Prime Video pricing (e.g., 'Free with ads', subscription label).
	Returns null if not a Prime Video product.
	"""
	primeVideoPricingString: LocalizedString
	"""
	Prime Video episode purchase price. Returns null if not a Prime Video purchase product.
	"""
	primeVideoPurchasePrice: Float
	"""
	Prime Video rental price. Returns null if not a Prime Video rental product.
	"""
	primeVideoRentalPrice: Float
	"""
	Prime Video season purchase price. Returns null if not a Prime Video season product.
	"""
	primeVideoSeasonPurchasePrice: Float
	"""
	Absolute savings amount off the standard price, in the currency indicated by currencyCode.
	Returns null if no savings information is available.
	"""
	savingsAmount: Float
	"""
	The list price before any deals or discounts. Returns null if unavailable.
	"""
	standardPrice: Float
}

type PrimaryWatchOption {
	"""
	Number provided when there are multiple watch options
	"""
	additionalWatchOptionsCount: Int
	watchOption: WatchOption!
}

input PrincipalCreditsFilter {
	"""
	A list of job category ids. User can input 'cast' to filter for 'actor', 'actress' or 'self'
	"""
	categories: [ID]
}

type PrincipalCreditsForCategory {
	"""
	Category (e.g. 'Producer' or 'Actor')
	"""
	category: CreditCategory!
	credits(limit: Int): [Credit]!
	"""
	Restriction related information if exists for the category
	"""
	restriction: CreditRestriction
	"""
	Total number of (principal and non-principal) credits in this category including uncredited credits
	"""
	totalCredits: Int!
}

type PrincipalCreditsForGrouping {
	"""
	Returns credits within this grouping.

	The API is optimized for retrieving principal credits with max-limit 30 (default 10 credits per grouping).
	For complete credit lists, please use the title.creditGroupings API.

	Handles both TV series and standard credits with specialized logic. For episode based titles like
	TV series, podcast etc. episode credits are consolidated to series level while preserving episode counts
	and consistent role details. Multiple character appearances are unified into single
	credit entries, with characters ordered by frequency and first appearance. Includes
	gap detection for optimal credit sequence display.

	See TitleCreditsV2Sort for how credits are ordered.
	"""
	credits(limit: Int): [CreditV2]!
	"""
	Credit Grouping details.
	Credit groupings are ordered according to a predefined sequence.
	Groupings not in the predefined list are sorted after all listed groupings.
	"""
	grouping(groupingTextInput: GroupingTextInput): CreditGrouping!
	"""
	Total number of (principal and non-principal) credits in this grouping
	"""
	totalCredits: Int!
}

input PrincipalCreditsV2Filter {
	"""
	Whether to include all credits, only credited, or only uncredited.
	Default is to include all.
	"""
	credited: CreditedOrUncreditedFilter
	"""
	Should the response include Appearances (credits with CAST_TRAIT or SELF_TRAIT trait). Default is to include them (true).
	"""
	includeAppearances: Boolean
	"""
	Denotes what kind of principal credits to fetch. Supported modes are:

	TOP_CAST: Only returns on-screen appearances (credits with CAST_TRAIT trait or SELF_TRAIT trait in a single grouping).

	DEFAULT: Provide Directors, Writers (includes Head writers within writer
	grouping) and Appearances for all title types, except TV where we provide
	Creators and Appearances. Producers are always excluded (deterministic behavior).

	EXTENDED: Provide an extended set of credit categories beyond the DEFAULT mode
	like showrunner, cinematographer etc. Head writers are vended in their own
	grouping seprate from writers, removing those credits from the writers grouping.

	NARROWED: Provide up to two categories from DEFAULT; suitable for smaller display surfaces.

	TITLE_REFERENCE: Provide Stars and Creators for series titles, and only Stars
	for non-series titles. Designed for title reference pages.
	"""
	mode: String
}

"""
A printed format, along with any attributes. For example, the BluRay version is 1080p.
"""
type PrintedFormat {
	attributes: [DisplayableAttribute]!
	"""
	Printed format as displayable property
	"""
	displayableProperty: DisplayableTechnicalSpecificationProperty!
	"""
	The printed format
	"""
	printedFormat: String!
}

"""
Printed formats for this title.
"""
type PrintedFormats {
	"""
	The list of printed format items
	"""
	items: [PrintedFormat]!
	restriction: TechnicalSpecificationsRestriction
	"""
	Total number of printed format items
	"""
	total: Int!
}

type PrivacyDirectives {
	"""
	Whether this user is a CHILD/TEEN/ADULT.
	Uses string for versioning purposes. See AgeRange enum.
	If null, there is no applicable age signal for this user.
	"""
	ageSignal: String
	"""
	Encoded consent token using amazon vendor list.
	"""
	avlTcfString: String
	"""
	Encoded token for data sharing and cross-use.
	"""
	crossUseString: String
	"""
	base64 encoded json representation of the privacy directives
	e.g. eyJkZWNpc2lvbiI6ICJBQ0NFUFRfQUxMIiwicHVycG9zZXMiOiBbMSwgMiwgM10sInZlbmRvcnMiOiBbMywgNSwgN119
	     Decoded: {"gvlTcfString":"ABCD","avlTcfString":"EFGH",etc}
	"""
	directivesCookie: String!
	"""
	Whether or not to disable Cross Context Behavioral Advertising for this user.
	If null, then CCBA disablement is not applicable in this country/region.
	"""
	disableCCBA: Boolean
	"""
	Whether or not to disable Interest Based Advertising for this user.
	If null, then IBA disablement is not applicable in this country/region.
	"""
	disableIBA: Boolean
	"""
	Whether or not to disable Personalization for this user.
	If null, then Personalization disablement is not applicable in this country/region.
	"""
	disableP13n: Boolean
	"""
	The time after which the privacy directives need to be refreshed.
	"""
	expirationDate: DateTime!
	"""
	Encoded IAB TCFv2 (Transparency and Consent Framework) token. Uses IAB global vendor list.
	"""
	gvlTcfString: String
	"""
	Granular purpose-level directives from customized privacy preferences
	If null, do not suppress any purposes
	"""
	purposes: [GranularDirective]
	"""
	Granular vendor-level directives from customized privacy preferences
	If null, do not suppress any vendors
	"""
	vendors: [GranularDirective]
}

type PrivacyDirectivesOutput {
	"""
	Contains the privacy directives that must be enforced for the user
	May be empty, in which case there are no privacy directives to enforce.
	May also return null if privacy enforcement is not launched
	"""
	directives: PrivacyDirectives
}

type PrivacyPrompt {
	"""
	Localized text for the accept button. Button sets PrivacyPromptAction in `respondToPrivacyPrompt` mutation
	"""
	acceptButtonLabel: PrivacyPromptText!
	"""
	Localized text for the customize button. If is null do not display button
	"""
	customizeButtonLabel: PrivacyPromptText
	"""
	A url from which the user can customize their associated privacy preferences.
	"""
	customizeUrl: URL!
	"""
	Unique identifier for the privacy prompt and its associated terms
	"""
	id: ID!
	"""
	Gives the localized markdown for this privacy prompt.
	"""
	promptMarkdown: LocalizedMarkdown!
	"""
	Localized text for the reject button. Button sets PrivacyPromptAction in `respondToPrivacyPrompt` mutation
	"""
	rejectButtonLabel: PrivacyPromptText!
	"""
	Whether or not the prompt should be shown on page load.
	"""
	showPromptOnPageLoad: Boolean!
}

input PrivacyPromptInput {
	"""
	The type of prompt we want to display
	"""
	type: PrivacyPromptType!
}

type PrivacyPromptOutput {
	"""
	This is the time at which we should
	re-evaluate whether or not we should prompt the user
	Clients should consider this response to be stale
	after this time as an upper limit.
	"""
	expirationDate: DateTime!
	"""
	The consent requirement or empty if the requested consent type is not required
	for the given user
	"""
	privacyPrompt: PrivacyPrompt
}

type PrivacyPromptText {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

enum PrivacyPromptType {
	"""
	The primary consent prompt experience
	"""
	CONSENT_PRIMARY
}

"""
A process used for the film, along with any attributes. For example, CinemaScope may have been used for the IMAX and
IMAX3D versions.
"""
type Process {
	attributes: [DisplayableAttribute]!
	"""
	Process as displayable property
	"""
	displayableProperty: DisplayableTechnicalSpecificationProperty!
	"""
	The process
	"""
	process: String!
}

"""
Processes for this title.
"""
type Processes {
	"""
	The list of process items
	"""
	items: [Process]!
	restriction: TechnicalSpecificationsRestriction
	"""
	Total number of process items
	"""
	total: Int!
}

"""
Official announcements related to a title
"""
type ProductionAnnouncement {
	comment: ProductionAnnouncementComment
	date: Date!
}

"""
Additional information relating to a title's announcement (e.g. a press release link)
"""
type ProductionAnnouncementComment {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

type ProductionBudget {
	budget: Money!
}

type ProductionDate {
	"""
	Additional attributes (e.g. 'live broadcast')
	"""
	attributes: [DisplayableAttribute]
	"""
	Production end date
	"""
	endDate: DisplayableDate
	"""
	Production start date
	"""
	startDate: DisplayableDate
}

"""
Pagination for ProductionDates types which always has a known total number of edges.
"""
type ProductionDatesConnection {
	"""
	Pages of ProductionDates
	"""
	edges: [ProductionDatesEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

type ProductionDatesEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A ProductionDate node
	"""
	node: ProductionDate!
	"""
	Position of the node in this context, eg: ProductionDates 4 out of 31
	"""
	position: Int!
}

type ProductionStage {
	id: ID!
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	language: DisplayableLanguage!
	"""
	Production stage text, e.g. 'In Development', 'Pre-Production', 'Released', etc.
	"""
	text: String!
}

enum ProductionStageFilter {
	ABANDONED
	COMPLETED
	IN_DEVELOPMENT
	IN_PRODUCTION
	POST_PRODUCTION
	PRE_PRODUCTION
	RELEASED
}

"""
Production stage for titles
Enum values pulled from https://code.amazon.com/packages/IMDbBaseTypes/blobs/1.5/--/src/imdb/basetypes/v1/TitleProductionStatusBase.java
"""
enum ProductionStageType {
	"""
	Title production has been abandoned or cancelled
	"""
	ABANDONED
	"""
	Title production is completed but not yet released
	"""
	COMPLETED
	"""
	Title is currently in the development phase
	"""
	IN_DEVELOPMENT
	"""
	Title is actively being produced or filmed
	"""
	IN_PRODUCTION
	"""
	Title is in the post-production phase (editing, effects, etc.)
	"""
	POST_PRODUCTION
	"""
	Title is in pre-production planning
	"""
	PRE_PRODUCTION
	"""
	Title has been publicly released
	"""
	RELEASED
}

type ProductionStatus {
	id: ID!
	language: DisplayableLanguage!
	"""
	Production status text, e.g. 'Filming', 'Development Unknown', 'Abandoned', etc.
	"""
	text: String!
}

type ProductionStatusDetails {
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	announcements: [ProductionAnnouncement]
	currentProductionStage: ProductionStage!
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	productionStatusHistory: [ProductionStatusHistory]
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	restriction: ProductionStatusHistoryRestriction
}

type ProductionStatusHistory {
	"""
	An optional user-displayable string providing additional information or
	context about the status (e.g., 'According to a June casting call, filming
	will commence in November 2006.')
	"""
	comment: ProductionStatusHistoryComment
	"""
	The date when the production status was updated
	"""
	date: Date!
	"""
	Production stage corresponding to the status
	"""
	stage: ProductionStage!
	"""
	Production status at a given point in time (excludes 'announced' statuses)
	"""
	status: ProductionStatus!
}

type ProductionStatusHistoryComment {
	"""
	An opaque unique identifier for the displayable comment
	"""
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	"""
	The comment display text
	"""
	text: String!
}

"""
Information about restrictions applied to production status
"""
type ProductionStatusHistoryRestriction {
	"""
	Explanation(s) for the restriction
	"""
	explanations: [RestrictionExplanation]!
	reasons: [ContentRestrictionReason]!
	"""
	DEPRECATED: Renamed, use `reasons` instead.
	"""
	restrictionReason: [ContentRestrictionReason]!
	"""
	The total number of items if there was no restriction
	"""
	unrestrictedTotal: Int
}

"""
Match titles based on when their production status was last updated.
Example: statusUpdateDays: 30 matches titles with production status updated in the last 30 days
"""
input ProductionStatusUpdateDaysConstraint {
	"""
	Match titles where production status was updated within the specified number of days.
	Must be a positive integer. No filtering is applied if omitted or null.

	Throws GRAPHQL_VALIDATION_FAILED if value is negative or zero.
	"""
	statusUpdateDays: Int
}

"""
Each NameProfession belongs to a ProfessionCategory.
For example, the Makeup Artist profession belongs to the “Makeup Department” ProfessionCategory.

All fields are marked as nullable in case of invalid graphlet cross-link - in practice these should never be null.
"""
type ProfessionCategory {
	"""
	The profession category ID e.g. amzn1.imdb.professionCategory.1.2.3
	"""
	id: ID!
	"""
	Details of the credit category mapped to profession category

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	linkedCreditCategory: CreditCategory
	"""
	The order to display this profession category when displaying a list of profession categories

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	listOrder: Int
	"""
	Details of the Profession Category

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	text: DisplayableProfessionCategory
	"""
	The unordered set of traits associated with this profession category.
	Traits are intended to be used as functional categorization markers for applying business logic.

	Current possible values:
	- `WORK_TYPE_CREDIT_TRAIT`. Profession categories that are typically credited on titles.
	- `WORK_TYPE_EMPLOYMENT_TRAIT`. Profession categories that are linked to employment roles (e.g. Executives).
	- `WORK_TYPE_REPRESENTATION_TRAIT`. Profession categories where the employed name represents clients (e.g. Talent Agents).
	- `UNDERGOING_TESTING_TRAIT`. Identifies categories that are still in testing
	phase and should be hidden from public-facing interfaces such as search
	filters, dropdowns, and selection controls. The presence of this trait
	indicates the category exists in the system but is not yet ready for general
	availability in user experiences.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	traits: [String]
}

"""
Match titles based on profession categories attached to their credits.
"""
input ProfessionCategorySearchConstraint {
	"""
	Match titles that have any of the specified profession category IDs attached.
	See: imdb.id.amzn1.imdb.concept.ProfessionCategoryId
	Example: amzn1.imdb.concept.profession_category.00000000-0000-7000-0000-000000000000
	"""
	anyProfessionCategoryIDs: [ID]
	"""
	Exclude titles that have any of the specified profession category IDs attached.
	See: imdb.id.amzn1.imdb.concept.ProfessionCategoryId
	Example: amzn1.imdb.concept.profession_category.00000000-0000-7000-0000-000000000000
	"""
	excludeProfessionCategoryIDs: [ID]
}

"""
Maps a number to a profession
"""
type ProfessionCount {
	profession: NameProfession!
	totalCount: Int!
}

type ProfessionCountsSummary {
	"""
	Display text associated with the map of profession counts
	e.g. "2 talent agents and 1 producer."
	"""
	displayableCounts: LocalizedString!
	"""
	A list of professions mapped to the number of people (of that profession)
	that have viewed the  page
	"""
	professionCounts: [ProfessionCount]!
}

input ProfessionSearchOptions {
	"""
	Indicates whether this profession is user selectable.
	By default, non-user selectable professions will be included in the results.
	"""
	isCustomerSelectable: Boolean
}

enum ProfessionsFilter {
	EXCLUDE_PRIMARY_PROFESSIONS
	PRIMARY_PROFESSIONS_ONLY
}

type PromotedVideoAd {
	"""
	Ad feedback URL to be linked in the sponsored label of the ad
	"""
	adFeedbackUrl: URL
	"""
	The video id (viconst) associated with the Promoted Video Ad
	"""
	id: ID!
	"""
	Third Party trackers as defined by the advertising customer
	"""
	thirdPartyTrackers: ThirdPartyTrackers!
	"""
	The video object associated with the Promoted Video Ad
	Throws RESOURCE_NOT_FOUND if the associated video is not available
	"""
	video: Video!
}

"""
Required and optional parameters to request a Promoted Video Ad from AAX with context from the client
"""
input PromotedVideoAdParameters {
	"""
	Optional CreativeId if an ad is being forced by a Freedonia override
	"""
	adOverrideCreativeId: String
	"""
	Required and optional parameters used to form the Amazon Ad Exchange request from the context of the app client
	Throws GRAPHQL_VALIDATION_FAILED if adParametersApp is not available for Mobile App requests
	"""
	adParametersApp: AdParametersApp
}

"""
Enum representing prompt types
"""
enum PromptType {
	"""
	Ratings prompt on title main page
	"""
	RATINGS_TITLE_MAIN
	"""
	Ratings prompt on title trivia sub-page
	"""
	RATINGS_TITLE_TRIVIA
}

"""
A blocked user entry.
"""
type ProNetworkingBlockedUser {
	"""
	When the user was blocked (ISO-8601).
	"""
	blockedAt: DateTime!
	"""
	Number of mutual connections between the viewer and this blocked person.
	"""
	mutualConnectionCount: Int!
	"""
	The blocked person.
	"""
	name: Name!
	"""
	Number of titles shared between the viewer and this blocked person.
	"""
	sharedTitleCount: Int!
}

"""
An edge in a blocked users list.
"""
type ProNetworkingBlockedUserEdge {
	"""
	Opaque cursor for pagination.
	"""
	cursor: ID!
	"""
	The blocked user.
	"""
	node: ProNetworkingBlockedUser!
	"""
	Position of this item in the result set.
	"""
	position: Int!
}

"""
Paginated list of blocked users.
"""
type ProNetworkingBlockedUsersConnection {
	"""
	List of blocked user edges.
	"""
	edges: [ProNetworkingBlockedUserEdge]!
	"""
	Pagination metadata.
	"""
	pageInfo: PageInfo!
	"""
	Total number of blocked users.
	"""
	total: Int!
}

"""
An established connection (relationship) between the viewer and another professional.
"""
type ProNetworkingConnection {
	"""
	When the connection was established (ISO-8601).
	"""
	connectedAt: DateTime!
	"""
	Unique identifier for this connection.
	"""
	id: ID!
	"""
	Number of mutual connections shared between viewer and this person.
	"""
	mutualConnectionCount: Int!
	"""
	The connected professional.
	"""
	name: Name!
	"""
	Number of titles shared between viewer and this person.
	"""
	sharedTitleCount: Int!
}

"""
Detailed connection information between the viewer and a target name,
optionally in the context of a page owner's name page.
"""
type ProNetworkingConnectionDetail {
	"""
	Structured reason category if the viewer connected via a request.
	"""
	connectionReasonCategory: ProNetworkingConnectionReasonCategory
	"""
	Mutual connections between this person and the page owner, enriched with dual-context stats.
	Returned only when pageOwnerNameId was provided, differs from targetNameId, and the viewer is
	connected to the page owner or the target; otherwise null. This restricts the page-owner overlap
	to viewers with a legitimate connection to one of the two people.
	If first is omitted, defaults to 5.
	Basic tier: returns total count only with empty edges.
	"""
	mutualConnectionsWithPageOwner(
		after: ID
		first: Int
	): ProNetworkingMutualConnectionsConnection
	"""
	Mutual connections between the viewer and this person, enriched with dual-context stats.
	If first is omitted, defaults to 5.
	Basic tier: returns total count only with empty edges.
	"""
	mutualConnectionsWithViewer(
		after: ID
		first: Int
	): ProNetworkingMutualConnectionsConnection!
	"""
	The target person.
	Clients should query federation fields directly through the Name reference:
	- STARmeter: `name.meterRanking { currentRank }`
	- Known for titles: `name.knownForV2 { credits { title { ... } } }`
	"""
	name: Name!
	"""
	Titles shared between this person and the page owner.
	Returned only when pageOwnerNameId was provided, differs from targetNameId, and the viewer is
	connected to the page owner or the target; otherwise null. This restricts the page-owner overlap
	to viewers with a legitimate connection to one of the two people.
	If first is omitted, defaults to 5.
	"""
	sharedTitlesWithPageOwner(
		after: ID
		first: Int
	): ProNetworkingSharedTitlesConnection
	"""
	Titles shared between the viewer and this person.
	If first is omitted, defaults to 5.
	"""
	sharedTitlesWithViewer(
		after: ID
		first: Int
	): ProNetworkingSharedTitlesConnection!
}

"""
An edge in a connections list.
"""
type ProNetworkingConnectionEdge {
	"""
	Opaque cursor for pagination.
	"""
	cursor: ID!
	"""
	The connection object.
	"""
	node: ProNetworkingConnection!
	"""
	Position of this item in the result set.
	"""
	position: Int!
}

"""
The reason a requester selects when sending a connection request, explaining how they know
the recipient. Viewer-supplied at request time. Distinct from ProNetworkingRecommendationCategory,
which is system-computed to explain why a professional is recommended to the viewer.
"""
enum ProNetworkingConnectionReasonCategory {
	"""
	Met in person at an event (e.g., festival, screening, conference).
	"""
	IN_PERSON_EVENT
	"""
	Connected through one or more mutual connections.
	"""
	MUTUAL_CONNECTION
	"""
	A reason not covered by the other categories.
	"""
	OTHER
	"""
	Representation relationship in either direction — the viewer represents this person, or this person represents the viewer.
	"""
	REP_CLIENT
	"""
	Work at the same company.
	"""
	SAME_COMPANY
	"""
	Members of the same guild or professional organization.
	"""
	SAME_GUILD
	"""
	Attended the same school.
	"""
	SAME_SCHOOL
	"""
	Worked together on a title or project.
	"""
	WORKED_ON_PROJECT
}

"""
A pending connection request between two professionals.
"""
type ProNetworkingConnectionRequest {
	"""
	Structured reason category for the connection request.
	"""
	connectionReasonCategory: ProNetworkingConnectionReasonCategory
	"""
	When the request was created (ISO-8601).
	"""
	createdAt: DateTime!
	"""
	Unique identifier for this request.
	"""
	id: ID!
	"""
	Number of mutual connections between requester and recipient.
	"""
	mutualConnectionCount: Int!
	"""
	The person who received the request.
	"""
	recipient: Name!
	"""
	The person who sent the request.
	"""
	requester: Name!
	"""
	Number of shared titles between requester and recipient.
	"""
	sharedTitleCount: Int!
}

"""
An edge in a connection requests list.
"""
type ProNetworkingConnectionRequestEdge {
	"""
	Opaque cursor for pagination.
	"""
	cursor: ID!
	"""
	The connection request object.
	"""
	node: ProNetworkingConnectionRequest!
	"""
	Position of this item in the result set.
	"""
	position: Int!
}

"""
Paginated list of connection requests following the Relay Connection spec.
"""
type ProNetworkingConnectionRequestsConnection {
	"""
	List of request edges.
	"""
	edges: [ProNetworkingConnectionRequestEdge]!
	"""
	Pagination metadata.
	"""
	pageInfo: PageInfo!
	"""
	Total number of pending requests in the entire set.
	"""
	total: Int!
}

"""
Paginated list of connections following the Relay Connection spec.
"""
type ProNetworkingConnectionsConnection {
	"""
	List of connection edges.
	"""
	edges: [ProNetworkingConnectionEdge]!
	"""
	Pagination metadata.
	"""
	pageInfo: PageInfo!
	"""
	Total number of connections in the entire set.
	"""
	total: Int!
}

"""
Sort options for the viewer's connections list.
"""
enum ProNetworkingConnectionSortBy {
	"""
	Sort by connection date, most recent first.
	"""
	DATE_ADDED_DESC
	"""
	Sort by number of mutual connections, highest first.
	"""
	MUTUAL_CONNECTIONS_DESC
	"""
	Sort by relevance: (1) mutual connection count desc, (2) creditedWithNames score desc,
	(3) STARmeter rank asc. This is the default sort order per BRD.
	"""
	RELEVANCE_DESC
	"""
	Sort by number of shared titles, highest first.
	"""
	SHARED_TITLES_DESC
}

"""
Connection status between the authenticated viewer and a given name.
Returns default (all false) values if the viewer is not authenticated.
"""
type ProNetworkingConnectionStatus {
	"""
	Whether the viewer is allowed to send a connection request to this person.
	"""
	canSendRequest: Boolean!
	"""
	Whether this person has sent the viewer an incoming request.
	"""
	hasIncomingRequest: Boolean!
	"""
	Whether the viewer has blocked this person.
	"""
	isBlockedByViewer: Boolean!
	"""
	Whether the viewer is connected to this person.
	"""
	isConnected: Boolean!
	"""
	Whether the viewer has a pending outgoing request to this person.
	"""
	isPending: Boolean!
}

"""
A title credit this name has been endorsed for, with its endorsement count.
"""
type ProNetworkingCreditEndorsementCount {
	"""
	The number of endorsements tagging this credit.
	"""
	count: Int!
	"""
	The title credit this name has been endorsed for.
	"""
	title: Title!
}

"""
An edge in ProNetworkingCreditEndorsementCountsConnection — a single
credit-count node with its pagination cursor and position.
"""
type ProNetworkingCreditEndorsementCountEdge {
	"""
	Opaque cursor for pagination.
	"""
	cursor: ID!
	"""
	The credit-count entry at this position.
	"""
	node: ProNetworkingCreditEndorsementCount!
	"""
	Position of this item in the result set.
	"""
	position: Int!
}

"""
A paginated list of credits a name has been endorsed for, with per-credit counts (cursor pagination).
"""
type ProNetworkingCreditEndorsementCountsConnection {
	"""
	List of credit-count edges.
	"""
	edges: [ProNetworkingCreditEndorsementCountEdge]!
	"""
	Pagination metadata.
	"""
	pageInfo: PageInfo!
	"""
	Total number of distinct credits this name has been endorsed for.
	"""
	total: Int!
}

"""
Input filter for `Name.creditEndorsements`.
"""
input ProNetworkingCreditEndorsementsInput {
	"""
	The title credit whose associated endorsements to return.
	"""
	titleId: ID!
}

"""
A directional endorsement: the author endorses the subject (a Name page). Each
endorsement item is a single content version with one approvalStatus; a pending
edit is its own PENDING item, separate from the APPROVED one it revises.
"""
type ProNetworkingEndorsement {
	"""
	Owner-approval status which gates visibility. PENDING is a new endorsement or
	a pending edit awaiting the owner's approval, visible to only the author and
	the subject. APPROVED is a publicly visible endorsement.
	"""
	approvalStatus: ProNetworkingEndorsementApprovalStatus!
	"""
	The endorsing member's name.
	"""
	author: Name!
	"""
	The version ID of the currently published content for APPROVED endorsements,
	or the pending content for PENDING endorsements.
	"""
	contentVersionId: ID!
	"""
	When this endorsement's content was created. For an edited endorsement this is
	the creation time of the currently-surfaced content version, not the original.
	Null if the creation time is unavailable, in which case clients should omit
	the date rather than treating it as an error.
	"""
	createdAt: DateTime
	"""
	An opaque identifier for the catalog item backing this endorsement — the
	identifier a contribution that removes this endorsement has to target.

	Like `id`, this addresses the (author → subject) endorsement slot rather than
	one endorsement: the catalog item is reused in place if the endorsement is
	removed and that same author later endorses that same subject again, so a guid
	can come to point at different content. A consumer that persists a guid to
	issue a write later must therefore re-verify `contentVersionId` before acting
	on it — the guid survives a substitution but `contentVersionId` does not. Null
	on a read that did not resolve the endorsement from its catalog item.
	"""
	guid: ID
	"""
	An opaque concept identifier for the (author → subject) endorsement slot on
	this name — not a permanent handle on one endorsement.

	The identifier is reused if an endorsement is removed and that same author
	later endorses that same subject again. It therefore names whichever
	endorsement currently occupies that author-subject slot, not one endorsement
	that was written once. Callers that persist this id (deep links, share URLs)
	must expect it to later resolve to a different endorsement, or to nothing at
	all. A caller that has to notice when the endorsement behind a persisted id
	has changed can store `contentVersionId` alongside it: the id survives a
	substitution but `contentVersionId` does not, so a changed `contentVersionId`
	means the content is no longer the one the link was created for — either an
	edited endorsement or a substituted one.
	"""
	id: ID!
	"""
	Free-text endorsement note about the subject. Null if omitted.
	"""
	note: String
	"""
	The endorsed subject's name.
	"""
	subject: Name!
	"""
	The subject's title credits that the author would like to endorse.
	"""
	taggedCredits: [Title]!
	"""
	Professions the author is endorsing the subject for.
	"""
	taggedProfessions: [NameProfession]!
}

"""
Owner-approval state of an endorsement.
"""
enum ProNetworkingEndorsementApprovalStatus {
	"""
	Approved by the page owner and publicly displayed on Pro.
	"""
	APPROVED
	"""
	Awaiting the page owner's approval; visible only to the author and the subject.
	"""
	PENDING
}

"""
A single endorsement in a paginated list.
"""
type ProNetworkingEndorsementEdge {
	"""
	Opaque cursor for pagination.
	"""
	cursor: ID!
	"""
	The endorsement at this position.
	"""
	node: ProNetworkingEndorsement!
	"""
	Position of this item in the result set.
	"""
	position: Int!
}

"""
A paginated list of endorsements using cursor pagination (first/after).
"""
type ProNetworkingEndorsementsConnection {
	"""
	List of endorsement edges.
	"""
	edges: [ProNetworkingEndorsementEdge]!
	"""
	Pagination metadata.
	"""
	pageInfo: PageInfo!
	"""
	Total number of endorsements in the entire set.
	"""
	total: Int!
}

"""
Optional filters for `Name.endorsements`.
"""
input ProNetworkingEndorsementsFilter {
	"""
	Return only this name's APPROVED endorsements with these ids (up to 100) — the
	batch companion to `Name.endorsement`, for pairing a pending edit with its
	live version. When provided (even as an empty list), the viewer-pin and
	RELEVANCE ranking are not applied. When omitted, the full list is returned.
	"""
	endorsementIds: [ID]
}

"""
Sort input for endorsement list reads: a criterion plus a direction (SortOrder). Clients must handle
unknown `by` values gracefully as new values may be added.
"""
input ProNetworkingEndorsementSort {
	"""
	Field to sort by.
	"""
	by: ProNetworkingEndorsementSortBy!
	"""
	Sort direction.
	"""
	order: SortOrder!
}

"""
Criteria for sorting endorsement list reads.
"""
enum ProNetworkingEndorsementSortBy {
	"""
	Submission/approval date.
	ASC: oldest first. DESC: most recent first.
	"""
	DATE
	"""
	Relevance (STARmeter-weighted + recency-weighted).
	ASC: least relevant first. DESC: most relevant first.
	"""
	RELEVANCE
}

"""
Aggregate summary of the endorsements a name has received.
"""
type ProNetworkingEndorsementSummary {
	"""
	Full, cursor-paginated set of title credits this name has been endorsed for, each with its endorsement count.
	Ordered by count (highest first), then by titleId. `first` defaults to 5, capped at 100.
	"""
	endorsedCreditsConnection(
		after: ID
		first: Int
	): ProNetworkingCreditEndorsementCountsConnection!
	"""
	Full, cursor-paginated set of professions this name has been endorsed for, each with its endorsement count.
	Ordered by count (highest first), then by professionId. `first` defaults to 5, capped at 100.
	"""
	endorsedProfessionsConnection(
		after: ID
		first: Int
	): ProNetworkingProfessionEndorsementCountsConnection!
	"""
	Total number of distinct endorsers of this name.
	"""
	totalCount: Int!
}

"""
A mutual collaborator suggestion — a person who shares filmography with both the viewer and the page owner.
"""
type ProNetworkingMutualCollaboratorSuggestion {
	"""
	Connection status between the viewer and this person.
	All mutual collaborator suggestions are pre-filtered: not connected, not
	blocked, not pending, allowlisted, privacy-eligible.
	Therefore isConnected, isPending, isBlockedByViewer are always false. canSendRequest is always true
	(unless the candidate has an incoming request to the viewer, in which case hasIncomingRequest is true
	and canSendRequest is false since the appropriate action is Accept, not Connect).
	"""
	connectionStatus: ProNetworkingConnectionStatus!
	"""
	Number of mutual connections between this person and the page owner.
	"""
	mutualConnectionCountWithPageOwner: Int!
	"""
	Number of mutual connections between this person and the viewer.
	"""
	mutualConnectionCountWithViewer: Int!
	"""
	The recommended person.
	"""
	name: Name!
	"""
	Number of titles shared between this person and the page owner.
	"""
	sharedTitleCountWithPageOwner: Int!
	"""
	Number of titles shared between this person and the viewer.
	"""
	sharedTitleCountWithViewer: Int!
}

"""
An edge in a mutual collaborator suggestions list.
"""
type ProNetworkingMutualCollaboratorSuggestionEdge {
	"""
	Opaque cursor for pagination.
	"""
	cursor: ID!
	"""
	The mutual collaborator suggestion.
	"""
	node: ProNetworkingMutualCollaboratorSuggestion!
	"""
	Position of this item in the result set.
	"""
	position: Int!
}

"""
Paginated list of mutual collaborator suggestions.
"""
type ProNetworkingMutualCollaboratorSuggestionsConnection {
	"""
	List of mutual collaborator suggestion edges.
	"""
	edges: [ProNetworkingMutualCollaboratorSuggestionEdge]!
	"""
	Pagination metadata.
	"""
	pageInfo: PageInfo!
	"""
	Total number of mutual collaborator suggestions.
	"""
	total: Int!
}

"""
A mutual connection edge with dual-context stats (viewer and page owner).
@entitlement(PRO_ANY_TIER, PRO_PREMIUM)
"""
type ProNetworkingMutualConnectionEdge {
	"""
	Opaque cursor for pagination.
	"""
	cursor: ID!
	"""
	Number of mutual connections between this mutual connection and the page owner.
	"""
	mutualConnectionCountWithPageOwner: Int!
	"""
	Number of mutual connections between this mutual connection and the viewer.
	"""
	mutualConnectionCountWithViewer: Int!
	"""
	The mutual connection's Name.
	"""
	node: Name!
	"""
	Zero-based position in the full list.
	"""
	position: Int!
	"""
	Number of titles shared between this mutual connection and the page owner.
	"""
	sharedTitleCountWithPageOwner: Int!
	"""
	Number of titles shared between this mutual connection and the viewer.
	"""
	sharedTitleCountWithViewer: Int!
}

"""
A paginated list of mutual connections with dual-context stats.
@entitlement(PRO_ANY_TIER, PRO_PREMIUM)
"""
type ProNetworkingMutualConnectionsConnection {
	"""
	The list of mutual connection edges.
	"""
	edges: [ProNetworkingMutualConnectionEdge]!
	"""
	Pagination metadata.
	"""
	pageInfo: PageInfo!
	"""
	Total number of mutual connections.
	"""
	total: Int!
}

"""
The viewer's own connections tab: established connections and pending incoming requests.
"""
type ProNetworkingMyConnectionsTab {
	"""
	The viewer's established connections.
	Default ordering: by relevance — (1) mutual connection count desc, (2) creditedWithNames
	relevance score desc, (3) STARmeter rank asc (lower rank = more popular = higher priority).
	Up to 100 connections can be requested at a time. If first exceeds 100, only 100 are returned.
	If first is omitted, defaults to 5.
	If sortBy is omitted, results are sorted by RELEVANCE_DESC.
	Basic tier: returns up to 3 connections as preview with upsell. Premium: full list.
	"""
	connections(
		after: ID
		first: Int
		sortBy: ProNetworkingConnectionSortBy
	): ProNetworkingConnectionsConnection!
	"""
	Pending connection requests received by the viewer, ordered by the specified sort criteria.
	When sortBy is omitted, defaults to DATE_RECEIVED_DESC (most recent first).
	Up to 100 requests can be requested at a time. If first exceeds 100, only 100 are returned.
	If first is omitted, defaults to 5.
	"""
	pendingRequests(
		after: ID
		first: Int
		sortBy: ProNetworkingRequestSortBy
	): ProNetworkingConnectionRequestsConnection!
}

"""
Connections tab data when viewing another person's name page.
"""
type ProNetworkingNameConnectionsTab {
	"""
	Mutual collaborator suggestions — people who share filmography with both the viewer and the page owner.
	Ordered by mutual connection count (descending), then STARmeter rank (ascending).
	Excludes people already shown in the Mutual Connections section.
	Default first: 5. Max first: 25. If first exceeds 25, only 25 are returned.
	Premium users see full details. Basic tier users see count only with upsell.
	"""
	mutualCollaboratorSuggestions(
		after: ID
		first: Int
	): ProNetworkingMutualCollaboratorSuggestionsConnection
	"""
	Mutual connections between the viewer and this person.
	Ordered by connection creation date (most recent first).
	Returns null if the viewer is not authenticated or is viewing their own page.
	Up to 50 mutual connections can be requested at a time. If first exceeds 50, only 50 are returned.
	If first is omitted, defaults to 5.
	Basic tier: returns total count only with empty edges (upsell prompt). Premium: full details.
	"""
	mutualConnections(
		after: ID
		first: Int
	): ProNetworkingMutualConnectionsConnection
	"""
	Titles shared between the viewer and this person.
	Ordered by strongest mutual relevance (ProNetworkingService STRONGEST_MUTUAL algorithm).
	Up to 50 shared titles can be requested at a time. If first exceeds 50, only 50 are returned.
	If first is omitted, defaults to 5.
	"""
	sharedTitles(after: ID, first: Int): ProNetworkingSharedTitlesConnection
}

"""
Viewer's connection privacy settings controlling who can send them requests.
"""
type ProNetworkingPrivacySettings {
	"""
	Allow requests from anyone regardless of relationship.
	"""
	allowFromAnyone: Boolean!
	"""
	Allow requests from people with mutual connections.
	"""
	allowFromMutualConnections: Boolean!
	"""
	Allow requests from the viewer's representative or client.
	"""
	allowFromRepClient: Boolean!
	"""
	Allow requests from people in the same affiliation (guild, union).
	"""
	allowFromSameAffiliation: Boolean!
	"""
	Allow requests from people at the same company.
	"""
	allowFromSameCompany: Boolean!
	"""
	Allow requests from people who share titles with the viewer.
	"""
	allowFromSharedTitles: Boolean!
	"""
	Whether the viewer is high-status (read-only, system-determined).
	"""
	isHighStatus: Boolean!
	"""
	Whether the viewer is a minor (read-only, system-determined).
	"""
	isMinor: Boolean!
}

"""
A profession this name has been endorsed for, with its endorsement count.
"""
type ProNetworkingProfessionEndorsementCount {
	"""
	The number of endorsements tagging this profession.
	"""
	count: Int!
	"""
	The profession this name has been endorsed for.
	"""
	profession: NameProfession!
}

"""
An edge in ProNetworkingProfessionEndorsementCountsConnection — a single
profession-count node with its pagination cursor and position.
"""
type ProNetworkingProfessionEndorsementCountEdge {
	"""
	Opaque cursor for pagination.
	"""
	cursor: ID!
	"""
	The profession-count entry at this position.
	"""
	node: ProNetworkingProfessionEndorsementCount!
	"""
	Position of this item in the result set.
	"""
	position: Int!
}

"""
A paginated list of professions a name has been endorsed for, with per-profession counts (cursor pagination).
"""
type ProNetworkingProfessionEndorsementCountsConnection {
	"""
	List of profession-count edges.
	"""
	edges: [ProNetworkingProfessionEndorsementCountEdge]!
	"""
	Pagination metadata.
	"""
	pageInfo: PageInfo!
	"""
	Total number of distinct professions this name has been endorsed for.
	"""
	total: Int!
}

"""
A recommended connection for the viewer, ranked by relevance.
"""
type ProNetworkingRecommendation {
	"""
	Categories explaining why this person is recommended.
	Includes at least one category. SHARED_TITLES is included when the viewer
	and candidate share title credits.
	"""
	categories: [ProNetworkingRecommendationCategory]!
	"""
	Detailed information for each recommendation category, including typed entity references.
	Only categories with associated entity data are included.
	"""
	categoryDetails: [ProNetworkingRecommendationCategoryDetail]!
	"""
	Number of mutual connections between the viewer and this person.
	"""
	mutualConnectionCount: Int!
	"""
	The recommended professional.
	"""
	name: Name!
	"""
	Number of shared titles between the viewer and this person.
	"""
	sharedTitleCount: Int!
}

"""
The system-computed reason a professional is recommended to the viewer, surfaced on
recommendation and mutual-collaborator-suggestion results. Distinct from
ProNetworkingConnectionReasonCategory, which is the viewer-supplied reason attached to a
connection request.
"""
enum ProNetworkingRecommendationCategory {
	"""
	Have mutual connections.
	"""
	MUTUAL_CONNECTIONS
	"""
	Representation relationship in either direction — the viewer represents this person, or this person represents the viewer.
	"""
	REP_CLIENT
	"""
	Work at the same company.
	"""
	SAME_COMPANY
	"""
	Member of the same guild or professional organization.
	"""
	SAME_GUILD
	"""
	Worked on shared titles.
	"""
	SHARED_TITLES
}

"""
Detailed information about a specific recommendation category,
including typed entity references for frontend resolution.
"""
type ProNetworkingRecommendationCategoryDetail {
	"""
	The recommendation category this detail describes.
	"""
	category: ProNetworkingRecommendationCategory!
	"""
	The shared company entity. Populated for SAME_COMPANY and SAME_GUILD categories.
	"""
	sharedCompany: Company!
}

"""
An edge in a connection recommendations list.
"""
type ProNetworkingRecommendationEdge {
	"""
	Opaque cursor for pagination.
	"""
	cursor: ID!
	"""
	The recommendation.
	"""
	node: ProNetworkingRecommendation!
	"""
	Position of this item in the result set.
	"""
	position: Int!
}

"""
Paginated list of connection recommendations following the Relay Connection spec.
"""
type ProNetworkingRecommendationsConnection {
	"""
	List of recommendation edges.
	"""
	edges: [ProNetworkingRecommendationEdge]!
	"""
	Pagination metadata.
	"""
	pageInfo: PageInfo!
	"""
	Total number of recommendations returned.
	"""
	total: Int!
}

"""
Sort criteria for pending connection requests.
"""
enum ProNetworkingRequestSortBy {
	"""
	Sort by request date, most recent first. This is the default sort order.
	"""
	DATE_RECEIVED_DESC
	"""
	Sort by the number of mutual connections with the requester, highest first.
	Tiebreaker: request date, most recent first.
	"""
	MUTUAL_CONNECTIONS_DESC
	"""
	Sort by the number of shared titles with the requester, highest first.
	Tiebreaker: request date, most recent first.
	"""
	SHARED_TITLES_DESC
}

"""
An edge in a shared titles list.
"""
type ProNetworkingSharedTitleEdge {
	"""
	Opaque cursor for pagination.
	"""
	cursor: ID!
	"""
	The title.
	"""
	node: SharedTitle!
	"""
	Position of this item in the result set.
	"""
	position: Int!
}

"""
Paginated list of shared titles.
"""
type ProNetworkingSharedTitlesConnection {
	"""
	List of title edges.
	"""
	edges: [ProNetworkingSharedTitleEdge]!
	"""
	Pagination metadata.
	"""
	pageInfo: PageInfo!
	"""
	Total number of shared titles.
	"""
	total: Int!
}

"""
Optional filters for `Name.myWrittenEndorsements`.
"""
input ProNetworkingWrittenEndorsementsFilter {
	"""
	Scope to endorsements of a single subject — at most one endorsement, plus its
	pending edit, since a name authors one endorsement per subject. When omitted,
	all authored endorsements are returned.
	"""
	subjectNameId: ID
}

"""
ProStatus: contains related pro subscription information.

Note: the Pro subscription status is published via Pro subscription service,
and may be out of sync with the customer's actual subscription
"""
type ProStatus {
	hasSubscription: Boolean
}

enum PublicationStatus {
	NOT_PUBLISHED
	PUBLISHED
	REDIRECTED
}

type PublicityArticle implements PublicityListingType {
	authors: [Markdown]
	category: PublicityListingCategory!
	date: Date
	"""
	The publicity listing as a displayable property
	"""
	displayableProperty: DisplayablePublicityListingProperty!
	language: DisplayableLanguage
	publication: String
	reference: String
	region: DisplayableCountry
	title: PublicityListingTitleText
}

type PublicityCategoryWithListings {
	category: PublicityListingCategory!
	publicityListings(first: Int): PublicityListingConnection
}

type PublicityInterview implements PublicityListingType {
	authors: [Markdown]
	category: PublicityListingCategory!
	date: Date
	"""
	The publicity listing as a displayable property
	"""
	displayableProperty: DisplayablePublicityListingProperty!
	language: DisplayableLanguage
	publication: String
	reference: String
	region: DisplayableCountry
	title: PublicityListingTitleText
}

"""
A category of publicity listings describing the nature of a subset of listings, e.g. 'Article'
"""
type PublicityListingCategory {
	id: ID!
	text: String!
}

"""
Pagination for Publicity Listing types which always has a known total number of edges.
"""
type PublicityListingConnection {
	"""
	Pages of publicityListings
	"""
	edges: [PublicityListingEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of publicityListings
"""
type PublicityListingEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A PublicityListing node
	"""
	node: PublicityListingType!
	"""
	Position of the node in this context, eg: PublicityListing 4 out of 312
	"""
	position: Int!
}

input PublicityListingsFilter {
	categories: [ID]
}

type PublicityListingTitleText {
	language: DisplayableLanguage
	text: String
}

interface PublicityListingType {
	category: PublicityListingCategory!
}

type PublicityMagazineCover implements PublicityListingType {
	authors: [Markdown]
	category: PublicityListingCategory!
	date: Date
	"""
	The publicity listing as a displayable property
	"""
	displayableProperty: DisplayablePublicityListingProperty!
	language: DisplayableLanguage
	publication: String
	reference: String
	region: DisplayableCountry
	title: PublicityListingTitleText
}

type PublicityPictorial implements PublicityListingType {
	authors: [Markdown]
	category: PublicityListingCategory!
	date: Date
	"""
	The publicity listing as a displayable property
	"""
	displayableProperty: DisplayablePublicityListingProperty!
	language: DisplayableLanguage
	publication: String
	reference: String
	region: DisplayableCountry
	title: PublicityListingTitleText
}

input PublicProfileEligibilityInput {
	"""
	The user ID to check for public profile eligibility.
	"""
	userId: ID!
}

type PublicProfileEligibilityOutput {
	"""
	Whether the customer identified by userId is eligible for a public profile.
	True when any eligibility condition is met, false otherwise.
	"""
	isEligible: Boolean!
	"""
	The profile ID for the customer identified by userId.
	Returned when isEligible is true, null when isEligible is false.
	"""
	profileId: ID
}

"""
Push notification metadata for an authenticated user.
"""
type PushNotificationUserSettings {
	"""
	Push notification ID for the authenticated user.
	"""
	pushNotificationUserId: ID!
}

"""
IMDb Discussion GraphQL API Schema

This schema defines the API for IMDb's discussion feature, enabling users to
participate in community discussions around IMDb interest entities (titles, names, etc.).

Key Features:
- Discussion communities tied to IMDb entities
- Threaded discussions with nested replies (max 1 level deep)
- Rich text content with entity mentions
- Spoiler handling
- Content moderation support
"""
type Query {
	"""
	Retrieves the data for displaying account data access dialogs. The function
	returns the text to be displayed for REQUEST, CONFIRM

	---------------------
	Graphlet Details:
	- Name: privacy
	- CTI: IMDb/GraphQL/Privacy
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	accountDataDialog(input: AccountDataDialogInput!): AccountDataDialogOutput!
	"""
	Search all IMDb names based on a wide variety of constraints

	If a Pro-only constraint is used, the x-imdb-customer-id header is required.

	The following errors can occur if a Pro-only constraint is used:
	  Throws UNAUTHENTICATED if not logged in
	  Throws FORBIDDEN if customer w/o any IMDbPro subscription
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	advancedNameSearch(
		after: String
		"""
		Provide options to filter down to a particular set of names
		"""
		constraints: AdvancedNameSearchConstraints
		first: Int!
		"""
		Option for index-based pagination. Jumps to a particular numerical, positive contextual position in
		the results (the position after taking "constraints" and "sort" into account), and will return the
		"first" names starting at the provided position value.

		Will throw a BAD_USER_INPUT if used with "after", if less than or equal to zero, or greater than or equal to 10,000.

		If the specified position is larger than the result set, will return no results (not null).
		"""
		jumpToPosition: Int
		"""
		Defaults to popularity sort (sortBy: POPULARITY, sortOrder: ASC)
		Names without data for the sort by key, are sorted after those that do.
		Falls back to sorting by popularity, and then nconst, for ties.
		"""
		sort: AdvancedNameSearchSort
	): AdvancedNameSearchConnection
	"""
	Search all IMDb titles based on a wide variety of constraints.

	If a Pro-only constraint is used, the x-imdb-customer-id header is required.

	The following errors can occur if a Pro-only constraint is used:
	  Throws UNAUTHENTICATED if not logged in
	  Throws FORBIDDEN if customer w/o any IMDbPro subscription
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	advancedTitleSearch(
		after: String
		"""
		Provide options to filter down to a particular set of titles
		"""
		constraints: AdvancedTitleSearchConstraints
		first: Int!
		"""
		Option for index-based pagination. Jumps to a particular numerical, positive contextual position in
		the results (the position after taking "constraints" and "sort" into account), and will return the
		"first" titles starting at the provided position value.

		Will throw a BAD_USER_INPUT if used with "after", if less than or equal to zero, or greater than or equal to 10,000.

		If the specified position is larger than the result set, will return no results (not null).
		"""
		jumpToPosition: Int
		"""
		Defaults to popularity sort (sortBy: POPULARITY sortOrder: ASC)
		Titles without data for the sort by key, are sorted after those that do, except RELEASE_DATE DESC.
		Falls back to sorting by popularity, and then tconst, for ties.
		"""
		sort: AdvancedTitleSearchSort
	): AdvancedTitleSearchConnection
	"""
	Determines if an Amazon customer is eligible for Single Sign-On into IMDb.
	Returns eligibility status for the provided customer identifier.

	Throws GRAPHQL_VALIDATION_FAILED if input parameters are invalid (e.g., incorrect customer identifier format, missing input, etc.).

	---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	amazonSsoEligibility(
		input: AmazonSsoEligibilityInput!
	): AmazonSsoEligibilityOutput!
	"""
	Used to fetch advertising related information for IMDb mobile apps.
	Returns an object containing eligibility for apps to serve 3p ads and a configuration map for the slots on each page.

	---------------------
	Graphlet Details:
	- Name: advertising
	- CTI: IMDb/GraphQL/Advertising
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-adsystem-overrides, x-imdb-client-ip, x-imdb-client-name, x-imdb-client-version, x-imdb-consent-info, x-imdb-detected-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-adtech-public
	"""
	appAdsInfo: AppAdsInfoOutput
	"""
	Returns product data for a list of Amazon ASINs in a given marketplace,
	including title, images, pricing, ratings, and availability information.

	Returns an empty asinData list if no products could be retrieved.
	Individual ASINs that fail to resolve are reported in asinErrors.
	Returns null if the Amazon API is unreachable or the request cannot be fulfilled.

	---------------------
	Graphlet Details:
	- Name: advertising
	- CTI: IMDb/GraphQL/Advertising
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-adsystem-overrides, x-imdb-client-ip, x-imdb-client-name, x-imdb-client-version, x-imdb-consent-info, x-imdb-detected-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-adtech-public
	"""
	asinData(input: AsinDataInput!): AsinDataOutput
	"""
	Get a single badge's metadata by its badgeId.

	Returns null if no badge exists for the given id.
	Throws UNSUPPORTED_IMAGE_TYPE if the service cannot provide the requested image type.

	---------------------
	Graphlet Details:
	- Name: badges
	- CTI: IMDb/GraphQL/Badges
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pwr-public
	"""
	badge(id: ID!): Badge
	"""
	Get all available badge guide help entries in the system which represent one
	or more badges for providing awarding instructions and context
	e.g. Oscars yearly badges are represented by the Oscars badge guide entry.
	Some entries only represent a single badge, like the IMDb Staff badge.
	Ordering handled by service ranking.

	22 badge guide entries are supported initially.

	Throws UNSUPPORTED_IMAGE_TYPE if service cannot provide the requested image type

	---------------------
	Graphlet Details:
	- Name: badges
	- CTI: IMDb/GraphQL/Badges
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pwr-public
	"""
	badgeGuideEntries(first: Int!): BadgeGuideEntryConnection!
	"""
	At most 30 Names born on the given date, ordered by STARmeter, excluding adult and controversial names.

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	bornToday(first: Int!, today: MonthDay!): NameSearchConnection
	"""
	Highest-grossing titles at the domestic box office over the most recent weekend.  At most 10 titles.

	---------------------
	Graphlet Details:
	- Name: mojo
	- CTI: IMDb/GraphQL/Mojo
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	boxOfficeWeekendChart(limit: Int): BoxOfficeWeekendChart
	"""
	Query used to fetch specified CTAs.
	Each CTA is defined as an individual field in CallToAction

	---------------------
	Graphlet Details:
	- Name: callToAction
	- CTI: IMDb/GraphQL/Call To Action
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-engagement-growth-public
	"""
	callToAction(
		"""
		Optional argument to specify context input data.
		When omitted, no context is applied to the conditional logic. Omitting context could affect
		the result of a CTA that expects context input. For those CTAs that don't need context input,
		providing a context value has no affect.
		"""
		context: CallToActionContextInput
	): CallToAction
	"""
	True if this customer can claim a name page

	Throws UNAUTHENTICATED if customer not authenticated

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	canClaimNamePage: Boolean
	"""
	@maxAuthAge=900
	Returns a URL to redirect to for user login & security page
	Throws UNAUTHENTICATED if user needs to reauthenticate
	Throws GRAPHQL_VALIDATION_FAILED if an invalid association handle is provided.
	Throws GRAPHQL_VALIDATION_FAILED if returnURL is not an absolute URL or not an IMDb domain (ends with 'imdb.com') if passed

	---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	changeLoginSecurityRedirectURL(
		input: ChangeLoginSecurityRedirectURLInput!
	): ChangeLoginSecurityRedirectURLOutput
	"""
	Retrieves data associated with specific name ranking chart in ordered ranking by default.
	It supports all filters and sorts, as needed by the Chart pages.
	Returns up to `first` non-adult names sorted by star meter rank. Adult names are excluded
	by default unless the caller passes an explicitContentConstraint with IncludeAdult.

	For NAME and LAST_NAME sorts, the top 100 results by popularity are fetched first,
	then re-sorted alphabetically in memory. This caps results at 100 for these sorts.

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	chartNames(
		after: String
		chart: ChartNameOptions!
		filter: AdvancedNameSearchConstraints
		first: Int!
		sort: AdvancedNameSearchSort
	): ChartNameSearchConnection
	"""
	Retrieves data associated with specific ranking chart in ordered ranking by default.
	It supports all filters and sorts, as needed by the Chart pages.

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	chartTitles(
		after: String
		chart: ChartTitleOptions!
		filter: AdvancedTitleSearchConstraints
		first: Int!
		sort: AdvancedTitleSearchSort
	): ChartTitleSearchConnection
	"""
	Get a Cinema by it's `ci` const.

	Notes:
	 - Returns null if the cinema doesn't exist.

	Errors:
	- Throws `BAD_USER_INPUT` if `id` isn't a valid `ci` const

	---------------------
	Graphlet Details:
	- Name: titleAppointments
	- CTI: IMDb/GraphQL/Title Appointments
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	cinema(id: ID!): Cinema
	"""
	The requesting customer's claimed name information

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	claimedName: ClaimedName
	"""
	At most 100 titles releasing wide within 45 days of the given Date, ordered by MOVIEmeter, excluding adult titles.
	NOTE: This will return data for the US no matter what x-imdb-user-country value is passed in.
	SEE: https://t.corp.amazon.com/V362117929/communication

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	comingSoon(
		"""
		Next page cursor for pagination.
		"""
		after: ID
		"""
		TV or MOVIE
		"""
		comingSoonType: ComingSoonType!
		"""
		Disable the default popularity cap.
		The cap filters out less popular movies, and is useful for returning higher
		quality results for short windows, like the default 45 days.
		Movies released further in the future are less likely to be popular, so
		removing the cap will give more complete results for longer time windows.
		"""
		disablePopularityFilter: Boolean
		first: Int!
		"""
		Uses the 'US' override if not specified
		"""
		regionOverride: String
		"""
		The earliest wide release date eligible to include in these results.
		"""
		releasingOnOrAfter: Date!
		"""
		Optional max date for setting a time window. If not set, we assume 45 days from the releasingOnOrAfter date
		"""
		releasingOnOrBefore: Date
		"""
		Defaults to [POPULARITY, ASC] if not specified
		"""
		sort: [ComingSoonSort]
	): TitleSearchConnection
	"""
	Output will be in the same order as input, including remapped (returned as
	mapped id) and non-existent consts (returned with null fields)

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	companies(ids: [ID]): [Company]!
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	company(id: ID!): Company
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	companyMetadata: CompanyMetadata
	"""
	Detailed connection info between the viewer and a target name.
	Optionally scoped to a page owner via pageOwnerNameId to surface the "with page owner" overlap
	(mutualConnectionsWithPageOwner / sharedTitlesWithPageOwner on the returned type). That overlap is
	returned only when pageOwnerNameId is provided, differs from targetNameId, and the viewer is
	connected to either the page owner or the target; otherwise those fields are null.
	Returns null if the targetNameId does not exist.
	Throws UNAUTHENTICATED if not logged in.
	@entitlement(PRO_ANY_TIER, PRO_PREMIUM)
	Premium users see full details. Basic tier users see limited mutual connection info.

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	connectionDetail(
		pageOwnerNameId: ID
		targetNameId: ID!
	): ProNetworkingConnectionDetail
	"""
	Personalized connection recommendations for the viewer.
	Candidates are sourced from shared title credits AND professional relationships
	(representatives, clients via Tango data).
	Ordered by tiered ranking: (1) direct representatives/clients, (2) candidates with mutual
	connections, (3) candidates with shared title credits, (4) company coworkers.
	Within each tier, tiebreakers include mutual connection count, shared title count,
	and STARmeter rank.
	Excludes connected, blocked, hidden, and pending-request names.
	Only returns allowlisted professionals.
	Up to 25 recommendations per request. If first exceeds 25, only 25 are returned. If first is omitted, defaults to 5.
	Throws UNAUTHENTICATED if not logged in.
	Premium only.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	connectionRecommendations(
		after: ID
		first: Int
	): ProNetworkingRecommendationsConnection!
	"""
	Batch lookup of connection statuses between the viewer and multiple names.
	Useful for rendering connection status badges on list views without N+1 queries.
	Maximum 50 nameIds per request. Returns statuses in the same order as the input nameIds.
	An empty array input returns an empty array.
	Premium: canSendRequest is true for any eligible target.
	Basic: canSendRequest is true only for targets with shared titles.
	Throws UNAUTHENTICATED if not logged in.
	Throws GRAPHQL_VALIDATION_FAILED if more than 50 nameIds are provided.
	@entitlement(PRO_ANY_TIER, PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	connectionStatuses(nameIds: [ID]!): [ProNetworkingConnectionStatus]!
	"""
	Searches for structured filter constraints matching a natural language query.
	Returns matched filters with a confidence score indicating how well the query
	maps to structured constraints. Used to power constraint-based search routing.

	Each result includes a filterType (TITLE or NAME) indicating whether the filter
	targets ATS or ANS constraints.

	Results are ordered by relevance score (most relevant first).

	Up to 100 results can be requested at a time. If `first` exceeds 100, only 100 results will be returned.
	Returns empty results with zero confidence if `searchTerm` is blank.
	Throws GRAPHQL_VALIDATION_FAILED if `first` is not a positive integer.
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	constraintSearch(
		input: Search_ConstraintSearchInput!
	): Search_ConstraintSearchResults!
	"""
	Contributor leaderboard queries

	---------------------
	Graphlet Details:
	- Name: contributor
	- CTI: IMDb/GraphQL/Contributor
	- Affected by headers: x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-contributors-tech
	"""
	contributorLeaderboards: ContributorLeaderboards
	"""
	Return contributor ranking for 1 or more leaderboards in reverse chronological order

	---------------------
	Graphlet Details:
	- Name: contributor
	- CTI: IMDb/GraphQL/Contributor
	- Affected by headers: x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-contributors-tech
	"""
	contributorRankings(
		after: ID
		filter: ContributorRankingsFilter!
		first: Int
	): ContributorLeaderboardRankConnection!
	"""
	Returns a URL to redirect to for user account creation
	Throws GRAPHQL_VALIDATION_FAILED if an invalid association handle is provided.
	Throws GRAPHQL_VALIDATION_FAILED if returnURL is not an absolute URL or not an IMDb domain (ends with 'imdb.com') if passed

	---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	createAccountRedirectURL(
		input: CreateAccountRedirectURLInput!
	): CreateAccountRedirectURLOutput
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	creditCategory(id: ID!): CreditCategory
	"""
	The requesting customer's saved credit filter preferences.
	Returns null if the customer is not authenticated.

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	creditFilterPreferences: CreditFilterPreferences
	"""
	Retrieves the data for displaying deletion dialogs. The function returns the
	text to be displayed and, if it's for a deletion REQUEST dialog, a requestId as well.

	---------------------
	Graphlet Details:
	- Name: privacy
	- CTI: IMDb/GraphQL/Privacy
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	deletionDialog(input: DeletionDialogInput!): DeletionDialogOutput!
	"""
	Provides a context-specific directed ID for a customer.
	Directed IDs are unique identifiers that vary based on the context in which they're used,
	allowing the same customer to have different IDs for different purposes (e.g., user profiles vs. third-party integrations).

	Throws UNAUTHENTICATED if the customer is not logged in.

	---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	directedId(input: DirectedIdInput!): DirectedIdOutput!
	"""
	Retrieve a discussion community by its ID.

	Communities are associated with IMDb entities (such as Interests) and contain discussions.

	@throws RESOURCE_NOT_FOUND if the community does not exist or is disabled.
	@entitlement(IMDB_STAFF)

	---------------------
	Graphlet Details:
	- Name: discussion
	- CTI: IMDb/GraphQL/Discussion
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	discussionCommunity(id: ID!): DiscussionCommunity!
	"""
	Valid reasons a moderator may attach to a community status change.

	---------------------
	Graphlet Details:
	- Name: discussion
	- CTI: IMDb/GraphQL/Discussion
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	discussionCommunityStatusReasons: [DiscussionCommunityStatusReason]!
	"""
	Retrieve the list of available deletion reasons.

	Used to populate the deletion reason dropdown when a user or moderator
	deletes a discussion or reply.

	---------------------
	Graphlet Details:
	- Name: discussion
	- CTI: IMDb/GraphQL/Discussion
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	discussionDeletionReasons: [DiscussionDeletionReason]!
	"""
	Retrieve a batch of discussion replies by ID. Support max 100 ids in single request.
	Returns an array of the same length as the input `ids`, where each position
	corresponds to the input ID at that index. A null value at a given position
	indicates invalid input id or the ID was not found.

	Used for pagination of child replies when users request additional replies
	beyond the initial set.

	@throws GRAPHQL_VALIDATION_FAILED if more than 100 ids are provided
	@entitlement(IMDB_STAFF)

	---------------------
	Graphlet Details:
	- Name: discussion
	- CTI: IMDb/GraphQL/Discussion
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	discussionReplies(ids: [ID]!): [DiscussionReply]!
	"""
	Valid reasons a moderator may attach to a thread availability change.

	---------------------
	Graphlet Details:
	- Name: discussion
	- CTI: IMDb/GraphQL/Discussion
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	discussionThreadAvailabilityStatusReasons: [DiscussionThreadAvailabilityStatusReason]!
	"""
	Retrieve a batch of discussions by ID. Support max 100 ids in single request.
	Returns an array of the same length as the input `ids`, where each position
	corresponds to the input ID at that index. A null value at a given position
	indicates invalid input id or the ID was not found.

	A discussion contains an original post with a title and optional body,
	plus zero or more replies.

	@throws GRAPHQL_VALIDATION_FAILED if more than 100 ids are provided
	@entitlement(IMDB_STAFF)

	---------------------
	Graphlet Details:
	- Name: discussion
	- CTI: IMDb/GraphQL/Discussion
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	discussionThreads(ids: [ID]!): [DiscussionThread]!
	"""
	Query used to fetch one or more ads for an IMDb app.
	Returns an array of ad slots and their creative info returned from the AAX call

	---------------------
	Graphlet Details:
	- Name: advertising
	- CTI: IMDb/GraphQL/Advertising
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-adsystem-overrides, x-imdb-client-ip, x-imdb-client-name, x-imdb-client-version, x-imdb-consent-info, x-imdb-detected-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-adtech-public
	"""
	displayAdsForApp(input: DisplayAdsForAppInput!): [AdSlot]
	"""
	Determine if a prompt should be displayed
	constId: const id of item to determine if prompt should be displayed for
	promptType: the type of prompt to be displayed

	---------------------
	Graphlet Details:
	- Name: personalization
	- CTI: IMDb/GraphQL/Personalized Picks
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-consent-info, x-imdb-detected-country, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	displayablePrompt(constId: ID!, promptType: PromptType!): DisplayablePrompt!
	"""
	Retrieves all email preferences for the authenticated user.

	The customer ID is automatically determined from the x-imdb-customer-id header.
	Returns the master marketing preference toggle and all individual email preferences
	categorized by type (marketing and subscription).

	@throws UNAUTHENTICATED if the customer is not logged in

	---------------------
	Graphlet Details:
	- Name: notificationPreferences
	- CTI: IMDb/GraphQL/Notification Preferences
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	emailPreferences(
		"""
		Business domain context for the request.
		Valid values are defined in the BusinessDomain enum.
		If not provided, defaults to CONSUMER domain.
		"""
		domain: BusinessDomain
	): EmailPreferences!
	"""
	Fetch processing jobs with pagination.
	By default returns non-PUBLISHED jobs (EXTRACTING, READY, FAILED),
	ordered by uploadedAt descending (newest first).

	Up to 100 jobs can be requested per page. If first exceeds 100, only 100 are returned.
	first must be a positive integer.

	IMDB_STAFF users see all jobs. SCORECARD users see only jobs for titles in their entitlement context.
	@entitlement(IMDB_STAFF, SCORECARD)

	---------------------
	Graphlet Details:
	- Name: hiddenFigures
	- CTI: IMDb/GraphQL/HiddenFigures
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	endCreditIngestionProcessingJobs(
		"""
		Cursor for forward pagination.
		"""
		after: ID
		"""
		Number of items to return per page.
		"""
		first: Int!
		"""
		Filter criteria for the query.
		"""
		input: EndCreditIngestionProcessingJobsInput
	): EndCreditIngestionProcessingJobConnection!
	"""
	Data for displaying the live winners for an ongoing award event. If there is no current ongoing event, returns null.
	Server Cache TTL is 15 minutes when null is returned, 30 seconds when data is returned.

	Optionally specify event override to return data for past events or events to be used for testing.

	---------------------
	Graphlet Details:
	- Name: winnersWidget
	- CTI: IMDb/GraphQL/Winners Widget
	- Affected by headers: x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	eventLiveResults(override: EventLiveResultsOverrideInput): EventLiveResults
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	eventMetadata: EventMetadata
	"""
	@experimental Get a single contribution basket by its reference.

	Scoped to the signed-in viewer. Returns null if no basket exists for the reference OR
	the viewer is not allowed to see it -- the two are deliberately indistinguishable so a
	reference can't be probed for existence.

	Required Headers:
	x-imdb-customer-id

	Possible Errors:
	Throws UNAUTHENTICATED if the customer is not signed in.

	---------------------
	Graphlet Details:
	- Name: contribution
	- CTI: IMDb/GraphQL/Contribution
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-contributors-okapi
	"""
	experimental_contributionSubmission(
		"""
		Reference of the contribution basket to load.
		"""
		submissionReference: ID!
	): Experimental_ContributionSubmission
	"""
	@experimental Get a leaderboard ranking by scope and time.
	Returns ranked users ordered by points descending (highest first).
	Omit scopeId for the global leaderboard spanning all scopes.

	Throws GRAPHQL_VALIDATION_FAILED if scopeId does not match a recognized const format.

	---------------------
	Graphlet Details:
	- Name: leaderboard
	- CTI: IMDb/GraphQL/Leaderboard
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-engagement-growth-public
	"""
	experimental_gameLeaderboard(
		input: Experimental_GameLeaderboardInput!
	): Experimental_GameLeaderboard!
	"""
	@experimental The game types that have leaderboards, for populating a game selector.

	---------------------
	Graphlet Details:
	- Name: leaderboard
	- CTI: IMDb/GraphQL/Leaderboard
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-engagement-growth-public
	"""
	experimental_gameLeaderboardGames: [Experimental_GameLeaderboardGame]!
	"""
	@experimental Retrieve the latest UI chat session - temporary field for chat prototype validation
	Provide a way for a client to re-fetch the complete chat conversation for a given
	chat session, to support recovery from an error, debugging tools, or UI restoration.
	Throws UNAUTHENTICATED if the requesting customer is not logged in.
	Throws FORBIDDEN if the requesting customer is not entitled to view the chat session requested.
	Throws RESOURCE_NOT_FOUND if the chat session ID requested does not exist.

	---------------------
	Graphlet Details:
	- Name: contributionSDUI
	- CTI: IMDb/GraphQL/ContributionSDUI
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-sdui
	"""
	experimental_getLatestUIChat(
		input: Experimental_GetLatestUIChatInput!
	): Experimental_GetLatestUIChatOutput!
	"""
	@experimental Search for available ANS (Advanced Name Search) constraint filters by search term.

	This API enables discovery of which filters can be applied to name searches. Given a search term,
	it returns matching filter metadata including the filter's display name, constraint name for use
	in ANS queries, and the specific values that matched the search.

	For example, searching "cinematographer" returns the Job Categories filter with the matched term
	"Cinematographer" and its constraint value for use in ANS queries.

	Returns structured filter information including:
	- filterId: Unique identifier for the filter
	- constraintName: The field name to use in AdvancedNameSearchConstraints
	- displayName: Human-readable filter name
	- requiresEntitlement: Array of entitlements needed (e.g., ["PRO_ANY_TIER"])
	- matchedTerms: Array of matched values with displayValue (for UI), constraintValue (for queries), and searchable terms

	WebLab: IMDB_SEARCH_DISCOVER_MODERN_1367402

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	experimental_nameFilterSearch(
		"""
		Number of results to return. Must be a positive integer. Maximum value is
		100. If first exceeds 100, only 100 results will be returned.
		"""
		first: Int!
		"""
		Search term to match against filter names and values. Returns empty results if blank.
		"""
		searchTerm: String!
	): Experimental_NameFilterSearchConnection
	"""
	@experimental Search for available ATS (Advanced Title Search) constraint filters by search term.

	This API enables discovery of which filters can be applied to title searches. Given a search term,
	it returns matching filter metadata including the filter's display name, constraint name for use
	in ATS queries, and the specific values that matched the search.

	For example, searching "action" returns the Genres filter with the matched term "Action" and its
	constraint value. Searching "movies with producer" returns multiple filters including Title Type
	(Movie, TV Movie) and Professions Attached (Producer).

	Returns structured filter information including:
	- filterId: Unique identifier for the filter
	- constraintName: The field name to use in AdvancedTitleSearchConstraints
	- displayName: Human-readable filter name
	- requiresEntitlement: Array of entitlements needed (e.g., ["PRO_ANY_TIER"])
	- matchedTerms: Array of matched values with displayValue (for UI), constraintValue (for queries), and searchable terms

	WebLab: IMDB_SEARCH_DISCOVER_MODERN_1367402

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	experimental_titleFilterSearch(
		"""
		Number of results to return. Must be a positive integer. Maximum value is
		100. If first exceeds 100, only 100 results will be returned.
		"""
		first: Int!
		"""
		Search term to match against filter names and values. Returns empty results if blank.
		"""
		searchTerm: String!
	): Experimental_TitleFilterSearchConnection
	"""
	@experimental Universal search endpoint for workflow elements using dataSourceId configuration.
	Provides searchable options for form elements with dynamic data loading.

	Supports:
	- Predefined option list searches
	- Custom data source searches

	Error Conditions:
	- UNAUTHENTICATED: Requesting customer is not logged in
	- FORBIDDEN: Requesting customer is not entitled to access the datasource
	- BAD_USER_INPUT:
	  * dataSourceId is not supported or invalid
	  * provided constraints are invalid for the data source
	  * first parameter is out of valid range
	  * after cursor is invalid or expired

	---------------------
	Graphlet Details:
	- Name: contributionSDUI
	- CTI: IMDb/GraphQL/ContributionSDUI
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-sdui
	"""
	experimental_uiWorkflowOptionsSearch(
		input: Experimental_UiWorkflowOptionsSearchInput!
	): Experimental_UiWorkflowOptionsSearchOutput!
	"""
	Retrieves Fan Picks titles. Max batch size is 100 titles.

	Providing filter input params filters fan picks titles response based on filtering criteria. Currently we support
	filtering by title type movie or tv.

	---------------------
	Graphlet Details:
	- Name: personalization
	- CTI: IMDb/GraphQL/Personalized Picks
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-consent-info, x-imdb-detected-country, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	fanPicksTitles(
		after: ID
		filter: FanPicksFilter
		first: Int!
	): FanPicksConnection
	"""
	Query used to determine if a customer has access to a given feature.

	Each field returns true if the feature access is allowed, false if not.
	Each field will return its default state for logged out customers, and will state its default in its comments.

	This field by itself does not grant access to any data, it just signals to the
	client that they should attempt to access a feature.
	It is intended for low latency gating checks that cannot otherwise be determined by using an entitled API directly,
	for example due to an entitled API being expensive to call at scale, or that there is no corresponding entitled API.

	---------------------
	Graphlet Details:
	- Name: callToAction
	- CTI: IMDb/GraphQL/Call To Action
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-engagement-growth-public
	"""
	featureAccess: FeatureAccess
	"""
	Returns all localized filter options for a set of filter IDs, selecting the
	appropriate Elasticsearch index based on `searchType` (TITLE → title-filters,
	NAME → name-filters). Each group contains the filter's display name and all
	available options with display and constraint values, translated via the Panther pipeline.

	This query only covers filters that have discrete, localized option sets (e.g. genres,
	title-type, profession-categories). It does not cover range-based filters (e.g. IMDbMeter
	range, release date), free-form text filters (e.g. "Title name contains"), or entity
	search filters (e.g. "People credited or attached", "Companies credited or attached").

	The `constraintValue` for each option is always in English and is intended for use in
	URL parameters (e.g. `genres=action`). The `displayValue` is the translated label for
	UI presentation.

	Ordering: Results are returned in the same order as the input `filterIds`. Duplicate
	IDs are deduplicated; only the first occurrence is kept. Options within each group
	are ordered alphabetically by `displayValue`.

	Filter IDs with no matching document in the selected index are silently omitted
	from the response. Returns an empty array if no filterIds match.

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	filterOptions(
		"""
		Input specifying which filter options to retrieve.
		"""
		input: FilterOptionsInput!
	): [FilterOptionsGroup]!
	"""
	Returns a paginated list of the requesting a logged in customer's followed
	entities. By default, the list is sorted by last updated date.

	In this context, entities refers to content the user has followed with a
	unique ID.  This could be publicly available content like an Interest or Name,
	or in the future a user could follow a conversation or a topic.

	Supports page size of at most 250.  Requesting a page size larger than the
	maximum supported will result in the maximum size being returned.

	Throws UNAUTHENTICATED if not logged in as determined via the x-imdb-customer-id header.

	---------------------
	Graphlet Details:
	- Name: follows
	- CTI: IMDb/GraphQL/Follows
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	followedEntities(
		after: ID
		filter: FollowFilter
		first: Int!
		sort: FollowSort
	): FollowedEntitiesConnection
	"""
	Returns a URL to redirect to for user account forgot password flow
	Throws GRAPHQL_VALIDATION_FAILED if an invalid association handle is provided.

	---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	forgotPasswordRedirectURL(
		input: ForgotPasswordRedirectURLInput!
	): ForgotPasswordRedirectURLOutput
	"""
	Retrieves the details of all exports based on the provided input. The identity
	of the owner of the exports is determined by the x-amzn-sessionid header for
	non-logged in users or the x-imdb-customer-id header for logged in users.
	If sort is not provided, export details are sorted by the export's StartedOn
	value in descending order, from the most recent to the oldest.
	Relevant headers: 'x-amzn-sessionid', 'x-imdb-customer-id'

	If the operation fails to complete, a SERVICE_UNAVAILABLE_ERROR will be thrown.

	---------------------
	Graphlet Details:
	- Name: privacy
	- CTI: IMDb/GraphQL/Privacy
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	getExports(
		after: ID
		filter: ExportFilterByInput
		first: Int!
		input: GetExportsInput!
		sort: ExportSortByInput
	): ExportDetailConnection!
	"""
	Provide a way for a client to re-fetch the last UI workflow model for a given
	workflow execution, to support recovery from an error, or debugging tools.
	Throws UNAUTHENTICATED if the requesting customer is not logged in.
	Throws FORBIDDEN if the requesting customer is not entitled to view the workflow execution requested.
	Throws RESOURCE_NOT_FOUND if the workflow execution ID requested does not exist.

	---------------------
	Graphlet Details:
	- Name: contributionSDUI
	- CTI: IMDb/GraphQL/ContributionSDUI
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-sdui
	"""
	getLatestUIWorkflow(
		input: GetLatestUIWorkflowInput!
	): GetLatestUIWorkflowOutput
	"""
	Retrieve guild membership detail for the currently logged in customer for the
	input companyId. Returns null if the guild membership detail record does not exist

	Throws UNAUTHENTICATED if customer is not authenticated
	Throws FORBIDDEN if the customer does not have access to retrieve the membership detail

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	guildMembershipDetail(companyId: ID!): GuildMembershipDetail
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	image(id: ID!): Image
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	imageGalleries(ids: [ID]!): [ImageGallery]!
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	imageGallery(id: ID!): ImageGallery
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	images(ids: [ID]!): [Image]!
	"""
	Metadata about a single interest.

	---------------------
	Graphlet Details:
	- Name: interests
	- CTI: IMDb/GraphQL/Interests
	- Affected by headers: x-imdb-client-name, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 600 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	interest(id: ID!): Interest
	"""
	A paginated list of all available interest categories, sorted alphabetically. Supports page size of at most 250.
	Requesting a page size larger than the maximum supported will result in the maximum size being returned.

	---------------------
	Graphlet Details:
	- Name: interests
	- CTI: IMDb/GraphQL/Interests
	- Affected by headers: x-imdb-client-name, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 600 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	interestCategories(
		after: ID
		filter: InterestCategoriesFilter
		first: Int!
	): InterestCategoryConnection
	"""
	Metadata about an array of interests.

	---------------------
	Graphlet Details:
	- Name: interests
	- CTI: IMDb/GraphQL/Interests
	- Affected by headers: x-imdb-client-name, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 600 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	interests(ids: [ID]!): [Interest]!
	"""
	Throws GRAPHQL_VALIDATION_FAILED if an invalid provider is provided.
	If a user with an existing linkage reaches this page, they will have to try login again.
	Returns an error page when user attempts to sign in with Google or Apple and does not have an existing linkage.

	---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	invalidAuthProviderInterstitial(
		input: InvalidAuthProviderInterstitialInput!
	): InvalidAuthProviderInterstitialOutput
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	keyword(id: ID!): Keyword
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	keywordMetadata: KeywordMetadata
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	keywords(ids: [ID]): [Keyword]!
	"""
	@entitlement(IMDB_STAFF)

	---------------------
	Graphlet Details:
	- Name: labs
	- CTI: IMDb/GraphQL/Labs
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-labs
	"""
	labs: LabsQuery!
	"""
	Paginated list of latest attachments sorted by attachment time (most recent first).

	An attachment is when a name is attached to a title.

	Supports page size of at most 250. Requesting a page size larger than the
	maximum supported will result in the maximum size being returned.

	Throws UNAUTHENTICATED if not logged in
	Throws FORBIDDEN if customer w/o any IMDbPro subscription
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	latestNameToTitleAttachments(
		after: String
		"""
		Provide options to filter down to a particular set of attachments
		"""
		attachmentConstraint: AttachmentSearchConstraint
		first: Int!
	): NameToTitleAttachmentConnection
	"""
	Return single list.

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	list(id: ID!): List
	"""
	Returns attribute metadata about the different fields of a list, allowing for users to determine the proper
	structure/standards for these fields (i.e. what is the maximum number of characters that a list name could be)

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	listFieldAttributeMetadata: ListFieldAttributeMetadata!
	"""
	Returns a list of public lists in modified date descending order.

	The lists can optionaly be searched by list name, description, or item description
	using a search term. Results can be filtered by modification date range.

	Supports page size of at most 250. Requesting a page size larger than the
	maximum supported will result in the maximum size being returned.

	Returns null if the customer is not authenticated.
	Returns null if the customer is not entitled.
	@entitlement(IMDB_STAFF)

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	listTextSearch(
		"""
		Cursor for pagination
		"""
		after: ID
		"""
		Text search constraints for lists
		"""
		constraints: ListTextSearchConstraint
		"""
		Maximum number of results to return
		"""
		first: Int!
	): ListCollectionConnection!
	"""
	Returns lists for the requested user filtered on provided input.
	The requested user is in the optional input listOwnerUserId if provided.
	Otherwise the requested user is the current authenticated user.
	listOwnerUserId can be omitted only when request is made with authenticated user.
	If listOwnerUserId is provided, whether user is currently authenticated or not, the result includes only public lists belongs to the listOwnerUserId.
	Otherwise the result includes lists with all visibilities belongs to the current authenticated user.
	If there's no matching list, it returns empty result.
	Default sort order is most recently modified first.

	This query is cacheable except when authenticated user loading their own lists (query made with authenticated user and without providing listOwnerUserId).

	Throws UNAUTHENTICATED if user is not logged in and listOwnerUserId is not provided.
	Throws BAD_USER_INPUT if the listOwnerUserId is not valid.
	Throws RESOURCE_NOT_FOUND if the listOwnerUserId doesn't exist.

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	lists(
		after: ID
		filter: ListFilter!
		first: Int!
		listOwnerUserId: ID
		sort: ListSort = { by: MODIFIED_DATE, order: DESC }
	): ListCollectionConnection!
	"""
	Returns a URL to redirect to for user account log out
	Throws GRAPHQL_VALIDATION_FAILED if an invalid association handle is provided.
	Throws GRAPHQL_VALIDATION_FAILED if returnURL is not an absolute URL or not an IMDb domain (ends with 'imdb.com') if passed

	---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	logoutRedirectURL(input: LogoutRedirectURLInput!): LogoutRedirectURLOutput
	"""
	Main search, searches one or multiple data types to find a mixed result set of answers.

	Note: Forward Pagination currently only works if you ask for one data type (eg, just NAMEs).
	Multi-index pagination might be added in the future.

	See: https://quip-amazon.com/Nsb2Ad66R8xF/Main-Search-Design

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	mainSearch(
		after: String
		first: Int!
		options: MainSearchOptions!
	): MainSearchConnection
	"""
	The viewer's blocked users list.
	Ordered by blocked date, most recent first.
	Up to 100 blocked users can be requested at a time. If first exceeds 100, only 100 are returned.
	If first is omitted, defaults to 5.
	Throws UNAUTHENTICATED if not logged in.
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	myBlockedUsers(after: ID, first: Int): ProNetworkingBlockedUsersConnection!
	"""
	The viewer's own connections tab: established connections and pending requests.
	Throws UNAUTHENTICATED if not logged in.
	@entitlement(PRO_ANY_TIER, PRO_PREMIUM)
	Premium users see full connections list. Basic tier users see a preview (up to 3 connections)
	and can approve/reject pending requests.

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	myConnectionsTab: ProNetworkingMyConnectionsTab!
	"""
	The viewer's connection privacy settings.
	Throws UNAUTHENTICATED if not logged in.
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: networking
	- CTI: IMDb/GraphQL/Networking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	myPrivacySettings: ProNetworkingPrivacySettings
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	name(id: ID!): Name
	"""
	Retrieves data associated with a specifc ranking chart in ordered ranking
	first: number of rankings to return for that chart. max is 250.
	input: type for querying certain ranking chart. Only India STARmeter chart is currently supported.

	---------------------
	Graphlet Details:
	- Name: trending
	- CTI: IMDb/GraphQL/Trending
	- Affected by headers: x-imdb-detected-country, x-imdb-user-country
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	nameChartRankings(
		first: Int!
		input: NameChartRankingsInput!
	): NameChartRankingsConnection
	"""
	Retrieve guild company affiliations for multiple names in a single request.
	Returns canonical (post-const-merge) Company entity references and Name entity
	references for each name's unsuppressed guild affiliations.

	Accepts up to 50 name IDs per request. Throws GRAPHQL_VALIDATION_FAILED if more
	than 50 name IDs are provided.

	Returns an empty guildCompanies array for names that have no guild affiliations
	or for name IDs that do not exist.

	Throws UNAUTHENTICATED if not logged in.
	Throws FORBIDDEN if not entitled to PRO_ANY_TIER.

	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	nameGuildCompanies(nameIds: [ID]!): [NameGuildCompanies]!
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	nameMetadata: NameMetadata
	"""
	Names recommended to the customer based on titles/names/other signals that the customer has shown interest in.

	Only for logged in customers (x-imdb-customer-id header; the gateway determines this from the request).

	Max returned count is 20. Pagination is currently not supported.

	For use in features recommending Names for the customer to favorite.

	---------------------
	Graphlet Details:
	- Name: personalization
	- CTI: IMDb/GraphQL/Personalized Picks
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-consent-info, x-imdb-detected-country, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	nameRecommendations: NameRecommendations
	"""
	Output will be in the same order as input, including remapped (returned as
	mapped id) and non-existent consts (returned with null fields)

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	names(ids: [ID]!): [Name]!
	"""
	Get cinemas in ascending distance from the request location.

	Notes:
	- If `filter.favorites` is specified as `ONLY_FAVORITES`, `filter.location.radiusInMeters` can be left as null to get all of a user's favorite cinemas.
	- If both lat/long and postal code are provided to `filter.location`, the location will default to using lat/long.
	- It's strongly recommended to explicitly provide startTime to avoid timezone mismatches between server and client, but if `filter.dateRange` is not provided, the startTime will default to server's 'now' and the endTime will default to `3000-01-01T00:00:00Z`.
	- If `filter.dateRange.endTime` is not provided, the endTime will default to `3000-01-01T00:00:00Z`.
	- Backwards pagination is not currently supported.

	Errors:
	- Throws `BAD_USER_INPUT` if `filter.location.radiusInMeters` is null AND filter.favorites` is not set to `ONLY_FAVORITES.
	- Throws `BAD_USER_INPUT` with symbol `filter.location.postalCode` if the location is unknown.
	- Throws `BAD_USER_INPUT` if lat/long is too precise. See `LatLong` input.

	Limits:
	- `first` is limited to a maximum of 100. If the `first` argument exceeds the maximum, the results will be capped to the maximum.

	---------------------
	Graphlet Details:
	- Name: titleAppointments
	- CTI: IMDb/GraphQL/Title Appointments
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	nearbyCinemas(
		after: ID
		filter: NearbyCinemasFilter!
		first: Int!
	): CinemaConnection
	"""
	Paginated list of latest news articles.
	Supports both Consumer and Pro news categories.

	---------------------
	Graphlet Details:
	- Name: bugle
	- CTI: IMDb/GraphQL/Bugle
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 900 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	news(after: String, category: NewsCategory!, first: Int!): NewsConnection
	"""
	---------------------
	Graphlet Details:
	- Name: bugle
	- CTI: IMDb/GraphQL/Bugle
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 900 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	newsArticle(id: ID!): News
	"""
	List of news categories along with additional metadata.

	The returned categoryIds can be used as inputs to the news() query.

	---------------------
	Graphlet Details:
	- Name: bugle
	- CTI: IMDb/GraphQL/Bugle
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 900 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	newsCategories(constraints: NewsCategoryConstraints): [NewsCategoryMetadata]!
	"""
	---------------------
	Graphlet Details:
	- Name: bugle
	- CTI: IMDb/GraphQL/Bugle
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 900 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	newsSource(id: ID!): NewsSource
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	nomination(id: ID!): Nomination
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	nominationEvent(id: ID!): NominationEvent
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	nominationEventEdition(id: ID!): NominationEventEdition
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	nominationEventEditions(ids: [ID]!): [NominationEventEdition]
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	nominationEvents(ids: [ID]!): [NominationEvent]
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	nominations(ids: [ID]!): [Nomination]
	"""
	Returns a paginated list of the requesting customer's notifications. By default, the list is sorted by last updated time.

	Supports page size of at most 250.  Requesting a page size larger than the
	maximum supported will result in the maximum size being returned.

	Throws UNAUTHENTICATED if neither x-imdb-customer-id nor x-amzn-sessionid is present in the header.

	---------------------
	Graphlet Details:
	- Name: notificationcenter
	- CTI: IMDb/GraphQL/NotificationCenter
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language, x-imdb-client-name, x-imdb-client-ip, user-agent
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	notifications(
		after: ID
		first: Int!
		sort: UserNotificationsSortOrder
	): UserNotificationConnection!
	"""
	@maxAuthAge=900
	Returns current settings for user's personal details.
	Throws UNAUTHENTICATED if customer needs to re-authenticate.

	---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	personalDetails: PersonalDetailsOutput!
	"""
	Returns the detail of a poll.

	---------------------
	Graphlet Details:
	- Name: polls
	- CTI: IMDb/GraphQL/Polls
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	poll(id: ID!): Poll
	"""
	Returns the list of poll admin activities (poll created/synced), from latest to earliest.
	Customer needs to have POLL_ADMIN entitlement to call this.

	Throws UNAUTHENTICATED if not logged in.
	@entitlement(POLL_ADMIN)

	---------------------
	Graphlet Details:
	- Name: polls
	- CTI: IMDb/GraphQL/Polls
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	pollAdminActivities(
		after: ID
		first: Int!
		useEntitlement: Boolean = true
	): PollAdminActivityConnection
	"""
	Returns a paginated list of the polls on IMDb. By default, the list is sorted by create time in descending order.
	Supports page size of at most 250. Requesting a page size larger than the maximum supported will result in the maximum size being returned.

	---------------------
	Graphlet Details:
	- Name: polls
	- CTI: IMDb/GraphQL/Polls
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	polls(
		after: ID
		filter: PollFilter
		first: Int!
		jumpToPosition: Int
		sort: PollSort = { by: CREATE_TIME, order: DESC }
	): PollsConnection
	"""
	Paginated list of most popular interests.
	Supports page size of at most 250. Requesting a page size larger than the
	maximum supported will result in the maximum size being returned.

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	popularInterests(after: String, first: Int!): InterestSearchConnection
	"""
	The most popular released movie or TV series titles, as determined by the movie meter ranking.
	If the specified limit is over the query's page limit (100), the limit will be capped at the query's limit
	To be replaced by topMeterTitles, which follows the Relay spec.

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	popularTitles(
		limit: Int!
		paginationToken: String
		queryFilter: PopularTitlesQueryFilter
	): PaginatedTitles
	"""
	Retrieve predefined user lists. Predefined User Lists have a ListClass of CHECK_INS, FAVORITE_ACTORS,
	FAVORITE_THEATRES, NOT_INTERESTED, RESEARCH_NOTES, SEEN, or WATCH_LIST.

	IMPORTANT: Predefined lists are NOT automatically created for users. They are created only when:
	- addItemToPredefinedList mutation is called (creates list if it doesn't exist)
	- createPredefinedList mutation is explicitly called

	When querying for a predefined list that doesn't exist, this query returns null (does not create the list).
	This distinction is critical for proper null-handling in client code to prevent infinite retry loops.

	Predefined lists can be accessed by userId and list class pair as there can only be one per user for a type.
	userId can be omitted only when the request is made while the authenticated user is present in the context (e.g., the user is logged in).
	If the predefined list does not exist for the user, it returns null instead of throwing an exception.

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	predefinedList(classType: ListClassId!, userId: ID): List
	"""
	Gets the privacy directives that must be enforced for the user
	Relevant headers: 'x-amzn-sessionid', 'x-imdb-customer-id', 'x-imdb-client-ip', 'x-imdb-client-name'

	---------------------
	Graphlet Details:
	- Name: privacy
	- CTI: IMDb/GraphQL/Privacy
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	privacyDirectives: PrivacyDirectivesOutput!
	"""
	If applicable, fetch the privacy prompt information that must be displayed to the user
	Relevant headers: 'x-amzn-sessionid', 'x-imdb-customer-id', 'x-imdb-client-ip', 'x-imdb-client-name'

	---------------------
	Graphlet Details:
	- Name: privacy
	- CTI: IMDb/GraphQL/Privacy
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	privacyPrompt(input: PrivacyPromptInput!): PrivacyPromptOutput!
	"""
	Query used to fetch profession detail

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	profession(id: ID!): NameProfession
	"""
	Query used to fetch profession Category detail.
	Output will be in the same order as input with non-existent consts being removed from response

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	professionCategories(ids: [ID]!): [ProfessionCategory]!
	"""
	Query used to fetch profession Category detail

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	professionCategory(id: ID!): ProfessionCategory
	"""
	Names people in a particular profession tend to track

	Throws UNAUTHENTICATED if logged out
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: track
	- CTI: IMDb/GraphQL/Track
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	professionNameTrackRecommendations(
		"""
		Pagination cursor
		"""
		after: ID
		"""
		Number of required track recommendations. Default is 200. Max is 999.
		"""
		first: Int
		input: TrackRecommendationsInput!
	): NameTrackRecommendationsConnection
	"""
	Titles people in a particular profession tend to track
	Throws UNAUTHENTICATED if logged out
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: track
	- CTI: IMDb/GraphQL/Track
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	professionTitleTrackRecommendations(
		"""
		Pagination cursor
		"""
		after: ID
		"""
		Number of required track recommendations. Default is 200. Max is 999.
		"""
		first: Int
		input: TrackRecommendationsInput!
	): TitleTrackRecommendationsConnection
	"""
	Query used to fetch profession detail.
	Output will be in the same order as input with non-existent consts being removed from response

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	professions(ids: [ID]!): [NameProfession]!
	"""
	This query is used to fetch the Promoted Video Ad if one exists for the client
	If there is an adOverrideCreativeId or x-imdb-adsystem-overrides, it will instead fetch that Promoted Video Ad
	Relevant headers: 'user-agent', 'x-amzn-sessionid', 'x-imdb-adsystem-overrides', 'x-imdb-client-ip', 'x-imdb-customer-id'

	---------------------
	Graphlet Details:
	- Name: advertising
	- CTI: IMDb/GraphQL/Advertising
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-adsystem-overrides, x-imdb-client-ip, x-imdb-client-name, x-imdb-client-version, x-imdb-consent-info, x-imdb-detected-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-adtech-public
	"""
	promotedVideoAd(input: PromotedVideoAdParameters!): PromotedVideoAd
	"""
	@entitlement(IMDB_STAFF)
	Determines if a user is eligible for a public profile.
	Returns eligibility status and profile ID if eligible.

	---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	publicProfileEligibility(
		input: PublicProfileEligibilityInput!
	): PublicProfileEligibilityOutput!
	"""
	Field exposing push notification information for authenticated users.
	Does not expose any param.
	Returns null if user not signed in.

	---------------------
	Graphlet Details:
	- Name: pushNotification
	- CTI: IMDb/GraphQL/Push Notification
	- Affected by headers: x-imdb-customer-id
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-engagement-growth-public
	"""
	pushNotificationUserSettings: PushNotificationUserSettings
	"""
	Returns a ranked collection of public lists ordered by engagement signals.

	Lists are ranked by a composite engagement score (listScore) incorporating views,
	saves, and reactions. Only public, trusted-owner, non-flagged lists are returned.

	Supports a maximum page size of 50. Requesting a page size larger than 50 will
	be capped at 50.

	Results are cached for 1 hour.

	Throws GRAPHQL_VALIDATION_FAILED if first is not a positive integer.
	Throws GRAPHQL_VALIDATION_FAILED if after cursor is invalid or malformed.

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	rankedListSearch(
		"""
		Cursor for forward pagination. When omitted, results start from the beginning of the result set.
		"""
		after: ID
		"""
		Constraints for filtering the ranked list results
		"""
		constraints: RankedListSearchConstraints!
		"""
		Maximum number of results to return (capped at 50).
		Must be a positive integer. Throws GRAPHQL_VALIDATION_FAILED if first is not positive.
		If first exceeds 50, only 50 results will be returned.
		"""
		first: Int!
		"""
		Sort order for results. When omitted, defaults to sorting by RELEVANCE in DESC order.
		"""
		sort: RankedListSearchSort
	): RankedListSearchConnection!
	"""
	Batch query for reactions on any IMDb entity types (videos, lists, titles, etc.)
	Returns reactions for multiple entities in a single call

	Maximum 100 entityIds per request

	---------------------
	Graphlet Details:
	- Name: reactions
	- CTI: IMDb/GraphQL/Reactions
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-video
	"""
	reactionsForEntities(entityIds: [ID]!): [EntityReactions]!
	"""
	Retrieves the latest videos that has been added to the video catalog.

	If the limit exceeds the maximum, the results will be capped to 100.
	Optionally, a list of VideoContentType can be provided to be used as a filter. If none are provided,
	we will return all results. When a pagination token is provided, the query filter will be ignored.

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	recentVideos(
		limit: Int!
		paginationToken: String
		queryFilter: RecentVideosQueryFilter
	): PaginatedVideos
	"""
	Retrieves RecentlyViewed items.
	Can query views for specific site (default is Consumer site).

	For Consumer page views:
	- Based on SessionId for all users
	- Max batch size is 100 items
	- Returns name and title consts

	For Pro page views:
	- Based on CustomerId for all users
	- Max batch size is 20 items
	- Returns name, title, and company consts

	---------------------
	Graphlet Details:
	- Name: personalization
	- CTI: IMDb/GraphQL/Personalized Picks
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-consent-info, x-imdb-detected-country, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	recentlyViewedItems(
		after: ID
		filter: RecentlyViewedItemsFilter
		first: Int!
	): RecentlyViewedConnection
	"""
	Returns news articles related to names, titles, and/or companies
	mentioned in the article.

	A maximum of 3 total related name, title, and/or company consts will be returned.
	For each related const, a maximum of 3 news items will be returned.

	For example, for an article that mentions Keanu Reeves and The Matrix,
	the response could include a list of 3 articles related to Keanu Reeves
	and a list of 3 articles related to The Matrix.

	---------------------
	Graphlet Details:
	- Name: bugle
	- CTI: IMDb/GraphQL/Bugle
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 900 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	relatedNews(id: ID!): [RelatedNews]
	"""
	Accepts any markdown string to be rendered

	---------------------
	Graphlet Details:
	- Name: markdown
	- CTI: IMDb/GraphQL/Markdown
	- Affected by headers: x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	renderedMarkdown(markdownString: String!): Markdown
	"""
	@maxAuthAge=900
	Retrieves the requested account data after data processing triggered by `confirmAccountDataRequest` mutation is complete.
	Relevant headers: 'x-imdb-customer-id'
	If re-authentication is successful, the function returns the downloadable data
	information through fileMetadata, success will be true,
	and a success text will be in message and a success title text in title.
	If it was unsuccessful, success will be false and an error text will be in message
	and an error title text will be in title. fileMetadata will have its values set to empty.
	Throws UNAUTHENTICATED if the user is not logged in or fails 15 minute maxAuthAge check.
	Throws FORBIDDEN if logged in user doesn't match the customerID from the initial request.
	Throws INVALID_INPUT_EXCEPTION if dataRequestId provided is invalid.

	---------------------
	Graphlet Details:
	- Name: privacy
	- CTI: IMDb/GraphQL/Privacy
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	retrieveAccountData(
		input: RetrieveAccountDataInput!
	): RetrieveAccountDataOutput!
	"""
	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	review(id: ID!): Review
	"""
	Get paginated saved search filters, of a given input SavedSearchFilterType, for a customerId.
	Sorted by updatedTimestamp, descending.
	Customer ID is taken from the request header, not as part of this input.

	Supports page size of at most 250. Requesting a page size larger than the
	maximum supported will result in the maximum size being returned.

	Throws UNAUTHENTICATED if user is not logged in.
	Throws BAD_USER_INPUT if the customerId is not valid.
	Throws RESOURCE_NOT_FOUND if the customerId doesn't exist.

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	savedSearchFilters(
		"""
		Next page cursor for pagination.
		"""
		after: String
		"""
		The first N filters per this query.
		"""
		first: Int!
		"""
		The type of filters to return, see: SavedSearchFilterType
		"""
		type: SavedSearchFilterType!
	): SavedSearchFiltersConnection
	"""
	Data that can be useful for crafting search queries.

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	searchMetadata: SearchMetadata
	"""
	Metadata for some self-verified name data attributes

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	selfVerifiedNameMetadata: SelfVerifiedNameMetadata
	"""
	The current shared editing status for the authenticated customer's name page.

	Throws UNAUTHENTICATED if customer is not authenticated.
	Throws FORBIDDEN if the customer does not have the required entitlement.

	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	sharedEditing: SharedEditingInfo
	"""
	Retrieves Showtimes titles. Max batch size is 100 titles. We currently do not support pagination in existing
	showtimes API but hope to in the future.

	Throws if `location.radiusInMeters` is null.

	---------------------
	Graphlet Details:
	- Name: titleAppointments
	- CTI: IMDb/GraphQL/Title Appointments
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	showtimesTitles(
		first: Int!
		location: ShowtimesLocation!
		queryMetadata: ShowtimesTitlesQueryMetadata!
	): ShowtimesTitleConnection
	"""
	Retrieves titles currently showing in the specified cinemas.

	Notes:
	- It's strongly recommended to explicitly provide startTime to avoid timezone mismatches between server and client, but if `filter.dateRange` is not provided, the startTime will default to server's 'now' and the endTime will default to `3000-01-01T00:00:00Z`.
	- If `filter.dateRange.endTime` is not provided, the endTime will default to `3000-01-01T00:00:00Z`.
	- We currently do not support pagination in existing showtimes API but hope to in the future.

	Sorting:
	- There is no guaranteed sort order for results returned.

	Throws:
	- Throws `GRAPHQL_VALIDATION_FAILED` if `cinemasMetadata.anyCinemaIds` is null.
	- Throws `BAD_USER_INPUT` if more than 400 unique cinemas are provided in `cinemasMetadata.anyCinemaIds`.

	---------------------
	Graphlet Details:
	- Name: titleAppointments
	- CTI: IMDb/GraphQL/Title Appointments
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	showtimesTitlesByCinemas(
		cinemasMetadata: ShowtimesTitlesCinemasMetadata!
		first: Int
	): ShowtimesTitleConnection
	"""
	Returns a single sign in option redirect URL based on association handle from login context
	The associationHandle in the input will affect the single sign in option returned
	Throws GRAPHQL_VALIDATION_FAILED if an invalid association handle is provided.
	Throws GRAPHQL_VALIDATION_FAILED if returnURL is not an absolute URL or not an IMDb domain (ends with 'imdb.com') if passed
	Throws GRAPHQL_VALIDATION_FAILED if no additionalWebAuthContext is given and associationHandle in input is an association handle
	    used in AAP (Amazon account pool) creation/conversion i.e. 'imdb_amazon', 'imdb_pro_amazon', etc.
	Throws GRAPHQL_VALIDATION_FAILED if an invalid signInPrefilledEmail is provided.

	---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	signInOptionRedirectURL(
		input: SignInOptionRedirectURLInput!
	): SignInOptionRedirectURLOutput
	"""
	Returns sign in options redirect URLs based on association handle from login context
	The associationHandle in the input will only affect the 1P IMDb sign in option, all of the other 3P logins will be configured with their own association handles
	Throws GRAPHQL_VALIDATION_FAILED if an invalid association handle is provided.
	Throws GRAPHQL_VALIDATION_FAILED if returnURL is not an absolute URL or not an IMDb domain (ends with 'imdb.com') if passed

	---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	signInOptionsRedirectURLs(
		input: SignInOptionsRedirectURLsInput!
	): SignInOptionsRedirectURLsOutput
	"""
	Names people that tend to track similar pages track
	Throws UNAUTHENTICATED if logged out
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: track
	- CTI: IMDb/GraphQL/Track
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	similarNameTrackRecommendations(
		"""
		Pagination cursor
		"""
		after: ID
		"""
		Number of required track recommendations. Default is 200. Max is 999.
		"""
		first: Int
	): NameTrackRecommendationsConnection
	"""
	Titles people that tend to track similar pages track
	Throws UNAUTHENTICATED if logged out
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: track
	- CTI: IMDb/GraphQL/Track
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	similarTitleTrackRecommendations(
		"""
		Pagination cursor
		"""
		after: ID
		"""
		Number of required track recommendations. Default is 200. Max is 999.
		"""
		first: Int
	): TitleTrackRecommendationsConnection
	"""
	Returns the SSO unlinking interstitial shown to customers who were automatically signed in via Amazon SSO.
	Provides a bottom sheet with options to continue browsing, sign in another way, or sign out.

	Throws UNAUTHENTICATED if the customer is not logged in.

	---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	singleSignOnUnlinkingInterstitial: SingleSignOnUnlinkingInterstitialOutput
	"""
	---------------------
	Graphlet Details:
	- Name: zukoTest
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: x-imdb-user-language, x-imdb-test-timestamp
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	staticSchemaTest: Test
	"""
	Gets the StreamingTitles for the providers applicable for this user/territory
	Since this is a personalized selection it will always be a finite size (typically
	less than 15 providers worth)
	Caller may optionally filter to only a select set of providers if the IDs have
	already been obtained
	Throws InvalidParameterError if filter is for TitleTypeCategoryValue other than tv or movie

	---------------------
	Graphlet Details:
	- Name: personalization
	- CTI: IMDb/GraphQL/Personalized Picks
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-consent-info, x-imdb-detected-country, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	streamingTitles(filter: StreamingTitlesFilter): [StreamingTitles]!
	"""
	---------------------
	Graphlet Details:
	- Name: zukoStubs
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: None
	- Cache TTL: 300 seconds
	- Slack Channel: #imdb-api-gateway-external
	"""
	stubQuery: QueryStubs
	"""
	Retrieve subscription alerts for the current user.
	Returns alerts related to subscription status, payment issues, and subscription notifications.
	If a User has no subscription alerts, returns null.

	Alerts are returned in display order, in relative order of importance.
	A subscription can only have between 0-2 alerts, and all must be shown, so limits and pagination are not required.

	If not logged in, or customer has no alerts, will return an empty list of alerts.
	The Entitlement is not a gate on the API; it's input to the logic.

	@entitlement(NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: subscription
	- CTI: IMDb/GraphQL/Subscription Status
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-payment-technology-external
	"""
	subscriptionAlerts(
		"""
		Optional result override for testing purposes.
		When provided, returns mock data for the specified result ID.
		"""
		resultOverride: ID
	): [SubscriptionAlert]
	"""
	Top search suggestions

	This suggestionSearch query is used by IMDb Consumer and "Pro" products for filling in suggestions for the search bar.
	More info at https://w.amazon.com/bin/view/IMDb/Search/Suggestion/
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	suggestionSearch(
		filter: SuggestionSearchFilter
		first: Int!
		options: SuggestionSearchOptions
		searchTerm: String!
		shouldShowOriginalTitles: Boolean
	): SuggestionSearchConnection
	"""
	Retrieves a list of placements from Content Symphony.

	This graphlet utilizes dynamic caching (https://w.amazon.com/bin/view/IMDb/Zuko/DynamicGraphletResponseCaching)
	The cached flag is false by default, customer and session IDs WILL be used to fetch placements,
	and the response WILL NOT be cached. If the cached flag is true,
	then customer and session IDs WILL NOT be used, and the response WILL be cached.

	If useEntitlement is set true, pro subscription tier will be considered when checking for scheduled content.

	@entitlement(PRO_PREMIUM, PRO_BASIC)

	---------------------
	Graphlet Details:
	- Name: contentSymphony
	- CTI: IMDb/GraphQL/ContentSymphony
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-detected-country, x-imdb-client-name, x-imdb-client-ip, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-foundation-web-platform-office-hours
	"""
	symphonyPlacements(
		cached: Boolean = false
		input: GetPlacementsInput!
		useEntitlement: Boolean = false
	): [SymphonyPlacement]
	"""
	---------------------
	Graphlet Details:
	- Name: zukoTest
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: x-imdb-user-language, x-imdb-test-timestamp
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	test: Test
	"""
	---------------------
	Graphlet Details:
	- Name: zukoAuthTest
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-ip, x-imdb-detected-country, x-imdb-test-timestamp
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	testAuth: TestAuth
	"""
	@maxAuthAge=120

	---------------------
	Graphlet Details:
	- Name: zukoAuthTest
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-ip, x-imdb-detected-country, x-imdb-test-timestamp
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	testAuthTimer: TestAuthTimer
	"""
	True if granted entitlement
	@entitlement(PREMIUM_OR_NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: zukoAuthTest
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-ip, x-imdb-detected-country, x-imdb-test-timestamp
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	testCombinedEntitlement: Boolean
	"""
	Each entry responds to input array, true if granted entitlement
	@entitlement(NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: zukoAuthTest
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-ip, x-imdb-detected-country, x-imdb-test-timestamp
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	testContextEntitlement(ids: [ID]!): [Boolean]!
	"""
	True if granted entitlement
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: zukoAuthTest
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-ip, x-imdb-detected-country, x-imdb-test-timestamp
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	testEntitlement: Boolean
	"""
	Test field with useEntitlement defaulting to false (query author can opt in)
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: zukoAuthTest
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-ip, x-imdb-detected-country, x-imdb-test-timestamp
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	testEntitlementWithOptIn(id: String!, useEntitlement: Boolean = false): String
	"""
	Test field with useEntitlement defaulting to true (query author can opt out)
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: zukoAuthTest
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-ip, x-imdb-detected-country, x-imdb-test-timestamp
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	testEntitlementWithOptOut(id: String!, useEntitlement: Boolean = true): String
	"""
	All received entitlements.
	@entitlement(NAME_OWNER,PRO_ANY_TIER,PRO_PREMIUM,PREMIUM_OR_NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: zukoAuthTest
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-ip, x-imdb-detected-country, x-imdb-test-timestamp
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	testEntitlements: [TestEntitlement]
	"""
	Throws an error of the specified type. Used by integration tests to verify gateway error handling.
	For UNAUTHENTICATED: throws when auth headers are absent, returns 'authenticated' otherwise.
	All other types always throw.

	---------------------
	Graphlet Details:
	- Name: zukoAuthTest
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-ip, x-imdb-detected-country, x-imdb-test-timestamp
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	testError(errorType: TestErrorType!): String
	"""
	Returns a large (>1KB) public cacheable string. Used by integration tests to verify response compression.

	---------------------
	Graphlet Details:
	- Name: zukoAuthTest
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-ip, x-imdb-detected-country, x-imdb-test-timestamp
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	testLargeData: String
	"""
	True if granted entitlement
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: zukoAuthTest
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-ip, x-imdb-detected-country, x-imdb-test-timestamp
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	testProAnyTierEntitlement: Boolean
	"""
	True if granted entitlement
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: zukoAuthTest
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-ip, x-imdb-detected-country, x-imdb-test-timestamp
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	testProPremiumEntitlement: Boolean
	"""
	Returns a public cacheable string. Used by integration tests to verify public cache-control headers, legal disclaimer, and region headers.

	---------------------
	Graphlet Details:
	- Name: zukoAuthTest
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-ip, x-imdb-detected-country, x-imdb-test-timestamp
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	testPublicCacheable: String
	"""
	Test field for employee entitlement validation
	@entitlement(IMDB_STAFF)

	---------------------
	Graphlet Details:
	- Name: zukoAuthTest
	- CTI: IMDb/GraphQL/Gateway
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-ip, x-imdb-detected-country, x-imdb-test-timestamp
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-api-gateway-external
	"""
	testStaffEntitlement: Boolean
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	title(id: ID!): Title
	"""
	Retrieves data associated with a specifc ranking chart in ordered ranking
	first: number of rankings to return for that chart
	after: pagination token to begin returning ranking set (Optional)
	input: type for querying certain ranking chart

	---------------------
	Graphlet Details:
	- Name: trending
	- CTI: IMDb/GraphQL/Trending
	- Affected by headers: x-imdb-detected-country, x-imdb-user-country
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	titleChartRankings(
		after: ID
		first: Int!
		input: TitleChartRankingsInput!
	): TitleChartRankingsConnection
	"""
	Titles recommended based on title genre a customer has shown interest in. Only for logged in customers.
	Max batch size is 30 items.
	Will return an UNAUTHENTICATED error code if no customer associated with the request.

	For use in Genre recommendation features.

	---------------------
	Graphlet Details:
	- Name: personalization
	- CTI: IMDb/GraphQL/Personalized Picks
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-consent-info, x-imdb-detected-country, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	titleGenreRecommendations: TitleGenreRecommendation
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	titleMetadata: TitleMetadata
	"""
	Titles recommended based on activity, either by user id when available, or session id otherwise.

	For use in Top Picks features.

	Providing placement context in request generates different title recommendations based on input. If no placement
	context is provided, or pageType passed in is not supported, the default behavior is optimized for home page
	placement, which is today's behavior. Currently supported page types are: home, title.

	Providing filter input params filters title recommendations response based on filtering criteria. Currently we support
	filtering by title type movie or tv.

	---------------------
	Graphlet Details:
	- Name: personalization
	- CTI: IMDb/GraphQL/Personalized Picks
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-consent-info, x-imdb-detected-country, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	titleRecommendations(
		after: String
		filter: TitleRecommendationsFilter
		first: Int!
		placementContext: PlacementContext
	): TitleRecommendationConnection
	"""
	Output will be in the same order as input, including remapped (returned as
	mapped id) and non-existent consts (returned with null fields)

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	titles(ids: [ID]!): [Title]!
	"""
	Top grossing releases at the Box Office for the specified filter.
	Currently there will be at most 10 releases.

	---------------------
	Graphlet Details:
	- Name: mojo
	- CTI: IMDb/GraphQL/Mojo
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	topGrossingReleases(
		after: ID
		filter: TopGrossingReleasesFilter!
		first: Int!
	): TopGrossingReleasesConnection
	"""
	Returns a collection of top Lists
	first can have a maximum value of <LIMIT>

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	topListsForItem(
		first: Int!
		itemId: String!
		topListType: TopListType!
	): ListCollectionConnection!
	"""
	The most popular names, sorted by StarMeter.
	Max page size supported is 250.

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	topMeterNames(after: String, first: Int!): NameSearchConnection
	"""
	The most popular titles, sorted by MovieMeter.
	Max page size supported is 250.

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	topMeterTitles(
		after: String
		filter: TopMeterTitlesFilter
		first: Int!
	): TitleSearchConnection
	"""
	Prefer titleRecommendations, which includes recommendation explanations

	---------------------
	Graphlet Details:
	- Name: personalization
	- CTI: IMDb/GraphQL/Personalized Picks
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-consent-info, x-imdb-detected-country, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	topPicksTitles(after: ID, first: Int!): TopPicksConnection
	"""
	---------------------
	Graphlet Details:
	- Name: trending
	- CTI: IMDb/GraphQL/Trending
	- Affected by headers: x-imdb-detected-country, x-imdb-user-country
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	topTrendingNames(
		after: ID
		first: Int!
		input: TopTrendingInput!
	): TrendingNameConnection
	"""
	---------------------
	Graphlet Details:
	- Name: trending
	- CTI: IMDb/GraphQL/Trending
	- Affected by headers: x-imdb-detected-country, x-imdb-user-country
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	topTrendingSetsPredefined(
		after: ID
		first: Int!
		input: TopTrendingSetsPredefinedInput!
	): TrendingTitleConnection
	"""
	---------------------
	Graphlet Details:
	- Name: trending
	- CTI: IMDb/GraphQL/Trending
	- Affected by headers: x-imdb-detected-country, x-imdb-user-country
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	topTrendingTitles(
		after: ID
		first: Int!
		input: TopTrendingInput!
	): TrendingTitleConnection
	"""
	---------------------
	Graphlet Details:
	- Name: trending
	- CTI: IMDb/GraphQL/Trending
	- Affected by headers: x-imdb-detected-country, x-imdb-user-country
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	topTrendingVideos(
		after: ID
		first: Int!
		input: TopTrendingInput!
	): TrendingVideoConnection
	"""
	List out a customer's tracked item notifications, sorted by most recent first.
	Throws UNAUTHENTICATED if logged out
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: track
	- CTI: IMDb/GraphQL/Track
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	trackNotifications(
		"""
		Pagination cursor
		"""
		after: ID
		"""
		Number of track notifications to return. Default is 4. Max is 100.
		"""
		first: Int
	): TrackNotificationConnection
	"""
	The requesting user's tracked names
	Throws UNAUTHENTICATED if logged out
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: track
	- CTI: IMDb/GraphQL/Track
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	trackedNames(
		"""
		Pagination cursor
		"""
		after: ID
		"""
		Number of required tracked names. Default is 500. Max is 2500.
		"""
		first: Int
	): TrackedNamesConnection
	"""
	The requesting user's tracked titles
	Throws UNAUTHENTICATED if logged out
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: track
	- CTI: IMDb/GraphQL/Track
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	trackedTitles(
		"""
		Pagination cursor
		"""
		after: ID
		"""
		Number of required tracked titles. Default is 500. Max is 2500.
		"""
		first: Int
	): TrackedTitlesConnection
	"""
	---------------------
	Graphlet Details:
	- Name: trending
	- CTI: IMDb/GraphQL/Trending
	- Affected by headers: x-imdb-detected-country, x-imdb-user-country
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	trendingNamesCollectionOptions: TrendingNameCollectionOptions
	"""
	---------------------
	Graphlet Details:
	- Name: trending
	- CTI: IMDb/GraphQL/Trending
	- Affected by headers: x-imdb-detected-country, x-imdb-user-country
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	trendingTitles(limit: Int!, paginationToken: String): PaginatedTitles
	"""
	---------------------
	Graphlet Details:
	- Name: trending
	- CTI: IMDb/GraphQL/Trending
	- Affected by headers: x-imdb-detected-country, x-imdb-user-country
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	trendingTitlesCollectionOptions: TrendingTitleCollectionOptions
	"""
	Query to return the unread notification count for the customer. 0 means no unread notification.

	The unread status is determined by the last time a customer visited their notification center. Clients should call
	updateNotificationCenterAccess to update it at proper times.

	Throws UNAUTHENTICATED if neither x-imdb-customer-id nor x-amzn-sessionid is present in the header.

	---------------------
	Graphlet Details:
	- Name: notificationcenter
	- CTI: IMDb/GraphQL/NotificationCenter
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language, x-imdb-client-name, x-imdb-client-ip, user-agent
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	unreadNotificationCount: Int!
	"""
	Unreleased titles people in a particular profession tend to track
	Throws UNAUTHENTICATED if logged out
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: track
	- CTI: IMDb/GraphQL/Track
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	unreleasedTitleTrackRecommendations(
		"""
		Pagination cursor
		"""
		after: ID
		"""
		Number of required track recommendations. Default is 200. Max is 999.
		"""
		first: Int
		input: TrackRecommendationsInput!
	): TitleTrackRecommendationsConnection
	"""
	Field exposing information for requestingUserId. Does not expose any param
	Includes private information for signed in user
	Returns null if user not signed in

	---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	user: User
	"""
	Get all earned badges, for either the authenticated user or for the optionally given userid
	Ordering handled by service ranking.

	100 badges are supported initially.

	Throws UNAUTHENTICATED if userId is not provided and the x-imdb-customer-id header is empty.
	Throws UNSUPPORTED_IMAGE_TYPE if service cannot provide the requested image type

	---------------------
	Graphlet Details:
	- Name: badges
	- CTI: IMDb/GraphQL/Badges
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pwr-public
	"""
	userBadges(after: ID, first: Int!, input: UserBadgesInput!): BadgeConnection
	"""
	Fetch whether the current user is opted in or out of a consent.

	---------------------
	Graphlet Details:
	- Name: privacy
	- CTI: IMDb/GraphQL/Privacy
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	userConsent(input: UserConsentInput!): UserConsentOutput
	"""
	@maxAuthAge=900
	Returns status and linking information for all auth providers for the current user.
	Throws UNAUTHENTICATED if customer needs to re-authenticate.
	Throws GRAPHQL_VALIDATION_FAILED if returnURL is not an absolute URL or not an IMDb domain (ends with 'imdb.com') if passed

	---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	userLinkedAuthProviderStatuses(
		input: UserLinkedAuthProviderStatusesInput
	): UserLinkedAuthProviderStatusesOutput
	"""
	Returns a filtered and sorted list of lists for a user

	There are two behaviors
	  1. authenticated user - retrun private and public lists
	  2. listOwnerUserId - return public lists owned by the user
	  Note: listOwnerUserId takes precendence over the authenticated user

	This query currently uses non-cached endpoint as IMDbSearchGraphlet does not support caching for List now.
	This query is designed for search scenarios, leveraging the search index to deliver eventual consistency while
	offering an efficient and cost effective approach to filtering and sorting user lists.

	For strict consistency requirements, the following APIs offer direct access to lists from the primary data store:
	1. lists API: Retrieves the lists for a user.
	2. predefinedList API: Retrieves a specific predefined list for a user.
	3. list API: Retrieves a single list for a list id.

	Throws UNAUTHENTICATED if user is not logged in and listOwnerUserId is not provided.
	Throws BAD_USER_INPUT if the listOwnerUserId is not valid.
	Throws RESOURCE_NOT_FOUND if the listOwnerUserId doesn't exist.

	---------------------
	Graphlet Details:
	- Name: search
	- CTI: IMDb/GraphQL/Search
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language, x-imdb-normalized-languages
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	userListSearch(
		after: ID
		filter: ListSearchFilter
		first: Int!
		listOwnerUserId: ID
		sort: ListSearchSort
	): ListCollectionConnection!
	"""
	Returns the status of a mark-all-as-watched job for the current customer.
	CustomerId will be taken from x-imdb-customer-id header.
	The job is identified by the customer and list, matching the input used in submitMarkAllAsWatched.

	Failed jobs are safe to retry by calling submitMarkAllAsWatched again.
	The system deduplicates concurrent requests so only one job runs at a time.
	Automatic retries are attempted if job fails.

	- Throws UNAUTHENTICATED for non-authenticated requests.
	- Throws RESOURCE_NOT_FOUND if no job exists for this customer and list.
	- Throws VALIDATION_ERROR for unsupported listClass values.
	- Throws VALIDATION_ERROR if listClass is LIST and listId is not provided.
	- Throws VALIDATION_ERROR if listClass is WATCHLIST and listId is provided.

	---------------------
	Graphlet Details:
	- Name: watched
	- CTI: IMDb/GraphQL/Watched
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	userMarkAllAsWatchedJobStatus(
		input: MarkAllAsWatchedInput!
	): MarkAllAsWatchedJobStatusResponse!
	"""
	Returns all Consumer notification topic preferences for the authenticated user, both enabled and disabled.

	Throws UNAUTHENTICATED if x-imdb-customer-id is not present in the header.

	---------------------
	Graphlet Details:
	- Name: notificationPreferences
	- CTI: IMDb/GraphQL/Notification Preferences
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	userNotificationTopics: [UserNotificationTopic]!
	"""
	Returns all Pro notification topic preferences for the authenticated user, both enabled and disabled.

	Throws UNAUTHENTICATED if x-imdb-customer-id is not present in the header.

	---------------------
	Graphlet Details:
	- Name: notificationPreferences
	- CTI: IMDb/GraphQL/Notification Preferences
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	userProNotificationTopics: [UserNotificationTopic]!
	"""
	Returns a user's profile.

	When the optional profileId is provided, the public version of that user's profile is returned.
	When no profileId is provided, authentication is required and the current user's profile is returned.
	This query is not cacheable when profileId is omitted for authenticated users viewing their own profile.

	Possible Errors:
	- Throws UNAUTHENTICATED if the user is not logged-in and no profileId is provided.
	- Throws FORBIDDEN if a non-null userId is provided (userId input is blocked to prevent scraping).
	- Throws BAD_USER_INPUT if the profileId resolves to an invalid userId.
	- Throws RESOURCE_NOT_FOUND if the profileId does not resolve to an existing user.

	---------------------
	Graphlet Details:
	- Name: userProfile
	- CTI: IMDb/GraphQL/UserProfile
	- Affected by headers: x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	userProfile(input: UserProfileInput): UserProfile
	"""
	Find a user's title ratings based on a variety of constraints

	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	userRatings(
		after: String
		"""
		Provide options to filter down to a particular set of titles
		"""
		constraints: RatingsConstraints
		first: Int!
		"""
		Defaults to top rated sort (sortBy: TOP_RATED sortOrder: ASC)
		"""
		sort: RatingsSort
		"""
		The user id we are searching ratings for, if it's not provided the
		requesting user's id is used. If the provided user is invalid or the requested
		user's ratings are private and not the requesting user, an error will be returned
		identifying which case occurred.
		- Throws BAD_USER_INPUT if user ID is invalid
		- Throws FORBIDDEN if user is not authorized to retrieve ratings
		"""
		userId: ID
	): RatingsConnection
	"""
	Find a user's title reviews based on a variety of constraints

	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	userReviews(
		after: String
		first: Int
		input: UserReviewsInput
	): ReviewsConnection!
	"""
	Returns the list of polls a customer has voted on, from latest to earliest.

	---------------------
	Graphlet Details:
	- Name: polls
	- CTI: IMDb/GraphQL/Polls
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	userVotedPolls(after: ID, first: Int!, userId: ID!): PollsConnection
	"""
	Returns a customer's watched titles privacy setting.
	If profileId is provided, returns the watched privacy of that profile.
	If profileId is not provided, returns the watched privacy of the current signed-in customer
	(customerId will be taken from x-imdb-customer-id header).

	- Throws UNAUTHENTICATED for non-authenticated requests if profileId is not provided.

	---------------------
	Graphlet Details:
	- Name: watched
	- CTI: IMDb/GraphQL/Watched
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	userWatchedPrivacy(
		"""
		The profileId we are searching watched privacy for, could accept either userId or profileId.
		"""
		profileId: ID
	): WatchedPrivacy!
	"""
	Returns watched titles and watched status for the current signed-in customer
	(customerId taken from x-imdb-customer-id header).
	first would have a maximum value of 1000.

	- Throws UNAUTHENTICATED for non-authenticated requests.

	---------------------
	Graphlet Details:
	- Name: watched
	- CTI: IMDb/GraphQL/Watched
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	userWatchedTitles(
		after: ID
		first: Int!
		input: UserWatchedTitlesInput
	): WatchedTitlesConnection!
	"""
	Information about a vanity URL

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	vanityUrl(urlPath: String): VanityUrl
	"""
	All verified partner affiliations (guilds, unions, and associations) currently active in the system.
	The list contains only admin-curated verified affiliations and is expected to be small enough to return in a single request.
	Returns null if the user is not entitled.

	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: managedCompany
	- CTI: IMDb/GraphQL/Managed Company
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	verifiedPartnerAffiliations: [Company]
	"""
	Retrieves a Video by its Id

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	video(id: ID!): Video
	"""
	This query is used to fetch an ad feedback URL for a given video ad

	---------------------
	Graphlet Details:
	- Name: advertising
	- CTI: IMDb/GraphQL/Advertising
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-adsystem-overrides, x-imdb-client-ip, x-imdb-client-name, x-imdb-client-version, x-imdb-consent-info, x-imdb-detected-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-adtech-public
	"""
	videoAdFeedbackUrl(input: VideoAdFeedbackUrlInput!): URL!
	"""
	Returns video items, with ad display indicators intermixed
	This query considers customer and session states

	first: number of items requested
	after: cursor for pagination. the value is from previous page's endCursor
	context: additional context used to generate the response

	---------------------
	Graphlet Details:
	- Name: personalization
	- CTI: IMDb/GraphQL/Personalized Picks
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-consent-info, x-imdb-detected-country, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	videoRecommendations(
		after: ID
		context: VideoRecommendationsContext!
		first: Int!
	): VideoRecommendationsConnection
	"""
	Retrieves Videos for all given video Ids. Maximum batch size is 100 videos

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	videos(ids: [ID]!): [Video]!
	"""
	Paginated list of all available watch providers in the requester's location, sorted alphabetically.
	Maximum page size supported is 250.

	---------------------
	Graphlet Details:
	- Name: watch
	- CTI: IMDb/GraphQL/Watch
	- Affected by headers: x-imdb-client-name, x-imdb-client-version, x-imdb-detected-country, x-imdb-detected-time-zone, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	watchProviders(
		after: ID
		filter: WatchProvidersQueryFilter
		first: Int!
	): WatchProviderConnection
	"""
	Query used to fetch display ads for a specific web page.
	Returns ad slots and their creative info for the requested slots.
	Throws SERVICE_UNAVAILABLE_ERROR if unable to render page header markup template.
	Throws BAD_USER_INPUT if bid request to PAS fails input validation.

	---------------------
	Graphlet Details:
	- Name: advertising
	- CTI: IMDb/GraphQL/Advertising
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-adsystem-overrides, x-imdb-client-ip, x-imdb-client-name, x-imdb-client-version, x-imdb-consent-info, x-imdb-detected-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-adtech-public
	"""
	webAds(input: WebAdsInput!): WebAdsOutput!
	"""
	Get configuration information for web ads on a specific page.
	Throws SERVICE_UNAVAILABLE_ERROR if unable to render ad creative markup template.
	Fails open on other dependency failures, returning minimal ad response.

	---------------------
	Graphlet Details:
	- Name: advertising
	- CTI: IMDb/GraphQL/Advertising
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-adsystem-overrides, x-imdb-client-ip, x-imdb-client-name, x-imdb-client-version, x-imdb-consent-info, x-imdb-detected-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-adtech-public
	"""
	webAdsConfig(input: WebAdsInput!): WebAdsConfigOutput!
}

type QueryStubs {
	matrix: Title
}

"""
Defines standard form constraints for a radio group type of input.
"""
type RadioGroupFieldConstraints {
	"""
	Specifies if the customer must select an option in the radio group
	"""
	isRequired: BooleanValidationConstraint
}

type RankChange {
	"""
	Indicates whether the current rank is better (UP), worse (DOWN), or unchanged (FLAT) from the previous rank
	"""
	changeDirection: RankChangeDirection!
	"""
	A non-negative integer indicating the difference between the current and the previous ranks
	"""
	difference: Int!
}

enum RankChangeDirection {
	DOWN
	FLAT
	UP
}

"""
The lifetime gross for the title within the relevant area.
"""
type RankedLifetimeBoxOfficeGross {
	"""
	The BoxOfficeAreaType the gross is related to.
	"""
	boxOfficeAreaType: BoxOfficeAreaType!
	"""
	The all-time rank for the title.
	"""
	rank: Int
	"""
	The total amount the title made at the box office.
	"""
	total: Money!
}

type RankedLifetimeBoxOfficeGrossConnection {
	"""
	Pages of ranked lifetime box office grosses
	"""
	edges: [RankedLifetimeBoxOfficeGrossEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

type RankedLifetimeBoxOfficeGrossEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	An ranked lifetime box office gross node
	"""
	node: RankedLifetimeBoxOfficeGross!
	"""
	Position of the node in this context, eg: ranked lifetime box office gross 4 out of 31
	"""
	position: Int!
}

input RankedLifetimeBoxOfficeGrossFilter {
	boxOfficeAreaCodes: [String]
}

"""
Category filter for ranked list search.
"""
enum RankedListCategory {
	"""
	Editorial/curated lists
	"""
	EDITORIAL
	"""
	User-created lists
	"""
	USER
}

"""
Class type filter for ranked list search.
"""
enum RankedListClassType {
	"""
	Check-in lists
	"""
	CHECK_INS
	"""
	User-created custom lists
	"""
	LIST
	"""
	Pro lists
	"""
	PRO_LIST
	"""
	Seen/watched lists
	"""
	SEEN
	"""
	Watchlists
	"""
	WATCHLIST
}

"""
Connection type for ranked list search results.
"""
type RankedListSearchConnection {
	edges: [RankedListSearchEdge]!
	pageInfo: PageInfo!
	total: Int!
}

"""
Constraints for filtering ranked list search results.

All fields are optional. When a field is omitted, no filtering is applied for
that dimension. When all fields are omitted, the query returns all public,
trusted-owner, non-flagged lists ranked by the requested sort order (a global
"most relevant lists" view).
"""
input RankedListSearchConstraints {
	"""
	Filter by list class types. When one or more values are provided, only lists
	matching one of the specified class types will be returned.
	An omitted field and an empty array are treated identically: no class-type
	filtering is applied (all class types included).
	"""
	anyClassTypes: [RankedListClassType]
	"""
	Filter by list category. When one or more values are provided, only lists
	matching one of the specified categories will be returned.
	An omitted field and an empty array are treated identically: no category
	filtering is applied (all categories included).
	"""
	anyListCategories: [RankedListCategory]
	"""
	Filter by list content types. When one or more values are provided, only lists
	matching one of the specified types will be returned.
	An omitted field and an empty array are treated identically: no content-type
	filtering is applied (all types included).
	"""
	anyListTypes: [RankedListType]
	"""
	Filter to lists containing this const ID (title or name).
	When provided, only lists that include this item will be returned.
	When omitted, lists are not filtered by item membership.
	"""
	itemConstId: ID
}

"""
Edge type for ranked list search results.
"""
type RankedListSearchEdge {
	cursor: ID!
	node: RankedListSearchResult!
	position: Int!
}

"""
A list result from ranked list search, with metadata resolved directly from the search index.
"""
type RankedListSearchResult {
	"""
	When the list was created
	"""
	created: DateTime!
	"""
	The list description. Returns null if the list author did not provide a description.
	"""
	description: String
	"""
	The list ID
	"""
	id: ID!
	"""
	Whether this is an editorial/curated list
	"""
	isEditorial: Boolean!
	"""
	Number of items in the list
	"""
	itemCount: Int!
	"""
	The content type of items in this list
	"""
	listType: RankedListType!
	"""
	When the list was last modified
	"""
	modified: DateTime!
	"""
	The list name/title
	"""
	name: String!
}

"""
Sort input for ranked list search.
When omitted, defaults to RELEVANCE DESC.
"""
input RankedListSearchSort {
	"""
	The field to sort by. Defaults to RELEVANCE.
	"""
	by: RankedListSortBy
	"""
	The sort direction. Defaults to DESC.
	"""
	order: SortOrder
}

"""
Sort options for ranked list search.
"""
enum RankedListSortBy {
	"""
	Sort by creation date.
	ASC: Oldest first. DESC: Most recently created first.
	"""
	CREATED
	"""
	Sort by modification date.
	ASC: Oldest modifications first. DESC: Most recent modifications first.
	"""
	MODIFIED
	"""
	Ranked by engagement signals (default).
	ASC: Least engaged lists first. DESC: Most engaged lists first.
	"""
	RELEVANCE
}

"""
Content type filter for ranked list search (what kind of items the list contains).
"""
enum RankedListType {
	"""
	Lists containing images
	"""
	IMAGES
	"""
	Lists containing people
	"""
	PEOPLE
	"""
	Lists containing titles
	"""
	TITLES
	"""
	Lists containing videos
	"""
	VIDEOS
}

input RankedTitleList {
	"""
	Match titles based on their ranks
	"""
	rankRange: IntRangeInput
	"""
	Select a ranking algorithm
	"""
	rankedTitleListType: RankedTitleListType!
}

input RankedTitleListSearchConstraint {
	"""
	A title match must be in ALL of these ranked title lists.
	"""
	allRankedTitleLists: [RankedTitleList]
	"""
	A title match must be in NONE of these ranked title lists.

	If the same input is specified in the all and exclude constraints,
	then the results should be empty.
	"""
	excludeRankedTitleLists: [RankedTitleList]
}

enum RankedTitleListType {
	"""
	Ranking description: https://w.amazon.com/bin/view/IMDb/Ratings/Build/Logic#Lowest_Rated_Movies
	"""
	LOWEST_RATED_MOVIES
	"""
	Popularity-based ranking that aggregates titles of type movie, short, and video.
	"""
	MOVIE_METER
	"""
	Popularity-based ranking that aggregates titles of all types aside from episodes (like podcast, tv, audio ...)
	"""
	TITLE_METER
	"""
	Ranking description: https://w.amazon.com/bin/view/IMDb/Ratings/Build/Logic#Top_Rated_Movies
	"""
	TOP_RATED_MOVIES
	"""
	Popularity-based ranking that aggregates titles of type tv_series, tv_miniseries, tv_movie, tv_short, and tv_special
	"""
	TV_METER
}

type Rating {
	date: DateTime!
	value: Int!
}

type RatingsBody {
	"""
	The ratings body code.
	"""
	id: ID!
	"""
	Display text for the rating body.
	"""
	text: String!
}

enum RatingsCondition {
	EQUALS
}

type RatingsConnection {
	edges: [RatingsEdge]!
	pageInfo: PageInfo!
	total: Int!
}

input RatingsConstraints {
	ratings: RatingValueConstraint
}

type RatingsEdge {
	cursor: ID!
	node: RatingsResult!
}

type RatingsExportDetail implements ExportDetail {
	"""
	The date when the export becomes unavailable.
	After this date, the export will no longer be returned by the backend.
	"""
	expiresOn: DateTime
	exportType: ExportType!
	resultUrl: URL
	startedOn: DateTime!
	status: ExportStatus!
	"""
	number of objects in the export
	"""
	totalExportedObjects: Int
}

type RatingsPrivacy {
	id: ID!
	"""
	The language of the ratings privacy setting
	"""
	language: DisplayableLanguage!
	"""
	The ratings privacy setting.
	"""
	setting: RatingsPrivacySetting!
	"""
	Localized string value of ratings privacy setting.
	"""
	text: String!
}

enum RatingsPrivacySetting {
	"""
	Ratings are always private
	"""
	PRIVATE
	"""
	Ratings are always public
	"""
	PUBLIC
	"""
	Ratings are only public with attached review
	"""
	PUBLIC_WITH_REVIEWS
}

type RatingsResult {
	title: Title!
	"""
	Retrieve user's rating data for the associated title. A null value is supported here
	in case a rating does not exist.
	Error cases:
	- Throws FORBIDDEN if requesting user is not authorized to view this rating
	This field intentionally mirrors Title.userRating's behavior to maintain
	consistency across the API. While the same information could be accessed via
	title.userRating, this field provides better performance when retrieving multiple ratings
	"""
	userRating: Rating
}

input RatingsSort {
	by: RatingsSortBy!
	order: SortOrder!
}

enum RatingsSortBy {
	"""
	Sort by most recent rating date.
	ASC: Oldest ratings will be first.
	DESC: Newest ratings will be first.
	"""
	MOST_RECENT
	"""
	Sort by highest rating
	ASC: Highest ratings will be first.
	DESC: Lowest ratings will be first.
	"""
	TOP_RATED
}

type RatingsSummary {
	aggregateRating: Float
	"""
	Optional notification text to be displayed about ratings summary.
	Notification language will default to English for now
	"""
	notificationText: LocalizedMarkdown
	topRanking: TopRanking
	voteCount: Int!
}

type RatingsSummaryByCountry {
	aggregate: Float!
	country: Country!
	"""
	Defaults to English for now
	"""
	displayText: LocalizedString!
	voteCount: Int!
}

input RatingValueConstraint {
	"""
	Condition we'll be using to match the titles ratings
	"""
	type: RatingsCondition!
	"""
	Match titles based on their user rating
	"""
	value: Int!
}

"""
Defines the base Reaction type, holding a description and ID
"""
type Reaction {
	"""
	Obfuscated identifier for the localized reaction
	"""
	id: ID!
	"""
	Language of the display text
	"""
	language: DisplayableLanguage!
	"""
	Unique, readable identifier for the reaction
	"""
	reactionId: ID!
	"""
	User-visible description of the reaction
	"""
	text: String!
}

"""
Denotes if a given ReactionsGroup should allow a SINGLE reaction to be selected,
or MULTIPLE
"""
enum ReactionsGroupSelectionType {
	"""
	Denotes there is no limit to the number of reactions from a group that may be selected
	"""
	MULTIPLE
	"""
	Denotes only one reaction from the group may be selected at a time
	"""
	SINGLE
}

"""
Defines list of Reaction Summary Groups
"""
type ReactionsSummary {
	"""
	Array of multiple groups of reactions
	Sorted by aggregate count, descending
	Groups each set of reactions, defines how many reactions can be selected from
	the group at a time, and provides an aggregate count for all reactions in the group
	"""
	reactionSummaryGroups: [ReactionSummaryGroup]!
}

"""
Holds a reaction and its current count
"""
type ReactionSummary {
	"""
	Current reaction count
	"""
	count: Int!
	"""
	Count used for display purposes, utilizes truncation. For example '3200' will be truncated to '3.2k'
	"""
	displayCount: LocalizedString!
	"""
	The entity reaction
	"""
	reaction: Reaction!
}

"""
Defines a group of reactions
"""
type ReactionSummaryGroup {
	"""
	Sum of all reaction counts in the group
	"""
	aggregateCount: Int!
	"""
	Count used for display purposes, utilizes truncation. For example '3200' will be truncated to '3.2k'
	"""
	displayCount: LocalizedString!
	"""
	Unique identifier for the group.
	"""
	groupId: ID!
	"""
	List of reactions in the group, and their individual counts
	"""
	reactionSummaries: [ReactionSummary]!
	"""
	Denotes if a given ReactionsGroup should allow a SINGLE reaction to be selected, or MULTIPLE
	"""
	selectionType: ReactionsGroupSelectionType!
}

type RecentlyViewedConnection {
	edges: [RecentlyViewedEdge]!
	pageInfo: PageInfo!
	refTag: RefTag
}

type RecentlyViewedEdge {
	cursor: ID!
	node: RecentlyViewedItem!
	position: Int!
}

union RecentlyViewedItem = Company | Name | Title

"""
Filter which recently viewed items to return
"""
input RecentlyViewedItemsFilter {
	domain: BusinessDomain!
}

"""
Recent page view statistics for a given Pro claimed name page,
including a more detailed breakdown if available
"""
type RecentlyViewedStatistics {
	"""
	Summary of profession counts of people who viewed the page
	"""
	professionCountsSummary: ProfessionCountsSummary
	"""
	Number of unique customers who viewed the page
	"""
	uniquePageViewCount: Int!
}

input RecentVideosQueryFilter {
	contentTypes: [VideoContentTypeId]
}

type RecommendationExplanation {
	title: Title!
}

input RecommendedVideoTimedTextTrackFilter {
	"""
	If no VideoTimedTextTrackFormat is provided it will default to SRT.
	"""
	format: VideoTimedTextTrackFormat
	"""
	Used to override the language from the x-imdb-user-language header.
	"""
	language: Language
}

type RedirectLink implements LocalizedLink {
	"""
	The text to display for the link
	"""
	label: LocalizedString!
	"""
	The URL to redirect to
	"""
	url: URL!
}

type RefTag {
	ep13nReftag: String
}

input RegionCertificateRatingInput {
	"""
	For example: 'PG-13'
	"""
	rating: String!
	"""
	Filter by specific ratings bodies (e.g., 'MPAA' for movie ratings).
	Useful for distinguishing between TV ratings and movie ratings in regions like US
	where titles may have both (e.g., TV-PG vs PG).

	Returns all ratings bodies if omitted, null, or empty array.
	"""
	ratingsBody: [String]
	"""
	For example: 'US'
	"""
	region: String!
}

"""
Details of news items related to a particular article
"""
type RelatedNews {
	"""
	Entity of the title, name, or company mentioned in the input article
	"""
	entity: NewsEntity!
	"""
	List of most recent items about the entity
	"""
	items: [News]!
}

type RelationName {
	"""
	Relative displayable property as Markdown
	"""
	displayableProperty: DisplayableRelationNameProperty!
	"""
	Name association to the relative if the relative has an IMDb Name that is known
	"""
	name: Name
	"""
	Display name text as plain text
	"""
	nameText: String
}

enum RelationshipTypeFilter {
	CHILDREN
	OTHERS
	PARENTS
	UNRELATED
}

"""
Match titles based on their release country.
"""
input ReleaseCountrySearchConstraint {
	"""
	Match titles that have any of the specified release countries.
	Example: ['AR'] for Argentina
	Country code in ISO-3166
	"""
	anyReleaseCountries: [String]
	"""
	Exclude titles that have any of the specified release countries.
	Example: ['AR'] for Argentina
	Country code in ISO-3166
	"""
	excludeReleaseCountries: [String]
}

type ReleaseDate {
	"""
	Additional attributes
	"""
	attributes: [DisplayableAttribute]
	country: LocalizedDisplayableCountry
	day: Int
	"""
	The release date displayable property
	"""
	displayableProperty: DisplayableTitleReleaseDateProperty!
	"""
	Whether this is a wide release
	"""
	isWideRelease: Boolean!
	month: Int
	restriction: ReleaseDateRestriction
	year: Int
}

type ReleaseDateConnection {
	edges: [ReleaseDateEdge]!
	pageInfo: PageInfo!
	total: Int!
}

type ReleaseDateEdge {
	cursor: ID!
	node: ReleaseDate!
	position: Int!
}

"""
Information about restrictions applied to release date
"""
type ReleaseDateRestriction {
	"""
	Explanation(s) for the restriction
	"""
	explanations: [RestrictionExplanation]!
	reasons: [ContentRestrictionReason]!
	"""
	DEPRECATED: Renamed, use `reasons` instead.
	"""
	restrictionReason: [ContentRestrictionReason]!
}

input ReleaseDateSearchConstraint {
	"""
	The earliest release date for a title must fall within the specified range
	"""
	releaseDateRange: DateRange
}

type RepresentationRelationshipType {
	"""
	An obfuscated persistent id to uniquely identify this data
	"""
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	"""
	A non-localized, somewhat human readable string representing this relationship
	type

	Intended to support filtering or search behavior where we need a unique
	way to represent this relationship type

	Example: 'talentAgent'
	"""
	relationshipTypeId: String!
	"""
	The display text for this relationship type - 'Talent Agent'
	"""
	text: String!
}

"""
Types of representation relationships for talent
"""
enum RepresentationType {
	"""
	Professional union, guild, or trade association providing collective
	representation and member advocacy (e.g., SAG-AFTRA, WGA)
	"""
	GUILD_UNION_ASSOCIATION
	"""
	Entertainment attorney handling contract negotiations, legal matters, and business affairs for talent
	"""
	LEGAL_REPRESENTATIVE
	"""
	Personal manager for talent
	"""
	MANAGER
	"""
	Production company that produces content and may also manage or employ talent under production deals
	"""
	PRODUCTION_COMPANY
	"""
	Public relations professional managing media relations, press coverage, and public image for talent
	"""
	PUBLICIST
	"""
	General talent agent representing performers across multiple entertainment mediums
	"""
	TALENT_AGENT
	"""
	Agent representing actors specifically for commercials
	"""
	TALENT_AGENT_COMMERCIAL
	"""
	Agent booking talent for hosting opportunities on TV shows, events, and live programming
	"""
	TALENT_AGENT_HOSTING
	"""
	Talent agent-literary
	"""
	TALENT_AGENT_LITERARY
	"""
	Agent representing models
	"""
	TALENT_AGENT_MODELING
	"""
	Agent booking talent for personal appearances, speaking engagements, live events, and public functions
	"""
	TALENT_AGENT_PERSONAL_APPEARANCE
	"""
	Agent specializing in television roles
	"""
	TALENT_AGENT_TELEVISION
	"""
	Agent representing actors for theater productions
	"""
	TALENT_AGENT_THEATRICAL
	"""
	Talent agent-TV literary
	"""
	TALENT_AGENT_TV_LITERARY
	"""
	Agent representing voice actors
	"""
	TALENT_AGENT_VOICE
	"""
	Representation type is not specified or does not fit into defined categories
	"""
	UNKNOWN
}

input RepresentationTypesConstraint {
	"""
	A name match must have ALL of these representation types.
	No filtering is applied if omitted.
	"""
	allRepresentationTypes: [RepresentationType]
	"""
	A name match must have AT LEAST ONE of these representation types.
	No filtering is applied if omitted.
	"""
	anyRepresentationTypes: [RepresentationType]
	"""
	A name match must not have any of these representation types.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeRepresentationTypes: [RepresentationType]
}

input RepresentativesIdsConstraint {
	"""
	A name match must be represented by ALL of these individuals.
	Representative IDs should be provided in IMDb name format.
	Example: "nm0000001" for a specific agent or manager
	No filtering is applied if omitted.
	"""
	allRepresentativeIds: [ID]
	"""
	A name match must be represented by AT LEAST ONE of these individuals.
	Representative IDs should be provided in IMDb name format.
	Example: "nm0000001" for a specific agent or manager
	No filtering is applied if omitted.
	"""
	anyRepresentativeIds: [ID]
	"""
	A name match must not be represented by any of these individuals.
	Representative IDs should be provided in IMDb name format.
	Example: "nm0000001" for a specific agent or manager
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeRepresentativeIds: [ID]
}

"""
Restriction explanations
"""
type RestrictionExplanation {
	"""
	Explanation id
	"""
	id: ID!
	"""
	Restriction reason for which explanation is about.
	"""
	reason: ContentRestrictionReason!
	"""
	A human readable text string, intended to provide details of what is being
	restricted e.g. Cast is limited to 5 for in-development titles.
	"""
	text: String!
}

"""
An identifier for a given result of a CTA. This value is only unique within the CTA. It is not globally unique.
When provided as a resultOverride, the value must match a resultId for one of the results of the requested CTA.
Otherwise it will be ignored and the evaluated result for the CTA will be returned.
If it does match to a result, the back-end conditional logic is skipped and the matching result is returned.
If the CTA supports a "do not display" result, the resultId to use for override will be "DoNotDisplayResult".
Result override should be used for testing purposes only.
"""
scalar ResultID

"""
All fields under this type are considered self-verified. This means that we accept the Pro customer's submission for
  their own name page at face value and display it on their page without any guarantees of its validity.
"""
type Resume {
	"""
	Self verified additional awards for this name.

	@entitlement(PRO_ANY_TIER)
	"""
	additionalAwards(after: ID, first: Int!): SelfVerifiedAwardConnection
	"""
	Self verified additional credits data for this name, grouped by category.

	Throws UNAUTHENTICATED if not logged in.
	Throws FORBIDDEN if not entitled to PRO_ANY_TIER.
	Throws SERVICE_UNAVAILABLE_ERROR if unable to retrieve additional credits.

	@entitlement(PRO_ANY_TIER)
	"""
	additionalCreditCategories(
		input: AdditionalCreditCategoriesInput
	): ResumeAdditionalCreditsCategories
	"""
	Self verified additional credits data for this name, returned as a paginated, flattened list across all categories.

	Throws UNAUTHENTICATED if not logged in.
	Throws FORBIDDEN if not entitled to PRO_ANY_TIER.
	Throws SERVICE_UNAVAILABLE_ERROR if unable to retrieve additional credits.

	@entitlement(PRO_ANY_TIER)
	"""
	additionalCredits(
		after: ID
		first: Int!
		input: AdditionalCreditsInput
	): AdditionalCreditItemConnection
	"""
	Self verified additional resume sections for this name.

	@entitlement(PRO_ANY_TIER)
	"""
	additionalResumeInfo(after: ID, first: Int!): AdditionalResumeInfoConnection
	"""
	Self verified training history data for this name.
	Returns null if user is not entitled.

	@entitlement(PRO_ANY_TIER)
	"""
	education(after: ID, first: Int!): SelfVerifiedEducationConnection
	"""
	Self verified performer profile data for this name.
	Returns null if user is not entitled.

	@entitlement(PRO_ANY_TIER)
	"""
	performerProfile(limit: Int, sections: [String]): [ResumeDataItem]
	"""
	Self verified personal details data for this name.
	Returns null if user is not entitled.

	@entitlement(PRO_ANY_TIER)
	"""
	personalDetails(limit: Int, sections: [String]): [ResumeDataItem]
	"""
	Self verified profession background data for this name.
	Returns null if user is not entitled.

	@entitlement(PRO_ANY_TIER)
	"""
	professionalBackground(limit: Int, sections: [String]): [ResumeDataItem]
	"""
	Self verified references for this name.

	@entitlement(PRO_ANY_TIER)
	"""
	references(after: ID, first: Int!): SelfVerifiedReferenceConnection
	"""
	Self verified skills data for this name.
	Returns null if user is not entitled.

	@entitlement(PRO_ANY_TIER)
	"""
	skills(limit: Int, sections: [String]): [ResumeDataItem]
	"""
	Self verified training history data for this name.
	Returns null if user is not entitled.

	@entitlement(PRO_ANY_TIER)
	"""
	training(after: ID, first: Int!): SelfVerifiedTrainingConnection
}

type ResumeAdditionalCreditsCategories {
	"""
	A list of categories for additional credits on a name page.
	"""
	categories: [ResumeAdditionalCreditsCategory]!
	"""
	The total number of categories returned.
	"""
	total: Int!
}

"""
These categories differ from normal credit categories and are specific to the
  unvetted additional credits pro customers can add to their resume.
"""
type ResumeAdditionalCreditsCategory {
	"""
	A list of credits belonging to this category.

	Ordered by ascending contribution date.
	"""
	credits(first: Int): [AdditionalCreditItem]!
	"""
	The unique identifier for the credit category.
	"""
	id: ID!
	"""
	The displayable title for the category.
	"""
	title: LocalizedString!
	"""
	The total number of credits in this category.
	"""
	total: Int!
}

type ResumeDataItem {
	"""
	The label for the data item.
	"""
	label: LocalizedString!
	"""
	The value for the data item.
	"""
	values: [LocalizedString]
}

input RetrieveAccountDataInput {
	"""
	Part of the confirmation link
	"""
	dataRequestId: ID!
}

type RetrieveAccountDataOutput {
	"""
	Returns the file metadata for the requested data. Will return one file.
	"""
	fileMetadata: [FileMetadata]!
	message: LocalizedMarkdown!
	success: Boolean!
	title: LocalizedMarkdown!
}

"""
Cacheable except when query includes correctionLink and/or deletionLink fields.
"""
type Review {
	"""
	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	author: UserProfile
	"""
	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	authorRating: Int
	"""
	Returns the Contribute Site link for the requesting user to edit the review if they authored it.
	Returns null if the requesting user did not author the review.

	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	correctionLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
	): ContributionLink
	"""
	Returns the Contribute Site link for the requesting user to delete the review if they authored it.
	Returns null if the requesting user did not author the review.

	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	deletionLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
	): ContributionLink
	"""
	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	helpfulness: ReviewHelpfulness
	id: ID!
	"""
	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	language: Language
	"""
	Returns the review reporting link.

	---------------------
	Graphlet Details:
	- Name: kahlo
	- CTI: IMDb/GraphQL/Kahlo
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-contributors-tech
	"""
	reportingLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
	): ContributionLink
	"""
	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	spoiler: Boolean
	"""
	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	submissionDate: Date
	"""
	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	summary: ReviewSummary
	"""
	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	text: ReviewText
	"""
	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	title: Title
}

type ReviewEdge {
	cursor: ID!
	node: Review!
}

type ReviewHelpfulness {
	downVotes: Int!
	score: Float!
	upVotes: Int!
}

type ReviewsConnection {
	edges: [ReviewEdge]!
	pageInfo: PageInfo!
	total: Int!
}

input ReviewsFilter {
	"""
	Filter by author's Rating value. Ratings value can be any integer between 1 and 10.
	"""
	authorRating: Int
	"""
	Filter reviews based on whether they contain spoilers.
	"""
	spoiler: FilterInclusion
}

input ReviewsSort {
	by: ReviewsSortBy!
	order: SortOrder!
}

enum ReviewsSortBy {
	HELPFULNESS_SCORE
	SUBMISSION_DATE
	SUBMITTER_REVIEW_COUNT
	TOTAL_VOTES
	USER_RATING
}

type ReviewSummary {
	"""
	Machine translated text using MachineTranslatedString from IMDbCommonGraphletSchema.
	Falls back to originalText with isMachineTranslation=false if translation service is unavailable.
	"""
	machineTranslatedText: MachineTranslatedString!
	originalText: String!
}

type ReviewText {
	"""
	Machine translated text using MachineTranslatedMarkdown from IMDbCommonGraphletSchema.
	Falls back to originalText with isMachineTranslation=false if translation service is unavailable.
	"""
	machineTranslatedText: MachineTranslatedMarkdown!
	originalText: Markdown!
}

type Runtime {
	attributes: [DisplayableAttribute]!
	country: DisplayableCountry
	displayableProperty: DisplayableTitleRuntimeProperty!
	id: ID!
	seconds: Int!
}

"""
Pagination for Runtime types which always has a known total number of edges.
"""
type RuntimeConnection {
	"""
	Pages of Runtime
	"""
	edges: [RuntimeEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of Runtime
"""
type RuntimeEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A Runtime node
	"""
	node: Runtime!
	"""
	Position of the node in this context, eg: Runtime 4 out of 31
	"""
	position: Int!
}

input RuntimeSearchConstraint {
	"""
	A title match must have a runtime within the specified range
	"""
	runtimeRangeMinutes: IntRangeInput
}

type Salary {
	amount: Money
	"""
	The attributes/qualifiers of that salary
	"""
	attributes: [DisplayableAttribute]
	"""
	The salary as a displayable property
	"""
	displayableProperty: DisplayableSalaryProperty!
	id: ID!
	"""
	The title for which this salary applies.
	"""
	title: Title!
}

"""
Pagination for Salary
"""
type SalaryConnection {
	"""
	Pages of Salary
	"""
	edges: [SalaryEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of Salary
"""
type SalaryEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	Details of the person's salaries
	"""
	node: Salary!
	"""
	Position of the node in this context, eg: Salary 4 out of 31
	"""
	position: Int!
}

type SavedSearchFilter {
	"""
	The time when the filter was created
	"""
	createdTimestamp: DateTime!
	"""
	The name of the saved filter
	"""
	name: String!
	"""
	Generated field, this will be unique under the same user and filter type.
	"""
	saveId: ID!
	"""
	The domain the filter applies to, such as Names, Titles etc.
	The types available are defined in SavedSearchFilterType
	"""
	type: SavedSearchFilterType!
	"""
	The time when the filter was last updated
	"""
	updatedTimestamp: DateTime!
	"""
	The URL query string of the filter (e.g., "genre=action&year=2024").
	This is the query parameters portion of the search URL, not a full URL.
	Maximum length: 2048 characters
	"""
	url: String!
}

"""
Pagination for SavedSearchFilters.
"""
type SavedSearchFiltersConnection {
	edges: [SavedSearchFiltersEdge]!
	pageInfo: PageInfo!
	total: Int!
}

"""
Item in a page of saved search filters results.
"""
type SavedSearchFiltersEdge {
	cursor: ID!
	"""
	Saved search filters node
	"""
	node: SavedSearchFilter!
}

enum SavedSearchFilterType {
	"""
	Filter on tagged names in lists
	"""
	LIST_ITEM_NAME_TAGS
	"""
	Filter on tagged titles in lists
	"""
	LIST_ITEM_TITLE_TAGS
	"""
	Filter on Names
	"""
	NAME
	"""
	Filter on Titles
	"""
	TITLE
}

"""
Date and time of a screening.
The screening time will be in the timezone of the Cinema.
"""
type ScreeningDateTime {
	"""
	DateTime of the screening in ISO 8601 format.
	"""
	dateTime: DateTime!
	"""
	Unique identifier for this ScreeningDateTime.
	"""
	id: ID!
	"""
	Language of the display text for the time of the screening.
	"""
	language: DisplayableLanguage!
	"""
	Display text for the time of the screening.
	"""
	text: String!
}

"""
Input for constraintSearch query.
"""
input Search_ConstraintSearchInput {
	"""
	Filter types to search. Searches all types if omitted.
	"""
	filterTypes: [Search_FilterConstraintFilterType]
	"""
	Number of results to return. Must be a positive integer; throws
	GRAPHQL_VALIDATION_FAILED if not positive. If first exceeds 100, only 100
	results will be returned.
	"""
	first: Int!
	"""
	Search term to match against filter names and values. Returns empty results if blank.
	"""
	searchTerm: String!
}

"""
A single matched constraint returned by a constraintSearch query.
"""
type Search_ConstraintSearchResult {
	"""
	Unique identifier for the constraint (e.g. 'genres', 'release-date').
	"""
	constraintId: ID!
	"""
	The constraint name used in ATS or ANS queries (e.g. 'genreConstraint').
	"""
	constraintName: String!
	"""
	Whether this constraint targets title (ATS) or name (ANS) constraints.
	"""
	constraintType: Search_FilterConstraintFilterType!
	"""
	Localized display name of the constraint.
	"""
	displayName: LocalizedString!
	"""
	Terms within this result that matched the search query.
	"""
	matchedTerms: [Search_ConstraintSearchTerm]!
}

"""
Results of a constraintSearch query, including matched filter constraints and a confidence score.

This is an intentionally bounded, non-paginated list. The result set is capped at 100 items
via the `first` input parameter and cursor-based pagination is not needed. The bound is stable
because filter search results are derived from a fixed set of filter definitions.

The confidence score output is a measure of how well the input search maps to constraints.
A client might use this measure when deciding if to actually use the filter constraints returned.
For example: "what movies did I watch today" might have a match (on "movies" for example)
but this maps poorly to constraints, compared to something like "action movies in italian"
"""
type Search_ConstraintSearchResults {
	"""
	Confidence that the query maps to structured constraints (0.0–1.0).
	"""
	confidence: Float!
	"""
	Matched filter constraints, ordered by relevance score (most relevant first).
	"""
	results: [Search_ConstraintSearchResult]!
	"""
	Total number of matched results returned.
	"""
	total: Int!
}

"""
A matched term within a constraint search result, linking a display value to a constraint value
and the input query terms that produced the match.
"""
type Search_ConstraintSearchTerm {
	"""
	The constraint value used in ATS or ANS query parameters.
	"""
	constraintValue: String!
	"""
	Human-readable display value for the matched term.
	"""
	displayValue: LocalizedString!
	"""
	The input query terms that matched this constraint term.
	"""
	terms: [String]!
}

"""
Filter type indicating whether a filter targets title (ATS) or name (ANS) constraints.
"""
enum Search_FilterConstraintFilterType {
	NAME
	TITLE
}

type SearchAwardCategory {
	"""
	Search-specific identifier for the award category for use in search filtering.
	"""
	id: ID!
	"""
	Language of the display text returned for the award category
	"""
	language: DisplayableLanguage!
	"""
	Localized displayable name for the award category.
	"""
	text: String!
}

type SearchAwardEvent {
	"""
	A selection of award categories specific to this event to be displayed as additional search filtering options.
	"""
	awardCategories: [SearchAwardCategory]
	"""
	The event const. For example, `ev0000003` is the id for Oscars.
	"""
	id: ID!
	"""
	Language of the display text returned for the event
	"""
	language: DisplayableLanguage!
	"""
	Localized displayable name for the event.
	"""
	text: String!
}

type SearchAwardEventOptions {
	"""
	Collection of events and specific award categories to display as options for search.
	"""
	events: [SearchAwardEvent]!
}

type SearchFacet {
	"""
	The value which can be used in a search constraint
	"""
	filterId: ID!
	"""
	A globally unique identifier for this facet
	"""
	id: ID!
	language: DisplayableLanguage!
	text: String!
	"""
	The count of entities associated with this facet
	"""
	total: Int!
}

"""
Selects which search domain's filter index to query.
"""
enum SearchFilterType {
	"""
	Query the name-filters index (for Discover Name refiners).
	"""
	NAME
	"""
	Query the title-filters index (for Discover Title refiners).
	"""
	TITLE
}

type SearchMetadata {
	"""
	Curated list of award events and categories to use as filters for advanced search.
	"""
	advancedSearchAwardOptions: SearchAwardEventOptions!
}

enum SearchTheaterAttribute {
	ONLINE_TICKETING
}

enum SearchWatchOptionType {
	"""
	Match titles with any digital watch options including subscriptions and
	rent/buy to stream online.
	"""
	ANY_DIGITAL
	"""
	Match titles with a subscription-based streaming watch option
	For example, watch options on Prime, Hulu, Netflix, etc. that can be streamed
	with a subscription
	"""
	SUBSCRIPTION
}

type SeasonValueDisplayableProperty {
	value: Markdown!
}

type SectionCallToAction {
	"""
	Contains the action link data
	"""
	action: ActionLink!
	"""
	An identifier for the returned result of a CTA.
	A CTA will have multiple potential results, with each result having its own identifier.
	This value is only unique within the CTA. It is not globally unique.
	The resultId can be used as resultOverride input argument to specify a specific result
	should be returned for the requested CTA.
	"""
	resultId: ResultID!
	"""
	Provides the secondary localized text for a CTA.
	"""
	sectionContent: CallToActionText
	"""
	Provides the primary localized text for a CTA.
	"""
	sectionTitle: CallToActionText
}

"""
Defines standard form constraints for a select / dropdown type of input,
"""
type SelectFieldConstraints {
	"""
	Specifies if the customer must select an option in the dropdown/select menu
	"""
	isRequired: BooleanValidationConstraint
}

type SelfVerified {
	"""
	Returns true if the item has been verified by the name owner
	"""
	isSelfVerified: Boolean!
}

type SelfVerifiedAward {
	"""
	The title of the award.
	"""
	awardTitle: LocalizedString
	"""
	Additional details about the award.
	"""
	details: LocalizedString
	"""
	The name of the event for the award.
	"""
	event: LocalizedString
	"""
	A unique identifier for a resume self-verified award.
	"""
	id: ID!
	"""
	The year in which the award was received.
	"""
	year: Int
}

type SelfVerifiedAwardConnection {
	edges: [SelfVerifiedAwardEdge]!
	pageInfo: PageInfo!
	total: Int!
}

type SelfVerifiedAwardEdge {
	cursor: ID!
	node: SelfVerifiedAward!
	position: Int!
}

type SelfVerifiedEducation {
	"""
	The degree that the person received.
	"""
	degree: LocalizedString
	"""
	Additional details about their education.
	"""
	details: LocalizedString
	"""
	A unique identifier for the resume education item.
	"""
	id: ID!
	"""
	The location of the school
	"""
	location: LocalizedString
	"""
	The name of the school.
	"""
	school: LocalizedString
	"""
	The year in which they completed their education.
	"""
	year: Int
}

type SelfVerifiedEducationConnection {
	edges: [SelfVerifiedEducationEdge]!
	pageInfo: PageInfo!
	total: Int!
}

type SelfVerifiedEducationEdge {
	cursor: ID!
	node: SelfVerifiedEducation!
	position: Int!
}

type SelfVerifiedNameAttribute {
	"""
	Total number of individual values (different than values length, if it is constrained by 'limit' argument)
	"""
	total: Int!
	"""
	The individual values matching input arguments (e.g. 'limit')
	"""
	values: [SelfVerifiedNameAttributeValue]!
}

type SelfVerifiedNameAttributeMetadata {
	"""
	Whether this attribute allows free-form text value
	"""
	allowFreeFormText: Boolean!
	"""
	The maximum number of values a customer can have for this attribute. A null value means no limit
	"""
	limit: Int
	"""
	All valid values for this attribute
	"""
	validValues: [SelfVerifiedNameAttributeValue]
}

type SelfVerifiedNameAttributeValue {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

type SelfVerifiedNameAward {
	"""
	Any additional details of the award
	"""
	details: SelfVerifiedNameAttributeValue
	event: SelfVerifiedNameAttributeValue!
	"""
	The unique ID of this award item
	"""
	id: ID!
	name: SelfVerifiedNameAttributeValue!
	year: Int!
}

type SelfVerifiedNameAwardConnection {
	"""
	A page of awards
	"""
	edges: [SelfVerifiedNameAwardEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of awards
	"""
	total: Int!
}

type SelfVerifiedNameAwardEdge {
	cursor: ID!
	"""
	A single award node
	"""
	node: SelfVerifiedNameAward!
	"""
	1-based position of the node in this context
	"""
	position: Int!
}

type SelfVerifiedNameCredit {
	companyOrDirector: SelfVerifiedNameAttributeValue
	"""
	The unique ID of this credit item
	"""
	id: ID!
	projectTitle: SelfVerifiedNameAttributeValue!
	roleOrPosition: SelfVerifiedNameAttributeValue
	type: SelfVerifiedNameCreditType!
}

type SelfVerifiedNameCreditConnection {
	"""
	A page of credits
	"""
	edges: [SelfVerifiedNameCreditEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of credits
	"""
	total: Int!
}

type SelfVerifiedNameCreditEdge {
	cursor: ID!
	"""
	A single credit node
	"""
	node: SelfVerifiedNameCredit!
	"""
	1-based position of the node in this context
	"""
	position: Int!
}

type SelfVerifiedNameCreditMetadata {
	"""
	All credit types
	"""
	creditTypes: [SelfVerifiedNameCreditType]!
	"""
	The maximum number of credits a customer can have for each credit type
	"""
	limit: Int!
}

type SelfVerifiedNameCreditType {
	id: ID!
	language: DisplayableLanguage!
	text: String!
}

input SelfVerifiedNameCreditTypeFilter {
	"""
	Type credit types to exclude from the response (cannot be specified together with 'includeTypes')
	"""
	excludeTypes: [ID]
	"""
	Type credit types to include in the response (cannot be specified together with 'excludeTypes')
	"""
	includeTypes: [ID]
}

type SelfVerifiedNameCreditTypeWithCredits {
	creditType: SelfVerifiedNameCreditType!
	credits(after: ID, first: Int): SelfVerifiedNameCreditConnection
}

type SelfVerifiedNameData {
	"""
	The person's accents

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	accents(limit: Int): SelfVerifiedNameAttribute
	"""
	The age range of characters that the person can play

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	agePlayingRange: AgePlayingRange
	"""
	The person's athletic skills

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	athleticSkills(limit: Int): SelfVerifiedNameAttribute
	"""
	The person's self-verified awards

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	awards(after: ID, first: Int): SelfVerifiedNameAwardConnection
	"""
	Link for the person's blog

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	blog: BlogLink
	"""
	The person's self-verified credits grouped by credit types

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	creditTypes(
		filter: SelfVerifiedNameCreditTypeFilter
	): [SelfVerifiedNameCreditTypeWithCredits]
	"""
	The types of the person's dance skills

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	danceSkills(limit: Int): SelfVerifiedNameAttribute
	"""
	The person's educational history

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	educationalHistory(
		after: ID
		first: Int
	): SelfVerifiedNameEducationalHistoryConnection
	"""
	The person's ethnic appearances, i.e. ethnicities the person appears to be in

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	ethnicAppearances(limit: Int): SelfVerifiedNameAttribute
	"""
	The person's eye color

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	eyeColor: SelfVerifiedNameAttributeValue
	"""
	The person's guild affiliation visibilities
	"""
	guildAffiliationVisibilities(
		after: ID
		filter: GuildAffiliationsFilter
		first: Int!
	): GuildAffiliationVisibilitiesConnection
	"""
	The person's guild affiliations
	"""
	guildAffiliations(
		after: ID
		filter: GuildAffiliationsFilter
		first: Int!
	): GuildAffiliationsConnection
	"""
	The person's guild status

	Returns null if not entitled
	@entitlement(PRO_ANY_TIER)
	"""
	guildStatus: GuildStatus
	"""
	The person's hair color

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	hairColor: SelfVerifiedNameAttributeValue
	"""
	The person's hair length

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	hairLength: SelfVerifiedNameAttributeValue
	"""
	True if the person has valid passport

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	hasValidPassport: Boolean
	"""
	True if the person is willing to work unpaid

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	isWillingToWorkUnpaid: Boolean
	"""
	The person's self-verified job categories

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	jobCategories(limit: Int): SelfVerifiedNameAttribute
	"""
	The person's job titles

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	jobTitles(limit: Int): SelfVerifiedNameAttribute
	"""
	Musical instruments that the person can play

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	musicalInstruments(limit: Int): SelfVerifiedNameAttribute
	"""
	The person's performer skills

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	performerSkills(limit: Int): SelfVerifiedNameAttribute
	"""
	The person's personal locations

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	personalLocations(limit: Int): NamePersonalLocations
	"""
	The person's physique

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	physique: SelfVerifiedNameAttributeValue
	"""
	The person's primary citizenship

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	primaryCitizenship: LocalizedDisplayableCountry
	"""
	The person's references from other people

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	references(after: ID, first: Int): SelfVerifiedNameReferenceConnection
	"""
	The person's custom sections in resume

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	resumeCustomSections(
		after: ID
		first: Int
	): SelfVerifiedResumeCustomSectionConnection
	"""
	The person's additional resume details

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	resumeDetails: SelfVerifiedNameAttributeValue
	"""
	Languages that the person can speak

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	spokenLanguages(limit: Int): SelfVerifiedNameAttribute
	"""
	The person's training experiences

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	trainings(after: ID, first: Int): SelfVerifiedNameTrainingConnection
	"""
	Link for the person's twitter

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	twitter: TwitterLink
	"""
	The person's unique traits

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	uniqueTraits(limit: Int): SelfVerifiedNameAttribute
	"""
	The person's voice types

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	voiceTypes(limit: Int): SelfVerifiedNameAttribute
	"""
	The person's weight

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	weight(input: GetNameWeightInput): NameWeight
	"""
	The person's work authorization information in countries

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	workAuthorizationCountries(limit: Int): WorkAuthorizationCountries
	"""
	The credit types the person has ever worked as

	Throws UNAUTHENTICATED if not logged in
	@entitlement(PRO_ANY_TIER)
	"""
	workHistoryCreditTypes(limit: Int): SelfVerifiedNameAttribute
}

type SelfVerifiedNameEducation {
	degree: SelfVerifiedNameAttributeValue!
	details: SelfVerifiedNameAttributeValue
	"""
	The unique ID of this education item
	"""
	id: ID!
	location: SelfVerifiedNameAttributeValue!
	school: SelfVerifiedNameAttributeValue!
	year: Int!
}

type SelfVerifiedNameEducationalHistoryConnection {
	"""
	A page of education items
	"""
	edges: [SelfVerifiedNameEducationEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of education items
	"""
	total: Int!
}

type SelfVerifiedNameEducationEdge {
	cursor: ID!
	"""
	A single education item node
	"""
	node: SelfVerifiedNameEducation
	"""
	1-based position of the node in this context
	"""
	position: Int!
}

"""
Metadata for some self-verified name data attributes
"""
type SelfVerifiedNameMetadata {
	accent: SelfVerifiedNameAttributeMetadata
	athleticSkill: SelfVerifiedNameAttributeMetadata
	award: SelfVerifiedNameAttributeMetadata
	credit: SelfVerifiedNameCreditMetadata
	danceSkill: SelfVerifiedNameAttributeMetadata
	educationalHistory: SelfVerifiedNameAttributeMetadata
	ethnicAppearance: SelfVerifiedNameAttributeMetadata
	eyeColor: SelfVerifiedNameAttributeMetadata
	guildAffiliation: SelfVerifiedNameAttributeMetadata
	hairColor: SelfVerifiedNameAttributeMetadata
	hairLength: SelfVerifiedNameAttributeMetadata
	jobCategory: SelfVerifiedNameAttributeMetadata
	jobTitle: SelfVerifiedNameAttributeMetadata
	musicalInstrument: SelfVerifiedNameAttributeMetadata
	performerSkill: SelfVerifiedNameAttributeMetadata
	personalLocation: NamePersonalLocationMetadata
	physique: SelfVerifiedNameAttributeMetadata
	primaryCitizenship: CountryAttributeMetadata
	reference: SelfVerifiedNameAttributeMetadata
	resumeCustomSection: SelfVerifiedNameAttributeMetadata
	spokenLanguage: SelfVerifiedNameAttributeMetadata
	training: SelfVerifiedNameAttributeMetadata
	uniqueTrait: SelfVerifiedNameAttributeMetadata
	voiceType: SelfVerifiedNameAttributeMetadata
	workAuthorizationCountry: CountryAttributeMetadata
	workHistoryCreditType: SelfVerifiedNameAttributeMetadata
}

type SelfVerifiedNameReference {
	contactInfo: SelfVerifiedNameAttributeValue
	"""
	The unique ID of this reference item
	"""
	id: ID!
	name: SelfVerifiedNameAttributeValue!
	relationship: SelfVerifiedNameAttributeValue!
}

type SelfVerifiedNameReferenceConnection {
	"""
	A page of references
	"""
	edges: [SelfVerifiedNameReferenceEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of references
	"""
	total: Int!
}

type SelfVerifiedNameReferenceEdge {
	cursor: ID!
	"""
	A single reference node
	"""
	node: SelfVerifiedNameReference!
	"""
	1-based position of this node in this context
	"""
	position: Int!
}

type SelfVerifiedNameTraining {
	details: SelfVerifiedNameAttributeValue
	"""
	The unique ID of this training item
	"""
	id: ID!
	instructor: SelfVerifiedNameAttributeValue!
	location: SelfVerifiedNameAttributeValue!
	school: SelfVerifiedNameAttributeValue!
	type: SelfVerifiedNameAttributeValue
	year: Int!
}

type SelfVerifiedNameTrainingConnection {
	"""
	A page of trainings
	"""
	edges: [SelfVerifiedNameTrainingEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of trainings
	"""
	total: Int!
}

type SelfVerifiedNameTrainingEdge {
	cursor: ID!
	"""
	A single training node
	"""
	node: SelfVerifiedNameTraining
	"""
	1-based position of the node in this context
	"""
	position: Int!
}

type SelfVerifiedReference {
	"""
	A contact value for this reference, can be a phone number, email address, etc.
	"""
	contact: LocalizedString
	"""
	A unique identifier for this reference item.
	"""
	id: ID!
	"""
	The name of their reference.
	"""
	name: LocalizedString
	"""
	Their relationship to this reference.
	"""
	relationship: LocalizedString
}

type SelfVerifiedReferenceConnection {
	edges: [SelfVerifiedReferenceEdge]!
	pageInfo: PageInfo!
	total: Int!
}

type SelfVerifiedReferenceEdge {
	cursor: ID!
	node: SelfVerifiedReference!
	position: Int!
}

type SelfVerifiedResumeCustomSection {
	body: SelfVerifiedNameAttributeValue!
	"""
	The unique ID of this custom section item
	"""
	id: ID!
	title: SelfVerifiedNameAttributeValue!
}

type SelfVerifiedResumeCustomSectionConnection {
	"""
	A page of customer sections
	"""
	edges: [SelfVerifiedResumeCustomSectionEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of resume custom sections
	"""
	total: Int!
}

type SelfVerifiedResumeCustomSectionEdge {
	cursor: ID!
	"""
	A single custom section node
	"""
	node: SelfVerifiedResumeCustomSection!
	"""
	1-based position of this node in this context
	"""
	position: Int!
}

type SelfVerifiedTraining {
	"""
	Additional details about the training.
	"""
	details: LocalizedString
	"""
	A unique identifier for this resume training item.
	"""
	id: ID!
	"""
	The name of the person who instructed them.
	"""
	instructor: LocalizedString
	"""
	The location of the school.
	"""
	location: LocalizedString
	"""
	The name of the school or place where they received training.
	"""
	school: LocalizedString
	"""
	The type of training they received.
	"""
	training: LocalizedString
	"""
	The year in which they completed the training.
	"""
	year: Int
}

type SelfVerifiedTrainingConnection {
	edges: [SelfVerifiedTrainingEdge]!
	pageInfo: PageInfo!
	total: Int!
}

type SelfVerifiedTrainingEdge {
	cursor: ID!
	node: SelfVerifiedTraining!
	position: Int!
}

type Series {
	displayableEpisodeNumber: DisplayableEpisodeNumber!
	"""
	The next episode of this series, if there is one
	"""
	nextEpisode: Title
	"""
	The previous episode of this series, if there is one
	"""
	previousEpisode: Title
	series: Title!
}

type SeriesCreditAttribute implements CreditAttribute {
	"""
	An opaque unique identifier for the displayable SeriesCreditAttribute.
	"""
	id: ID!
	language: DisplayableLanguage!
	text: String!
	"""
	The total number of episodes this attribute appears on
	"""
	total: Int!
}

"""
The severity level of a particular title, together with the number of users
who voted for this level of severity.
"""
type SeverityLevel {
	"""
	Severity ID
	"""
	id: ID!
	"""
	The language the text is written in
	"""
	language: DisplayableLanguage!
	"""
	Severity text, e.g. 'Mild', 'Moderate', 'Severe'
	"""
	text: String!
	"""
	The SeverityVote type used in voteForSeverityTitleParentGuide mutation
	"""
	voteType: SeverityVote!
	"""
	The number of users who voted for this level of severity
	"""
	votedFor: Int!
}

enum SeverityVote {
	MILD
	MODERATE
	NONE
	SEVERE
}

"""
Count for a CreditCategory, used to fiter the name connections data
"""
type SharedCreditCategorySummary {
	creditCategory: CreditCategory!
	"""
	Number of shared names connections for this Credit Category
	"""
	total: Int!
}

type SharedEditingInfo {
	"""
	The expiration date in ISO-8601 format, present only when status is TEMPORARY
	"""
	expirationDate: Date
	"""
	The current shared editing status
	"""
	status: SharedEditingStatus!
}

enum SharedEditingStatus {
	"""
	Shared editing is turned off
	"""
	DISABLED
	"""
	Shared editing is active permanently with no expiration
	"""
	PERMANENT
	"""
	Shared editing is active with a specific expiration date
	"""
	TEMPORARY
}

"""
Page of Name that have a 2 degree connection with the input and parent names
"""
type SharedNameEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A shared name Item with related info
	"""
	node: SharedNameItem!
	"""
	Position of the node in this context, eg: SharedTitleEdge 2 out of 10
	"""
	position: Int!
}

"""
A shared name item containing all the data related to mutual nmconst
"""
type SharedNameItem {
	"""
	The name of the mutual nmconst
	"""
	mutualName: Name!
	"""
	Bounded list of titles shared between the input and mutual nmconsts.
	The default number of titles returned is 2.
	The titles are returned in the default order (ie. matching tango).
	"""
	sharedTitlesWithNameInput(first: Int): [SharedTitle]!
	"""
	Bounded list of titles shared between the parent and mutual nmconsts.
	The default number of titles returned is 2.
	The titles are returned in the default order (ie. matching tango).
	"""
	sharedTitlesWithNamePage(first: Int): [SharedTitle]!
	"""
	Total number of shared titles between the input and mutual nmconsts
	"""
	totalSharedTitlesWithNameInput: Int!
	"""
	Total number of shared titles between the parent and mutual nmconsts.
	"""
	totalSharedTitlesWithNamePage: Int!
}

type SharedNamesConnection {
	"""
	Pages of Names that have a 2 degree connection with the input and parent nmconsts
	"""
	edges: [SharedNameEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of shared names
	"""
	total: Int!
}

input SharedNamesFilter {
	"""
	Match the name connections with the credit categories of input-mutual and parent-mutual names
	"""
	creditCategoryIds: [ID]!
}

input SharedNamesInput {
	"""
	NameId used to generate shared names with the parent nameId
	If no nameId is provided, we will use the claimed nameId of the logged in user.

	Throws FORBIDDEN is no nameId is passed in with input AND user does not have an active name claim.
	"""
	nameId: ID
}

type SharedNamesResult {
	sharedCreditCategorySummary: [SharedCreditCategorySummary]
	"""
	A connection of shared names
	"""
	sharedNames: SharedNamesConnection
	"""
	Null if customer has connections, otherwise contains messaging for customer.
	"""
	statusMessage: LocalizedMarkdown
}

type SharedNamesSummary {
	"""
	Whether or not the current user is in the same network as this name.
	"""
	inNetwork: Boolean!
	"""
	Displayable text for the connections summary. Is null if there are no shared connections.

	ex. "In your network" or "7 shared connections"
	"""
	summaryText: LocalizedString
	"""
	The total number of shared connection the current user has with this name.
	"""
	totalSharedConnections: Int!
}

type SharedTitle {
	"""
	The job categories the input name is credited with on this title.
	Results are sorted with the acting credit always first, followed by the default crew credit sort order.
	"""
	nameInputCreditedJobCategories(first: Int): [CreditCategory]!
	"""
	The job categories the name page is credited with on this title.
	Results are sorted with the acting credit always first, followed by the default crew credit sort order.
	"""
	namePageCreditedJobCategories(first: Int): [CreditCategory]!
	"""
	The title that is shared between two name pages
	"""
	title: Title!
}

type SharedTitleEdge {
	"""
	Cursor ID of the shared title
	"""
	cursor: ID!
	"""
	A shared Title node
	"""
	node: SharedTitle!
	"""
	Position of the node in this context, eg: SharedTitleEdge 2 out of 10
	"""
	position: Int!
}

type SharedTitlesConnection {
	"""
	Pages of shared titles between the input and the parent name
	"""
	edges: [SharedTitleEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of shared titles
	"""
	total: Int!
}

input SharedTitlesInput {
	"""
	NameId used to generate shared titles with the parent nameId.
	If no nameId is provided, we will use the claimed nameId of the logged in user.

	Throws FORBIDDEN is no nameId is passed in with input AND user does not have an active name claim.
	"""
	nameId: ID
}

"""
A single showtime for a title at a particular cinema.
"""
type Showtime {
	"""
	Unique identifier for this showtime.
	"""
	id: ID!
	"""
	The primary ticketing information for this showtime.
	A null `ticketing` value indicates that there is no ticketing information available.
	"""
	primaryTicketing: ShowtimeTicketing
	"""
	Date and time of the screening.
	"""
	screeningStart: ScreeningDateTime!
	"""
	The screening type of this showing (e.g., standard, 3D, IMAX).
	"""
	screeningType: ShowtimeScreeningType!
}

"""
List of showtimes for this screening type and title at this cinema.
"""
type ShowtimesByScreeningType {
	"""
	The screening type of the showtimes contained in this object (e.g., standard, 3D, IMAX).
	"""
	screeningType: ShowtimeScreeningType!
	"""
	List of showtimes for this screening type and title at this cinema.
	Ordered by ascending date time of the showtime/screening.
	If no limit is provided, all showtimes will be provided for this Title/Cinema and date range.
	"""
	showtimes(limit: Int): [Showtime]!
}

"""
The type of showtime screening with displayable text.
"""
type ShowtimeScreeningType {
	"""
	Unique identifier for this showtime screening type.
	"""
	id: ID!
	"""
	Language of the showtime screening type display text.
	"""
	language: DisplayableLanguage!
	"""
	Localized text indicating the showtime screening type.
	"""
	text: String!
}

"""
The location of the client. A lat/long or postal code location must be given for the client input to be valid. If both
lat/long and postal code are provided we default to using lat/long.
"""
input ShowtimesLocation {
	latLong: LatLong
	postalCode: ShowtimesPostalCodeLocation
	"""
	This can be null in some cases; see the calling field for more information.
	Value must be greater than 0 and less then or equal to 100,000.
	"""
	radiusInMeters: Int
}

"""
The location of the client by postal code. The country field is an ISO 3166-1 alpha-2 country code (ex. US or GB).
The postalCode field is alpha numeric (ex. 98109 or D-11179).
See Supported Countries:
https://help.imdb.com/article/imdb/discover-watch/showtimes-ticket-experience-faq/GVV88QD6CJ8FJ2EK#
"""
input ShowtimesPostalCodeLocation {
	country: String!
	postalCode: String!
}

type ShowtimesTitleConnection {
	edges: [ShowtimesTitleEdge]!
	pageInfo: PageInfo!
}

type ShowtimesTitleEdge {
	cursor: ID!
	node: Title!
	position: Int!
}

"""
The cinemas to retrieve showtimes for. If no cinemas are provided no showtimes will be returned.
"""
input ShowtimesTitlesCinemasMetadata {
	"""
	Include titles that are showing in at least one of the specified theaters.
	There is no guaranteed sort order for results returned.
	Throws `BAD_USER_INPUT` if more than 400 unique cinemas are provided.
	"""
	anyCinemaIds: [ID]
	"""
	Include titles that have showtimes for a given date range.
	"""
	dateRange: ShowtimesTitlesDateRangeFilter
}

"""
Filter showtimes to those within the given date range.
It's strongly recommended to explicitly provide startTime to avoid timezone mismatches between server and client, but if `ShowtimesTitlesDateRangeFilter` is not provided, the startTime will default to server's 'now' and the endTime will default to `3000-01-01T00:00:00Z`.
If no endTime is provided, the endTime will default to `3000-01-01T00:00:00Z`.
"""
input ShowtimesTitlesDateRangeFilter {
	endTime: DateTime
	startTime: DateTime!
}

"""
Filter for constraining titles that have a showtime in a theater across a specific date/time range.
Both `start` and `end` DateTimes must be in ISO-8601 format (e.g. `2025-06-19T17:35:25Z`).
"""
input ShowtimesTitlesDateTimeRangeFilter {
	"""
	The `end DateTime to filter to (inclusive).
	Must be provided in ISO-8601 format (e.g. `2025-06-19T17:35:25Z`).
	Defaults to `3000-01-01T00:00:00Z` if not provided.
	"""
	end: DateTime
	"""
	The `start` DateTime to filter from (inclusive).
	Must be provided in ISO-8601 format (e.g. `2025-06-19T17:35:25Z`).
	"""
	start: DateTime!
}

input ShowtimesTitlesQueryMetadata {
	dateRange: ShowtimesTitlesDateRangeFilter
	sortField: ShowtimesTitlesSortField!
	sortOrder: ShowtimesTitlesSortOrder!
	ticketingTypes: [TicketingType]
}

enum ShowtimesTitlesSortField {
	SHOWTIMES_COUNT
}

enum ShowtimesTitlesSortOrder {
	ASC
	DESC
}

"""
Ticketing information for a showtime.
"""
type ShowtimeTicketing {
	"""
	URL for purchasing tickets for this showtime.
	"""
	link: URL!
}

type SignInOption {
	"""
	Indicates whether this sign-in option is available for the user.
	When false, the redirectURL should not be used (e.g., due to regional restrictions).
	"""
	isAvailable: Boolean!
	"""
	Redirect URL for the sign in option
	"""
	redirectURL: URL!
	"""
	Type of authentication provider
	"""
	type: AuthProviderType!
}

input SignInOptionRedirectURLInput {
	"""
	Optional additional web context for URL building including the client context (i.e. ubid token)
	Required if associationHandle in input is an association handle used in AAP (Amazon account pool) creation/conversion
	    i.e. 'imdb_amazon', 'imdb_pro_amazon', etc., otherwise throws GRAPHQL_VALIDATION_FAILED error
	"""
	additionalWebAuthContext: AdditionalWebAuthContext
	"""
	Association handle of the login context i.e. imdb_us
	This will affect the single sign in option returned
	"""
	associationHandle: String!
	"""
	Optional URL to return to post sign in, defaults to home page if omitted
	"""
	returnURL: URL
	"""
	Optional prefilled email for use on Pro when signing in with an Amazon account email that matches an IMDb account email
	"""
	signInPrefilledEmail: String
}

type SignInOptionRedirectURLOutput {
	"""
	Option for a user to sign in through IMDb or a Third Party Provider based on association handle provided in input
	"""
	signInOption: SignInOption!
}

input SignInOptionsRedirectURLsInput {
	"""
	Additional web context for URL building including the client context (i.e. ubid token)
	Required for building cross domain Amazon login redirect URL
	"""
	additionalWebAuthContext: AdditionalWebAuthContext!
	"""
	Optional flag to tell if you need Amazon login url with email gating.
	Default returning login url without email gating if not provided
	This will only affect sign-in with Amazon since Amazon account can be registered with mobile only.
	This is used for clients who requires customer to have email with their account (For example: Help site)
	"""
	amazonAccountEmailGating: Boolean
	"""
	Association handle of the login context i.e. imdb_us
	This will only affect the 1P IMDb sign in option, all of the other 3P logins will be configured with their own association handles
	"""
	associationHandle: String!
	"""
	Optional URL to return to post sign in, defaults to home page if omitted
	"""
	returnURL: URL
}

type SignInOptionsRedirectURLsOutput {
	"""
	Options for a user to sign in through IMDb or a Third Party Provider
	"""
	signInOptions: [SignInOption]!
}

"""
Output for the SSO unlinking interstitial query.
"""
type SingleSignOnUnlinkingInterstitialOutput {
	"""
	The interstitial content to display, including header, title, body text, and action buttons.
	"""
	interstitial: Interstitial!
}

"""
Constrain the search results based on a user's ratings.
"""
input SingleUserRatingSearchConstraint {
	"""
	Select whether constraint should restrict-to or exclude titles that the customer has rated.
	Example: undefined if we sort titles by SINGLE_USER_RATING/SINGLE_USER_RATING_DATE without filtering.
	Example: INCLUDE if we restrict to only titles within rating range and sort
	titles by SINGLE_USER_RATING/SINGLE_USER_RATING_DATE.
	"""
	filterType: SingleUserRatingSearchFilterType
	"""
	Match titles based on rating given by the requesting user.
	Infers "min: 1", or "max: 10" if one of those is not provided.
	Example: To restrict to only titles the customer rated between 5-10, use {min: 5}
	Example: To restrict to only titles the customer has rated use {min: 1, max: 5}
	"""
	ratingRange: IntRangeInput
	"""
	The user id we are filtering/sorting ratings for.

	If the provided user is invalid or the requested
	user's ratings are private and not the requesting user, an error will be returned
	identifying which case occurred.
	- Throws BAD_USER_INPUT if user ID is invalid
	- Throws FORBIDDEN if user is not authorized to retrieve ratings
	"""
	userId: ID!
}

enum SingleUserRatingSearchFilterType {
	EXCLUDE
	INCLUDE
}

"""
Constrain the search results based on a specific user's public watched history.
Used for viewing another user's watched titles when their watch history is set to public.
Privacy filtering is applied server-side: titles where the target user's ratings or check-ins
are private will be excluded automatically.
"""
input SingleUserWatchedSearchConstraint {
	"""
	Select whether constraint should restrict-to or exclude titles that the user has watched.
	Optional: when omitted, no watched filtering is applied. The constraint with just userId
	can still be used to identify the target user for SINGLE_USER_WATCHED_DATE sorting.
	Example: INCLUDE to restrict to only titles the user has watched.
	Example: EXCLUDE to exclude titles the user has watched.
	"""
	filterType: SingleUserWatchedSearchFilterType
	"""
	The user id whose public watched history we are filtering/sorting for.

	If the provided user is invalid or the requested user's watch history is private,
	an error will be returned identifying which case occurred.
	- Throws BAD_USER_INPUT if user ID is invalid
	- Throws FORBIDDEN if user's watch history is not public
	"""
	userId: ID!
}

enum SingleUserWatchedSearchFilterType {
	EXCLUDE
	INCLUDE
}

"""
Entry in the slots size map.
This type serves as an input descriptor of an ad slot, while CreativeSize denotes the size of the returned ad.
These are not the same for certain types of ad.
"""
input SlotSize {
	"""
	The height of the slot in pixels
	"""
	height: Int!
	"""
	The name of the slot
	"""
	name: String!
	"""
	The width of the slot in pixels
	"""
	width: Int!
}

"""
The order in which listed elements should be sorted
"""
enum SortOrder {
	"""
	Ascending order e.g. 1, 2, 3
	"""
	ASC
	"""
	Descending order e.g. 3, 2, 1
	"""
	DESC
}

enum SortSearchOrder {
	ASC
	DESC
}

"""
A sound mix, along with any attributes. For example, we could have a DTS sound that used an RCA sound system.
"""
type SoundMix {
	attributes: [DisplayableAttribute]!
	"""
	Sound mix as displayable property
	"""
	displayableProperty: DisplayableTechnicalSpecificationProperty!
	"""
	An ID for this sound mix, suitable for use in URLs
	"""
	id: ID!
	"""
	The display text for this sound mix
	"""
	text: String!
}

"""
Sound mixes for this title.
"""
type SoundMixes {
	"""
	The list of sound mix items
	"""
	items: [SoundMix]!
	restriction: TechnicalSpecificationsRestriction
	"""
	Total number of sound mix items
	"""
	total: Int!
}

"""
Example: if you want to match titles of sound mix 'Silent' OR 'Dolby' AND NOT 'Mono',
specify anySoundMixTypes: ['silent','dolby'] and excludeSoundMixTypes: ['mono']
"""
input SoundMixSearchConstraint {
	"""
	Match titles that have the specific sound mix
	"""
	anySoundMixTypes: [String]
	"""
	Match titles that do not have the specific sound mix

	If the same input is specified in the any and exclude constraints,
	then the results should be empty.
	"""
	excludeSoundMixTypes: [String]
}

"""
Pagination for soundtrack items which always has a known total number of edges.
"""
type SoundtrackConnection {
	"""
	Pages of tracks
	"""
	edges: [SoundtrackEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	restriction: SoundtrackRestriction
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of Tracks
"""
type SoundtrackEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A Track node
	"""
	node: Track!
	"""
	Position of the Track in this context, eg: Track 6 out of 11
	"""
	position: Int!
}

"""
Example: if you want to match titles of soundtrack 'Dissolved Girl' OR 'String Quartet', PLUS
the title must contain 'Dead Fingers Talking', specify anyAlternateTextTerms: ['Dissolved Girl',
 'String Quartet'] plus allSoundtrackTextTerms: ['Dead Fomgers Talking'].
"""
input SoundtrackMatchingSearchConstraint {
	"""
	A title match must contain ALL of these search terms in at least one of its tracks.
	"""
	allSoundtrackTextTerms: [String]
	"""
	A title match must contain AT LEAST ONE of these search terms in at least one of its tracks.
	"""
	anySoundtrackTextTerms: [String]
}

"""
Restriction information applied to Soundtracks
"""
type SoundtrackRestriction {
	"""
	Explanation(s) for the restriction
	"""
	explanations: [RestrictionExplanation]!
	reasons: [ContentRestrictionReason]!
	"""
	DEPRECATED: Renamed, use `reasons` instead.
	"""
	restrictionReason: [ContentRestrictionReason]!
	"""
	The total number of items if there was no restriction
	"""
	unrestrictedTotal: Int
}

"""
Image source details, including attribution requirements.
"""
type Source {
	"""
	Link provided by the image source, if required by the license
	"""
	attributionUrl: String
	"""
	Attribution banner image, if required by the license
	"""
	banner: Banner
	"""
	The image source ID
	"""
	id: ID!
	"""
	Displayable text for the image source, if required by the license
	"""
	text: String
}

type SpokenLanguage {
	"""
	Spoken language as displayable property.
	"""
	displayableProperty: DisplayableTitleSpokenLanguageProperty!
	"""
	The language code - either an ISO 639 code or an internally defined code if no ISO code exists for the language.
	"""
	id: ID!
	"""
	The language of the display text.
	"""
	language: DisplayableLanguage!
	"""
	Display text for the language (e.g. 'English').
	"""
	text: String!
}

type SpokenLanguages {
	"""
	DEPRECATED - DO NOT USE
	"""
	language: DisplayableLanguage!
	spokenLanguages(limit: Int): [SpokenLanguage]!
}

input SpokenLanguagesConstraint {
	"""
	A name match must speak ALL of these languages.
	No filtering is applied if omitted.
	"""
	allSpokenLanguages: [SpokenLanguageType]
	"""
	A name match must speak AT LEAST ONE of these languages.
	No filtering is applied if omitted.
	"""
	anySpokenLanguages: [SpokenLanguageType]
	"""
	A name match must not speak any of these languages.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeSpokenLanguages: [SpokenLanguageType]
}

"""
Spoken languages for talent
"""
enum SpokenLanguageType {
	ARABIC
	ARMENIAN
	CANTONESE
	CHINESE
	DUTCH
	FILIPINO
	FRENCH
	GERMAN
	GREEK
	HEBREW
	HINDI
	INDONESIAN
	ITALIAN
	JAPANESE
	KOREAN
	LATIN
	MANDARIN
	NORWEGIAN
	PERSIAN
	POLISH
	PORTUGUESE
	RUSSIAN
	SIGN_LANGUAGE
	SPANISH
	SWEDISH
	TAIWANESE
	TURKISH
	URDU
	VIETNAMESE
	YIDDISH
}

type SpouseAttributes {
	"""
	The spouse attribute
	"""
	id: ID!
	"""
	The language of the localized text
	"""
	language: DisplayableLanguage!
	"""
	The localized text
	"""
	text: String!
}

type SpouseName {
	"""
	Display name text as plain text, or nmconst link if person is an IMDb name
	"""
	asMarkdown: Markdown!
	"""
	Name association to the spouse if the spouse has an IMDb Name that is known
	"""
	name: Name
}

"""
StaffCategory: The most specific group / org this account belongs to
"""
enum StaffCategory {
	IMDb
}

"""
StaffStatus: contains related staff information
"""
type StaffStatus {
	category: StaffCategory!
}

input StarMeterConstraint {
	"""
	Match names based on their STARmeter ranking.

	Infers min 1 and unbounded max if part of the range is not provided.
	Follows STARmeter conventions, so rank 1 = most popular.
	"""
	starMeterRank: IntRangeInput!
}

type StreamingTitle {
	refTag: String!
	title: Title!
}

type StreamingTitleConnection {
	edges: [StreamingTitleEdge]!
	pageInfo: PageInfo!
}

type StreamingTitleEdge {
	cursor: ID!
	node: StreamingTitle!
}

type StreamingTitles {
	"""
	The place where a customer can stream the given titles; for example, "primeVideo".

	Please note given the federated nature of this API there is a chance that this value may be null;
	in those cases you should assume that the returned titles should not be displayed to customers.
	"""
	provider: WatchProvider
	"""
	Returns a paginated view of the titles that are currently streaming on the provider. Again, if the above
	provider is requested and null, then these probably should not be shown to customers.
	"""
	titles(after: ID, first: Int!): StreamingTitleConnection
}

input StreamingTitlesFilter {
	"""
	Includes streaming titles for below title type category values. At time of writing, streamingTitles() only
	supports movie and tv title type categories.
	"""
	includeTitleTypes: [TitleTypeCategoryValue]
	"""
	Limits the number of providers to get streaming titles for, by pruning the providers list based on size
	"""
	maxProviders: Int
	"""
	List of providers to get streaming titles for
	"""
	providers: [ID]
}

"""
An alert displayed to users about issues with their subscription status.
"""
type SubscriptionAlert {
	"""
	Unique identifier for this alert
	"""
	alertId: ID!
	"""
	Main alert message content in Markdown format.
	Does not include target link - clients should use the target field for navigation.
	"""
	alertText: Markdown!
	"""
	Brief heading text for the alert.
	"""
	heading: String!
	"""
	Navigation target that helps clients determine the appropriate link URL
	"""
	target: SubscriptionAlertTarget!
	"""
	Alert severity level that determines the icon and styling to display
	"""
	type: SubscriptionAlertType!
}

"""
Navigation targets that tell clients what URLs to build.
"""
enum SubscriptionAlertTarget {
	"""
	Alert relates to the group this customer is a member of, so link to group management page.
	"""
	GROUP_MANAGEMENT
	"""
	Alert relates to general membership settings, so link to membership settings page.
	"""
	MEMBERSHIP_SETTINGS
	"""
	Alert has no specific navigation target, so do not build a link.
	"""
	NONE
	"""
	Alert relates to payment settings or billing, so link to payment settings page.
	"""
	PAYMENT_SETTINGS
}

"""
Alert severity levels that determine visual presentation
"""
enum SubscriptionAlertType {
	INFO
	PROBLEM
	WARN
}

type SubtitleLanguage {
	"""
	Subtitle language as displayable property.
	"""
	displayableProperty: DisplayableTitleSubtitleLanguageProperty!
	"""
	The language code - either an ISO 639 code or an internally defined code if no ISO code exists for the language.
	"""
	id: ID!
	"""
	The language of the display text.
	"""
	language: DisplayableLanguage!
	"""
	Display text for the language (e.g. 'English').
	"""
	text: String!
}

type SuggestionSearchConnection {
	edges: [SuggestionSearchEdge]!
	pageInfo: PageInfo!
	"""
	Constraint-based search metadata. Only populated when searchMode includes
	INTEGRATED_ADVANCED_SEARCH_V1, the query maps to structured constraints with
	sufficient confidence, and ATS/ANS returns results. Null otherwise, including
	when filter search fails, confidence is below threshold, or ATS/ANS returns
	no results — in all these cases the response falls back to text-match only.
	"""
	searchContext: SuggestionSearchContext
}

input SuggestionSearchConstraints {
	nameConstraints: SuggestionSearchNameConstraints
}

"""
Metadata about constraint-based search results returned by INTEGRATED_ADVANCED_SEARCH_V1.
Populated when the query is identified as constraint-based with sufficient confidence and
ATS/ANS returns results. Null when the query falls back to text-match only.
Clients can use this to display what constraints were matched (e.g. "TitleType:Movie, Genre:Horror etc").
"""
type SuggestionSearchContext {
	"""
	The structured constraints that were extracted from the user's natural language query
	and used to execute the ATS/ANS search. Each entry represents a matched filter
	(e.g. genre = "Horror", release year = "1999") with its constraint name and values.
	"""
	resolvedConstraints: [Search_ConstraintSearchResult]!
}

type SuggestionSearchEdge {
	cursor: ID!
	node: SuggestionSearchItem!
}

input SuggestionSearchFilter {
	constraints: SuggestionSearchConstraints
	type: [SuggestionSearchType]
}

type SuggestionSearchItem {
	constId: ID
	displayLabels: DisplayLabels
	id: ID!
	image: MediaServiceImage
	rank: Int
	"""
	Partial ref tag suffix for overrides.
	Ex: 'gnr_act' could be used to represent the Action genre override,
	which would then be used to generate a full ref tag such as 'andp_srch_sg_gnr_act'.
	"""
	refTagFragment: String
	releaseYear: YearRange
	titleTypeId: String
	topVideos: [VideoMedia]
	"""
	Url path of the IMDb destination page
	"""
	url: String
	videoCount: Int
}

enum SuggestionSearchMethodType {
	"""
	search index
	"""
	INSTANT_INDEX_V1
	"""
	Weighted week + day ranking on the
	"""
	instant
}

enum SuggestionSearchModeType {
	"""
	Enables constraint-based search via nlp parallel filterSearch + text-match.
	"""
	INTEGRATED_ADVANCED_SEARCH_V1
}

input SuggestionSearchNameConstraints {
	professions: [SuggestionSearchNameProfession]
}

enum SuggestionSearchNameProfession {
	TALENT_AGENT
}

input SuggestionSearchOptions {
	"""
	CONSUMER or PRO.  Defaults to CONSUMER when omitted.
	"""
	searchDomain: BusinessDomain
	"""
	Controls for different search modes.
	Allowing natural language queries for example.
	Defaults to standard text-match search when omitted.
	"""
	searchMode: SuggestionSearchModeType
	"""
	Controls for different search algorithms. Defaults to basic consumer suggestion search.
	deprecated - Use searchDomain instead
	"""
	searchType: SuggestionSearchMethodType
}

enum SuggestionSearchType {
	COMPANY
	INTEREST
	NAME
	TITLE
}

type SymphonyArgument {
	name: String!
	value: String!
}

type SymphonyMetadata {
	contentId: ID
	creativeId: ID
	"""
	The name of the multi-slot optimization group name. Null if it is not part of an MSO group
	"""
	multiSlotGroupName: String
	"""
	The order of the slot in it's multi-slot group. Null if it is not part of an MSO group
	"""
	multiSlotOrder: Int
	placementId: ID
}

input SymphonyMultiValueMapEntry {
	key: String!
	value: [String]!
}

type SymphonyPlacement {
	"""
	The key-value pair arguments defined in the symphony component/creative
	"""
	componentArguments: [SymphonyArgument]!
	"""
	The metadata of the Symphony placement.
	"""
	componentMetadata: SymphonyMetadata
	"""
	The name of the widget
	"""
	componentName: String!
	"""
	The name of the slot
	eg. "center-1"
	"""
	slot: String!
}

"""
Tagline details
"""
type Tagline {
	"""
	The tagline displayable property
	"""
	displayableProperty: DisplayableTitleTaglineProperty!
	"""
	Machine translated tagline text. Returns the text field translated based on the language header.
	"""
	machineTranslatedText: MachineTranslatedString!
	"""
	The tagline text
	"""
	text: String!
}

"""
Pagination for tagline types which always has a known total number of edges.
"""
type TaglineConnection {
	"""
	Pages of taglines
	"""
	edges: [TaglineEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of taglines
"""
type TaglineEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A Tagline node
	"""
	node: Tagline!
	"""
	Position of the node in this context, eg: Tagline 4 out of 46
	"""
	position: Int!
}

"""
A set of technical specifications
"""
type TechnicalSpecifications {
	aspectRatios: AspectRatios!
	cameras: Cameras!
	colorations: Colorations!
	filmLengths: FilmLengths!
	laboratories: Laboratories!
	negativeFormats: NegativeFormats!
	printedFormats: PrintedFormats!
	processes: Processes!
	soundMixes: SoundMixes!
}

"""
Allows filtering of technical specifications based on different versions of a title.
"""
input TechnicalSpecificationsFilter {
	versions: FilterVersions
}

"""
Information about restrictions applied to technicalSpecifications
"""
type TechnicalSpecificationsRestriction {
	"""
	Explanation(s) for the restriction
	"""
	explanations: [RestrictionExplanation]!
	reasons: [ContentRestrictionReason]!
	"""
	DEPRECATED: Renamed, use `reasons` instead.
	"""
	restrictionReason: [ContentRestrictionReason]!
	"""
	The total number of items if there was no restriction
	"""
	unrestrictedTotal: Int
}

type Test {
	error: String
	"""
	@experimental experimental field
	"""
	experimental: String
	result: String
	testItems(cachebuster: String, limit: Int!): [TestItem]
}

type TestAuth {
	cacheableResult: String
	cacheableResultWithNoCacheCustomerId: String
	cacheableResultWithNoCacheUserId: String
	clientIp: String
	clientName: String
	detectedCountry: String
	hasAuthenticationHeaders: Boolean
	hasGatewayName: Boolean
	hasSessionId: Boolean
	hasTransitiveAuthenticationHeaders: Boolean
	isInternalClient: Boolean
	isNon1PInternalClient: Boolean
	language: String
	nonCacheableResult: String
	operationName: String
	result: String
}

type TestAuthTimer {
	authTimer: String
}

type TestEntitlement {
	entitlement: String!
	result: String!
}

enum TestErrorType {
	BAD_USER_INPUT
	ENTITLEMENT_DENIED
	FORBIDDEN
	INTERNAL_SERVER_ERROR
	RESOLVER_TIMEOUT
	RESOURCE_NOT_FOUND
	SERVICE_UNAVAILABLE
	UNAUTHENTICATED
	VALIDATION_ERROR
}

type TestItem {
	id: ID!
}

"""
Defines a standard shape for vending form constraints for a text area input.
Currently the same as text constraints but in future may include text-area
specific constraints such as maximum lines or rows.
"""
type TextAreaFieldConstraints {
	"""
	Specifies if the customer must provide a non-empty (ie not null or empty string) value
	"""
	isRequired: BooleanValidationConstraint
	"""
	Specifies the maximum allowed number of characters for this text area input
	"""
	maximumCharacterLength: IntThresholdValidationConstraint
	"""
	Specifies the minimum allowed number of characters for this text area input
	"""
	minimumCharacterLength: IntThresholdValidationConstraint
}

"""
Defines a standard shape for vending form constraints for a text input.
"""
type TextFieldConstraints {
	"""
	Specifies if the customer must provide a non-empty (ie not null or empty string) value
	"""
	isRequired: BooleanValidationConstraint
	"""
	Specifies the maximum allowed number of characters for this text input
	"""
	maximumCharacterLength: IntThresholdValidationConstraint
	"""
	Specifies the minimum allowed number of characters for this text input
	"""
	minimumCharacterLength: IntThresholdValidationConstraint
}

type ThirdPartyTrackers {
	"""
	Third Party trackers to fire when the ad is displayed
	"""
	impressionTrackers: [URL]!
	"""
	Third Party trackers to fire when the title poster image is clicked
	"""
	titlePosterClickTrackers: [URL]!
	"""
	Third Party trackers to fire when the video completes
	"""
	videoCompleteTrackers: [URL]!
	"""
	Third Party trackers to fire when the video reaches the first quartile
	"""
	videoFirstQuartileTrackers: [URL]!
	"""
	Third Party trackers to fire when the video reaches the midpoint
	"""
	videoMidpointTrackers: [URL]!
	"""
	Third Party trackers to fire when the video starts
	"""
	videoStartTrackers: [URL]!
	"""
	Third Party trackers to fire when the video reaches the third quartile
	"""
	videoThirdQuartileTrackers: [URL]!
}

type Thumbnail {
	height: Int!
	url: String!
	width: Int!
}

enum TicketingType {
	ONLINE
}

"""
Enums
"""
enum TimeUnit {
	SECONDS
}

"""
Title type
Extends external type.
"""
type Title {
	"""
	Theater admissions (tickets sold) for this title, broken out by region.
	Sourced from editorial contributions and international film board reports.
	Returns null if no admissions data exists for this title.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: mojo
	- CTI: IMDb/GraphQL/Mojo
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	admissions: TitleAdmissions
	"""
	Aggregate Ratings Breakdown for the title

	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	aggregateRatingsBreakdown: AggregateRatingsBreakdown
	"""
	Other title texts by which this Title is known

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	akas(
		after: ID
		before: ID
		filter: AkaFilter
		first: Int
		last: Int
		sort: AkaSort
	): AkaConnection
	"""
	Alternate versions for a title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	alternateVersions(
		after: ID
		before: ID
		first: Int
		last: Int
	): AlternateVersionConnection
	"""
	Album products of Amazon Music related to this title

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	amazonMusicAlbums(limit: Int): [AmazonMusicProduct]
	"""
	The awards that the title has won or been nominated for

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	awardNominations(
		after: ID
		before: ID
		filter: AwardNominationsFilter
		first: Int
		jumpTo: ID
		last: Int
		sort: AwardNominationsSort
	): AwardNominationConnection
	"""
	Box office performance data for this title.
	Returns null if no box office data exists for this title.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: mojo
	- CTI: IMDb/GraphQL/Mojo
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	boxOfficePerformance(filter: TitleBoxOfficeFilter): TitleBoxOfficePerformance
	"""
	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	canRate: CanRate
	"""
	A full URL to see the title on www.imdb.com

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	canonicalUrl: String
	"""
	The localized certificate for the title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	certificate: Certificate
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	certificates(
		after: ID
		before: ID
		filter: CertificatesFilter
		first: Int
		jumpTo: ID
		last: Int
	): CertificatesConnection
	"""
	For a given Title, returns the Title's showtimes grouped by cinema and screening type.

	Notes:
	- Only cinemas with showtimes for the requested title will be returned.
	- If both lat/long and postal code are provided to `filter.location`, the location will default to using lat/long.
	- It's strongly recommended to explicitly provide startTime to avoid timezone mismatches between server and client, but if `filter.dateRange` is not provided, the startTime will default to the server's 'now' and the endTime will default to `3000-01-01T00:00:00Z`.
	- If `filter.dateRange.endTime` is not provided, the endTime will default to `3000-01-01T00:00:00Z`.
	- Backwards pagination is not currently supported.
	- If `fallback` is not provided, there will be no fallback logic applied to the response.
	- If `fallback` has `NEXT_AVAILABLE_DATE`  as it's value and the original result contains no data, the graphlet will attempt to fallback the results to the showtimes for the next available date from the provided `filter.dateRange.startTime` input value. If fallback results are found, the `fallback` results field will be non-null and provide information about the fallback result set.

	Sorting:
	- If `filter.location` provided, sorting will be in ascending distance from the provided location.
	- If `filter.anyCinemaIds` provided, the sort order will not be guaranteed. Currently, there is no requirement to support any sort for the resulting cinemaShowtimes, since the only use case we have is for a singular cinema, which will only return a single node.

	Errors:
	- Throws `BAD_USER_INPUT` with symbol `filter.location.postalCode` if the location is unknown.
	- Throws `BAD_USER_INPUT` if lat/long is too precise. See `LatLong` input.
	- Throws `BAD_USER_INPUT` if `filter.location.radiusInMeters` is null.
	- Throws `BAD_USER_INPUT` if both or neither `filter.anyCinemaIds` and `filter.location` are provided. Exactly one must be provided.
	- Throws `BAD_USER_INPUT` if both `filter.anyCinemaIds` and `fallback` are provided.
	- Throws `BAD_USER_INPUT` if more than 400 unique cinemas are provided in `filter.anyCinemaIds`.

	Limits:
	- `first` is limited to a maximum of 100. If the `first` argument exceeds the maximum, the results will be capped to the maximum.

	---------------------
	Graphlet Details:
	- Name: titleAppointments
	- CTI: IMDb/GraphQL/Title Appointments
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	cinemaShowtimesByScreeningType(
		after: ID
		fallback: TitleCinemaShowtimesFallbackOption
		filter: TitleCinemaShowtimesFilter!
		first: Int!
	): TitleCinemaShowtimesByScreeningTypeConnection
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	companyCreditCategories(
		filter: CompanyCreditsFilter
	): [CompanyCreditCategoryWithCompanyCredits]
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	companyCredits(
		after: ID
		before: ID
		filter: CompanyCreditsFilter
		first: Int
		last: Int
	): CompanyCreditConnection
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	connectionCategories: [ConnectionCategoryWithConnections]
	"""
	The connections for the title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	connections(
		after: ID
		before: ID
		filter: ConnectionsFilter
		first: Int
		jumpTo: ID
		last: Int
	): TitleConnectionConnection
	"""
	Copyright holders for the title.

	This is a bounded list returned in full (a title has only a handful of
	copyright entries). Results are ordered lexicographically as produced by the
	upstream source. Returns an empty list when the title has no copyright entries.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	copyrights: [TitleCopyright]
	"""
	A list of the countries of origin for the title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	countriesOfOrigin: CountriesOfOrigin
	"""
	Crazy credits for a title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	crazyCredits(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): CrazyCreditConnection
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	creditCategories(
		filter: TitleCreditCategoryWithCreditsFilter
	): [TitleCreditCategoryWithCredits]
	"""
	Vends name credits for this title, grouped into "Groupings" in a way that best represents a title's end credits.
	Actor and Actress credits are grouped together in a 'Cast' grouping.
	Archive Footage and similar roles where the person was not directly involved, are kept within this 'Cast' grouping
	(which differs to how name.creditGroupings vends credits).
	All other credit categories are presented 1-1 as groupings.

	Groupings are presented in a fixed order, presenting groupings in an order similar to that seen
	on movie or TV end credits. This field is suitable for building experiences that highlight work on a title separately
	across disciplines, such as displaying all credits for a title.

	When a name has credits on individual episodes in a series, these are rolled up and presented at the series level
	as a single CreditV2 value, giving a summary of their contribution. Details of individual episodes are available
	from "episodeCredits" on a CreditedRole within a CreditV2 value, or on the CreditV2 value.

	When a credit has performed many roles on a title, these are aggregated into a single CreditV2 value, with each
	individual role represented as a separate CreditedRole within the CreditV2.
	This makes this field useful for building experiences where you want to show each name for a title only once.
	For example, credits for The Office (2005-2013) (tt0386676) would include Mindy Kaling (nm1411676) once per grouping.
	All of their roles as producer (co-executive producer, producer, and so on) are vended as a single CreditV2 value,
	and separate values are given for their roles as actress, writer and director.
	Note that the aggregateBy parameter can be used to disable aggregation for specific groupings.

	Credits on in-development titles are only available to clients providing a Pro tier entitlement.

	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	creditGroupings(
		"""
		Cursor from which to begin providing CreditGroupings from.
		Use cursor values vended on CreditGroupingEdge values to continue pagination in subsequent calls.

		Note that if you include filtering parameters in an initial call, you must pass the same parameters along with
		your cursor, to ensure the full listing of credits is consistent.
		"""
		after: String
		"""
		Defines how credits are aggregated together into a single CreditV2 result.

		By default, credits are aggregated by NAME.
		"""
		aggregateBy: CreditAggregation
		"""
		Defines the ordering of credits returned.
		When many credited roles are aggregated into a single CreditV2 value (for example combining roles across many
		groupings), we sort based on the lowest-sorting credited role within the CreditV2,

		By default, credits are sorted by importance (see TitleCreditsV2Sort for definition).
		"""
		creditSort: TitleCreditsV2Sort
		"""
		Allows for filtering in or out of credits based on several criteria.
		See TitleCreditsV2Filter for details.

		By default, no filtering is applied.
		"""
		filter: TitleCreditsV2Filter
		"""
		Number of CreditGrouping values to fetch.
		Maximum per request is 250.
		"""
		first: Int!
		"""
		When true, empty groupings can be returned for four primary credit groupings (Director, Writer, Cast, Producer)
		to encourage contribution of missing data. Empty groupings are those that contain no credits.

		This is useful for building experiences that want to show placeholder sections for missing credit types,
		allowing users to contribute data for roles that haven't been credited yet.

		By default, empty groupings are not returned.
		"""
		includeEmptyGroupings: Boolean = false
		"""
		In general: whether to utilise any entitlements passed to alter the behaviour of this API.
		Specifically: For industry professionals (IMDbPro members) IMDb discloses attachment credits; which are essentially
		rumored credits to in-development title that have not been publicly announced.

		When false; attachments are not provided, as seen on the IMDb website.
		When true; attachments are provided if the requester is entitled to receive the credits.
		"""
		useEntitlement: Boolean = true
	): CreditGroupingConnection
	"""
	The entire list of cast & crew members

	Default sort is done by billing order
	If there is no billing order it is done alphabetically

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	credits(
		after: ID
		before: ID
		filter: TitleCreditsFilter
		first: Int
		last: Int
		sort: TitleCreditSort
	): CreditConnection
	"""
	Vends name credits for this title.

	When a name has credits on individual episodes in a series, these are rolled up and presented at the series level
	as a single CreditV2 value, giving a summary of their contribution. Details of individual episodes are available
	from "episodeCredits" on a CreditedRole within a CreditV2 value.

	When a credit has performed many roles on a title, these are aggregated into a single CreditV2 value, with each
	individual role represented as a separate CreditedRole within the CreditV2.
	For example, credits for The Office (2005-2013) (tt0386676) would include Mindy Kaling (nm1411676) once,
	listing all of their roles as producer (co-executive producer, producer, and so on), actress, writer and director
	as a single CreditV2.
	This makes this field useful for building experiences where you want to show each name for a title only once.
	Note that the aggregateBy parameter can be used to disable aggregation for specific groupings.

	Related field title.creditGroupings allows fetching credits for a title, grouped in a way that best represents
	a title's end credits. When a single grouping contains large numbers of credits, additional pages of those credits
	can be fetched using this field (and providing `filter.creditLevelFilter.groupings` to restrict to the grouping
	being paginated).

	Credits on in-development titles are only available to clients providing a Pro tier entitlement.

	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	creditsV2(
		"""
		Cursor from which to begin providing credits from.
		Use cursor values vended on CreditV2Edge values to continue pagination in subsequent calls.

		Note that if you include filtering parameters in an initial call, you must pass the same parameters along with
		your cursor, to ensure the full listing of credits is consistent.
		"""
		after: ID
		"""
		Defines how credits are aggregated together into a single CreditV2 result.

		By default, credits are aggregated by NAME.
		"""
		aggregateBy: CreditAggregation
		"""
		Defines the ordering of credits returned.
		When many credited roles are aggregated into a single CreditV2 value (for example combining roles across many
		groupings), we sort based on the lowest-sorting credited role within the CreditV2,

		By default, credits are sorted by importance (see TitleCreditsV2Sort for definition).
		"""
		creditSort: TitleCreditsV2Sort
		"""
		Allows for filtering in or out of credits based on several criteria.

		By default, no filtering is applied.
		"""
		filter: TitleCreditsV2Filter
		"""
		Number of CreditV2 values to fetch.
		Maximum per request is 1000.
		"""
		first: Int!
		"""
		In general: whether to utilise any entitlements passed to alter the behaviour of this API.
		Specifically: For industry professionals (IMDbPro members) IMDb discloses attachment credits; which are essentially
		rumored credits to in-development title that have not been publicly announced.

		When false; attachments are not provided, as seen on the IMDb website.
		When true; attachments are provided if the requester is entitled to receive the credits.
		"""
		useEntitlement: Boolean = true
	): CreditV2Connection
	"""
	Roman numeral used to differentiate between titles sharing the same name and year

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	disambiguator: Disambiguation
	"""
	Return Engagement Statistics for a title

	---------------------
	Graphlet Details:
	- Name: engagementData
	- CTI: IMDb/GraphQL/Engagement Data
	- Affected by headers: x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-engagement-growth-public
	"""
	engagementStatistics: EngagementStatistics
	"""
	Credits for episodes of this title by a single name. For access to more broad credit data, see
	title.creditsV2 and title.creditGroupings fields.

	Each episode title is represented by a single CreditV2 value. When a name has performed multiple roles on the title,
	these are represented as separate CreditedRoles within one CreditV2 value.

	Credits on in-development titles are only available to clients providing a Pro tier entitlement.

	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	episodeCredits(
		"""
		Cursor from which to begin providing credits from.
		Clients wishing to retrieve episode credits can fetch an initial list of episodes for this name-title pair,
		via the title.creditsV2 or title.creditGroupings field. These calls can provide a cursor to use to fetch episodes
		specific to a CreditV2 or CreditedRole value.

		Note that if you include filtering parameters in an initial call, you must pass the same parameters along with
		your cursor, to ensure the full listing of credits is consistent.
		For example, if you first call title.creditsV2 with a filter of `titleType: PODCAST`, you must include this filter
		in your call to title.episodeCredits for the API to know to continue applying this filter.
		"""
		after: ID
		"""
		Defines how credits are aggregated together into a single CreditV2 result.

		By default, credits are aggregated by NAME.
		"""
		aggregateBy: CreditAggregation
		"""
		Multi-level filtering system supporting episode, credit, and name-level filters.
		Allows precise control over which episode credits are returned for the specified person.
		"""
		filter: TitleEpisodeCreditsFilter
		"""
		Number of episode CreditV2 values to fetch.
		Maximum per request is 250.
		"""
		first: Int!
		"""
		ID of the person/name to fetch episode credits for.
		This parameter is required and scopes the results to episodes where this specific person has credits.
		Use title.creditsV2 or title.creditGroupings to find name IDs that have credits on this title.
		"""
		nameId: ID!
		"""
		Defines the ordering of credits returned.
		Ascending sort by release date, then season number, then episode number.
		This provides chronological ordering of episodes within the series.

		By default, credits are sorted by release date ascending (see EpisodeCreditsSortBy for definition).
		"""
		sort: EpisodeCreditsSortBy
		"""
		In general: whether to utilise any entitlements passed to alter the behaviour of this API.
		Specifically: For industry professionals (IMDbPro members) IMDb discloses attachment credits; which are essentially
		rumored credits to in-development title that have not been publicly announced.

		When false; attachments are not provided, as seen on the IMDb website.
		When true; attachments are provided if the requester is entitled to receive the credits.
		"""
		useEntitlement: Boolean = true
	): EpisodeCreditConnection
	"""
	For titles which are a series, provides information about the episodes of that series

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	episodes: Episodes
	"""
	@experimental The viewer's in-progress (SAVED) contribution on this title, if any.

	Today a viewer has at most one open basket per title, so this returns a wrapper whose
	`previousSubmission` is that single basket (null when none — the client shows "Start"
	rather than "Resume").

	Scoped to the signed-in viewer and never shared between viewers.

	Required Headers:
	x-imdb-customer-id

	Possible Errors:
	Throws UNAUTHENTICATED if the customer is not signed in.

	---------------------
	Graphlet Details:
	- Name: contribution
	- CTI: IMDb/GraphQL/Contribution
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-contributors-okapi
	"""
	experimental_activeContributionSubmissions: Experimental_ActiveContributionSubmissions
	"""
	Experimental Field: DO NOT USE. The entire list of credits for the title

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	experimental_credits(
		after: ID
		before: ID
		filter: ExperimentalTitleCreditsFilter
		first: Int
		last: Int
		sort: TitleCreditSort
	): ExperimentalCreditConnection
	"""
	@experimental A roll-up of the viewer's post-submit item statuses across ALL of their
	submitted baskets on this title, so a contributor sees at a glance how many of their
	in-flight edits here are pending, approved, or declined.

	Counts individual items, not baskets: every submitted item the viewer has on this title
	is bucketed by its moderation status and the buckets are summed across baskets.

	Scoped to the signed-in viewer and never shared between viewers. Every bucket is zero
	when the viewer has nothing submitted here.

	Required Headers:
	x-imdb-customer-id

	Possible Errors:
	Throws UNAUTHENTICATED if the customer is not signed in.

	---------------------
	Graphlet Details:
	- Name: contribution
	- CTI: IMDb/GraphQL/Contribution
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-contributors-okapi
	"""
	experimental_pendingContributionSubmissionsSummary: Experimental_PostSubmitStatusCounts
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	externalLinkCategories(
		filter: ExternalLinksFilter
	): [ExternalLinkCategoryWithExternalLinks]
	"""
	External links for a title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	externalLinks(
		after: ID
		before: ID
		filter: ExternalLinksFilter
		first: Int
		last: Int
	): ExternalLinkConnection
	"""
	Frequently asked questions (FAQs) for a title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	faqs(
		after: ID
		before: ID
		filter: FaqsFilter
		first: Int
		jumpTo: ID
		last: Int
	): FaqConnection
	"""
	Featured polls for the title.
	Capped at <LIMIT> polls. No pagination

	---------------------
	Graphlet Details:
	- Name: polls
	- CTI: IMDb/GraphQL/Polls
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	featuredPolls(first: Int!): PollsConnection
	"""
	Featured reviews for the title.
	Capped at 5 reviews. No pagination

	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	featuredReviews(first: Int!): ReviewsConnection
	"""
	All FilmingDates for the title

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	filmingDates(
		after: ID
		before: ID
		first: Int
		last: Int
	): FilmingDatesConnection
	"""
	Title filming locations

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	filmingLocations(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): FilmingLocationConnection
	"""
	The global earliest release date of the title across all regions.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	globalEarliestReleaseDate: ReleaseDate
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	goofCategories(filter: GoofCategoryWithGoofsFilter): [GoofCategoryWithGoofs]
	"""
	Goofs for a title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	goofs(
		after: ID
		before: ID
		filter: GoofsFilter
		first: Int
		jumpTo: ID
		last: Int
	): GoofConnection
	id: ID!
	"""
	Images related to the title, grouped by image types

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	imageTypes(filter: ImagesFilter): [ImageTypeWithImages]
	"""
	Returns the 'upload url' Link.

	---------------------
	Graphlet Details:
	- Name: kahlo
	- CTI: IMDb/GraphQL/Kahlo
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-contributors-tech
	"""
	imageUploadLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
	): ContributionLink
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	images(
		after: ID
		before: ID
		"""
		Parameter to go around cache, expected to use cursor as a value
		"""
		bust: String
		filter: ImagesFilter
		first: Int
		jumpTo: ID
		last: Int
	): ImageConnection
	"""
	Paginated list of interests directly tagged to this title. Sorted alphabetically. Supports page size of at most 250.
	Requesting a page size larger than the maximum supported will result in the maximum size being returned.

	---------------------
	Graphlet Details:
	- Name: interests
	- CTI: IMDb/GraphQL/Interests
	- Affected by headers: x-imdb-client-name, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 600 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	interests(after: ID, first: Int!): InterestConnection
	"""
	A boolean flag that indicates whether IMDb considers this title to be primarily pornographic or adult genre.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	isAdult: Boolean
	"""
	Whether this title has no spoken dialogue (a 'silent film'). Derived from
	sound mix data when available (true if the title has a 'Silent' sound mix),
	otherwise from language attributes (true if any language is marked as silent
	or having no dialogue). Null when no data is available.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	isSilent: Boolean
	"""
	if the writer credits have been verified for this title. WGA is the Writers
	Guild of America they ensure that writers are credited correctly for a given title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	isWGAVerified: Boolean
	"""
	Title keywords grouped by item scope category

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	keywordItemCategories(
		filter: TitleKeywordItemCategoriesFilter
		"""
		Sort order for keywords within each category.
		When omitted, keywords are sorted by relevance in descending order (most relevant first).
		"""
		keywordSort: KeywordItemCategorySort
	): [TitleKeywordItemCategoryWithKeywords]
	"""
	Title keywords.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	keywords(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
		sort: TitleKeywordsSort
	): TitleKeywordConnection
	"""
	The latest trailer for a title

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	latestTrailer: Video
	"""
	The latest trailer (player not video) for a title.
	It's different from 'latestTrailer'. This one is used for querying a title's
	latest trailer's webviewUrl by filter.

	Optionally, country, preferredAudioLanguage and maturityLevel can be provided in filter to be used to
	search for the videos of specific region and with specific language, and filter out
	videos with mature content. If none are provided, we will set them according to the description
	in LatestTrailerFilter.

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	latestTrailerWebviewPlayer(filter: LatestTrailerFilter): WebviewVideoPlayer
	"""
	Liftime gross for this title

	---------------------
	Graphlet Details:
	- Name: mojo
	- CTI: IMDb/GraphQL/Mojo
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	lifetimeGross(boxOfficeArea: BoxOfficeArea!): BoxOfficeGross
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	meta: TitleMeta
	"""
	---------------------
	Graphlet Details:
	- Name: metacritic
	- CTI: IMDb/GraphQL/Metacritic
	- Affected by headers: x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 10800 seconds
	- Slack Channel: #imdb-community-and-video
	"""
	metacritic: Metacritic
	"""
	Returns up to 5 category-specific rankings for a title.
	Returns either MovieMeter or TVMeter insights depending on the title type.
	Use TITLE_METER in input to get rankings across both TV and movies.

	---------------------
	Graphlet Details:
	- Name: ranking
	- CTI: IMDb/GraphQL/ProRanking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	meterInsights(input: TitleMeterInsightsInput): [TitleMeterInsight]
	"""
	Returns either the MovieMeter or TVMeter value depending on the title type.
	Ranking information beyond 5000 requires entitlement proving Pro subscription.
	Use TITLE_METER in input to get a ranking across both TV Show and movies.
	Use useEntitlement = false to access non entitled version.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: ranking
	- CTI: IMDb/GraphQL/ProRanking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	meterRank(
		input: TitleMeterRankingInput
		useEntitlement: Boolean = true
	): TitleMeterRanking
	"""
	History of IMDbPro Title meter rankings of a title.
	Requires entitlement proving Pro subscription.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: ranking
	- CTI: IMDb/GraphQL/ProRanking
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	meterRankingHistory(
		input: TitleMeterRankingHistoryInput
	): TitleMeterRankingHistory
	"""
	Similar titles to this title. Clients may request a max of 100 similar titles. Number of titles returned may be fewer.

	Pagination not supported:
	Pagination may produce duplicate title results as the ML model is not deterministic and ranking is affected
	by customer behavioral data. Clients are advised to retrieve titles using a single request for now.

	For the 'after' parameter, please use 'PageInfo.endCursor' instead of 'MoreLikeThisEdge.cursor'.

	---------------------
	Graphlet Details:
	- Name: personalization
	- CTI: IMDb/GraphQL/Personalized Picks
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-consent-info, x-imdb-detected-country, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	moreLikeThisTitles(after: ID, first: Int!): MoreLikeThisConnection
	"""
	News articles about a title

	---------------------
	Graphlet Details:
	- Name: bugle
	- CTI: IMDb/GraphQL/Bugle
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 900 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	news(after: String, first: Int!): NewsConnection
	"""
	The nominations that the title has won or been nominated for
	Default sort is done by sorting nomination id in ascending order

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	nominations(
		after: ID
		before: ID
		filter: NominationsFilter
		first: Int
		jumpTo: ID
		last: Int
		sort: NominationsSort
	): NominationConnection
	"""
	Opening weekend gross for this title.

	---------------------
	Graphlet Details:
	- Name: mojo
	- CTI: IMDb/GraphQL/Mojo
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	openingWeekendGross(boxOfficeArea: BoxOfficeArea!): OpeningWeekendGross
	"""
	Opening weekend grosses for this title.

	---------------------
	Graphlet Details:
	- Name: mojo
	- CTI: IMDb/GraphQL/Mojo
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	openingWeekendGrosses(
		after: ID
		before: ID
		filter: OpeningWeekendBoxOfficeGrossFilter
		first: Int
		last: Int
	): OpeningWeekendBoxOfficeGrossConnection
	"""
	The original runtime for the title, without region-specific selection. Returns
	null if no non-regional runtime is available.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	originalRuntime: Runtime
	"""
	The original text for the title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	originalTitleText: TitleText
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	parentsGuide: ParentsGuide
	"""
	Returns the parental guide contribution link.

	---------------------
	Graphlet Details:
	- Name: kahlo
	- CTI: IMDb/GraphQL/Kahlo
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-contributors-tech
	"""
	parentsGuideContributionLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
	): ContributionLink
	"""
	The plot for the title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	plot: Plot
	"""
	Returns the plot contribution link.

	---------------------
	Graphlet Details:
	- Name: kahlo
	- CTI: IMDb/GraphQL/Kahlo
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-contributors-tech
	"""
	plotContributionLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
	): ContributionLink
	"""
	The plots for the title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	plots(
		after: ID
		before: ID
		filter: FilterPlots
		first: Int
		"""
		Return plot outlines in all available locales
		"""
		includeAllLocales: Boolean
		jumpTo: ID
		last: Int
	): PlotConnection
	"""
	The most prestigious awards that the title has won or been nominated for

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	prestigiousAwardSummary: PrestigiousAwardSummary
	"""
	The primary image for the title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	primaryImage: Image
	"""
	An ordered list of the primary videos related to a title.
	If the first argument exceeds 100, the results will be capped to 100.

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	primaryVideos(first: Int): VideoConnection
	"""
	Primary watch option determined by ordering the providers based on an order defined by business.

	If the location is included, returns any location-specific watch options (will be required in the future)
	Promoted provider feature is no longer supported by the Ads team - https://sim.amazon.com/issues/W2W-170. Passing value onto the optional parameter will not have any effect.

	---------------------
	Graphlet Details:
	- Name: watch
	- CTI: IMDb/GraphQL/Watch
	- Affected by headers: x-imdb-client-name, x-imdb-client-version, x-imdb-detected-country, x-imdb-detected-time-zone, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	primaryWatchOption(
		location: WatchOptionsLocation
		promotedProvider: String
	): PrimaryWatchOption
	"""
	Array of most important credit categories and credits in that category

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	principalCredits(
		filter: PrincipalCreditsFilter
	): [PrincipalCreditsForCategory]
	"""
	Principal Credits gives the primary creative contributors to a title, organized by groupings.

	We group credits in a way that highlights the primary creative roles on a title.
	Showrunner, Creator and Head Writer roles are moved out of their original categories
	(typically 'Producer' and 'Writer') and presented in a separate grouping per role type.
	For all other credits, the grouping strategy on title.creditGroupings is followed.
	Note that we label the 'Cast' grouping as 'Stars'.
	Also when the TOP_CAST mode is used (see filter parameter), we vend a "Top X" or "Key X" text per grouping,
	replacing the standard text.

	The number of credits per grouping we vend is limited to 30, to reflect that most use cases
	looking for a summary of principal credits want a limited subset of data.
	For fuller data see title.creditGroupings.

	Clients can control the maximum number of groupings returned using the `filter` input.
	The specific groupings returned will depend on the title type
	(for example giving Showrunner for TV series but not for movies, and Director for movies but not for TV series).

	Note that the returned groupings may change over time, as our set of supported credit categories evolves.

	Principal credits for in-development titles only available when a Pro entitlement is provided.
	Without this, we only provide access to a count per grouping.

	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	principalCreditsV2(
		filter: PrincipalCreditsV2Filter
		"""
		In general: whether to utilise any entitlements passed to alter the behaviour of this API.
		Specifically: For industry professionals (IMDbPro members) IMDb discloses attachment credits; which are essentially
		rumored credits to in-development title that have not been publicly announced.

		When false; attachments are not provided, as seen on the IMDb website.
		When true; attachments are provided if the requester is entitled to receive the credits.
		"""
		useEntitlement: Boolean = true
	): [PrincipalCreditsForGrouping]
	"""
	Product category classifications for this title (e.g. movies/TV/OTT, music video). Null when no data is available.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	productCategories: [TitleProductCategory]
	"""
	Estimated amount of money spent on the entire film project

	---------------------
	Graphlet Details:
	- Name: mojo
	- CTI: IMDb/GraphQL/Mojo
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	productionBudget: ProductionBudget
	"""
	All productionDates for the title

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	productionDates(
		after: ID
		before: ID
		first: Int
		last: Int
	): ProductionDatesConnection
	"""
	Current production stage of the title, and the status history.

	Production status history for in-development titles is only available to clients providing a Pro tier entitlement.

	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	productionStatus(
		"""
		In general: whether to utilise any entitlements passed to alter the behaviour of this API.
		Specifically: For industry professionals (IMDbPro members) IMDb discloses
		production status history for in-development titles.

		When false; production status history is not provided for in-development titles, as seen on the IMDb website.
		When true; production status history is provided if the requester is entitled to receive the data.
		"""
		useEntitlement: Boolean = true
	): ProductionStatusDetails
	"""
	Quotes in this Title

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	quotes(
		after: ID
		before: ID
		filter: TitleQuotesFilter
		first: Int
		jumpTo: ID
		last: Int
	): TitleQuoteConnection
	"""
	The lifetime box office gross for this title within the specified area. Rank
	is compared to the lifetime grosses of other titles within the specified area.

	---------------------
	Graphlet Details:
	- Name: mojo
	- CTI: IMDb/GraphQL/Mojo
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	rankedLifetimeGross(
		boxOfficeArea: BoxOfficeArea!
	): RankedLifetimeBoxOfficeGross
	"""
	The lifetime box office grosses for this title within the specified areas.
	Rank is compared to the lifetime grosses of other titles within the specified area.

	---------------------
	Graphlet Details:
	- Name: mojo
	- CTI: IMDb/GraphQL/Mojo
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	rankedLifetimeGrosses(
		after: ID
		before: ID
		filter: RankedLifetimeBoxOfficeGrossFilter
		first: Int
		last: Int
	): RankedLifetimeBoxOfficeGrossConnection
	"""
	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	ratingsSummary: RatingsSummary
	"""
	Paginated list of interests related to this title's interests. Sorted alphabetically. Supports page size of at most 250.
	Requesting a page size larger than the maximum supported will result in the maximum size being returned.

	---------------------
	Graphlet Details:
	- Name: interests
	- CTI: IMDb/GraphQL/Interests
	- Affected by headers: x-imdb-client-name, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 600 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	relatedInterests(after: ID, first: Int!): InterestConnection
	"""
	Get lists related to a title

	---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	"""
	relatedLists(after: ID, first: Int!): ListCollectionConnection
	"""
	The earliest localized release date of the title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	releaseDate: ReleaseDate
	"""
	All release dates of the title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	releaseDates(
		after: ID
		before: ID
		filter: TitleReleaseDatesFilter
		first: Int
		last: Int
	): ReleaseDateConnection
	"""
	The year of the title. The existing IMDb concept of a year that is independent of the release
	date.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	releaseYear: YearRange
	"""
	Returns the review contribution link.

	---------------------
	Graphlet Details:
	- Name: kahlo
	- CTI: IMDb/GraphQL/Kahlo
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-contributors-tech
	"""
	reviewContributionLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
	): ContributionLink
	"""
	A summary of user reviews for this title, including themes.

	Returns null if the customer is not entitled or no review summary is available for this title.

	@entitlement(LOGGED_IN)

	---------------------
	Graphlet Details:
	- Name: ziggy
	- CTI: IMDb/GraphQL/Ziggy
	- Affected by headers: x-imdb-user-language
	- Cache TTL: 900 seconds
	- Slack Channel: #imdb-ziggy
	"""
	reviewSummary(
		"""
		Controls entitlement gating for GenAI-generated review summaries.
		When true, the full summary is returned only if the requester holds the LOGGED_IN entitlement;
		unauthenticated users receive null.
		When false, all users receive null regardless of entitlement status.
		"""
		useEntitlement: Boolean = true
	): TitleReviewSummary
	"""
	A truncated teaser preview of the review summary for this title.
	Returns null when no review summary is available for this title.

	---------------------
	Graphlet Details:
	- Name: ziggy
	- CTI: IMDb/GraphQL/Ziggy
	- Affected by headers: x-imdb-user-language
	- Cache TTL: 900 seconds
	- Slack Channel: #imdb-ziggy
	"""
	reviewSummaryTeaser: TitleReviewSummaryTeaser
	"""
	All Reviews for the title.

	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	reviews(
		after: ID
		filter: ReviewsFilter
		first: Int!
		sort: ReviewsSort
	): ReviewsConnection
	"""
	The runtime for the title, in seconds

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	runtime: Runtime
	"""
	All runtimes for the title, in seconds

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	runtimes(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): RuntimeConnection
	"""
	For titles which are part of a series, provides information about its position within the series

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	series: Series
	"""
	Sound track for a title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	soundtrack(
		after: ID
		before: ID
		first: Int
		jumpTo: ID
		last: Int
	): SoundtrackConnection
	"""
	A list of the languages spoken in the title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	spokenLanguages: SpokenLanguages
	"""
	The list of subtitle languages. Returns all subtitle languages when limit is not specified.
	Maximum number of subtitle languages for any title is typically under 50.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	subtitleLanguages(limit: Int): [SubtitleLanguage]
	"""
	Taglines for a title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	taglines(after: ID, before: ID, first: Int, last: Int): TaglineConnection
	"""
	Technical specifications for a title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	technicalSpecifications(
		filter: TechnicalSpecificationsFilter
	): TechnicalSpecifications
	"""
	Theatrical rental earnings (distributor's share of box office revenue) for this title,
	broken out by region.
	Returns null if no theatrical rental data exists for this title.
	@entitlement(PRO_PREMIUM)

	---------------------
	Graphlet Details:
	- Name: mojo
	- CTI: IMDb/GraphQL/Mojo
	- Affected by headers: x-imdb-client-name
	- Cache TTL: 3600 seconds
	- Slack Channel: #imdb-coverage-tech
	"""
	theatricalRentals: TitleTheatricalRentals
	"""
	The genres for the title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	titleGenres: TitleGenres
	"""
	The localized text for the title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	titleText: TitleText
	"""
	The type of a given title

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	titleType: TitleType
	"""
	The requesting user's notification preferences for this title.
	Throws FORBIDDEN if customer does not hve a pro subscription
	Throws UNAUTHENTICATED if logged out
	@entitlement(PRO_ANY_TIER)

	---------------------
	Graphlet Details:
	- Name: track
	- CTI: IMDb/GraphQL/Track
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	trackNotificationPreferences: TrackNotificationPreferences
	"""
	Trivia for a title.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	trivia(
		after: ID
		before: ID
		filter: TriviaFilter
		first: Int
		jumpTo: ID
		last: Int
	): TriviaConnection
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	triviaCategories(
		filter: TriviaCategoryWithTriviaFilter
	): [TriviaCategoryWithTrivia]
	"""
	Returns the 'trivia' Link.

	---------------------
	Graphlet Details:
	- Name: kahlo
	- CTI: IMDb/GraphQL/Kahlo
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-contributors-tech
	"""
	triviaContributionLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
	): ContributionLink
	"""
	Retrieve user's rating on a title. Returns null if rating does not exist.
	Error returned identifying specific error cases.
	- Throws FORBIDDEN for non-authenticated requests or if requesting user is not authorized to retrieve the rating.
	- Throws BAD_USER_INPUT if userId parameter is invalid.

	---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	userRating(
		"""
		Optional user id input, if it's not provided ratings retrieved for the authenticated user.
		"""
		userId: ID
	): Rating
	"""
	Returns the current signed-in customer's watched status of this title.
	CustomerId will be taken from x-imdb-customer-id header.
	- Throws UNAUTHENTICATED for non-authenticated requests.

	---------------------
	Graphlet Details:
	- Name: watched
	- CTI: IMDb/GraphQL/Watched
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	userWatchedStatus: WatchedStatus
	"""
	An ordered list of prominent videos related to a title
	If the first argument exceeds 100, the results will be capped to 100.
	When there is no sort passed in it, default to the current videoStrip sort logic and behavior.

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	videoStrip(
		after: ID
		filter: VideosQueryFilter
		first: Int
		sort: VideoSort
	): VideoConnection
	"""
	Videos related to the title, grouped by video type

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	videoTypes(filter: VideosQueryFilter, sort: VideoSort): [VideoTypeWithVideos]
	"""
	Returns the watch option from the given provider/location for this title if it is available.

	The location is required for some providers.

	---------------------
	Graphlet Details:
	- Name: watch
	- CTI: IMDb/GraphQL/Watch
	- Affected by headers: x-imdb-client-name, x-imdb-client-version, x-imdb-detected-country, x-imdb-detected-time-zone, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	watchOption(location: WatchOptionsLocation, providerId: ID!): WatchOption
	"""
	Watch options for a given titleId.
	The options are ordered in an order defined by business and grouped by the watch option category.

	If the location is included, returns any location-specific watch options (will be required in the future)
	Promoted provider feature is no longer supported by the Ads team - https://sim.amazon.com/issues/W2W-170. Passing value onto the optional parameter will not have any effect.

	Returns a list with the maximum number of watch option categories based on the filters. NO PAGINATION

	---------------------
	Graphlet Details:
	- Name: watch
	- CTI: IMDb/GraphQL/Watch
	- Affected by headers: x-imdb-client-name, x-imdb-client-version, x-imdb-detected-country, x-imdb-detected-time-zone, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	watchOptionsByCategory(
		limit: Int
		location: WatchOptionsLocation
		promotedProvider: String
		queryFilter: WatchOptionQueryFilter
	): CategorizedWatchOptionsList
	"""
	Watch options across all countries for a given titleId.

	Returns watch options grouped by country.
	Every country is returned even if there are no watch offers for that country. The countries are returned sorted by their country code.
	Individual watch options are returned in an order defined by the business in which they should be displayed, and grouped by the watch option category.
	Theatrical watch options are not included within this response due to their hyper-localized nature.

	If queryFilter is omitted, returns all available watch option categories.
	This query will return up to 1000 total watch options. If more than 1000 watch options are available, a service error will be thrown instead.
	NO PAGINATION

	---------------------
	Graphlet Details:
	- Name: watch
	- CTI: IMDb/GraphQL/Watch
	- Affected by headers: x-imdb-client-name, x-imdb-client-version, x-imdb-detected-country, x-imdb-detected-time-zone, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	watchOptionsByCountry(
		queryFilter: WatchOptionQueryFilter
	): [CountryWatchOptions]
}

"""
A single admission record — the number of theater tickets sold for a title
in a specific region on or around a specific date.
"""
type TitleAdmissionEntry {
	"""
	Number of theater admissions (tickets sold). Always >= 0.
	"""
	admissionCount: Int!
	"""
	Geographic area for this admission count.
	Null when the source data does not specify a region.
	"""
	area: BoxOfficeAreaType
	"""
	Supplementary context for this admission entry (e.g. 'opening week', '123 screens').
	Empty list if none.
	"""
	attributes: [LocalizedString]!
	"""
	Date of the admissions data. Null if no date is available.
	May be a partial date (e.g. only year and month are known).
	"""
	date: BoxOfficePartialDate
}

"""
Admissions data for a title.
Entries are ordered by region code, then by date descending (most recent first) within each region.
"""
type TitleAdmissions {
	"""
	All admission entries for this title, returned unpaginated.
	This is a small bounded list: entries are capped by the fixed set of
	countries that report theater admissions (~30-40 worldwide), with
	typically one cumulative figure per country. Expected maximum ~50 entries;
	most titles have fewer than 10.
	"""
	entries(
		"""
		Optional maximum number of entries to return, in the order described above.
		If omitted, all entries are returned. Must be >= 0.

		Intentionally optional (not the standard's `limit: Int!`): the primary
		consumer renders the full table (1:1 parity with the legacy box office tab),
		and the list is small and bounded (see above), so "return all" is the natural
		default. `limit` is a convenience for callers that only need the top entries.
		"""
		limit: Int
	): [TitleAdmissionEntry]!
}

input TitleBoxOfficeFilter {
	"""
	Inflation adjustment year (e.g. 2024). NOT YET SUPPORTED: providing any value
	currently throws BAD_USER_INPUT because this service has no CPI data source
	and can only serve nominal values. Omit to receive nominal values.
	"""
	adjustToYear: Int
	"""
	Area filter. If omitted, uses the first available area.
	"""
	area: BoxOfficeReleasesAreaFilter
	"""
	Interval type ID (from BoxOfficeIntervalType.id). If omitted, uses the default
	interval type. Throws BAD_USER_INPUT if the ID is invalid.
	"""
	intervalType: ID
	"""
	Release group to view. If omitted, uses the first release group. Throws
	BAD_USER_INPUT if the ID does not exist for this title.
	"""
	releaseGroupId: ID
}

type TitleBoxOfficeFilterOptions {
	"""
	Available areas (contextual to current release group selection).
	"""
	availableAreas: [BoxOfficeAreaType]!
	"""
	Which interval types have data (contextual to current selection).
	"""
	availableIntervalTypes: [BoxOfficeIntervalType]!
	"""
	All available release groups for this title.
	"""
	availableReleaseGroups: [TitleBoxOfficeReleaseGroup]!
	"""
	Currently selected area.
	"""
	selectedArea: BoxOfficeAreaType!
	"""
	Currently selected interval type.
	"""
	selectedIntervalType: BoxOfficeIntervalType!
	"""
	Currently selected release group.
	"""
	selectedReleaseGroup: TitleBoxOfficeReleaseGroup!
}

type TitleBoxOfficePerformance {
	"""
	Date when the box office data was last updated. Null if unknown.
	"""
	dataAsOfDate: Date
	"""
	Per-interval performance entries. Up to 200 entries per page.
	If `first` exceeds 200, only 200 will be returned.
	Default ordering: date interval descending (most recent first).
	"""
	entries(
		"""
		Cursor for pagination. If omitted, returns results from the beginning.
		"""
		after: ID
		first: Int!
		sort: TitleBoxOfficeSortInput
	): BoxOfficeReleasePerformanceConnection!
	"""
	Filter options contextual to the current selection.
	"""
	filterOptions: TitleBoxOfficeFilterOptions!
	"""
	Lifetime grosses per area for the selected release group.
	"""
	lifetimeGrosses: [TitleLifetimeGross]
}

"""
A release group represents a distinct theatrical release of a title
(e.g. original release, IMAX re-release, anniversary re-release).
"""
type TitleBoxOfficeReleaseGroup {
	id: ID!
	language: DisplayableLanguage!
	"""
	Generated display text (e.g. 'Inception', 'Inception 2011 IMAX').
	"""
	text: String!
}

enum TitleBoxOfficeSortBy {
	"""
	ASC: earliest date interval first. DESC: most recent date interval first.
	"""
	DATE_INTERVAL
	"""
	ASC: lowest gross first. DESC: highest gross first.
	"""
	GROSS
	"""
	ASC: lowest cumulative gross first. DESC: highest cumulative gross first.
	"""
	GROSS_TO_DATE
	"""
	ASC: lowest per-theater average first. DESC: highest per-theater average first.
	"""
	PER_THEATER_AVERAGE
	"""
	ASC: lowest rank number first (best ranked). DESC: highest rank number first.
	"""
	RANK
	"""
	ASC: earliest release date first. DESC: most recent release date first.
	"""
	RELEASE_DATE
	"""
	ASC: fewest theaters first. DESC: most theaters first.
	"""
	THEATERS
	"""
	ASC: largest decline first. DESC: largest increase first.
	"""
	WEEKLY_DELTA
}

input TitleBoxOfficeSortInput {
	by: TitleBoxOfficeSortBy!
	"""
	If omitted, defaults to ASC for RANK, DESC for others.
	"""
	order: SortOrder
}

type TitleChartMetadata {
	chartDescription: LocalizedString!
	chartName: LocalizedString!
}

type TitleChartRankingsConnection {
	edges: [TitleChartRankingsEdge]!
	metadata: TitleChartMetadata!
	pageInfo: PageInfo!
}

type TitleChartRankingsEdge {
	cursor: ID!
	node: TitleChartRankingsNode!
	position: Int!
}

input TitleChartRankingsInput {
	rankingsChartType: TitleChartRankingsType!
}

type TitleChartRankingsNode {
	"""
	Rating for title that appears on the chart that may be different from ratings on other properties
	See for details: https://w.amazon.com/bin/view/IMDb/Ratings/Build/Logic/
	"""
	chartRating: Float!
	"""
	Count of votes used in determining chart rating for title
	"""
	chartVoteCount: Int!
	item: Title!
}

"""
Enum exposing all the available Title Chart Ranking Types
"""
enum TitleChartRankingsType {
	"""
	Overall IMDb Bottom 100 Feature List
	"""
	BOTTOM_100
	"""
	Top 50 Bengali Feature List
	"""
	TOP_50_BENGALI
	"""
	Top 50 Malayalam Feature List
	"""
	TOP_50_MALAYALAM
	"""
	Top 50 Tamil Feature List
	"""
	TOP_50_TAMIL
	"""
	Top 50 Telugu Feature List
	"""
	TOP_50_TELUGU
	"""
	Overall IMDb Top 250 Feature List
	"""
	TOP_250
	"""
	Top 250 English Feature List
	"""
	TOP_250_ENGLISH
	"""
	Top 250 Indian Feature List
	"""
	TOP_250_INDIA
	"""
	Overall IMDb Top 250 TV List
	"""
	TOP_250_TV
}

"""
Showtimes grouped by screening type for a specific title and cinema.
"""
type TitleCinemaShowtimesByScreeningType {
	"""
	The cinema related to the set of showtimes given in this object.
	"""
	cinema: Cinema!
	"""
	Distance from the requested location to this cinema.
	If no location input is provided, this field will be null.
	"""
	distanceToCinema: DistanceToCinema
	"""
	Unique identifier for this TitleCinemaShowtimes.
	"""
	id: ID!
	"""
	List of showtimes for this title at this cinema by screening type.
	"""
	showtimesByScreeningType: [ShowtimesByScreeningType]!
	"""
	The title related to the set of showtimes given in this object.
	"""
	title: Title!
}

"""
Connection type for paginated cinema showtimes by screening type results.
"""
type TitleCinemaShowtimesByScreeningTypeConnection {
	"""
	Edges of TitleCinemaShowtimesByScreeningType.
	"""
	edges: [TitleCinemaShowtimesByScreeningTypeEdge]!
	"""
	Contains information about the fallback results when no showtimes were found in the original response.
	Only populated when fallback is provided as an input and fallback results were found.
	"""
	fallback: TitleCinemaShowtimesFallbackResult
	"""
	Pagination information.
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set.
	"""
	total: Int!
}

"""
Edge type for cinema showtimes by screening type for a given title in a paginated result.
"""
type TitleCinemaShowtimesByScreeningTypeEdge {
	"""
	Cursor for pagination.
	"""
	cursor: ID!
	"""
	Cinema showtime information for the title.
	"""
	node: TitleCinemaShowtimesByScreeningType!
	"""
	Position of this edge in the result set.
	"""
	position: Int!
}

"""
If specified and the original query returns no showtimes, determines how results should be adjusted.
See calling field for more information.
"""
enum TitleCinemaShowtimesFallbackOption {
	"""
	Fallback to the next available date of showtimes from the provided `filter.dateRange.startTime` input value, if the original result contains no showtimes.
	"""
	NEXT_AVAILABLE_DATE
}

"""
Contains information about fallback results when the original query returned no showtimes data.
Only populated when fallback option is specified and fallback results are found.
See calling field for more information.
"""
type TitleCinemaShowtimesFallbackResult {
	"""
	The ISO 8601 date that the graphlet fell back to.
	"""
	fallbackDate: Date!
}

"""
Input type for filtering cinema showtimes for a title.
"""
input TitleCinemaShowtimesFilter {
	"""
	Filter to only include specific cinemas (e.g., `ci1234567`).
	When combined with dateRange, will only return cinemas that have showtimes within the specified dates.
	Exactly one of `anyCinemaIds` OR `location` must be provided, but not both.
	Throws `BAD_USER_INPUT` if more than 400 unique cinemas are provided.
	"""
	anyCinemaIds: [ID]
	"""
	Filter showtimes by date range.
	"""
	dateRange: ShowtimesTitlesDateRangeFilter
	"""
	Filter cinemas based on favorite status.
	If not provided, defaults to return all cinemas.
	"""
	favorites: FavoriteFilterOption
	"""
	Filter for cinemas within a certain location.
	If Provided, the returned array will be in ascending distance from this location.
	Exactly one of `anyCinemaIds` OR `location` must be provided, but not both.
	"""
	location: ShowtimesLocation
}

"""
Title connection details
"""
type TitleConnection {
	"""
	The other title in this connection
	"""
	associatedTitle: Title!
	"""
	The category of this connection
	"""
	category: TitleConnectionCategory!
	"""
	The unique identifier for this title connection. This ID represents a
	persistent reference to the connection relationship between two titles.
	Returns null when the connection does not have a persistent identifier assigned in the source data.
	Can be used by clients to uniquely identify and track specific connection relationships.
	"""
	connectionId: ID
	"""
	A description of how these two titles are connected, can contain markdown links to other names and titles
	"""
	description: Markdown
}

type TitleConnectionCategory {
	id: ID!
	text: String!
}

"""
Pagination for title connection which always has a known total number of edges.
"""
type TitleConnectionConnection {
	"""
	Pages of title connections
	"""
	edges: [TitleConnectionEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of title connection
"""
type TitleConnectionEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A TitleConnection node
	"""
	node: TitleConnection!
	"""
	Position of the node in this context, eg: TitleConnection 3 out of 340
	"""
	position: Int!
}

"""
A copyright holder entry for a title.
"""
type TitleCopyright {
	"""
	The copyright entry, as it appears in the credits, on print, in a registry, etc.
	"""
	copyright: String!
	"""
	Optional free-text attributes qualifying the copyright entry.
	"""
	freeTextAttributes: [String]
}

type TitleCreditCategoryWithCredits {
	"""
	Returns the credit category for the grouping, default is singular version.
	"""
	category(variant: CreditCategoryDisplayVariant = SINGLE): CreditCategory!
	credits(first: Int): CreditConnection
}

input TitleCreditCategoryWithCreditsFilter {
	credited: CreditedOrUncreditedFilter
	excludePrincipal: Boolean
	names: [ID]
}

"""
Example:
1. if you want to match titles with name const `nm0000138` AND `nm0000206` then specify:
`
allCredits: [{nameId: 'nm0000138'}, {nameId: 'nm0000206'}]
`

2. if you want to match titles where name const `nm0000138` was a `Director` AND `nm0000206` was a `Producer` then
specify:
`
allCredits: [{nameId: 'nm0000138', jobCategory: 'Director'}, {nameId: 'nm0000206', jobCategory: 'Producer'}]
`

3. if you want to match titles WITHOUT a `Writer` credit then specify:
`
excludeCredits: [{jobCategory: 'Writer'}]
`

4. if you want to match Titles where `nm0185819` played `James Bond` AND `nm1560977` was a `Director` then specify:
`
allCredits: [{nameId: 'nm0185819', character: 'James Bond'}, {nameId: 'nm1560977', jobCategory: 'Director'}]
`

5. if you want to match Titles where a character was called `Mario` then specifiy:
`
allCredits: [{character: 'Mario'}]
`
"""
input TitleCreditsConstraint {
	"""
	A title match must contain ALL of these credits
	"""
	allCredits: [TitleCreditSearchInput]
	"""
	A title match must contain AT LEAST ONE of these credits
	"""
	anyCredits: [TitleCreditSearchInput]
	"""
	A title match must contain NONE of these credits

	If the same input is specified in the any/all and exclude constraints,
	then the results should be empty.
	"""
	excludeCredits: [TitleCreditSearchInput]
}

"""
If none of the fields is provided the input will be ignored.
"""
input TitleCreditSearchInput {
	"""
	The Character name associated with the credit
	"""
	character: String
	"""
	The credit category associated with the credit.

	Valid credit categories can be fetched from the `NAME_CREDIT_CATEGORIES` facet, or from
	the `nameMetadata.nameCreditCategories` query
	"""
	creditCategory: String
	"""
	The job category associated with the credit.

	Valid job categories can be fetched from the `NAME_JOB_CATEGORIES` facet, or from
	the `nameMetadata.nameCreditCategories` query
	"""
	jobCategory: String
	"""
	The NameId associated with the title credit.
	"""
	nameId: ID
	"""
	The occupation or specific job title associated with the credit.
	This allows for more granular searching than jobCategory, supporting
	specific roles like "Junior VFX Artist", "Lead Animator", etc.

	When omitted or null, no occupation-based filtering is applied. The search will
	match titles based on other specified constraints (character, nameId, jobCategory, etc.).
	"""
	occupation: String
}

input TitleCreditsFilter {
	"""
	Filter for specific credit categories
	"""
	categories: [ID]
	"""
	Filter for credited or uncredited credits only
	"""
	credited: CreditedOrUncreditedFilter
	"""
	Filter out specific credit categories
	"""
	excludeCategories: [ID]
	excludePrincipal: Boolean
	"""
	Filter credits on these specific names
	"""
	names: [ID]
}

input TitleCreditSort {
	by: TitleCreditSortBy
	order: SortOrder
}

enum TitleCreditSortBy {
	"""
	Sort by popular cast members
	"""
	TOP_CAST
}

input TitleCreditsV2Filter {
	"""
	Filters that apply at the individual contributed credit level.
	"""
	creditLevelFilter: CreditLevelCreditsFilterSet
	"""
	Filters that apply to credits at the name level.
	"""
	nameLevelFilter: NameLevelCreditsFilterSet
}

input TitleCreditsV2Sort {
	"""
	This is modelled as a string to allow for altering the list of allowed values over time. Supported values are:

	CREDIT_IMPORTANCE: Credit ordering follows this priority sequence:
	1. Grouping - Based on a fixed order of groupings, chosen to be similar to that seen on movie or TV end credits.
	2. Top Stars Ranking - Priority based on special top performers list, for cast only.
	3. Credited Status - Credited roles take precedence over uncredited roles.
	4. Episode Count - For a series, higher episode counts appear first (names with more episodes rank higher).
	5. Order - For a series, sum the order values across all episodes. For non-series, use the order value for the credit.
	   (Order numbers are ignored when a large order value gap is found between 2 credits.)
	6. Grouped Order - Order based on multi-level groupings, as seen in Writer category with sub-groupings.
	7. Chronological Order - For a series, sort earlier start years ahead of later start years. Similar logic for end years.
	8. Name Text - Alphabetical ordering by name, which typically treats "Firstname Lastname" as "Lastname, Firstname".
	9. Name ID - A final tie-break by name ID to ensure consistent ordering (smallest first).

	CREDIT_IMPORTANCE is the default.

	The sort order can be controlled using the order parameter (ASC/DESC). When DESC (default), higher importance
	credits appear first. When ASC, lower importance credits appear first.
	"""
	by: String
	order: SortOrder
}

enum TitleDataType {
	ALTERNATE_VERSION
	AWARD
	BUSINESS_INFO
	CRAZY_CREDIT
	GOOF
	LOCATION
	PLOT
	QUOTE
	SOUNDTRACK
	TECHNICAL
	TRIVIA
}

type TitleDisplayOutput {
	"""
	Current user's country or region selection for title display. ID in ISO 3166 format.
	"""
	country: ID
	"""
	Current user's language selection for title display. ISO 639 format.
	"""
	language: ID
	"""
	Current user's reference view selection for contributors.
	"""
	prefersReferenceView: Boolean!
}

input TitleEpisodeCreditsFilter {
	"""
	Filters that apply at the individual contributed credit level.
	These filters operate on the credit itself (groupings, traits, credited status, etc.).
	"""
	creditLevelFilter: CreditLevelCreditsFilterSet
	"""
	Filters that apply at the episode level.
	"""
	episodes: EpisodesFilter
	"""
	Filters that apply to credits at the name level.
	"""
	nameLevelFilter: NameLevelCreditsFilterSet
}

"""
Pagination for title facets
"""
type TitleFacetConnection {
	edges: [TitleFacetEdge]!
	pageInfo: PageInfo!
	"""
	Provides information on the total number of titles you could filter on
	"""
	total: Int!
}

type TitleFacetEdge {
	cursor: ID!
	node: Title!
	position: Int!
	"""
	How many items are associated with this title node
	"""
	total: Int!
}

enum TitleFacetField {
	AWARD_WINS
	CERTIFICATES
	COUNTRY_OF_ORIGIN
	EVENTS
	GENRES
	INTERESTS
	KEYWORDS
	NAME_CREDIT_CATEGORIES
	NAME_JOB_CATEGORIES
	PRIMARY_LANGUAGE
	PRODUCTION_STATUS
	PRODUCTION_STATUS_UPDATE_DAYS
	PROFESSIONS_ATTACHED
	PROFESSIONS_NOT_ATTACHED
	RELEASE_COUNTRY
	RELEASE_YEAR
	TITLE_TYPE
	"""
	US-only certificates facet. Returns certificate ratings filtered to US region only,
	with counts representing unique titles per rating. Use this instead of CERTIFICATES
	when you need accurate US certificate counts that match constraint filtering behavior.
	"""
	US_CERTIFICATES
	WATCH_PROVIDERS
}

type TitleGenre {
	genre: GenreItem!
	relevanceRanking: Int!
	subGenres: [TitleKeyword]!
}

type TitleGenreRecommendation {
	"""
	Text to be displayed as a subheading for the carousel, explaining why we recommend those titles
	"""
	explanation: LocalizedMarkdown!
	"""
	Text to be displayed as the title for the carousel
	"""
	label: LocalizedString!
	refTag: String!
	titles(after: String, first: Int!): TitleGenreRecommendationConnection
}

type TitleGenreRecommendationConnection {
	edges: [TitleGenreRecommendationEdge]!
	pageInfo: PageInfo!
}

type TitleGenreRecommendationEdge {
	cursor: ID!
	node: Title!
}

type TitleGenres {
	genres(limit: Int, sort: GenreSort): [TitleGenre]!
}

"""
TitleKeyword details
"""
type TitleKeyword {
	"""
	Votes from users about whether this keyword item is interesting
	"""
	interestScore: InterestScore!
	"""
	Item scope keyword category, i.e. category assigned for keyword inside title
	"""
	itemCategory: TitleKeywordItemCategory
	"""
	The keyword
	"""
	keyword: Keyword!
	"""
	Legacy identifier intended for search
	"""
	legacyId: String!
}

"""
Pagination for keywords which always has a known total number of edges.
"""
type TitleKeywordConnection {
	"""
	Pages of keywords
	"""
	edges: [TitleKeywordEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of keywords
"""
type TitleKeywordEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A TitleKeyword node
	"""
	node: TitleKeyword!
	"""
	Position of the node in this context, eg: TitleKeyword 3 out of 340
	"""
	position: Int!
}

input TitleKeywordItemCategoriesFilter {
	"""
	Filter out specific title keywords categories
	"""
	excludeItemCategories: [String]
	"""
	Filter for specific title keywords categories
	"""
	itemCategories: [String]
}

"""
TitleKeyword item scope category details
"""
type TitleKeywordItemCategory {
	"""
	An opaque ID, suitable for caching the concept
	"""
	id: ID!
	"""
	Not localized category value, e.g. plot-detail, can be used for search and filters
	"""
	itemCategoryId: String!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	"""
	Category display text
	"""
	text: String!
}

type TitleKeywordItemCategoryWithKeywords {
	"""
	Keyword item scope category
	"""
	itemCategory: TitleKeywordItemCategory!
	"""
	List of categorized keywords with the same category
	"""
	keywords(
		"""
		Cursor for forward pagination. Use the endCursor from pageInfo to fetch the next page.
		Each category has its own cursor — when paginating, filter to a single category on the parent field.
		Pass the same sort and filter parameters with your cursor to get consistent results.

		Example:
		    keywordItemCategories(filter: { itemCategories: ["plot-detail"] }, keywordSort: { by: RELEVANCE }) {
		      keywords(first: 50, after: "a3cwMDE2Nzcz") {
		        pageInfo { endCursor hasNextPage }
		        edges { node { text } }
		      }
		    }
		"""
		after: ID
		"""
		Number of keywords to return. Maximum page size is 250.
		If first is greater than 250, only 250 keywords will be returned.
		When omitted, returns all available keywords (up to the maximum page size of 250).
		"""
		first: Int
	): TitleKeywordConnection!
}

input TitleKeywordsSort {
	by: TitleKeywordsSortBy!
}

enum TitleKeywordsSortBy {
	ALPHABETICAL
}

"""
Filters that apply to credits at the title level.
Each specified filter is AND-ed together.
For filters that accept a list of values, the filter is an OR across that list, so any
credit matching one value meets the filter's criteria.
"""
input TitleLevelCreditsFilterSet {
	"""
	Filter out credits on titles with specific genres.
	The list of genres can be accessed from titleMetadata.titleGenres.
	By default, credits for all genres are included.
	"""
	excludeGenres: [ID]
	"""
	Filter out specific production stages.
	Cannot be used in conjunction with "productionStage": a title can only have one of these values;
	if you specify values to include and exclude, the behaviour for unspecified values is ambiguous.
	By default, credits for all production stages are included.
	"""
	excludeProductionStage: [ProductionStageFilter]
	"""
	Filter out credits for titles with specific titleTypes.
	The list of titleTypes can be accessed from titleMetadata.titleTypes.
	Cannot be used in conjunction with "titleType": a title can only have one of these values;
	if you specify values to include and exclude, the behaviour for unspecified values is ambiguous.
	By default, credits for all title types are included.
	"""
	excludeTitleType: [ID]
	"""
	Filter for credits on titles with specific genres.
	The list of genres can be accessed from titleMetadata.titleGenres.
	By default, credits for all genres are included.
	"""
	genres: [ID]
	"""
	Filter for specific production stages.
	Cannot be used in conjunction with "excludeProductionStage": a title can only have one of these values;
	if you specify values to include and exclude, the behaviour for unspecified values is ambiguous.
	By default, credits for all production stages are included.
	"""
	productionStage: [ProductionStageFilter]
	"""
	Filter for a project status of UPCOMING, PREVIOUS or PROJECT_IN_DEVELOPMENT.
	By default, credits for all project statuses are included. Note that PROJECT_IN_DEVELOPMENT titles are not included
	when the request has no Pro tier entitlement.
	"""
	projectStatus: [ID]
	"""
	Filter credits to only those for these specific titles.
	By default, credits for all titles are included.
	"""
	titleId: [ID]
	"""
	Filter credits for titles with specific titleTypes.
	The list of titleTypes can be accessed from titleMetadata.titleTypes.
	Cannot be used in conjunction with "excludeTitleType": a title can only have one of these values;
	if you specify values to include and exclude, the behaviour for unspecified values is ambiguous.
	By default, credits for all title types are included.
	"""
	titleType: [ID]
	"""
	Filter credits for titles with specific titleTypeCategory.
	By default, credits for all title type categories are included.
	"""
	titleTypeCategory: [TitleTypeCategoryValue]
}

type TitleLifetimeGross {
	"""
	The geographic area for this lifetime gross.
	"""
	area: BoxOfficeAreaType!
	"""
	Lifetime cumulative gross for the selected release group in this area (nominal, not inflation-adjusted).
	"""
	gross: BoxOfficeGross!
}

type TitleListItemSearchConnection {
	edges: [TitleListItemSearchEdge]!
	facet(facetField: TitleFacetField!, limit: Int): [SearchFacet]
	"""
	The number of titles in the filtered list that a customer has rated.
	Cutomer Id is required for this field or an authentication error will be thrown.
	"""
	myRatedCount: Int
	"""
	The percentage of titles in the filtered list that a customer has rated.  We round the percentage
	to the nearest integer but we may change the precision depending on Product's requirement update.
	Customer Id is required for this field or an authentication error will be thrown.
	"""
	myRatedPercentage: LocalizedString
	pageInfo: PageInfo!
	total: Int!
}

type TitleListItemSearchEdge {
	cursor: ID!
	node: ListItemSearchNode!
	title: Title!
}

input TitleListSearchSort {
	by: TitleListSearchSortBy!
	order: SortOrder!
}

enum TitleListSearchSortBy {
	"""
	Gross revenue pulled in via box-office in Domestic market for entire lifetime of title.
	Domestic refers to North America (U.S., Canada, and Puerto Rico)
	ASC: Lower numbers means the title has pulled in less box-office revenue, so poorer performing titles will be first.
	"""
	BOX_OFFICE_GROSS_DOMESTIC
	"""
	Date when the list item was added to the list
	ASC: Earlier date will be first.
	"""
	DATE_ADDED
	"""
	List order determined by the customer
	"""
	LIST_ORDER
	"""
	Overall Metascore based on critic reviews. Titles without a metascore are
	placed at the end when using ASC sort order.
	ASC: Lower Metacritic score means the title is rated more poorly, so titles with worse scores will be first.
	"""
	METACRITIC_SCORE
	"""
	Star Rating given by the requesting user.
	ASC: Lower star rating means the title the user rated the title more poorly, so most disliked titles will be first.
	"""
	MY_RATING
	"""
	Date when customer rated a title.
	ASC: Earlier (older) ratings will be first.
	"""
	MY_RATING_DATE
	"""
	TitleMeterType.TITLE_METER (aka Pro MOVIEMeter). Score given to non-episodic title types.
	ASC: Lower popularity score means that the title is more popular, so the most popular titles will be first.
	"""
	POPULARITY
	"""
	Sort results based on specified ranking algorithm.  For the advancedTitleSearch query, exactly one ranked title list
	constraint must be specified for using this sort option.
	ASC: Higher ranks will be first.
	"""
	RANKING
	"""
	Earliest wide release date of a title. Titles without a release date are
	placed at the end when using ASC sort order.
	ASC: Earlier (older) released title will be first.
	"""
	RELEASE_DATE
	"""
	The length of the title in terms of runtime.
	ASC: Lower runtime means the title is shorter, so shortest titles will be first.
	"""
	RUNTIME
	"""
	Star Rating given by the specified user in the SpecifiedUserRatingSearchConstraint.
	Throws BAD_USER_INPUT if the SpecifiedUserRatingSearchConstraint is not specified.
	ASC: Lower star rating means the title the user rated the title more poorly, so most disliked titles will be first.
	"""
	SINGLE_USER_RATING
	"""
	Date when the specified user rated a title.
	Throws BAD_USER_INPUT if the SpecifiedUserRatingSearchConstraint is not specified.
	ASC: Earlier (older) ratings will be first.
	"""
	SINGLE_USER_RATING_DATE
	"""
	Alphabetical sorting based on regional title text as determined by user language preferences.
	Language preference is determined by x-imdb-user-country and x-imdb-user-language headers.
	Only supports the languages/regions we support for localized search. Defaults to original title otherwise.
	ASC: Lower numbers and letters near the top of the alphabet will be returned first.
	"""
	TITLE_REGIONAL
	"""
	Weighted IMDb Star Rating as determined by users
	Note: IMDb maintains a threshold to a minimum number of ratings before it is considered.
	ASC: Lower star rating means the title is rated more poorly, so titles with worse ratings will be first.
	"""
	USER_RATING
	"""
	Count of ratings given by users
	Note: IMDb maintains a threshold to a minimum number of ratings before it is considered.
	ASC: Lower count of ratings means the title has been rated a fewer number of
	times, so titles with least ratings will be first.
	"""
	USER_RATING_COUNT
	"""
	The recognized year of the title. Typically, the release year, but guidelines are here:
	https://help.imdb.com/article/contribution/titles/title-formatting/G56U5ERK7YY47CQB
	ASC: Earlier (older) titles will be first.
	"""
	YEAR
}

type TitleMeta {
	canonicalId: ID!
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	publicationStatus: PublicationStatus!
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	restrictions: TitleMetaRestrictions
}

type TitleMetadata {
	"""
	All the valid external link categories
	"""
	externalLinkCategories: [ExternalLinkCategory]!
	"""
	All the valid goof types
	"""
	goofCategories: [GoofCategory]!
	"""
	All the connection categories
	"""
	titleConnectionCategories: [TitleConnectionCategory]!
	"""
	All title Genres.
	"""
	titleGenres: [GenreItem]!
	"""
	All title type categories with title types.
	"""
	titleTypeCategories: [TitleTypeCategoryWithTitleTypes]!
	"""
	All title types, or all title types filtered by category.
	"""
	titleTypes(category: TitleTypeCategoryValue): [TitleType]!
}

"""
Information about restrictions applied to meta
"""
type TitleMetaRestrictions {
	"""
	Explanation(s) for the restriction
	"""
	explanations: [RestrictionExplanation]!
	reasons: [ContentRestrictionReason]!
	"""
	DEPRECATED: Renamed, use `reasons` instead.
	"""
	restrictionReason: [ContentRestrictionReason]!
}

"""
Ranking of a Title within a set of dimensions
"""
type TitleMeterInsight {
	"""
	The insight chart identifier
	"""
	chartId: ID
	"""
	The insight dimensions for which the rank was computed
	"""
	dimensions: [MeterInsightDimension]!
	"""
	The displayable text for the insight
	"""
	insightText: MeterInsightText
	"""
	The insight type
	"""
	insightType: MeterInsightType!
	"""
	The Pro Meter type used for the insight
	"""
	meterType: TitleMeterType
	"""
	The current rank within the category defined by the insight dimensions
	"""
	rank: Int!
}

input TitleMeterInsightsInput {
	"""
	Maximum number of insights to return
	"""
	limit: Int
	"""
	If omitted, the meter type used will either be the pure MovieMeter or TVMeter value depending on the title type.
	"""
	meterType: TitleMeterType
}

type TitleMeterRanking {
	"""
	The current IMDbPro meter ranking of the title. A lower value is better.
	Ranking information beyond 5000 requires entitlement proving Pro subscription.
	"""
	currentRank: Int!
	"""
	The Pro Meter type that this rank represents
	"""
	meterType: TitleMeterType
	"""
	If a previous rank exists, this is a set of fields comparing the current rank
	to the previous rank
	"""
	rankChange: MeterRankChange
}

"""
Requires entitlement proving Pro subscription.
"""
type TitleMeterRankingHistory {
	"""
	The best historical IMDbPro Title Meter ranking of a title.
	A lower value is better. By default, returns the values over all time.
	Requires entitlement proving Pro subscription.
	"""
	bestRank: MeterRankingHistoryEntry
	"""
	History of IMDbPro Title meter rankings of a name.
	"""
	ranks: [MeterRankingHistoryEntry]
	restriction: MeterRestriction
}

input TitleMeterRankingHistoryInput {
	"""
	Last date to obtain Title meter history for. If not provided, default is most recent ranking date.
	If there is no ranking on end date, the date is the most recent date before this one.
	"""
	endDate: Date
	"""
	Type of ranking history being requested.
	If omitted, the meter type used will either be the pure MovieMeter, TVMeter, or TitleMeter value depending
	on the meter type.
	"""
	meterType: TitleMeterType
	"""
	First date to obtain Title meter history for. If not provided, default is first ranking date.
	If there is no ranking on start date, the date is the earliest date after this one.
	"""
	startDate: Date
}

input TitleMeterRankingInput {
	"""
	If omitted, the meter type used will either be the pure MovieMeter or TVMeter value depending on the title type.
	"""
	meterType: TitleMeterType
}

input TitleMeterSearchConstraint {
	"""
	Match titles that have a current Title Meter value that falls in the specified range
	At this time, we only support max up to 100 for TV_METER and MOVIE_METER.
	"""
	rankRange: IntRangeInput
	"""
	The default is TITLE_METER
	"""
	titleMeterType: TitleMeterType
}

enum TitleMeterType {
	"""
	Ranking that aggregates titles of type movie, short, and video
	"""
	MOVIE_METER
	"""
	Ranking that aggregates all title types aside from episodes. Best used for sorting.
	"""
	TITLE_METER
	"""
	Ranking that aggregates titles of type tv_series, tv_miniseries, tv_movie, tv_short, and tv_special
	"""
	TV_METER
}

enum TitleProductCategory {
	"""
	Title classified under movies, TV, or OTT streaming content.
	"""
	MOVIES_TV_OTT
	"""
	Title classified as a music video.
	"""
	MUSIC_VIDEO
}

"""
Quote details
"""
type TitleQuote {
	"""
	The quote as a displayable article
	"""
	displayableArticle: DisplayableArticle!
	"""
	Title Quote ID
	"""
	id: ID!
	"""
	Votes from users about whether this Quote is interesting
	"""
	interestScore: InterestScore!
	"""
	Is this Title Quote a spoiler
	"""
	isSpoiler: Boolean!
	"""
	The language of this Title Quote
	"""
	language: DisplayableLanguage!
	"""
	The verbal and/or non-verbal lines that make up this Title Quote
	"""
	lines: [TitleQuoteLine]!
	"""
	Machine translated displayable article. Returns the displayable article translated based on the language header.
	"""
	machineTranslatedDisplayableArticle: MachineTranslatedDisplayableArticle
}

"""
A character speaking a line in a Title Quote
"""
type TitleQuoteCharacter {
	"""
	The name of the character in the Title
	"""
	character: String!
	"""
	The Name behind that character
	"""
	name: Name
}

"""
Pagination for Quote types which always has a known total number of edges.
"""
type TitleQuoteConnection {
	"""
	Pages of Quotes
	"""
	edges: [TitleQuoteEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of Quotes
"""
type TitleQuoteEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A Quote node
	"""
	node: TitleQuote!
	"""
	Position of the node in this context, eg: Quote 3 out of 27
	"""
	position: Int!
}

"""
A specific line in the Title Quote. Can be a verbal line with characters speaking or stage directions
"""
type TitleQuoteLine {
	"""
	The characters who speak this line, e.g.  'Rick'. Not required: a line may be non-verbal
	"""
	characters: [TitleQuoteCharacter]
	"""
	Machine translated stage direction. Returns the stage direction field translated based on the language header.
	"""
	machineTranslatedStageDirection: MachineTranslatedString
	"""
	Machine translated quote line text. Returns the text field translated based on the language header.
	"""
	machineTranslatedText: MachineTranslatedString
	"""
	Stage direction, e.g. 'Rick gently places his hand under her chin and raises it so their eyes meet'. Not required.
	"""
	stageDirection: String
	"""
	The body of the quotation line, e.g 'Here's looking at you kid. '.  Not
	required: you may have stage directions with no dialogue.
	"""
	text: String
}

"""
Example: if you want to match titles of quote 'actually mammals' OR 'planet', PLUS
the title must contain 'cancer', specify anyQuoteTextTerms: ['actually mammals', 'planet'] plus
allQuoteTextTerms: ['cancer'].
"""
input TitleQuoteMatchingSearchConstraint {
	"""
	A title match must contain ALL of these search terms in at least one of its quotes.
	"""
	allQuoteTextTerms: [String]
	"""
	A title match must contain AT LEAST ONE of these search terms in at least one of its quotes.
	"""
	anyQuoteTextTerms: [String]
}

input TitleQuotesFilter {
	"""
	Only return Quotes in which one of these Names are present
	"""
	names: [ID]
	"""
	Filter or only return spoilers
	"""
	spoilers: FilterSpoilers
}

type TitleRecommendation {
	explanations: [RecommendationExplanation]!
	refTag: String!
	title: Title!
}

type TitleRecommendationConnection {
	edges: [TitleRecommendationEdge]!
	pageInfo: PageInfo!
}

type TitleRecommendationEdge {
	cursor: ID!
	node: TitleRecommendation!
}

input TitleRecommendationsFilter {
	"""
	Includes title recommendation titles for below title type category values. At time of writing, titleRecommendations() only
	supports movie and tv title type categories, and can only support filtering for a single title type.
	"""
	includeTitleTypes: [TitleTypeCategoryValue]
}

input TitleReleaseDatesFilter {
	"""
	Filter release dates for specific countries"
	The country code - either an ISO 3166 code or an internally defined code if no ISO code exists for that country.
	"""
	countries: [ID]
	"""
	Filter release dates
	"""
	wideRelease: WideReleaseFilter
}

"""
A review that mentions a theme, and an excerpt from it.
"""
type TitleReviewExcerpt {
	"""
	The excerpt from the review (optional)
	"""
	excerpt: LocalizedMarkdown
	"""
	The review
	"""
	review: Review!
}

"""
Review summaries at different length levels for a particular sentiment
"""
type TitleReviewSentimentSection {
	"""
	Long summary
	"""
	long: LocalizedMarkdown!
	"""
	Machine translated long summary
	"""
	machineTranslatedLong: MachineTranslatedMarkdown!
	"""
	Machine translated medium summary
	"""
	machineTranslatedMedium: MachineTranslatedMarkdown!
	"""
	Machine translated short summary
	"""
	machineTranslatedShort: MachineTranslatedMarkdown!
	"""
	Medium length summary
	"""
	medium: LocalizedMarkdown!
	"""
	Short summary
	"""
	short: LocalizedMarkdown!
}

"""
Summary and themes of reviews for a title
"""
type TitleReviewSummary {
	"""
	Summary focused on negative reviews (optional)
	"""
	negative: TitleReviewSentimentSection
	"""
	Overall summary of reviews across all sentiments
	"""
	overall: TitleReviewSentimentSection!
	"""
	Summary focused on positive reviews (optional)
	"""
	positive: TitleReviewSentimentSection
	"""
	ISO-8601 timestamp of when this review summary was last published.
	Null means the publish time is unknown, not that the summary is unpublished.
	"""
	publishedAt: DateTime
	"""
	Common themes discussed across reviews
	"""
	themes(
		"""
		Filter criteria for themes (if missing, all themes are returned)
		"""
		filter: TitleReviewSummaryThemesFilter
		"""
		Maximum number of themes to return (if missing, all (up to 10) themes are returned)
		"""
		limit: Int
	): [TitleReviewTheme]
}

"""
A truncated teaser preview of the review summary for this title.
"""
type TitleReviewSummaryTeaser {
	"""
	Machine translated teaser
	"""
	machineTranslatedTeaser: MachineTranslatedMarkdown!
	"""
	Teaser of the overall review summary
	"""
	teaser: LocalizedMarkdown!
}

"""
Input type for filtering themes in a title's review summary
"""
input TitleReviewSummaryThemesFilter {
	"""
	Exclude themes with these sentiments
	"""
	excludeSentiments: [TitleReviewThemeSentiment]
	"""
	Only include themes with these sentiments
	"""
	includeSentiments: [TitleReviewThemeSentiment]
	"""
	Filter to specific theme IDs
	"""
	themeIds: [ID]
}

"""
A theme identified across multiple reviews
"""
type TitleReviewTheme {
	"""
	Displayable label for the theme, e.g. ('Intense')
	"""
	label: LocalizedString!
	"""
	Machine translated summary text
	"""
	machineTranslatedSummary: MachineTranslatedMarkdown!
	"""
	Reviews/excerpts related to this theme
	"""
	reviews(
		"""
		Maximum number of reviews/excerpts to return (if missing, all (up to 5) reviews/excerpts are returned)
		"""
		limit: Int
	): [TitleReviewExcerpt]!
	"""
	The sentiment classification for this theme e.g. POSITIVE
	"""
	sentiment: TitleReviewThemeSentiment!
	"""
	Summary text describing this theme's presence in reviews.
	"""
	summary: LocalizedMarkdown!
	"""
	Identifier for the theme, e.g. ('intense')
	"""
	themeId: ID!
}

"""
Sentiment classification for review themes
"""
enum TitleReviewThemeSentiment {
	"""
	Theme is discussed negatively in reviews
	"""
	NEGATIVE
	"""
	Theme is discussed neutrally in reviews
	"""
	NEUTRAL
	"""
	Theme is discussed positively in reviews
	"""
	POSITIVE
}

type TitleSearchConnection {
	edges: [TitleSearchEdge]!
	pageInfo: PageInfo!
}

type TitleSearchEdge {
	cursor: ID!
	node: Title!
}

input TitleSearchOptions {
	"""
	The earliest release date for a title must fall within the specified range
	By default, we filter titles by release year provided in the search term, if applicable.
	If a releaseDate is provided we do not apply default search term based release year filter.
	"""
	releaseDateRange: DateRange
	"""
	The types of titles to include in results.
	By default, all title types are included in the search.
	"""
	type: [MainSearchTitleType]
}

type TitleText {
	country: DisplayableCountry
	isOriginalTitle: Boolean!
	language: DisplayableLanguage
	text: String!
}

"""
Title name constraints are affected by user language preferences.
Language preference is determined by x-imdb-user-language header.
"""
input TitleTextSearchConstraint {
	"""
	Include title "AKAs" (also known as) in the search.
	Defaults to false when omitted - only primary titles are searched.
	"""
	includeAkas: Boolean
	"""
	Matches titles based on their original name as well as regional name corresponding with user
	language preferences. If no language preferences are provided, en-US is used by default.
	There is no fuzzy matching.
	For example: 'Toy Story' will be matched by 'Toy' and 'Story' but not 'Toy Stor'
	"""
	searchTerm: String
	"""
	Enable fuzzy search results to include approximate matches.
	Defaults to false when omitted - only exact matches are returned.
	"""
	useFuzzySearch: Boolean
}

"""
A single theatrical rental record — the distributor's share of box office
revenue for a title in a specific region.
"""
type TitleTheatricalRentalEntry {
	"""
	Geographic area for this rental earning.
	"""
	area: BoxOfficeAreaType!
	"""
	Source or context information for this rental entry
	(e.g. '1967 re-release', 'Variety, January 7, 1957').
	Empty list if none.
	"""
	attributes: [LocalizedString]!
	"""
	Rental earnings (distributor's gross share).
	"""
	earnings: BoxOfficeGross!
}

"""
Theatrical rental data for a title.
Entries are ordered by region code, then by earnings within each region,
following the order provided by the upstream source data (the source does not
guarantee an ascending or descending direction for earnings).
"""
type TitleTheatricalRentals {
	"""
	All theatrical rental entries for this title, returned unpaginated.
	This is a small bounded list backed by a frozen historical dataset
	(legacy theatrical rental figures, predominantly US). Typically 1-5
	entries; expected maximum well under ~50.
	"""
	entries(
		"""
		Optional maximum number of entries to return, in the order described above.
		If omitted, all entries are returned. Must be >= 0.

		Intentionally optional (not the standard's `limit: Int!`): the primary
		consumer renders the full table (1:1 parity with the legacy box office tab),
		and the list is small and bounded (see above), so "return all" is the natural
		default. `limit` is a convenience for callers that only need the top entries.
		"""
		limit: Int
	): [TitleTheatricalRentalEntry]!
}

type TitleTrackRecommendationEdge {
	"""
	Cursor ID of the title track recommendation
	"""
	cursor: ID!
	"""
	A title track recommendation node
	"""
	node: Title!
	"""
	Position of the node in this context, e.g. TitleTrackRecommendationEdge 2 out of 10
	"""
	position: Int!
}

type TitleTrackRecommendationsConnection {
	"""
	Page of title track recommendations
	"""
	edges: [TitleTrackRecommendationEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of title track recommendations
	"""
	total: Int!
}

type TitleTrivia {
	"""
	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	category: TriviaCategory!
	"""
	Returns the trivia correction Link.

	---------------------
	Graphlet Details:
	- Name: kahlo
	- CTI: IMDb/GraphQL/Kahlo
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-contributors-tech
	"""
	correctionLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
	): ContributionLink
	"""
	The trivia as a displayable article

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	displayableArticle: DisplayableArticle!
	id: ID!
	"""
	trivia Voting summary

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	interestScore: InterestScore!
	"""
	Is this trivia item a spoiler

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	isSpoiler: Boolean!
	"""
	Machine translated displayable article. Returns the displayable article translated based on the language header.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	machineTranslatedDisplayableArticle: MachineTranslatedDisplayableArticle
	"""
	Machine translated trivia text. Returns the text field translated based on the language header.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	machineTranslatedText: MachineTranslatedMarkdown!
	"""
	machine translated trademark, returns the trademark field translated based on the language header.

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	machineTranslatedTrademark: MachineTranslatedMarkdown
	"""
	trivia Related Name IDs

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	relatedNames: [Name]
	"""
	Returns the trivia reporting Link.

	---------------------
	Graphlet Details:
	- Name: kahlo
	- CTI: IMDb/GraphQL/Kahlo
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-contributors-tech
	"""
	reportingLink(
		"""
		The required metadata for returning from the contribution experience.
		"""
		contributionContext: ContributionContext!
	): ContributionLink
	"""
	The trivia text

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	text: Markdown!
	"""
	Title related to the trivia

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	title: Title
	"""
	trivia Trademark text

	---------------------
	Graphlet Details:
	- Name: janet
	- CTI: IMDb/GraphQL/Janet
	- Affected by headers: x-imdb-client-name, x-imdb-normalized-languages, x-imdb-user-country, x-imdb-user-language, x-imdb-customer-id
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-content-api
	"""
	trademark: Markdown
}

"""
Example: if you want to match titles of trivia 'twisted ankle' OR 'shooting scenes', PLUS
the title must contain 're-cast', specify anyTriviaTextTerms: ['twisted ankle', 'shooting scenes']
 plus allTriviaTextTerms: ['re-cast'].
"""
input TitleTriviaMatchingSearchConstraint {
	"""
	A title match must contain ALL of these search terms in at least one of its trivia.
	"""
	allTriviaTextTerms: [String]
	"""
	A title match must contain AT LEAST ONE of these search terms in at least one of its trivia.
	"""
	anyTriviaTextTerms: [String]
}

type TitleType {
	"""
	Whether this title type can have episodes.
	It does not guarantee that a title of this type actually has episodes, only that it can have episodes.
	"""
	canHaveEpisodes: Boolean!
	"""
	The categories the title type is in.
	"""
	categories: [TitleTypeCategory]!
	"""
	Title type as displayable property
	"""
	displayableProperty: DisplayableTitleTypeProperty!
	"""
	Title type ID
	"""
	id: ID!
	"""
	A boolean flag asserting whether this title type is an episode.
	"""
	isEpisode: Boolean!
	"""
	A boolean flag asserting whether this title type is a series.
	"""
	isSeries: Boolean!
	"""
	The language the text is written in
	"""
	language: DisplayableLanguage!
	"""
	The displayable title type, e.g. 'TV Series'
	"""
	text: String!
}

type TitleTypeCategory {
	"""
	A token representing the title type category.
	"""
	id: ID!
	"""
	The language of the display text.
	"""
	language: DisplayableLanguage!
	"""
	The display text for the title type category (e.g. 'TV').
	"""
	text: String!
	"""
	The enum value representing the title type category.
	"""
	value: TitleTypeCategoryValue!
}

type TitleTypeCategorySummary {
	"""
	The title type category
	"""
	titleTypeCategory: TitleTypeCategory!
	"""
	The total number of credits for the title type category
	"""
	total: Int!
}

enum TitleTypeCategoryValue {
	"""
	Category containing audio related title types (e.g. 'Podcast Series').
	"""
	audio
	"""
	Category containing gaming related title types (e.g. 'Video Game').
	"""
	gaming
	"""
	Category containing movie related title types (e.g. 'Movie').
	"""
	movie
	"""
	Category containing music related title types (e.g. 'Music Video').
	"""
	music
	"""
	Category containing anything else
	"""
	other
	"""
	Category containing TV related title types (e.g. 'TV Series').
	"""
	tv
	"""
	Category containing video related title types (e.g. 'Video').
	"""
	video
}

type TitleTypeCategoryWithTitleTypes {
	"""
	A category of title types.
	"""
	category: TitleTypeCategory!
	"""
	All title types in the category.
	"""
	titleTypes: [TitleType]!
}

input TitleTypeSearchConstraint {
	"""
	Match titles based on any of the title types specified.
	Title type ids can be obtained by querying `titleMetadata.titleType.id`
	For example: movie, tvSeries, videoGame, etc.
	"""
	anyTitleTypeIds: [String]
	"""
	Exclude titles based on any of the title types specified.
	Title type ids can be obtained by querying `titleMetadata.titleType.id`
	For example: movie, tvSeries, videoGame, etc.

	If the same input is specified in the any and exclude constraints,
	then the results should be empty.
	"""
	excludeTitleTypeIds: [String]
}

type TitleTypeSummary {
	"""
	The title type
	"""
	titleType: TitleType!
	"""
	The total number of credits for the title type
	"""
	total: Int!
}

enum TitleWatchedSourceType {
	"""
	The customer checked-in this title before.
	"""
	CHECKIN
	"""
	The customer explicitly mark this title as watched
	"""
	EXPLICIT
	"""
	The customer rated this title before.
	"""
	RATING
	"""
	The customer reviewed this title before.
	"""
	REVIEW
}

type TitleWatchedStatus {
	title: Title!
	"""
	Watched status of this title.
	"""
	watchedStatus: WatchedStatus!
}

type TopGrossingReleasesConnection {
	"""
	Displayable Box Office area
	"""
	displayableBoxOfficeArea: BoxOfficeAreaType
	"""
	Pages of top grossing releases
	"""
	edges: [TopGrossingReleasesEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	End date of the specified timeWindow
	"""
	timeWindowEndDate: Date!
	"""
	Start date of the specified timeWindow
	"""
	timeWindowStartDate: Date!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

type TopGrossingReleasesEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A top grossing release
	"""
	node: TopGrossingReleasesNode!
	"""
	Position of the release in this context, eg: top grossing release 2 out of 10
	"""
	position: Int!
}

"""
Filter to specify the constraints to use to calculate the grosses.
Requesting constraints for which no data is available will return null.
Requesting invalid constraints (i.e. a non-existing area) will return an error.
Currently only the following are available:
- XDOM + LATEST_DAY
- XDOM + LATEST_WEEKEND
- GB + LATEST_WEEKEND
"""
input TopGrossingReleasesFilter {
	"""
	Time window over which the grosses are calculated
	"""
	timeWindow: TopGrossingReleasesTimeWindow!
	"""
	Area for which the grosses are calculated
	"""
	topGrossingReleasesArea: BoxOfficeReleasesAreaFilter!
}

type TopGrossingReleasesNode {
	"""
	Gross of the release according to the specified filter
	"""
	gross: BoxOfficeGross!
	"""
	Top Grossing Release
	"""
	release: BoxOfficeRelease!
}

input TopGrossingReleasesTimeWindow {
	timeWindowPeriod: TopGrossingReleasesTimeWindowPeriod!
}

"""
Grosses will be calculated over the specified time window period.
LATEST_ means that the period used will be the latest corresponding period for which data is available.
"""
enum TopGrossingReleasesTimeWindowPeriod {
	LATEST_DAY
	LATEST_WEEKEND
}

enum TopListType {
	ALL
	EDITORIAL
}

input TopMeterTitlesFilter {
	"""
	Filter titles based on their genre
	"""
	genreId: String
	"""
	Movies, TV, or All
	"""
	topMeterTitlesType: TopMeterTitlesType
}

enum TopMeterTitlesType {
	"""
	Movies and TV Series
	"""
	ALL
	"""
	Only Movies
	"""
	MOVIE
	"""
	Only TV Series
	"""
	TV
}

type TopPicksConnection {
	edges: [TopPicksEdge]!
	pageInfo: PageInfo!
	refTag: RefTag
}

type TopPicksEdge {
	cursor: ID!
	node: Title!
	position: Int!
}

type TopRanking {
	"""
	A combination of all dimensions of ranking is used to generate an unique id value for each title.
	"""
	id: ID!
	rank: Int!
	"""
	TODO: There might be changes based on ongoing discussion pertaining to Naming and using DisplaybleConcept
	"""
	text: LocalizedString!
}

input TopTrendingInput {
	dataWindow: TrendingDataWindow!
	trafficSource: TrendingTrafficSource!
}

"""
Enum exposing all the supported Region Specific Title Trending Sets
"""
enum TopTrendingPredefinedEnum {
	"""
	Trending Set for released Indian Titles based on Global Traffic
	"""
	INDIA_TITLE_TRENDS_RELEASED
	"""
	Trending Set for released Indian Titles with primary language Tamil
	based on Global Traffic
	"""
	INDIA_TITLE_TRENDS_RELEASED_TAMIL
	"""
	Trending Set for released Indian Titles with primary language Telugu
	based on Global Traffic
	"""
	INDIA_TITLE_TRENDS_RELEASED_TELUGU
	"""
	Trending Set for upcoming Indian Titles based on Global Traffic
	"""
	INDIA_TITLE_TRENDS_UPCOMING
}

input TopTrendingSetsPredefinedInput {
	topTrendingSetPredefined: TopTrendingPredefinedEnum!
}

type TotalCredits {
	"""
	Restriction related information if exists for credits
	"""
	restriction: CreditRestriction
	"""
	The total number of credits after the restriction is applied
	"""
	total: Int!
}

type Track {
	"""
	Products of Amazon Music for this soundtrack
	"""
	amazonMusicProducts(limit: Int): [AmazonMusicProduct]
	"""
	Comments for this track
	"""
	comments(limit: Int): [Markdown]
	"""
	The track as a displayable article
	"""
	displayableArticle: DisplayableArticle!
	"""
	A token representing the track.
	"""
	id: ID!
	"""
	Machine translated displayable article. Returns the displayable article translated based on the language header.
	"""
	machineTranslatedDisplayableArticle: MachineTranslatedDisplayableArticle
	"""
	Optional display text for the track (e.g. 'Danger Zone').
	"""
	text: String
}

union TrackedEntity = Name | Title

type TrackedNameEdge {
	"""
	Cursor ID of the tracked name
	"""
	cursor: ID!
	"""
	A tracked name node
	"""
	node: Name!
	"""
	Position of the node in this context, e.g. TrackedNameEdge 2 out of 10
	"""
	position: Int!
}

type TrackedNamesConnection {
	"""
	Page of tracked names
	"""
	edges: [TrackedNameEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of tracked names
	"""
	total: Int!
}

type TrackedTitleEdge {
	"""
	Cursor ID of the tracked title
	"""
	cursor: ID!
	"""
	A tracked title node
	"""
	node: Title!
	"""
	Position of the node in this context, e.g. TrackedTitleEdge 2 out of 10
	"""
	position: Int!
}

type TrackedTitlesConnection {
	"""
	Page of tracked titles
	"""
	edges: [TrackedTitleEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of tracked titles
	"""
	total: Int!
}

type TrackNotification {
	"""
	Headline to display
	"""
	content: Markdown!
	"""
	The URL the client should navigate to when this notification is tapped.
	Null when no URL is associated with this notification — no navigation should occur.
	"""
	destinationUrl: URL
	"""
	The type of event that triggered this notification.
	- UNKNOWN: the backend provided an event type not yet in this enum — treat as a generic notification.
	- null: the backend did not provide an event type — treat as a generic notification.
	"""
	eventType: TrackNotificationEventType
	"""
	Opaque ID for pagination purposes
	"""
	id: ID!
	"""
	Primary image of most relevant entity, null if no tracked entities have an image.
	"""
	image: Image
	"""
	Last time track notification was updated
	"""
	lastUpdated: DateTime!
	"""
	The origin service or source of this notification, such as Deadline or The Hollywood Reporter. Null if not specified.
	"""
	notificationSource: LocalizedString
	"""
	The subject line of this notification. Also used as fallback display text for
	content when body is not available. Null if not provided.
	"""
	notificationSubject: LocalizedString
	"""
	Whether this notification has been read by the customer or not yet
	"""
	read: Boolean!
	"""
	List of tracked/trackable entities for this notification
	"""
	relatedEntities: [TrackedEntity]!
	"""
	How destinationUrl should be opened by the client.
	- UNKNOWN: the backend provided a URL type not yet in this enum — fall back to opening as a standard web URL.
	- null: no URL is associated (destinationUrl is also null) — ignore this field.
	Invariant: null if and only if destinationUrl is null.
	"""
	urlType: TrackNotificationUrlType
}

type TrackNotificationConnection {
	edges: [TrackNotificationEdge]!
	pageInfo: PageInfo!
	total: Int!
}

type TrackNotificationEdge {
	cursor: ID!
	node: TrackNotification!
}

"""
The type of event that triggered a track notification.
Enum chosen over String for compile-time type safety on clients; the set is stable and additions
require coordinated backend + schema changes. The UNKNOWN sentinel handles forward compatibility
for the rare case of a new backend type before the schema is updated.
"""
enum TrackNotificationEventType {
	"""
	A credit change on a tracked entity
	"""
	CREDIT_CHANGE
	"""
	An employment change on a tracked entity
	"""
	EMPLOYMENT_CHANGE
	"""
	Notification should be ignored/hidden by the client
	"""
	IGNORE
	"""
	A job role submission notification
	"""
	JOBS_ROLE_SUBMISSION
	"""
	A nudge to update known-for credits
	"""
	KNOWN_FOR_NUDGE
	"""
	A meeting request from another user
	"""
	MEETING_REQUEST
	"""
	A news article related to a tracked entity
	"""
	NEWS_ARTICLE
	"""
	A production status change on a tracked entity
	"""
	PRODUCTION_STATUS_CHANGE
	"""
	A connection request was accepted
	"""
	PRO_CONNECTION_ACCEPTED
	"""
	A connection request from another user
	"""
	PRO_CONNECTION_REQUEST
	"""
	A representation change on a tracked entity
	"""
	REPRESENTATION_CHANGE
	"""
	A general change to a tracked entity
	"""
	TRACKED_CHANGE
	"""
	An event type not yet in this enum
	"""
	UNKNOWN
	"""
	A user rating notification
	"""
	USER_RATING
}

type TrackNotificationPreference {
	"""
	True if requesting customer receives notifications for this preference type
	"""
	interested: Boolean!
	"""
	Preference type
	"""
	type: TrackNotificationPreferenceType!
}

type TrackNotificationPreferences {
	"""
	Indicates if current user is tracking given const
	"""
	isTracking: Boolean!
	"""
	The requesting user's notification preferences for this const.
	"""
	notificationPreferences: [TrackNotificationPreference]
}

"""
Preference type for a tracked page
"""
type TrackNotificationPreferenceType {
	"""
	Unique identifier for the notification preference type
	"""
	id: ID!
	"""
	Language of the text
	"""
	language: DisplayableLanguage!
	"""
	Text describing the notification preference type
	"""
	text: String!
	"""
	The notification preference type identifier, this value should be used when submitting mutations to trackConst
	"""
	typeId: ID!
}

"""
How the notification URL should be opened by the client.
"""
enum TrackNotificationUrlType {
	"""
	A deep link into the app
	"""
	DEEP_LINK
	"""
	Open the app without a specific destination
	"""
	OPEN_APP
	"""
	A URL type not yet in this enum
	"""
	UNKNOWN
	"""
	A standard web URL
	"""
	URL
}

input TrackRecommendationsInput {
	"""
	Profession used in query for certain recommendation strategies
	"""
	profession: String
}

type Trademark {
	"""
	The trademark as a displayable article
	"""
	displayableArticle: DisplayableArticle
	"""
	Machine translated displayable article. Returns the displayable article translated based on the language header.
	"""
	machineTranslatedDisplayableArticle: MachineTranslatedDisplayableArticle
	"""
	Machine translated trademark text. Returns the text field translated based on the language header.
	"""
	machineTranslatedText: MachineTranslatedMarkdown!
	"""
	Details of the person's trademarks
	"""
	text: Markdown!
}

"""
Pagination for Trademarks which always has a known total number of edges.
"""
type TrademarkConnection {
	"""
	Pages of Trademarks
	"""
	edges: [TrademarkEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of Trademarks
"""
type TrademarkEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	Details of the person's trademark
	"""
	node: Trademark!
	"""
	Position of the node in this context, eg: Trademark 4 out of 31
	"""
	position: Int!
}

type TrendingCollectionOption {
	country: DisplayableCountry!
	dataWindow: TrendingDataWindow!
	trafficSource: TrendingTrafficSource!
}

"""
We will throw exception if trending set does not exist, which means that
the response will contain an error field with the exception code and the
data field set to null
https://w.amazon.com/bin/view/IMDb/Trending/#Ranking_Sets
"""
enum TrendingDataWindow {
	HOURS
	MINUTES
}

type TrendingNameCollection {
	items(first: Int!): TrendingNameConnection!
	option: TrendingCollectionOption!
}

type TrendingNameCollectionOptions {
	options: [TrendingCollectionOption]!
	selectedItem: TrendingNameCollection!
}

type TrendingNameConnection {
	edges: [TrendingNameEdge]!
	pageInfo: PageInfo!
}

type TrendingNameEdge {
	cursor: ID!
	node: TrendingNameNode!
}

type TrendingNameNode {
	item: Name!
	rank: Int!
	weightRank: Int!
}

type TrendingTitleCollection {
	items(first: Int!): TrendingTitleConnection!
	option: TrendingCollectionOption!
}

type TrendingTitleCollectionOptions {
	options: [TrendingCollectionOption]!
	selectedItem: TrendingTitleCollection!
}

"""
Trending schema
"""
type TrendingTitleConnection {
	edges: [TrendingTitleEdge]!
	pageInfo: PageInfo!
}

type TrendingTitleEdge {
	cursor: ID!
	node: TrendingTitleNode!
}

type TrendingTitleNode {
	item: Title!
	rank: Int!
	weightRank: Int!
}

"""
We will throw exception if trending set does not exist, which means that
the response will contain an error field with the exception code and the
data field set to null
https://w.amazon.com/bin/view/IMDb/Trending/#Ranking_Sets
All TrendingTrafficSource values except XWW are deprecated
"""
enum TrendingTrafficSource {
	XWW
}

type TrendingVideoConnection {
	edges: [TrendingVideoEdge]!
	pageInfo: PageInfo!
}

type TrendingVideoEdge {
	cursor: ID!
	node: TrendingVideoNode!
}

type TrendingVideoNode {
	item: Video!
	rank: Int!
	weightRank: Int!
}

"""
A category of trivia describing the nature of a subset of trivia, e.g. 'Director Trademark'. Not all trivia has a category.
"""
type TriviaCategory {
	id: ID!
	"""
	The language of the display text
	"""
	language: DisplayableLanguage!
	text: String!
}

type TriviaCategoryWithTrivia {
	category: TriviaCategory
	restriction: TriviaRestriction
	trivia(filter: TriviaCategoryWithTriviaFilter, first: Int): TriviaConnection
}

input TriviaCategoryWithTriviaFilter {
	spoilers: FilterSpoilers
}

"""
Pagination for Trivia types which always has a known total number of edges.
"""
type TriviaConnection {
	"""
	Pages of Trivia
	"""
	edges: [TriviaEdge]!
	"""
	Details of the page
	"""
	pageInfo: PageInfo!
	"""
	Details of any applied restriction
	"""
	restriction: TriviaRestriction
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

"""
Page of Trivia
"""
type TriviaEdge {
	"""
	Cursor ID
	"""
	cursor: ID!
	"""
	A Trivia node
	"""
	node: TitleTrivia!
	"""
	Position of the node in this context, eg: Trivia 4 out of 31
	"""
	position: Int!
}

input TriviaFilter {
	categories: [ID]
	spoilers: FilterSpoilers
}

"""
Information about restrictions applied to trivia
"""
type TriviaRestriction {
	"""
	Explanation(s) for the restriction
	"""
	explanations: [RestrictionExplanation]!
	reasons: [ContentRestrictionReason]!
	"""
	DEPRECATED: Renamed, use `reasons` instead.
	"""
	restrictionReason: [ContentRestrictionReason]!
	"""
	The total number of items if there was no restriction
	"""
	unrestrictedTotal: Int
}

type TwitterLink {
	label: String
	url: String!
	"""
	Twitter username
	"""
	username: String!
}

"""
The top-level model for a server-driven UI-based workflow editing experience.
"""
type UIWorkflow {
	"""
	Contains the actions available to display in the action tray
	"""
	actionTray: UIWorkflowActionTray!
	"""
	Contains the main form elements or menu of options available within this workflow
	"""
	body: UIWorkflowBody!
	"""
	Contains information about the specific content the customer is affecting in this specific workflow, e.g. 'Add an FAQ'
	"""
	contentHeader: UIWorkflowContentHeader!
	"""
	Contains information about the subject of the workflow, the context within
	which the user is taking some action, e.g. a title being edited
	"""
	contextHeader: UIWorkflowContextHeader!
	"""
	Contains any Global Menu controls, if required, such as links to alternative experiences
	"""
	globalMenu: UIWorkflowGlobalMenu
	"""
	Contains information about the current workflow execution state
	"""
	workflowState: UIWorkflowExecutionState!
	"""
	Indicates the type of operation this workflow is intended for, so that clients can choose appropriate icons etc
	"""
	workflowType: UIWorkflowType!
}

"""
Represents an actionable button within a UI workflow.
Contains all the necessary information to render and handle a workflow action.
"""
type UIWorkflowAction {
	"""
	Unique identifier for the action
	"""
	id: ID!
	"""
	Display text for the action
	"""
	label: LocalizedString!
	"""
	Specifies the direction of navigation through a workflow when a customer triggers this action. A client
	can use this field to animate the transition with a horizontal/vertical slide in the direction suggestion,
	if they wish.
	If not provided, the action is assumed to have no inherent direction, ie the action leaves the workflow
	in the same overall state, so clients should not animate the transition beyond showing a loading indicator.
	"""
	navigationDirection: UIWorkflowActionNavigationDirection
	"""
	Indicates whether the action needs form data to be submitted in calls to updateUIWorkflow
	"""
	requiresFormData: Boolean!
	"""
	Visual style and importance level of the action
	"""
	type: UIWorkflowActionType!
}

"""
Defines conceptual directions forward or backward through a workflow, to enable clients to
animate or provide customers navigational hints as they flow through a workflow by triggering actions.
"""
enum UIWorkflowActionNavigationDirection {
	"""
	Moves the workflow backwards towards an earlier state
	"""
	Backward
	"""
	Moves the workflow forwards towards a completion state
	"""
	Forward
}

"""
Represents the action tray component of a UI workflow.
Contains the available actions that can be performed in the current workflow state.
"""
type UIWorkflowActionTray {
	"""
	The bounded and ordered list of available actions that can be performed in the current workflow state.
	The number of actions available is not expected to be more than three.
	"""
	actions: [UIWorkflowAction]!
}

"""
Defines the visual hierarchy and importance of workflow actions.
Used to determine how actions should be displayed in the UI.
"""
enum UIWorkflowActionType {
	"""
	Action to exit or abandon the current workflow
	"""
	Cancel
	"""
	Main action that represents the primary operation
	"""
	Primary
	"""
	Supporting actions that represent alternative operations
	"""
	Secondary
}

"""
Defines configuration for an individual attribute field within the attributes selector.
Specifies how each attribute should be displayed and validated.

Input type guidance by fieldName:
- detailTags: Boolean selection (typically rendered as checkboxes)
- creditedAs, segments, detail: Free-text inputs supporting multiple values
"""
type UIWorkflowAttributeFieldDefinition {
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide this attribute if element A has a value X or if element B has a
	value of Y, and disable if attribute C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	The category/field this attribute belongs to (detailTags, creditedAs, segments, or detail)
	"""
	fieldName: String!
	"""
	Explanatory text describing the purpose and usage of this attribute field
	"""
	infoText: String!
	"""
	Display label for the attribute field (e.g., uncredited, as...)
	"""
	label: String!
	"""
	Name of the attribute field (e.g., UNCREDITED, credited_as)
	"""
	name: String!
	"""
	Format string for displaying the attribute value (e.g., '(as {{value}})' for credited_as).
	{{value}} can be used as a token that is replaced by the attribute value for display.
	"""
	serialisedFormat: String!
}

"""
Represents an attributes selector form element in a UI workflow.
Allows users to select multiple attributes across different categories with mutual exclusivity rules.
"""
type UIWorkflowAttributesSelectorFormElement {
	"""
	Configuration for all available attribute fields
	"""
	attributesConfig: [UIWorkflowAttributeFieldDefinition]!
	"""
	Optional validation constraints for this attributes selector
	"""
	attributesSelectorConstraints: SelectFieldConstraints
	"""
	The currently selected attribute values organized by category. Null if no attributes have been selected.
	"""
	attributesSelectorValue: UIWorkflowAttributesSelectorValue
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Optional ordered and bound list of feedback messages associated with this form element (typically 0 or 1 entries).
	Null or 0 entries means there is no feedback to display.
	Clients must respect the order the feedback is returned in.
	"""
	feedback: [UIWorkflowFeedbackElement]
	"""
	Optional help link providing additional guidance
	"""
	helpLink: HelpLink
	"""
	Unique identifier for the form element
	"""
	id: ID!
	"""
	The label text to display for this attributes selector element
	"""
	label: LocalizedMarkdown!
}

"""
Represents the value structure for an attributes selector form element.
Contains categorized attribute selections organized by field type.
"""
type UIWorkflowAttributesSelectorValue {
	"""
	Used to capture the name in a credit, when credited as a different name to the IMDb name
	"""
	creditedAs: [String]!
	"""
	Additional unstructured free-text information to accompany the credit
	"""
	detail: [String]!
	"""
	Structured tags to provide additional information (e.g., UNCREDITED, RUMORED, HEAD_WRITER)
	"""
	detailTags: [String]!
	"""
	Captures the specific segments of a title the name in a credit performed
	"""
	segments: [String]!
}

"""
Represents the main body content of a UI workflow.
Contains an ordered collection of UI elements that form the workflow's content.
"""
type UIWorkflowBody {
	"""
	The bounded and ordered list of UI elements that make up the workflow's body content.
	Clients should always show all workflow body elements, and in the order determined by the server, to ensure the
	workflow is able to be completed correctly. The number of elements is usually less than 5, but a very complex
	workflow may have more.
	"""
	elements: [UIWorkflowElementOrGroup]!
}

"""
Represents a checkbox form element in the UI workflow.
"""
type UIWorkflowCheckboxFormElement {
	"""
	Optional checkbox constraints
	"""
	checkboxConstraints: CheckboxFieldConstraints
	"""
	The current checkbox 'checked' boolean value
	"""
	checkboxValue: Boolean!
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Optional ordered and bound list of feedback messages associated with this form element (typically 0 or 1 entries).
	Null or 0 entries means there is no feedback to display.
	Clients must respect the order the feedback is returned in.
	"""
	feedback: [UIWorkflowFeedbackElement]
	"""
	Optional help link providing additional guidance
	"""
	helpLink: HelpLink
	"""
	Unique identifier for the checkbox
	"""
	id: ID!
	"""
	The label text to display next to the checkbox
	"""
	label: LocalizedMarkdown!
}

"""
Search configuration for searching company entities.
"""
type UIWorkflowCompanySearchConfiguration {
	"""
	Optional list of company types to filter results.
	If omitted, all company types are included in search results.
	"""
	types: [String]
	"""
	Whether to use fuzzy search.
	- true: Returns companies that include approximate matches
	- false (default): Only exact matches are returned
	If omitted, defaults to false
	"""
	useFuzzySearch: Boolean
}

"""
Represents the content header section of a UI workflow.
Contains the main heading and optional help resources for the workflow.
"""
type UIWorkflowContentHeader {
	"""
	The heading to display as part of the main content panel and its controls
	"""
	heading: LocalizedMarkdown!
	"""
	An optional help link to display in the Content Header for the customer to get help
	"""
	helpLink: HelpLink
}

"""
Represents the context header section of a UI workflow.
Provides contextual information about the current workflow being performed.
"""
type UIWorkflowContextHeader {
	"""
	The heading to display to the customer to contextualize the workflow, such as 'FAQs' or 'New Title'.
	"""
	heading: LocalizedMarkdown!
	"""
	The subject const of the workflow (optional).
	In the future we will need to support workflows that create new primaries, so a subject entity will not yet exist.
	"""
	subject: UIWorkflowSubject
}

"""
Constraint scoping a title search to titles credited to specific name(s). Each mode is optional.
"""
type UIWorkflowCreditedNameConstraint {
	"""
	A title match must contain ALL of these name IDs (AND). If omitted, no all-of constraint is applied.
	"""
	allNameIds: [ID]
	"""
	A title match must contain AT LEAST ONE of these name IDs (OR). If omitted, no any-of constraint is applied.
	"""
	anyNameIds: [ID]
	"""
	A title match must contain NONE of these name IDs (exclude). If omitted, no names are excluded.
	"""
	excludeNameIds: [ID]
}

"""
Search configuration for custom data sources.
Supports both SDUI-managed fixed option lists and custom backend services.
Provides flexibility for any non-catalog data source.
"""
type UIWorkflowCustomDataSourceSearchConfiguration {
	"""
	Data source identifier specifying which custom data source to query.
	Clients will use that data source id in the SDUI search query.
	"""
	dataSourceId: ID!
	"""
	Whether to use fuzzy search.
	- true: Returns results that include approximate matches
	- false (default): Only exact matches are returned
	If omitted, defaults to false
	"""
	useFuzzySearch: Boolean
}

"""
Multiple display controls with logical aggregation.
Supports nested controls for complex logic like: x OR (y AND z)
"""
type UIWorkflowDisplayAggregateControl {
	"""
	Logical operator to combine conditions
	"""
	aggregateOperation: UIWorkflowDisplayControlAggregateOperation!
	"""
	Array of display controls to evaluate (supports nesting)
	"""
	controls: [UIWorkflowDisplayControl]!
}

"""
Union type for all display condition types.
"""
union UIWorkflowDisplayCondition =
	| UIWorkflowDisplayConditionStringArrayToStringSet
	| UIWorkflowDisplayConditionStringToString
	| UIWorkflowDisplayConditionStringToStringSet

"""
String-array-to-string-set display condition: Check if string[] element values overlap with a set of strings.
"""
type UIWorkflowDisplayConditionStringArrayToStringSet {
	"""
	Set comparison operator
	"""
	operator: UIWorkflowDisplayConditionStringArrayToStringSetOperator!
	"""
	Set of string values to compare against
	"""
	value: [String]!
}

"""
String-array-to-string-set display condition operators for comparing string[] element values against a set of strings.
"""
enum UIWorkflowDisplayConditionStringArrayToStringSetOperator {
	"""
	At least one array element is in the set: array ∩ set ≠ ∅
	"""
	INTERSECTS
	"""
	All array elements are in the set: array ⊆ set
	"""
	SUBSET
}

"""
String-to-string display condition: Compare string element value against a single string.
"""
type UIWorkflowDisplayConditionStringToString {
	"""
	String comparison operator
	"""
	operator: UIWorkflowDisplayConditionStringToStringOperator!
	"""
	Single string value to compare against
	"""
	value: String!
}

"""
String-to-string display condition operators for comparing string element values against a single string.
"""
enum UIWorkflowDisplayConditionStringToStringOperator {
	"""
	Substring match
	"""
	CONTAINS
	"""
	Suffix match
	"""
	ENDS_WITH
	"""
	Exact match
	"""
	EQUALS
	"""
	Regex match
	"""
	MATCHES
	"""
	Prefix match
	"""
	STARTS_WITH
}

"""
String-to-string-set display condition: Check if string element value is in a set of strings.
"""
type UIWorkflowDisplayConditionStringToStringSet {
	"""
	Set comparison operator
	"""
	operator: UIWorkflowDisplayConditionStringToStringSetOperator!
	"""
	Set of string values to compare against
	"""
	value: [String]!
}

"""
String-to-string-set display condition operators for comparing string element values against a set of strings.
"""
enum UIWorkflowDisplayConditionStringToStringSetOperator {
	"""
	Value is in the set: value ∈ set
	"""
	IN
}

"""
Union type for display control logic.
Can be either a single control or multiple controls with aggregation.
"""
union UIWorkflowDisplayControl =
	| UIWorkflowDisplayAggregateControl
	| UIWorkflowDisplaySingleControl

"""
Aggregate operation for combining multiple display controls.
"""
enum UIWorkflowDisplayControlAggregateOperation {
	"""
	All conditions must be true
	"""
	AND
	"""
	At least one condition must be true
	"""
	OR
}

"""
Display control configuration.
Assumes Elements are shown and enabled by default.
"""
type UIWorkflowDisplayControlConfig {
	"""
	The control logic
	"""
	controlConfig: UIWorkflowDisplayControl!
	"""
	The display state to control
	"""
	state: UIWorkflowDisplayControlState!
}

"""
Display control state types for conditional element visibility and interaction.
Elements are shown and enabled by default.
"""
enum UIWorkflowDisplayControlState {
	"""
	Element is disabled when condition is true
	"""
	DISABLE
	"""
	Element is hidden when condition is true
	"""
	HIDE
}

"""
Single display control with one condition.
"""
type UIWorkflowDisplaySingleControl {
	"""
	Condition that evaluates the target element's value
	"""
	condition: UIWorkflowDisplayCondition!
	"""
	Whether to negate the condition result.
	When true, applies state when condition is false.
	When null or false, applies state when condition is true.
	"""
	negate: Boolean
	"""
	ID of the element whose value determines this element's display state
	"""
	targetElementId: ID!
}

"""
Union type for all possible UI workflow body elements excluding nested groups.
Combines all interactive and display elements that can appear in a workflow body or within a nested group.
"""
union UIWorkflowElement =
	| Experimental_UIWorkflowHappyOrNotElement
	| UIWorkflowAttributesSelectorFormElement
	| UIWorkflowCheckboxFormElement
	| UIWorkflowFeedbackCheckboxFormElement
	| UIWorkflowFeedbackDisplayElement
	| UIWorkflowFileUploadFormElement
	| UIWorkflowMarkdownDisplayElement
	| UIWorkflowMarkdownTextAreaFormElement
	| UIWorkflowMultiSelectCheckboxGroupFormElement
	| UIWorkflowMultiSelectFormElement
	| UIWorkflowNumberInputFormElement
	| UIWorkflowRadioGroupFormElement
	| UIWorkflowSearchFormElement
	| UIWorkflowSearchMultiSelectFormElement
	| UIWorkflowSelectFormElement
	| UIWorkflowTextAreaFormElement
	| UIWorkflowTextFormElement
	| UIWorkflowTitleRatingElement

"""
Represents a group of related UI workflow elements.
Allows for logical grouping and organization of form elements.
"""
type UIWorkflowElementGroup {
	"""
	Optional display controls to control group's states based on other elements values.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	The ordered and bound list of UI elements contained in this group
	"""
	elements: [UIWorkflowElement]!
	"""
	Optional heading for the group
	"""
	heading: LocalizedMarkdown
	"""
	Optional help link providing additional guidance for this group
	"""
	helpLink: HelpLink
	"""
	Unique identifier for the element group
	"""
	id: ID!
}

"""
Union type for all possible UI workflow body elements including nested groups.
"""
union UIWorkflowElementOrGroup =
	| Experimental_UIWorkflowHappyOrNotElement
	| UIWorkflowAttributesSelectorFormElement
	| UIWorkflowCheckboxFormElement
	| UIWorkflowElementGroup
	| UIWorkflowFeedbackCheckboxFormElement
	| UIWorkflowFeedbackDisplayElement
	| UIWorkflowFileUploadFormElement
	| UIWorkflowMarkdownDisplayElement
	| UIWorkflowMarkdownTextAreaFormElement
	| UIWorkflowMultiSelectCheckboxGroupFormElement
	| UIWorkflowMultiSelectFormElement
	| UIWorkflowNumberInputFormElement
	| UIWorkflowRadioGroupFormElement
	| UIWorkflowSearchFormElement
	| UIWorkflowSearchMultiSelectFormElement
	| UIWorkflowSelectFormElement
	| UIWorkflowTextAreaFormElement
	| UIWorkflowTextFormElement
	| UIWorkflowTitleRatingElement

type UIWorkflowExecutionState {
	"""
	A globally unique ID for a customer's specific run through the chosen server-driven workflow
	"""
	executionId: ID!
	"""
	A globally unique ID for the most recent interaction/request regarding this workflow execution
	"""
	interactionId: ID!
	"""
	A debug-friendly string describing the current status of the server-driven workflow being run
	"""
	status: String!
	"""
	A canonical ID assigned to the server-driven UI workflow the customer is interacting with
	"""
	workflowId: ID!
}

"""
Represents an element to display in the UI workflow body containing feedback that requires checking of a box by the
customer to acknowledge the feedback. The current checked or unchecked state is provided within the booleanValue field.
"""
type UIWorkflowFeedbackCheckboxFormElement {
	"""
	Current checked or unchecked state of the acknowledgement checkbox
	"""
	checkboxValue: Boolean!
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Feedback to show the customer
	"""
	feedback: ValidationFeedbackGroup!
	"""
	Optional help link providing additional guidance
	"""
	helpLink: HelpLink
	"""
	Unique identifier for the feedback element
	"""
	id: ID!
	"""
	Label to show the customer alongside the acknowledgement checkbox
	"""
	label: LocalizedMarkdown!
}

"""
Represents an element to display in the UI workflow body containing feedback about the customer's workflow.
"""
type UIWorkflowFeedbackDisplayElement {
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Feedback about the customer's content
	"""
	feedback: ValidationFeedbackGroup!
	"""
	Unique identifier for the feedback element
	"""
	id: ID!
}

"""
Union type for all possible feedback elements in the UI workflow.
Combines both interactive and display-only feedback elements.
"""
union UIWorkflowFeedbackElement =
	| UIWorkflowFeedbackCheckboxFormElement
	| UIWorkflowFeedbackDisplayElement

"""
Configuration for uploading a file. The client builds a multipart form request
using the provided fields and file field name, then POSTs to the given URL.
Backend-agnostic — the same upload logic works regardless of storage provider.
"""
type UIWorkflowFileUploadConfig {
	"""
	Expiration time of the upload authorization. Client should refresh config if expired.
	"""
	expiresAt: DateTime!
	"""
	Ordered list of form fields to include before the file. Client appends each to FormData in order.
	"""
	fields: [UIWorkflowFileUploadFormField]!
	"""
	The form field name to use when appending the file to the multipart request.
	Required because storage backends expect the file under different field names.
	"""
	fileFieldName: String!
	"""
	The endpoint URL to POST the multipart FormData to
	"""
	url: URL!
}

"""
Defines form constraints for a file upload input.
These are for client-side validation UI (e.g., showing "Max 10MB" hints before upload).
The actual enforcement is in the upload policy — the backend rejects non-conforming uploads.
"""
type UIWorkflowFileUploadFieldConstraints {
	"""
	Allowed MIME types constraint. Null means all types accepted.
	"""
	allowedMimeTypes: MimeTypeValidationConstraint
	"""
	Specifies if the customer must upload at least one file
	"""
	isRequired: BooleanValidationConstraint
	"""
	Maximum file size in bytes per file
	"""
	maxFileSizeInBytes: IntThresholdValidationConstraint
	"""
	Maximum number of files allowed
	"""
	maxFiles: IntThresholdValidationConstraint
}

"""
File upload form element. Supports both S3 and Alexandria upload modes
through a unified interface — the client upload logic is identical for both.

Client upload flow:
1. User selects file(s)
2. Client validates against fileUploadConstraints (UX hints)
3. For each file, build FormData from UIWorkflowFileUploadConfig.fields, append
file using UIWorkflowFileUploadConfig.fileFieldName, POST to
UIWorkflowFileUploadConfig.url
4. On success, client stores the UIWorkflowFileUploadResult (fileReference, fileName) as the form value
5. Client submits the workflow form via updateUIWorkflow
"""
type UIWorkflowFileUploadFormElement {
	"""
	Optional display control configuration that controls element visibility and interactivity
	based on other element values.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Optional ordered and bound list of feedback messages associated with this form element.
	Null or 0 entries means there is no feedback to display.
	Clients must respect the order the feedback is returned in.
	"""
	feedback: [UIWorkflowFeedbackElement]
	"""
	Upload configurations — one per file the user is allowed to upload.
	The list length equals the maximum number of files permitted (matching maxFiles in constraints).
	Each config is single-use: the client consumes one config per uploaded file, in order.
	"""
	fileUploadConfigs: [UIWorkflowFileUploadConfig]!
	"""
	Constraints for client-side validation hints
	"""
	fileUploadConstraints: UIWorkflowFileUploadFieldConstraints
	"""
	Previously uploaded file references when returning to this step. Null if no files uploaded yet.
	"""
	fileUploadValue: [UIWorkflowFileUploadResult]
	"""
	Optional help link providing additional guidance
	"""
	helpLink: HelpLink
	"""
	Unique identifier for the file upload element
	"""
	id: ID!
	"""
	The label text to display for the file upload element
	"""
	label: LocalizedMarkdown!
}

"""
A single form field to include in the upload request.
The client appends each field as a key-value pair to the FormData before appending the file.
"""
type UIWorkflowFileUploadFormField {
	"""
	The form field name (e.g., 'AX-Signature', 'Content-Type', 'key')
	"""
	name: String!
	"""
	The form field value
	"""
	value: String!
}

"""
Result of a successful file upload. Stored as the form value.
"""
type UIWorkflowFileUploadResult {
	"""
	The original filename from the client
	"""
	fileName: String!
	"""
	The reference identifier for the uploaded file:
	- For S3: the S3 object key
	- For Alexandria: the document URN (e.g., urn:alx:doc:...)
	"""
	fileReference: String!
}

input UIWorkflowFormElementValues {
	"""
	The form element ID the associated data values relate to
	"""
	id: ID!
	"""
	The 0 or more values associated with the form element.
	While most form elements have a single value, some such as multi-select or attribute-pickers will support
	multiple options. For extensibility we are making this an array type.
	"""
	values: [String]!
}

"""
Represents the global menu structure for the UI workflow system.
Contains navigation items that appear in the global header.
"""
type UIWorkflowGlobalMenu {
	"""
	Bounded list of menu items to show in the global header.
	Number of items is not expected to grow beyond just a few entries.
	"""
	menuItems: [UIWorkflowGlobalMenuItem]!
}

"""
The set of possible menu item entry types
"""
union UIWorkflowGlobalMenuItem = UIWorkflowGlobalMenuItemLink

type UIWorkflowGlobalMenuItemLink implements LocalizedLink {
	"""
	The localized label for the link
	"""
	label: LocalizedString!
	"""
	The type of link, such as navigation
	"""
	linkType: UIWorkflowGlobalMenuItemLinkType!
	"""
	The target URL for the link
	"""
	url: URL!
}

"""
The set of possible menu item link types, used to enable clients to show different icons or choose different targets.
We only need Navigation right now, but in the future we anticipate both external links and download link types.
"""
enum UIWorkflowGlobalMenuItemLinkType {
	Navigation
}

"""
Represents a localized string for an input option in a multi-option form component such as a dropdown menu
or a radio button group.
"""
type UIWorkflowLocalizedStringInputOption {
	"""
	Optional help text displayed below the option label
	"""
	helpText: LocalizedString
	"""
	The text to display for this option in the form element
	"""
	text: LocalizedString!
	"""
	The value to set for the form element if this option is chosen by the customer
	"""
	value: String!
}

"""
Represents a block of markdown text to display within the body of UI workflow form.
"""
type UIWorkflowMarkdownDisplayElement {
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Unique identifier for the element
	"""
	id: ID!
	"""
	The markdown content to display
	"""
	markdown: LocalizedMarkdown!
}

"""
Represents a markdown-enabled text area input form element in the UI workflow.
"""
type UIWorkflowMarkdownTextAreaFormElement {
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Optional ordered and bound list of feedback messages associated with this form element (typically 0 or 1 entries).
	Null or 0 entries means there is no feedback to display.
	Clients must respect the order the feedback is returned in.
	"""
	feedback: [UIWorkflowFeedbackElement]
	"""
	Optional help link providing additional guidance
	"""
	helpLink: HelpLink
	"""
	Unique identifier for the text input
	"""
	id: ID!
	"""
	The label text to display above the text input
	"""
	label: LocalizedMarkdown!
	"""
	Optional validation constraints for the markdown text area input
	"""
	markdownTextAreaConstraints: MarkdownTextAreaFieldConstraints
	"""
	The current markdown text area value, if present
	"""
	markdownTextAreaValue: Markdown
}

"""
Represents a multi-select checkbox group form element in a UI workflow.
"""
type UIWorkflowMultiSelectCheckboxGroupFormElement {
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Optional ordered and bound list of feedback messages associated with this form element (typically 0 or 1 entries).
	Null or 0 entries means there is no feedback to display.
	Clients must respect the order the feedback is returned in.
	"""
	feedback: [UIWorkflowFeedbackElement]
	"""
	Optional help link providing additional guidance
	"""
	helpLink: HelpLink
	"""
	Unique identifier for the form element
	"""
	id: ID!
	"""
	The label text to display for this checkbox group element
	"""
	label: LocalizedMarkdown!
	"""
	Optional validation constraints for this checkbox group
	"""
	multiSelectCheckboxGroupConstraints: MultiSelectFieldConstraints
	"""
	The available checkbox options a customer can select from
	"""
	multiSelectCheckboxGroupOptions: [UIWorkflowMultiSelectCheckboxGroupOption]!
	"""
	The values of the currently selected checkboxes. Null if user has not selected anything previously.
	"""
	multiSelectCheckboxGroupValue: [String]
}

union UIWorkflowMultiSelectCheckboxGroupOption =
	| UIWorkflowLocalizedStringInputOption

"""
Represents a multi-select dropdown form element in a UI workflow.
"""
type UIWorkflowMultiSelectFormElement {
	"""
	Whether custom options can be added beyond the predefined options
	"""
	allowCustomOptions: Boolean
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Optional ordered and bound list of feedback messages associated with this form element (typically 0 or 1 entries).
	Null or 0 entries means there is no feedback to display.
	Clients must respect the order the feedback is returned in.
	"""
	feedback: [UIWorkflowFeedbackElement]
	"""
	Optional help link providing additional guidance
	"""
	helpLink: HelpLink
	"""
	Unique identifier for the form element
	"""
	id: ID!
	"""
	The label text to display for this multi-select dropdown element
	"""
	label: LocalizedMarkdown!
	"""
	Optional validation constraints for this multi-select dropdown
	"""
	multiSelectConstraints: MultiSelectFieldConstraints
	"""
	The available options a customer can select from
	"""
	multiSelectOptions: [UIWorkflowMultiSelectOption]!
	"""
	The values of the currently selected options, if present. Null if user has not selected anything previously.
	"""
	multiSelectValue: [String]
	"""
	Popular options that appear in the dropdown before typing
	"""
	popularOptions: [UIWorkflowMultiSelectOption]
}

union UIWorkflowMultiSelectOption = UIWorkflowLocalizedStringInputOption

"""
Search configuration for searching name entities.
"""
type UIWorkflowNameSearchConfiguration {
	"""
	Whether to use fuzzy search.
	- true: Returns names that include approximate matches
	- false (default): Only exact matches are returned
	If omitted, defaults to false
	"""
	useFuzzySearch: Boolean
}

"""
Represents a number input form element in the UI workflow.
"""
type UIWorkflowNumberInputFormElement {
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Optional ordered and bound list of feedback messages associated with this form element (typically 0 or 1 entries).
	Null or 0 entries means there is no feedback to display.
	Clients must respect the order the feedback is returned in.
	"""
	feedback: [UIWorkflowFeedbackElement]
	"""
	Optional help link providing additional guidance
	"""
	helpLink: HelpLink
	"""
	Unique identifier for the number input
	"""
	id: ID!
	"""
	The label text to display above the number input
	"""
	label: LocalizedMarkdown!
	"""
	Optional validation constraints for the number input
	"""
	numberConstraints: NumberFieldConstraints
	"""
	The current numeric value, if present. Null indicates no value has been
	entered. Supports both integers and floating-point numbers.
	"""
	numberValue: Float
}

"""
Controls the visual presentation of a radio group form element.
"""
enum UIWorkflowRadioGroupDisplayVariant {
	"""
	Options are visually contained in a bordered group
	"""
	CONTAINED
	"""
	Options are rendered without a visual container
	"""
	PLAIN
}

"""
Represents a radio button group form element in a UI workflow.
"""
type UIWorkflowRadioGroupFormElement {
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Visual presentation variant. CONTAINED wraps options in a bordered container. PLAIN renders options without a container.
	"""
	displayVariant: UIWorkflowRadioGroupDisplayVariant!
	"""
	Optional ordered and bound list of feedback messages associated with this form element (typically 0 or 1 entries).
	Null or 0 entries means there is no feedback to display.
	Clients must respect the order the feedback is returned in.
	"""
	feedback: [UIWorkflowFeedbackElement]
	"""
	Optional help link providing additional guidance
	"""
	helpLink: HelpLink
	"""
	Unique identifier for the form element
	"""
	id: ID!
	"""
	The label text to display for this radio group element
	"""
	label: LocalizedMarkdown!
	"""
	Optional validation constraints for this radio group
	"""
	radioGroupConstraints: RadioGroupFieldConstraints
	"""
	The available radio button options
	"""
	radioGroupOptions: [UIWorkflowRadioOption]!
	"""
	The value of the currently selected radio button option, if present
	"""
	radioGroupValue: String
}

"""
The set of possible radio group option types. Currently just one is available, but in the future we anticipate
Title, Name and Company-entity option types, so using a Union for extensibility.
"""
union UIWorkflowRadioOption = UIWorkflowLocalizedStringInputOption

"""
Union type for search configurations supporting different data source types.
Provides type-safe configuration with specific presets for catalog entities
while maintaining extensibility for custom data sources.
"""
union UIWorkflowSearchConfiguration =
	| UIWorkflowCompanySearchConfiguration
	| UIWorkflowCustomDataSourceSearchConfiguration
	| UIWorkflowNameSearchConfiguration
	| UIWorkflowTitleSearchConfiguration

"""
Represents a searchable input form element in a UI workflow.
Supports both catalog entity searches and fixed option list searches.
"""
type UIWorkflowSearchFormElement {
	"""
	Whether custom entry can be added beyond the search results. If omitted, defaults to false
	"""
	allowCustomEntry: Boolean
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Optional ordered and bound list of feedback messages associated with this form element.
	Null or 0 entries means there is no feedback to display.
	"""
	feedback: [UIWorkflowFeedbackElement]
	"""
	Help link providing additional guidance. If omitted, no help link is displayed
	"""
	helpLink: HelpLink
	"""
	Unique identifier for the form element
	"""
	id: ID!
	"""
	The label text to display for this search select element
	"""
	label: LocalizedMarkdown!
	"""
	Search configuration specifying data source and filters. Null if no constraints should be applied
	"""
	searchConfiguration: UIWorkflowSearchConfiguration
	"""
	Validation constraints for this search select. If omitted, no validation is applied
	"""
	searchConstraints: SelectFieldConstraints
	"""
	The currently selected option value. Null if no option has been selected
	"""
	searchValue: String
	"""
	Suggested options to display before user searches (e.g., recently selected, popular options) in the stored order.
	If omitted, no suggestions are shown.
	"""
	suggestedOptions: [UIWorkflowSearchOption]
}

"""
Represents a multi-selection searchable input form element in a UI workflow.
Supports both catalog entity searches and fixed option list searches.
"""
type UIWorkflowSearchMultiSelectFormElement {
	"""
	Whether custom entries can be added beyond the search results. If omitted, defaults to false
	"""
	allowCustomEntries: Boolean
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Optional ordered and bound list of feedback messages associated with this form element.
	Null or 0 entries means there is no feedback to display.
	"""
	feedback: [UIWorkflowFeedbackElement]
	"""
	Help link providing additional guidance. If omitted, no help link is displayed
	"""
	helpLink: HelpLink
	"""
	Unique identifier for the form element
	"""
	id: ID!
	"""
	The label text to display for this multi-search select element
	"""
	label: LocalizedMarkdown!
	"""
	Search configuration specifying data source and filters. Null if no constraints should be applied
	"""
	searchConfiguration: UIWorkflowSearchConfiguration
	"""
	Validation constraints for this search select. If omitted, no validation is applied
	"""
	searchMultiSelectConstraints: MultiSelectFieldConstraints
	"""
	The currently selected option values. Null if no options have been selected
	"""
	searchMultiSelectValue: [String]
	"""
	Suggested options to display before user searches (e.g., recently selected, popular options) in the stored order.
	If omitted, no suggestions are shown.
	"""
	suggestedOptions: [UIWorkflowSearchOption]
}

"""
Represents a search result option.
Returns different types based on the search configuration:
- UIWorkflowLocalizedStringInputOption: For non-entity options
- Name: For name entity searches (searchConfiguration type is UIWorkflowNameSearchConfiguration)
- Title: For title entity searches (searchConfiguration type is UIWorkflowTitleSearchConfiguration)
- Company: For company entity searches (searchConfiguration type is UIWorkflowCompanySearchConfiguration)
"""
union UIWorkflowSearchOption =
	| Company
	| Name
	| Title
	| UIWorkflowLocalizedStringInputOption

"""
Represents a select dropdown form element in a UI workflow.
"""
type UIWorkflowSelectFormElement {
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Optional ordered and bound list of feedback messages associated with this form element (typically 0 or 1 entries).
	Null or 0 entries means there is no feedback to display.
	Clients must respect the order the feedback is returned in.
	"""
	feedback: [UIWorkflowFeedbackElement]
	"""
	Optional help link providing additional guidance
	"""
	helpLink: HelpLink
	"""
	Unique identifier for the form element
	"""
	id: ID!
	"""
	The label text to display for this select dropdown element
	"""
	label: LocalizedMarkdown!
	"""
	Optional validation constraints for this select dropdown
	"""
	selectConstraints: SelectFieldConstraints
	"""
	The available options a customer can select from
	"""
	selectOptions: [UIWorkflowSelectOption]!
	"""
	The value of the currently selected option, if present
	"""
	selectValue: String
}

"""
The set of possible select option types. Currently just one is available, but in the future we anticipate
Title, Name and Company-entity option types, so using a Union for extensibility.
"""
union UIWorkflowSelectOption = UIWorkflowLocalizedStringInputOption

"""
The set of possible workflow subjects
"""
union UIWorkflowSubject = Company | LabsPrototype | Name | Title | UserProfile

"""
Represents a text area input form element in the UI workflow.
"""
type UIWorkflowTextAreaFormElement {
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Optional ordered and bound list of feedback messages associated with this form element (typically 0 or 1 entries).
	Null or 0 entries means there is no feedback to display.
	Clients must respect the order the feedback is returned in.
	"""
	feedback: [UIWorkflowFeedbackElement]
	"""
	Optional help link providing additional guidance
	"""
	helpLink: HelpLink
	"""
	Unique identifier for the text input
	"""
	id: ID!
	"""
	The label text to display above the text input
	"""
	label: LocalizedMarkdown!
	"""
	Optional validation constraints for the text area input
	"""
	textAreaConstraints: TextAreaFieldConstraints
	"""
	The current text area value, if present
	"""
	textAreaValue: String
}

"""
Represents a text input form element in the UI workflow.
"""
type UIWorkflowTextFormElement {
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Optional ordered and bound list of feedback messages associated with this form element (typically 0 or 1 entries).
	Null or 0 entries means there is no feedback to display.
	Clients must respect the order the feedback is returned in.
	"""
	feedback: [UIWorkflowFeedbackElement]
	"""
	Optional help link providing additional guidance
	"""
	helpLink: HelpLink
	"""
	Unique identifier for the text input
	"""
	id: ID!
	"""
	The label text to display above the text input
	"""
	label: LocalizedMarkdown!
	"""
	Optional validation constraints for the text input
	"""
	textConstraints: TextFieldConstraints
	"""
	The current text value, if present
	"""
	textValue: String
}

"""
Represents an element in the form to let a customer rate the title specified.
Clients are expected to use their idiomatic title rating control and to make calls to update the customer's
rating on-demand rather than treat this as part of a larger workflow form submission.
"""
type UIWorkflowTitleRatingElement {
	"""
	Optional display control configuration that controls element visibility and interactivity based on other element values.
	Evaluated client-side when referenced element values change. When a condition is met, the specified state is applied.
	e.g. hide if element A has a value X or if element B has a value of Y, and disable if element C has a value of Z.
	"""
	displayControls: [UIWorkflowDisplayControlConfig]
	"""
	Unique identifier for the rating element
	"""
	id: ID!
	"""
	The title being rated
	"""
	title: Title!
}

"""
Search configuration for searching title entities.
"""
type UIWorkflowTitleSearchConfiguration {
	"""
	Optional constraint scoping results to titles credited to specific name(s).
	If omitted, results are not scoped by credit.
	"""
	creditedNameConstraint: UIWorkflowCreditedNameConstraint
	"""
	Optional list of title types to filter results.
	If omitted, all title types are included in search results.
	"""
	types: [String]
	"""
	Whether to use fuzzy search.
	- true: Returns titles that include approximate matches
	- false (default): Only exact matches are returned
	If omitted, defaults to false
	"""
	useFuzzySearch: Boolean
}

"""
The set of available workflow types.
TODO: Move to common types package after review for re-use in other graphlets for CTA vending
"""
enum UIWorkflowType {
	Add
	Delete
	Edit
	Report
}

input UnionAffiliationsConstraint {
	"""
	A name match must have ALL of these union affiliations.
	Union affiliations should be provided as company IDs.
	Example: "co0015930" for SAG-AFTRA, "co0081427" for DGA
	No filtering is applied if omitted.
	"""
	allUnionAffiliations: [ID]
	"""
	A name match must have AT LEAST ONE of these union affiliations.
	Union affiliations should be provided as company IDs.
	Example: "co0015930" for SAG-AFTRA, "co0081427" for DGA
	No filtering is applied if omitted.
	"""
	anyUnionAffiliations: [ID]
	"""
	A name match must not have any of these union affiliations.
	Union affiliations should be provided as company IDs.
	Example: "co0015930" for SAG-AFTRA, "co0081427" for DGA
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeUnionAffiliations: [ID]
}

input UniqueTraitsConstraint {
	"""
	A name match must have ALL of these unique traits.
	No filtering is applied if omitted.
	"""
	allUniqueTraits: [UniqueTraitType]
	"""
	A name match must have AT LEAST ONE of these unique traits.
	No filtering is applied if omitted.
	"""
	anyUniqueTraits: [UniqueTraitType]
	"""
	A name match must not have any of these unique traits.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeUniqueTraits: [UniqueTraitType]
}

"""
Unique physical traits and characteristics for talent
"""
enum UniqueTraitType {
	AMPUTEE_ARM
	AMPUTEE_LEG
	CEREBRAL_PALSY
	DEAF
	HARD_OF_HEARING
	LITTLE_PERSON
	TRIPLET
	TWIN
	VISUALLY_IMPAIRED
	WALKING_IMPAIRMENT
	WHEELCHAIR
}

enum UnknownReleaseDateFilter {
	EXCLUDE_UNKNOWN
	"""
	If UNKNOWN_ONLY is used with releasedOnOrBefore or releasedOnOrAfter filters then an empty array will be returned.
	"""
	UNKNOWN_ONLY
}

type UpdateUserProfileFeedback {
	"""
	Validation feedback on a user's profile update.
	Null is returned when there is no validationFeedback.
	"""
	validationFeedback: ValidationFeedback
}

"""
The type to represent a URL for some resource. Format is as follows:

scheme:[userinfo@]host[:port]path[?query][#fragment]

https://en.wikipedia.org/wiki/URL
"""
scalar URL

"""
User Type available only for authenticated users
Includes private info.
Also includes public info in profile field.
Includes optional ProStatus and StaffStatus fields.
"""
type User {
	"""
	Returns the AAP conversion prompt status for the current user.
	Null for AAP users and unauthenticated callers (Query.user returns null when not authenticated).
	Use interstitial != null as the sole render signal — do not rely on shouldShow alone.
	"""
	amazonAccountPoolConversionPromptStatus: AmazonAccountPoolConversionPromptStatus
	"""
	Fetch a single attribute for the currently signed-in user.

	Throws GRAPHQL_VALIDATION_FAILED when:
	- Attribute name is unknown
	- Attribute is not accessible (no permission)

	Throws UNAUTHENTICATED if the user is not authenticated.

	When attribute exists and is accessible but has no value set, returns the attribute with isSet=false.

	System/downstream errors throw SERVICE_UNAVAILABLE_ERROR (client can retry).

	This query is NOT cacheable as it returns the authenticated user's own data.
	"""
	attribute(input: UserAttributeInput!): UserAttributeOutput!
	"""
	User's first name, or nickname if no first name was provided
	"""
	displayName: String!
	"""
	Returns whether the user has given feedback for that particular feature
	"""
	feedbackGiven(input: FeedbackGivenInput!): FeedbackGiven!
	"""
	User's full name, or empty string if no name provided
	"""
	fullName: String!
	"""
	Returns the current user's list of interests. Currently only 50 interests can be stored.
	"""
	interests(
		after: ID
		filter: UserInterestsFilter
		first: Int!
	): UserInterestsConnection!
	"""
	Returns if account is Amazon Account Pool (AAP) account or already converted to AAP account.
	"""
	isAmazonAccount: Boolean!
	"""
	Whether the customer is eligible for account conversion to AAP.
	Returns false when the customer is already AAP or holds a PAP-originated
	Pro subscription (is-lap-subscription = "1").
	Returns true for all other PAP customers.
	Throws UNAUTHENTICATED if the user is not authenticated.
	"""
	isEligibleForConversion: Boolean!
	"""
	Returns whether the account is mobile-only (has no email address).
	Mobile-only accounts are created without an email and can only be accessed via mobile authentication.
	"""
	isMobileOnlyAccount: Boolean!
	"""
	Returns the linked providers available for the authenticated customer.
	"""
	linkedAuthProviders: [LinkedAuthProvider]!
	"""
	Returns the current user's over-18 self-attestation status.
	Owner-scoped: only available for the authenticated user (Query.user is null when not authenticated).
	Nullable by design: a downstream (UDS) failure degrades only this field with a field error,
	rather than null-propagating and nulling out the whole User object.
	"""
	over18Status: Over18Status
	"""
	A setting of the users preferred language, composed of ISO 369-1 code dash ISO 3166-1/UN M.49 code
	or null if no such preferred language is defined.
	"""
	preferredLanguage: String
	"""
	Returns the list of watch providers of user
	"""
	preferredStreamingProviders(
		after: ID
		filter: UserPreferredStreamingProvidersInput
		first: Int!
	): UserPreferredStreamingProvidersOutput!
	proStatus: ProStatus
	profile: UserProfile!
	"""
	Returns the current user's ratings privacy setting.
	"""
	ratingsPrivacy: RatingsPrivacy
	"""
	If staffStatus is null, the user account is not marked as staff
	"""
	staffStatus: StaffStatus
	"""
	Returns the current user's title display settings.
	"""
	titleDisplay: TitleDisplayOutput!
}

"""
Input for User.attribute query.
Wraps the attribute name to allow future extensibility.
"""
input UserAttributeInput {
	"""
	The name/key of the attribute to fetch.
	Must be a valid attribute name in AttributesConfig with graphReadable permission.
	"""
	name: String!
}

"""
Output wrapper for User.attribute query.
Wraps the AttributeValue union to allow future extensibility.
"""
type UserAttributeOutput {
	"""
	The attribute value. Use inline fragments to access type-specific fields.
	"""
	attribute: AttributeValue!
}

input UserBadgesInput {
	"""
	Returns badges for a specific user if included.
	Defaults to the current authenticated user based on the x-imdb-customer-id header if omitted.
	When userId is provided the data can be cached.
	Throws UNAUTHENTICATED if neither is provided.
	"""
	userId: ID
}

enum UserCategory {
	IMDB_USERS
	TOP_1000_VOTERS
}

input UserConsentInput {
	"""
	The type of consent a user has opted in or out of.
	"""
	consentType: ConsentType!
	"""
	Mobile ID For Advertising, for requests coming from apps.
	Used for the THIRD_PARTY_DATA_SHARING consent.
	"""
	idfa: String
}

type UserConsentOutput {
	"""
	Detailed consent information.
	"""
	consent: Consent
	"""
	Whether the user has opted in or out of the consent.
	Is a required return field for all but TRACKING_COOKIE consent operations
	"""
	consentOperation: ConsentOperation
	"""
	The type of consent a user has opted in or out of.
	"""
	consentType: ConsentType!
}

type UserInterestsConnection {
	edges: [InterestEdge]!
	pageInfo: PageInfo!
	"""
	The total number of objects in this set
	"""
	total: Int!
}

input UserInterestsFilter {
	includeInterestIds: [ID]
}

input UserLinkedAuthProviderStatusesInput {
	"""
	URL customer will return to after linking/unlinking with a provider. Defaults to third party links page if omitted
	"""
	returnURL: URL
}

type UserLinkedAuthProviderStatusesOutput {
	providers: [AuthProviderStatus]!
}

"""
Entity type of a single notification in the Notification Center.
"""
type UserNotification {
	"""
	Header text of the notification. Could be the notification type like Now Streaming, New Trailer.
	"""
	header: LocalizedString!
	"""
	Unique id of the notification returned from Notification Service.
	"""
	id: ID!
	"""
	Attached image of the notification. Returns null if it does not have an image (e.g., system notification).
	"""
	image: MediaServiceImage
	"""
	Last updated time of the notification.
	"""
	lastUpdated: DateTime!
	"""
	Main content of the notification. e.g., XXX from your watchlist is streaming on YYY.
	"""
	primaryContent: LocalizedMarkdown!
	"""
	Secondary text of the notification, e.g., Because you followed XXX. Returns null if it has no value.
	"""
	secondaryContent: LocalizedString
	"""
	URL deep link/navigate to when the notification is clicked. Returns null if it has no link.
	"""
	url: URL
}

"""
Pagination for notifications, which always has a known total number of edges.
"""
type UserNotificationConnection {
	"""
	A page of notifications.
	"""
	edges: [UserNotificationEdge]!
	"""
	Details of the page.
	"""
	pageInfo: PageInfo!
	"""
	Total number of notifications in the entire set.
	"""
	total: Int!
}

"""
Item in a page of notifications.
"""
type UserNotificationEdge {
	"""
	Cursor ID.
	"""
	cursor: ID!
	"""
	A notification node.
	"""
	node: UserNotification!
	"""
	Position of this item in this context.
	"""
	position: Int!
}

enum UserNotificationsSortBy {
	"""
	Sort notifications based on their last updated time.
	DESC: Latest dates to earliest dates.
	"""
	LAST_UPDATED
}

input UserNotificationsSortOrder {
	by: UserNotificationsSortBy!
	order: SortOrder!
}

"""
Represents the current state of a user's notification topic preference
"""
type UserNotificationTopic {
	"""
	The user's channel settings for the topic
	"""
	channels: [NotificationChannelState]!
	"""
	The notification topic
	"""
	topic: NotificationTopic!
}

input UserPreferredStreamingProvidersInput {
	"""
	We can know whether particular streaming provider exists in the users list
	"""
	includeFilter: [ID]
}

type UserPreferredStreamingProvidersOutput {
	streamingProviders: WatchProviderConnection!
	"""
	Returns the total number of streaming providers stored
	"""
	total: Int!
}

"""
User type
Extends external type.
"""
type UserProfile {
	"""
	Returns publicly visible attributes for this user profile.  Also returns attributes for authenticated users with
	graphReadableByOwner permissions.
	Successfully retrieved attributes are in the attributes list.
	Failed attributes are in the errors list.

	---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	attributes(names: [String]!): AttributesOutput
	"""
	The user's profile biography

	---------------------
	Graphlet Details:
	- Name: userProfile
	- CTI: IMDb/GraphQL/UserProfile
	- Affected by headers: x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	bio: UserProfileBio
	"""
	The date that this IMDb User Profile was created.
	There are some active users whose accounts do not have a creation date. Field will return null response for those cases.

	---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	"""
	creationDate: DateTime
	"""
	@experimental This user's leaderboard stats for the given game. Null if the user has never earned points in it.

	---------------------
	Graphlet Details:
	- Name: leaderboard
	- CTI: IMDb/GraphQL/Leaderboard
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-engagement-growth-public
	"""
	experimental_gameLeaderboardStats(
		input: Experimental_GameLeaderboardStatsInput!
	): Experimental_GameLeaderboardUserStats
	"""
	The user's profile primary image

	---------------------
	Graphlet Details:
	- Name: userProfile
	- CTI: IMDb/GraphQL/UserProfile
	- Affected by headers: x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	primaryImage: UserProfileImage
	"""
	The profileId of this profile.

	---------------------
	Graphlet Details:
	- Name: discussion
	- CTI: IMDb/GraphQL/Discussion
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	### ALTERNATE GRAPHLET---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	### ALTERNATE GRAPHLET---------------------
	Graphlet Details:
	- Name: leaderboard
	- CTI: IMDb/GraphQL/Leaderboard
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-engagement-growth-public
	"""
	profileId: ID
	"""
	Retrieve user's rating aggregations.
	Returns list of Aggregations in order of requested aggregate types.
	Returns empty list if User has not rated any title.
	- Throws FORBIDDEN for non-authenticated requests or if requesting user is not authorized to retrieve the rating.

	---------------------
	Graphlet Details:
	- Name: userProfile
	- CTI: IMDb/GraphQL/UserProfile
	- Affected by headers: x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	ratingsAggregation(
		"""
		List of User Ratings Aggregate Types to retrieve
		"""
		inputs: [UserRatingsAggregateInput]!
	): [UserRatingsAggregation]
	"""
	---------------------
	Graphlet Details:
	- Name: contributionSDUI
	- CTI: IMDb/GraphQL/ContributionSDUI
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-sdui
	### ALTERNATE GRAPHLET---------------------
	Graphlet Details:
	- Name: contributor
	- CTI: IMDb/GraphQL/Contributor
	- Affected by headers: x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-contributors-tech
	### ALTERNATE GRAPHLET---------------------
	Graphlet Details:
	- Name: identity
	- CTI: IMDb/GraphQL/Identity
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-auth-cookie, x-imdb-client-ip, x-imdb-client-name, x-imdb-detected-country, x-imdb-detected-country-region, x-imdb-linking-cookie, x-imdb-user-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-trust
	### ALTERNATE GRAPHLET---------------------
	Graphlet Details:
	- Name: list
	- CTI: IMDb/GraphQL/List
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-disco
	### ALTERNATE GRAPHLET---------------------
	Graphlet Details:
	- Name: polls
	- CTI: IMDb/GraphQL/Polls
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language, x-amzn-sessionid
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	### ALTERNATE GRAPHLET---------------------
	Graphlet Details:
	- Name: ratings
	- CTI: IMDb/GraphQL/Ratings Reviews
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	### ALTERNATE GRAPHLET---------------------
	Graphlet Details:
	- Name: userProfile
	- CTI: IMDb/GraphQL/UserProfile
	- Affected by headers: x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	userId: ID!
	"""
	The user's profile username aka nickname

	---------------------
	Graphlet Details:
	- Name: userProfile
	- CTI: IMDb/GraphQL/UserProfile
	- Affected by headers: x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	username: UserProfileUsername
}

type UserProfileBio {
	"""
	The status of any PENDING or REJECTED updates to a user's profile bio when a user is viewing their own profile.
	This will return null if a user is not logged in OR if a logged in user is viewing someone else's profile.
	"""
	status: UserProfileBioUpdateStatus
	"""
	When a user is viewing someone else's profile this will return the live published profile bio aka the most recently
	ACCEPTED update.

	When a user is logged in AND viewing their own profile AND they have a PENDING update this will return the pending
	profile bio, otherwise this will return the live published profile bio.
	"""
	text: Markdown
}

type UserProfileBioUpdateStatus {
	"""
	A UTC timestamp of when the user profile bio update status was most recently updated
	"""
	lastUpdated: DateTime
	"""
	The user profile bio submitted for update.

	This field will always be returned as part of the mutation response as well as when the updateStatus is PENDING.
	"""
	modifiedItem: Markdown
	"""
	Feedback regarding the user profile bio update
	"""
	updateFeedback: [UpdateUserProfileFeedback]
	"""
	The status of the user profile bio update.
	"""
	updateStatus: UserProfileUpdateStatus!
}

type UserProfileImage {
	"""
	When a user is viewing someone else's profile this will return the live published profile image aka the most
	recently ACCEPTED update.

	When a user is logged in AND viewing their own profile AND they have a PENDING update this will return the pending
	profile image, otherwise this will return the live published profile image.

	---------------------
	Graphlet Details:
	- Name: userProfile
	- CTI: IMDb/GraphQL/UserProfile
	- Affected by headers: x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	image: Image
	"""
	The status of any PENDING or REJECTED updates to a user's profile image when a user is viewing their own profile.
	This will return null if a user is not logged in OR if a logged in user is viewing someone else's profile.
	"""
	status: UserProfileImageUpdateStatus
}

type UserProfileImageUpdateStatus {
	"""
	A UTC timestamp of when the user profile image update status was most recently updated
	"""
	lastUpdated: DateTime
	"""
	The user profile image submitted for update.

	This field will only be returned as part of the mutation response when the updateStatus is ACCEPTED or PENDING.
	When the updateStatus is REJECTED, modifiedItem will be null

	---------------------
	Graphlet Details:
	- Name: userProfile
	- CTI: IMDb/GraphQL/UserProfile
	- Affected by headers: x-imdb-client-name, x-imdb-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	modifiedItem: Image
	"""
	Feedback regarding the user profile image update
	"""
	updateFeedback: [UpdateUserProfileFeedback]
	"""
	The status of the user profile image update
	"""
	updateStatus: UserProfileUpdateStatus!
}

input UserProfileInput {
	"""
	Optional profileId input to fetch a user's profile. Defaults to the current authenticated user when omitted.
	"""
	profileId: ID
	"""
	@deprecated - userId input is no longer supported for non-entity queries.
	Passing a non-null userId will result in a FORBIDDEN error.
	This field is retained in the schema for backwards compatibility with old app versions
	that include it in their query definitions but always pass null.
	Use profileId instead.
	"""
	userId: ID
}

"""
Possible statuses for a UserProfile update
"""
enum UserProfileUpdateStatus {
	ACCEPTED
	PENDING
	REJECTED
}

type UserProfileUsername {
	"""
	The status of any PENDING or REJECTED updates to a user's profile bio when a user is viewing their own profile.
	This will return null if a user is not logged in OR if a logged in user is viewing someone else's profile.
	"""
	status: UserProfileUsernameUpdateStatus
	"""
	When a user is viewing someone else's profile this will return the live published username aka the most recently
	ACCEPTED update.

	When a user is logged in AND viewing their own profile AND they have a PENDING update this will return the pending
	username, otherwise this will return the live published username.

	Null is returned when a user has no username.
	"""
	text: String
}

type UserProfileUsernameUpdateStatus {
	"""
	A UTC timestamp of when the user profile username update status was most recently updated
	"""
	lastUpdated: DateTime
	"""
	The user profile username submitted for update.

	This field will always be returned as part of the mutation response as well as when the updateStatus is PENDING.
	"""
	modifiedItem: String
	"""
	Feedback regarding the user profile username update
	"""
	updateFeedback: [UpdateUserProfileFeedback]
	"""
	The status of the user profile username update.
	"""
	updateStatus: UserProfileUpdateStatus!
}

type UserRatingsAggregate {
	id: ID!
	language: DisplayableLanguage!
	"""
	Displayable Aggregate Type Name
	"""
	text: String!
	"""
	Enum corresponding to input type requested.
	"""
	type: UserRatingsAggregateType!
}

input UserRatingsAggregateInput {
	"""
	Parameter to limit the number of top buckets to fetch from GraphQL for each for the aggregations.
	Maximum value for first is 50.
	"""
	first: Int!
	"""
	Sort buckets by. Defaults to order by COUNT DESC
	"""
	sort: UserRatingsAggregateSort = { by: COUNT, order: DESC }
	"""
	Requested User Ratings Aggregate Type
	"""
	type: UserRatingsAggregateType!
}

"""
Sort input for User Ratings Aggregate buckets
"""
input UserRatingsAggregateSort {
	by: UserRatingsAggregateSortBy!
	order: SortOrder!
}

"""
Enum for User Ratings aggregates bucket sort options.
"""
enum UserRatingsAggregateSortBy {
	"""
	Sort buckets by average rating for the titles in the aggregation group.
	DESC: Higher average means User has rated titles in this bucker higher on average.
	"""
	AVG_RATING
	"""
	Sort buckets by count of titles in the aggregate group.
	DESC: Higher count means User has rated higher titles for this bucket.
	"""
	COUNT
}

"""
Input Aggregate Types for UserRatings Aggregate.
Aggregate types may be added over time to expand capability. Aggregate types once added will not be removed.
"""
enum UserRatingsAggregateType {
	"""
	Aggregate by Interests of the titles rated
	"""
	INTERESTS
	"""
	Aggregate by ratings value
	"""
	RATINGS_VALUE
	"""
	Aggregate by Release Year of the titles rated
	"""
	RELEASE_YEAR
}

"""
User Ratings Aggregations response
"""
type UserRatingsAggregation {
	"""
	Total number of buckets for this aggregation.
	"""
	bucketCount: Int!
	"""
	List of aggregation buckets.
	Use case does not require pagination of aggregation buckets in response beyond
	limiting the number of top buckets to fetch.
	"""
	buckets: [UserRatingsBucket]!
	"""
	Time of last update of aggregation data.
	Returns null if there are no items for this aggregation.
	"""
	lastUpdated: DateTime
	type: UserRatingsAggregate!
}

"""
Represents a specific bucket.
"""
type UserRatingsBucket {
	"""
	Average rating for the titles in the aggregation group.
	Returns null in case average is not computable/displayable for certain aggregation types.
	"""
	averageRating: Float
	"""
	Count of titles in the aggregate group
	"""
	count: Int!
	"""
	Destination Link for redirection.
	"""
	destinationLink: UserRatingsBucketLink!
	text: UserRatingsBucketText!
}

"""
Contains Redirection Url on click
Is a type for extendability.
"""
type UserRatingsBucketLink {
	"""
	Metadata for app-specific routing.
	"""
	metadata: UserRatingsBucketMetadata!
	url: URL!
}

"""
Bucket metadata for app-specific routing and filtering.
"""
type UserRatingsBucketMetadata {
	"""
	Identifier for the bucket.
	Format varies by bucket type:
	- INTERESTS: inconst (e.g., "in0000076")
	- RATINGS_VALUE: numeric string (e.g., "1")
	- RELEASE_YEAR: year string (e.g., "1977")
	"""
	id: String!
}

type UserRatingsBucketText {
	"""
	Bucket Id. Example, 'Comedy' for Genre, 'in0000001' for Interests
	"""
	id: ID!
	language: DisplayableLanguage!
	"""
	Displayable Bucket header
	"""
	text: String!
}

input UserRatingsSearchConstraint {
	"""
	Match titles based on their weighted IMDb star rating
	"""
	aggregateRatingRange: FloatRangeInput
	"""
	Match titles based on the number of user ratings they have received
	"""
	ratingsCountRange: IntRangeInput
}

"""
Defines a reaction made by a user
"""
type UserReaction {
	"""
	The entity id that the user reacted upon, e.g. a viconst
	"""
	entityId: ID!
	"""
	Timestamp of when the user last performed this reaction on an entity
	"""
	lastUpdated: DateTime!
	"""
	The user's reaction
	"""
	reaction: Reaction!
}

input UserReviewsInput {
	"""
	Optional Reviews filter
	"""
	filter: ReviewsFilter
	"""
	Defaults to most recent sort (sortBy: SUBMISSION_DATE sortOrder: DESC)
	"""
	sort: UserReviewsSort
	"""
	Optional userId input to fetch a user's reviews.
	Fall back to the requesting customerId (x-imdb-customer-id header) if input is missing.
	Throws BAD_USER_INPUT if userId is invalid.
	Throws UNAUTHENTICATED if both requesting customerId header and userId input are missing.
	"""
	userId: ID
}

input UserReviewsSort {
	by: UserReviewsSortBy!
	order: SortOrder!
}

enum UserReviewsSortBy {
	"""
	Title text
	"""
	ALPHABETICAL_TITLE
	"""
	Helpfulness score of review based on up/down votes
	"""
	HELPFULNESS_SCORE
	"""
	Date of review submission
	"""
	SUBMISSION_DATE
	"""
	Total vote count
	"""
	TOTAL_VOTES
	"""
	Author's rating on review
	"""
	USER_RATING
}

"""
The role of of a user modifying the known for for consumption by IMDb admins
"""
enum UserRole {
	"""
	A staff administrator role
	"""
	ADMIN
	"""
	A customer role
	"""
	CUSTOMER
}

input UserWatchedTitlesInput {
	"""
	Default to sort by first watched date in descending order.
	"""
	sort: UserWatchedTitlesSort
}

input UserWatchedTitlesSort {
	by: WatchedTitlesSortBy!
	order: SortOrder!
}

type ValidationFeedback {
	"""
	Localized message of validation feedback, providing further detail for the user
	"""
	message: LocalizedMarkdown
	"""
	Status of the validation feedback
	"""
	status: ValidationFeedbackStatus!
	"""
	Localized displayable title for validation feedback.
	Null is returned when there is no detailed message.
	"""
	title: LocalizedMarkdown!
}

"""
Represents the localized feedback content to be displayed to the customer within a given feedback dialog/alert
"""
type ValidationFeedbackContent {
	"""
	An optional help link, to provide additional guidance to customers on how to deal with this specific feedback.
	Not all validation messages will have a dedicated help link available.
	"""
	helpLink: HelpLink
	"""
	Localized message of validation feedback, providing further detail for the user.
	Null is returned when there is no detailed message.
	"""
	message: LocalizedMarkdown
	"""
	Localized displayable title for validation feedback.
	"""
	title: LocalizedMarkdown!
}

"""
Represents multiple validation feedback messages of the same status/severity level.
Enables clients to more easily aggregate/group a list of related messages under the same message dialog/alert.
"""
type ValidationFeedbackGroup {
	"""
	Bounded, ordered list of feedback messages sharing the same status, to show the customer.
	Should never be more than a few feedback messages.
	"""
	feedbackItems: [ValidationFeedbackContent]!
	"""
	Optional display hint controlling whether a feedback icon should be shown.
	When true, clients should display the default icon for the given status.
	When false, clients should hide the icon.
	Null when the resolver has no opinion on icon visibility; clients should default to showing the icon.
	"""
	showIcon: Boolean
	"""
	The common status for all feedback messages in this group
	"""
	status: ValidationFeedbackStatus!
}

"""
Possible statuses for returning validation feedback to a user. These may be mapped to visual icons displayed to a user
to help describe the feedback.
"""
enum ValidationFeedbackStatus {
	"""
	Indicates a validation error that blocks the user from proceeding.
	"""
	ERROR
	"""
	Indicates an informational message with no action required from the user.
	"""
	INFORMATION
	"""
	Indicates a neutral feedback state with no positive or negative connotation.
	"""
	NEUTRAL
	"""
	Indicates a successful validation or action completion.
	"""
	SUCCESS
	"""
	Indicates a warning that the user should be aware of but does not block progress.
	"""
	WARNING
}

type VanityUrl {
	label: String
	"""
	The name page associated with this vanity URL
	"""
	name: Name!
	"""
	The full vanity URL, e.g. https://imdb.me/abc
	"""
	url: String!
	"""
	The path of the vanity URL excluding the leading slash.
	For example, if full vanity URL is 'https://imdb.me/abc', then 'urlPath' will be 'abc'.
	"""
	urlPath: String!
}

"""
Video type
Extends external type.
"""
type Video {
	"""
	Fetches the ad URL for a video ad on app clients

	---------------------
	Graphlet Details:
	- Name: advertising
	- CTI: IMDb/GraphQL/Advertising
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-adsystem-overrides, x-imdb-client-ip, x-imdb-client-name, x-imdb-client-version, x-imdb-consent-info, x-imdb-detected-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-adtech-public
	"""
	appAdURL(adParameters: AdParametersApp!): URL
	"""
	Query used to fetch the ad request information for a video ad on app clients.
	Returns app video ad request for the given ad parameters when ad request is available.
	Returns null when no ad request is available for the given parameters,
	in which case the client should handle gracefully by not rendering video ads.
	Throws SERVICE_UNAVAILABLE_ERROR when experiencing internal server issues
	(e.g., unexpected system errors during ad request construction).
	This is a server error eligible for client retry with exponential backoff.

	---------------------
	Graphlet Details:
	- Name: advertising
	- CTI: IMDb/GraphQL/Advertising
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-adsystem-overrides, x-imdb-client-ip, x-imdb-client-name, x-imdb-client-version, x-imdb-consent-info, x-imdb-detected-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-adtech-public
	"""
	appVideoAdRequest(adParameters: VideoAdParametersApp!): VideoAdRequest
	"""
	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	contentType: VideoContentType
	"""
	The date the video was uploaded. Not all videos may have the createdDate datum.

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	createdDate: DateTime
	"""
	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	description: LocalizedString
	id: ID!
	"""
	Whether this video contains content inappropriate for more sensitive viewers

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	isMature: Boolean!
	"""
	Returns whether this video is the user-selected primary video for the given Name.

	This only reflects a manual primary video selection made by the owner of the
	Name identified by `nameId`. It does not consider automatically selected
	primary videos.

	Throws GRAPHQL_VALIDATION_FAILED if the provide name ID is invalid.
	Throws UNAUTHENTICATED if not logged in
	Throws FORBIDDEN if the requesting customer does not have the required entitlement.

	@entitlement(PREMIUM_NAME_OWNER)

	---------------------
	Graphlet Details:
	- Name: managedName
	- CTI: IMDb/GraphQL/Managed Name
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-pro-exp-office-hours
	"""
	isUserSelectedPrimaryForName(nameId: ID!): Boolean
	"""
	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	name: LocalizedString!
	"""
	Personalized suggested videos for a given videoId. Unique per user. Limit of 50.

	---------------------
	Graphlet Details:
	- Name: personalization
	- CTI: IMDb/GraphQL/Personalized Picks
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-client-name, x-imdb-consent-info, x-imdb-detected-country, x-imdb-device-age-range, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: 0 seconds
	- Slack Channel: #imdb-pwr-public
	"""
	personalizedSuggestedVideos(
		after: ID
		first: Int!
	): PersonalizedSuggestedVideosConnection
	"""
	The URLs that can be used by video players to playback the content of the video.
	This is a very finite set of known encodings thus there is no pagination available
	to the caller, but if the caller only supports a particular mime type(s) or
	resolution(s) it can filter to those criteria.

	---------------------
	Graphlet Details:
	- Name: videoPlayback
	- CTI: IMDb/GraphQL/Video Playback
	- Affected by headers: x-imdb-detected-country
	- Cache TTL: 900 seconds
	- Slack Channel: #imdb-community-and-video
	"""
	playbackURLs(filter: VideoContentFilter): [PlaybackURL]!
	"""
	The URLs containing the preview manifest of the video.
	Currently the list will only have HLS preview manifest.

	---------------------
	Graphlet Details:
	- Name: videoPlayback
	- CTI: IMDb/GraphQL/Video Playback
	- Affected by headers: x-imdb-detected-country
	- Cache TTL: 900 seconds
	- Slack Channel: #imdb-community-and-video
	"""
	previewURLs: [PlaybackURL]!
	"""
	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	primaryTitle: Title
	"""
	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	providerType: VideoProviderType!
	"""
	Cacheable
	Sorted by aggregate count, descending
	Vends the list of the current reactions, and their associated counts, for this entity

	---------------------
	Graphlet Details:
	- Name: reactions
	- CTI: IMDb/GraphQL/Reactions
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-video
	"""
	reactionsSummary: ReactionsSummary
	"""
	Returns a recommended VideoTimedTextTrack for a given videoId. To be used in situations clients want to display a
	VideoTimedTextTrack but a user hasn't made an explicit language selection (ex. auto-play). The recommended
	VideoTimedTextTrack is determined using the header x-imdb-user-language and criteria defined by business.

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	recommendedTimedTextTrack(
		filter: RecommendedVideoTimedTextTrackFilter
	): VideoTimedTextTrack
	"""
	Names related to the given video. Results capped to first 100

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	relatedNames(first: Int!): VideoNameRelationConnection
	"""
	Titles related to the given video. Results capped to first 100

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	relatedTitles(first: Int!): VideoTitleRelationConnection
	"""
	Videos related to the current video. The results will be capped to 100.

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	relatedVideos(first: Int!): VideoConnection
	"""
	Runtime is required for vi consts but not for vc consts

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	runtime: VideoRuntime
	"""
	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	thumbnail: Thumbnail!
	"""
	Returns a list of available VideoTimedTextTracks for a given videoId, sorted alphabetically.

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	timedTextTracks(filter: VideoTimedTextTracksFilter): [VideoTimedTextTrack]
	"""
	Not cacheable
	Sorted by most recently reacted
	Vends the list of reactions the user has already provided
	Requires the x-imdb-customer-id header from a logged-in user

	---------------------
	Graphlet Details:
	- Name: reactions
	- CTI: IMDb/GraphQL/Reactions
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-imdb-detected-country, x-imdb-user-country, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-video
	"""
	userReactions: [UserReaction]
	"""
	Dimension sizing information for this video

	---------------------
	Graphlet Details:
	- Name: video
	- CTI: IMDb/GraphQL/Video
	- Affected by headers: x-imdb-detected-country, x-imdb-adsystem-overrides, x-imdb-user-language, x-imdb-client-name
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-community-and-video
	"""
	videoDimensions: VideoDimensions
	"""
	Fetches the ad URL for a video ad on web (desktop and mobile web) clients

	---------------------
	Graphlet Details:
	- Name: advertising
	- CTI: IMDb/GraphQL/Advertising
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-adsystem-overrides, x-imdb-client-ip, x-imdb-client-name, x-imdb-client-version, x-imdb-consent-info, x-imdb-detected-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-adtech-public
	"""
	webAdURL(adParameters: AdParametersWeb!): URL
	"""
	Query used to fetch the ad request information for a video ad on web (desktop and mobile web) clients.
	Returns web video ad request for the given ad parameters when ad request is available.
	Returns null when no ad request is available for the given parameters,
	in which case the client should handle gracefully by not rendering video ads.
	Throws SERVICE_UNAVAILABLE_ERROR when experiencing internal server issues
	(e.g., unexpected system errors during ad request construction).
	This is a server error eligible for client retry with exponential backoff.

	---------------------
	Graphlet Details:
	- Name: advertising
	- CTI: IMDb/GraphQL/Advertising
	- Affected by headers: x-amzn-transitive-authentication-token, x-imdb-customer-id, x-amzn-customer-id, x-auth-session-customer-id, x-amzn-sessionid, x-imdb-adsystem-overrides, x-imdb-client-ip, x-imdb-client-name, x-imdb-client-version, x-imdb-consent-info, x-imdb-detected-country, x-imdb-user-language, user-agent
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-adtech-public
	"""
	webVideoAdRequest(adParameters: VideoAdParametersWeb!): VideoAdRequest
}

"""
Parameters used to construct an ad feedback URL for a video ad
"""
input VideoAdFeedbackUrlInput {
	"""
	The video ad in XML format for which an ad feedback URL is requested.
	"""
	videoAdXml: String!
}

"""
Required parameters to fetch ad requests for a video ad on app clients.
"""
input VideoAdParametersApp {
	"""
	Common ad parameters for video ad
	"""
	adParameters: AdParameters!
	"""
	Device-specific parameters for ad fetching on app clients
	"""
	deviceParameters: DeviceParameters!
}

"""
Required parameters to fetch ad requests for a video ad on web clients.
"""
input VideoAdParametersWeb {
	"""
	Common ad parameters for video ad
	"""
	adParameters: AdParameters!
}

"""
Output containing PAS V2 POST request information for video ads
"""
type VideoAdRequest {
	"""
	Stringified JSON payload for the PAS V2 POST request body. Always non-null as it's constructed by AdTech.
	"""
	requestBody: String!
	"""
	PAS V2 API endpoint URL for the POST request. Always non-null as it's a constant regional endpoint.
	"""
	requestUrl: URL!
}

enum VideoAppearance {
	HORIZONTAL
	SQUARE
	VERTICAL
}

type VideoConnection {
	"""
	List of videos
	"""
	edges: [VideoEdge]!
	"""
	Indicates the possible video facets available for filtering on.
	"""
	facets: VideoFacets
	"""
	Additional information useful for pagination
	"""
	pageInfo: PageInfo!
	"""
	Total number of videos that fit the selection criteria.
	"""
	total: Int!
}

"""
Allows for the caller to specify the mime types and/or definitions that should be included
"""
input VideoContentFilter {
	"""
	The list of definitions to include URLs for. Note that M3U8 does not support any definitions.
	"""
	definitions: [VideoDefinition]
	"""
	The list of mime types to include URLs for. Note that M3U8 does not support any definitions.
	"""
	mimeTypes: [VideoMimeType]
}

type VideoContentType {
	"""
	A localized version of the content type for display purposes
	"""
	displayName: LocalizedString!
	"""
	The ID of the content type. This should not be hard-coded, and should always be fetched at runtime.
	These IDs may change over time
	"""
	id: ID!
}

enum VideoContentTypeId {
	CLIP
	DEMO_REEL
	FEATURETTE
	FEATURE_FILM
	FILM_SHORT
	INTERVIEW
	MUSIC_VIDEO
	NEWS
	OTHER
	PROMOTIONAL
	REVIEW
	TRAILER
	TV_MINISODE
	TV_PROGRAM
	WEB_CLIP
}

"""
IMDb supports a number of resolutions for the video/mp4 and video/webm mime types.
"""
enum VideoDefinition {
	DEF_240p
	DEF_360p
	DEF_480p
	DEF_720p
	DEF_1080p
	DEF_AUTO
	DEF_SD
}

type VideoDimensions {
	"""
	The bucketed classification of the video's aspect ratio, either VERTICAL HORIZONTAL or SQUARE
	This value is primarily meant for logging metadata, and as such these buckets are subject to change
	and should not be assumed to apply to any particular aspect ratio
	"""
	appearance: VideoAppearance!
	"""
	The aspect ratio of the video expressed as a value X where X:1 is the aspect ratio
	"""
	aspectRatio: Float!
	"""
	The height of the source uploaded video file in pixels. Note that individual encodings may have smaller values due to scaling
	"""
	height: Int!
	"""
	The width of the source uploaded video file in pixels. Note that individual encodings may have smaller values due to scaling
	"""
	width: Int!
}

type VideoEdge {
	cursor: ID!
	node: Video!
	position: Int!
}

"""
Response facets are impacted by the VideosQueryFilter. All types, nameConstraints, and titleConstraints filter options
will be included in the facet results, even if just set to a count of 0
"""
type VideoFacets {
	"""
	Video names with counts of how many videos are of that name, sorted by their counts in descending order
	"""
	names: [VideoNameFacet]
	"""
	Video titles with counts of how many videos are of that title, sorted by their counts in descending order
	"""
	titles: [VideoTitleFacet]
	"""
	Video types with counts of how many videos are of that type, sorted by their counts in descending order
	"""
	types: [VideoTypeFacet]
}

type VideoMedia {
	"""
	IMDb viconst
	"""
	id: ID!
	name: LocalizedString!
	primaryImage: MediaServiceImage
	runtime: VideoRuntime
}

"""
MIME is an internet standard that explains the kind of file, in this case we support
the following video types.

https://en.wikipedia.org/wiki/MIME
"""
enum VideoMimeType {
	"""
	A computer file format for a multimedia playlist. One common use of the M3U file format is
	creating a single-entry playlist file pointing to a stream on the Internet - application/x-mpegurl
	https://en.wikipedia.org/wiki/M3U
	"""
	M3U8
	"""
	The ISO standard video file format based on Quicktime - video/mp4
	https://www.iana.org/assignments/media-types/video/mp4
	"""
	MP4
	"""
	A video file format designed as a royalty-free alternative to use in HTML5 video - video/webm
	https://en.wikipedia.org/wiki/WebM
	"""
	WEBM
}

"""
Provides information on how many videos in a gallery match the specified video name
"""
type VideoNameFacet {
	name: Name!
	total: Int!
}

input VideoNameFilterConstraints {
	"""
	Match videos related to all of the requested names
	"""
	allNameIds: [ID]
}

type VideoNameRelationConnection {
	"""
	List of name relations
	"""
	edges: [VideoNameRelationEdge]!
	"""
	Additional information useful for pagination
	"""
	pageInfo: PageInfo!
	"""
	Total number of names related to this video
	"""
	total: Int!
}

type VideoNameRelationEdge {
	cursor: ID!
	node: Name!
	position: Int!
}

type VideoProviderType {
	"""
	The ID representing the Video source provider type. eg: 'amzn1.imdb.video.providertype.staff'
	"""
	id: ID!
}

"""
Clients are to request the corresponding ad data. See VideoRecommendationsAdType documentation for corresponding queries.
Any other metadata for ads in this context goes here
"""
type VideoRecommendationsAdItem {
	adType: VideoRecommendationsAdType!
	transitionDelay: Float
}

"""
An enum representing which ad types are supported in Query.videoRecommendations

DISPLAY_AD indicates a call to
 - Query.displayAdForApp, for apps
"""
enum VideoRecommendationsAdType {
	DISPLAY_AD
}

type VideoRecommendationsConnection {
	edges: [VideoRecommendationsItemsEdge]!
	pageInfo: PageInfo!
}

input VideoRecommendationsContext {
	"""
	Specifies how the videos will be displayed to customers
	"""
	displayType: VideoRecommendationsDisplayType!
	"""
	A list of the different VideoRecommendationsAdType(s) which are supported by clients
	Only VideoRecommendationsAdType(s) specified in supportedAdTypes will be returned
	If supportedAdTypes is empty, there will be no ads returned
	"""
	supportedAdTypes: [VideoRecommendationsAdType]!
}

"""
Different options for how the videos will be displayed to customers
"""
enum VideoRecommendationsDisplayType {
	"""
	PORTRAIT_ORIENTATION indicates the videos will all be shown in a portrait video player
	"""
	PORTRAIT_ORIENTATION
}

union VideoRecommendationsItem =
	| VideoRecommendationsAdItem
	| VideoRecommendationsVideoItem

type VideoRecommendationsItemsEdge {
	cursor: ID!
	node: VideoRecommendationsItem!
}

"""
The video to be displayed to customers
Any other metadata for the video in this context goes here

'video' will be null when we try to return a video that is unavailable (e.g. availability block, geo-block)
"""
type VideoRecommendationsVideoItem {
	video: Video
}

"""
Types
"""
type VideoRuntime {
	unit: TimeUnit!
	value: Int!
}

input VideoSort {
	by: VideoSortBy!
	order: SortOrder!
}

enum VideoSortBy {
	DATE
	DURATION
}

input VideosQueryFilter {
	"""
	Match videos based on their maturity classification.
	Affects all values, including actual videos returned and total videos count.
	"""
	maturityLevel: MaturityLevel
	"""
	Return videos meeting the specified names
	"""
	nameConstraints: VideoNameFilterConstraints
	"""
	Return videos meeting the specified titles
	"""
	titleConstraints: VideoTitleFilterConstraints
	"""
	Return only videos which are one of these types
	"""
	types: [VideoContentTypeId]
}

"""
Represents a single VideoTimedTextTrack that can be used to display text that is synced with a video's audio during
video playback. Contains a URL, language, and a descriptive displayName. For a given video language (ex. en-US) pairing
only a single VideoTimedTextTrack will be returned, based on accessibility criteria defined by business.
"""
type VideoTimedTextTrack {
	"""
	A short description of the VideoTimedTextTrack experience suitable for displaying to the customer to set
	expectations. This may be used in the video player menu. The displayName language is the language of the
	VideoTimedTextTrack. (Ex. `English` for `en-US`, `Française` for`fr-FR`)
	"""
	displayName: LocalizedString!
	"""
	The language for a VideoTimedTextTrack
	"""
	language: Language!
	"""
	A fragment to include in ref markers that references this VideoTimedTextTrack, for example 'cc-es-SP'.
	The length of this string will not exceed 11 characters.
	"""
	refTagFragment: String!
	"""
	The type for a VideoTimedTextTrack
	"""
	type: VideoTimedTextTrackType!
	"""
	A URL that can be used in a video player to display the contents of the VideoTimedTextTrack.
	It may or may not be valid for a limited time and thus is intended for immediate use by the caller.
	"""
	url: URL!
}

"""
The VideoTimedTextTrackFormat format(s) supported by IMDb
"""
enum VideoTimedTextTrackFormat {
	"""
	SubRip Subtitle
	"""
	SRT
}

input VideoTimedTextTracksFilter {
	"""
	If no VideoTimedTextTrackFormat is provided it will default to SRT.
	"""
	format: VideoTimedTextTrackFormat
}

"""
The VideoTimedTextTrackType types supported by IMDb
"""
enum VideoTimedTextTrackType {
	CLOSED_CAPTION
	SUBTITLE
}

"""
Provides information on how many videos in a gallery match the specified video title
"""
type VideoTitleFacet {
	title: Title!
	total: Int!
}

input VideoTitleFilterConstraints {
	"""
	Match videos related to any of the requested titles
	"""
	anyTitleIds: [ID]
}

type VideoTitleRelationConnection {
	"""
	List of title relations
	"""
	edges: [VideoTitleRelationEdge]!
	"""
	Additional information useful for pagination
	"""
	pageInfo: PageInfo!
	"""
	Total number of names related to this video
	"""
	total: Int!
}

type VideoTitleRelationEdge {
	cursor: ID!
	node: Title!
	position: Int!
}

"""
Provides information on how many videos in a gallery match the specified video type
"""
type VideoTypeFacet {
	total: Int!
	"""
	Video Type information including display label
	"""
	type: VideoContentType!
}

"""
Pagination for videos by type, page info is intended for use with the related videos field
"""
type VideoTypesConnection {
	edges: [VideoEdge]!
	pageInfo: PageInfo!
	"""
	Total number of objects in the entire set
	"""
	total: Int!
}

type VideoTypeWithVideos {
	"""
	The video type common for all of the associated videos
	"""
	videoType: VideoContentType!
	"""
	The first page of videos in each category
	"""
	videos(first: Int!): VideoTypesConnection
}

"""
Viewport dimensions of the client's device in pixels.
"""
input ViewPortSize {
	"""
	The height of the viewport in pixels
	"""
	height: Int!
	"""
	The width of the viewport in pixels
	"""
	width: Int!
}

"""
Enum values for the valid visibility levels for data items
"""
enum VisibilityLevel {
	HIDDEN
	PRO_SITE_ONLY
	PUBLIC
}

"""
Voice types for talent
"""
enum VoiceType {
	ALTO
	BARITONE
	BASS
	SOPRANO
	TENOR
}

input VoiceTypesConstraint {
	"""
	A name match must have ALL of these voice types.
	No filtering is applied if omitted.
	"""
	allVoiceTypes: [VoiceType]
	"""
	A name match must have AT LEAST ONE of these voice types.
	No filtering is applied if omitted.
	"""
	anyVoiceTypes: [VoiceType]
	"""
	A name match must not have any of these voice types.
	No filtering is applied if omitted.

	If the same input is specified in the any/all and exclude constraints,
	then the results will be empty.
	"""
	excludeVoiceTypes: [VoiceType]
}

enum WatchedListClass {
	"""
	A user-created list. Requires listId to be provided.
	"""
	LIST
	"""
	The customer's watchlist.
	"""
	WATCHLIST
}

type WatchedPrivacy {
	id: ID!
	"""
	The language of the watched privacy setting.
	"""
	language: DisplayableLanguage!
	"""
	The watched privacy setting.
	"""
	setting: WatchedPrivacySetting!
	"""
	Localized string value of watched privacy setting.
	"""
	text: String!
}

enum WatchedPrivacySetting {
	"""
	Watched titles data is only visible to the customer themselves.
	"""
	PRIVATE
	"""
	Watched titles data is visible to other customers.
	"""
	PUBLIC
}

type WatchedStatus {
	"""
	The time the first watch signal was received.
	"""
	firstWatched: DateTime
	"""
	Whether this title has been watched before.
	"""
	isWatched: Boolean!
	"""
	Lists all the sources of the remaining watch signals.
	"""
	remainingWatchedSourceTypes: [TitleWatchedSourceType]
}

type WatchedTitleEdge {
	cursor: ID!
	node: TitleWatchedStatus!
}

type WatchedTitlesConnection {
	edges: [WatchedTitleEdge]!
	pageInfo: PageInfo!
	"""
	Total number of the titles this customer has watched.
	"""
	total: Int!
}

enum WatchedTitlesSortBy {
	"""
	Sort by first watched date.
	"""
	FIRST_WATCHED_DATE
}

type WatchlistStatistics {
	"""
	Displayable Count of total number of users who have added the title to their watchlist
	"""
	displayableCount: LocalizedDisplayableCount
	"""
	The total number of users who have added the title to their watchlist
	"""
	totalCount: Int
}

type WatchOption {
	"""
	The earliest non-expired availability window for this watch option.
	Every returned watch option has an availability window.
	"""
	availabilityWindow: WatchOptionAvailabilityWindow!
	"""
	Description of the watch option with more details.
	"""
	description: LocalizedString
	"""
	Returns a link that navigates either to the content or a landing page for the content.
	"""
	link(platform: PlatformLinkFormatId!): URL!
	"""
	The offer type of the watch option (e.g. subscription, rental, purchase, free)
	"""
	offerType: OfferType!
	"""
	Returns true if the watch option's provider is promoted. False or null means that it is not promoted
	Promoted provider feature is no longer supported by the Ads team - https://sim.amazon.com/issues/W2W-170. The field will always return false.
	"""
	promoted: Boolean
	"""
	Provider of the watch option
	"""
	provider: WatchProvider!
	"""
	The name of the provider for the watch option.
	"""
	providerName: LocalizedString!
	"""
	The provider suffix that can be added to the refTag attached to the link
	"""
	providerRefTagFragment: String!
	"""
	The season number associated with this watch option, applicable to TV series titles.
	Returns null if the watch option is not season-specific or not a series offer.
	"""
	seasonNumber: Int
	"""
	Description of the watch option with shortened details.
	"""
	shortDescription: LocalizedString
	"""
	The short display text for the watch option. (call to action for the button)
	This can be used for buttons where concise naming is required.
	"""
	shortTitle: LocalizedString!
	"""
	The display text for the watch option. (call to action for the button)
	This can be used for a descriptive call to action.
	"""
	title: LocalizedString!
}

"""
The availability window for a watch option, representing when the content is available from a provider.
Only the earliest non-expired window is returned.
"""
type WatchOptionAvailabilityWindow {
	"""
	The end date and time of the availability window in ISO-8601 format (UTC).
	"""
	end: DateTime!
	"""
	The start date and time of the availability window in ISO-8601 format (UTC).
	"""
	start: DateTime!
}

"""
Watch option category types.

There is no filtering support for below category types; graphlet will throw an exception.
These are present in the enum due to them being valid response watch option category types for other APIs.
Not all APIs support filtering:
    - PHYSICAL

IMDbTV is no longer a supported watch option category type. Using the enum will not have any effect.
"""
enum WatchOptionCategoryType {
	IMDB_TV
	PHYSICAL
	PODCAST
	RENT_OR_BUY
	SUBSCRIPTION
	THEATRICAL
}

"""
The filters to be used in the query.
"""
input WatchOptionQueryFilter {
	"""
	If true, returns individual watch offers per season for TV series titles,
	in addition to any series-level offer. If omitted or false, only a single
	offer per provider is returned (the series-level or lowest season offer).
	Has no effect on non-series titles.
	"""
	includeOffersBySeason: Boolean
	"""
	If true, returns watch offers whose availability window has not yet started.
	If omitted or false, returns only offers within their active availability window.
	Expired offers are never included.
	"""
	includeUpcomingOffers: Boolean
	"""
	Include watch options for provided watch option categories only.
	"""
	includeWatchOptionCategories: [WatchOptionCategoryType]
}

"""
The location of the client. A lat/long or postal code location must be given for the client input to be valid. If both
lat/long and postal code are provided we default to using lat/long.
"""
input WatchOptionsLocation {
	latLong: LatLong
	postalCodeLocation: PostalCodeLocation
}

"""
By default, watch option results are filtered to the region of the request
(determined by the x-imdb-detected-country header). Use `anyWatchRegions` to
override this with specific regions.

Gather watchProviderIds using the `watchProviders` query. Results are limited to
100 titles when filtering by non-Amazon providers.

Note: provider, mediaType, and offerType filters are evaluated independently. A title matches
if it has any watch offer satisfying each filter, but the same offer does not need to satisfy
all filters simultaneously.

Example: if you want to match titles that are available for purchase/rent on Prime Video in the US,
specify anyWatchProviderIds: ['amzn1.imdb.w2w.provider.prime_video'] AND anyWatchRegions: ['US']

Example: if you want to exclude titles that are available for physical purchase on Amazon in the UK,
specify excludeAnyWatchProviderIds: ['amzn1.imdb.w2w.provider.amazon'] AND anyWatchRegions: ['GB']
"""
input WatchOptionsSearchConstraint {
	"""
	Match titles WITH watch options by the specified providers.
	* Use query `watchProviders` to fetch a curated list of options to use for this constraint.
	"""
	anyWatchProviderIds: [String]
	"""
	Match titles that are available to watch in any of the specified regions. Regions supplied
	in this field will override values from the x-imdb-detected-country header for all
	watch option constraints.

	Country code in ISO 3166 (IMDbLocaleConfig:region_codes.json)
	Example: To limit to Watch Provider availability in United States or Great Britain: ['US', 'GB']
	"""
	anyWatchRegions: [String]
	"""
	Match titles WITHOUT watch options by the specified providers.
	* Use query `watchProviders` to fetch a curated list of options to use for this constraint.

	If the same input is specified in the any and exclude constraints,
	then the results should be empty.
	"""
	excludeWatchProviderIds: [String]
	"""
	Override the regions to exclude for the given request.
	Country code in ISO 3166 (IMDbLocaleConfig:region_codes.json)
	Example: To exclude Watch Provider availability from United States or Great Britain: ['US', 'GB']
	"""
	excludeWatchRegions: [String]
	"""
	Match titles that have the specified type of watch options.
	"""
	hasWatchOptionTypes: [SearchWatchOptionType]
}

type WatchProvider {
	"""
	Description of the watch option with more details.

	---------------------
	Graphlet Details:
	- Name: watch
	- CTI: IMDb/GraphQL/Watch
	- Affected by headers: x-imdb-client-name, x-imdb-client-version, x-imdb-detected-country, x-imdb-detected-time-zone, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	description: LocalizedString
	id: ID!
	"""
	Indicates whether the provider is a popular one in the client's region

	---------------------
	Graphlet Details:
	- Name: watch
	- CTI: IMDb/GraphQL/Watch
	- Affected by headers: x-imdb-client-name, x-imdb-client-version, x-imdb-detected-country, x-imdb-detected-time-zone, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	isPopular: Boolean!
	"""
	Indicates whether the watch provider is supported for the client's location and platform

	---------------------
	Graphlet Details:
	- Name: watch
	- CTI: IMDb/GraphQL/Watch
	- Affected by headers: x-imdb-client-name, x-imdb-client-version, x-imdb-detected-country, x-imdb-detected-time-zone, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	isSupported(platform: PlatformId!): Boolean!
	"""
	Watch provider logos.

	---------------------
	Graphlet Details:
	- Name: watch
	- CTI: IMDb/GraphQL/Watch
	- Affected by headers: x-imdb-client-name, x-imdb-client-version, x-imdb-detected-country, x-imdb-detected-time-zone, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	logos: WatchProviderLogos
	"""
	The name of this watch option provider, for example 'Prime Video'

	---------------------
	Graphlet Details:
	- Name: watch
	- CTI: IMDb/GraphQL/Watch
	- Affected by headers: x-imdb-client-name, x-imdb-client-version, x-imdb-detected-country, x-imdb-detected-time-zone, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	name: LocalizedString!
	"""
	Fragment to include in ref markers that reference this WatchProvider, for example 'netflix'

	---------------------
	Graphlet Details:
	- Name: watch
	- CTI: IMDb/GraphQL/Watch
	- Affected by headers: x-imdb-client-name, x-imdb-client-version, x-imdb-detected-country, x-imdb-detected-time-zone, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	refTagFragment: String!
	"""
	Category type which describes the watch provider so client can categorize

	---------------------
	Graphlet Details:
	- Name: watch
	- CTI: IMDb/GraphQL/Watch
	- Affected by headers: x-imdb-client-name, x-imdb-client-version, x-imdb-detected-country, x-imdb-detected-time-zone, x-imdb-user-language
	- Cache TTL: Varies. Reach out to graphlet owner
	- Slack Channel: #imdb-coverage-tech
	"""
	watchOptionCategoryType: WatchOptionCategoryType
}

type WatchProviderConnection {
	edges: [WatchProviderEdge]!
	pageInfo: PageInfo!
}

type WatchProviderEdge {
	cursor: ID!
	node: WatchProvider!
}

"""
Types
"""
type WatchProviderLogos {
	"""
	Square image. Ex. It may be used in front of providers in the WatchOptions dialog
	"""
	icon: MediaServiceImage
	"""
	Rectangular image. Ex. It may be used as a slate in the WatchOptions carousel
	"""
	slate: MediaServiceImage
}

"""
The filters to be used in the watch providers query.
"""
input WatchProvidersQueryFilter {
	"""
	Include watch options for provided watch option categories only.
	"""
	includeWatchOptionCategories: [WatchOptionCategoryType]
}

"""
AdCreativeInfo with additional web-specific fields.
This should be merged with the AdSlot type used for apps in future schema consolidation.
"""
type WebAdCreativeInfo {
	"""
	Type of ad product, provided if ad contains creative from Amazon Ad Exchange
	"""
	aaxAdType: String
	"""
	Ad product type information, provided if ad contains creative
	"""
	adProductType: AdProductType
	"""
	ID of the creative, provided if ad contains creative
	"""
	creativeId: String
	"""
	Flag that determines if the ad slot is eligible to run 3p auction
	"""
	isEligibleFor3pAd: Boolean!
	"""
	Flag that determines if the slot is eligible for automatic ad refresh
	"""
	isEligibleForRefresh: Boolean!
	"""
	Flag used to communicate if the ad returned is premium
	"""
	isPremium: Boolean!
	"""
	Height and width of the returned creative
	"""
	size: CreativeSize!
	"""
	HTML markup for the slot containing the ad creative
	"""
	slotMarkup: String!
}

"""
Output from the webAdsConfig operation containing all information needed to request ads
"""
type WebAdsConfigOutput {
	"""
	APS slot information for Amazon Publisher Services integration
	"""
	apsSlotInfoMap: [WebApsSlotInfo]!
	"""
	Flag indicating if ads slots are enabled on the page.
	This can be false if ads are blocked by targeting, not allowed on a specific page,
	or if the BigRedButton is used to disable display ads.
	"""
	areSlotsEnabled: Boolean!
	"""
	Flag indicating if the page has premium ads
	"""
	hasPremiumAd: Boolean!
	"""
	HTML markup to be included in the head of the document
	"""
	headerMarkup: String!
	"""
	Flag indicating if ad refresh is enabled
	"""
	isAdRefreshEnabled: Boolean!
	"""
	PLAID overrides for the page
	"""
	plaidOverrides: [PlaidOverride]!
	"""
	Breakpoint to ad size mappings for responsive AdSlot
	"""
	responsiveSlotSizes: [WebResponsiveSlotSize]!
	"""
	List of ad slots available on the page
	"""
	slots: [WebAdSlot]!
}

"""
Parameters used to make a display ad request for web pages.
Note: There is some ambiguity between AdParametersWeb and WebAdTargetingParameters, each of which predate
the web ads schemas, and must be extended to capture all parameters for web display ads.
In the future, inputs for ad request resolvers should be consolidated to contain one platform-specific parameters
object (e.g. app vs. web), and one that is format specific (e.g. display vs. video).
SIM for schema consolidation: https://sim.amazon.com/issues/IMDBADTECH-5463
"""
input WebAdsInput {
	"""
	A set of parameters needed from web clients to target ads.
	These parameters include page type, subpage type, and other targeting information.
	This input type is shared with the video ad for web resolver schema.
	"""
	adParameters: AdParametersWeb!
	"""
	A set of parameters used to target display ads, across all site variants
	"""
	displayAdTargetingParameters: DisplayAdTargetingParameters!
	"""
	A set of parameters specific to web ad targeting, across ad formats.
	This input type is shared with the display ad for app resolver schema.
	"""
	webAdTargetingParameters: WebAdTargetingParameters!
}

"""
AdSlot with additional web-specific fields.
This should be merged with the AdSlot type used for apps in future schema consolidation.
"""
type WebAdSlot {
	"""
	Ad feedback URL to be linked in the sponsored label of the ad.
	This will be null for ad slots not containing a creative, such as the SIS pixel or placeholders.
	"""
	adFeedbackUrl: URL
	"""
	The creative metadata and slotsMarkup returned for the given ad slot
	"""
	creativeInfo: WebAdCreativeInfo!
	"""
	The name of the ad slot
	"""
	name: String!
}

"""
Output for the webAds query
"""
type WebAdsOutput {
	"""
	Flag indicating if any premium ads are present
	"""
	hasPremiumAd: Boolean!
	"""
	Flag indicating if request was for automatic ad refresh.
	The resolver will set this as true if refreshableSlots input was populated.
	"""
	isRefreshRequest: Boolean!
	"""
	Mapping of slots with resizing re-fetch disabled to their blocked breakpoints
	"""
	responsiveResizingDisabledSlots: [WebResponsiveResizingDisabledSlot]!
	"""
	A list of ad slots and their creative info
	"""
	slots: [WebAdSlot]!
}

"""
Parameters specific to web ad targeting
"""
input WebAdTargetingParameters {
	"""
	Extended IDs (eids) from CxM identity vendors (e.g. SharedID, ID5, Lotame, LiveRamp).
	Used to pass 3P identity signals through to PAS for unified auction.
	If not provided, no 3P identity signals will be forwarded to PAS and 3P buyers will rely
	solely on their own cookie/ID matching for the impression.

	These are generated client-side via CxM identity scripts. We treat these IDs as passthrough for PAS/AAX

	Up to four different identifiers present in this field at launch, with the possibility of a fifth
	later on. There is no maximum array size

	Example:
	extendedIds: [
	  { source: "pubcid.org", userIds: [{ id: "e52a-4033-b54c-f67d", agentType: 1 }] },
	  { source: "id5-sync.com", userIds: [{ id: "ID5-ZHMO_gMpLk...", agentType: 1 }] },
	  { source: "crwdcntrl.net", userIds: [{ id: "pan-id-abc123", agentType: 1 }] },
	  { source: "liveramp.com", userIds: [{ id: "AsvkAsvQTfYZ...", agentType: 3 }] }
	]
	"""
	extendedIds: [AdExtendedId]
	"""
	True if the client is requesting content that meets the AdBlockPlus Acceptable Ads program,
	false if the request is for standard ad content.
	"""
	isAbpAcceptable: Boolean!
	"""
	Referrer URL that caused navigation to the current page (i.e. the page the user came from).
	Captured by the web client from the page-navigation request's referer header (distinct from
	the Zuko/GraphQL request's own referer) and passed through to site.ref in the OpenRTB bid
	request for third-party bidder targeting in the unified auction.
	If not provided, site.ref will not be set.
	"""
	referrer: URL
	"""
	The IMDb site variant on which the ads are intended to be shown
	"""
	site: AdSite!
	"""
	Mappings of requested ad slot names to requested ad slot sizes.
	Required for non-mobile sites, but is optional in the schema.
	The resolver is responsible for safely handling empty input.
	"""
	slotSizeMap: [SlotSize]
	"""
	URL of the page being targeted.
	If not provided, defaults to homepage URL corresponding to site field.
	"""
	url: URL
}

"""
Information about an APS slot for Amazon Publisher Services integration
"""
type WebApsSlotInfo {
	"""
	Identifiers for the ad slot
	"""
	apsSlot: ApsSlot!
	"""
	Parameters for the ad slot
	"""
	apsSlotParams: [ApsSlotParam]!
	"""
	The name of the ad slot
	"""
	slotName: String!
}

"""
Viewport breakpoint with disabled flag
"""
type WebResponsiveResizingDisabledBreakpoint {
	"""
	The width in pixels of the breakpoint
	"""
	breakpoint: Int!
	"""
	Whether responsive resizing is disabled for this breakpoint
	"""
	isDisabled: Boolean!
}

"""
Slot with responsive resizing disabled
"""
type WebResponsiveResizingDisabledSlot {
	"""
	The breakpoints for which responsive resizing is disabled
	"""
	breakpoints: [WebResponsiveResizingDisabledBreakpoint]!
	"""
	The name of the slot
	"""
	name: String!
}

"""
Mapping between slot names and their size configurations for different viewport breakpoints
"""
type WebResponsiveSlotSize {
	"""
	List of viewport breakpoints and corresponding ad sizes
	"""
	breakpoints: [WebViewportBreakpointSlotSize]!
	"""
	The name of the ad slot
	"""
	slotName: String!
}

type WebsiteLink {
	label: String
	url: String!
}

"""
Viewport breakpoint configuration for responsive ad sizing
"""
type WebViewportBreakpointSlotSize {
	"""
	The maximum window width to serve this ad size
	"""
	breakpoint: Int!
	"""
	The ad size for this breakpoint (e.g. 728x90)
	"""
	slotSize: CreativeSize!
}

type WebviewVideoPlayer {
	"""
	Audio language of the video.
	"""
	audioLanguage: Language!
	"""
	Language of the burned-in captions. Returns null if there is no burned-in captions.
	"""
	burnedInCaptionsLanguage: Language
	"""
	Description of the video.
	"""
	description: LocalizedString
	"""
	The customized webview Url.
	Client's info, which is got from the header, will be used to customize this Url
	to contain client-related params.
	"""
	webviewUrl: String!
}

enum WeightUnit {
	KILOGRAM
	POUND
}

"""
Filter of wide-released or non-wide-released release dates
"""
enum WideReleaseFilter {
	EXCLUDE_WIDE_RELEASE
	WIDE_RELEASE_ONLY
}

enum WinsFilter {
	EXCLUDE_WINS
	WINS_ONLY
}

"""
The constraints here are combined (AND).

examples:

withNameDataConstraint: {
    allDataAvailable: [TRIVIA],
    noDataAvailable: [TRIVIA]
}
will not match any names.

withNameDataConstraint: {
    allDataAvailable: [TRIVIA, BIRTH_PLACE],
    anyDataAvailable: [BIRTH_DATE, QUOTES]
    noDataAvailable: [DEATH_PLACE]
}
will match names that have (trivia data AND a birth place) AND no death place AND (either a birth date OR quotes).
"""
input WithNameDataSearchConstraint {
	"""
	Match names that have ALL of the selected data available.
	"""
	allDataAvailable: [NameDataType]
	"""
	Match names that have AT LEAST ONE of the selected data available.
	"""
	anyDataAvailable: [NameDataType]
	"""
	Match names that have NONE of the selected data available.
	"""
	noDataAvailable: [NameDataType]
}

input WithTitleDataSearchConstraint {
	"""
	Match titles that have ALL of these data available
	"""
	allDataAvailable: [TitleDataType]
	"""
	Match titles that have AT LEAST ONE of these data available
	"""
	anyDataAvailable: [TitleDataType]
	"""
	Match titles that do NOT have ALL of these data
	"""
	noDataAvailable: [TitleDataType]
}

type WorkAuthorizationCountries {
	"""
	Total number of work authorization items for the person
	"""
	total: Int!
	"""
	The work authorization items matching input arguments (e.g. 'limit')
	"""
	workAuthorizations: [WorkAuthorizationInCountry]!
}

type WorkAuthorizationInCountry {
	"""
	A country
	"""
	country: LocalizedDisplayableCountry!
	"""
	Whether authorized to work in this country
	"""
	isAuthorized: Boolean!
}

type YearDisplayableProperty {
	value: Markdown!
}

"""
A range of years
"""
type YearRange {
	"""
	The end year
	"""
	endYear: Int
	"""
	The start year
	"""
	year: Int
}

"""
Represents a range of years for filtering filmography credits.
Both fields are optional. If both are omitted, no filtering is applied.
"""
input YearRangeInput {
	"""
	The end year of the range (inclusive).
	If omitted, matches all years from startYear onwards.
	Must be non-negative. Throws GRAPHQL_VALIDATION_FAILED if value is negative.
	Throws GRAPHQL_VALIDATION_FAILED if endYear < startYear.
	"""
	endYear: Int
	"""
	The start year of the range (inclusive).
	If omitted, matches all years up to and including endYear.
	Must be non-negative. Throws GRAPHQL_VALIDATION_FAILED if value is negative.
	Throws GRAPHQL_VALIDATION_FAILED if startYear > endYear.
	"""
	startYear: Int
}
